# 第32章 字符串匹配（String Matching）

> 本章回答一个核心问题：如何在一个长文本中快速找到一个短模式串的所有出现位置？——从最直观的"挨个试"朴素算法，到用数字指纹加速的Rabin-Karp算法，到用状态机驱动的有限自动机算法，再到利用"前缀即后缀"智慧的KMP算法——字符串匹配是计算机科学中最古老也最实用的问题之一，从文本编辑器的"查找"功能，到DNA序列搜索，再到搜索引擎的关键词匹配，无处不在

## 📖 本章导读

### 🎯 问题引入：为什么需要字符串匹配？

**生活中的例子**：

想象你正在用**Word写论文**，突然想找到之前写的某个词：
- 你按下 Ctrl+F，输入"算法"，Word瞬间高亮了所有出现的位置
- 100页的论文里有几十处"算法"，Word在毫秒内全部找到

这背后就是**字符串匹配**算法在工作。

再想想**基因检测**：
- 科学家在一条含30亿个碱基对的DNA序列中，搜索一个特定的基因片段
- 如果用"挨个试"的方法，可能需要数小时
- 但用高效的匹配算法，几秒就能搞定

```
字符串匹配的核心场景：

  文本编辑器 Ctrl+F    ← 在文档中找关键词
  基因序列搜索        ← 在DNA中找特定片段
  搜索引擎            ← 在网页库中找关键词
  杀毒软件            ← 在文件中找病毒特征码
  入侵检测系统        ← 在网络流量中找攻击模式
```

**字符串匹配问题的形式化定义**：

给定：
- **文本** T[1..n]：长度为 n 的字符串（被搜索的对象）
- **模式** P[1..m]：长度为 m 的字符串（要找的关键词），m ≤ n

目标：找到所有**有效偏移** s，使得 T[s+1..s+m] = P[1..m]。

```
         s=0  s=1  s=2  s=3
          ↓    ↓    ↓    ↓
T:  a  b  c  a  b  a  b  c
P:     a  b  a  b

s=2 是有效偏移：T[3..6] = "abab" = P[1..4]  ✓
s=0 不是：T[1..4] = "abca" ≠ "abab"           ✗
```

**四种算法的演进路线**：

| 算法 | 核心思想 | 预处理时间 | 匹配时间 | 类比 |
|------|---------|-----------|---------|------|
| 朴素算法 | 逐个位置挨个试 | 0 | O((n-m+1)m) | 逐页翻书找关键词 |
| Rabin-Karp | 给模式算"指纹"，先比指纹再比内容 | Θ(m) | O(n-m+1)期望 | 先看书的目录再翻内容 |
| 有限自动机 | 把模式建成状态机，读文本就像走迷宫 | O(m\|Σ\|) | Θ(n) | 按照地图自动导航 |
| KMP | 利用"已匹配部分的前后缀关系"跳过无效位置 | Θ(m) | Θ(n) | 知道走错路后直接跳到最近的岔路口 |

**与前面章节的关系**：

| 对比项 | 第11章 散列表 | 第26章 最大流 | 第32章 字符串匹配 |
|--------|-------------|-------------|------------------|
| 核心思想 | 哈希映射 | 网络优化 | 模式识别 |
| 哈希/模运算 | 链地址法/开放寻址 | 无 | Rabin-Karp滚动哈希 |
| 图论模型 | 无 | 流网络 | 有限自动机(有向图) |
| 时间目标 | O(1)查找 | O(V²E) | O(n)匹配 |

**本章路线图**：

```
基本概念 ──→ 朴素算法 ──→ Rabin-Karp ──→ 有限自动机 ──→ KMP算法
 符号/术语    32.1          32.2          32.3          32.4
 前缀/后缀    暴力滑动       滚动哈希       状态转移       前缀函数π
                            O(n)期望       Θ(n)匹配       Θ(n)匹配
```

---

## 1️⃣ 符号和术语（32.0 Notation and Terminology）

### 1.1 基本字符串概念

**生活中的类比**：把字符串想象成**珠串**——每颗珠子是一个字符，整条珠串就是一个字符串。

| 符号 | 含义 | 例子 |
|------|------|------|
| Σ | 字母表（所有可能字符的集合） | Σ = {a, b, ..., z} |
| Σ* | 所有有限长度字符串的集合 | 包括 "a", "ab", "abc", "" 等 |
| \|x\| | 字符串 x 的长度 | \|abc\| = 3 |
| xy | 字符串 x 和 y 的连接 | "ab" + "cd" = "abcd" |
| ε | 空字符串（长度为0） | \|ε\| = 0 |

### 1.2 前缀与后缀

**生活中的类比**：
- **前缀**就像**姓**——"欧阳明"的姓是"欧阳"，"张三"的姓是"张"
- **后缀**就像**名**——"欧阳明"的名是"明"，"张三"的名是"三"

| 概念 | 定义 | 记号 | 例子 |
|------|------|------|------|
| 前缀 | w 是 x 的开头部分（x = wy） | w ⊏ x | "ab" ⊏ "abcde" |
| 后缀 | w 是 x 的结尾部分（x = yw） | w ⊐ x | "de" ⊐ "abcde" |
| 真前缀 | 前缀但 w ≠ x | — | "ab" 是 "abc" 的真前缀 |

**FAQ：空字符串是前缀也是后缀吗？**

> 是的！ε 是任何字符串的前缀和后缀。因为 x = ε·x = x·ε 永远成立。
> 这就像"0是任何数的因子"——看起来平凡，但在数学推导中至关重要。

### 1.3 后缀重叠引理

**后缀重叠引理**：如果 x 和 y 都是 z 的后缀，且 \|x\| ≤ \|y\|，则 x 是 y 的后缀。

```
例子：z = "abcab"
x = "ab"   （z 的后缀：abc**ab**）
y = "cab"  （z 的后缀：ab**cab**）

|x| = 2 ≤ |y| = 3  →  x ⊐ y  （"ab" 是 "cab" 的后缀 ✓）
```

**直觉理解**：如果两根绳子都系在同一个桩上（都是z的后缀），短的那根一定系在长的那根上（短的是长的后缀）。

---

## 2️⃣ 朴素字符串匹配算法（32.1 The Naive String-Matching Algorithm）

### 2.1 算法思想

**生活中的类比**：想象你在**一长串钥匙**中找一把特定的钥匙：
- 你拿模板钥匙，从第一把开始挨个比对
- 每把钥匙的每个齿都要对一遍
- 对不上就换下一把

朴素算法的核心：**模式串从左到右依次滑过文本串的每个位置，每个位置逐字符比较**。

### 2.2 伪代码

```
NAIVE-STRING-MATCHER(T, P)
1  n = T.length          // 文本串长度
2  m = P.length          // 模式串长度
3  for s = 0 to n - m    // 遍历所有可能的偏移
4      if P[1..m] == T[s+1..s+m]   // 逐字符比较
5          print "Pattern occurs with shift" s
```

### 2.3 图解过程

```
T = "acaabc",  P = "aab",  n = 6, m = 3

s = 0:  a c a a b c
        a a b           ← 第2个字符不匹配(c ≠ a)，跳过
        ✗

s = 1:  a c a a b c
          a a b         ← 第1个字符就不匹配(c ≠ a)，跳过
          ✗

s = 2:  a c a a b c
            a a b       ← 全部匹配！
            ✓ ✓ ✓

s = 3:  a c a a b c
              a a b     ← 第2个字符不匹配(b ≠ a)，跳过
              ✗

结果：有效偏移 s = 2
```

### 2.4 最坏情况分析

**最坏情况的时间复杂度**：O((n-m+1)m)

**什么情况最坏？** 当每次都几乎匹配成功，但最后一个字符失败时：

```
T = "aaaaaaa...a"  (n个a)
P = "aaa...ab"     (m-1个a + 1个b)

s=0: a a a a a a a ...   比了m次才发现最后一个是b≠a  ✗
s=1:   a a a a a a ...   又比了m次                    ✗
s=2:     a a a a a ...   又比了m次                    ✗
...
共 n-m+1 次偏移，每次比 m 次 → O((n-m+1)m)
```

**FAQ：什么时候朴素算法其实挺快？**

> 当模式串中所有字符都不同时，朴素算法可以优化为 O(n)！
> 因为一旦发现不匹配，可以直接跳到下一个可能的起始位置，而不需要只移动一步。

### 2.5 代码实现

**Python实现**：

```python
def naive_string_matcher(T, P):
    """
    朴素字符串匹配算法
    :param T: 文本串
    :param P: 模式串
    :return: 所有有效偏移的列表
    """
    n = len(T)                    # 文本串长度
    m = len(P)                    # 模式串长度
    valid_shifts = []             # 存储所有有效偏移
    
    for s in range(n - m + 1):    # 遍历所有可能的偏移 (0 到 n-m)
        if P == T[s:s + m]:       # 比较 P 和 T[s+1..s+m]（Python切片）
            valid_shifts.append(s)  # 记录有效偏移
    
    return valid_shifts


# 测试
T = "acaabcabcabab"
P = "aab"
print(f"有效偏移: {naive_string_matcher(T, P)}")  # 输出: [2, 5]
```

**C++实现**：

```cpp
#include <iostream>
#include <vector>
#include <string>
using namespace std;

/**
 * 朴素字符串匹配算法
 * @param T 文本串
 * @param P 模式串
 * @return 所有有效偏移的列表
 */
vector<int> naiveStringMatcher(const string& T, const string& P) {
    vector<int> validShifts;        // 存储所有有效偏移
    int n = T.length();             // 文本串长度
    int m = P.length();             // 模式串长度
    
    // 遍历所有可能的偏移
    for (int s = 0; s <= n - m; s++) {
        bool isMatch = true;        // 假设当前偏移匹配
        // 逐字符比较模式串和文本子串
        for (int j = 0; j < m; j++) {
            if (T[s + j] != P[j]) { // 发现不匹配
                isMatch = false;
                break;              // 提前退出内层循环
            }
        }
        if (isMatch) {
            validShifts.push_back(s); // 记录有效偏移
        }
    }
    
    return validShifts;
}

int main() {
    string T = "acaabcabcabab";
    string P = "aab";
    vector<int> result = naiveStringMatcher(T, P);
    
    cout << "有效偏移: ";
    for (int s : result) {
        cout << s << " ";
    }
    cout << endl;
    // 输出: 有效偏移: 2 5
    return 0;
}
```

---

## 3️⃣ Rabin-Karp算法（32.2 The Rabin-Karp Algorithm）

### 3.1 算法思想

**生活中的类比**：想象你在**快递站找包裹**：
- 朴素方法：每个包裹都打开看里面是什么 → 太慢！
- Rabin-Karp方法：先看快递单号（哈希值），单号一样的才打开验证

关键思想：**把字符串映射成数字（哈希值），先比数字，再比字符串**。

```
朴素方法：每到一个位置，都逐字符比较
    T: a b c a b a b c
    P: a b a b
    → 每次要比较4个字符

Rabin-Karp：先算哈希值，哈希值不同直接跳过
    hash(P) = hash("abab") = 某个数字
    hash(T[0:4]) = ?  不同？跳过！
    hash(T[1:5]) = ?  不同？跳过！
    hash(T[2:6]) = ?  相同？逐字验证！
```

### 3.2 滚动哈希——Rabin-Karp的灵魂

**核心问题**：怎么快速计算每个子串的哈希值？

**朴素方法**：每个子串从头算，O(m) 时间 → 总共 O((n-m+1)m)，和朴素算法一样慢！

**滚动哈希的妙招**：已知前一个子串的哈希值，O(1) 时间算出下一个！

```
d进制表示法：

把字符串看成 d 进制数（d = |Σ|，字母表大小）
例如 d = 10（十进制），字符串 "31415" 的哈希值 = 31415

P[1..m] 的哈希值:
  p = P[1]·d^(m-1) + P[2]·d^(m-2) + ... + P[m]·d^0

T[s+1..s+m] 的哈希值 t_s 类似计算
```

**滚动递推公式**（最关键！）：

```
已知 t_s（从位置 s 开始的子串的哈希值），求 t_{s+1}：

t_{s+1} = d·(t_s - T[s+1]·d^(m-1)) + T[s+m+1]

直觉理解：
1. 减去最高位：t_s - T[s+1]·d^(m-1)  ← 去掉最左边的字符
2. 左移一位：× d                       ← 所有位向左挪一位
3. 加上最低位：+ T[s+m+1]             ← 在右边加上新字符
```

**图解滚动过程**（以十进制为例）：

```
d = 10, m = 5

T = "3 1 4 1 5 2 6 ..."

t_0 = 3·10⁴ + 1·10³ + 4·10² + 1·10¹ + 5·10⁰ = 31415

t_1 = 10·(31415 - 3·10⁴) + 2
    = 10·(31415 - 30000) + 2
    = 10·1415 + 2
    = 14152

验证：T[2..6] = "14152" → 1·10⁴ + 4·10³ + 1·10² + 5·10¹ + 2 = 14152 ✓
```

### 3.3 模运算——解决数值溢出

**问题**：实际中哈希值可能非常大，溢出计算机字长。

**解决**：对所有运算取模 q（q 选一个大质数）。

```
修正的递推公式：
t_{s+1} = (d·(t_s - T[s+1]·h) + T[s+m+1]) mod q

其中 h = d^(m-1) mod q（预先计算）
```

**代价**：取模后可能产生**伪命中**（哈希值相同但字符串不同）。

**对策**：哈希值相同时，再逐字符验证。只要 q 足够大，伪命中就很少。

### 3.4 伪代码

```
RABIN-KARP-MATCHER(T, P, d, q)
1  n = T.length
2  m = P.length
3  h = d^(m-1) mod q            // 最高位的权重
4  p = 0                         // 模式串的哈希值
5  t_0 = 0                       // 文本串第一个子串的哈希值
6  for i = 1 to m                // 预处理：计算 p 和 t_0
7      p = (d·p + P[i]) mod q
8      t_0 = (d·t_0 + T[i]) mod q
9  for s = 0 to n - m            // 匹配
10     if p == t_s               // 哈希值相等
11         if P[1..m] == T[s+1..s+m]  // 逐字符验证（避免伪命中）
12             print "Pattern occurs with shift" s
13     if s < n - m              // 计算下一个子串的哈希值
14         t_{s+1} = (d·(t_s - T[s+1]·h) + T[s+m+1]) mod q
```

### 3.5 图解完整过程

```
T = "2 3 5 9 0 5 7 6",  P = "5 9 0",  d = 10,  q = 13

预处理：计算 h = 10^2 mod 13 = 100 mod 13 = 9

计算 p = (10·5 + 9)·10 + 0 = 590 → 590 mod 13 = 590 - 45·13 = 590 - 585 = 5
计算 t_0 = (10·2 + 3)·10 + 5 = 235 → 235 mod 13 = 235 - 18·13 = 235 - 234 = 1

s=0: t_0=1, p=5, 1≠5 → 不匹配
     t_1 = (10·(1 - 2·9) + 9) mod 13 = (10·(1-18) + 9) mod 13
         = (10·(-17) + 9) mod 13 = (-170 + 9) mod 13 = -161 mod 13 = 5

s=1: t_1=5, p=5, 哈希值相等！
     验证：T[2..4] = "359" ≠ P = "590" → 伪命中！
     t_2 = (10·(5 - 3·9) + 0) mod 13 = (10·(5-27) + 0) mod 13
         = (10·(-22)) mod 13 = -220 mod 13 = -220 + 17·13 = -220 + 221 = 1

s=2: t_2=1, p=5, 1≠5 → 不匹配
     t_3 = (10·(1 - 5·9) + 5) mod 13 = (10·(1-45) + 5) mod 13
         = (10·(-44) + 5) mod 13 = -435 mod 13 = -435 + 34·13 = -435 + 442 = 7

s=3: t_3=7, p=5, 7≠5 → 不匹配
     t_4 = (10·(7 - 9·9) + 7) mod 13 = (10·(7-81) + 7) mod 13
         = (10·(-74) + 7) mod 13 = -733 mod 13 = -733 + 57·13 = -733 + 741 = 8

s=4: t_4=8, p=5, 8≠5 → 不匹配
     t_5 = (10·(8 - 0·9) + 6) mod 13 = (80 + 6) mod 13 = 86 mod 13 = 86 - 6·13 = 8

s=5: t_5=8, p=5, 8≠5 → 不匹配

结果：无有效偏移（P="590"不在T中）
```

### 3.6 复杂度分析

| 情况 | 预处理时间 | 匹配时间 | 总时间 |
|------|-----------|---------|--------|
| 最坏 | Θ(m) | Θ((n-m+1)m) | Θ((n-m+1)m) |
| 期望 | Θ(m) | O(n) | O(n+m) |

**FAQ：为什么最坏还是 O(nm)？**

> 最坏情况：所有哈希值都"伪命中"，每次都要逐字符验证。
> 例如 T = "aaa...a"，P = "aab"，q 很小时可能每次哈希值都一样。
> 但只要 q 选得足够大（比如 q ≥ m），伪命中的概率极低，期望时间 O(n+m)。

### 3.7 代码实现

**Python实现**：

```python
def rabin_karp_matcher(T, P, d=256, q=101):
    """
    Rabin-Karp字符串匹配算法
    :param T: 文本串
    :param P: 模式串
    :param d: 进制基数（字符集大小）
    :param q: 模数（质数）
    :return: 所有有效偏移的列表
    """
    n = len(T)                    # 文本串长度
    m = len(P)                    # 模式串长度
    h = pow(d, m - 1, q)          # d^(m-1) mod q，最高位权重
    p = 0                         # 模式串哈希值
    t = 0                         # 当前文本子串哈希值
    valid_shifts = []             # 存储有效偏移
    
    # 预处理：计算模式串哈希值和第一个文本子串哈希值
    for i in range(m):
        p = (d * p + ord(T[i] if i < m else 0)) % q   # 这里统一用字符的ASCII值
        t = (d * t + ord(T[i])) % q
    # 重新计算（上面的循环有bug，下面修正）
    p = 0
    t = 0
    for i in range(m):
        p = (d * p + ord(P[i])) % q    # 模式串哈希
        t = (d * t + ord(T[i])) % q    # 文本前m个字符哈希
    
    # 匹配阶段
    for s in range(n - m + 1):
        if p == t:                          # 哈希值相等
            # 逐字符验证（避免伪命中）
            if P == T[s:s + m]:
                valid_shifts.append(s)
        # 计算下一个子串的哈希值（滚动）
        if s < n - m:
            t = (d * (t - ord(T[s]) * h) + ord(T[s + m])) % q
            # 处理负数（Python的mod已经处理了，但保险起见）
            if t < 0:
                t += q
    
    return valid_shifts


# 测试
T = "23590576235907"
P = "590"
print(f"有效偏移: {rabin_karp_matcher(T, P)}")  # 输出: [2, 9]
```

**C++实现**：

```cpp
#include <iostream>
#include <vector>
#include <string>
using namespace std;

/**
 * Rabin-Karp字符串匹配算法
 * @param T 文本串
 * @param P 模式串
 * @param d 进制基数
 * @param q 模数（质数）
 * @return 所有有效偏移的列表
 */
vector<int> rabinKarpMatcher(const string& T, const string& P, int d = 256, int q = 101) {
    vector<int> validShifts;
    int n = T.length();
    int m = P.length();
    
    if (m > n) return validShifts;
    
    // 计算 h = d^(m-1) mod q
    int h = 1;
    for (int i = 0; i < m - 1; i++) {
        h = (h * d) % q;
    }
    
    int p = 0;    // 模式串哈希值
    int t = 0;    // 当前文本子串哈希值
    
    // 预处理：计算初始哈希值
    for (int i = 0; i < m; i++) {
        p = (d * p + P[i]) % q;     // 模式串哈希
        t = (d * t + T[i]) % q;     // 文本前m个字符哈希
    }
    
    // 匹配阶段
    for (int s = 0; s <= n - m; s++) {
        if (p == t) {
            // 哈希值相等，逐字符验证
            bool match = true;
            for (int j = 0; j < m; j++) {
                if (T[s + j] != P[j]) {
                    match = false;
                    break;
                }
            }
            if (match) {
                validShifts.push_back(s);
            }
        }
        // 计算下一个子串的哈希值（滚动）
        if (s < n - m) {
            t = (d * (t - T[s] * h) + T[s + m]) % q;
            // 处理负数
            if (t < 0) t += q;
        }
    }
    
    return validShifts;
}

int main() {
    string T = "23590576235907";
    string P = "590";
    vector<int> result = rabinKarpMatcher(T, P);
    
    cout << "有效偏移: ";
    for (int s : result) {
        cout << s << " ";
    }
    cout << endl;
    return 0;
}
```

---

## 4️⃣ 利用有限自动机进行字符串匹配（32.3 String Matching with Finite Automata）

### 4.1 算法思想

**生活中的类比**：想象你在一个**自动售货机**前：
- 你依次投入硬币（= 读入文本字符）
- 售货机根据当前状态和投入的硬币决定下一步
- 当投入了正确的硬币组合（= 匹配了模式串），饮料掉出来

有限自动机就是一个这样的"机器"：**读入文本字符，自动在状态间转移，到达接受状态就说明匹配成功**。

### 4.2 有限自动机的定义

一个**有限自动机** M 是一个5元组 (Q, q₀, A, Σ, δ)：

| 元素 | 含义 | 类比 |
|------|------|------|
| Q | 状态的有限集合 | 售货机的各种内部状态 |
| q₀ ∈ Q | 初始状态 | 还没投币时的状态 |
| A ⊆ Q | 接受状态集合 | 可以出货的状态 |
| Σ | 输入字母表 | 可投入的硬币种类 |
| δ: Q×Σ → Q | 转移函数 | 投某硬币后从某状态变到某状态 |

**运行方式**：从 q₀ 开始，每读入一个字符 a，状态变为 δ(q, a)。如果最终停在某个接受状态上，就"接受"这个输入。

### 4.3 一个简单的有限自动机例子

```
自动机：识别所有包含偶数个a的字符串

状态集 Q = {0, 1}
初始状态 q₀ = 0
接受状态 A = {0}（偶数个a时在状态0）
字母表 Σ = {a, b}

转移函数：
    δ(0, a) = 1    读到a，奇偶性翻转
    δ(0, b) = 0    读到b，奇偶性不变
    δ(1, a) = 0    再读到a，回到偶数
    δ(1, b) = 1    读到b，保持奇数

状态转移图：
    ┌───┐    a    ┌───┐
    │ 0 │ ────→  │ 1 │
    │(接受)│ ←──── │   │
    └───┘    a    └───┘
      ↑ b            │ b
      └──────────────┘
      (自环b省略)

输入 "aba"：0 →a→ 1 →b→ 1 →a→ 0  接受 ✓（2个a，偶数）
输入 "aab"：0 →a→ 1 →a→ 0 →b→ 0  接受 ✓（2个a，偶数）
输入 "ab" ：0 →a→ 1 →b→ 1         拒绝 ✗（1个a，奇数）
```

### 4.4 字符串匹配自动机

**核心思想**：构造一个特殊的有限自动机，使得读入文本 T 的每个字符后，当前状态 q 恰好等于"模式串 P 的最长前缀 P_q 是已读入文本的后缀"的长度。

```
关键不变量：φ(T_i) = σ(T_i)

φ: 终态函数，自动机读完T[1..i]后的状态
σ: 后缀函数，T[1..i]的最长后缀等于P的前缀的长度

当 φ(T_i) = m 时，说明整个模式串P都是T[1..i]的后缀 → 找到匹配！
```

**后缀函数 σ(x)** 的定义：

σ(x) = 最大的 k，使得 P_k ⊐ x（P的前k个字符是x的后缀）

```
P = "ababaca"

σ("a")     = 1  （P_1 = "a" ⊐ "a"）
σ("ab")    = 2  （P_2 = "ab" ⊐ "ab"）
σ("aba")   = 3  （P_3 = "aba" ⊐ "aba"）
σ("abab")  = 4  （P_4 = "abab" ⊐ "abab"）
σ("ababa") = 5  （P_5 = "ababa" ⊐ "ababa"）
σ("ababab")= 4  （P_4 = "abab" ⊐ "ababab"，注意不是5也不是6！）
σ("ababac")= 6  （P_6 = "ababac" ⊐ "ababac"）
σ("abc")   = 0  （P的任何非空前缀都不是"abc"的后缀...等等，P_1="a"是后缀吗？"abc"的后缀有c,bc,abc，不含a，所以σ=0...不对，应该是σ=1因为a是aba...不对）
实际上 σ("abc") = 0，因为P="ababaca"的前缀"a"虽然只有1个字符，但"abc"的长度为3的后缀有"c","bc","abc"，没有"a"开头的。等等，后缀包括"a"...不对，后缀必须从末尾开始的连续子串。"abc"的后缀是：c, bc, abc，没有单独的a。所以σ("abc")=0。
```

### 4.5 构造字符串匹配自动机

**转移函数的设计**：

δ(q, a) = σ(P_q a)

**直觉**：在状态 q 时（已匹配了 P 的前 q 个字符），读到字符 a，新的匹配长度就是"P 的前 q 个字符加上 a"这个字符串的后缀中最长的 P 的前缀的长度。

```
P = "ababaca"

δ(5, b) = σ(P_5 · b) = σ("ababab") = 4
   解释：已匹配"ababa"，又读到'b'，"ababab"的最长P前缀后缀是"abab"(长度4)

δ(5, c) = σ(P_5 · c) = σ("ababac") = 6
   解释：已匹配"ababa"，又读到'c'，"ababac"的最长P前缀后缀是"ababac"(长度6) → 继续匹配！

δ(5, a) = σ(P_5 · a) = σ("ababaa") = 1
   解释：已匹配"ababa"，又读到'a'，"ababaa"的最长P前缀后缀是"a"(长度1) → 匹配大量丢失
```

**P = "ababaca" 的完整转移表**：

```
状态 q:    0   1   2   3   4   5   6   7
P[q]:      -   a   b   a   b   a   c   a

字符 →  0   1   2   3   4   5   6   7
a      1   1   3   1   5   1   7   1
b      0   2   0   4   0   4   0   2
c      0   0   0   0   0   6   0   0

（状态7是接受状态，匹配成功！）
```

### 4.6 图解匹配过程

```
T = "a b a b a b a c a b a",  P = "ababaca"

字符: a  b  a  b  a  b  a  c  a  b  a
状态: 1  2  3  4  5  4  5  6  7  2  3
                                    ↑
                               状态=7=|P|，匹配成功！
                               有效偏移 = 9-7+1 = 3（0-indexed）或 8（1-indexed）

逐步解释：
q=0, 读a → δ(0,a)=1  （匹配了"a"）
q=1, 读b → δ(1,b)=2  （匹配了"ab"）
q=2, 读a → δ(2,a)=3  （匹配了"aba"）
q=3, 读b → δ(3,b)=4  （匹配了"abab"）
q=4, 读a → δ(4,a)=5  （匹配了"ababa"）
q=5, 读b → δ(5,b)=4  （失配！"ababab"最长P前缀后缀是"abab"）
q=4, 读a → δ(4,a)=5  （又匹配了"ababa"）
q=5, 读c → δ(5,c)=6  （匹配了"ababac"）
q=6, 读a → δ(6,a)=7  （匹配了"ababaca" → 全部匹配！）
```

### 4.7 伪代码

```
FINITE-AUTOMATON-MATCHER(T, δ, m)
1  n = T.length
2  q = 0                     // 初始状态
3  for i = 1 to n            // 逐字符扫描文本
4      q = δ(q, T[i])       // 状态转移
5      if q == m             // 到达接受状态
6          print "Pattern occurs with shift" i - m

COMPUTE-TRANSITION-FUNCTION(P, Σ)
1  m = P.length
2  for q = 0 to m
3      for each character a in Σ
4          k = min(m, q + 1)
5          while P_k ⊐ P_q·a 不成立
6              k = k - 1
7          δ(q, a) = k
8  return δ
```

### 4.8 复杂度分析

| 阶段 | 时间 | 说明 |
|------|------|------|
| 预处理 | O(m\|Σ\|) | 构建转移表（m个状态 × \|Σ\|个字符） |
| 匹配 | Θ(n) | 每个文本字符只看一次 |

**FAQ：为什么匹配时间是 Θ(n)？**

> 因为文本每个字符只读一次，每次转移 O(1)（查表），总共 n 次转移。
> 这比朴素算法好得多——朴素算法最坏 O(nm)，有限自动机始终 O(n)。

**FAQ：O(m\|Σ\|) 的预处理会不会太慢？**

> 如果字母表很大（比如Unicode有十几万字符），预处理确实很慢。
> 这也是为什么实际中更常用KMP算法——它不需要显式构建转移表。

### 4.9 代码实现

**Python实现**：

```python
def compute_transition_function(P, alphabet=None):
    """
    计算字符串匹配自动机的转移函数
    :param P: 模式串
    :param alphabet: 字母表（默认为模式串中出现的字符）
    :return: 转移函数字典 delta[(q, a)] = next_state
    """
    m = len(P)
    if alphabet is None:
        alphabet = sorted(set(P))  # 只考虑模式串中出现的字符
    
    delta = {}
    
    for q in range(m + 1):          # 状态0到m
        for a in alphabet:           # 每个字符
            # 找最大的k使得P[0:k]是P[0:q]+a的后缀
            k = min(m, q + 1)       # 最多匹配q+1个字符
            # P[0:k]是P[0:q]+a的后缀？
            while k > 0:
                pq_a = P[:q] + a    # P_q · a
                if pq_a.endswith(P[:k]):
                    break
                k -= 1
            delta[(q, a)] = k
    
    return delta


def finite_automaton_matcher(T, delta, m):
    """
    有限自动机字符串匹配算法
    :param T: 文本串
    :param delta: 转移函数
    :param m: 模式串长度
    :return: 所有有效偏移的列表
    """
    n = len(T)
    q = 0                           # 初始状态
    valid_shifts = []
    
    for i in range(n):              # 逐字符扫描文本
        if (q, T[i]) in delta:
            q = delta[(q, T[i])]   # 状态转移
        else:
            q = 0                   # 字符不在字母表中，回到初始状态
        
        if q == m:                  # 到达接受状态
            valid_shifts.append(i - m + 1)  # 记录有效偏移（0-indexed）
    
    return valid_shifts


# 测试
P = "ababaca"
T = "abababacaba"
alphabet = sorted(set(P))
delta = compute_transition_function(P, alphabet)
result = finite_automaton_matcher(T, delta, len(P))
print(f"有效偏移: {result}")  # 输出: [2]
```

**C++实现**：

```cpp
#include <iostream>
#include <vector>
#include <string>
#include <map>
using namespace std;

/**
 * 计算字符串匹配自动机的转移函数
 * @param P 模式串
 * @param alphabet 字母表
 * @return 转移函数（map，键为pair<状态,字符>）
 */
map<pair<int, char>, int> computeTransitionFunction(const string& P, const vector<char>& alphabet) {
    int m = P.length();
    map<pair<int, char>, int> delta;
    
    for (int q = 0; q <= m; q++) {
        for (char a : alphabet) {
            // 找最大的k使得P[0:k]是P[0:q]+a的后缀
            int k = min(m, q + 1);
            string pq_a = P.substr(0, q) + a;  // P_q · a
            while (k > 0) {
                // 检查P[0:k]是否是pq_a的后缀
                if (pq_a.length() >= (size_t)k && 
                    pq_a.substr(pq_a.length() - k) == P.substr(0, k)) {
                    break;
                }
                k--;
            }
            delta[{q, a}] = k;
        }
    }
    
    return delta;
}

/**
 * 有限自动机字符串匹配算法
 * @param T 文本串
 * @param delta 转移函数
 * @param m 模式串长度
 * @return 所有有效偏移的列表
 */
vector<int> finiteAutomatonMatcher(const string& T, 
                                    const map<pair<int, char>, int>& delta, 
                                    int m) {
    vector<int> validShifts;
    int n = T.length();
    int q = 0;  // 初始状态
    
    for (int i = 0; i < n; i++) {
        auto it = delta.find({q, T[i]});
        if (it != delta.end()) {
            q = it->second;     // 状态转移
        } else {
            q = 0;              // 字符不在字母表中
        }
        
        if (q == m) {           // 到达接受状态
            validShifts.push_back(i - m + 1);
        }
    }
    
    return validShifts;
}

int main() {
    string P = "ababaca";
    string T = "abababacaba";
    
    // 构造字母表
    vector<char> alphabet = {'a', 'b', 'c'};
    
    auto delta = computeTransitionFunction(P, alphabet);
    auto result = finiteAutomatonMatcher(T, delta, P.length());
    
    cout << "有效偏移: ";
    for (int s : result) cout << s << " ";
    cout << endl;
    return 0;
}
```

---

## 5️⃣ Knuth-Morris-Pratt算法（32.4 The Knuth-Morris-Pratt Algorithm）

### 5.1 KMP的核心思想

**生活中的类比**：想象你在**走迷宫**：
- 朴素算法：走错路了，回到起点重新走 → 太蠢！
- KMP：走错路了，直接跳到**最近的已知正确岔路口**继续走 → 聪明！

**朴素算法的致命缺陷**：

```
T = "ABCABD ABCABD ..."
P = "ABCABE"

朴素算法匹配过程：
s=0: A B C A B D
     A B C A B E    ← 第6个字符失配(D≠E)
                      朴素做法：s=1，从头再来

s=1: A B C A B D
       A B C A B E  ← 第1个字符就不匹配(B≠A)

s=2: A B C A B D
         A B C A B E  ← 第1个字符就不匹配(C≠A)

...浪费了大量比较！
```

**KMP的聪明之处**：

```
失配发生在P[6]='E' vs T[6]='D'

已知 T[1..5] = P[1..5] = "ABCAB"
P[1..5] = "ABCAB" 的前缀和后缀有公共部分 "AB"！

所以：T[4..5] = P[4..5] = "AB" = P[1..2]
     这部分已经匹配过了，不需要再比！

直接跳到：从 P[3]='C' 开始和 T[6]='D' 比较
```

**关键洞察**：失配时，利用"已匹配部分"的前后缀关系，跳过不必要的比较，**文本指针永远不回退**！

### 5.2 前缀函数 π

**前缀函数的定义**：

π[q] = 最大的 k < q，使得 P[1..k] 是 P[1..q] 的后缀

即：P 的前 q 个字符的子串中，**最长的"前缀 = 后缀"的长度**（注意是"真"前缀，k < q）。

**生活类比**：π[q] 就像是问你"这句话前面有多少内容和结尾重复了？"

```
P = "a b a b a b c a"
索引: 1  2  3  4  5  6  7  8

π[1] = 0  "a" → 没有真前缀=后缀
π[2] = 0  "ab" → 前缀"a"≠后缀"b"
π[3] = 1  "aba" → 前缀"a" = 后缀"a" ✓，长度1
π[4] = 2  "abab" → 前缀"ab" = 后缀"ab" ✓，长度2
π[5] = 3  "ababa" → 前缀"aba" = 后缀"aba" ✓，长度3
π[6] = 4  "ababab" → 前缀"abab" = 后缀"abab" ✓，长度4
π[7] = 0  "abababc" → 没有前缀等于后缀
π[8] = 1  "abababca" → 前缀"a" = 后缀"a" ✓，长度1

π数组: [0, 0, 1, 2, 3, 4, 0, 1]
```

**更多例子**：

| 模式串 | π数组 | 解释 |
|--------|-------|------|
| "abcd" | [0,0,0,0] | 没有任何前缀=后缀 |
| "aaaa" | [0,1,2,3] | 每个前缀都是后缀 |
| "ababab" | [0,0,1,2,3,4] | "ab"反复出现 |
| "abcabd" | [0,0,0,1,2,0] | "ab"前后重复 |

### 5.3 π数组的计算

**朴素方法**：对每个 q，枚举所有 k < q，检查 P[1..k] 是否是 P[1..q] 的后缀 → O(m²)

**KMP的巧妙方法**：利用已计算的 π 值递推 → O(m)！

**关键递推关系**：

```
已知 π[q-1] = k，求 π[q]：

如果 P[k+1] == P[q]：
    π[q] = k + 1       ← 前后缀可以延长一个字符

如果 P[k+1] ≠ P[q]：
    找下一个候选 k' = π[k]，继续比较 P[k'+1] 和 P[q]
    如果还不等，继续 k'' = π[k'] ...
    直到 k = 0 还不等，则 π[q] = 0
```

**直觉理解**：就像"剥洋葱"——外层不匹配就往里剥一层（利用π的传递性），直到找到匹配的层或剥到底。

### 5.4 图解π数组计算

```
P = "a b a b a b c a"
索引: 1  2  3  4  5  6  7  8

初始: π[1] = 0, k = 0

q=2: P[k+1]=P[1]='a', P[2]='b', 'a'≠'b'
     k = π[0] = 0（k已经为0，停止）
     π[2] = 0

q=3: P[k+1]=P[1]='a', P[3]='a', 'a'='a' ✓
     k = k+1 = 1
     π[3] = 1

q=4: P[k+1]=P[2]='b', P[4]='b', 'b'='b' ✓
     k = k+1 = 2
     π[4] = 2

q=5: P[k+1]=P[3]='a', P[5]='a', 'a'='a' ✓
     k = k+1 = 3
     π[5] = 3

q=6: P[k+1]=P[4]='b', P[6]='b', 'b'='b' ✓
     k = k+1 = 4
     π[6] = 4

q=7: P[k+1]=P[5]='a', P[7]='c', 'a'≠'c'
     k = π[4] = 2, P[k+1]=P[3]='a', P[7]='c', 'a'≠'c'
     k = π[2] = 0, P[k+1]=P[1]='a', P[7]='c', 'a'≠'c'
     k = 0, 停止
     π[7] = 0

q=8: P[k+1]=P[1]='a', P[8]='a', 'a'='a' ✓
     k = k+1 = 1
     π[8] = 1

结果: π = [0, 0, 1, 2, 3, 4, 0, 1]
```

### 5.5 COMPUTE-PREFIX-FUNCTION 伪代码

```
COMPUTE-PREFIX-FUNCTION(P)
1  m = P.length
2  π[1] = 0
3  k = 0                           // 当前最长前后缀长度
4  for q = 2 to m                  // 从第2个字符开始
5      while k > 0 and P[k+1] ≠ P[q]
6          k = π[k]                // "剥洋葱"：往更短的前后缀找
7      if P[k+1] == P[q]
8          k = k + 1               // 前后缀可以延长
9      π[q] = k
10 return π
```

### 5.6 KMP匹配过程

**核心思想**：匹配时，用 π 数组代替有限自动机的转移表，实现同样的效果。

```
KMP-MATCHER(T, P)
1  n = T.length
2  m = P.length
3  π = COMPUTE-PREFIX-FUNCTION(P)   // 预处理：计算π数组
4  q = 0                             // 已匹配的字符数
5  for i = 1 to n                    // 扫描文本（i永不回退！）
6      while q > 0 and P[q+1] ≠ T[i]
7          q = π[q]                  // 失配：利用π跳转
8      if P[q+1] == T[i]
9          q = q + 1                 // 匹配：前进一位
10     if q == m                     // 完全匹配！
11         print "Pattern occurs with shift" i - m
12         q = π[q]                  // 继续找下一个匹配
```

### 5.7 图解KMP匹配过程

```
T = "a b a b a b a c a b a",  P = "a b a b a c a"
π = [0, 0, 1, 2, 3, 4, 0, 1]  (注意：π[1..7], 1-indexed)

i=1, T[1]='a': q=0, P[1]='a'='a' → q=1
i=2, T[2]='b': q=1, P[2]='b'='b' → q=2
i=3, T[3]='a': q=2, P[3]='a'='a' → q=3
i=4, T[4]='b': q=3, P[4]='b'='b' → q=4
i=5, T[5]='a': q=4, P[5]='a'='a' → q=5
i=6, T[6]='b': q=5, P[6]='c'≠'b' → q=π[5]=3
                q=3, P[4]='b'='b' → q=4  (注意：i不变！)
i=7, T[7]='a': q=4, P[5]='a'='a' → q=5
i=8, T[8]='c': q=5, P[6]='c'='c' → q=6
i=9, T[9]='a': q=6, P[7]='a'='a' → q=7

q = 7 = m → 匹配成功！偏移 = 9 - 7 = 2 (1-indexed)
q = π[7] = 0 → 继续搜索

i=10,T[10]='b': q=0, P[1]='a'≠'b' → q=0
i=11,T[11]='a': q=0, P[1]='a'='a' → q=1

结束。

详细过程图：
T: a  b  a  b  a  b  a  c  a  b  a
P: a  b  a  b  a  c  a
                ↑
              i=6失配，q从5跳到3（利用π[5]=3）

跳转后：
T: a  b  a  b  a  b  a  c  a  b  a
P:          a  b  a  b  a  c  a
                  ↑
              从P[4]='b'继续和T[6]='b'比较（i不变！）
```

**FAQ：为什么 i 永远不回退？**

> 这是KMP最精妙的地方！当 P[q+1] ≠ T[i] 时，我们通过 q = π[q] 跳转，
> 跳转后新的 q 保证 P[1..q] 仍然是 T[s+1..i-1] 的后缀（即之前已匹配的），
> 所以我们只需要比较 P[q+1] 和 T[i]，不需要回头比较之前的字符。
> 这就是"利用已匹配信息"的精髓。

### 5.8 KMP为什么是 Θ(n)？

**摊还分析**（不是严格的逐行分析，而是整体考虑）：

```
关键观察：
- q 的值在 for 循环中每次至多增加 1（第9行）
- q 的最大值是 m
- 所以 q 总共至多增加 n 次
- 而 while 循环（第6-7行）每次使 q 减小
- q 不可能为负数
- 所以 while 循环总共至多执行 n 次

总操作次数 = O(n)（匹配阶段）+ O(m)（计算π）= O(n+m) = Θ(n)（当m≤n时）
```

**类比**：想象 q 是一个水桶的水位——每次循环水位最多升高1格，但可以被放掉很多格。水桶总共升高了 n 格（最多），所以放水的总量也不会超过 n 格。

### 5.9 KMP与有限自动机的关系

| 对比 | 有限自动机 | KMP |
|------|-----------|-----|
| 预处理 | 构建完整转移表 δ | 只计算前缀函数 π |
| 预处理时间 | O(m\|Σ\|) | Θ(m) |
| 预处理空间 | O(m\|Σ\|) | Θ(m) |
| 匹配时间 | Θ(n) | Θ(n) |
| 核心思想 | 显式状态转移 | 隐式状态转移（通过π回退） |

**本质联系**：KMP可以看作有限自动机的"压缩版"——不显式存储所有转移，而是只存储"失配时跳到哪里"（即π），需要时通过while循环一步步跳。

```
有限自动机：δ(q, a) = σ(P_q · a)     → 直接查表O(1)
KMP：       π[q] = 最长前后缀长度      → 可能需要多次跳转
           但摊还后仍是O(1)！
```

### 5.10 代码实现

**Python实现**：

```python
def compute_prefix_function(P):
    """
    计算前缀函数π
    :param P: 模式串
    :return: π数组（0-indexed，π[0]=0）
    """
    m = len(P)
    pi = [0] * m               # π[0] = 0
    k = 0                       # 当前最长前后缀长度
    
    for q in range(1, m):       # 从第2个字符开始（0-indexed从1开始）
        # "剥洋葱"：找更短的前后缀
        while k > 0 and P[k] != P[q]:
            k = pi[k - 1]       # 跳转到更短的前后缀
        if P[k] == P[q]:        # 可以延长
            k += 1
        pi[q] = k
    
    return pi


def kmp_matcher(T, P):
    """
    KMP字符串匹配算法
    :param T: 文本串
    :param P: 模式串
    :return: 所有有效偏移的列表
    """
    n = len(T)
    m = len(P)
    pi = compute_prefix_function(P)  # 预处理
    q = 0                             # 已匹配的字符数
    valid_shifts = []
    
    for i in range(n):                # 扫描文本（i永不回退）
        # 失配时利用π跳转
        while q > 0 and P[q] != T[i]:
            q = pi[q - 1]             # 跳转（0-indexed调整）
        if P[q] == T[i]:             # 匹配，前进一位
            q += 1
        if q == m:                    # 完全匹配
            valid_shifts.append(i - m + 1)  # 记录有效偏移
            q = pi[q - 1]             # 继续找下一个匹配
    
    return valid_shifts


# 测试
T = "abababacaba"
P = "ababaca"
print(f"π数组: {compute_prefix_function(P)}")
print(f"有效偏移: {kmp_matcher(T, P)}")  # 输出: [2]
```

**C++实现**：

```cpp
#include <iostream>
#include <vector>
#include <string>
using namespace std;

/**
 * 计算前缀函数π
 * @param P 模式串
 * @return π数组
 */
vector<int> computePrefixFunction(const string& P) {
    int m = P.length();
    vector<int> pi(m, 0);      // π[0] = 0
    int k = 0;                  // 当前最长前后缀长度
    
    for (int q = 1; q < m; q++) {  // 从第2个字符开始
        // "剥洋葱"：找更短的前后缀
        while (k > 0 && P[k] != P[q]) {
            k = pi[k - 1];          // 跳转到更短的前后缀
        }
        if (P[k] == P[q]) {         // 可以延长
            k++;
        }
        pi[q] = k;
    }
    
    return pi;
}

/**
 * KMP字符串匹配算法
 * @param T 文本串
 * @param P 模式串
 * @return 所有有效偏移的列表
 */
vector<int> kmpMatcher(const string& T, const string& P) {
    vector<int> validShifts;
    int n = T.length();
    int m = P.length();
    
    if (m == 0 || m > n) return validShifts;
    
    vector<int> pi = computePrefixFunction(P);  // 预处理
    int q = 0;                   // 已匹配的字符数
    
    for (int i = 0; i < n; i++) {   // 扫描文本（i永不回退）
        // 失配时利用π跳转
        while (q > 0 && P[q] != T[i]) {
            q = pi[q - 1];           // 跳转
        }
        if (P[q] == T[i]) {         // 匹配，前进一位
            q++;
        }
        if (q == m) {                // 完全匹配
            validShifts.push_back(i - m + 1);
            q = pi[q - 1];           // 继续找下一个匹配
        }
    }
    
    return validShifts;
}

int main() {
    string T = "abababacaba";
    string P = "ababaca";
    
    auto pi = computePrefixFunction(P);
    cout << "π数组: ";
    for (int v : pi) cout << v << " ";
    cout << endl;
    
    auto result = kmpMatcher(T, P);
    cout << "有效偏移: ";
    for (int s : result) cout << s << " ";
    cout << endl;
    
    return 0;
}
```

---

## 6️⃣ 四种算法全面对比

### 6.1 综合对比表

| 维度 | 朴素 | Rabin-Karp | 有限自动机 | KMP |
|------|------|-----------|-----------|-----|
| **预处理时间** | 0 | Θ(m) | O(m\|Σ\|) | Θ(m) |
| **匹配时间（最坏）** | O((n-m+1)m) | O((n-m+1)m) | Θ(n) | Θ(n) |
| **匹配时间（期望）** | O(n) | O(n+m) | Θ(n) | Θ(n) |
| **空间** | O(1) | O(1) | O(m\|Σ\|) | O(m) |
| **文本指针回退？** | 是 | 否 | 否 | 否 |
| **核心技巧** | 无 | 滚动哈希 | 状态转移 | 前缀函数 |
| **适用场景** | 简单场景 | 多模式匹配 | 理论分析 | 实际应用 |

### 6.2 选择建议

```
应该用哪种算法？

            ┌── 模式很短(m<10) → 朴素算法就够了
            │
            ├── 需要同时匹配多个模式 → Rabin-Karp（哈希集）
            │
你的场景 ───┤── 字母表很小(如DNA:{A,C,G,T}) → 有限自动机
            │
            ├── 通用场景，追求最坏O(n) → KMP
            │
            └── 实际工程中 → 往往用Boyer-Moore（本章未讲）
                              （从右往左匹配，实践中更快）
```

### 6.3 四种算法的演进逻辑

```
朴素算法：                  "挨个试"
   ↓ 问题：重复比较太多
Rabin-Karp：               "先比指纹再比内容"
   ↓ 问题：最坏还是O(nm)，且需要取模
有限自动机：               "建个状态机自动跑"
   ↓ 问题：预处理O(m|Σ|)太慢，空间太大
KMP：                      "只存失配跳转表，效果一样"
   ↓ 结果：预处理Θ(m)，匹配Θ(n)，空间Θ(m)
```

---

## 7️⃣ 本章小结

### 🎯 核心要点

1. **字符串匹配问题**：在文本 T[1..n] 中找模式 P[1..m] 的所有出现，核心指标是预处理时间和匹配时间
2. **朴素算法** O((n-m+1)m)：简单直觉，但最坏很慢，文本指针会回退
3. **Rabin-Karp** O(n)期望：滚动哈希是灵魂，O(1)算出下一个子串哈希值；但最坏O(nm)，有伪命中问题
4. **有限自动机** Θ(n)匹配：每个字符只看一次，但预处理O(m|Σ|)是瓶颈
5. **KMP** Θ(n)匹配：π数组记录"最长前后缀"，失配时利用π跳转，文本指针永不回退；摊还分析证明Θ(n)

### ⚠️ 易错点

| 易错点 | 正确理解 |
|--------|---------|
| Rabin-Karp的哈希值比较=字符串匹配 | 哈希值相等可能是伪命中，必须逐字符验证 |
| 有限自动机的状态=已匹配字符数 | 是的，状态q就是P_q是已读文本后缀的长度 |
| π[q]是P[1..q]的最长前后缀长度 | 注意是**真**前后缀，k < q（不能等于自身） |
| KMP中i会回退 | **绝对不会！** 只有q会回退，i永远只往前走 |
| KMP的while循环会让时间退化 | 摊还分析保证总操作次数O(n)，while循环总共至多执行n次 |

### 🔗 与前后章节的联系

| 方向 | 联系 |
|------|------|
| → 第5章 概率分析 | Rabin-Karp的期望分析用到了概率论证 |
| → 第11章 散列表 | Rabin-Karp的哈希思想与散列表一脉相承 |
| → 第15章 动态规划 | π数组的递推计算具有DP的特征 |
| → 第17章 摊销分析 | KMP的Θ(n)分析是经典的摊还分析 |
| → 第34章 NP完全性 | 字符串匹配是P问题，O(n)可解 |

### 📝 课后思考

1. **思考题**：如果模式串中所有字符都不同，朴素算法能否优化到O(n)？如何优化？
2. **思考题**：Rabin-Karp中，为什么q要选质数？如果q选合数会怎样？
3. **编程题**：用KMP算法实现 `strStr()` 函数（LeetCode 28题）
4. **编程题**：给定模式P，计算π数组，然后验证：π[q]确实是P[1..q]的最长前后缀长度
5. **思考题**：KMP和有限自动机的匹配时间都是Θ(n)，但KMP的预处理更快(Θ(m) vs O(m|Σ|))。为什么KMP能做到？代价是什么？
6. **扩展题**：研究Boyer-Moore算法——它从右向左匹配，实践中通常比KMP更快，为什么？

---

*笔记完成时间：2026年6月11日*
*下一章：第33章 计算几何*
