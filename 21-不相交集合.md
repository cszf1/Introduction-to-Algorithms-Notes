# 第21章 不相交集合（Disjoint Sets / Union-Find）

> 本章介绍一种专门处理"不相交集合"合并与查询问题的数据结构——并查集，它是解决等价类问题的利器

## 📖 本章导读

### 🎯 问题引入：什么是"不相交集合"？

**生活中的例子**：

想象你在管理一个**社交网络**：

- 每个人都有自己的人际圈子
- 当两个人互相认识时，他们的圈子就**合并**成一个
- 你需要快速判断：**两个人是否在同一个圈子里？**

这就是**不相交集合**（Disjoint Sets）要解决的问题！

**具体场景**：

```
社交网络初始化：
- 每个人是独立的圈子（自己就是一个集合）
- 圈子之间没有交集（不相交）

建立好友关系：
- A 和 B 成为朋友 → 合并 {A} 和 {B} → {A, B}
- C 和 D 成为朋友 → 合并 {C} 和 {D} → {C, D}

查询关系：
- "A 和 B 在同一个圈子吗？" → 是
- "A 和 C 在同一个圈子吗？" → 否

再次合并：
- B 和 C 成为朋友 → {A, B} 和 {C, D} 合并 → {A, B, C, D}
```

**核心操作只有两个**：

| 操作 | 含义 | 类比 |
|------|------|------|
| **MAKE-SET(x)** | 创建一个只包含x的新集合 | 每个人都自己建一个群 |
| **FIND(x)** | 找到x所属集合的代表元素 | 问"你在哪个群？" |
| **UNION(x, y)** | 合并x和y所在的两个集合 | 两个群合并成一个 |

---

### 💡 并查集的核心设计思想

并查集的关键在于**如何高效地合并和查找**：

```
朴素想法（很慢）：
- 每个集合用一个链表存储
- 合并需要遍历一个链表的所有元素
- 时间复杂度：O(n)

优化思想：
- 让每个集合形成一棵树
- 只存储"父结点"关系
- 合并时只需要改变根节点的父指针
- 查找时沿着父指针往上找

更进一步的优化：
- 路径压缩：找过的结点直接指向根
- 按秩合并：小树合并到大树下
```

**为什么叫"并查集"？**

- **并** = UNION（合并操作）
- **查** = FIND（查找操作）
- **集** = SET（集合）

---

## 1️⃣ 并查集的基本实现

### 1.1 最朴素的方法：链表表示

**每个集合用一个链表表示**：

```
集合 {A, B, C} 的链表表示：
┌─────────┐    ┌─────────┐    ┌─────────┐
│   A     │───▶│   B     │───▶│   C     │
│(set ptr)│    │(next)   │    │(next) │
└─────────┘    └─────────┘    └─────────┘
   ↑              ↑              │
   └──────────────┴──────────────┘
              (tail指针)
```

**问题**：合并两个集合需要遍历整个链表！

```
合并 {A,B} 和 {C,D}：
{A,B,C} 链表需要 O(n) 时间遍历
```

### 1.2 用森林表示：父指针数组

**核心思想**：每个结点只存储它的"父结点"

```
森林表示示例：
         A               E
        /                \
       B                 F
      /
     C

父指针数组：
parent[A] = B
parent[B] = C  
parent[C] = null (C是根)
parent[E] = F
parent[F] = null (F是根)
```

**基本代码实现**：

```python
# Python 实现：并查集基础版
class UnionFind:
    def __init__(self, n):
        """初始化：每个元素都是自己的集合"""
        self.parent = list(range(n))  # parent[i] = i 表示i的父结点是自己
    
    def find(self, x):
        """查找：找到x所属集合的根结点"""
        if self.parent[x] != x:
            # 递归查找父结点，直到根结点
            return self.find(self.parent[x])
        return x
    
    def union(self, x, y):
        """合并：把x和y所在的集合合并"""
        root_x = self.find(x)
        root_y = self.find(y)
        
        if root_x != root_y:  # 不同集合才合并
            self.parent[root_y] = root_x  # 把y的根挂到x的根下面
```

```cpp
// C++ 实现：并查集基础版
class UnionFind {
private:
    vector<int> parent;  // 父指针数组
    
public:
    UnionFind(int n) {
        parent.resize(n);
        for (int i = 0; i < n; i++) {
            parent[i] = i;  // 初始化：每个元素的父结点是自己
        }
    }
    
    // 查找：找到x的根结点（代表元素）
    int find(int x) {
        if (parent[x] != x) {
            return find(parent[x]);  // 递归找父结点
        }
        return x;
    }
    
    // 合并：把x和y所在的集合合并
    void union_set(int x, int y) {
        int root_x = find(x);
        int root_y = find(y);
        
        if (root_x != root_y) {
            parent[root_y] = root_x;  // 把y的根挂到x的根下面
        }
    }
    
    // 判断两个元素是否在同一集合
    bool connected(int x, int y) {
        return find(x) == find(y);
    }
};
```

**基本版的问题**：树可能变成一条长长的链！

```
最坏情况：每次合并都把大树挂在小树下
         ○                    ○
        /                     |
       ○                     ○
      /                      |
     ○          ===>         ○  ...  O(n) 查找！
    /
   ○
```

---

## 2️⃣ 两种核心优化策略

### 2.1 策略一：按秩（Rank）合并

**核心思想**：**小树挂到大树下**，避免树变深

**秩（Rank）的含义**：
- 可以理解为树的"高度上界"
- 秩小的树合并到秩大的树下

```
按秩合并示例：

合并前：                    合并后（按秩）：
   A(秩=2)                     A(秩=2)
  /|\                           /|\
 ○ ○ ○                        ○ ○ ○
                              /
                             B(秩=1)
                            / \
                           ○   ○
                          /
                         C(秩=0)

B树的秩(1) < A树的秩(2)，所以B合并到A下
```

**为什么按秩合并有效？**

```
数学证明（理解即可）：
- 秩为 k 的树至少有 2^k 个结点
- 所以树的深度最多是 O(log n)
- 合并时秩增加的概率很小

直观理解：
- 小树合并到大树，大树的深度不会增加
- 只有两棵树秩相同时，合并后秩才+1
- 秩增加的情况很少
```

**按秩合并的代码**：

```python
# Python 实现：按秩合并
class UnionFindRank:
    def __init__(self, n):
        self.parent = list(range(n))
        self.rank = [0] * n  # 秩数组，初始为0
    
    def find(self, x):
        """路径压缩：在查找过程中把路径上的结点直接指向根"""
        if self.parent[x] != x:
            self.parent[x] = self.find(self.parent[x])  # 递归 + 路径压缩
        return self.parent[x]
    
    def union(self, x, y):
        """按秩合并：秩小的合并到秩大的"""
        root_x = self.find(x)
        root_y = self.find(y)
        
        if root_x == root_y:
            return  # 已经在同一个集合，无需合并
        
        # 比较秩，按秩合并
        if self.rank[root_x] > self.rank[root_y]:
            # x的根秩更大，y合并到x
            self.parent[root_y] = root_x
        elif self.rank[root_x] < self.rank[root_y]:
            # y的根秩更大，x合并到y
            self.parent[root_x] = root_y
        else:
            # 秩相同，合并后秩+1
            self.parent[root_y] = root_x
            self.rank[root_x] += 1
```

```cpp
// C++ 实现：按秩合并
class UnionFindRank {
private:
    vector<int> parent;  // 父指针
    vector<int> rank;   // 秩数组
    
public:
    UnionFindRank(int n) {
        parent.resize(n);
        rank.resize(n, 0);
        for (int i = 0; i < n; i++) {
            parent[i] = i;
        }
    }
    
    // 查找 + 路径压缩
    int find(int x) {
        if (parent[x] != x) {
            parent[x] = find(parent[x]);  // 路径压缩
        }
        return parent[x];
    }
    
    // 按秩合并
    void union_set(int x, int y) {
        int root_x = find(x);
        int root_y = find(y);
        
        if (root_x == root_y) return;  // 同集合不合并
        
        if (rank[root_x] > rank[root_y]) {
            parent[root_y] = root_x;  // x的根更大，y合并到x
        } else if (rank[root_x] < rank[root_y]) {
            parent[root_x] = root_y;  // y的根更大，x合并到y
        } else {
            parent[root_y] = root_x;  // 秩相同，y合并到x，秩+1
            rank[root_x]++;
        }
    }
};
```

### 2.2 策略二：路径压缩（Path Compression）

**核心思想**：查找时把路径上的所有结点直接指向根

**图解路径压缩过程**：

```
路径压缩前：              路径压缩后：
      ○                       ○
      │                       │
      ○                       ○
      │           ===>        │
      ○                       ○
      │
      ○
      │
      ● ← 查找●

执行 find(●) 后，●到根的路径上的所有结点都直接指向根！
```

**为什么路径压缩有效？**

```
多次查找后，树会变得非常扁平：
- 第一次 find：路径压缩一次
- 第二次 find：路径更短了
- 每次 find 都让树变得更扁平

时间复杂度接近 O(α(n))，其中 α 是 Ackermann 函数的反函数
α(n) 在实际应用中 <= 4
```

**路径压缩的代码**：

```python
# Python：完整的路径压缩实现
class UnionFindOptimized:
    def __init__(self, n):
        self.parent = list(range(n))
        self.rank = [0] * n
    
    def find(self, x):
        """
        路径压缩：递归把查找路径上的所有结点指向根
        
        过程演示：
        find(5) 的调用链：
        5 → 3 → 1 → 0 (根)
        
        返回时：
        parent[5] = 0
        parent[3] = 0  
        parent[1] = 0
        """
        if self.parent[x] != x:
            # 关键：把当前结点的父结点设为根
            self.parent[x] = self.find(self.parent[x])
        return self.parent[x]
    
    def union(self, x, y):
        root_x = self.find(x)
        root_y = self.find(y)
        
        if root_x == root_y:
            return
        
        if self.rank[root_x] > self.rank[root_y]:
            self.parent[root_y] = root_x
        elif self.rank[root_x] < self.rank[root_y]:
            self.parent[root_x] = root_y
        else:
            self.parent[root_y] = root_x
            self.rank[root_x] += 1
```

```cpp
// C++：完整的路径压缩实现
class UnionFindOptimized {
private:
    vector<int> parent;
    vector<int> rank;
    
public:
    UnionFindOptimized(int n) {
        parent.resize(n);
        rank.resize(n, 0);
        for (int i = 0; i < n; i++) {
            parent[i] = i;
        }
    }
    
    int find(int x) {
        // 路径压缩：递归把查找路径上的所有结点指向根
        if (parent[x] != x) {
            parent[x] = find(parent[x]);
        }
        return parent[x];
    }
    
    void union_set(int x, int y) {
        int root_x = find(x);
        int root_y = find(y);
        
        if (root_x == root_y) return;
        
        // 按秩合并
        if (rank[root_x] > rank[root_y]) {
            parent[root_y] = root_x;
        } else if (rank[root_x] < rank[root_y]) {
            parent[root_x] = root_y;
        } else {
            parent[root_y] = root_x;
            rank[root_x]++;
        }
    }
    
    bool connected(int x, int y) {
        return find(x) == find(y);
    }
};
```

---

## 3️⃣ 两种优化的效果分析

### 3.1 为什么单独优化不够？

| 优化策略 | 单独使用效果 | 问题 |
|---------|-------------|------|
| 只用路径压缩 | 树变浅 | 最坏情况仍是 O(log n) |
| 只用按秩合并 | 深度可控 | 单次查找仍需 O(log n) |

### 3.2 两种优化结合：近乎完美的 O(α(n))

**Ackermann 函数及其反函数**：

```
Ackermann 函数 A(m, n) 增长极快：
A(0, n) = n + 1
A(1, n) = n + 2
A(2, n) = 2n + 3
A(3, n) = 2^(n+3) - 3  ← 开始爆炸增长！
A(4, n) = ... (无法用常规方式写出)

Ackermann 反函数 α(n)：
α(n) = min{ m | A(m, m) >= n }

实际意义：
- α(n) 增长极慢
- α(10^80) ≈ 5（宇宙中原子数！）
- 所以 O(α(n)) 几乎等于 O(1)
```

**两种优化结合后的复杂度**：

```
┌────────────────────────────────────────────────────┐
│            并查集的最优时间复杂度                      │
├────────────────────────────────────────────────────┤
│                                                    │
│   m 次操作（FIND + UNION） 的总时间：O(m α(n))      │
│                                                    │
│   其中 α(n) 是 Ackermann 反函数                    │
│   对于 n < 10^80，α(n) <= 5                        │
│                                                    │
│   结论：并查集几乎是 O(1) 的数据结构！               │
│                                                    │
└────────────────────────────────────────────────────┘
```

**渐进分析**：

| 操作 | 朴素版 | +按秩合并 | +路径压缩 | 两者结合 |
|------|--------|-----------|-----------|----------|
| FIND | O(n) | O(log n) | ~O(1)* | **O(α(n))** |
| UNION | O(n) | O(log n) | ~O(1)* | **O(α(n))** |

*路径压缩单独使用时，均摊复杂度仍是 O(α(n))

---

## 4️⃣ 并查集的应用场景

### 4.1 亲戚关系判定

**问题**：给定 m 对亲戚关系，判断 n 对询问是否是亲戚

```
输入示例：
5 3          // 5个人，3对亲戚关系
1 2          // 1和2是亲戚
2 3          // 2和3是亲戚（通过2，1和3也是亲戚）
4 5          // 4和5是亲戚
2            // 2次查询
1 3          // 1和3是亲戚吗？ → 是
1 4          // 1和4是亲戚吗？ → 否
```

**代码实现**：

```python
# Python：亲戚关系判定
def main():
    import sys
    sys.setrecursionlimit(1000000)
    
    data = sys.stdin.read().strip().split()
    if not data:
        return
    it = iter(data)
    n = int(next(it))  # 人数
    m = int(next(it))  # 亲戚关系数
    
    uf = UnionFindOptimized(n + 1)  # 1-based 编号
    
    # 建立亲戚关系
    for _ in range(m):
        x = int(next(it))
        y = int(next(it))
        uf.union(x, y)
    
    # 处理查询
    q = int(next(it))
    for _ in range(q):
        x = int(next(it))
        y = int(next(it))
        if uf.find(x) == uf.find(y):
            print("Yes")
        else:
            print("No")

# 测试
# input: "5 3\n1 2\n2 3\n4 5\n2\n1 3\n1 4\n"
# output: "Yes\nNo\n"
```

```cpp
// C++：亲戚关系判定
#include <iostream>
#include <vector>
using namespace std;

class UnionFind {
private:
    vector<int> parent, rank;
    
public:
    UnionFind(int n) {
        parent.resize(n);
        rank.resize(n, 0);
        for (int i = 0; i < n; i++) parent[i] = i;
    }
    
    int find(int x) {
        if (parent[x] != x) {
            parent[x] = find(parent[x]);  // 路径压缩
        }
        return parent[x];
    }
    
    void union_set(int x, int y) {
        int rx = find(x), ry = find(y);
        if (rx == ry) return;
        
        // 按秩合并
        if (rank[rx] > rank[ry]) {
            parent[ry] = rx;
        } else if (rank[rx] < rank[ry]) {
            parent[rx] = ry;
        } else {
            parent[ry] = rx;
            rank[rx]++;
        }
    }
    
    bool connected(int x, int y) {
        return find(x) == find(y);
    }
};

int main() {
    int n, m;
    cin >> n >> m;
    
    UnionFind uf(n + 1);  // 1-based
    
    // 建立亲戚关系
    for (int i = 0; i < m; i++) {
        int x, y;
        cin >> x >> y;
        uf.union_set(x, y);
    }
    
    // 处理查询
    int q;
    cin >> q;
    while (q--) {
        int x, y;
        cin >> x >> y;
        if (uf.connected(x, y)) {
            cout << "Yes" << endl;
        } else {
            cout << "No" << endl;
        }
    }
    
    return 0;
}
```

### 4.2 Kruskal 最小生成树算法

**最小生成树问题**：

```
给定 n 个结点和 m 条带权边，求连接所有结点的最小代价边集合

应用场景：
- 城市光缆铺设
- 电路板布线
- 聚类分析

Kruskal 算法思路：
1. 把所有边按权值从小到大排序
2. 依次检查每条边：
   - 如果它的两个端点不在同一集合 → 加入生成树，合并集合
   - 否则 → 跳过（会形成环）
```

**Kruskal 算法实现**：

```python
# Python：Kruskal 最小生成树
from typing import List, Tuple

class UnionFind:
    def __init__(self, n):
        self.parent = list(range(n))
        self.rank = [0] * n
    
    def find(self, x):
        if self.parent[x] != x:
            self.parent[x] = self.find(self.parent[x])
        return self.parent[x]
    
    def union(self, x, y):
        rx, ry = self.find(x), self.find(y)
        if rx == ry:
            return False  # 已在同一集合
        if self.rank[rx] > self.rank[ry]:
            self.parent[ry] = rx
        elif self.rank[rx] < self.rank[ry]:
            self.parent[rx] = ry
        else:
            self.parent[ry] = rx
            self.rank[rx] += 1
        return True


def kruskal(n: int, edges: List[Tuple[int, int, int]]) -> int:
    """
    Kruskal 最小生成树算法
    
    参数：
        n: 结点数（0 到 n-1）
        edges: 边列表，每条边为 (u, v, w)，w 是权值
    
    返回：
        最小生成树的权值之和
    """
    # 1. 按权值排序
    edges.sort(key=lambda x: x[2])
    
    # 2. 初始化并查集
    uf = UnionFind(n)
    
    # 3. 贪心选择
    mst_weight = 0
    edge_count = 0
    
    for u, v, w in edges:
        if uf.union(u, v):  # 如果可以合并
            mst_weight += w
            edge_count += 1
            if edge_count == n - 1:  # 已经选了 n-1 条边
                break
    
    return mst_weight


# 测试
def test_kruskal():
    # 示例图：
    #       10
    #   0 -------- 1
    #   |  \      /|
    #  6|   5\  /15|
    #   |     \/   |
    #   |     /\   |4
    #   2 -------- 3
    #       4
    
    n = 4
    edges = [
        (0, 1, 10),  # (u, v, w)
        (0, 2, 6),
        (0, 3, 5),
        (1, 3, 15),
        (2, 3, 4)
    ]
    
    result = kruskal(n, edges)
    print(f"最小生成树权值: {result}")  # 输出: 19


# 运行测试
test_kruskal()
```

```cpp
// C++：Kruskal 最小生成树
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

class UnionFind {
private:
    vector<int> parent, rank;
    
public:
    UnionFind(int n) {
        parent.resize(n);
        rank.resize(n, 0);
        for (int i = 0; i < n; i++) parent[i] = i;
    }
    
    int find(int x) {
        if (parent[x] != x) {
            parent[x] = find(parent[x]);
        }
        return parent[x];
    }
    
    // 返回是否成功合并
    bool unite(int x, int y) {
        int rx = find(x), ry = find(y);
        if (rx == ry) return false;
        
        if (rank[rx] > rank[ry]) {
            parent[ry] = rx;
        } else if (rank[rx] < rank[ry]) {
            parent[rx] = ry;
        } else {
            parent[ry] = rx;
            rank[rx]++;
        }
        return true;
    }
};

// 边结构体
struct Edge {
    int u, v, w;
    bool operator<(const Edge& other) const {
        return w < other.w;  // 按权值排序
    }
};

int kruskal(int n, vector<Edge>& edges) {
    sort(edges.begin(), edges.end());  // 按权值排序
    
    UnionFind uf(n);
    int mst_weight = 0;
    int edge_count = 0;
    
    for (const auto& e : edges) {
        if (uf.unite(e.u, e.v)) {
            mst_weight += e.w;
            edge_count++;
            if (edge_count == n - 1) break;
        }
    }
    
    return mst_weight;
}

int main() {
    int n = 4;
    vector<Edge> edges = {
        {0, 1, 10},
        {0, 2, 6},
        {0, 3, 5},
        {1, 3, 15},
        {2, 3, 4}
    };
    
    cout << "最小生成树权值: " << kruskal(n, edges) << endl;  // 输出: 19
    
    return 0;
}
```

### 4.3 其他经典应用

| 应用场景 | 说明 |
|---------|------|
| **连通分量** | 图中连通分量的个数和合并 |
| **岛屿数量** | 网格中连通陆地的数量统计 |
| **网络连接判断** | 社交网络中好友关系的传递闭包 |
| **等价类划分** | 把元素划分到不同的等价类 |
| **课程先修关系** | 判断是否能完成所有课程 |
| **编译器符号表** | 合并相同名称的全局变量 |

---

## 5️⃣ 算法导论课后重点习题解析

### 习题 21.1-2（连通分量）

**题目**：说明在执行完下列顺序的 UNION 操作后，每个结点属于哪个集合。另外给出对应的森林。

```
操作序列：MAKE-SET(1), MAKE-SET(2), MAKE-SET(3), MAKE-SET(4), 
          MAKE-SET(5), MAKE-SET(6), MAKE-SET(7), MAKE-SET(8),
          UNION(1,2), UNION(3,4), UNION(5,6), UNION(1,4),
          FIND(1), FIND(2), FIND(3), FIND(4), FIND(5), FIND(6)
```

**解答**：

```
初始状态（8个独立集合）：
1 2 3 4 5 6 7 8

UNION(1,2)：{1,2} 合并
┌───┐     ┌───┐     ┌───┐     ┌───┐
│1,2│  3  4  5  6  7  8
└───┘

UNION(3,4)：{3,4} 合并
┌───┐  ┌───┐     ┌───┐     ┌───┐
│1,2│ │3,4│  5  6  7  8
└───┘  └───┘

UNION(5,6)：{5,6} 合并
┌───┐  ┌───┐  ┌───┐     ┌───┐
│1,2│ │3,4│ │5,6│  7  8
└───┘  └───┘  └───┘

UNION(1,4)：{1,2} 和 {3,4} 合并
┌─────────────┐     ┌───┐     ┌───┐
│  1  2  3  4 │    │5,6│  7  8
└─────────────┘     └───┘

FIND 结果：
- FIND(1) → 1 (根)
- FIND(2) → 1 (通过2→1)
- FIND(3) → 1 (通过3→4→1)
- FIND(4) → 1 (通过4→1)
- FIND(5) → 5 (根)
- FIND(6) → 5 (通过6→5)

最终集合：
- 集合 {1, 2, 3, 4}，代表元素 1
- 集合 {5, 6}，代表元素 5
- 集合 {7}，代表元素 7
- 集合 {8}，代表元素 8
```

### 习题 21.2-3（按秩合并的影响）

**题目**：假设我们改变按秩合并的规则：总是把较大秩的根设为较小秩的根的父结点。证明或者反驳：这个变化仍能保持 O(m log n) 的摊销时间。

**解答**：

```
变化后的规则：
原规则：秩小的树合并到秩大的树下
新规则：秩大的树合并到秩小的树下

这个变化是有问题的！考虑以下情况：

假设我们有多个秩为 1 的树：
   ○    ○    ○    ○    ○
  / \  / \  / \  / \  / \
 ○   ○ ○   ○ ○   ○ ○   ○

依次合并：
1. 合并两棵秩为1的树（按新规则，秩大的合并到秩小的下）
   原：新的秩=2（因为把1合并到1，秩+1）
   新：把秩为1的合并到另一棵秩为1下，也是2

2. 再合并一棵秩为1的树：
   原：把1合并到2下，秩=2（不变）
   新：把2合并到1下，秩=2（不变）

实际上...让我重新分析

关键问题：新规则可能导致秩大的树深度增加

假设树A秩=2，树B秩=1
按新规则：B合并到A下

这样其实也是对的！因为是把"小"树合并到"大"树下
只是比较的基准变了

结论：这个变化仍然能保持 O(m log n)
原因：树的秩仍然表示高度的界，合并后高度不增
```

---

## ❓ FAQ：常见疑难点解答

### Q1：为什么并查集叫"不相交集合"？

**A**：因为并查集处理的数据结构要求**集合之间互不相交**。每个元素只能属于一个集合，两个集合合并后仍然是**不相交**的（只是变成了一个更大的集合）。这叫做**不相交集合的并查集**（Disjoint Sets Union-Find）。

### Q2：路径压缩和按秩合并哪个更重要？

**A**：两者结合效果最好，缺一不可！

```
实验数据（来自 CLRS）：
- 只用按秩合并：O(m log n)
- 只用路径压缩：O(m α(n))，但均摊分析复杂
- 两者结合：O(m α(n))，简洁优美

实际应用中，两者都加上代码也不复杂，建议同时使用！
```

### Q3：为什么说 α(n) "几乎是常数"？

**A**：看下面这个表格就明白了：

```
┌──────────────────────────────────────────┐
│       Ackermann 反函数 α(n) 的值           │
├──────────────────────────────────────────┤
│                                          │
│  α(1) = 0                                 │
│  α(2) = 1                                 │
│  α(3) = 2                                 │
│  α(4) = 2                                 │
│  α(5) = 2                                 │
│  ...                                      │
│  α(65536) = 3                             │
│  α(2^65536) = 4                           │
│                                          │
│  即使 n = 宇宙中原子数 (约 10^80)          │
│  α(n) ≤ 5                                 │
│                                          │
└──────────────────────────────────────────┘
```

### Q4：并查集和哈希表、平衡树的区别是什么？

**A**：

| 数据结构 | 支持的操作 | 时间复杂度 | 应用场景 |
|---------|-----------|-----------|---------|
| 并查集 | UNION, FIND | O(α(n)) ≈ O(1) | 等价类、连通分量 |
| 哈希表 | INSERT, DELETE, SEARCH | O(1) 平均 | 查找、去重 |
| 平衡树 | INSERT, DELETE, SEARCH | O(log n) | 有序查找、范围查询 |

**核心区别**：并查集专门优化了**合并**操作，而其他数据结构不支持高效的合并。

### Q5：为什么 FIND 操作返回的是"代表元素"而不是集合？

**A**：因为**代表元素（Representative）**是集合的"代言人"：

```
- 每个集合选择一个元素作为代表（通常是根结点）
- FIND(x) 返回 x 所在集合的代表
- 两个元素在同一个集合 ⟺ 它们的代表相同

好处：
1. 判断等价：只需比较代表
2. 节省空间：不需要存储完整的集合信息
3. 易于合并：只需改变代表

示例：
集合 {1, 2, 3, 4} → 代表 1
FIND(3) → 1 (3和1在同一个集合)
```

---

## 📝 本章知识点总结

```
┌─────────────────────────────────────────────────────────────┐
│                    第21章 不相交集合 知识图谱                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  基础操作                                                    │
│  ├── MAKE-SET(x)  → 创建只含x的新集合                        │
│  ├── FIND(x)      → 找到x所在集合的代表                      │
│  └── UNION(x,y)   → 合并x和y所在的两个集合                   │
│                                                             │
│  两种优化                                                    │
│  ├── 按秩合并（Union by Rank）                               │
│  │   └── 小秩树合并到大秩树下                                │
│  └── 路径压缩（Path Compression）                           │
│      └── 查找时让路径上的结点直接指向根                      │
│                                                             │
│  时间复杂度                                                  │
│  └── O(m α(n)) ≈ O(m)  几乎常数时间！                       │
│                                                             │
│  经典应用                                                    │
│  ├── 亲戚关系判定                                            │
│  ├── Kruskal 最小生成树                                      │
│  ├── 连通分量计数                                            │
│  └── 等价类划分                                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🚀 延伸学习建议

1. **LeetCode 刷题推荐**：
   - [547. 省份数量](https://leetcode.com/problems/number-of-provinces/) - 连通分量
   - [200. 岛屿数量](https://leetcode.com/problems/number-of-islands/) - 网格连通
   - [684. 冗余连接](https://leetcode.com/problems/redundant-connection/) - 检测环

2. **进阶阅读**：
   - 《算法导论》第十三章：跳表（Skip List）
   - Tarjan 的论文《Efficiency of a Good But Not Linear Set Union Algorithm》

3. **并查集的变种**：
   - 带权并查集（记录集合之间的"距离"）
   - 并查集的撤销操作（用于离线算法）
   - 增量连通性（Incremental Connectivity）

---

**🔗 本章对应 LeetCode 题目**：

| 题目 | 难度 | 链接 |
|------|------|------|
| 省份数量 | 中等 | https://leetcode.com/problems/number-of-provinces/ |
| 岛屿数量 | 中等 | https://leetcode.com/problems/number-of-islands/ |
| 冗余连接 | 中等 | https://leetcode.com/problems/redundant-connection/ |
| 连接所有点的最小费用 | 困难 | https://leetcode.com/problems/minimum-cost-to-connect-all-points/ |
| 情侣牵手 | 中等 | https://leetcode.com/problems/couples-holding-hands/ |
