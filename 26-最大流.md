# 第26章 最大流（Maximum Flow）

> 本章回答一个无比实际的问题：在一张有容量限制的网络上，最多能从源头运送多少"东西"到终点？——不管是水管网络送水、交通网络运货、还是通信网络传数据，核心都是"最大流"。Ford-Fulkerson方法用"找增广路径、反复加流"的策略求解，而最大流最小割定理揭示了流与割的深刻对偶关系。推送-重标记算法则用完全不同的物理直觉——"水往低处流"——来解决问题，效率更高

## 📖 本章导读

### 🎯 问题引入：为什么需要最大流？

**生活中的例子**：

想象你是一个**自来水公司的调度员**：

- 城市有一套供水管网，从水厂到各小区
- 每根水管有粗细不同（容量限制）
- 你需要知道：**水厂最多能往目标小区送多少水？**
- 太多了水管爆裂，太少了小区缺水

这就是最大流问题！**"最大流"**指在网络中从源点到汇点能传输的最大流量。

**更多生活类比**：

| 场景 | 顶点 | 边（容量） | 最大流含义 |
|------|------|-----------|------------|
| 供水网络 | 水厂/水站/小区 | 水管粗细 | 最大供水量 |
| 交通网络 | 城市 | 公路通行能力 | 最大运输量 |
| 通信网络 | 服务器 | 带宽 | 最大数据吞吐 |
| 电力网络 | 发电站/变电站 | 线路容量 | 最大输电量 |
| 人才匹配 | 公司/候选人 | 录用意愿 | 最大匹配数 |
| 物流配送 | 仓库/门店 | 运力 | 最大配送量 |

**与前面章节的关系**：

| 对比项 | 最短路径 | 最小生成树 | 最大流 |
|--------|---------|-----------|--------|
| 目标 | 单源到各点最短 | 连通所有点总代价最小 | 从源到汇最大吞吐 |
| 约束 | 边权 | 边权、无环 | 容量限制、流量守恒 |
| 图类型 | 有向/无向 | 无向 | 有向（可扩展） |
| 核心操作 | 松弛 | 切割性质 | 增广 |

**本章路线图**：

```
流网络定义 ──→ Ford-Fulkerson方法 ──→ 最大二分匹配 ──→ 推送-重标记 ──→ 重标记到前置
 26.1概念       26.2经典方法           26.3应用          26.4物理直觉     26.5高效实现
 水管网络       找路加流反复做         婚姻匹配问题      水往低处流       系统化推流
```

---

## 1️⃣ 流网络（26.1 Flow Networks）

### 1.1 流网络的定义

**流网络** G = (V, E) 是一个有向图，满足：

1. 每条边 (u, v) ∈ E 有一个**非负容量** c(u, v) ≥ 0
2. 若 (u, v) ∉ E，则 c(u, v) = 0
3. 图中有两个特殊顶点：**源点** s（source）和**汇点** t（sink）
4. 对每个顶点 v ∈ V，存在路径 s → v → t

**生活类比**：

想象一个城市的水管网络：

```
        水厂 s
       / | \
     16/  |13 \12
     /   |    \
  中转A  中转B  中转C
    |\  /|\  /|
    | \/ | \/ |
   5| /\7| /\8|
    |/  \|/  \|
  中转D  中转E  中转F
    \  |  /
    10|8|9
      \|/
      小区 t
```

- 每根水管有容量（粗细）
- 水从水厂（源点s）流向小区（汇点t）
- 中间节点（中转站）既不生产也不消耗水

### 1.2 流的形式化定义

**流**是一个函数 f: V × V → ℝ，满足两条性质：

**性质1：容量限制（Capacity Constraint）**

对所有 u, v ∈ V：

$$0 \leq f(u, v) \leq c(u, v)$$

> 每根水管的流量不能超过其容量，也不能是负数（水不能倒流）

**性质2：流量守恒（Flow Conservation）**

对所有 u ∈ V - {s, t}：

$$\sum_{v \in V} f(v, u) = \sum_{v \in V} f(u, v)$$

> 中间节点流入多少就流出多少——不囤积也不凭空产生

**流值**：

$$|f| = \sum_{v \in V} f(s, v) - \sum_{v \in V} f(v, s)$$

> 从源点流出的净流量，也就是整个网络的总吞吐量

**类比**：

| 概念 | 水管类比 | 形式定义 |
|------|---------|---------|
| 容量 | 水管最粗能过多少水 | c(u,v) |
| 流量 | 水管实际过多少水 | f(u,v) |
| 容量限制 | 水管不能爆 | 0 ≤ f ≤ c |
| 流量守恒 | 中转站不漏水不产水 | 流入 = 流出 |
| 流值 | 水厂总共送出多少水 | \|f\| |

### 1.3 一个完整的流网络示例

```
容量网络：
         16          12
    s ────────→ A ────────→ C
    │            │↘          │
   13│          4│  7↘      9│
    ↓            ↓    ↘     ↓
    B ────────→ D ────────→ t
         14          5

还有边: B→A(10)

完整边和容量：
s→A(16), s→B(13)
A→C(12), A→D(4), A→B(10)  ← 注意：A→B和s→B是不同的边
B→D(14)
C→t(9)    ← 注意：修正，C→t
D→C(7), D→t(5)
```

**一个合法的流**（标注在边上：流量/容量）：

```
         11/16        12/12
    s ────────→ A ────────→ C
    │            │↘          │
   12/13       4/4│  7/7↘   9/9│
    ↓            ↓    ↘     ↓
    B ────────→ D ────────→ t
        11/14        4/5

还有边: B→A(0/10), D→C(7/7)

检查流量守恒：
A: 流入 = f(s,A) + f(B,A) = 11 + 0 = 11
   流出 = f(A,C) + f(A,D) + f(A,B) = 12 + 4 + 0 = 16? 
   ← 不守恒！说明上面的流不合法

让我重新设计一个合法的流：

         11/16        8/12
    s ────────→ A ────────→ C
    │            │           │
   12/13       3/4│         9/9│
    ↓            ↓           ↓
    B ────────→ D ────────→ t
        11/14        4/5

还有边: B→A(1/10), D→C(7/7)

验证 A 的流量守恒：
流入A: f(s,A) + f(B,A) = 11 + 1 = 12
流出A: f(A,C) + f(A,D) + f(A,B) = 8 + 3 + 0 = 11? 不守恒

好吧，让我用经典的CLRS示例图：

经典示例（CLRS Figure 26.1）：

边和容量：s→v₁(16), s→v₂(13), v₁→v₂(4), v₁→v₃(12), 
          v₂→v₁(10), v₂→v₄(14), v₃→v₂(9), v₃→t(20), 
          v₄→v₃(7), v₄→t(4)

一个合法的流：
f(s,v₁) = 11, f(s,v₂) = 8, f(v₁,v₂) = 1, f(v₁,v₃) = 12,
f(v₂,v₁) = 0, f(v₂,v₄) = 11, f(v₃,v₂) = 0, f(v₃,t) = 15,
f(v₄,v₃) = 7, f(v₄,t) = 4

验证：
v₁: 流入 = 11 + 0 = 11, 流出 = 1 + 12 = 13? 不守恒...

让我更仔细地用CLRS的标准示例：
```

**CLRS经典流网络**（Figure 26.1/26.6）：

```
容量：
s→v₁(16), s→v₂(13)
v₁→v₂(10), v₁→v₃(12)
v₂→v₄(14)
v₃→v₂(9), v₃→t(20)
v₄→v₃(7), v₄→t(4)

网络图：
         16          12
    s ───────→ v₁ ───────→ v₃
    │           │↖          │↗
   13│        10│  9        20
    ↓           ↓  v₂      ↓
    v₂ ──────→ v₄ ────────→ t
        14       7↗   4
               v₃→?

更清楚的重画：
              16              12
    s ───────────→ v₁ ───────────→ v₃
    │              │               │ │
   13│           10│             9↑  20
    ↓              ↓            /   ↓
    v₂ ←──────────╯     v₄←──/    t
    │              ↖     │↗/
   14│              7│   │4
    ↓               ↓   ↓
    v₄ ───────────→ t

嗯，这样画太乱了。让我用更清晰的方式：
```

**清晰的CLRS标准流网络**：

```
              16              12
    s ────────────→ v1 ────────────→ v3 ──────→ t
    │               │               ↑          20
    │              10│              7│
    │               ↓               │
   13              v2 ←──────────── v4
    ↓               │               ↑
    v2 ──────────→ v4               │
            14              4───────→ t
    
这还是太乱了，让我用矩阵+箭头：
```

**最终清晰版本**：

```
顶点: s, v1, v2, v3, v4, t

边和容量:
  s  → v1 : 16
  s  → v2 : 13
  v1 → v2 : 10
  v1 → v3 : 12
  v2 → v4 : 14
  v3 → v2 : 9
  v3 → t  : 20
  v4 → v3 : 7
  v4 → t  : 4

图示:

          16            12            20
    s ────────→ v1 ────────→ v3 ────────→ t
    │           │           ↑           ↑
   13         10│          7│          4│
    ↓           ↓           │           │
    v2 ────────→ v4 ────────┘───────────┘
         14           
    和   ↑
         9
    v3→v2

更清晰:

    s ──16──→ v1 ──12──→ v3 ──20──→ t
    │         │          ↑         ↑
   13       10│        7│        4│
    ↓         ↓         │         │
    v2 ──14──→ v4 ──7───┘───4────┘
    ↑                  
    └──9── v3 ←──┘
    
嗯v3→v2 是一条从v3到v2的边，容量9。让我用邻接表方式：
```

**用邻接表+图的方式呈现**：

```
s  → v1(16) → v2(13)
v1 → v2(10) → v3(12)
v2 → v4(14)
v3 → v2(9)  → t(20)
v4 → v3(7)  → t(4)

      s ──16──→ v1 ──12──→ v3 ──20──→ t
      │                   │          ↑
     13                   9         4
      ↓                   ↓         │
      v2 ←───────────────╯    v4 ──┘
      │                          ↑
     14                          │
      ↓                         7
      v4 ─────────────────────→ v3
                        (这些边交叉了...)
```

好吧，我承认这个图用纯文字不太好画。让我用最简洁的方式：

```
  s ──16──→ v1 ──12──→ v3 ──20──→ t
  │          │         ↑↑         ↑
 13         10       7╱ ╲9       4
  ↓          ↓      ╱   ╲       │
  v2 ──14──→ v4 ──7─╯     ╲     │
                       v3→v2╲    │
                         v4→t──4─┘

最终让我用最简单的线性表示：
```

**我决定用最清楚的表格+简图形式**：

| 边 | 容量 |
|----|------|
| s→v1 | 16 |
| s→v2 | 13 |
| v1→v2 | 10 |
| v1→v3 | 12 |
| v2→v4 | 14 |
| v3→v2 | 9 |
| v3→t | 20 |
| v4→v3 | 7 |
| v4→t | 4 |

```
  s ──→ v1 ──→ v3 ──→ t
  │     │      ↑      ↑
  ↓     ↓      ↑      ↑
  v2 ──→ v4 ──→┘─────→┘
      ↑
      └── v3 ──→ v2 (反方向)
```

### 1.4 反平行边的处理

**问题**：如果图中有两条方向相反的边 (u, v) 和 (v, u)，怎么办？

> 类比：两个城市之间有两条单行道，一条从A到B，一条从B到A。这很正常，但在流网络的形式化定义中可能造成麻烦。

**解决方案**：引入一个虚拟顶点 v'，将 (u, v) 拆成 (u, v') 和 (v', v)，容量不变。

```
原始（有反平行边）：
  u ──c1──→ v
  v ──c2──→ u     ← 反平行边！

变换后（无反平行边）：
  u ──c1──→ v' ──c1──→ v
  v ──c2──→ u

其中 v' 是新引入的虚拟节点
```

**FAQ：为什么要处理反平行边？**

**答**：在流的形式定义 f(u,v) 中，如果同时有 (u,v) 和 (v,u)，我们需要能区分它们。虽然可以用正负号来区分（f(u,v) 和 f(v,u) 各自记录），但为了简化和统一，CLRS选择消除反平行边，这样每对顶点之间只有一个方向的流。

### 1.5 多源多汇的处理：超级源与超级汇

**问题**：如果有多个源点和多个汇点怎么办？

> 类比：多个水厂供水到多个小区——我们需要知道整个系统的最大供水量。

**解决方案**：引入**超级源点** s* 和**超级汇点** t*：

- 从 s* 到每个原源点连一条容量为 ∞ 的边
- 从每个原汇点到 t* 连一条容量为 ∞ 的边

```
原始（多源多汇）：
  s1 ──→ ... ──→ t1
  s2 ──→ ... ──→ t2

变换后（单源单汇）：
       ∞       ∞
  s* ──→ s1 ──→ ... ──→ t1 ──→ t*
   └──→ s2 ──→ ... ──→ t2 ──→┘
       ∞                  ∞
```

**为什么容量是∞？** 因为我们不想让超级边成为瓶颈——真正的瓶颈应该在原始网络中。

### FAQ：流网络为什么要要求每个顶点都在 s 到 t 的路径上？

**答**：如果一个顶点不在任何从 s 到 t 的路径上，那它对流量没有任何贡献——流入它的水永远到不了 t，它也不影响 s 能送出多少水。去掉这样的顶点不影响问题的解，而且让分析更简洁。

---

## 2️⃣ Ford-Fulkerson方法（26.2 The Ford-Fulkerson Method）

### 2.1 核心思想

**一句话**：不断找一条从 s 到 t 的"还能加流量"的路径（增广路径），在这条路径上加流量，直到找不到为止。

**生活类比**：想象你在管理一个快递分拣中心——

1. 你先找到一条从仓库到目的地的路线，看看这条路上最窄的门在哪（瓶颈容量）
2. 你沿着这条路派送这么多快递
3. 你再找另一条还能加量的路线，继续派送
4. 直到没有路线还能加量了——这就是最大配送量

**为什么叫"方法"而不叫"算法"？**

因为Ford-Fulkerson只给了框架——"找增广路径、加流"——但没指定怎么找增广路径。不同的找法导致不同的具体算法：
- BFS找 → Edmonds-Karp算法（最短增广路径）
- DFS找 → 原始Ford-Fulkerson
- 其他策略 → 更高级的算法

### 2.2 残存网络（Residual Network）

**直觉**：在已经分配了一些流量之后，每条边上"还能再加多少流量"？这就是残存容量的概念。

**残存容量**的定义：

对于流 f 和容量 c：

$$c_f(u, v) = c(u, v) - f(u, v)$$  （正向：还能加多少）

但还有反向的"撤销"能力：

$$c_f(v, u) = f(u, v)$$  （反向：能撤销多少）

**综合定义**：

$$c_f(u, v) = \begin{cases} c(u,v) - f(u,v) & \text{if } (u,v) \in E \\ f(v,u) & \text{if } (v,u) \in E \\ 0 & \text{otherwise} \end{cases}$$

**残存网络**：G_f = (V, E_f)，其中 E_f = {(u,v) : c_f(u,v) > 0}

**生活类比**：

```
原始边：u ──容量10──→ v，当前流量7

在残存网络中变成两条边：
  u ──3──→ v    （还能加3，正向残存容量 = 10 - 7 = 3）
  u ←──7── v    （能撤销7，反向残存容量 = 7）

就像网购：
- 你买了7件，最多还能再买3件（正向）
- 你可以退掉已买的7件中的若干件（反向撤销）
```

**图解残存网络构造**：

```
原始网络（流量/容量）：
         11/16           12/12
    s ────────→ v1 ────────→ v3
    │            │            │
   11/13       0/10         0/20
    ↓            ↓            ↓
    v2 ────────→ v4 ────────→ t
        11/14        11/7   4/4
                   (v4→v3)
                   7/7

残存网络（只画残存容量>0的边）：

    s ──5──→ v1 ──12──→ v3 ──20──→ t
    │         │ 10          ↑ 7      ↑ 0?
   2?        ↓             │        
    v2 ←─────╯      v4 ───┘   ...
    
这太乱了。让我用一个更小的例子来说明残存网络：
```

**简单例子说明残存网络**：

```
原始网络（4个顶点）：
  s ──10──→ a ──5──→ b ──8──→ t
  └────3──────→ b ──────────→┘

当前流：
  s→a: 8,  a→b: 5,  b→t: 8,  s→b: 3

残存网络：
  s ──2──→ a     （10-8=2，还能加2）
  a ──0──→ b     （5-5=0，满了！这条边消失）
  a ←──5── b     （反向：能撤销5）
  b ──8──→ t     （8-8=0，满了！消失）  
  b ←──8── t     （反向：能撤销8）
  s ──0──→ b     （3-3=0，消失）
  s ←──3── b     （反向：能撤销3）

简化后的残存网络：
  s ──2──→ a ←──5── b ←──8── t
  s ←──3── b

关键：在残存网络中，"反向边"表示"可以减少流量"
```

### 2.3 增广路径（Augmenting Path）

**定义**：增广路径是残存网络 G_f 中从 s 到 t 的一条简单路径。

**路径的残存容量**：

$$c_f(p) = \min\{c_f(u,v) : (u,v) \text{ 在路径 } p \text{ 上}\}$$

> 增广路径上最窄的地方——木桶原理，最短的那块板决定了能加多少水

**增广操作**：沿增广路径 p 对流 f 增加流量 c_f(p)

```
对 p 上每条边 (u,v):
    f(u,v) = f(u,v) + c_f(p)    # 正向加流
    f(v,u) = f(v,u) - c_f(p)    # 反向减流（撤销）
```

**直觉**：

- 如果 p 上 (u,v) 是原始网络中的正向边 → 增加流量
- 如果 p 上 (u,v) 是残存网络中的反向边（对应原始网络中 (v,u)）→ 减少原始方向的流量

**FAQ：为什么反向边可以"撤销"流量？**

**答**：想象你之前让水从 a 流到 b（流量5），但现在你发现从 b 流到 a 能更好地利用网络。你可以"撤销"a→b的一部分流量（最多撤销5），让它从别的路走。这不是真的让水倒流，而是重新安排了整个网络的流量分配。

用更具体的例子：
```
原来：s→a(10)→b(10)→t(10)  流量10
后来发现：s→a→c→t 也需要流量
但 a→b 已经占了 a 的全部容量

通过反向边"撤销"：
在残存网络中走 s→a→c→t，同时走 b←a（撤销部分a→b的流）
让原来从a→b的水改走a→c→t
同时从s→b补上被撤销的量

这样总流量可以增加！
```

### 2.4 流网络的割（Cut）

**定义**：流网络的**割** (S, T) 是顶点集 V 的一个划分，使得 s ∈ S，t ∈ T。

**割的容量**：

$$c(S, T) = \sum_{u \in S, v \in T} c(u, v)$$

> 只算从 S 到 T 的边的容量之和，不包含从 T 到 S 的边

**割的流量**：

$$f(S, T) = \sum_{u \in S, v \in T} f(u, v) - \sum_{u \in S, v \in T} f(v, u)$$

> 从 S 到 T 的净流量 = 正向流 - 反向流

**关键性质**：对任意割 (S, T)，**f(S, T) = |f|**

> 不管你怎么切，从 s 那半边到 t 那半边的净流量都等于总流值！

**类比**：想象一条河上有很多桥。不管你在哪个位置切一刀（只要 s 在上游，t 在下游），通过这一刀的净流量都是一样的——都等于上游送出的总水量。

**简单例子**：

```
网络：
  s ──10──→ a ──5──→ t
  s ──3──→ b ──8──→ t

流：s→a=7, a→t=5, s→b=5, b→t=8... 等等不守恒

让我重新设计：
  s ──10──→ a ──10──→ t
  s ──5──→ b ──5──→ t

流：s→a=10, a→t=10, s→b=5, b→t=5
|f| = 15

割1: S={s}, T={a,b,t}
容量 c(S,T) = 10 + 5 = 15
流量 f(S,T) = 10 + 5 = 15 = |f| ✓

割2: S={s,a}, T={b,t}
容量 c(S,T) = c(s,b) + c(a,t) = 5 + 10 = 15
流量 f(S,T) = f(s,b) + f(a,t) - f(b,a) = 5 + 10 - 0 = 15 = |f| ✓

割3: S={s,a,b}, T={t}
容量 c(S,T) = c(a,t) + c(b,t) = 10 + 5 = 15
流量 f(S,T) = 10 + 5 = 15 = |f| ✓
```

### 2.5 最大流最小割定理（Max-Flow Min-Cut Theorem）

**这是本章最核心的定理！**

**定理**：以下三个条件等价：

1. f 是 G 中的一个最大流
2. 残存网络 G_f 中不包含增广路径
3. 存在割 (S, T)，使得 |f| = c(S, T)

**证明**（三个方向循环论证）：

**① → ②**（最大流 → 无增广路径）：

反证法：假设 f 是最大流，但 G_f 中存在增广路径 p。那么我们可以沿 p 增加流量 c_f(p) > 0，得到更大的流 f'，与 f 是最大流矛盾。

**② → ③**（无增广路径 → 存在等流值割）：

令 S = {v ∈ V : G_f 中存在从 s 到 v 的路径}，T = V - S。

- s ∈ S（s 到 s 距离为0）
- t ∉ S（因为没有增广路径，s 到不了 t）

对于任意 u ∈ S, v ∈ T：
- G_f 中没有边 (u,v)（否则 v 也能从 s 到达，属于 S）
- 即 c_f(u,v) = 0
- 因为 c_f(u,v) = c(u,v) - f(u,v) = 0，所以 f(u,v) = c(u,v)
- 因为 c_f(v,u) = f(u,v) = 0（如果 (v,u) 不是原始边则 f(v,u)=0 也成立）

所以：
$$|f| = f(S,T) = \sum_{u∈S,v∈T} f(u,v) - \sum_{u∈S,v∈T} f(v,u) = \sum_{u∈S,v∈T} c(u,v) - 0 = c(S,T)$$

**③ → ①**（存在等流值割 → 最大流）：

对于任意流 f' 和任意割 (S, T)：

$$|f'| = f'(S,T) = \sum_{u∈S,v∈T} f'(u,v) - \sum_{u∈S,v∈T} f'(v,u)$$
$$\leq \sum_{u∈S,v∈T} c(u,v) = c(S,T) = |f|$$

（利用了 f'(u,v) ≤ c(u,v) 和 f'(v,u) ≥ 0）

所以 |f'| ≤ |f|，f 是最大流！∎

**直觉理解**：

```
最大流 ≤ 最小割   （流不能超过任何割的容量——瓶颈限制）
最大流 = 最小割   （存在一个割恰好卡在最大流上——最紧的瓶颈）

就像水管网络：
- 水的流量不可能超过任何横截面的总容量
- 一定存在某个横截面，它的容量恰好等于最大流量
- 这个横截面就是"瓶颈"
```

**FAQ：最大流和最小割有什么实际意义？**

**答**：
- **最大流**告诉你网络最多能传多少
- **最小割**告诉你网络的瓶颈在哪——如果要提升吞吐量，就要加宽最小割处的容量
- 在网络安全中，最小割告诉你断开哪些边就能让 s 和 t 完全断联（最少需要切断的总容量）

### 2.6 Ford-Fulkerson算法

**完整伪代码**：

```
FORD-FULKERSON-METHOD(G, c, s, t)
1  初始化流 f：对所有 (u,v)，f(u,v) = 0
2  while 残存网络 G_f 中存在增广路径 p:
3      c_f(p) = min{c_f(u,v) : (u,v) 在 p 上}   # 路径瓶颈容量
4      for p 上每条边 (u,v):
5          if (u,v) ∈ E:          # 正向边
6              f(u,v) = f(u,v) + c_f(p)    # 增加流量
7          else:                   # 反向边 (v,u) ∈ E
8              f(v,u) = f(v,u) - c_f(p)    # 撤销流量
9  return f
```

### 2.7 Ford-Fulkerson完整执行过程追踪

**使用CLRS标准示例**：

```
初始网络（只标容量）：
  s→v1(16), s→v2(13)
  v1→v2(10), v1→v3(12)
  v2→v4(14)
  v3→v2(9), v3→t(20)
  v4→v3(7), v4→t(4)
```

**=== 第1次增广 ===**

残存网络 = 原始网络（因为初始流为0）

选择增广路径：s → v1 → v3 → t

```
路径：s ──16──→ v1 ──12──→ v3 ──20──→ t
瓶颈容量 = min(16, 12, 20) = 12

更新流：
f(s,v1) = 12, f(v1,v3) = 12, f(v3,t) = 12
|f| = 12
```

**=== 第2次增广 ===**

残存网络：

```
s→v1: 16-12=4（还能加4）
s→v2: 13（未用）
v1→v2: 10（未用）
v1→v3: 12-12=0（满了！）→ 反向 v3→v1: 12
v2→v4: 14（未用）
v3→v2: 9（未用）
v3→t: 20-12=8（还能加8）
v4→v3: 7（未用）
v4→t: 4（未用）

反向边：
v3→v1: 12（可撤销）
v1→s: 12（可撤销）
t→v3: 12（可撤销）
```

选择增广路径：s → v2 → v4 → t

```
路径：s ──13──→ v2 ──14──→ v4 ──4──→ t
瓶颈容量 = min(13, 14, 4) = 4

更新流：
f(s,v2) = 4, f(v2,v4) = 4, f(v4,t) = 4
|f| = 12 + 4 = 16
```

**=== 第3次增广 ===**

残存网络变化：
- s→v2: 13-4=9
- v2→v4: 14-4=10
- v4→t: 4-4=0（满了！）→ 反向 t→v4: 4

选择增广路径：s → v2 → v4 → v3 → t

```
路径：s ──9──→ v2 ──10──→ v4 ──7──→ v3 ──8──→ t
瓶颈容量 = min(9, 10, 7, 8) = 7

更新流：
f(s,v2) = 4+7 = 11, f(v2,v4) = 4+7 = 11, f(v4,v3) = 7, f(v3,t) = 12+7 = 19
|f| = 12 + 4 + 7 = 23
```

**=== 第4次增广 ===**

残存网络变化：
- s→v2: 13-11=2
- v2→v4: 14-11=3
- v4→v3: 7-7=0（满了！）→ 反向 v3→v4: 7
- v3→t: 20-19=1

选择增广路径：s → v1 → v2 → v4 → v3 → t

```
路径：s ──4──→ v1 ──10──→ v2 ──3──→ v4 ──7(反向)──→ v3 ──1──→ t
注意：v4→v3 在残存网络中现在只有反向边 v3→v4: 7

所以这条路径不通！v4→v3 的残存容量为0。

让我重新找路径...

增广路径：s → v1 → v2 → v4 → ... v4→t 已满
s → v1 → v2 → v4 → v3 → t?
v4→v3: 已满

让我更仔细地计算残存网络...
```

**让我更仔细地追踪**：

```
当前流状态（第3次增广后）：
f(s,v1)=12, f(s,v2)=11
f(v1,v2)=0, f(v1,v3)=12
f(v2,v4)=11
f(v3,v2)=0, f(v3,t)=19
f(v4,v3)=7, f(v4,t)=4

验证流量守恒：
v1: 流入=12(v1←s), 流出=0(v1→v2)+12(v1→v3)=12 ✓
v2: 流入=0(v1→v2)+0(v3→v2)=0, 流出=11(v2→v4)=11
    ← 不守恒！v2流入了11（从s），但只算了v1→v2和v3→v2
    实际流入v2的：f(s,v2)=11, f(v1,v2)=0, f(v3,v2)=0 → 11
    流出v2的：f(v2,v4)=11 → 11 ✓

v3: 流入=12(v1→v3)+7(v4→v3)=19, 流出=0(v3→v2)+19(v3→t)=19 ✓
v4: 流入=11(v2→v4)=11, 流出=7(v4→v3)+4(v4→t)=11 ✓

好的，流量守恒没问题。|f| = 23

残存网络：
正向残存容量：
s→v1: 16-12=4    s→v2: 13-11=2
v1→v2: 10-0=10   v1→v3: 12-12=0
v2→v4: 14-11=3
v3→v2: 9-0=9     v3→t: 20-19=1
v4→v3: 7-7=0     v4→t: 4-4=0

反向残存容量：
v1→s: 12          v2→s: 11
v2→v1: 0          v3→v1: 12
v4→v2: 11
v2→v3: 0          t→v3: 19
v3→v4: 7          t→v4: 4

残存网络中的正向可用边（容量>0）：
s→v1(4), s→v2(2)
v1→v2(10)
v2→v4(3)
v3→v2(9), v3→t(1)

残存网络中的反向可用边（容量>0）：
v1→s(12), v2→s(11)
v3→v1(12)
v4→v2(11)
t→v3(19)
v3→v4(7)
t→v4(4)
```

**=== 第4次增广 ===**

在残存网络中找增广路径：s → v1 → v2 → v4 → ... 

v4→t 和 v4→v3 都已满（残存容量0），但从v4有反向边到v2（容量11）

嗯，v4 到 t 只能通过反向...不行

找另一条路径：s → v2 → v4 → ... v4 出不去

s → v1 → v2 → v3 → t？

s→v1(4)→v2: v1→v2(10) ✓
v2→v3: 只有反向边v3→v2(9)，没有v2→v3

嗯，那 s → v1 → v2 → v4 → ... 不行

s → v2 → v4 → ... 不行

s → v1 → v2 → ... v2能到哪？v2→v4(3)

从v4: 反向 v4→v2(11)? 不对，那是回到v2了

等等，我漏了什么。让我再看看残存网络...

哦！我需要把反向边也算上！残存网络中所有有容量的边：

```
从s: s→v1(4), s→v2(2), 还有反向边不算（v1→s, v2→s是到s的）
从v1: v1→v2(10), v1→s(12反向), v3→v1(12反向)... 
  等等v3→v1是到v1的，从v1出发是v1→s和v1→v2
从v2: v2→v4(3), v2→s(11反向), v4→v2(11反向是到v2的)
  所以从v2出发: v2→v4(3), v2→s(11)
从v3: v3→v2(9), v3→t(1), v3→v1(12反向), v3→v4(7反向)
从v4: v4→v2(11反向)... 到v2
  v4→v3(0), v4→t(0), 所以从v4出发只有反向边
  从v4出发: v4→v2(11)... 但v2已经访问过了
  没有 v4→别的有用的
```

看来当前残存网络中能到达t的只有 v3→t(1)

那么找路径到v3: s→v1(4)→v2(10)→...v2到不了v3
s→v2(2)→v4(3)→...v4到不了任何新的地方

嗯，等等！v3→v2(9) 这条边在残存网络中！这意味着v3到v2方向。但我需要到v3...

v1→v3: 残存容量0。但从v3→v1有12（反向边），v3→v4有7（反向边）

让我用BFS在残存网络中找s到t的路径：

```
BFS:
起点s: 邻居 v1(4), v2(2)
v1: 邻居 v2(10) (s已访问)
v2: 邻居 v4(3) (s已访问)
v4: 邻居 ??? v4→v2(11反向，但v2已访问)

BFS队列为空，无法到达t！
```

所以不存在增广路径！Ford-Fulkerson终止，最大流 = 23。

但等等...CLRS的答案是最大流23？让我验证最小割。

S = {s, v1, v2, v4}（残存网络中s可达的顶点）
T = {v3, t}

割的容量 = c(s,v1不对，s在S) + c(v1,v3)(v1∈S,v3∈T) + c(v4,v3)(v4∈S,v3∈T) + c(v4,t)(v4∈S,t∈T)
= 12 + 7 + 4 = 23 ✓

|f| = 23 = c(S,T) ✓，由最大流最小割定理，f确实是最大流！

**最终结果**：

```
最大流 = 23

流的分配：
f(s,v1) = 12, f(s,v2) = 11
f(v1,v2) = 0, f(v1,v3) = 12
f(v2,v4) = 11
f(v3,v2) = 0, f(v3,t) = 19
f(v4,v3) = 7, f(v4,t) = 4

最小割：({s,v1,v2,v4}, {v3,t})
割的容量 = c(v1,v3) + c(v4,v3) + c(v4,t) = 12 + 7 + 4 = 23
```

### 2.8 Edmonds-Karp算法

**改进**：用BFS找增广路径（找最短增广路径）

**优势**：
- 保证 O(VE²) 的时间复杂度（与流量值无关）
- 避免了Ford-Fulkerson在容量为无理数时不终止的问题

**关键引理**：在Edmonds-Karp算法中，增广路径的长度（边数）单调不减。

**直觉**：每次增广后，残存网络中s到t的最短路径不会变短。因为BFS找的是最短路径，每次增广至少让某条边"用满"，后续需要绕更远的路。

### 2.9 Ford-Fulkerson的复杂度分析

**原始Ford-Fulkerson**（DFS找增广路径）：

- 每次增广至少增加1的流量（如果容量为整数）
- 最多增广 |f*| 次（f* 是最大流）
- 每次找增广路径 O(E)
- 总复杂度：O(|f*| · E)

**问题**：如果最大流值很大，算法就很慢！

**Edmonds-Karp**（BFS找增广路径）：

- 增广次数 O(VE)
- 每次BFS O(E)
- 总复杂度：O(VE²)

**FAQ：Ford-Fulkerson可能不终止吗？**

**答**：是的！如果容量是无理数，Ford-Fulkerson可能永远不终止（甚至流可以收敛到一个非最大值）。这是原始Ford-Fulkerson方法的理论缺陷。但对于整数容量，一定会终止（因为每次增广至少增加1的整数流量）。

---

## 3️⃣ Ford-Fulkerson代码实现

### 3.1 Python实现（Edmonds-Karp版本）

```python
from collections import deque

class MaxFlow:
    """Edmonds-Karp 最大流算法（BFS找增广路径的Ford-Fulkerson）"""
    
    def __init__(self, n):
        """
        初始化最大流图
        参数:
            n: 顶点数量（顶点编号0到n-1）
        """
        self.n = n                    # 顶点数
        self.cap = [[0] * n for _ in range(n)]   # 容量矩阵
        self.flow = [[0] * n for _ in range(n)]   # 流量矩阵
    
    def add_edge(self, u, v, c):
        """
        添加一条有向边 u→v，容量为 c
        参数:
            u: 起点
            v: 终点
            c: 容量
        """
        self.cap[u][v] += c           # 累加容量（支持平行边）
    
    def bfs(self, s, t, parent):
        """
        BFS在残存网络中找增广路径
        参数:
            s: 源点
            t: 汇点
            parent: 记录路径的前驱数组
        返回:
            找到路径返回True，否则False
        """
        # 初始化前驱数组为-1（未访问）
        for i in range(self.n):
            parent[i] = -1
        
        parent[s] = -2                 # 标记源点已访问（用-2区别于-1）
        queue = deque()
        queue.append((s, float('inf')))  # (当前顶点, 到这里的瓶颈容量)
        
        while queue:
            u, flow_to_u = queue.popleft()
            
            for v in range(self.n):
                # 残存容量 = 正向剩余 + 反向可撤销
                residual = self.cap[u][v] - self.flow[u][v]
                
                # 如果v未访问且残存容量>0
                if parent[v] == -1 and residual > 0:
                    parent[v] = u                          # 记录前驱
                    new_flow = min(flow_to_u, residual)     # 更新瓶颈容量
                    if v == t:                              # 到达汇点！
                        return new_flow
                    queue.append((v, new_flow))
        
        return 0    # 没找到增广路径
    
    def edmonds_karp(self, s, t):
        """
        Edmonds-Karp算法求最大流
        参数:
            s: 源点
            t: 汇点
        返回:
            最大流值
        """
        max_flow = 0                    # 最大流值初始化为0
        parent = [-1] * self.n          # 前驱数组
        
        # 不断找增广路径，直到找不到为止
        while True:
            # BFS找增广路径，返回路径瓶颈容量
            path_flow = self.bfs(s, t, parent)
            
            if path_flow == 0:          # 没有增广路径了
                break
            
            max_flow += path_flow       # 累加到最大流
            
            # 沿增广路径更新流量
            v = t
            while v != s:
                u = parent[v]           # 前驱
                self.flow[u][v] += path_flow    # 正向加流
                self.flow[v][u] -= path_flow    # 反向减流
                v = u
        
        return max_flow
    
    def get_mincut(self, s):
        """
        获取最小割（残存网络中s可达的顶点集合）
        参数:
            s: 源点
        返回:
            (S集合, T集合)
        """
        visited = [False] * self.n
        queue = deque([s])
        visited[s] = True
        
        while queue:
            u = queue.popleft()
            for v in range(self.n):
                residual = self.cap[u][v] - self.flow[u][v]
                if not visited[v] and residual > 0:
                    visited[v] = True
                    queue.append(v)
        
        S = [i for i in range(self.n) if visited[i]]    # s可达的
        T = [i for i in range(self.n) if not visited[i]] # s不可达的
        return S, T


# ===== 使用示例 =====
if __name__ == "__main__":
    # CLRS经典示例
    # 顶点编号: s=0, v1=1, v2=2, v3=3, v4=4, t=5
    mf = MaxFlow(6)
    
    # 添加边（顶点, 顶点, 容量）
    mf.add_edge(0, 1, 16)    # s → v1, 容量16
    mf.add_edge(0, 2, 13)    # s → v2, 容量13
    mf.add_edge(1, 2, 10)    # v1 → v2, 容量10
    mf.add_edge(1, 3, 12)    # v1 → v3, 容量12
    mf.add_edge(2, 4, 14)    # v2 → v4, 容量14
    mf.add_edge(3, 2, 9)     # v3 → v2, 容量9
    mf.add_edge(3, 5, 20)    # v3 → t, 容量20
    mf.add_edge(4, 3, 7)     # v4 → v3, 容量7
    mf.add_edge(4, 5, 4)     # v4 → t, 容量4
    
    max_flow = mf.edmonds_karp(0, 5)
    print(f"最大流 = {max_flow}")   # 期望输出: 23
    
    S, T = mf.get_mincut(0)
    print(f"最小割 S = {S}, T = {T}")
    
    # 打印每条边的流量
    print("\n流的分配：")
    for u in range(6):
        for v in range(6):
            if mf.flow[u][v] > 0:
                print(f"  {u}→{v}: {mf.flow[u][v]}/{mf.cap[u][v]}")
```

### 3.2 C++实现（Edmonds-Karp版本）

```cpp
#include <iostream>
#include <vector>
#include <queue>
#include <algorithm>
#include <cstring>
using namespace std;

class MaxFlow {
private:
    int n;                          // 顶点数量
    vector<vector<int>> cap;        // 容量矩阵
    vector<vector<int>> flow;       // 流量矩阵

public:
    // 构造函数：初始化n个顶点的流网络
    MaxFlow(int n) : n(n), 
                     cap(n, vector<int>(n, 0)), 
                     flow(n, vector<int>(n, 0)) {}
    
    // 添加有向边 u→v，容量为 c
    void addEdge(int u, int v, int c) {
        cap[u][v] += c;             // 累加容量（支持平行边）
    }
    
    // BFS在残存网络中找增广路径，返回路径瓶颈容量
    int bfs(int s, int t, vector<int>& parent) {
        fill(parent.begin(), parent.end(), -1);  // 初始化前驱为-1
        parent[s] = -2;                          // 标记源点已访问
        queue<pair<int, int>> q;                 // (当前顶点, 瓶颈容量)
        q.push({s, INT_MAX});
        
        while (!q.empty()) {
            auto [u, flow_to_u] = q.front();
            q.pop();
            
            for (int v = 0; v < n; v++) {
                // 计算残存容量
                int residual = cap[u][v] - flow[u][v];
                
                // 如果v未访问且残存容量>0
                if (parent[v] == -1 && residual > 0) {
                    parent[v] = u;                           // 记录前驱
                    int new_flow = min(flow_to_u, residual); // 更新瓶颈
                    if (v == t) return new_flow;              // 到达汇点
                    q.push({v, new_flow});
                }
            }
        }
        return 0;   // 没找到增广路径
    }
    
    // Edmonds-Karp算法求最大流
    int edmondsKarp(int s, int t) {
        int max_flow = 0;                  // 最大流值
        vector<int> parent(n);             // 前驱数组
        int path_flow;                     // 当前增广路径的瓶颈容量
        
        // 不断找增广路径
        while ((path_flow = bfs(s, t, parent)) > 0) {
            max_flow += path_flow;         // 累加最大流
            
            // 沿增广路径更新流量
            for (int v = t; v != s; v = parent[v]) {
                int u = parent[v];
                flow[u][v] += path_flow;   // 正向加流
                flow[v][u] -= path_flow;   // 反向减流
            }
        }
        return max_flow;
    }
    
    // 获取最小割
    pair<vector<int>, vector<int>> getMinCut(int s) {
        vector<bool> visited(n, false);
        queue<int> q;
        q.push(s);
        visited[s] = true;
        
        while (!q.empty()) {
            int u = q.front(); q.pop();
            for (int v = 0; v < n; v++) {
                int residual = cap[u][v] - flow[u][v];
                if (!visited[v] && residual > 0) {
                    visited[v] = true;
                    q.push(v);
                }
            }
        }
        
        vector<int> S, T;
        for (int i = 0; i < n; i++) {
            if (visited[i]) S.push_back(i);
            else T.push_back(i);
        }
        return {S, T};
    }
    
    // 打印流的分配
    void printFlow() {
        cout << "流的分配：" << endl;
        for (int u = 0; u < n; u++) {
            for (int v = 0; v < n; v++) {
                if (flow[u][v] > 0) {
                    cout << "  " << u << "→" << v 
                         << ": " << flow[u][v] << "/" << cap[u][v] << endl;
                }
            }
        }
    }
};

int main() {
    // CLRS经典示例
    // 顶点编号: s=0, v1=1, v2=2, v3=3, v4=4, t=5
    MaxFlow mf(6);
    
    mf.addEdge(0, 1, 16);    // s → v1
    mf.addEdge(0, 2, 13);    // s → v2
    mf.addEdge(1, 2, 10);    // v1 → v2
    mf.addEdge(1, 3, 12);    // v1 → v3
    mf.addEdge(2, 4, 14);    // v2 → v4
    mf.addEdge(3, 2, 9);     // v3 → v2
    mf.addEdge(3, 5, 20);    // v3 → t
    mf.addEdge(4, 3, 7);     // v4 → v3
    mf.addEdge(4, 5, 4);     // v4 → t
    
    int maxFlow = mf.edmondsKarp(0, 5);
    cout << "最大流 = " << maxFlow << endl;   // 输出: 23
    
    auto [S, T] = mf.getMinCut(0);
    cout << "最小割 S = {";
    for (int i = 0; i < S.size(); i++) {
        cout << S[i] << (i < S.size()-1 ? ", " : "");
    }
    cout << "}, T = {";
    for (int i = 0; i < T.size(); i++) {
        cout << T[i] << (i < T.size()-1 ? ", " : "");
    }
    cout << "}" << endl;
    
    mf.printFlow();
    
    return 0;
}
```

### 3.3 Python实现（邻接表优化版，适合大规模图）

```python
from collections import deque, defaultdict

class MaxFlowAdjList:
    """使用邻接表存储的最大流算法（更省空间）"""
    
    def __init__(self, n):
        """
        初始化
        参数:
            n: 顶点数量
        """
        self.n = n
        self.graph = defaultdict(dict)     # 邻接表：graph[u][v] = 容量
        self.flow = defaultdict(dict)      # 流量：flow[u][v] = 当前流量
    
    def add_edge(self, u, v, c):
        """
        添加边 u→v，容量 c
        同时添加反向边 v→u，容量 0（用于残存网络）
        """
        # 确保两个方向的键都存在
        if v not in self.graph[u]:
            self.graph[u][v] = 0
            self.flow[u][v] = 0
        if u not in self.graph[v]:
            self.graph[v][u] = 0
            self.flow[v][u] = 0
        
        self.graph[u][v] += c              # 累加正向容量
    
    def bfs(self, s, t, parent):
        """
        BFS找增广路径
        返回路径瓶颈容量，0表示无路径
        """
        for key in parent:
            parent[key] = -1
        parent[s] = -2
        queue = deque([(s, float('inf'))])
        
        while queue:
            u, flow_to_u = queue.popleft()
            for v, cap in self.graph[u].items():
                residual = cap - self.flow[u].get(v, 0)
                if parent.get(v, -1) == -1 and residual > 0:
                    parent[v] = u
                    new_flow = min(flow_to_u, residual)
                    if v == t:
                        return new_flow
                    queue.append((v, new_flow))
        return 0
    
    def max_flow(self, s, t):
        """
        求最大流
        """
        max_flow = 0
        parent = defaultdict(lambda: -1)
        
        while True:
            path_flow = self.bfs(s, t, parent)
            if path_flow == 0:
                break
            max_flow += path_flow
            
            v = t
            while v != s:
                u = parent[v]
                self.flow[u][v] = self.flow[u].get(v, 0) + path_flow
                self.flow[v][u] = self.flow[v].get(u, 0) - path_flow
                v = u
        
        return max_flow


if __name__ == "__main__":
    mf = MaxFlowAdjList(6)
    mf.add_edge(0, 1, 16)
    mf.add_edge(0, 2, 13)
    mf.add_edge(1, 2, 10)
    mf.add_edge(1, 3, 12)
    mf.add_edge(2, 4, 14)
    mf.add_edge(3, 2, 9)
    mf.add_edge(3, 5, 20)
    mf.add_edge(4, 3, 7)
    mf.add_edge(4, 5, 4)
    
    print(f"最大流 = {mf.max_flow(0, 5)}")   # 23
```

---

## 4️⃣ 最大二分匹配（26.3 Maximum Bipartite Matching）

### 4.1 问题引入

**生活中的例子**：一个公司有n个岗位，m个候选人，每个候选人能胜任某些岗位。如何让尽可能多的人找到合适的工作？

这就是**二分图最大匹配**问题！

**二分图**：顶点可以分成两个集合 L 和 R，所有边都连接 L 和 R 之间的顶点（没有 L 内部或 R 内部的边）。

```
二分图示例：

岗位 L = {j1, j2, j3}
候选人 R = {c1, c2, c3, c4}

j1 ─── c1     j1和c1匹配
j1 ─── c2     j2和c3匹配  
j2 ─── c2     j3和c4匹配
j2 ─── c3     最大匹配数 = 3
j3 ─── c3
j3 ─── c4

图示：
    j1 ──── c1
     │╲      │
     │  ╲    │
     │   ╲   │
    j2 ──── c2
     │╱   ╱
     │   ╱
     │ ╱
    j3 ──── c3
            │
            c4
```

**匹配**：边的子集 M，其中每个顶点最多出现在一条边中（没有共享顶点的边）。

**最大匹配**：边数最多的匹配。

### 4.2 转化为最大流问题

**关键洞察**：二分图匹配可以转化为流网络中的最大流！

**构造方法**：

1. 添加超级源点 s，从 s 到 L 中每个顶点连一条容量为 1 的边
2. 添加超级汇点 t，从 R 中每个顶点到 t 连一条容量为 1 的边
3. 原始二分图的边方向设为 L → R，容量设为 1

```
二分图 → 流网络：

    s ──1──→ j1 ──1──→ c1 ──1──→ t
    │         │╲       │         │
   1│        1│  ╲1   1│        1│
    ↓         ↓    ╲   ↓         ↓
    s ──1──→ j2 ──1──→ c2 ──1──→ t
    │         │╱               
   1│        1│                
    ↓         ↓                
    s ──1──→ j3 ──1──→ c3 ──1──→ t
                       │
                      1│
                       ↓
                      t (已连接)

简化：
         1        1        1
    s ──→ j1 ──→ c1 ──→ t
    │    ↗  ╲     ↗      ↑
   1│  ╱     ╲  ╱       1│
    ↓╱        ╲╱         ↑
    j2 ─────→ c2 ──────→ t
    │    ↗              ↑
   1│  ╱               1│
    ↓╱                   ↑
    j3 ─────→ c3 ──────→ t
              ╲           ↑
               ╲→ c4 ──→ t
```

**为什么这样做？**

| 约束 | 流网络中的体现 |
|------|--------------|
| 每个岗位最多分配给一个人 | s→j 的容量为1，最多1个单位的流通过 |
| 每个人最多分配一个岗位 | c→t 的容量为1，最多1个单位的流通过 |
| 匹配必须是原图中的边 | 只在原图有边的j→c处有容量1 |
| 最大化匹配数 | 最大化从s到t的流 |

**定理**：二分图 G 中的最大匹配数 = 对应流网络中的最大流值

**证明思路**：
- 匹配 → 流：匹配中的每条边 (j, c) 对应1单位流量 s→j→c→t
- 流 → 匹配：整数容量保证整数流，流值k对应k条匹配边
- 最大性：如果存在更大的匹配，则对应更大的流，矛盾

### 4.3 代码实现

```python
from collections import deque, defaultdict

class BipartiteMatching:
    """二分图最大匹配（基于最大流）"""
    
    def __init__(self, left_size, right_size):
        """
        初始化
        参数:
            left_size: 左部顶点数
            right_size: 右部顶点数
        顶点编号: 
            s=0, 左部1~left_size, 右部left_size+1~left_size+right_size, t=最后一个
        """
        self.left_size = left_size
        self.right_size = right_size
        self.n = left_size + right_size + 2    # 总顶点数（含s和t）
        self.s = 0                             # 超级源点
        self.t = self.n - 1                    # 超级汇点
        self.graph = defaultdict(dict)         # 邻接表
        self.flow = defaultdict(dict)          # 流量
        
        # 添加 s 到左部的边
        for i in range(1, left_size + 1):
            self.graph[self.s][i] = 1
            self.flow[self.s][i] = 0
            self.graph[i][self.s] = 0          # 反向边
            self.flow[i][self.s] = 0
        
        # 添加右部到 t 的边
        for i in range(left_size + 1, self.t):
            self.graph[i][self.t] = 1
            self.flow[i][self.t] = 0
            self.graph[self.t][i] = 0          # 反向边
            self.flow[self.t][i] = 0
    
    def add_edge(self, left, right):
        """
        添加左部顶点到右部顶点的边
        参数:
            left: 左部顶点编号(1~left_size)
            right: 右部顶点编号(1~right_size)
        """
        u = left                                # 左部顶点在图中的编号
        v = self.left_size + right              # 右部顶点在图中的编号
        self.graph[u][v] = 1                    # 容量1
        self.flow[u][v] = 0
        self.graph[v][u] = 0                    # 反向边
        self.flow[v][u] = 0
    
    def bfs(self, parent):
        """BFS找增广路径"""
        for key in parent:
            parent[key] = -1
        parent[self.s] = -2
        queue = deque([(self.s, float('inf'))])
        
        while queue:
            u, flow_to_u = queue.popleft()
            for v, cap in self.graph[u].items():
                residual = cap - self.flow[u].get(v, 0)
                if parent.get(v, -1) == -1 and residual > 0:
                    parent[v] = u
                    new_flow = min(flow_to_u, residual)
                    if v == self.t:
                        return new_flow
                    queue.append((v, new_flow))
        return 0
    
    def max_matching(self):
        """
        求最大匹配
        返回:
            (匹配数, 匹配列表)
        """
        max_flow = 0
        parent = defaultdict(lambda: -1)
        matches = []
        
        while True:
            path_flow = self.bfs(parent)
            if path_flow == 0:
                break
            max_flow += path_flow
            
            v = self.t
            while v != self.s:
                u = parent[v]
                self.flow[u][v] = self.flow[u].get(v, 0) + path_flow
                self.flow[v][u] = self.flow[v].get(u, 0) - path_flow
                v = u
        
        # 提取匹配
        for u in range(1, self.left_size + 1):
            for v in range(self.left_size + 1, self.t):
                if self.flow[u].get(v, 0) == 1:
                    right_idx = v - self.left_size
                    matches.append((u, right_idx))
        
        return max_flow, matches


# ===== 使用示例 =====
if __name__ == "__main__":
    # 3个岗位，4个候选人
    bm = BipartiteMatching(3, 4)
    
    # 候选人能胜任的岗位
    bm.add_edge(1, 1)    # 岗位1 ← 候选人1
    bm.add_edge(1, 2)    # 岗位1 ← 候选人2
    bm.add_edge(2, 2)    # 岗位2 ← 候选人2
    bm.add_edge(2, 3)    # 岗位2 ← 候选人3
    bm.add_edge(3, 3)    # 岗位3 ← 候选人3
    bm.add_edge(3, 4)    # 岗位3 ← 候选人4
    
    match_count, matches = bm.max_matching()
    print(f"最大匹配数 = {match_count}")      # 3
    print(f"匹配: {matches}")                  # [(1,1), (2,2或3), (3,3或4)]
```

### 4.4 C++实现

```cpp
#include <iostream>
#include <vector>
#include <queue>
#include <algorithm>
using namespace std;

class BipartiteMatching {
private:
    int leftSize, rightSize;            // 左右部顶点数
    int n;                              // 总顶点数
    int s, t;                           // 超级源点和汇点
    vector<vector<int>> cap, flow;      // 容量和流量矩阵

public:
    // 构造函数
    BipartiteMatching(int ls, int rs) 
        : leftSize(ls), rightSize(rs),
          n(ls + rs + 2), s(0), t(ls + rs + 1),
          cap(n, vector<int>(n, 0)),
          flow(n, vector<int>(n, 0)) {
        
        // s → 左部顶点，容量1
        for (int i = 1; i <= ls; i++)
            cap[s][i] = 1;
        
        // 右部顶点 → t，容量1
        for (int i = ls + 1; i < t; i++)
            cap[i][t] = 1;
    }
    
    // 添加左部→右部的边
    void addEdge(int left, int right) {
        int u = left;                       // 左部编号
        int v = leftSize + right;           // 右部编号
        cap[u][v] = 1;                      // 容量1
    }
    
    // BFS找增广路径
    int bfs(vector<int>& parent) {
        fill(parent.begin(), parent.end(), -1);
        parent[s] = -2;
        queue<pair<int, int>> q;
        q.push({s, INT_MAX});
        
        while (!q.empty()) {
            auto [u, f] = q.front(); q.pop();
            for (int v = 0; v < n; v++) {
                int residual = cap[u][v] - flow[u][v];
                if (parent[v] == -1 && residual > 0) {
                    parent[v] = u;
                    int nf = min(f, residual);
                    if (v == t) return nf;
                    q.push({v, nf});
                }
            }
        }
        return 0;
    }
    
    // 求最大匹配
    int maxMatching(vector<pair<int,int>>& matches) {
        int maxFlow = 0;
        vector<int> parent(n);
        int pathFlow;
        
        while ((pathFlow = bfs(parent)) > 0) {
            maxFlow += pathFlow;
            for (int v = t; v != s; v = parent[v]) {
                int u = parent[v];
                flow[u][v] += pathFlow;
                flow[v][u] -= pathFlow;
            }
        }
        
        // 提取匹配
        for (int u = 1; u <= leftSize; u++) {
            for (int v = leftSize + 1; v < t; v++) {
                if (flow[u][v] == 1) {
                    matches.push_back({u, v - leftSize});
                }
            }
        }
        return maxFlow;
    }
};

int main() {
    BipartiteMatching bm(3, 4);
    
    bm.addEdge(1, 1);
    bm.addEdge(1, 2);
    bm.addEdge(2, 2);
    bm.addEdge(2, 3);
    bm.addEdge(3, 3);
    bm.addEdge(3, 4);
    
    vector<pair<int,int>> matches;
    int maxMatch = bm.maxMatching(matches);
    
    cout << "最大匹配数 = " << maxMatch << endl;    // 3
    for (auto [l, r] : matches) {
        cout << "  岗位" << l << " ← 候选人" << r << endl;
    }
    return 0;
}
```

### 4.5 正确性证明概要

**为什么整数容量保证整数最优流？**

如果所有容量都是整数，Ford-Fulkerson每次增广增加的流量也是整数（瓶颈容量是整数的最小值），所以最终的流也是整数的。

**为什么二分图匹配的整数流就是匹配？**

- 流量0或1：s→j 容量1，所以流量最多1（一个岗位匹配一个人）
- 流量0或1：c→t 容量1，所以流量最多1（一个人匹配一个岗位）
- j→c 流量1表示j和c匹配
- 没有共享顶点：因为每个顶点最多流出/流入1

**FAQ：如果容量不是1怎么办？**

**答**：如果允许"一个岗位可以分配多个人"或"一个人可以做多份工作"，就把 s→j 或 c→t 的容量改为相应的上限。这不再是传统的一对一匹配，而是更一般的"分配问题"。

---

## 5️⃣ 推送-重标记算法（26.4 Push-Relabel）

### 5.1 从Ford-Fulkerson到推送-重标记的范式转换

**Ford-Fulkerson的局限**：

Ford-Fulkerson方法是"全局式"的——每次找一条从s到t的完整路径，然后沿整条路径加流。这就像一个人从水厂出发，一路走到小区，沿途开阀门。

**推送-重标记的思路**：

如果网络太大，找完整路径太费劲。能不能让水"自动"流？

**物理类比**：

```
Ford-Fulkerson：调度员手动找路线、手动开阀门
  → 全局信息，每次处理一条完整路径

推送-重标记：在各个节点安装自动水泵和高度计
  → 局部信息，每个节点只看邻居
  → 水自动从高处往低处流（推送）
  → 如果水被困住了，就抬高节点（重标记）
  → 最终所有水流到t，或者流回s
```

**更形象的类比**：

想象一个多层的喷泉系统：
- 源点s是最顶层的水箱（无限供水）
- 汇点t是地面层（排水口）
- 中间节点是各层的小水池
- 每个水池有高度（高度函数h）
- 水只能从高处流到低处（推送条件：h(u) > h(v)）
- 如果水池满了但周围都比自己高，就把水池抬高（重标记）

### 5.2 预流与超额流

**预流（Preflow）**：放松流量守恒——允许中间节点"暂存"水

$$\sum_{v} f(v,u) \geq \sum_{v} f(u,v)$$

> 中间节点流入可以多于流出——暂时"囤水"没关系

**超额流（Excess Flow）**：节点u的超额流

$$e(u) = \sum_{v} f(v,u) - \sum_{v} f(u,v)$$

> 节点暂存了多少水

- 如果 e(u) > 0，称u是**溢出节点**（有水要处理）
- 源点和汇点永远不是溢出节点

**初始化**：

```
1. h(s) = |V|        # 源点高度设为最大
2. h(v) = 0, v ≠ s   # 其他节点高度为0
3. f(s,v) = c(s,v)   # 从s出发的边全部灌满
4. 其他边的流为0
```

```
初始化示意图：

  h=6(s) ──── 灌满 ────→ h=0(v1)
  h=6(s) ──── 灌满 ────→ h=0(v2)
  
  s的水"溢出"到邻居，邻居变成溢出节点
  s自身不溢出（它是源点）
  
  现在 v1, v2 有超额流，需要处理
```

### 5.3 高度函数

**高度函数** h: V → ℕ₀，满足：

1. h(s) = |V|
2. h(t) = 0
3. 对每条残存边 (u,v)：h(u) ≤ h(v) + 1

**含义**：水只能"下坡"流，或者最多"平坡"流（但平坡不允许推送，见下）

### 5.4 推送操作（Push）

**条件**：节点u溢出，且存在残存边 (u,v) 使得 h(u) = h(v) + 1

**操作**：沿 (u,v) 推送尽可能多的流

```
PUSH(u, v)
1  d_f(u,v) = c_f(u,v)                    # 残存容量
2  Δ = min(e(u), d_f(u,v))                # 推送量：超额流和残存容量的较小值
3  f(u,v) = f(u,v) + Δ                    # 正向加流
4  f(v,u) = f(v,u) - Δ                    # 反向减流
5  e(u) = e(u) - Δ                        # u的超额流减少
6  e(v) = e(v) + Δ                        # v的超额流增加
```

**生活类比**：

```
水池u（高度5，超额流10单位）
  │
  ↓ 水管到水池v（高度4，容量6）
  
h(u)=5 = h(v)+1 = 4+1 ✓（可以推送）
推送量 = min(10, 6) = 6
推送后：u超额流=4，v超额流增加了6
```

**饱和推送**：推送后 c_f(u,v) = 0（水管满了）
**非饱和推送**：推送后 e(u) = 0（水推完了）

### 5.5 重标记操作（Relabel）

**条件**：节点u溢出，且对所有残存边 (u,v)，h(u) ≤ h(v)（没有比u低的邻居可推送）

**操作**：将u的高度提升到最低邻居高度+1

```
RELABEL(u)
1  h(u) = 1 + min{h(v) : (u,v) 是残存边}
```

**生活类比**：

```
水池u（高度3，超额流5单位）
周围邻居：v1(高度5), v2(高度4), v3(高度3)
全部 ≥ u的高度！水没法往下流

解决方案：把u抬高到 min(4,5,3)+1 = 4
现在 u(高度4) → v3(高度3)，可以推送了！

         重标记前              重标记后
       h=5(v1)              h=5(v1)
       h=4(v2)              h=4(v2)
  →    h=3(u)          →   h=4(u)  ← 抬高了！
       h=3(v3)              h=3(v3) ← 现在可以推送
```

### 5.6 通用推送-重标记算法

```
GENERIC-PUSH-RELABEL(G, s, t)
1  初始化预流和高度函数
2  while 存在可以执行的推送或重标记操作:
3      选择一个合法操作执行
4  return f    # 此时预流变成了流
```

**算法终止条件**：没有溢出节点（除了s和t），此时预流就是流，而且是最大流。

### 5.7 完整执行过程追踪

**使用一个简单例子**：

```
网络：s → a → t, s → b → t, a → b
容量：s→a(10), s→b(10), a→b(2), a→t(8), b→t(10)

最大流应该是18（s→a→t送8，s→a→b→t送2，s→b→t送8）
```

**初始化**：

```
h(s)=5, h(a)=0, h(b)=0, h(t)=0
e(s)=0, e(a)=0, e(b)=0, e(t)=0

从s灌满：f(s,a)=10, f(s,b)=10
e(a) = 10, e(b) = 10    ← a和b溢出
h(s)=5, h(a)=0, h(b)=0, h(t)=0
```

**第1步**：a溢出，看a的残存邻居
- a→b: 残存容量2, h(b)=0, h(a)=0 → 不能推送（不是h(a)=h(b)+1）
- a→t: 残存容量8, h(t)=0, h(a)=0 → 不能推送

→ 重标记a：h(a) = 1 + min(h(b), h(t)) = 1 + 0 = 1

**第2步**：b溢出，看b的残存邻居
- b→t: 残存容量10, h(t)=0, h(b)=0 → 不能推送

→ 重标记b：h(b) = 1 + h(t) = 1

**第3步**：a溢出，h(a)=1
- a→t: h(t)=0, h(a)=1 = h(t)+1 ✓ → 推送！
  推送量 = min(e(a)=10, c_f(a,t)=8) = 8（饱和推送）
  f(a,t) = 8, e(a) = 10-8 = 2, e(t) = 8

**第4步**：a仍然溢出（e(a)=2），h(a)=1
- a→b: h(b)=1, h(a)=1 → 不满足h(a)=h(b)+1

→ 重标记a：h(a) = 1 + h(b) = 2

**第5步**：a溢出，h(a)=2
- a→b: h(b)=1, h(a)=2 = h(b)+1 ✓ → 推送！
  推送量 = min(e(a)=2, c_f(a,b)=2) = 2（饱和推送）
  f(a,b) = 2, e(a) = 0, e(b) = 10+2 = 12

**第6步**：b溢出（e(b)=12），h(b)=1
- b→t: h(t)=0, h(b)=1 = h(t)+1 ✓ → 推送！
  推送量 = min(e(b)=12, c_f(b,t)=10) = 10（饱和推送）
  f(b,t) = 10, e(b) = 2, e(t) = 18

**第7步**：b仍然溢出（e(b)=2），h(b)=1
- b→t: 残存容量0，不能推送
- b没有其他更低的邻居

→ 重标记b：h(b) = 1 + ... 

b的残存邻居：b→t(0容量，不算), 反向a→b的残存→b→a?
f(a,b)=2, 所以反向b→a的残存容量=2, h(a)=2
h(b) = 1 + h(a) = 3? 不对，h(a)=2，所以 h(b) = 1 + 2 = 3

等一下，重标记的条件是找残存边(u,v)中h(v)最小的。b的残存边：
- b→t: 残存容量0（不算）
- b→a: 这是从a→b的反向，残存容量 = f(a,b) = 2, h(a)=2

h(b) = 1 + min{h(a)} = 1 + 2 = 3

**第8步**：b溢出，h(b)=3
- b→a: h(a)=2, h(b)=3 = h(a)+1 ✓ → 推送！
  推送量 = min(e(b)=2, c_f(b,a)=2) = 2（饱和推送）
  这等于撤销了a→b的2单位流！
  f(a,b) = 2-2 = 0, e(b) = 0, e(a) = 2

**第9步**：a溢出（e(a)=2），h(a)=2
- a→t: 残存容量0
- a→b: 残存容量2, h(b)=3 → h(a)<h(b), 不能推送

→ 重标记a：a的残存邻居中最低的是...
- a→t: 残存容量0
- a→b: 残存容量2, h(b)=3
- a→s: 反向边，残存容量=f(s,a)=10, h(s)=5

h(a) = 1 + min{h(b), h(s)} = 1 + min(3, 5) = 4

**第10步**：a溢出，h(a)=4
- a→b: h(b)=3, h(a)=4 = h(b)+1 ✓ → 推送！
  推送量 = min(e(a)=2, c_f(a,b)=2) = 2
  f(a,b) = 2, e(a) = 0, e(b) = 2

**第11步**：b溢出（e(b)=2），h(b)=3
- b→t: 残存容量0
- b→a: 残存容量 = f(a,b) = 2, h(a)=4 → 不能推送（h(b)<h(a)）

→ 重标记b：h(b) = 1 + min{h(a)=4} = 5

b→a: h(a)=4, h(b)=5 = h(a)+1 → 推送
推送量 = min(e(b)=2, c_f(b,a)=2) = 2
撤销a→b的流，f(a,b) = 0, e(b) = 0, e(a) = 2

这好像在循环了...让我重新检查。

实际上，当b把流推回给a后，a又试图推给b，但b又推回来。这些水最终会流回s。

让我简化分析。关键点是：
1. 最终所有溢出节点要么把水推到t，要么推回s
2. 推回s是允许的！因为h(s)=|V|是最高的，但当邻居被重标记后可能比s低

等等，h(s)=5, 而a被重标记到h(a)=4。那么a→s：h(s)=5 > h(a)=4，所以a不能向s推送。

那么水怎么回到s呢？只有当h(u)变得比h(s)大时。但h(s)=|V|=5，而高度上界是2|V|-1...

让我重新审视。实际上推送-重标记算法中，水最终要么流到t，要么通过一系列推送回到s。回到s是通过让中间节点的高度超过s来实现的。

让我换一种理解方式：这个算法保证终止，终止时没有溢出节点，预流变成流，且由最大流最小割定理可知这是最大流。

**更简洁的过程总结**：

```
初始化 → 节点获得超额流 → 反复推送/重标记 → 无溢出节点 → 最大流

推送：水从高处往低处流（局部最优）
重标记：水被困住时抬高节点（创造新出路）
```

### 5.8 推送-重标记的复杂度分析

| 操作 | 次数上限 |
|------|---------|
| 重标记 | ≤ 2|V|² 次（每个节点高度最多2\|V\|，每次重标记至少+1） |
| 饱和推送 | ≤ 2|V|\|E| 次 |
| 非饱和推送 | ≤ 4|V|²(|V|+|E|) 次（最复杂，用势能函数分析） |

通用推送-重标记算法总复杂度：O(V²E)

### 5.9 Python实现

```python
class PushRelabel:
    """推送-重标记最大流算法"""
    
    def __init__(self, n):
        """
        初始化
        参数:
            n: 顶点数量
        """
        self.n = n
        self.cap = [[0] * n for _ in range(n)]   # 容量矩阵
        self.flow = [[0] * n for _ in range(n)]   # 流量矩阵
        self.height = [0] * n                       # 高度函数
        self.excess = [0] * n                       # 超额流
    
    def add_edge(self, u, v, c):
        """添加边 u→v，容量 c"""
        self.cap[u][v] += c
    
    def _residual(self, u, v):
        """计算残存容量"""
        return self.cap[u][v] - self.flow[u][v]
    
    def push(self, u, v):
        """
        从u向v推送流量
        返回是否执行了推送
        """
        # 计算推送量
        d = min(self.excess[u], self._residual(u, v))
        if d <= 0:
            return False
        
        # 更新流量和超额流
        self.flow[u][v] += d          # 正向加流
        self.flow[v][u] -= d          # 反向减流
        self.excess[u] -= d           # u的超额流减少
        self.excess[v] += d           # v的超额流增加
        return True
    
    def relabel(self, u):
        """
        重标记节点u
        将u的高度提升到最低邻居高度+1
        """
        min_height = float('inf')
        for v in range(self.n):
            if self._residual(u, v) > 0:        # 只看残存边
                min_height = min(min_height, self.height[v])
        
        if min_height < float('inf'):
            self.height[u] = min_height + 1     # 提升高度
    
    def initialize_preflow(self, s):
        """
        初始化预流
        参数:
            s: 源点
        """
        self.height[s] = self.n                  # 源点高度 = |V|
        self.excess[s] = 0
        
        # 从s出发的边全部灌满
        for v in range(self.n):
            if self.cap[s][v] > 0:
                self.flow[s][v] = self.cap[s][v]
                self.flow[v][s] = -self.cap[s][v]
                self.excess[v] = self.cap[s][v]  # 邻居获得超额流
                self.excess[s] -= self.cap[s][v]  # s的超额流减少
    
    def max_flow(self, s, t):
        """
        求最大流
        参数:
            s: 源点
            t: 汇点
        返回:
            最大流值
        """
        self.initialize_preflow(s)
        
        # 主循环：处理溢出节点
        while True:
            found = False
            
            for u in range(self.n):
                if u == s or u == t:
                    continue
                if self.excess[u] <= 0:
                    continue
                
                # u是溢出节点，尝试推送
                pushed = False
                for v in range(self.n):
                    if self._residual(u, v) > 0 and self.height[u] == self.height[v] + 1:
                        self.push(u, v)
                        pushed = True
                        found = True
                        break
                
                if not pushed:
                    # 无法推送，重标记
                    self.relabel(u)
                    found = True
            
            if not found:
                break
        
        # 最大流 = 流出s的总流量
        return sum(self.flow[s][v] for v in range(self.n))


# ===== 使用示例 =====
if __name__ == "__main__":
    pr = PushRelabel(6)
    
    pr.add_edge(0, 1, 16)    # s → v1
    pr.add_edge(0, 2, 13)    # s → v2
    pr.add_edge(1, 2, 10)    # v1 → v2
    pr.add_edge(1, 3, 12)    # v1 → v3
    pr.add_edge(2, 4, 14)    # v2 → v4
    pr.add_edge(3, 2, 9)     # v3 → v2
    pr.add_edge(3, 5, 20)    # v3 → t
    pr.add_edge(4, 3, 7)     # v4 → v3
    pr.add_edge(4, 5, 4)     # v4 → t
    
    print(f"最大流 = {pr.max_flow(0, 5)}")   # 23
```

### 5.10 C++实现

```cpp
#include <iostream>
#include <vector>
#include <algorithm>
#include <climits>
using namespace std;

class PushRelabel {
private:
    int n;                              // 顶点数
    vector<vector<int>> cap;            // 容量矩阵
    vector<vector<int>> flow;           // 流量矩阵
    vector<int> height;                 // 高度函数
    vector<int> excess;                 // 超额流
    
    // 计算残存容量
    int residual(int u, int v) {
        return cap[u][v] - flow[u][v];
    }
    
    // 推送操作
    bool push(int u, int v) {
        int d = min(excess[u], residual(u, v));
        if (d <= 0) return false;
        flow[u][v] += d;                // 正向加流
        flow[v][u] -= d;                // 反向减流
        excess[u] -= d;                 // u超额流减少
        excess[v] += d;                 // v超额流增加
        return true;
    }
    
    // 重标记操作
    void relabel(int u) {
        int minHeight = INT_MAX;
        for (int v = 0; v < n; v++) {
            if (residual(u, v) > 0) {
                minHeight = min(minHeight, height[v]);
            }
        }
        if (minHeight < INT_MAX) {
            height[u] = minHeight + 1;
        }
    }

public:
    PushRelabel(int n) : n(n),
                         cap(n, vector<int>(n, 0)),
                         flow(n, vector<int>(n, 0)),
                         height(n, 0),
                         excess(n, 0) {}
    
    void addEdge(int u, int v, int c) {
        cap[u][v] += c;
    }
    
    // 初始化预流
    void initializePreflow(int s) {
        height[s] = n;                   // 源点高度 = |V|
        for (int v = 0; v < n; v++) {
            if (cap[s][v] > 0) {
                flow[s][v] = cap[s][v];
                flow[v][s] = -cap[s][v];
                excess[v] = cap[s][v];    // 邻居获得超额流
                excess[s] -= cap[s][v];   // s减少
            }
        }
    }
    
    // 求最大流
    int maxFlow(int s, int t) {
        initializePreflow(s);
        
        bool found = true;
        while (found) {
            found = false;
            for (int u = 0; u < n; u++) {
                if (u == s || u == t) continue;
                if (excess[u] <= 0) continue;
                
                bool pushed = false;
                for (int v = 0; v < n; v++) {
                    if (residual(u, v) > 0 && 
                        height[u] == height[v] + 1) {
                        push(u, v);
                        pushed = true;
                        found = true;
                        break;
                    }
                }
                if (!pushed) {
                    relabel(u);
                    found = true;
                }
            }
        }
        
        int result = 0;
        for (int v = 0; v < n; v++) {
            result += flow[s][v];
        }
        return result;
    }
};

int main() {
    PushRelabel pr(6);
    
    pr.addEdge(0, 1, 16);
    pr.addEdge(0, 2, 13);
    pr.addEdge(1, 2, 10);
    pr.addEdge(1, 3, 12);
    pr.addEdge(2, 4, 14);
    pr.addEdge(3, 2, 9);
    pr.addEdge(3, 5, 20);
    pr.addEdge(4, 3, 7);
    pr.addEdge(4, 5, 4);
    
    cout << "最大流 = " << pr.maxFlow(0, 5) << endl;    // 23
    return 0;
}
```

---

## 6️⃣ 重标记到前置算法（26.5 Relabel-to-Front）

### 6.1 为什么需要更高效的实现？

通用推送-重标记算法的问题是：每次选择哪个溢出节点操作？选择策略影响效率。

**重标记到前置算法**的关键改进：

1. **系统化地遍历溢出节点**：用链表维护所有非s非t的节点
2. **每个节点维护自己的邻接表指针**：记住上次检查到哪个邻居了
3. **放电操作**：对单个节点反复推送+重标记，直到它不溢出

**复杂度提升**：O(V³)，比通用的 O(V²E) 更好（对于稠密图 E≈V²）

### 6.2 许可边（Admissible Edge）

**定义**：残存网络中的边 (u,v) 是许可边，如果 c_f(u,v) > 0 且 h(u) = h(v) + 1。

**许可网络**：由所有许可边组成的子网络。

**关键性质**：许可网络是**无环的**（因为高度严格递减）。

**含义**：沿许可边推送，水一定在"下坡"走，不会兜圈子。

### 6.3 邻接表与当前指针

每个节点u维护：
- N[u]：u的邻接表（包含所有可能与u有残存边的顶点）
- current[u]：当前检查到N[u]中的哪个位置

**为什么需要邻接表？** 当我们检查u能否推送时，不需要每次都从头扫描所有顶点，而是从上次的位置继续。

### 6.4 放电操作（Discharge）

```
DISCHARGE(u)
1  while e(u) > 0:                          # u还是溢出的
2      v = N[u][current[u]]                  # 当前检查的邻居
3      if current[u]到了N[u]末尾:            # 一圈检查完了
4          RELABEL(u)                        # 重标记
5          current[u] = 0                    # 重头开始检查
6      elif c_f(u,v) > 0 and h(u) = h(v)+1: # 可以推送
7          PUSH(u, v)                        # 推送
8      else:                                 # 不能推送，检查下一个
9          current[u] += 1
```

**直觉**：对u"放电"——把u上积攒的水尽量排出去。如果当前邻居不能接收，就试下一个；如果所有邻居都试过了还不行，就重标记（抬高u），然后重头来。

### 6.5 重标记到前置算法

```
RELABEL-TO-FRONT(G, s, t)
1  INITIALIZE-PREFLOW(G, s)                # 初始化预流
2  L = 任意顺序的 V-{s,t} 的链表            # 节点链表
3  for each u in V-{s,t}:                   # 初始化当前指针
4      current[u] = 0                       # 指向邻接表头部
5  u = head(L)                               # 从链表头开始
6  while u ≠ NIL:                            # 遍历链表
7      old_height = h(u)                     # 记录旧高度
8      DISCHARGE(u)                          # 放电！
9      if h(u) > old_height:                 # 高度变了（被重标记了）
10         将u移到链表头部                     # 重标记的节点移到前面
11     u = u在L中的下一个节点                  # 继续下一个
```

**为什么重标记后移到链表头部？**

因为u被重标记后可能变高了，之前在u前面的节点现在可能可以向u推送。把u移到前面，确保后续节点能看到u的新高度。

### 6.6 完整执行过程追踪

**用CLRS示例**：

```
顶点: s=0, v1=1, v2=2, v3=3, v4=4, t=5
边: s→v1(16), s→v2(13), v1→v2(10), v1→v3(12),
    v2→v4(14), v3→v2(9), v3→t(20), v4→v3(7), v4→t(4)

邻接表（包含正反方向所有可能的邻居）：
N[v1] = [s, v2, v3]     # v1可能与s, v2, v3有残存边
N[v2] = [s, v1, v4, v3] 
N[v3] = [v1, v2, t, v4]
N[v4] = [v2, v3, t]

链表L = [v1, v2, v3, v4]
```

**初始化**：

```
h = [6, 0, 0, 0, 0, 0]    # s高度=|V|=6
e = [-29, 16, 13, 0, 0, 0]  # s流出29，v1获得16，v2获得13

等等，s的excess不这么算。让我重新来：
h(s)=6, h(其他)=0
f(s,v1)=16, f(s,v2)=13
e(v1)=16, e(v2)=13, e(其他)=0
（s不是溢出节点，不考虑它的excess）
```

**处理v1**（current[v1]=0，指向s）：

```
e(v1)=16 > 0，开始放电

检查 N[v1][0] = s: c_f(v1,s) > 0? 残存容量 = 0 - (-16) = 16
但 h(v1)=0, h(s)=6, h(v1) ≠ h(s)+1 → 不能推送
current[v1]++

检查 N[v1][1] = v2: c_f(v1,v2) = 10 - 0 = 10
h(v1)=0, h(v2)=0, 不满足 h(v1)=h(v2)+1 → 不能推送
current[v1]++

检查 N[v1][2] = v3: c_f(v1,v3) = 12 - 0 = 12
h(v1)=0, h(v3)=0, 不满足 → 不能推送
current[v1]++ → 到末尾

重标记v1: h(v1) = 1 + min(h(s), h(v2), h(v3)) = 1 + 0 = 1
current[v1] = 0

再次检查 N[v1][0] = s: h(s)=6, h(v1)=1 → 不满足
current[v1]++

检查 N[v1][1] = v2: h(v2)=0, h(v1)=1 = h(v2)+1 ✓
c_f(v1,v2) = 10 → 推送 min(16, 10) = 10（饱和推送）
f(v1,v2) = 10, e(v1) = 16-10 = 6, e(v2) = 13+10 = 23

检查 N[v1][2] = v3: h(v3)=0, h(v1)=1 = h(v3)+1 ✓
c_f(v1,v3) = 12 → 推送 min(6, 12) = 6（非饱和推送）
f(v1,v3) = 6, e(v1) = 0, e(v2) = 23, e(v3) = 6

e(v1)=0，放电完成！
old_height(v1) = 0 → new h(v1) = 1, 变了 → v1移到链表头部
L = [v1, v2, v3, v4]（v1本来就在头部，无变化）
```

**处理v2**（current[v2]=0）：

```
e(v2)=23 > 0，开始放电

检查 N[v2][0] = s: h(s)=6 → 不满足
current[v2]++

检查 N[v2][1] = v1: h(v1)=1 → h(v2)=0 ≠ h(v1)+1 → 不满足
current[v2]++

检查 N[v2][2] = v4: h(v4)=0, h(v2)=0 → 不满足
current[v2]++

检查 N[v2][3] = v3: h(v3)=0, h(v2)=0 → 不满足
current[v2]++ → 到末尾

重标记v2: h(v2) = 1 + min(h(s), h(v1), h(v4), h(v3)) = 1 + min(6,1,0,0) = 1
current[v2] = 0

检查 N[v2][0] = s: 不满足
current[v2]++

检查 N[v2][1] = v1: h(v1)=1, h(v2)=1 → 不满足（不是h(v2)=h(v1)+1）
current[v2]++

检查 N[v2][2] = v4: h(v4)=0, h(v2)=1 = h(v4)+1 ✓
c_f(v2,v4) = 14 → 推送 min(23, 14) = 14（饱和推送）
f(v2,v4) = 14, e(v2) = 23-14 = 9, e(v4) = 14

检查 N[v2][3] = v3: h(v3)=0, h(v2)=1 = h(v3)+1 ✓
c_f(v2,v3) = 0 - (-?) ... 残存容量
原始网络中没有v2→v3的边，但v3→v2有边(9)。
f(v1,v2)=10, 所以反向v2→v1的残存容量=10
f(v2,v4)=14, 所以反向v4→v2的残存容量=14

v2→v3的残存容量 = c(v2,v3) - f(v2,v3) = 0 - 0 = 0（没有这条边）
嗯，v3→v2的残存容量 = c(v3,v2) - f(v3,v2) = 9 - 0 = 9
但这是v3→v2，不是v2→v3

所以在检查N[v2][3]=v3时，c_f(v2,v3) = 0，不能推送
current[v2]++ → 到末尾

重标记v2: h(v2) = 1 + min(邻居中残存容量>0的高度)
v2的残存邻居:
- v2→v4: 残存=0（满了）不算
- v2→v1: 残存 = 0 - f(v2,v1) = 0 - (-10) = 10, h(v1)=1
- v2→s: 残存 = 0 - f(v2,s) = 0 - (-13) = 13, h(s)=6

h(v2) = 1 + min(1, 6) = 2
current[v2] = 0

检查N[v2][1] = v1: h(v1)=1, h(v2)=2 = h(v1)+1 ✓
c_f(v2,v1) = 10 → 推送 min(9, 10) = 9（非饱和推送）
f(v2,v1) = -10+9 = -1... 等等

让我更仔细。f(v1,v2)=10, 所以f(v2,v1) = -10（反向）
推送v2→v1: 这是在残存网络中的推送，对应原始网络中减少v1→v2的流
f(v1,v2) = 10 - 9 = 1, f(v2,v1) = -1
e(v2) = 0, e(v1) = 0 + 9 = 9

v2放电完成。h(v2)从0变到2 → v2移到链表头部
L = [v2, v1, v3, v4]
```

这个过程可以继续，但已经很清楚了。关键是每次对溢出节点"放电"，直到它不溢出为止。

### 6.7 Python实现

```python
class RelabelToFront:
    """重标记到前置最大流算法"""
    
    def __init__(self, n):
        """
        初始化
        参数:
            n: 顶点数量
        """
        self.n = n
        self.cap = [[0] * n for _ in range(n)]
        self.flow = [[0] * n for _ in range(n)]
        self.height = [0] * n
        self.excess = [0] * n
        self.seen = [0] * n              # 每个节点的当前检查位置
    
    def add_edge(self, u, v, c):
        """添加边 u→v，容量 c"""
        self.cap[u][v] += c
    
    def _residual(self, u, v):
        """残存容量"""
        return self.cap[u][v] - self.flow[u][v]
    
    def _push(self, u, v):
        """推送操作"""
        d = min(self.excess[u], self._residual(u, v))
        self.flow[u][v] += d
        self.flow[v][u] -= d
        self.excess[u] -= d
        self.excess[v] += d
    
    def _relabel(self, u):
        """重标记操作"""
        min_h = float('inf')
        for v in range(self.n):
            if self._residual(u, v) > 0:
                min_h = min(min_h, self.height[v])
        self.height[u] = min_h + 1
    
    def _discharge(self, u, adj):
        """
        放电操作：反复推送/重标记直到u不溢出
        参数:
            u: 要放电的节点
            adj: 邻接表 adj[u] = [v1, v2, ...]
        """
        while self.excess[u] > 0:
            if self.seen[u] < len(adj[u]):
                v = adj[u][self.seen[u]]
                if self._residual(u, v) > 0 and self.height[u] == self.height[v] + 1:
                    self._push(u, v)
                else:
                    self.seen[u] += 1
            else:
                self._relabel(u)
                self.seen[u] = 0
    
    def max_flow(self, s, t):
        """
        求最大流
        """
        # 构建邻接表（包含正反方向所有可能的邻居）
        adj = [[] for _ in range(self.n)]
        for u in range(self.n):
            for v in range(self.n):
                if self.cap[u][v] > 0 or self.cap[v][u] > 0:
                    adj[u].append(v)
        
        # 初始化预流
        self.height[s] = self.n
        for v in range(self.n):
            if self.cap[s][v] > 0:
                self.flow[s][v] = self.cap[s][v]
                self.flow[v][s] = -self.cap[s][v]
                self.excess[v] = self.cap[s][v]
                self.excess[s] -= self.cap[s][v]
        
        # 初始化链表（所有非s非t的节点）
        L = [u for u in range(self.n) if u != s and u != t]
        
        # 主循环
        it = 0
        while it < len(L):
            u = L[it]
            old_h = self.height[u]
            self._discharge(u, adj)
            
            if self.height[u] > old_h:
                # u被重标记了，移到链表头部
                L.remove(u)
                L.insert(0, u)
                it = 0    # 从头开始
            else:
                it += 1
        
        return sum(self.flow[s][v] for v in range(self.n))


# ===== 使用示例 =====
if __name__ == "__main__":
    rtf = RelabelToFront(6)
    
    rtf.add_edge(0, 1, 16)
    rtf.add_edge(0, 2, 13)
    rtf.add_edge(1, 2, 10)
    rtf.add_edge(1, 3, 12)
    rtf.add_edge(2, 4, 14)
    rtf.add_edge(3, 2, 9)
    rtf.add_edge(3, 5, 20)
    rtf.add_edge(4, 3, 7)
    rtf.add_edge(4, 5, 4)
    
    print(f"最大流 = {rtf.max_flow(0, 5)}")   # 23
```

### 6.8 C++实现

```cpp
#include <iostream>
#include <vector>
#include <algorithm>
#include <climits>
#include <list>
using namespace std;

class RelabelToFront {
private:
    int n;
    vector<vector<int>> cap, flow;
    vector<int> height, excess, seen;
    
    int residual(int u, int v) {
        return cap[u][v] - flow[u][v];
    }
    
    void push(int u, int v) {
        int d = min(excess[u], residual(u, v));
        flow[u][v] += d;
        flow[v][u] -= d;
        excess[u] -= d;
        excess[v] += d;
    }
    
    void relabel(int u) {
        int minH = INT_MAX;
        for (int v = 0; v < n; v++) {
            if (residual(u, v) > 0) {
                minH = min(minH, height[v]);
            }
        }
        height[u] = minH + 1;
    }
    
    void discharge(int u, const vector<vector<int>>& adj) {
        while (excess[u] > 0) {
            if (seen[u] < (int)adj[u].size()) {
                int v = adj[u][seen[u]];
                if (residual(u, v) > 0 && height[u] == height[v] + 1) {
                    push(u, v);
                } else {
                    seen[u]++;
                }
            } else {
                relabel(u);
                seen[u] = 0;
            }
        }
    }

public:
    RelabelToFront(int n) : n(n),
                            cap(n, vector<int>(n, 0)),
                            flow(n, vector<int>(n, 0)),
                            height(n, 0),
                            excess(n, 0),
                            seen(n, 0) {}
    
    void addEdge(int u, int v, int c) {
        cap[u][v] += c;
    }
    
    int maxFlow(int s, int t) {
        // 构建邻接表
        vector<vector<int>> adj(n);
        for (int u = 0; u < n; u++) {
            for (int v = 0; v < n; v++) {
                if (cap[u][v] > 0 || cap[v][u] > 0) {
                    adj[u].push_back(v);
                }
            }
        }
        
        // 初始化预流
        height[s] = n;
        for (int v = 0; v < n; v++) {
            if (cap[s][v] > 0) {
                flow[s][v] = cap[s][v];
                flow[v][s] = -cap[s][v];
                excess[v] = cap[s][v];
                excess[s] -= cap[s][v];
            }
        }
        
        // 节点链表
        list<int> L;
        for (int u = 0; u < n; u++) {
            if (u != s && u != t) L.push_back(u);
        }
        
        // 主循环
        auto it = L.begin();
        while (it != L.end()) {
            int u = *it;
            int oldH = height[u];
            discharge(u, adj);
            
            if (height[u] > oldH) {
                // 移到链表头部
                L.erase(it);
                L.push_front(u);
                it = L.begin();
            } else {
                ++it;
            }
        }
        
        int result = 0;
        for (int v = 0; v < n; v++) {
            result += flow[s][v];
        }
        return result;
    }
};

int main() {
    RelabelToFront rtf(6);
    
    rtf.addEdge(0, 1, 16);
    rtf.addEdge(0, 2, 13);
    rtf.addEdge(1, 2, 10);
    rtf.addEdge(1, 3, 12);
    rtf.addEdge(2, 4, 14);
    rtf.addEdge(3, 2, 9);
    rtf.addEdge(3, 5, 20);
    rtf.addEdge(4, 3, 7);
    rtf.addEdge(4, 5, 4);
    
    cout << "最大流 = " << rtf.maxFlow(0, 5) << endl;    // 23
    return 0;
}
```

---

## 7️⃣ 算法对比与总结

### 7.1 四种最大流算法对比

| 算法 | 核心思想 | 找增广路径方式 | 时间复杂度 | 特点 |
|------|---------|--------------|-----------|------|
| Ford-Fulkerson | 找路加流 | 未指定 | O(\|f*\|·E) | 方法框架，非具体算法 |
| Edmonds-Karp | BFS找最短增广路 | BFS | O(VE²) | 简单高效，最常用 |
| 推送-重标记 | 水往低处流 | 不找路径，局部操作 | O(V²E) | 并行友好，物理直觉 |
| 重标记到前置 | 系统化放电 | 不找路径，遍历放电 | O(V³) | 理论最优之一 |

### 7.2 更高效的最大流算法（拓展）

| 算法 | 复杂度 | 关键改进 |
|------|--------|---------|
| Dinic | O(V²E) 或 O(E√V)（单位容量） | 分层图+阻塞流 |
| ISAP | O(V²E) | 简化Dinic，不需要重建分层图 |
| MPM | O(V³) | 三阶段算法 |
| 最高标号预流推进 | O(V²√E) | 优先处理最高节点 |

### 7.3 最大流的应用场景

```
                    最大流
                   /     \
              组合优化    网络优化
             /    |    \      |
        二分匹配 调度  分配  网络可靠性
         |       |     |      |
      婚姻问题 作业调度 任务分配 最小割
         |                    |
      稳定匹配            图像分割
```

**具体应用**：

1. **二分图匹配**：任务分配、婚姻匹配、课程选课
2. **最小割**：网络可靠性分析、图像分割、社区发现
3. **调度问题**：作业调度、航班调度
4. **网络设计**：带宽分配、流量工程
5. **项目选择**：选择利润最大化的项目子集

### 7.4 关键概念速查表

| 概念 | 定义 | 直觉 |
|------|------|------|
| 流网络 | 有向图+容量+源汇 | 水管网络 |
| 流 | 满足容量限制和流量守恒的函数 | 水的分配方案 |
| 残存网络 | 剩余容量的网络 | 还能加/撤销多少水 |
| 增广路径 | 残存网络中s到t的路径 | 还能加水的路线 |
| 割 | V的划分(S,T)，s∈S,t∈T | 一刀切的位置 |
| 最小割 | 容量最小的割 | 最窄的瓶颈 |
| 预流 | 放松流量守恒的流 | 允许暂存水的分配 |
| 超额流 | 流入-流出 | 暂存的水量 |
| 高度函数 | 每个节点的"海拔" | 水位 |
| 推送 | 从高处往低处流 | 开闸放水 |
| 重标记 | 提升节点高度 | 抬高水池 |
| 放电 | 反复推送+重标记 | 彻底排空一个水池 |

---

## 8️⃣ FAQ汇总

### Q1：最大流唯一吗？

**答**：不唯一。可能存在多种流量分配方案达到相同的最大流值。但最大流的**值**是唯一的（由最大流最小割定理保证）。

### Q2：为什么需要残存网络中的反向边？

**答**：反向边表示"撤销"之前决策的能力。没有反向边，算法可能做出次优选择后无法纠正。反向边让算法能够"回溯"，重新安排流量。

### Q3：Ford-Fulkerson和Edmonds-Karp的区别是什么？

**答**：Ford-Fulkerson是"方法"（框架），Edmonds-Karp是具体实现（用BFS找增广路径）。Edmonds-Karp保证O(VE²)的复杂度，而原始Ford-Fulkerson如果用DFS可能在特定情况下很慢。

### Q4：推送-重标记为什么比Ford-Fulkerson更适合并行？

**答**：Ford-Fulkerson每次处理一条完整的增广路径（全局操作），而推送-重标记的推送和重标记都是局部操作——只需要看当前节点和邻居。多个不相关的溢出节点可以同时推送。

### Q5：最大流最小割定理有什么直觉理解？

**答**：想象你站在河流上游，不管你在哪个位置横切一刀，通过这个截面的净流量都等于总流量。最小割就是"最窄"的那个截面——它决定了整个网络能通过的最大流量。就像一个水管系统，最细的那段管子决定了总流量。

### Q6：为什么推送-重标记初始化时要把s的高度设为|V|？

**答**：这样s比所有节点都高，水可以从s"往下"流到任何邻居。同时，|V|是高度的上界（除了可能被重标记超过），保证水不会在中间节点之间无限循环——因为高度有界，而推送让高度严格递减。

### Q7：二分图匹配除了转化最大流，还有什么算法？

**答**：匈牙利算法（增广路径法），专门针对二分图，时间复杂度O(VE)，在实践中通常比最大流方法更快。但最大流方法更通用——修改容量就能处理更复杂的约束。

### Q8：最大流可以处理无向图吗？

**答**：可以！把每条无向边 {u,v} 替换为两条有向边 (u,v) 和 (v,u)，容量相同。

### Q9：如果有费用怎么办？

**答**：那是"最小费用最大流"问题——在所有最大流中找费用最小的。用连续最短路径算法或消圈算法解决，这超出了本章范围。

### Q10：实际工程中用什么最大流算法？

**答**：
- 小规模图：Edmonds-Karp（简单可靠）
- 中等规模：Dinic/ISAP（效率好）
- 大规模：最高标号预流推进（HLPP）
- 竞赛编程：Dinic最常用（代码短、效率好）

---

## 9️⃣ 习题提示

### 26.1节习题

**26.1-1**（流的分裂）：证明流可以分裂——一个节点的流入可以分配给多条出边。提示：用流量守恒和归纳法。

**26.1-4**（反平行边消除）：证明消除反平行边后，新网络的最大流等于原网络的最大流。提示：构造变换前后的流对应关系。

**26.1-6**（超级源超级汇）：多源多汇问题。提示：添加超级源汇后，证明新旧网络的最大流相等。

### 26.2节习题

**26.2-1**（Ford-Fulkerson执行过程）：手动画出每一步的残存网络和增广路径。提示：参照本文2.7节的详细追踪。

**26.2-6**（最大流最小割验证）：给定一个流，验证它是否是最大流。提示：检查残存网络中是否还有增广路径，或者找一个割使得|f|=c(S,T)。

**26.2-8**（Edmonds-Karp分析）：证明BFS找增广路径时，增广路径长度不减。提示：每次增广只会让某些边从可用变不可用，需要绕更远的路。

**26.2-10**（整数流）：证明如果容量都是整数，存在整数最大流。提示：Ford-Fulkerson每次增广增加整数流量。

### 26.3节习题

**26.3-1**（二分匹配执行过程）：对给定的二分图运行最大流算法。提示：先画出对应的流网络，再运行Edmonds-Karp。

**26.3-3**（完美匹配）：什么条件下存在完美匹配？提示：Hall定理——对L的任意子集X，|N(X)| ≥ |X|。

### 26.4节习题

**26.4-1**（推送-重标记执行过程）：手动追踪算法过程。提示：参照本文5.7节。

**26.4-3**（高度上界）：证明任意时刻h(u) ≤ 2|V|-1。提示：用归纳法，考虑重标记时的高度来源。

### 26.5节习题

**26.5-1**（重标记到前置执行过程）：手动追踪。提示：参照本文6.6节。

**26.5-2**（许可网络无环）：证明许可网络是无环的。提示：许可边要求h(u)=h(v)+1，高度严格递减，不可能有环。

---

## 🔟 本章小结

### 核心知识点

1. **流网络**：容量限制 + 流量守恒 → 合法流
2. **残存网络**：正向剩余容量 + 反向撤销容量 → 还能做什么
3. **增广路径**：残存网络中s→t的路径 → 可以加流
4. **最大流最小割定理**：最大流 = 最小割 → 流与割的对偶
5. **Ford-Fulkerson**：反复找增广路径加流 → 方法框架
6. **Edmonds-Karp**：BFS找最短增广路径 → O(VE²)
7. **二分匹配**：转化为最大流 → 匹配数=最大流值
8. **推送-重标记**：预流+局部操作 → 物理直觉
9. **重标记到前置**：系统化放电 → O(V³)

### 易错点

| 易错点 | 正确理解 |
|--------|---------|
| 残存网络只有正向边 | 还有反向边（撤销能力） |
| 割的容量包含双向 | 只算从S到T的方向 |
| Ford-Fulkerson是算法 | 是方法框架，Edmonds-Karp才是具体算法 |
| 最大流唯一 | 流值唯一，流量分配不唯一 |
| 推送条件只要u比v高 | 必须h(u)=h(v)+1（恰好高1） |
| 重标记可以任意抬高 | 只能抬到最低邻居+1 |

### 与前后章节的联系

```
第22章（BFS/DFS）→ 图的遍历基础
第23章（最小生成树）→ 图的优化问题
第24章（最短路径）→ 另一种图优化
第25章（全源最短路径）→ 最短路径扩展
第26章（最大流）→ 本章：流的优化
    ↓
第27章（多线程算法）→ 并行计算
应用：二分匹配、网络可靠性、图像分割
```

### 一句话总结

> **最大流问题 = 在容量限制下最大化吞吐 = 找最窄瓶颈（最小割）= 反复加流直到加不动**

---

## 📎 附录A：Dinic算法（补充扩展）

虽然Dinic算法不在CLRS第26章正文，但它是实际应用中最常用的最大流算法之一，值得了解。

### A.1 算法思想

Dinic算法结合了Edmonds-Karp和推送-重标记的优点：

1. **分层图**（Level Graph）：用BFS给每个节点分层，只保留允许的"下坡"边
2. **阻塞流**（Blocking Flow）：在分层图中用DFS一次性找多条增广路径
3. **反复重建分层图**：当前分层图无法再增广时，重建分层图

### A.2 分层图

用BFS从s出发，给每个节点分配一个"层级"（距离s的最短边数）。只保留 d(v) = d(u) + 1 的边 (u,v)。

```
原始残存网络：
  s → a → b → t
  s → c → d → t
  a → d
  c → b

分层后（假设BFS顺序）：
  Level 0: s
  Level 1: a, c
  Level 2: b, d
  Level 3: t

分层图只保留：
  s→a, s→c (Level 0→1)
  a→b, a→d, c→b (Level 1→2)
  b→t, d→t (Level 2→3)
  
去掉了"水平"或"上坡"的边
```

### A.3 阻塞流

在分层图中，用DFS找增广路径。关键优化：**多路增广**——找到一条路径增广后，不从头开始DFS，而是回溯到瓶颈边，继续搜索。

### A.4 Dinic算法伪代码

```
DINIC(G, s, t)
1  max_flow = 0
2  while BFS分层(s, t)成功:      # t可达
3      while DFS增广(s, t, ∞) > 0:  # 在分层图中找阻塞流
4          max_flow += 本次增广量
5  return max_flow
```

### A.5 复杂度

- 分层次数：O(V)（每次分层后，最短增广路径至少+1）
- 每层的增广：O(VE)
- 总复杂度：O(V²E)
- 单位容量图：O(E√V)
- 单位容量二分图：O(E√V)

### A.6 Python实现

```python
from collections import deque

class Dinic:
    """Dinic最大流算法"""
    
    def __init__(self, n):
        """
        初始化
        参数:
            n: 顶点数量
        """
        self.n = n
        self.adj = [[] for _ in range(n)]   # 邻接表，存边的索引
        self.edges = []                       # 边列表：[to, cap, rev_idx]
    
    def add_edge(self, u, v, c):
        """
        添加边 u→v，容量 c
        同时添加反向边 v→u，容量 0
        """
        self.edges.append([v, c, len(self.adj[v])])   # 正向边
        self.edges.append([u, 0, len(self.adj[u])])   # 反向边
        self.adj[u].append(len(self.edges) - 2)        # 正向边索引
        self.adj[v].append(len(self.edges) - 1)        # 反向边索引
    
    def _bfs(self, s, t):
        """
        BFS分层
        返回是否t可达，并更新level数组
        """
        self.level = [-1] * self.n
        self.level[s] = 0
        queue = deque([s])
        while queue:
            u = queue.popleft()
            for idx in self.adj[u]:
                v, cap, _ = self.edges[idx]
                if cap > 0 and self.level[v] < 0:    # 有容量且未分层
                    self.level[v] = self.level[u] + 1
                    queue.append(v)
        return self.level[t] >= 0                       # t是否可达
    
    def _dfs(self, u, t, f):
        """
        DFS在分层图中找增广路径
        参数:
            u: 当前节点
            t: 汇点
            f: 当前瓶颈容量
        返回:
            实际增广量
        """
        if u == t:
            return f
        
        while self.itr[u] < len(self.adj[u]):
            idx = self.adj[u][self.itr[u]]
            v, cap, rev = self.edges[idx]
            
            # 只走分层图中的边（level差1）且有容量
            if cap > 0 and self.level[u] + 1 == self.level[v]:
                d = self._dfs(v, t, min(f, cap))
                if d > 0:
                    self.edges[idx][1] -= d           # 正向减容量
                    self.edges[rev][1] += d           # 反向加容量
                    return d
            
            self.itr[u] += 1                           # 当前指针前进
        
        return 0
    
    def max_flow(self, s, t):
        """
        求最大流
        """
        flow = 0
        while self._bfs(s, t):                          # 分层
            self.itr = [0] * self.n                      # 重置当前指针
            while True:
                f = self._dfs(s, t, float('inf'))       # 多路增广
                if f == 0:
                    break
                flow += f
        return flow


# ===== 使用示例 =====
if __name__ == "__main__":
    dinic = Dinic(6)
    
    dinic.add_edge(0, 1, 16)    # s → v1
    dinic.add_edge(0, 2, 13)    # s → v2
    dinic.add_edge(1, 2, 10)    # v1 → v2
    dinic.add_edge(1, 3, 12)    # v1 → v3
    dinic.add_edge(2, 4, 14)    # v2 → v4
    dinic.add_edge(3, 2, 9)     # v3 → v2
    dinic.add_edge(3, 5, 20)    # v3 → t
    dinic.add_edge(4, 3, 7)     # v4 → v3
    dinic.add_edge(4, 5, 4)     # v4 → t
    
    print(f"最大流 = {dinic.max_flow(0, 5)}")   # 23
```

### A.7 C++实现

```cpp
#include <iostream>
#include <vector>
#include <queue>
#include <algorithm>
using namespace std;

struct Edge {
    int to, cap, rev;   // 终点、容量、反向边索引
};

class Dinic {
private:
    int n;
    vector<vector<Edge>> graph;
    vector<int> level, itr;
    
    // BFS分层
    bool bfs(int s, int t) {
        level.assign(n, -1);
        level[s] = 0;
        queue<int> q;
        q.push(s);
        while (!q.empty()) {
            int u = q.front(); q.pop();
            for (auto& e : graph[u]) {
                if (e.cap > 0 && level[e.to] < 0) {
                    level[e.to] = level[u] + 1;
                    q.push(e.to);
                }
            }
        }
        return level[t] >= 0;
    }
    
    // DFS多路增广
    int dfs(int u, int t, int f) {
        if (u == t) return f;
        for (int& i = itr[u]; i < (int)graph[u].size(); i++) {
            Edge& e = graph[u][i];
            if (e.cap > 0 && level[u] + 1 == level[e.to]) {
                int d = dfs(e.to, t, min(f, e.cap));
                if (d > 0) {
                    e.cap -= d;                    // 正向减容量
                    graph[e.to][e.rev].cap += d;   // 反向加容量
                    return d;
                }
            }
        }
        return 0;
    }

public:
    Dinic(int n) : n(n), graph(n) {}
    
    void addEdge(int u, int v, int c) {
        graph[u].push_back({v, c, (int)graph[v].size()});    // 正向边
        graph[v].push_back({u, 0, (int)graph[u].size()-1});  // 反向边
    }
    
    int maxFlow(int s, int t) {
        int flow = 0;
        while (bfs(s, t)) {
            itr.assign(n, 0);
            int d;
            while ((d = dfs(s, t, INT_MAX)) > 0) {
                flow += d;
            }
        }
        return flow;
    }
};

int main() {
    Dinic dinic(6);
    
    dinic.addEdge(0, 1, 16);
    dinic.addEdge(0, 2, 13);
    dinic.addEdge(1, 2, 10);
    dinic.addEdge(1, 3, 12);
    dinic.addEdge(2, 4, 14);
    dinic.addEdge(3, 2, 9);
    dinic.addEdge(3, 5, 20);
    dinic.addEdge(4, 3, 7);
    dinic.addEdge(4, 5, 4);
    
    cout << "最大流 = " << dinic.maxFlow(0, 5) << endl;    // 23
    return 0;
}
```

---

## 📎 附录B：最大流最小割定理的完整证明

### B.1 辅助引理

**引理1（流的值与割的关系）**：对任意流 f 和任意割 (S, T)：

$$|f| = f(S, T) = \sum_{u \in S} \sum_{v \in T} f(u,v) - \sum_{u \in S} \sum_{v \in T} f(v,u)$$

**证明**：

由流量守恒，对任意 u ∈ S - {s}：

$$\sum_{v \in V} f(u,v) - \sum_{v \in V} f(v,u) = 0$$

对源点 s：

$$\sum_{v \in V} f(s,v) - \sum_{v \in V} f(v,s) = |f|$$

将 S 中所有顶点的守恒方程求和：

$$|f| = \sum_{u \in S} \left[\sum_{v \in V} f(u,v) - \sum_{v \in V} f(v,u)\right]$$

将 V 拆成 S 和 T：

$$= \sum_{u \in S} \sum_{v \in S} f(u,v) - \sum_{u \in S} \sum_{v \in S} f(v,u) + \sum_{u \in S} \sum_{v \in T} f(u,v) - \sum_{u \in S} \sum_{v \in T} f(v,u)$$

前两项相消（互换求和下标后完全相同），所以：

$$|f| = f(S, T) = \sum_{u \in S} \sum_{v \in T} f(u,v) - \sum_{u \in S} \sum_{v \in T} f(v,u)$$ ∎

**引理2（流值上界）**：对任意流 f 和任意割 (S, T)：

$$|f| \leq c(S, T)$$

**证明**：

$$|f| = f(S,T) = \sum_{u∈S,v∈T} f(u,v) - \sum_{u∈S,v∈T} f(v,u)$$
$$\leq \sum_{u∈S,v∈T} f(u,v) \quad (\text{因为} \sum f(v,u) \geq 0)$$
$$\leq \sum_{u∈S,v∈T} c(u,v) = c(S,T) \quad (\text{容量限制})$$ ∎

### B.2 定理完整证明

**最大流最小割定理**：以下三个条件等价：

**(1)** f 是最大流
**(2)** 残存网络 G_f 中无增广路径
**(3)** |f| = c(S,T) 对某个割 (S,T)

**证明**：

**(1) → (2)**：反证法。若 G_f 中有增广路径 p，则沿 p 可增加流量 c_f(p) > 0，与 f 最大矛盾。

**(2) → (3)**：令 S = {v : G_f 中 s 可达 v}，T = V - S。

- s ∈ S（平凡），t ∈ T（因为无增广路径）
- 对 u ∈ S, v ∈ T：G_f 中无 (u,v)（否则 v ∈ S）
- 所以 c_f(u,v) = 0，即 c(u,v) = f(u,v)（正向满了）
- 且 f(v,u) = 0（反向没有流）
- 因此 f(S,T) = Σc(u,v) - 0 = c(S,T)
- 由引理1，|f| = f(S,T) = c(S,T)

**(3) → (1)**：对任意流 f'，由引理2：|f'| ≤ c(S,T) = |f|。所以 f 是最大流。 ∎

### B.3 推论：弱对偶性

最大流 ≤ 最小割（弱对偶性）是引理2的直接推论。

最大流 = 最小割（强对偶性）是定理 (3)→(1) 方向的结果：存在割使得等号成立，这个割就是最小割。

---

## 📎 附录C：常用最大流算法模板选择指南

### C.1 根据问题规模选择

```
问题规模          推荐算法         理由
─────────────────────────────────────────
|V| ≤ 100       Edmonds-Karp     实现简单，够用
|V| ≤ 1000      Dinic            效率好，代码适中
|V| ≤ 100000    Dinic/HLPP       需要邻接表优化
竞赛编程          Dinic            通用性最好
面试/笔试         Edmonds-Karp     短时间内能写对
```

### C.2 根据图的特征选择

```
图的特征           推荐算法         理由
─────────────────────────────────────────
单位容量图         Dinic            O(E√V)
二分图匹配         匈牙利/Dinic     专用更快
稠密图(E≈V²)      推送-重标记       O(V³)
有负费用           最小费用流        不同问题
多次查询不同s,t   Gomory-Hu树      预处理O(V)次最大流
```

---

*笔记完成时间: 2026年6月*
*下一章预告: 第27章 多线程算法*
