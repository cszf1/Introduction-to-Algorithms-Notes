# 第18章 B树 (B-Trees)

> 本章介绍一种专为磁盘等外部存储设备设计的数据结构——B树，它能在海量数据中实现高效查找

## 📖 本章导读

### 🎯 问题引入：为什么需要B树？

**生活中的例子**：

想象你在一个**超大型图书馆**找书：
- **二叉搜索树的场景**：图书馆只有几千本书，你可以一层层书架找
- **B树的场景**：图书馆有**上亿本书**，书架有几十层高

如果用二叉搜索树，光是定位书架就要几十次磁盘访问。但B树的设计就是为了**减少磁盘I/O次数**，让每次读取尽可能多地获取数据。

**核心区别**：

| 数据结构 | 适用场景 | 特点 |
|---------|---------|------|
| 二叉搜索树 | 数据在内存中 | 内存访问很快 |
| **B树** | **数据在磁盘上** | **专为减少磁盘I/O设计** |

### 💡 B树的核心设计思想

1. **大分支因子**：每个结点可以有几十到几千个孩子
2. **磁盘块对齐**：每个结点的大小 ≈ 磁盘页大小
3. **矮胖结构**：树的高度比二叉树低很多
4. **所有叶子同深度**：保证查找路径长度一致

**一个典型的B树**：
- 最小度数 t = 50（常见值）
- 根结点可持久保存在内存中
- 查找任意关键字最多只需 **2次磁盘读取**

---

## 1️⃣ B树的定义与性质

### 1.1 B树的数据结构

**结点属性**（每个结点x）：

```python
class BTreeNode:
    def __init__(self, leaf=False):
        self.leaf = leaf              # 是否为叶子结点
        self.n = 0                    # 当前关键字个数
        self.keys = []                # 关键字数组 (已排序)
        self.children = []            # 孩子指针数组
```

**伪代码形式**（来自教材）：

```
每个结点 x 包含：
- x.n：当前关键字个数
- x.key_1, x.key_2, ..., x.key_{x.n}：关键字（已排序）
- x.leaf：是否为叶子结点
- x.c_1, x.c_2, ..., x.c_{x.n+1}：孩子指针（仅内部结点）
```

### 1.2 关键字的分割作用

**B树的关键字就是"分隔点"**：

```
        [    15    |    30    |    45    ]
       /          |          |          \
   < 15        15~30        30~45        > 45
  子树0        子树1        子树2        子树3
```

如果关键字 k 不在内部结点 x 中，则 k 必定落在某个区间：
```
key_1 ≤ k ≤ key_2  →  进入 c_2 子树
key_2 ≤ k ≤ key_3  →  进入 c_3 子树
...
```

### 1.3 B树的五大性质

```
┌─────────────────────────────────────────────────────────┐
│                    B树的五大性质                          │
├─────────────────────────────────────────────────────────┤
│  性质1：每个结点 x 有 x.n 个关键字                        │
│                                                         │
│  性质2：关键字按非降序排列：key_1 ≤ key_2 ≤ ...          │
│                                                         │
│  性质3：每个内部结点有 x.n+1 个孩子指针                   │
│                                                         │
│  性质4：所有叶子结点深度相同（树的高度 h）                 │
│                                                         │
│  性质5：关键字数有上下界（用最小度数 t ≥ 2 表示）          │
│         - 非根结点：至少有 t-1 个关键字                  │
│         - 每个结点：至多有 2t-1 个关键字（满结点）        │
└─────────────────────────────────────────────────────────┘
```

### 1.4 最小度数 t 的含义

| 参数 | 含义 | 举例（t=3） |
|------|------|------------|
| t | 最小度数 | 3 |
| t-1 | 非根结点最少关键字数 | 2 |
| 2t-1 | 每个结点最多关键字数 | 5 |
| t | 非根结点最少孩子数 | 3 |
| 2t | 每个结点最多孩子数 | 6 |

### 1.5 特殊：B树的根结点

根结点有**特殊待遇**：
- 非空树：根结点至少要有 **1个关键字**（而不是 t-1 个）
- 空树：创建时根为叶子结点，0个关键字

**FAQ：为什么 t ≥ 2？**

> 如果 t = 1，则每个结点至少有 0 个关键字（即可以有空结点），这在逻辑上没有意义，也会导致定义混乱。

### 1.6 B树高度分析

**定理 18.1**：如果 n ≥ 1，最小度数为 t ≥ 2 的 B 树高度 h 满足：

$$h \leq \log_t \frac{n+1}{2}$$

**证明思路**（图解）：

```
                    根 (至少1个关键字)
                   /  |  \  ...  \
               t-1  t-1  t-1  ... (每个分支至少有 t-1 个关键字)
              
深度1: 至少有 2 个结点
深度2: 至少有 2t 个结点  
深度3: 至少有 2t² 个结点
...
深度h: 至少有 2t^{h-1} 个结点

总关键字数 n ≥ 1 + (t-1) × (2 + 2t + 2t² + ... + 2t^{h-1})
       ≥ 1 + 2(t-1) × (t^h - 1)/(t-1)
       = 2t^h - 1

所以：t^h ≤ (n+1)/2
     h ≤ log_t((n+1)/2)  ✓
```

**对比红黑树**：虽然两者高度都是 O(log n)，但B树的底数 t 可以是 50~2000，这意味着**实际高度要低得多**！

---

## 2️⃣ B树的基本操作

### 2.1 磁盘操作

B树算法需要考虑两种时间：
1. **磁盘I/O时间**：主要成本
2. **CPU计算时间**：相对较小

```python
def disk_read(node):
    """将结点从磁盘读入内存"""
    # 实际中：读取一个磁盘页（4KB~16KB）
    pass

def disk_write(node):
    """将结点从内存写回磁盘"""
    # 实际中：写入一个磁盘页
    pass
```

**约定**：
- 根结点常驻内存，无需DISK-READ
- 每次递归调用前需要DISK-READ

### 2.2 查找操作

**查找思路**（类似二叉搜索树的多路版本）：

```
B-TREE-SEARCH(x, k):
    i = 1
    while i ≤ x.n and k > x.key_i:
        i = i + 1
    
    if i ≤ x.n and k == x.key_i:
        return (x, i)      # 找到！
    
    if x.leaf:
        return NIL         # 到达叶子，未找到
    
    DISK-READ(x.c_i)       # 读取孩子结点
    return B-TREE-SEARCH(x.c_i, k)
```

**图解查找过程**：

```
查找关键字 k = 25

                    [ 15 | 30 | 45 ]
                   /      |      \
        k < 15              |              k > 45
          ↓               15 < k < 30          ↓
      [5|10]                  ↓                 [50|60]
         ↓               [20|25|28]              ↓
        ...              /   |    \           ...
                    k=25!    |      \
                            叶子    叶子
        
时间复杂度：O(t × log_t n) = O(log n)
磁盘访问：最多 h+1 次 = O(log_t n) 次
```

**Python实现**：

```python
def btree_search(node, key):
    """
    在B树中查找关键字key
    
    参数:
        node: 当前检查的结点
        key: 要查找的关键字
    返回:
        (node, index) 如果找到
        None 如果未找到
    """
    # 在当前结点的keys中进行线性查找
    i = 0
    while i < node.n and key > node.keys[i]:
        i += 1
    
    # 找到匹配关键字
    if i < node.n and key == node.keys[i]:
        return (node, i)
    
    # 到达叶子结点，未找到
    if node.leaf:
        return None
    
    # 读取目标子树，继续查找
    disk_read(node.children[i])
    return btree_search(node.children[i], key)
```

**C++实现**：

```cpp
// B树结点结构
struct BTreeNode {
    int n;              // 当前关键字个数
    bool leaf;          // 是否为叶子结点
    int keys[MAX_KEYS]; // 关键字数组
    BTreeNode* children[MAX_CHILDREN]; // 孩子指针数组
    
    BTreeNode(bool is_leaf) : n(0), leaf(is_leaf) {}
};

// 查找函数
pair<BTreeNode*, int> btree_search(BTreeNode* x, int k) {
    // 在keys中找到第一个 >= k 的位置
    int i = 0;
    while (i < x->n && k > x->keys[i]) {
        i++;
    }
    
    // 找到关键字
    if (i < x->n && k == x->keys[i]) {
        return {x, i};
    }
    
    // 到达叶子，未找到
    if (x->leaf) {
        return {nullptr, -1};
    }
    
    // 读取并递归查找子树
    disk_read(x->children[i]);
    return btree_search(x->children[i], k);
}
```

### 2.3 创建空B树

```python
def btree_create():
    """创建一个空的B树"""
    new_node = BTreeNode(leaf=True)  # 初始为叶子结点
    disk_write(new_node)             # 写回磁盘
    return new_node
```

**时间复杂度**：O(1)（实际操作很少）
**磁盘I/O**：1次（创建并写回根结点）

### 2.4 分裂操作 (Split-Child)

**什么时候需要分裂？**

当向一个**已满的结点**（有 2t-1 个关键字）插入新关键字时，必须先分裂。

**分裂过程图解**：

```
分裂前（满结点，5个关键字，t=3）：
                    父结点
                      |
                  [ 20 | 40 ]
                 /     |     \
        [10|15|20|25|30]  [50|60|70]   ← 要分裂的满结点
    
分裂过程：
- 将中间的key[3]=25提升到父结点
- 前半部分(10,15)留在原结点
- 后半部分(30)放到新结点

分裂后：
                    父结点
                  [ 20 | 25 | 40 ]
                 /     |     |     \
        [10|15]  [30]  [50|60|70]
           ↓       ↓        ↓
         左子     新子      右子
```

**分裂代码**：

```python
def btree_split_child(parent, i, child):
    """
    分裂child结点（必须是满的）
    parent: 父结点
    i: child在parent中的索引
    child: 已满的结点（有2t-1个关键字）
    """
    t = MINIMUM_DEGREE
    
    # 创建新结点（接收后半部分）
    new_node = BTreeNode(leaf=child.leaf)
    new_node.n = t - 1  # 新结点有t-1个关键字
    
    # 复制后半部分关键字到新结点
    for j in range(t - 1):
        new_node.keys[j] = child.keys[j + t]
    
    # 如果不是叶子，复制后半部分孩子
    if not child.leaf:
        for j in range(t):
            new_node.children[j] = child.children[j + t]
    
    # 更新原结点的关键字数
    child.n = t - 1
    
    # 在父结点中为新结点腾出位置
    for j in range(parent.n, i, -1):
        parent.children[j + 1] = parent.children[j]
    parent.children[i + 1] = new_node
    
    # 将中间关键字提升到父结点
    for j in range(parent.n - 1, i - 1, -1):
        parent.keys[j + 1] = parent.keys[j]
    parent.keys[i] = child.keys[t - 1]
    parent.n += 1
    
    # 写回磁盘
    disk_write(child)
    disk_write(new_node)
    disk_write(parent)
```

**时间复杂度**：O(t)
**磁盘I/O**：3次（读写三个结点）

### 2.5 插入操作

**插入策略**：先找到插入位置（叶子结点），如果结点已满则分裂。

**关键函数**：先保证插入路径上的结点不是满的，再执行真正的插入。

```python
def btree_insert(tree, key):
    """
    将key插入B树
    """
    root = tree.root
    
    # 情况1：根结点已满，需要分裂
    if root.n == 2 * MINIMUM_DEGREE - 1:
        new_root = BTreeNode(leaf=False)
        new_root.children[0] = root
        tree.root = new_root
        
        # 分裂原根结点
        btree_split_child(new_root, 0, root)
        
        # 现在新根的children[0]和[1]都不是满的
        btree_insert_nonfull(new_root, key)
    else:
        # 情况2：根结点未满，直接插入
        btree_insert_nonfull(root, key)


def btree_insert_nonfull(node, key):
    """
    向非满结点插入key
    """
    i = node.n - 1
    
    if node.leaf:
        # 在叶子结点中插入
        while i >= 0 and key < node.keys[i]:
            node.keys[i + 1] = node.keys[i]
            i -= 1
        
        node.keys[i + 1] = key
        node.n += 1
        disk_write(node)
    else:
        # 找到应该插入的子树
        while i >= 0 and key < node.keys[i]:
            i -= 1
        i += 1
        
        # 读取目标孩子，检查是否需要分裂
        disk_read(node.children[i])
        
        if node.children[i].n == 2 * MINIMUM_DEGREE - 1:
            btree_split_child(node, i, node.children[i])
            
            # 分裂后，中间关键字提升了
            # 可能需要调整插入位置
            if key > node.keys[i]:
                i += 1
        
        btree_insert_nonfull(node.children[i], key)
```

**C++实现**：

```cpp
// 插入主函数
void btree_insert(BTree* T, int key) {
    BTreeNode* r = T->root;
    
    // 根已满，分裂根
    if (r->n == 2 * T->t - 1) {
        BTreeNode* s = new BTreeNode(false);
        s->children[0] = r;
        T->root = s;
        btree_split_child(s, 0, r);
        btree_insert_nonfull(s, key);
    } else {
        btree_insert_nonfull(r, key);
    }
}

// 向非满结点插入
void btree_insert_nonfull(BTreeNode* x, int key) {
    int i = x->n - 1;
    
    if (x->leaf) {
        // 叶子结点：腾出位置，插入key
        while (i >= 0 && key < x->keys[i]) {
            x->keys[i + 1] = x->keys[i];
            i--;
        }
        x->keys[i + 1] = key;
        x->n++;
        disk_write(x);
    } else {
        // 非叶子结点：找到合适的子树
        while (i >= 0 && key < x->keys[i]) {
            i--;
        }
        i++;
        
        disk_read(x->children[i]);
        
        // 子树已满，需要分裂
        if (x->children[i]->n == 2 * T->t - 1) {
            btree_split_child(x, i, x->children[i]);
            
            // 分裂后可能需要调整
            if (key > x->keys[i]) {
                i++;
            }
        }
        
        btree_insert_nonfull(x->children[i], key);
    }
}

// 分裂函数
void btree_split_child(BTreeNode* x, int i, BTreeNode* y) {
    // y是满结点，有2t-1个关键字
    // 分裂后：左半部分在y，右半部分在新结点
    BTreeNode* z = new BTreeNode(y->leaf);
    z->n = T->t - 1;
    
    // 复制关键字
    for (int j = 0; j < T->t - 1; j++) {
        z->keys[j] = y->keys[j + T->t];
    }
    
    // 复制孩子指针
    if (!y->leaf) {
        for (int j = 0; j < T->t; j++) {
            z->children[j] = y->children[j + T->t];
        }
    }
    
    y->n = T->t - 1;
    
    // 在x中腾出位置
    for (int j = x->n; j > i; j--) {
        x->children[j + 1] = x->children[j];
    }
    x->children[i + 1] = z;
    
    for (int j = x->n - 1; j >= i; j--) {
        x->keys[j + 1] = x->keys[j];
    }
    x->keys[i] = y->keys[T->t - 1];
    x->n++;
    
    disk_write(y);
    disk_write(z);
    disk_write(x);
}
```

### 2.6 插入过程图解

**示例**：向 t=3 的B树依次插入 F, S, Q, K, C...

```
初始：空树

插入 F (根为空，直接插入):
[F]

插入 S (根未满，插入到后面):
[F|S]

插入 Q (根未满):
[F|Q|S]

插入 K (根未满):
[F|K|Q|S]

插入 C (根未满):
[C|F|K|Q|S]

插入 L (根满了！需要分裂):
分裂根前:        分裂后:
[C|F|K|Q|S]  →    [K]
                  /   \
              [C|F]  [Q|S]
                
继续插入 L 到合适位置:
        [K]
        /   \
    [C|F|L]  [Q|S]
```

---

## 3️⃣ 从B树中删除关键字

### 3.1 删除的复杂性

删除比插入更复杂，因为需要**保证删除后结点仍然合法**。

**删除可能涉及的两种情况**：

| 情况 | 说明 | 处理 |
|------|------|------|
| A | 要删除的关键字在叶子结点 | 直接删除 |
| B | 要删除的关键字在内部结点 | 用前驱/后继替代 |
| C | 删除后结点关键字不足 | 借调或合并 |

### 3.2 借调与合并

**借调（左兄弟有多余关键字）**：

```
借调前：
    父结点: [ 20 | 30 ]
           /      \
左子:[10|15]    右子:[40]

借调后（从左子借调20到右子）：
    父结点: [ 15 | 30 ]
           /      \
左子:[10]       右子:[20|40]
```

**合并**：

```
合并前：
    父结点: [ 20 | 30 | 40 ]
           /    |    |    \
左子:[10]  中:[15]  右子:[50]  ← 只有1个关键字(需要借调但兄弟也不够)

合并后（合并左子+中+右子）：
    父结点: [ 30 ]
           /    \
      [10|15|20|40|50]
```

### 3.3 删除代码框架

```python
def btree_delete(node, key):
    """
    从B树中删除关键字key
    """
    # 找到key的位置或应进入的子树
    i = 0
    while i < node.n and key > node.keys[i]:
        i += 1
    
    # 情况1：关键字就在当前结点
    if i < node.n and key == node.keys[i]:
        if node.leaf:
            # 直接在叶子中删除
            for j in range(i, node.n - 1):
                node.keys[j] = node.keys[j + 1]
            node.n -= 1
            disk_write(node)
        else:
            # 在内部结点，用前驱/后继替代
            btree_delete_internal(node, i)
    # 情况2：关键字不在当前结点
    elif node.leaf:
        # 到达叶子仍未找到
        return
    else:
        # 进入子树继续删除
        child = node.children[i]
        disk_read(child)
        
        # 确保孩子至少有t个关键字（否则借调或合并）
        if child.n < MINIMUM_DEGREE:
            btree_fix_shortage(node, i)
            
            # 借调后可能需要调整指针
            if i > node.n:
                i -= 1
        
        btree_delete(node.children[i], key)


def btree_delete_internal(node, i):
    """
    删除内部结点中的第i个关键字
    用前驱或后继替代后删除
    """
    key = node.keys[i]
    
    # 尝试用前驱替代（子树有足够关键字）
    if node.children[i].n >= MINIMUM_DEGREE:
        pred = find_max(node.children[i])
        node.keys[i] = pred
        btree_delete(node.children[i], pred)
    # 用后继替代
    elif node.children[i + 1].n >= MINIMUM_DEGREE:
        succ = find_min(node.children[i + 1])
        node.keys[i] = succ
        btree_delete(node.children[i + 1], succ)
    else:
        # 两个子树关键字都不够，合并
        merge(node, i)
        btree_delete(node.children[i], key)


def btree_fix_shortage(node, i):
    """
    修复孩子关键字不足的问题
    """
    t = MINIMUM_DEGREE
    
    # 有左兄弟且左兄弟有多余关键字，借调
    if i > 0 and node.children[i - 1].n > t - 1:
        borrow_from_left(node, i)
    # 有右兄弟且右兄弟有多余关键字，借调
    elif i < node.n and node.children[i + 1].n > t - 1:
        borrow_from_right(node, i)
    # 左右兄弟都不够，尝试合并
    elif i > 0:
        merge(node, i - 1)
    else:
        merge(node, i)
```

---

## 4️⃣ B树的变种

### 4.1 B+树

**B+树是B树的变种**，在数据库和文件系统中更常用：

| 特征 | B树 | B+树 |
|------|-----|------|
| 卫星数据 | 存在所有结点 | 只存在叶子结点 |
| 内部结点 | 只存关键字和指针 | 只存关键字和指针 |
| 叶子结点 | 所有叶子深度相同 | 用链表连接所有叶子 |
| 查找 | 可能在内部结点终止 | 必须到达叶子 |

```
B+树结构示例：

内部结点: [ 15 | 30 ]
           /    |    \
叶子:    [5,10][15,20][30,40] ← 数据都在叶子
          ↓      ↓      ↓
        [数据] [数据] [数据] ← 叶子用链表连接
```

**B+树的优点**：
- 内部结点更小，可以容纳更多孩子（更高的分支因子）
- 叶子链表支持范围查询

### 4.2 B*树

B*树要求**每个结点至少是2/3满的**（而不是1/2满）：

- 非根结点：至少 ⌈2t/3⌉ 个关键字
- 满结点：先尝试借调给兄弟，两个兄弟都满了才分裂

这使得B*树的**空间利用率更高**（至少2/3）。

---

## 5️⃣ B树 vs 红黑树

| 对比项 | B树 | 红黑树 |
|--------|-----|--------|
| 应用场景 | 磁盘存储 | 内存存储 |
| 分支因子 | 50~2000 | 最多3 |
| 高度 | O(log_t n) | O(log n) |
| 磁盘I/O | 极少（主要优势） | 多 |
| 实现复杂度 | 较复杂 | 较简单 |

**为什么B树在磁盘场景下更快？**

```
假设存储 10亿 条记录：

红黑树（假设高度25）:
- 查找需要：25次磁盘访问
- 每次磁盘访问：10ms
- 总时间：250ms

B树（t=100，高度3）:
- 查找需要：3-4次磁盘访问
- 每次磁盘访问：10ms
- 总时间：30-40ms
```

---

## 6️⃣ 实际应用场景

### 6.1 数据库索引

**B树是关系数据库索引的主流实现**：

```
MySQL InnoDB: 使用B+树作为主键索引
Oracle: 使用B树索引
PostgreSQL: 支持B树和B+树索引
```

### 6.2 文件系统

- **NTFS**（Windows）：使用B+树
- **HFS+**（macOS）：使用B树
- **ext4**（Linux）：使用extent（B树的变种）

### 6.3 AI for EDA中的意义

在EDA（电子设计自动化）领域，B树相关数据结构用于：
- **版图数据管理**：海量标准单元的快速检索
- **设计规则检查**：快速定位违反规则的几何图形
- **布线算法**：高效查询可用资源

---

## 📝 本章小结

### 核心要点

1. **B树设计目标**：减少磁盘I/O次数
2. **核心性质**：每个结点最多2t-1个关键字，所有叶子同深度
3. **高度上界**：h ≤ log_t((n+1)/2)
4. **基本操作**：
   - 查找：O(t log_t n) = O(log n)
   - 插入：先分裂，O(t log_t n)
   - 删除：可能需要借调或合并

### FAQ 常见疑问

**Q1: B树和二叉搜索树都能O(log n)查找，B树的优点在哪？**

> 关键在于**底数不同**。二叉树底数是2，B树底数是t（通常50~2000）。这意味着B树的高度**低得多**，磁盘访问次数也少得多。

**Q2: 为什么最小度数 t 要 ≥ 2？**

> 如果 t = 1，每个结点可以有0个关键字（只有根例外）。这会导致定义混乱，删除操作也更复杂。所以算法导论规定 t ≥ 2。

**Q3: B树在实际中是如何权衡的？**

> t 的选择取决于磁盘页大小。例如：
> - 磁盘页 4KB，每个关键字+指针 8字节
> - 则 t ≈ 4KB / 8B ≈ 500
> - 查找10亿条记录只需约3次磁盘访问！

---

## 📚 参考文献

- Thomas H. Cormen et al., *Introduction to Algorithms*, Chapter 18
- 《算法导论》第18章：B树
