# 第33章 计算几何（Computational Geometry）

> 本章回答一个核心问题：如何用算法高效地解决几何问题？——从判断两条线段是否相交的基础操作，到用扫描线算法检测多条线段的相交关系，到寻找点集凸包的Graham扫描法，再到分治法求最近点对——计算几何是计算机科学中"让几何直觉变成代码"的分支，广泛应用于图形学渲染、机器人路径规划、GIS地理信息系统、自动驾驶碰撞检测等领域

## 📖 本章导读

### 🎯 问题引入：为什么需要计算几何？

**生活中的例子**：

想象你正在用**手机导航**开车：
- 导航软件需要判断你的路线是否会与前方障碍物相交（线段相交检测）
- 它还需要把附近的建筑物轮廓简化成一个"外壳"来快速判断碰撞（凸包）
- 如果你想找最近的加油站，这就是最近点对问题的变体

再想想**游戏开发**：
- 两个角色是否碰撞？——判断两条线段或多边形是否相交
- 怪物的视野范围是一个凸多边形——凸包问题
- AI寻路时找最近的路径点——最近点对

```
计算几何的核心场景：

  游戏碰撞检测        ← 判断线段/多边形是否相交
  机器人导航          ← 路径规划，避障
  GIS地理信息系统     ← 地图叠加分析
  自动驾驶            ← 激光雷达点云处理
  CAD/CAM            ← 工程图纸的几何运算
  分子建模            ← 蛋白质结构的空间关系
```

**本章的四个核心问题**：

| 问题 | 核心思想 | 时间复杂度 | 类比 |
|------|---------|-----------|------|
| 线段性质（33.1） | 叉积判断方向和相交 | O(1) | 用指南针判断方向 |
| 任意线段相交（33.2） | 扫描线从左到右扫 | O(n log n) | 用尺子从左到右扫过纸面 |
| 凸包（33.3） | Graham扫描：排序+栈 | O(n log n) | 给散钉拉一根橡皮筋 |
| 最近点对（33.4） | 分治法：左右递归+合并 | O(n log n) | 先分区域找，再跨区域检查 |

**与前面章节的关系**：

| 对比项 | 第4章 分治策略 | 第17章 摊销分析 | 第33章 计算几何 |
|--------|-------------|----------------|----------------|
| 分治法 | 归并排序 | 无 | 最近点对、凸包预处理 |
| 摊销分析 | 无 | 动态表、多栈弹出 | Graham扫描的POP操作 |
| 扫描线 | 无 | 无 | 任意线段相交检测 |
| 排序 | O(n log n) | 无 | Graham扫描需排序 |

**本章路线图**：

```
线段性质(33.1) ──→ 任意线段相交(33.2) ──→ 凸包(33.3) ──→ 最近点对(33.4)
 叉积/方向          扫描线算法             Graham扫描       分治法
 O(1)判断           O(n log n)            O(n log n)       O(n log n)
 基础工具           事件驱动               排序+栈          递归+合并
```

---

## 1️⃣ 线段的性质（33.1 Line-Segment Properties）

### 1.1 叉积——计算几何的"指南针"

**生活中的类比**：想象你站在一个十字路口，面朝北方：
- 有人告诉你"目的地在你左手边"——这就是**左转**
- "目的地在你右手边"——这就是**右转**
- "目的地就在你正前方"——这就是**共线**

叉积就是帮计算机做这个判断的"指南针"。

**叉积的定义**：

给定两个向量 p₁ = (x₁, y₁) 和 p₂ = (x₂, y₂)，叉积定义为：

```
p₁ × p₂ = x₁y₂ - x₂y₁
```

**叉积的几何意义**：

```
叉积 > 0：p₂ 在 p₁ 的逆时针方向（左转方向）

         p₂
        /
       /  ← 逆时针
      /
     O─────→ p₁

叉积 < 0：p₂ 在 p₁ 的顺时针方向（右转方向）

     O─────→ p₁
      \
       \  ← 顺时针
        \
         p₂

叉积 = 0：p₁ 和 p₂ 共线（方向相同或相反）

     O─────→ p₁─────→ p₂   (同向)
     O─────→ p₁←─────p₂    (反向)
```

**更一般的形式**：对于以 p₀ 为起点的两个有向线段 p₀p₁ 和 p₀p₂：

```
(p₁ - p₀) × (p₂ - p₀) = (x₁ - x₀)(y₂ - y₀) - (x₂ - x₀)(y₁ - y₀)
```

**FAQ：为什么不用角度来判断方向？**

> 角度需要用到三角函数（arctan），计算量大且浮点误差大。叉积只需要乘法和减法，O(1)时间完成，且数值稳定性更好。这就是为什么计算几何中"叉积是万能的"。

### 1.2 判断方向——左转还是右转？

**生活中的类比**：开车导航时，GPS会告诉你"前方500米左转"。计算机怎么知道是"左转"？——叉积！

给定三个点 p₀, p₁, p₂，我们想知道从 p₀p₁ 方向走到 p₁ 后，朝 p₂ 方向是左转还是右转：

```
方向判断：计算 d = (p₁ - p₀) × (p₂ - p₁)

d > 0 → 左转（逆时针）
d < 0 → 右转（顺时针）
d = 0 → 直行（三点共线）
```

```
图解：左转 vs 右转

   左转 (d > 0)：          右转 (d < 0)：

   p₂                        p₁
   ↑                        /
   |                       /
   p₁ ──→                p₀
   ↗
  p₀                          p₂
                                \
                                 ↘
```

**Python实现**：

```python
def cross_product(p0, p1, p2):
    """
    计算叉积 (p1-p0) × (p2-p0)
    参数：p0, p1, p2 都是 (x, y) 元组
    返回：叉积值（>0逆时针，<0顺时针，=0共线）
    """
    return (p1[0] - p0[0]) * (p2[1] - p0[1]) - (p2[0] - p0[0]) * (p1[1] - p0[1])

def direction(p0, p1, p2):
    """
    判断从 p0→p1→p2 的转向
    返回：'LEFT' / 'RIGHT' / 'COLLINEAR'
    """
    d = cross_product(p0, p1, p2)
    if d > 0:
        return 'LEFT'      # 左转（逆时针）
    elif d < 0:
        return 'RIGHT'     # 右转（顺时针）
    else:
        return 'COLLINEAR' # 共线
```

**C++实现**：

```cpp
// 计算叉积 (p1-p0) × (p2-p0)
// 返回值：>0逆时针，<0顺时针，=0共线
double cross(Point p0, Point p1, Point p2) {
    return (p1.x - p0.x) * (p2.y - p0.y)
         - (p2.x - p0.x) * (p1.y - p0.y);
}

// 判断转向
// 返回：1=左转, -1=右转, 0=共线
int direction(Point p0, Point p1, Point p2) {
    double d = cross(p0, p1, p2);
    if (d > EPS) return 1;       // 左转
    else if (d < -EPS) return -1; // 右转
    else return 0;                // 共线
}
```

### 1.3 判断两条线段是否相交

**生活中的类比**：想象桌上有两根筷子，你想知道它们是否交叉：
- 如果两根筷子各自"跨过"对方所在的那条直线——它们一定交叉
- 如果其中一根筷子的端点正好落在另一根上——也是"相交"（边界情况）

**判断两条线段相交的步骤**：

设两条线段为 s₁ = p₁p₂ 和 s₂ = p₃p₄。

**步骤1：快速排斥实验**（粗筛）
- 如果两条线段的包围盒（最小外接矩形）不相交，则线段一定不相交
- 这一步用来快速排除明显不相交的情况

```
包围盒不重叠 → 一定不相交：

  ┌─────┐ s₁的包围盒
  │     │
  └─────┘
                    ┌─────┐ s₂的包围盒
                    │     │
                    └─────┘
   两个盒子完全不重叠，线段不可能相交！
```

**步骤2：跨立实验**（精确判断）
- 线段 s₁ 的两个端点在 s₂ 所在直线的两侧（叉积符号相反）
- 且线段 s₂ 的两个端点在 s₁ 所在直线的两侧（叉积符号相反）
- 如果两条线段互相"跨立"，则它们相交

```
跨立实验：互相跨立 → 一定相交

     p₃
    / \
   /   \
  p₁────p₂    d₁和d₂符号相反（p₃和p₄在p₁p₂的两侧）
   \   /      d₃和d₄符号相反（p₁和p₂在p₃p₄的两侧）
    \ /
     p₄

其中：d₁ = (p₁-p₃)×(p₄-p₃)，d₂ = (p₂-p₃)×(p₄-p₃)
      d₃ = (p₃-p₁)×(p₂-p₁)，d₄ = (p₄-p₁)×(p₂-p₁)
```

**边界情况处理**：
- 如果某个叉积恰好为0，说明对应端点在另一条线段所在的直线上
- 此时需要检查该端点是否在线段上（而不仅仅在直线上）

**完整算法流程**：

```
SEGMENTS-INTERSECT(p1, p2, p3, p4)
1  d1 = direction(p3, p4, p1)   // p1相对于线段p3p4的方向
2  d2 = direction(p3, p4, p2)   // p2相对于线段p3p4的方向
3  d3 = direction(p1, p2, p3)   // p3相对于线段p1p2的方向
4  d4 = direction(p1, p2, p4)   // p4相对于线段p1p2的方向
5  if ((d1>0 and d2<0) or (d1<0 and d2>0)) and   // p1p2跨立p3p4
6     ((d3>0 and d4<0) or (d3<0 and d4>0))       // p3p4跨立p1p2
7      return TRUE                                // 严格相交
8  if d1==0 and on-segment(p3, p4, p1)   // p1在线段p3p4上
9      return TRUE
10 if d2==0 and on-segment(p3, p4, p2)   // p2在线段p3p4上
11     return TRUE
12 if d3==0 and on-segment(p1, p2, p3)   // p3在线段p1p2上
13     return TRUE
14 if d4==0 and on-segment(p1, p2, p4)   // p4在线段p1p2上
15     return TRUE
16 return FALSE                          // 不相交
```

**判断点是否在线段上**：

```
ON-SEGMENT(pi, pj, pk)
  // 前提：pk已经在pi-pj所在的直线上（叉积=0）
  // 检查pk是否在pi和pj构成的矩形内
1  if min(pi.x, pj.x) ≤ pk.x ≤ max(pi.x, pj.x) and
2     min(pi.y, pj.y) ≤ pk.y ≤ max(pi.y, pj.y)
3      return TRUE
4  else return FALSE
```

**图解完整过程**：

```
例1：严格相交

  p₁(0,0)──────────p₂(4,0)
          \    /
           \  /
            \/
            /\
           /  \
          /    \
  p₃(1,-1)──────p₄(3,1)

d1 = direction(p3,p4,p1) = +2 > 0
d2 = direction(p3,p4,p2) = -2 < 0   ← 符号相反 ✓
d3 = direction(p1,p2,p3) = +1 > 0
d4 = direction(p1,p2,p4) = -1 < 0   ← 符号相反 ✓
→ 互相跨立，相交！


例2：不相交

  p₁(0,0)──────────p₂(4,0)
                              p₃(5,1)
                             /
                            /
                           p₄(6,0)

d1 = direction(p3,p4,p1) = 1 > 0
d2 = direction(p3,p4,p2) = 2 > 0   ← 同号 ✗
→ 不跨立，不相交！
```

**Python完整实现**：

```python
def on_segment(pi, pj, pk):
    """
    判断点pk是否在线段pi-pj上
    前提：pk已经在pi-pj所在的直线上
    """
    return (min(pi[0], pj[0]) <= pk[0] <= max(pi[0], pj[0]) and
            min(pi[1], pj[1]) <= pk[1] <= max(pi[1], pj[1]))

def segments_intersect(p1, p2, p3, p4):
    """
    判断线段p1-p2和线段p3-p4是否相交
    返回：True/False
    """
    d1 = cross_product(p3, p4, p1)  # p1相对于p3p4的方向
    d2 = cross_product(p3, p4, p2)  # p2相对于p3p4的方向
    d3 = cross_product(p1, p2, p3)  # p3相对于p1p2的方向
    d4 = cross_product(p1, p2, p4)  # p4相对于p1p2的方向

    # 情况1：严格跨立（叉积符号相反）
    if ((d1 > 0 and d2 < 0) or (d1 < 0 and d2 > 0)) and \
       ((d3 > 0 and d4 < 0) or (d3 < 0 and d4 > 0)):
        return True

    # 情况2：边界情况（某个端点在另一条线段上）
    if d1 == 0 and on_segment(p3, p4, p1): return True
    if d2 == 0 and on_segment(p3, p4, p2): return True
    if d3 == 0 and on_segment(p1, p2, p3): return True
    if d4 == 0 and on_segment(p1, p2, p4): return True

    return False
```

**C++完整实现**：

```cpp
const double EPS = 1e-9;  // 浮点精度

// 叉积
double cross(Point p0, Point p1, Point p2) {
    return (p1.x - p0.x) * (p2.y - p0.y)
         - (p2.x - p0.x) * (p1.y - p0.y);
}

// 判断点pk是否在线段pi-pj上（前提：pk在pi-pj直线上）
bool onSegment(Point pi, Point pj, Point pk) {
    return min(pi.x, pj.x) - EPS <= pk.x && pk.x <= max(pi.x, pj.x) + EPS
        && min(pi.y, pj.y) - EPS <= pk.y && pk.y <= max(pi.y, pj.y) + EPS;
}

// 判断线段p1p2和p3p4是否相交
bool segmentsIntersect(Point p1, Point p2, Point p3, Point p4) {
    double d1 = cross(p3, p4, p1);
    double d2 = cross(p3, p4, p2);
    double d3 = cross(p1, p2, p3);
    double d4 = cross(p1, p2, p4);

    // 严格跨立
    if (((d1 > EPS && d2 < -EPS) || (d1 < -EPS && d2 > EPS)) &&
        ((d3 > EPS && d4 < -EPS) || (d3 < -EPS && d4 > EPS)))
        return true;

    // 边界情况
    if (fabs(d1) < EPS && onSegment(p3, p4, p1)) return true;
    if (fabs(d2) < EPS && onSegment(p3, p4, p2)) return true;
    if (fabs(d3) < EPS && onSegment(p1, p2, p3)) return true;
    if (fabs(d4) < EPS && onSegment(p1, p2, p4)) return true;

    return false;
}
```

**FAQ：为什么需要两步（排斥+跨立），不能只用跨立？**

> 只用跨立实验已经足够了！排斥实验只是一个优化，用来快速排除明显不相交的情况，避免计算叉积。在实际实现中，如果你不在意那一点点常数优化，只做跨立实验完全可以。但注意：如果两条线段共线但不重叠，只做跨立实验不够，还需要on-segment判断。

---

## 2️⃣ 判断任意一对线段是否相交（33.2 Determining Whether Any Pair of Segments Intersects）

### 2.1 问题定义

**输入**：n条线段的集合 S = {s₁, s₂, ..., sₙ}
**输出**：是否存在至少一对线段相交？

朴素方法：检查所有 C(n,2) = O(n²) 对线段，每对O(1)判断，总共O(n²)。

**能更快吗？** 能！用扫描线算法，可以达到 O(n log n)。

### 2.2 扫描线算法——核心思想

**生活中的类比**：想象你拿着一把**竖直的尺子**，从纸的左边慢慢滑到右边：
- 尺子滑过的地方，你能看到哪些线段在尺子位置上
- 如果两条线段在某个位置"交换了上下顺序"（之前A在B上方，之后B在A上方），那它们一定相交了！

```
扫描线示意：

  初始状态：扫描线在最左边
  |
  |  s₂──────────────
  |      s₁──────────────────
  |          s₃────────────────
  |
  ──────────────────────────→ x

  扫描线向右移动，经过s₁的左端点时，s₁"出现"了
  经过s₂的左端点时，s₂"出现"了
  在某个位置，s₁和s₂的上下关系变了→相交！
```

### 2.3 关键概念

**1. 扫描线状态T**：
- 当前扫描线位置处，所有与扫描线相交的线段的**上下有序集合**
- 用平衡BST维护（如红黑树），支持插入、删除、查询上方/下方线段

**2. 事件点**：
- 每条线段的两个端点（左端点=入事件，右端点=出事件）
- 按x坐标从小到大排序，扫描线依次经过这些事件点

**3. 核心观察**：
> 如果两条线段相交，那么在交点处它们在扫描线状态T中一定是**相邻**的。

这非常关键！它意味着我们不需要检查所有线段对，只需要检查T中相邻的线段对。

```
为什么相邻才可能相交？

不相邻的线段之间被别的线段隔开了，不可能直接相交：

  扫描线位置：T中顺序是 a > b > c

  a ────────────── a在b上方
      b ────────── b在c上方
          c ───────

  a和c之间隔着b，不可能直接相交
  只需检查 (a,b) 和 (b,c) 这两对相邻线段
```

### 2.4 算法伪代码

```
ANY-SEGMENTS-INTERSECT(S)
1  T = ∅                           // 初始化扫描线状态（空BST）
2  对S中所有线段的2n个端点按x坐标排序  // 事件点排序
3  for 每个事件点p（从左到右）
4      if p是某条线段s的左端点     // 入事件
5          INSERT(T, s)            // 将s插入T
6          if ABOVE(T,s)与s相交 or BELOW(T,s)与s相交
7              return TRUE         // 找到相交！
8      else                        // p是右端点，出事件
9          s = 包含p作为右端点的线段
10         if ABOVE(T,s)与BELOW(T,s)相交  // 删除前检查邻居
11             return TRUE
12         DELETE(T, s)            // 将s从T中删除
13 return FALSE                    // 没有相交
```

### 2.5 图解过程

```
示例：4条线段，判断是否有相交

线段：
s₁: (1,3)→(6,1)   s₂: (2,4)→(7,3)
s₃: (3,2)→(8,0)   s₄: (4,5)→(5,0)  ← s₄与s₁相交！

扫描过程：

事件点（按x排序）：1,2,3,4,5,6,7,8

x=1: s₁左端点 → INSERT(s₁), T={s₁}          无邻居
x=2: s₂左端点 → INSERT(s₂), T={s₂,s₁}       检查(s₂,s₁)→不相交
x=3: s₃左端点 → INSERT(s₃), T={s₂,s₁,s₃}    检查(s₁,s₃)→不相交
x=4: s₄左端点 → INSERT(s₄), T={s₂,s₄,s₁,s₃} 检查(s₂,s₄)?(s₄,s₁)
     → s₄与s₁相邻且相交！return TRUE ✓
```

### 2.6 复杂度分析

| 操作 | 次数 | 每次时间 | 小计 |
|------|------|---------|------|
| 事件点排序 | 1次 | O(n log n) | O(n log n) |
| INSERT | n次 | O(log n) | O(n log n) |
| DELETE | n次 | O(log n) | O(n log n) |
| 相交检查 | ≤2n次 | O(1) | O(n) |

**总时间复杂度：O(n log n)**

### 2.7 Python实现

```python
def any_segments_intersect(segments):
    """
    判断n条线段中是否存在至少一对相交
    segments: 线段列表，每个线段是 ((x1,y1), (x2,y2))
    返回：True/False
    """
    # 构建事件点列表：(x坐标, 类型, 线段索引)
    events = []
    for i, ((x1, y1), (x2, y2)) in enumerate(segments):
        if x1 > x2:  # 确保左端点在前
            x1, y1, x2, y2 = x2, y2, x1, y1
        events.append((x1, 'left', i))   # 左端点事件
        events.append((x2, 'right', i))  # 右端点事件

    events.sort()  # 按x坐标排序

    T = []  # 扫描线状态（简单用列表，实际用BST更高效）

    for x, event_type, seg_idx in events:
        s = segments[seg_idx]

        if event_type == 'left':
            # 插入线段，按扫描线位置处的y坐标排序
            T.append(seg_idx)
            T.sort(key=lambda i: segments[i][0][1])  # 简化排序

            # 检查与邻居是否相交
            pos = T.index(seg_idx)
            if pos > 0:  # 有上方邻居
                neighbor = T[pos - 1]
                if segments_intersect(s[0], s[1],
                                      segments[neighbor][0], segments[neighbor][1]):
                    return True
            if pos < len(T) - 1:  # 有下方邻居
                neighbor = T[pos + 1]
                if segments_intersect(s[0], s[1],
                                      segments[neighbor][0], segments[neighbor][1]):
                    return True

        else:  # 'right'
            pos = T.index(seg_idx)
            above = T[pos - 1] if pos > 0 else None
            below = T[pos + 1] if pos < len(T) - 1 else None

            # 删除前检查上方和下方邻居是否相交
            if above is not None and below is not None:
                if segments_intersect(segments[above][0], segments[above][1],
                                      segments[below][0], segments[below][1]):
                    return True

            T.remove(seg_idx)

    return False
```

### 2.8 C++实现

```cpp
#include <vector>
#include <set>
#include <algorithm>
using namespace std;

struct Segment {
    Point p1, p2;  // p1.x <= p2.x
    int id;
};

// 扫描线状态中的比较函数（按当前x位置的y坐标排序）
struct SweepCompare {
    double x;  // 当前扫描线x坐标
    bool operator()(const Segment& a, const Segment& b) const {
        // 计算在x处两条线段的y值并比较
        double ya = a.p1.y + (x - a.p1.x) * (a.p2.y - a.p1.y) / (a.p2.x - a.p1.x);
        double yb = b.p1.y + (x - b.p1.x) * (b.p2.y - b.p1.y) / (b.p2.x - b.p1.x);
        return ya < yb;
    }
};

bool anySegmentsIntersect(vector<Segment>& segs) {
    // 构建事件点
    struct Event {
        double x;
        int type;  // 0=左端点, 1=右端点
        int segId;
    };
    vector<Event> events;
    for (auto& s : segs) {
        if (s.p1.x > s.p2.x) swap(s.p1, s.p2);
        events.push_back({s.p1.x, 0, s.id});
        events.push_back({s.p2.x, 1, s.id});
    }
    sort(events.begin(), events.end(),
         [](const Event& a, const Event& b) { return a.x < b.x; });

    set<Segment, SweepCompare> T(SweepCompare{});
    // 注意：实际实现需维护SweepCompare中的x值
    // 此处为算法框架，省略细节

    for (auto& e : events) {
        // ... INSERT/DELETE/检查邻居逻辑
    }
    return false;
}
```

**FAQ：扫描线算法只能判断"是否存在相交"，能找出所有交点吗？**

> 本节的算法只回答"是否存在"（检测问题），时间O(n log n)。如果要报告所有交点，最坏情况下可能有O(n²)个交点（如网格状排列），所以时间也是O(n²)。但有改进算法可以在O(n log n + k)时间内报告k个交点。

---

## 3️⃣ 寻找凸包（33.3 Finding the Convex Hull）

### 3.1 什么是凸包？

**生活中的类比**：想象你在桌上撒了一把图钉，然后用一根橡皮筋围住它们——橡皮筋绷紧后形成的形状就是**凸包**。

```
凸包的直观理解：

  散点：                    凸包（橡皮筋）：

    ·  ·                    *───*
  ·    ·  ·               /     \
 ·  ·    ·               ·       ·
    ·  ·  ·               \     /
      ·                    *───*

  凸包 = 包含所有点的最小凸多边形
  凸包上的点 = "最外围"的点
```

**形式化定义**：

点集Q的凸包 CH(Q) 是包含Q的最小凸集。直观地说，就是"包含Q中所有点的最小凸多边形"。

**凸性**：多边形内任意两点的连线都在多边形内。

```
凸 vs 非凸：

  凸多边形：                  非凸多边形：
  ·─────·                    ·─────·
  │╲   ╱│                    │     │
  │ ╲ ╱ │                    │  ·──│ ← 这里凹进去了
  │  ╳  │                    │     │
  │ ╱ ╲ │                    ·─────·
  │╱   ╲│
  ·─────·
  任意两点连线都在内部        有连线跑到外面去了
```

### 3.2 Graham扫描法

**核心思想**：排序 + 栈维护

**步骤**：

1. **找基准点 p₀**：选y坐标最小的点（y相同选x最小的）→ 一定在凸包上
2. **按极角排序**：其余点按与p₀的极角逆时针排序（极角相同只留最远的）
3. **栈扫描**：用栈维护凸包顶点，遇到"非左转"就弹出

**生活中的类比**：
- 想象你在用一根绳子从一个钉子（p₀）开始，逆时针绕着所有钉子
- 如果绕到某个钉子时发现绳子"凹进去了"（右转），就跳过这个钉子
- 只有"凸出来"（左转）的钉子才留在绳子上

### 3.3 算法伪代码

```
GRAHAM-SCAN(Q)
1  let p₀ be the point with min y-coordinate (leftmost if tie)
2  let ⟨p₁, p₂, ..., pₘ⟩ be the remaining points sorted by
     polar angle in counterclockwise order around p₀
     (if same angle, keep only the farthest from p₀)
3  let S be an empty stack
4  PUSH(p₀, S)
5  PUSH(p₁, S)
6  PUSH(p₂, S)
7  for i = 3 to m
8      while the angle NEXT-TO-TOP(S), TOP(S), pᵢ
            makes a non-left turn  // 叉积 ≤ 0
9          POP(S)
10     PUSH(pᵢ, S)
11 return S   // 栈中从底到顶就是凸包顶点（逆时针顺序）
```

### 3.4 图解过程

```
输入点集：

  P₈·              ·P₇
            P₆·
  P₁₀·
                     ·P₅
  P₁₁·      P₉·
       P₁₂·
  P₀────────P₁
       P₃·
              ·P₂
          ·P₄

Step 1: 找p₀ → y最小点（底部最左）

Step 2: 按极角排序 → p₁, p₂, ..., p₁₂（逆时针）

Step 3: 栈扫描

初始：S = [p₀, p₁, p₂]

i=3:  p₁p₂p₃ 是左转 → PUSH  S = [p₀, p₁, p₂, p₃]
i=4:  p₂p₃p₄ 是左转 → PUSH  S = [p₀, p₁, p₂, p₃, p₄]
i=5:  p₃p₄p₅ 是非左转 → POP p₄
      p₂p₃p₅ 是左转 → PUSH  S = [p₀, p₁, p₂, p₃, p₅]
i=6:  p₃p₅p₆ 是左转 → PUSH  S = [p₀, p₁, p₂, p₃, p₅, p₆]
i=7:  p₅p₆p₇ 是非左转 → POP p₆
      p₃p₅p₇ 是左转 → PUSH  S = [p₀, p₁, p₂, p₃, p₅, p₇]
i=8:  p₅p₇p₈ 是左转 → PUSH  S = [p₀, p₁, p₂, p₃, p₅, p₇, p₈]
...

最终凸包：p₀, p₁, p₂, p₃, p₅, p₇, p₈, p₁₀, p₁₁
（所有不在凸包上的点p₄, p₆, p₉, p₁₂都被弹出了）
```

### 3.5 正确性论证（要点）

**循环不变量**：在第7-10行for循环的每次迭代开始时：

1. 栈S从底到顶的点是已处理点中"可能是凸包顶点"的子集
2. 这些点按逆时针顺序排列，形成凸多边形
3. 被弹出的点一定不是凸包顶点

**为什么被弹出的点一定不是凸包顶点？**

> 如果NEXT-TO-TOP(S), TOP(S), pᵢ 构成非左转，说明TOP(S)在NEXT-TO-TOP(S)和pᵢ连线的"凹侧"。就像橡皮筋不可能凹进去一样，这个点不可能在凸包上。

### 3.6 复杂度分析

| 步骤 | 时间 |
|------|------|
| 找p₀ | O(n) |
| 极角排序 | O(n log n) |
| 栈扫描 | O(n) ← 每个点最多入栈一次、出栈一次（摊销分析） |

**总时间：O(n log n)**，瓶颈在排序。

**FAQ：栈扫描不是有while循环吗，为什么是O(n)？**

> 这和第17章摊销分析的思想一样：每个点最多被PUSH一次、被POP一次。虽然有while循环可能连续POP多个点，但总共POP的次数不超过PUSH的次数，而PUSH总共只有n次。所以总操作次数 = O(n)。

### 3.7 Python实现

```python
def graham_scan(points):
    """
    Graham扫描法求凸包
    points: 点列表，每个点是 (x, y)
    返回：凸包顶点列表（逆时针顺序）
    """
    # Step 1: 找基准点（y最小，y相同取x最小）
    p0 = min(points, key=lambda p: (p[1], p0[0]) if 'p0' in dir() else (p[1], p[0]))

    # Step 2: 按极角排序
    def polar_angle(p):
        """计算p相对于p0的极角（用叉积代替三角函数）"""
        return (p[0] - p0[0], p[1] - p0[1])  # 用向量代替角度

    def angle_compare(a, b):
        """
        比较两点相对p0的极角
        返回：-1/0/1（用叉积判断）
        """
        d = cross_product(p0, a, b)
        if d > 0:    # a在b的逆时针方向 → a角度更小
            return -1
        elif d < 0:  # a在b的顺时针方向 → a角度更大
            return 1
        else:        # 共线：距离近的排前面，距离远的保留
            da = (a[0]-p0[0])**2 + (a[1]-p0[1])**2
            db = (b[0]-p0[0])**2 + (b[1]-p0[1])**2
            return -1 if da < db else 1

    sorted_pts = sorted([p for p in points if p != p0], key=functools.cmp_to_key(angle_compare))

    # 去除极角相同的较近点（只保留最远的）
    unique = []
    for p in sorted_pts:
        while (len(unique) > 0 and
               cross_product(p0, unique[-1], p) == 0):
            # 保留更远的那个
            d1 = (unique[-1][0]-p0[0])**2 + (unique[-1][1]-p0[1])**2
            d2 = (p[0]-p0[0])**2 + (p[1]-p0[1])**2
            if d2 > d1:
                unique.pop()
            else:
                break
        unique.append(p)
    sorted_pts = unique

    # Step 3: 栈扫描
    if len(sorted_pts) < 2:
        return [p0] + sorted_pts

    stack = [p0, sorted_pts[0], sorted_pts[1]]

    for i in range(2, len(sorted_pts)):
        while len(stack) >= 2:
            # 检查 NEXT-TO-TOP, TOP, pi 是否为非左转
            if cross_product(stack[-2], stack[-1], sorted_pts[i]) <= 0:
                stack.pop()  # 非左转，弹出
            else:
                break
        stack.append(sorted_pts[i])

    return stack
```

### 3.8 C++实现

```cpp
#include <vector>
#include <algorithm>
#include <stack>
using namespace std;

struct Point {
    double x, y;
    bool operator<(const Point& p) const {
        return y < p.y || (y == p.y && x < p.x);
    }
};

// 叉积
double cross(Point o, Point a, Point b) {
    return (a.x - o.x) * (b.y - o.y) - (b.x - o.x) * (a.y - o.y);
}

// 距离平方
double distSq(Point a, Point b) {
    return (a.x - b.x) * (a.x - b.x) + (a.y - b.y) * (a.y - b.y);
}

// 极角比较函数
Point p0;  // 全局基准点
bool polarCompare(Point a, Point b) {
    double d = cross(p0, a, b);
    if (d > 0) return true;        // a在b的逆时针方向
    if (d < 0) return false;       // a在b的顺时针方向
    return distSq(p0, a) < distSq(p0, b);  // 共线：近的排前
}

vector<Point> grahamScan(vector<Point>& pts) {
    int n = pts.size();
    if (n <= 2) return pts;

    // Step 1: 找基准点
    p0 = *min_element(pts.begin(), pts.end());

    // Step 2: 按极角排序
    sort(pts.begin(), pts.end(), polarCompare);

    // 去除极角相同但更近的点
    int m = 1;
    for (int i = 1; i < n; i++) {
        while (i < n - 1 && cross(p0, pts[i], pts[i+1]) == 0)
            i++;
        pts[m++] = pts[i];
    }

    if (m < 3) return vector<Point>(pts.begin(), pts.begin() + m);

    // Step 3: 栈扫描
    vector<Point> hull = {pts[0], pts[1], pts[2]};

    for (int i = 3; i < m; i++) {
        while (hull.size() >= 2 &&
               cross(hull[hull.size()-2], hull[hull.size()-1], pts[i]) <= 0) {
            hull.pop_back();  // 非左转，弹出
        }
        hull.push_back(pts[i]);
    }

    return hull;
}
```

**FAQ：除了Graham扫描，还有其他凸包算法吗？**

> 有！本章还提到了 **Jarvis步进法**（也叫礼物包装法），时间O(nh)（h是凸包顶点数），当h很小时比Graham更快。另外还有 **Andrew单调链算法**（按x坐标排序而非极角，实现更简单），以及 **QuickHull**（类似快排思路，平均O(n log n)）。

---

## 4️⃣ 寻找最近点对（33.4 Finding the Closest Pair of Points）

### 4.1 问题定义

**输入**：平面上n个点的集合 Q
**输出**：Q中距离最近的点对

**距离**：欧几里得距离 d(p₁, p₂) = √[(x₁-x₂)² + (y₁-y₂)²]

**生活中的类比**：
- 空中交通管制系统需要找两架最近的飞机，防止碰撞
- 在地图上找两个最近的快递站点

**朴素方法**：检查所有 C(n,2) 对，O(n²)。

**分治法**：O(n log n)！

### 4.2 分治法核心思想

**关键观察**：把点集按x坐标分成左右两半，分别递归求解，然后合并。

```
分治三步曲：

1. Divide（分解）：用垂直线l把点集分成左右两半 P_L 和 P_R
2. Conquer（求解）：递归求P_L和P_R中的最近点对
3. Combine（合并）：检查"跨越分界线"的点对

   P_L          l          P_R
  · ·                     · ·
    ·        |            ·
  ·   ·      |          ·   ·
      ·      |            ·
    ·        |          ·
```

**合并的难点**：最近点对可能一个点在左半、一个点在右半！

### 4.3 合并步骤详解

设 δ_L = P_L中最近点对距离，δ_R = P_R中最近点对距离，δ = min(δ_L, δ_R)。

**问题**：是否存在一个点对(p_L, p_R)，其距离 < δ？

**关键观察**：如果存在这样的点对，两个点一定在分界线l两侧各δ的范围内！

```
只有这个2δ宽的"条带"内的点对才可能比δ更近：

         δ       δ
    ←───→←───→
         │l│
    ·    │ │    ·
      ·  │ │  ·     ← 这些点在条带内，需要检查
    ·    │ │
         │ │  ·     ← 这个点在条带外，不可能与左侧的点距离<δ
    ·    │ │    ·
```

**更关键的性质**：在条带内，按y坐标排序后，对于每个点，只需检查它后面最多7个点！

**为什么最多7个？**

> 在一个 δ × 2δ 的矩形内，最多能放8个互相距离 ≥ δ 的点（左右各4个，构成一个4×2的网格）。所以对于每个点，后面的第8个点一定距离 ≥ δ。

```
δ × 2δ 矩形内最多8个点（每侧4个）：

  ┌────────────────┐
  │ ·  ·  ·  ·     │ ← 左侧4个
  │       l        │
  │ ·  ·  ·  ·     │ ← 右侧4个
  └────────────────┘
  ←── δ ──→←── δ ──→
```

### 4.4 算法伪代码

```
CLOSEST-PAIR(P, X, Y)
1  if |P| ≤ 3
2      return 暴力法求最近点对
3  构造垂直线l：X中第 ⌈n/2⌉ 个点的x坐标
4  将P分成 P_L（l及左侧）和 P_R（l右侧）
5  将X分成 X_L 和 X_R（分别对应P_L和P_R，按x排序）
6  将Y分成 Y_L 和 Y_R（分别对应P_L和P_R，按y排序）
7  (p₁, p₂) = CLOSEST-PAIR(P_L, X_L, Y_L)   // 递归左半
8  (p₃, p₄) = CLOSEST-PAIR(P_R, X_R, Y_R)   // 递归右半
9  δ = min(dist(p₁,p₂), dist(p₃,p₄))
10 从Y中筛选出距l不超过δ的点，构成数组Y'
11 对Y'中每个点，检查其后7个点的距离
12 if 找到距离 < δ 的点对
13     return 该点对
14 else
15     return min对应的点对
```

### 4.5 图解过程

```
示例：8个点

   ·(2,8)                ·(9,8)
       ·(4,6)    ·(7,6)
   ·(1,4)                    ·(10,4)
       ·(3,2)    ·(8,2)
            ·(5,1)

Step 1: 按x排序，取中位数x=5作为分界线l

   P_L (x≤5)              P_R (x>5)
   (2,8)(4,6)(1,4)        (9,8)(7,6)(10,4)
   (3,2)(5,1)             (8,2)

Step 2: 递归求P_L最近点对
   暴力检查所有对 → δ_L = dist((2,8),(4,6)) = 2√2 ≈ 2.83

Step 3: 递归求P_R最近点对
   暴力检查 → δ_R = dist((7,6),(8,2)) = √17 ≈ 4.12

Step 4: δ = min(δ_L, δ_R) = 2.83

Step 5: 筛选条带内的点（|x - 5| < δ）
   (4,6) x=4, |4-5|=1 < 2.83 ✓
   (5,1) x=5, |5-5|=0 < 2.83 ✓
   (7,6) x=7, |7-5|=2 < 2.83 ✓
   Y' = [(5,1), (4,6), (7,6)]  (按y排序)

Step 6: 对Y'中每个点检查后7个
   (5,1)→(4,6): dist = √(1+25) = √26 ≈ 5.10 > δ
   (5,1)→(7,6): dist = √(4+25) = √29 ≈ 5.39 > δ
   (4,6)→(7,6): dist = 3 > δ

没有更近的 → 返回 (2,8)和(4,6)，距离 ≈ 2.83
```

### 4.6 复杂度分析

**递推关系**：

```
T(n) = 2T(n/2) + O(n)
```

- 2T(n/2)：两次递归调用
- O(n)：分割数组 + 条带检查（每个点最多检查7个）

由主方法：**T(n) = O(n log n)**

**关键优化**：Y数组（按y排序）在递归时不需要重新排序，只需在分割时把Y分成Y_L和Y_R即可（预排序技巧）。如果每次递归都排序，复杂度会变成O(n log²n)。

### 4.7 Python实现

```python
import math

def distance(p1, p2):
    """计算两点之间的欧几里得距离"""
    return math.sqrt((p1[0]-p2[0])**2 + (p1[1]-p2[1])**2)

def brute_force(points):
    """暴力法：对≤3个点求最近点对"""
    min_dist = float('inf')
    pair = None
    for i in range(len(points)):
        for j in range(i+1, len(points)):
            d = distance(points[i], points[j])
            if d < min_dist:
                min_dist = d
                pair = (points[i], points[j])
    return min_dist, pair

def closest_pair_rec(Px, Py):
    """
    最近点对递归函数
    Px: 按x坐标排序的点列表
    Py: 按y坐标排序的点列表
    返回：(最短距离, 最近点对)
    """
    n = len(Px)
    if n <= 3:
        return brute_force(Px)

    # 分割：按x中位数分成左右
    mid = n // 2
    mid_point = Px[mid]

    Qx = Px[:mid]    # 左半（按x排序）
    Rx = Px[mid:]    # 右半（按x排序）

    # 按y坐标分割（保持排序）
    Qy = [p for p in Py if p[0] <= mid_point[0]]   # 左半（按y排序）
    Ry = [p for p in Py if p[0] > mid_point[0]]     # 右半（按y排序）

    # 递归
    delta_L, pair_L = closest_pair_rec(Qx, Qy)
    delta_R, pair_R = closest_pair_rec(Rx, Ry)

    delta = min(delta_L, delta_R)
    best_pair = pair_L if delta_L <= delta_R else pair_R

    # 条带检查
    strip = [p for p in Py if abs(p[0] - mid_point[0]) < delta]

    for i in range(len(strip)):
        # 只检查后面7个点
        for j in range(i+1, min(i+8, len(strip))):
            d = distance(strip[i], strip[j])
            if d < delta:
                delta = d
                best_pair = (strip[i], strip[j])

    return delta, best_pair

def closest_pair(points):
    """
    最近点对主函数
    points: 点列表，每个点是 (x, y)
    返回：(最短距离, 最近点对)
    """
    Px = sorted(points, key=lambda p: p[0])  # 按x排序
    Py = sorted(points, key=lambda p: p[1])  # 按y排序
    return closest_pair_rec(Px, Py)
```

### 4.8 C++实现

```cpp
#include <vector>
#include <algorithm>
#include <cmath>
#include <limits>
using namespace std;

struct Point {
    double x, y;
};

double dist(Point a, Point b) {
    return sqrt((a.x - b.x) * (a.x - b.x) + (a.y - b.y) * (a.y - b.y));
}

// 暴力法（≤3个点）
pair<double, pair<Point,Point>> bruteForce(vector<Point>& P) {
    double minD = numeric_limits<double>::max();
    pair<Point,Point> best;
    for (int i = 0; i < P.size(); i++)
        for (int j = i+1; j < P.size(); j++) {
            double d = dist(P[i], P[j]);
            if (d < minD) { minD = d; best = {P[i], P[j]}; }
        }
    return {minD, best};
}

pair<double, pair<Point,Point>> closestRec(
    vector<Point>& Px, int lo, int hi, vector<Point>& Py) {

    int n = hi - lo;
    if (n <= 3) {
        vector<Point> sub(Px.begin()+lo, Px.begin()+hi);
        return bruteForce(sub);
    }

    int mid = lo + n / 2;
    Point midPoint = Px[mid];

    // 按y分割
    vector<Point> PyL, PyR;
    for (auto& p : Py) {
        if (p.x <= midPoint.x) PyL.push_back(p);
        else PyR.push_back(p);
    }

    auto [dL, pairL] = closestRec(Px, lo, mid, PyL);
    auto [dR, pairR] = closestRec(Px, mid, hi, PyR);

    double delta = min(dL, dR);
    auto best = (dL <= dR) ? pairL : pairR;

    // 条带检查
    vector<Point> strip;
    for (auto& p : Py)
        if (fabs(p.x - midPoint.x) < delta)
            strip.push_back(p);

    for (int i = 0; i < strip.size(); i++)
        for (int j = i+1; j < strip.size() && j < i+8; j++) {
            double d = dist(strip[i], strip[j]);
            if (d < delta) {
                delta = d;
                best = {strip[i], strip[j]};
            }
        }

    return {delta, best};
}

pair<double, pair<Point,Point>> closestPair(vector<Point>& points) {
    sort(points.begin(), points.end(),
         [](Point a, Point b) { return a.x < b.x; });
    vector<Point> Py = points;
    sort(Py.begin(), Py.end(),
         [](Point a, Point b) { return a.y < b.y; });
    return closestRec(points, 0, points.size(), Py);
}
```

### 4.9 FAQ

**Q1：为什么是"后面7个点"而不是更多？**

> 在δ × 2δ的矩形内，左右各有一个δ × δ的正方形。每个正方形内最多放4个互相距离≥δ的点（因为每个δ × δ正方形的对角线长δ√2 > δ，所以可以用2×2网格论证最多4个点）。两个正方形共8个点，减去当前点本身，最多还需检查7个。

**Q2：如果有多对距离相同的最近点对怎么办？**

> 算法会返回其中一对。如果需要找所有最近点对，需要额外处理。

**Q3：如果用Manhattan距离（L₁距离）呢？**

> 算法框架不变，只需将距离函数替换。在条带检查中，需要检查的点数可能不同（最多5个而非7个），因为L₁距离下δ × 2δ矩形内能放的点更少。

---

## 5️⃣ 本章小结

### 核心要点

| 知识点 | 核心公式/算法 | 时间复杂度 | 关键洞察 |
|--------|-------------|-----------|---------|
| 叉积 | p₁×p₂ = x₁y₂ - x₂y₁ | O(1) | 不用角度，用叉积判断方向 |
| 线段相交 | 跨立实验 + 边界判断 | O(1) | 互相跨立 = 相交 |
| 扫描线 | 事件驱动 + BST维护 | O(n log n) | 相交线段在扫描线状态中必相邻 |
| 凸包 | Graham扫描 | O(n log n) | 排序+栈，非左转就弹出 |
| 最近点对 | 分治法 | O(n log n) | 条带内只检查7个点 |

### 易错点

1. **叉积方向**：p₁×p₂ > 0 是逆时针（左转），不要记反！
2. **线段相交 vs 直线相交**：叉积只能判断直线关系，线段相交还需要on-segment检查端点是否在线段范围内
3. **浮点精度**：计算几何中不能直接用 `==` 比较浮点数，必须用 `fabs(a-b) < EPS`
4. **Graham扫描的极角排序**：不需要真的算角度！用叉积就能比较极角大小
5. **最近点对的条带检查**：必须按y坐标排序后检查，否则无法保证"只看7个"
6. **预排序**：最近点对中Y数组必须预排序传入递归，不能每次重新排序

### 与前后章节的联系

| 前置知识 | 本章应用 |
|---------|---------|
| 第4章 分治策略 | 最近点对的分治框架 |
| 第6章 堆排序 | 事件点排序 |
| 第12章 BST | 扫描线状态T |
| 第13章 红黑树 | 扫描线状态的高效实现 |
| 第17章 摊销分析 | Graham扫描的POP操作摊销O(1) |
| 第28章 矩阵运算 | 叉积的行列式理解 |

### 延伸阅读

- **de Berg et al.**《Computational Geometry: Algorithms and Applications》——计算几何经典教材
- **O'Rourke**《Computational Geometry in C》——更偏实现
- **3D凸包**：本章只讲2D凸包，3D凸包可用增量法或分治法，时间O(n log n)
- **Voronoi图**：与凸包对偶的重要结构，广泛用于最近邻查询
- **Delaunay三角剖分**：与Voronoi图对偶，保证"最均匀"的三角剖分

---

## 6️⃣ 课后练习

### 思考题

1. **叉积的几何意义**：为什么叉积等于平行四边形的面积？从行列式的角度解释。

2. **扫描线变种**：如果所有线段都是水平的，扫描线算法还需要吗？能否设计更简单的算法？

3. **凸包的唯一性**：证明点集的凸包是唯一的。

4. **最近点对的下界**：为什么最近点对问题的下界是Ω(n log n)？（提示：与排序问题归约）

5. **共线点**：如果输入点全部共线，Graham扫描法的结果是什么？

### 编程题

1. **线段相交检测器**：实现一个程序，输入n条线段，输出所有相交的线段对。

2. **凸包可视化**：随机生成100个点，用Graham扫描法求凸包，画出凸包和内部点。

3. **最近点对基准测试**：比较暴力法O(n²)和分治法O(n log n)在n=1000, 10000, 100000时的运行时间。

4. **旋转卡壳**：在凸包的基础上，实现旋转卡壳算法求凸包直径（最远点对），时间O(n)。

---

*本章完成时间：2026年6月12日*
*下一章预告：第34章 NP完全性——从"能高效解决"到"不知道能不能高效解决"的理论边界*
