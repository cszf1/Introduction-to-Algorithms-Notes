# 第27章 多线程算法（Multithreaded Algorithms）

> 本章回答一个越来越重要的问题：当一个处理器不够快时，如何让多个处理器同时工作来加速算法？——不管是多核CPU并行计算、GPU加速、还是分布式系统的任务分派，核心都是"多线程算法"。spawn/sync关键字提供了优雅的并行表达方式，而工作量T₁与持续时间T∞的分析框架，让我们能精确衡量并行算法的效率与可扩展性。

## 📖 本章导读

### 🎯 问题引入：为什么需要多线程算法？

**生活中的例子**：

想象你是一个**餐厅的大厨**：

- 今晚要准备一场100人的宴会
- 你一个人做，需要8小时（串行）
- 但你有4个帮厨，能不能2小时搞定？（并行）
- 问题来了：**哪些菜可以同时做？哪些必须等前一步完成？**

这就是多线程算法的核心问题！**"多线程算法"**研究如何把计算任务拆分成可以同时执行的部分，让多个处理器协作完成。

**更多生活类比**：

| 场景 | 串行 | 并行 | 关键问题 |
|------|------|------|---------|
| 做饭 | 一人做所有菜 | 多人同时做不同菜 | 有些菜需要先煮汤才能用 |
| 建房子 | 一人从头盖到尾 | 多工种同时施工 | 地基不打完不能砌墙 |
| 写报告 | 一人写全部章节 | 多人分写不同章节 | 统一格式后才能汇总 |
| 查字典 | 一人翻遍所有字典 | 多人各查一本字典 | 结果需要汇总 |
| 排序扑克 | 一人一张一张排 | 多人分堆排再合并 | 合并时需要协调 |

**与前面章节的关系**：

| 对比项 | 串行算法 | 多线程算法 |
|--------|---------|-----------|
| 执行模型 | 单处理器，一步一步 | 多处理器，同时多步 |
| 复杂度度量 | 时间复杂度T(n) | 工作量T₁ + 持续时间T∞ |
| 关键挑战 | 算法正确性 | 正确性 + 并行度 + 负载均衡 |
| 编程范式 | 顺序执行 | spawn/sync并行 |
| 典型加速 | N/A | 最多P倍（P=处理器数） |

**本章路线图**：

```
动态多线程基础 ──→ 多线程矩阵乘法 ──→ 多线程归并排序 ──→ 多线程归并 ──→ 并行循环与竞争
  27.1模型          27.2递归并行       27.3并行排序        27.4关键子程序  27.5实用构造
  spawn/sync        工作量分析         分治并行            二元递归归并    parallel-for
```

---

## 1️⃣ 动态多线程基础（27.1 The Basics of Dynamic Multithreading）

### 1.1 从串行到并行：为什么需要新的计算模型？

**问题**：我们之前的所有算法都是串行的——一步一步执行。但现实中：

- 你电脑的CPU有4核、8核甚至更多
- 数据中心有上千台机器
- 有些任务天然可以拆分

**生活类比**：

```
串行（一个人搬家）：
第1小时：搬书
第2小时：搬衣服
第3小时：搬家具
第4小时：搬电器
总时间：4小时

并行（四个人搬家）：
         人物A：搬书 ──┐
         人物B：搬衣服 ─┤──→ 1小时搞定！
         人物C：搬家具 ─┤
         人物D：搬电器 ─┘

但如果是"做蛋糕"呢？
步骤1：和面（必须先做）
步骤2：烤蛋糕（必须等和面完）
步骤3：做奶油（可以和烤蛋糕同时做）
步骤4：装饰（必须等步骤2和3都完）

         和面 ──→ 烤蛋糕 ──┐
                   ┌───────┤──→ 装饰
                   做奶油 ──┘
并行也省不了太多时间！因为步骤间有依赖
```

**关键洞察**：并行加速的上限取决于任务中**必须串行执行**的部分（Amdahl定律）。

### 1.2 动态多线程计算模型

CLRS提出的**动态多线程**模型，使用三个关键字来表达并行：

| 关键字 | 含义 | 生活类比 |
|--------|------|---------|
| `spawn` | 创建一个可以与当前线程并行执行的子线程 | 大厨吩咐帮厨："你去切菜，我继续炒菜" |
| `sync` | 等待所有spawn的子线程完成 | 大厨喊："等切菜的都切完，我们再一起出锅" |
| `parallel-for` | 并行执行循环的每次迭代 | "你们4个人，每人切1/4的土豆" |

**串行伪代码 vs 并行伪代码**：

```
串行版本（斐波那契）：
FIB(n):
  if n ≤ 1
    return n
  x = FIB(n-1)       ← 先算n-1
  y = FIB(n-2)       ← 再算n-2
  return x + y

并行版本（斐波那契）：
P-FIB(n):
  if n ≤ 1
    return n
  x = spawn P-FIB(n-1)   ← 去算n-1，不用等！
  y = P-FIB(n-2)          ← 同时算n-2
  sync                     ← 等n-1算完
  return x + y
```

**执行过程图解**（P-FIB(4)）：

```
时间 →  0    1    2    3    4    5    6    7
        
线程1:  P-FIB(4)
        ├─spawn P-FIB(3) ──────────→ 等sync ── return
        │                              ↓
        └─P-FIB(2) ──────→ sync ────┘
          
线程2:       P-FIB(3)
             ├─spawn P-FIB(2) ──────→ 等sync ── return
             │                          ↓
             └─P-FIB(1)──→ sync ─────┘

线程3:              P-FIB(2)
                    ├─spawn P-FIB(1)─→ 等sync ─ return
                    │                     ↓
                    └─P-FIB(0)──→ sync ─┘

线程4:                    P-FIB(2)  ← 上面spawn的
                          ├─spawn P-FIB(1)─→等sync─return
                          │                    ↓
                          └─P-FIB(0)──→sync──┘
```

### 1.3 串行等价性（Serial Equivalence）

**定义**：如果一个多线程算法的串行化版本（去掉所有spawn/sync）与某个已知串行算法相同，则称该多线程算法是**串行等价的**。

**为什么重要？**

> 如果一个并行算法和串行算法在逻辑上等价，那么它的正确性就已经被串行版本保证了——我们不需要重新证明！

**生活类比**：

```
好比一个食谱：
- 串行版：先切菜，再炒菜，再装盘
- 并行版：帮厨A切菜（spawn），帮厨B备料（spawn），大厨炒菜，等大家都完（sync），装盘

不管哪种方式，做出来的菜是一样的！
→ 正确性不需要重新验证，只需要验证"并行不会搞乱顺序"
```

**P-FIB的串行等价性**：

```
P-FIB去掉spawn/sync后：
P-FIB(n):
  if n ≤ 1
    return n
  x = P-FIB(n-1)     ← 原来是spawn，去掉后变成普通调用
  y = P-FIB(n-2)
  ← 原来是sync，去掉
  return x + y

这和标准FIB完全一样！→ 串行等价 ✓
```

### 1.4 工作量与持续时间

这是本章最重要的概念对！

**工作量（Work）T₁**：
- 定义：在一个处理器上执行的总操作数
- 直觉：**一个人干完所有活需要多少时间**
- 对应串行时间复杂度

**持续时间（Span）T∞**：
- 定义：在无限多处理器上，沿最长依赖链的执行时间
- 直觉：**即使有无穷多帮手，最少也需要多少时间**（因为有些步骤必须等）
- 也叫"关键路径长度"

**生活类比**：

```
搬家公司例子：
- 总共需要搬50箱东西
  T₁ = 50箱的工作量（一个人搬50次）
  
- 但是：大冰箱必须2个人抬（1步），小箱子可以同时搬
  最长依赖链：拆床架→搬床板→搬床垫→装车 = 4步
  T∞ = 4（即使有无穷多工人，也要4步）
  
- 并行度 = T₁/T∞ = 50/4 = 12.5
  → 最多能利用12.5个工人
  → 给13个工人也没用，有1个会闲着
```

**形式化定义**：

对于计算DAG（有向无环图）：

$$T_1 = \Theta(\text{DAG中所有节点的总计算量})$$

$$T_\infty = \Theta(\text{DAG中最长路径上的计算量})$$

**并行度（Parallelism）**：

$$\text{并行度} = \frac{T_1}{T_\infty}$$

> 并行度越大，越能从更多处理器中获益

**加速比（Speedup）**：

在P个处理器上的执行时间T_P满足：

$$\frac{T_1}{P} \leq T_P \leq T_1$$

理想加速比为P（线性加速），实际通常小于P。

**Work Law（工作量定律）**：

$$T_P \geq \frac{T_1}{P}$$

> P个处理器做T₁的工作，最好也只能均分，不可能更快

**Span Law（持续时间定律）**：

$$T_P \geq T_\infty$$

> 即使有无限多处理器，最长依赖链也必须一步一步走完

### 1.5 计算DAG图解

**P-FIB(4)的计算DAG**：

```
              P-FIB(4)
             /        \
        spawn/          \P-FIB(2)
            /              \
       P-FIB(3)          ┌──┴──┐
      /        \         │     │
 spawn/          \     spawn/  P-FIB(0)=0
    /              \      /
 P-FIB(2)     P-FIB(1)=1 P-FIB(1)=1
  /    \                    
spawn/  P-FIB(0)=0         
  /                          
P-FIB(1)=1                   

完整的计算DAG（每个节点=一个操作）：

层级0:    [P-FIB(4): spawn] ─────────────────────→ [sync] → [return 3]
           ↓                                              ↑
层级1:    [P-FIB(3): spawn] → [sync] → [return 2]      |
           ↓                          ↑                  |
层级2:    [P-FIB(2): spawn] → [sync]→|    [P-FIB(1):ret1]
           ↓                    ↑     |
层级3:    [P-FIB(1):ret 1]    [P-FIB(0):ret 0]

最长路径（关键路径）= T∞:
P-FIB(4):spawn → P-FIB(3):spawn → P-FIB(2):spawn → P-FIB(1):return → 
P-FIB(2):sync → P-FIB(3):sync → P-FIB(4):sync → P-FIB(4):return
长度 = O(n)

总工作量 = T₁:
所有节点的操作总数 = 斐波那契递归树的节点数 = F(n+2) ≈ φⁿ
```

**FAQ：为什么T∞不一定是O(log n)？**

> 很多同学以为并行就一定是对数级别。错！T∞取决于最长的**依赖链**。在P-FIB中，P-FIB(n)必须等P-FIB(n-1)，而P-FIB(n-1)又必须等P-FIB(n-2)……形成了一条长度为O(n)的链。即使有无穷多处理器，这条链也得一步一步走。就像做蛋糕，不管有多少帮手，和面→烤→装饰这条链不能跳步。

### 1.6 P-FIB的工作量与持续时间分析

**工作量分析**：

P-FIB与FIB串行等价，所以：

$$T_1(n) = T_1(n-1) + T_1(n-2) + \Theta(1)$$

这正是斐波那契递推！所以：

$$T_1(n) = \Theta(\phi^n)$$

其中 φ = (1+√5)/2 ≈ 1.618 是黄金比例。

**持续时间分析**：

P-FIB(n)的关键路径：
- spawn P-FIB(n-1)后，本线程继续执行P-FIB(n-2)
- P-FIB(n-1)的关键路径长度 = T∞(n-1)
- P-FIB(n-2)的关键路径长度 = T∞(n-2)
- sync要等两者都完成

$$T_\infty(n) = \max(T_\infty(n-1), T_\infty(n-2)) + \Theta(1)$$

因为T∞(n-1) ≥ T∞(n-2)，所以：

$$T_\infty(n) = T_\infty(n-1) + \Theta(1) = \Theta(n)$$

**并行度**：

$$\text{并行度} = \frac{T_1(n)}{T_\infty(n)} = \frac{\Theta(\phi^n)}{\Theta(n)}$$

> 并行度指数级增长！意味着P-FIB可以充分利用大量处理器。

**数值示例**：

| n | T₁ (工作量) | T∞ (持续时间) | 并行度 T₁/T∞ |
|---|------------|--------------|-------------|
| 5 | 15 | 5 | 3 |
| 10 | 177 | 10 | 17.7 |
| 20 | 21,891 | 20 | 1,094 |
| 30 | 2,692,573 | 30 | 89,752 |
| 40 | 331,160,281 | 40 | 8,279,007 |

### 1.7 多线程算法的执行调度

**问题**：有了P个处理器，如何把计算DAG中的任务分配给处理器？

**贪心调度器（Greedy Scheduler）**：

- 每一步，对每个空闲的处理器，分配一个"就绪"（所有前驱已完成）的步骤
- 如果就绪步骤数 ≥ P，称为**完全步**（complete step）
- 如果就绪步骤数 < P，称为**不完全步**（incomplete step）

**定理**：在P个处理器上，贪心调度器的执行时间满足：

$$T_P \leq \frac{T_1}{P} + T_\infty$$

**直觉理解**：

```
完全步：每步完成P个工作量 → 最多 T₁/P 步完全步
不完全步：每步完成 < P 个工作量 → 最多 T∞ 步不完全步

为什么不完全步最多T∞步？
因为在不完全步中，只有 < P 个就绪步骤，
意味着DAG中有"瓶颈"——并行度不够。
而不完全步的数量受关键路径长度的限制。
```

**图解贪心调度**：

```
计算DAG示例（8个任务，2个处理器）：

任务图：     1 → 3 → 5 → 7
             1 → 4 → 6 → 8
             2 → 4
             2 → 5

T₁ = 8, T∞ = 4

时间步 | 处理器1 | 处理器2 | 类型
-------|---------|---------|------
  1    |   1     |   2     | 完全（2个就绪，2个处理器）
  2    |   3     |   4     | 完全
  3    |   5     |   6     | 完全
  4    |   7     |   8     | 完全

T_P = 4 = T₁/P + T∞ = 8/2 + 4 = 8？不对，应该是T_P ≤ T₁/P + T∞

实际情况：T₂ = 4 = max(T₁/P, T∞) = max(4, 4) = 4 ✓

另一个例子（3个处理器）：
时间步 | 处理器1 | 处理器2 | 处理器3
-------|---------|---------|--------
  1    |   1     |   2     |  (空闲)   ← 不完全！只有2个就绪
  2    |   3     |   4     |  (空闲)   ← 不完全
  3    |   5     |   6     |  (空闲)   ← 不完全
  4    |   7     |   8     |  (空闲)   ← 不完全

T₃ = 4 > T₁/3 + T∞ = 8/3 + 4 ≈ 6.67？不对...
T₃ = 4 ≤ 8/3 + 4 ≈ 6.67 ✓ （不等式成立）
```

### 1.8 P-FIB的Python与C++模拟实现

**Python实现**（使用多线程模拟spawn/sync语义）：

```python
import threading

def p_fib(n):
    """
    多线程斐波那契 —— 模拟spawn/sync语义
    参数:
        n: 斐波那契数列的第n项
    返回:
        F(n)
    """
    # 基本情况：n ≤ 1时直接返回
    if n <= 1:
        return n
    
    # 用于存储spawn线程的结果
    result_x = [0]       # 列表包装，以便在线程间共享
    result_y = [0]
    
    # 定义子线程函数：计算F(n-1)
    def compute_fib_n1():
        """子线程：计算P-FIB(n-1)"""
        result_x[0] = p_fib(n - 1)    # 递归计算
    
    # ===== spawn: 创建子线程计算F(n-1) =====
    thread = threading.Thread(target=compute_fib_n1)
    thread.start()                     # 启动子线程（相当于spawn）
    
    # ===== 主线程继续：计算F(n-2) =====
    result_y[0] = p_fib(n - 2)        # 主线程同时计算
    
    # ===== sync: 等待子线程完成 =====
    thread.join()                      # 等待子线程（相当于sync）
    
    # 返回两者之和
    return result_x[0] + result_y[0]


# 测试
if __name__ == "__main__":
    for i in range(10):
        print(f"P-FIB({i}) = {p_fib(i)}")
    # 输出: P-FIB(0)=0, P-FIB(1)=1, P-FIB(2)=1, ..., P-FIB(9)=34
```

**C++实现**：

```cpp
#include <iostream>
#include <thread>
#include <future>

// 多线程斐波那契 —— 使用std::async模拟spawn/sync
int p_fib(int n) {
    // 基本情况
    if (n <= 1) {
        return n;
    }
    
    // ===== spawn: 异步启动计算F(n-1) =====
    // std::launch::async 强制创建新线程（不等lazy）
    std::future<int> fut = std::async(std::launch::async, p_fib, n - 1);
    
    // ===== 主线程继续：计算F(n-2) =====
    int y = p_fib(n - 2);           // 主线程同步计算
    
    // ===== sync: 获取子线程结果（等待完成） =====
    int x = fut.get();              // 阻塞等待，相当于sync
    
    return x + y;                   // 返回两者之和
}

int main() {
    for (int i = 0; i < 10; i++) {
        std::cout << "P-FIB(" << i << ") = " << p_fib(i) << std::endl;
    }
    return 0;
}
```

**FAQ：实际中能这样用多线程吗？**

> 不能！这个实现只是教学演示。实际问题是：
> 1. 线程创建开销远大于计算F(n-2)的收益（n小时）
> 2. 线程数会指数级增长，远超CPU核心数
> 3. 应该使用线程池+任务队列，而非直接创建线程
> 
> CLRS的spawn/sync是**逻辑模型**，实际编译器/运行时会映射到有限的物理线程。就像菜谱说"让帮厨切菜"，不是真的每个步骤雇一个人，而是安排现有的人去做。

---

## 2️⃣ 多线程递归：矩阵乘法（27.2 Multithreaded Matrix Multiplication）

### 2.1 矩阵乘法的递归分治

**回顾**：两个n×n矩阵A和B的乘积C = A·B，其中：

$$C_{ij} = \sum_{k=1}^{n} A_{ik} \cdot B_{kj}$$

朴素算法时间复杂度 Θ(n³)。

**递归分治思路**：

将n×n矩阵分成4个(n/2)×(n/2)子矩阵：

```
A = | A₁₁  A₁₂ |    B = | B₁₁  B₁₂ |    C = | C₁₁  C₁₂ |
    | A₂₁  A₂₂ |        | B₂₁  B₂₂ |        | C₂₁  C₂₂ |

则：
C₁₁ = A₁₁·B₁₁ + A₁₂·B₂₁
C₁₂ = A₁₁·B₁₂ + A₁₂·B₂₂
C₂₁ = A₂₁·B₁₁ + A₂₂·B₂₁
C₂₂ = A₂₁·B₁₂ + A₂₂·B₂₂
```

**生活类比**：

```
做四道菜（C₁₁, C₁₂, C₂₁, C₂₂）：
每道菜需要两种食材组合（乘法）再混合（加法）
- C₁₁ = 食材A₁₁配B₁₁ + 食材A₁₂配B₂₁
- 四道菜之间没有依赖！→ 可以并行做！
- 每道菜内部的"配食材"和"混合"也有并行空间
```

### 2.2 串行递归矩阵乘法

```
MATRIX-MULTIPLY-RECURSIVE(A, B, C, n):
  if n == 1
    C[1,1] += A[1,1] * B[1,1]     // 基本情况：1×1矩阵
    return
  
  // 将A, B, C分成四个n/2 × n/2子矩阵
  
  // 计算 C₁₁ = A₁₁·B₁₁ + A₁₂·B₂₁
  MATRIX-MULTIPLY-RECURSIVE(A₁₁, B₁₁, C₁₁, n/2)    // 步骤1
  MATRIX-MULTIPLY-RECURSIVE(A₁₂, B₂₁, C₁₁, n/2)    // 步骤2：累加到C₁₁
  
  // 计算 C₁₂ = A₁₁·B₁₂ + A₁₂·B₂₂
  MATRIX-MULTIPLY-RECURSIVE(A₁₁, B₁₂, C₁₂, n/2)    // 步骤3
  MATRIX-MULTIPLY-RECURSIVE(A₁₂, B₂₂, C₁₂, n/2)    // 步骤4
  
  // 计算 C₂₁ = A₂₁·B₁₁ + A₂₂·B₂₁
  MATRIX-MULTIPLY-RECURSIVE(A₂₁, B₁₁, C₂₁, n/2)    // 步骤5
  MATRIX-MULTIPLY-RECURSIVE(A₂₁, B₂₁, C₂₁, n/2)    // 步骤6  ← 注意：应该是A₂₂·B₂₁
  
  // 计算 C₂₂ = A₂₁·B₁₂ + A₂₂·B₂₂
  MATRIX-MULTIPLY-RECURSIVE(A₂₁, B₁₂, C₂₂, n/2)    // 步骤7  ← 应该保持一致性
  MATRIX-MULTIPLY-RECURSIVE(A₂₂, B₂₂, C₂₂, n/2)    // 步骤8
```

8次递归调用，每次规模减半。串行复杂度：T₁(n) = 8T₁(n/2) + Θ(1) = Θ(n³)

### 2.3 多线程递归矩阵乘法

**关键观察**：8次递归调用中，很多是独立的，可以并行！

```
P-MATRIX-MULTIPLY-RECURSIVE(A, B, C, n):
  if n == 1
    C[1,1] += A[1,1] * B[1,1]
    return
  
  // 第1组：计算8个乘积项（全部可以并行！）
  spawn P-MATRIX-MULTIPLY-RECURSIVE(A₁₁, B₁₁, C₁₁, n/2)   // T1
  spawn P-MATRIX-MULTIPLY-RECURSIVE(A₁₂, B₂₁, C₁₁, n/2)   // T2
  spawn P-MATRIX-MULTIPLY-RECURSIVE(A₁₁, B₁₂, C₁₂, n/2)   // T3
  spawn P-MATRIX-MULTIPLY-RECURSIVE(A₁₂, B₂₂, C₁₂, n/2)   // T4
  spawn P-MATRIX-MULTIPLY-RECURSIVE(A₂₁, B₁₁, C₂₁, n/2)   // T5
  spawn P-MATRIX-MULTIPLY-RECURSIVE(A₂₂, B₂₁, C₂₁, n/2)   // T6
  spawn P-MATRIX-MULTIPLY-RECURSIVE(A₂₁, B₁₂, C₂₂, n/2)   // T7
  P-MATRIX-MULTIPLY-RECURSIVE(A₂₂, B₂₂, C₂₂, n/2)         // T8: 最后一个不用spawn
  
  sync   // 等所有8个乘积完成
```

**等等，这有问题！** T1和T2都写入C₁₁，存在数据竞争！

**正确版本**：使用临时变量避免竞争

```
P-MATRIX-MULTIPLY-RECURSIVE(A, B, n):
  if n == 1
    return A[1,1] * B[1,1]         // 返回1×1矩阵乘积
  
  // 分配8个临时子矩阵存储8个乘积
  spawn S₁ = P-MATRIX-MULTIPLY-RECURSIVE(A₁₁, B₁₁, n/2)   // 并行
  spawn S₂ = P-MATRIX-MULTIPLY-RECURSIVE(A₁₂, B₂₁, n/2)   // 并行
  spawn S₃ = P-MATRIX-MULTIPLY-RECURSIVE(A₁₁, B₁₂, n/2)   // 并行
  spawn S₄ = P-MATRIX-MULTIPLY-RECURSIVE(A₁₂, B₂₂, n/2)   // 并行
  spawn S₅ = P-MATRIX-MULTIPLY-RECURSIVE(A₂₁, B₁₁, n/2)   // 并行
  spawn S₆ = P-MATRIX-MULTIPLY-RECURSIVE(A₂₂, B₂₁, n/2)   // 并行
  spawn S₇ = P-MATRIX-MULTIPLY-RECURSIVE(A₂₁, B₁₂, n/2)   // 并行
  S₈ = P-MATRIX-MULTIPLY-RECURSIVE(A₂₂, B₂₂, n/2)         // 主线程
  
  sync   // 等所有乘积完成
  
  // 合并结果（加法，此时无竞争）
  C₁₁ = S₁ + S₂      // 串行或并行加法
  C₁₂ = S₃ + S₄
  C₂₁ = S₅ + S₆
  C₂₂ = S₇ + S₈
  
  return C
```

**并行执行图解**（2×2矩阵示例）：

```
                    P-MAT-MULT(A, B, 2)
                    /   |   |   |   |   |   |   \
                  spawn spawn spawn spawn spawn spawn spawn
                  /    |    |    |    |    |    |     \
               S₁    S₂   S₃   S₄   S₅   S₆   S₇    S₈
              A₁₁B₁₁ A₁₂B₂₁ ...                      A₂₂B₂₂
               |      |    |    |    |    |    |      |
               ↓      ↓    ↓    ↓    ↓    ↓    ↓      ↓
              sync（等待所有8个完成）
                    |
              C₁₁=S₁+S₂  C₁₂=S₃+S₄  C₂₁=S₅+S₆  C₂₂=S₇+S₈

8个子任务完全独立 → T∞只有O(log n)层递归深度！
```

### 2.4 工作量与持续时间分析

**工作量 T₁**：

由于与串行版本等价：

$$T_1(n) = 8T_1(n/2) + \Theta(n^2)$$

- 8次递归调用，每次规模n/2
- Θ(n²)来自矩阵加法（合并结果时4个子矩阵加法，每个n²/4）

用主定理解：a=8, b=2, f(n)=Θ(n²)
- log_b(a) = log₂8 = 3
- f(n) = Θ(n²) = Θ(n^{3-1}) → 情况1（正则条件满足）

$$T_1(n) = \Theta(n^3)$$

> 与朴素矩阵乘法相同！并行没有增加总工作量。

**持续时间 T∞**：

$$T_\infty(n) = T_\infty(n/2) + \Theta(1)$$

为什么？因为：
- 8个spawn可以同时执行，只贡献Θ(1)
- sync等最慢的那个完成
- 合并结果只需常数时间（spawn矩阵加法的话也是O(1)）

Wait，合并4个子矩阵加法也需要分析：

如果加法也是并行的，那么矩阵加法的T∞ = O(1)（所有元素独立相加）

$$T_\infty(n) = T_\infty(n/2) + O(1) = O(\log n)$$

**并行度**：

$$\text{并行度} = \frac{T_1}{T_\infty} = \frac{\Theta(n^3)}{\Theta(\log n)}$$

> 对于n=1000，并行度 ≈ 10⁹/10 ≈ 10⁸，非常可观！

**数值示例**：

| n | T₁ = Θ(n³) | T∞ = Θ(log n) | 并行度 |
|---|-----------|--------------|--------|
| 2 | 8 | 1 | 8 |
| 4 | 64 | 2 | 32 |
| 8 | 512 | 3 | 171 |
| 16 | 4,096 | 4 | 1,024 |
| 32 | 32,768 | 5 | 6,554 |
| 64 | 262,144 | 6 | 43,691 |
| 128 | 2,097,152 | 7 | 299,593 |
| 1024 | 10⁹ | 10 | 10⁸ |

### 2.5 多线程矩阵乘法代码实现

**Python实现**：

```python
import numpy as np
from concurrent.futures import ThreadPoolExecutor, as_completed

def matrix_add(A, B):
    """矩阵加法（用于合并子结果）"""
    return A + B

def p_matrix_multiply_recursive(A, B, threshold=64):
    """
    多线程递归矩阵乘法
    参数:
        A: n×n 矩阵（numpy数组）
        B: n×n 矩阵（numpy数组）
        threshold: 当矩阵小于此大小时切换到串行（避免线程开销）
    返回:
        C = A × B
    """
    n = A.shape[0]
    
    # 基本情况：1×1矩阵
    if n == 1:
        return A * B                  # 直接返回标量乘积
    
    # 小矩阵切换到朴素乘法（减少线程创建开销）
    if n <= threshold:
        return A @ B                  # 使用numpy的优化矩阵乘法
    
    # 分割矩阵为4个子矩阵
    mid = n // 2
    A11 = A[:mid, :mid]              # 左上
    A12 = A[:mid, mid:]              # 右上
    A21 = A[mid:, :mid]              # 左下
    A22 = A[mid:, mid:]              # 右下
    B11 = B[:mid, :mid]
    B12 = B[:mid, mid:]
    B21 = B[mid:, :mid]
    B22 = B[mid:, mid:]
    
    # ===== spawn 8个并行乘法任务 =====
    with ThreadPoolExecutor(max_workers=8) as executor:
        futures = {
            # C₁₁ = A₁₁B₁₁ + A₁₂B₂₁
            executor.submit(p_matrix_multiply_recursive, A11, B11, threshold): 'S1',
            executor.submit(p_matrix_multiply_recursive, A12, B21, threshold): 'S2',
            # C₁₂ = A₁₁B₁₂ + A₁₂B₂₂
            executor.submit(p_matrix_multiply_recursive, A11, B12, threshold): 'S3',
            executor.submit(p_matrix_multiply_recursive, A12, B22, threshold): 'S4',
            # C₂₁ = A₂₁B₁₁ + A₂₂B₂₁
            executor.submit(p_matrix_multiply_recursive, A21, B11, threshold): 'S5',
            executor.submit(p_matrix_multiply_recursive, A22, B21, threshold): 'S6',
            # C₂₂ = A₂₁B₁₂ + A₂₂B₂₂
            executor.submit(p_matrix_multiply_recursive, A21, B12, threshold): 'S7',
            executor.submit(p_matrix_multiply_recursive, A22, B22, threshold): 'S8',
        }
        
        # ===== sync: 收集所有结果 =====
        results = {}
        for future in as_completed(futures):
            key = futures[future]
            results[key] = future.result()   # 阻塞等待每个任务完成
    
    # 合并结果
    C11 = matrix_add(results['S1'], results['S2'])   # C₁₁ = S₁ + S₂
    C12 = matrix_add(results['S3'], results['S4'])   # C₁₂ = S₃ + S₄
    C21 = matrix_add(results['S5'], results['S6'])   # C₂₁ = S₅ + S₆
    C22 = matrix_add(results['S7'], results['S8'])   # C₂₂ = S₇ + S₈
    
    # 拼装完整矩阵
    top = np.hstack([C11, C12])          # 上半部分
    bottom = np.hstack([C21, C22])       # 下半部分
    return np.vstack([top, bottom])      # 合并


# 测试
if __name__ == "__main__":
    np.random.seed(42)
    n = 128                             # 矩阵大小（2的幂）
    A = np.random.randn(n, n)
    B = np.random.randn(n, n)
    
    C_parallel = p_matrix_multiply_recursive(A, B)
    C_numpy = A @ B                     # 用numpy验证
    
    print(f"最大误差: {np.max(np.abs(C_parallel - C_numpy)):.2e}")
    # 输出应接近0
```

**C++实现**：

```cpp
#include <iostream>
#include <vector>
#include <future>

using Matrix = std::vector<std::vector<double>>;

// 矩阵加法
Matrix matrix_add(const Matrix& A, const Matrix& B) {
    int n = A.size();
    Matrix C(n, std::vector<double>(n));
    for (int i = 0; i < n; i++)
        for (int j = 0; j < n; j++)
            C[i][j] = A[i][j] + B[i][j];     // 逐元素相加
    return C;
}

// 朴素矩阵乘法（小矩阵用）
Matrix matrix_multiply_naive(const Matrix& A, const Matrix& B) {
    int n = A.size();
    Matrix C(n, std::vector<double>(n, 0));
    for (int i = 0; i < n; i++)
        for (int j = 0; j < n; j++)
            for (int k = 0; k < n; k++)
                C[i][j] += A[i][k] * B[k][j]; // 经典三重循环
    return C;
}

// 提取子矩阵
Matrix submatrix(const Matrix& M, int row_start, int col_start, int size) {
    Matrix sub(size, std::vector<double>(size));
    for (int i = 0; i < size; i++)
        for (int j = 0; j < size; j++)
            sub[i][j] = M[row_start + i][col_start + j];  // 复制子矩阵
    return sub;
}

// 多线程递归矩阵乘法
Matrix p_matrix_multiply(const Matrix& A, const Matrix& B, int threshold = 64) {
    int n = A.size();
    
    // 基本情况
    if (n == 1) {
        return Matrix{{A[0][0] * B[0][0]}};     // 1×1直接乘
    }
    
    // 小矩阵用朴素方法
    if (n <= threshold) {
        return matrix_multiply_naive(A, B);
    }
    
    int mid = n / 2;
    
    // 提取8个子矩阵
    Matrix A11 = submatrix(A, 0, 0, mid);       // A的左上
    Matrix A12 = submatrix(A, 0, mid, mid);     // A的右上
    Matrix A21 = submatrix(A, mid, 0, mid);     // A的左下
    Matrix A22 = submatrix(A, mid, mid, mid);   // A的右下
    Matrix B11 = submatrix(B, 0, 0, mid);
    Matrix B12 = submatrix(B, 0, mid, mid);
    Matrix B21 = submatrix(B, mid, 0, mid);
    Matrix B22 = submatrix(B, mid, mid, mid);
    
    // ===== spawn 8个并行乘法 =====
    auto fut1 = std::async(std::launch::async, p_matrix_multiply, 
                           std::cref(A11), std::cref(B11), threshold);  // S1
    auto fut2 = std::async(std::launch::async, p_matrix_multiply, 
                           std::cref(A12), std::cref(B21), threshold);  // S2
    auto fut3 = std::async(std::launch::async, p_matrix_multiply, 
                           std::cref(A11), std::cref(B12), threshold);  // S3
    auto fut4 = std::async(std::launch::async, p_matrix_multiply, 
                           std::cref(A12), std::cref(B22), threshold);  // S4
    auto fut5 = std::async(std::launch::async, p_matrix_multiply, 
                           std::cref(A21), std::cref(B11), threshold);  // S5
    auto fut6 = std::async(std::launch::async, p_matrix_multiply, 
                           std::cref(A22), std::cref(B21), threshold);  // S6
    auto fut7 = std::async(std::launch::async, p_matrix_multiply, 
                           std::cref(A21), std::cref(B12), threshold);  // S7
    Matrix S8 = p_matrix_multiply(A22, B22, threshold);                  // S8: 主线程
    
    // ===== sync: 获取所有结果 =====
    Matrix S1 = fut1.get();                      // 阻塞等待
    Matrix S2 = fut2.get();
    Matrix S3 = fut3.get();
    Matrix S4 = fut4.get();
    Matrix S5 = fut5.get();
    Matrix S6 = fut6.get();
    Matrix S7 = fut7.get();
    
    // 合并
    Matrix C11 = matrix_add(S1, S2);             // C₁₁ = S₁ + S₂
    Matrix C12 = matrix_add(S3, S4);             // C₁₂ = S₃ + S₄
    Matrix C21 = matrix_add(S5, S6);             // C₂₁ = S₅ + S₆
    Matrix C22 = matrix_add(S7, S8);             // C₂₂ = S₇ + S₈
    
    // 拼装
    Matrix C(n, std::vector<double>(n));
    for (int i = 0; i < mid; i++) {
        for (int j = 0; j < mid; j++) {
            C[i][j] = C11[i][j];                 // 左上
            C[i][j + mid] = C12[i][j];           // 右上
            C[i + mid][j] = C21[i][j];           // 左下
            C[i + mid][j + mid] = C22[i][j];     // 右下
        }
    }
    return C;
}
```

### 2.6 Strassen算法的并行化（拓展）

Strassen算法将8次递归减少为7次，T₁ = Θ(n^{lg7}) ≈ Θ(n^{2.81})：

```
P-STRASSEN(A, B, n):
  if n == 1
    return A * B
  
  // 计算7个中间矩阵（可以全部并行！）
  spawn S₁ = P-STRASSEN(A₁₁, B₁₂ - B₂₂, n/2)
  spawn S₂ = P-STRASSEN(A₁₁ + A₁₂, B₂₂, n/2)
  spawn S₃ = P-STRASSEN(A₂₁ + A₂₂, B₁₁, n/2)
  spawn S₄ = P-STRASSEN(A₂₂, B₂₁ - B₁₁, n/2)
  spawn S₅ = P-STRASSEN(A₁₁ + A₂₂, B₁₁ + B₂₂, n/2)
  spawn S₆ = P-STRASSEN(A₁₂ - A₂₂, B₂₁ + B₂₂, n/2)
  S₇ = P-STRASSEN(A₁₁ - A₂₁, B₁₁ + B₁₂, n/2)
  
  sync
  
  C₁₁ = S₅ + S₄ - S₂ + S₆
  C₁₂ = S₁ + S₂
  C₂₁ = S₃ + S₄
  C₂₂ = S₅ + S₁ - S₃ - S₇
  
  return C
```

**复杂度对比**：

| 算法 | T₁ | T∞ | 并行度 |
|------|------|------|--------|
| 朴素并行 | Θ(n³) | Θ(log n) | Θ(n³/log n) |
| Strassen并行 | Θ(n^{2.81}) | Θ(log² n) | Θ(n^{2.81}/log² n) |

> Strassen的工作量更优，但持续时间从Θ(log n)变为Θ(log² n)，因为7个spawn中的矩阵加减也需要O(log n)的持续时间。

**FAQ：为什么Strassen的T∞是Θ(log²n)？**

> 每一层递归需要：
> 1. 计算加减法（合并矩阵），T∞_add = Θ(log n)
> 2. 递归调用本身
> 所以 T∞(n) = T∞(n/2) + Θ(log n) = Θ(log² n)
> 
> 相比之下，朴素并行的加减法不涉及递归，直接并行做即可，所以T∞ = Θ(log n)。

---

## 3️⃣ 多线程归并排序（27.3 Multithreaded Merge Sort）

### 3.1 归并排序的并行化机会

**回顾串行归并排序**：

```
MERGE-SORT(A, left, right):
  if left < right
    mid = (left + right) / 2
    MERGE-SORT(A, left, mid)       ← 排左半
    MERGE-SORT(A, mid+1, right)    ← 排右半
    MERGE(A, left, mid, right)     ← 合并两半
```

**并行化思路**：

```
P-MERGE-SORT(A, left, right):
  if left < right
    mid = (left + right) / 2
    spawn P-MERGE-SORT(A, left, mid)    ← 排左半（并行！）
    P-MERGE-SORT(A, mid+1, right)       ← 排右半（主线程）
    sync                                  ← 等两边都排好
    P-MERGE(A, left, mid, right)         ← 并行归并
```

**生活类比**：

```
整理1000份文件：
- 串行：一人先排前500份，再排后500份，再合并
- 并行：
  1. spawn：让帮手排前500份
  2. 你自己排后500份
  3. sync：等两边都排好
  4. 两人一起合并（并行归并）
  
关键瓶颈：合并步骤也可以并行吗？
→ 可以！这就是下一节要讲的多线程归并
```

### 3.2 串行归并的工作量分析

**串行归并排序**：

$$T_1(n) = 2T_1(n/2) + \Theta(n) = \Theta(n \log n)$$

- 两次递归：2T(n/2)
- 归并：Θ(n)

### 3.3 多线程归并排序的持续时间分析

**P-MERGE-SORT的持续时间**：

$$T_\infty(n) = T_\infty(n/2) + T_\infty^{merge}(n) + O(1)$$

其中T∞^{merge}(n)是并行归并的持续时间。

如果并行归并的T∞^{merge}(n) = Θ(log n)（下一节会推导），则：

$$T_\infty(n) = T_\infty(n/2) + \Theta(\log n) = \Theta(\log^2 n)$$

**逐层展开**：

$$T_\infty(n) = \Theta(\log n) + \Theta(\log(n/2)) + \Theta(\log(n/4)) + \cdots + \Theta(1)$$

$$= \Theta(\log n + \log n - 1 + \log n - 2 + \cdots + 1) = \Theta(\log^2 n)$$

**并行度**：

$$\text{并行度} = \frac{T_1(n)}{T_\infty(n)} = \frac{\Theta(n \log n)}{\Theta(\log^2 n)} = \Theta\left(\frac{n}{\log n}\right)$$

> 对于n = 10⁶，并行度 ≈ 10⁶/20 ≈ 50,000，可以充分利用几千个处理器。

**数值示例**：

| n | T₁ = Θ(n log n) | T∞ = Θ(log² n) | 并行度 |
|---|-----------------|----------------|--------|
| 16 | 64 | 16 | 4 |
| 64 | 384 | 36 | 10.7 |
| 256 | 2,048 | 64 | 32 |
| 1024 | 10,240 | 100 | 102.4 |
| 10⁶ | 20,000,000 | 400 | 50,000 |
| 10⁹ | 30,000,000,000 | 900 | 33,333,333 |

### 3.4 多线程归并排序的执行过程图解

**对数组 [5, 2, 4, 7, 1, 3, 2, 6] 执行P-MERGE-SORT**：

```
第1层（递归深度0）：
P-MERGE-SORT([5,2,4,7,1,3,2,6])
├── spawn P-MERGE-SORT([5,2,4,7])    ← 线程A
└── P-MERGE-SORT([1,3,2,6])          ← 线程B（主线程）

第2层（递归深度1）：
线程A:                          线程B:
├── spawn P-MERGE-SORT([5,2])   ├── spawn P-MERGE-SORT([1,3])
└── P-MERGE-SORT([4,7])         └── P-MERGE-SORT([2,6])

第3层（递归深度2）：
线程A的子线程:                  线程B的子线程:
├── [5] vs [2] → merge→[2,5]    ├── [1] vs [3] → merge→[1,3]
└── [4] vs [7] → merge→[4,7]    └── [2] vs [6] → merge→[2,6]

回溯到第2层（sync后并行归并）：
线程A: [2,5] + [4,7] → P-MERGE → [2,4,5,7]
线程B: [1,3] + [2,6] → P-MERGE → [1,2,3,6]

回溯到第1层（sync后并行归并）：
[2,4,5,7] + [1,2,3,6] → P-MERGE → [1,2,2,3,4,5,6,7]

时间线（2个处理器）：

时刻 0:  启动 P-MERGE-SORT(全数组)
时刻 1:  spawn左半, 主线程做右半
时刻 2:  各自递归...
时刻 3:  基本情况返回
时刻 4:  第3层归并（可并行）
时刻 5:  第2层归并（可并行）
时刻 6:  第1层归并
总时间 ≈ Θ(log²8) = Θ(9)
```

### 3.5 多线程归并排序代码实现

**Python实现**：

```python
from concurrent.futures import ThreadPoolExecutor
import multiprocessing

def p_merge_sort(arr, threshold=1000):
    """
    多线程归并排序
    参数:
        arr: 待排序列表
        threshold: 小于此长度时切换到串行排序
    返回:
        排序后的列表
    """
    # 基本情况：长度≤1直接返回
    if len(arr) <= 1:
        return arr
    
    # 小数组用串行排序（避免线程开销）
    if len(arr) <= threshold:
        return sorted(arr)
    
    mid = len(arr) // 2
    
    # ===== spawn: 并行排序两半 =====
    with ThreadPoolExecutor(max_workers=2) as executor:
        future_left = executor.submit(p_merge_sort, arr[:mid], threshold)
        future_right = executor.submit(p_merge_sort, arr[mid:], threshold)
        
        left = future_left.result()       # sync: 等左半完成
        right = future_right.result()     # sync: 等右半完成
    
    # 合并两半（这里用串行归并，也可改为并行归并）
    return merge(left, right)


def merge(left, right):
    """
    串行归并两个有序列表
    参数:
        left: 有序列表
        right: 有序列表
    返回:
        合并后的有序列表
    """
    result = []
    i = j = 0
    
    # 双指针归并
    while i < len(left) and j < len(right):
        if left[i] <= right[j]:
            result.append(left[i])       # 取左边较小的
            i += 1
        else:
            result.append(right[j])      # 取右边较小的
            j += 1
    
    # 处理剩余元素
    result.extend(left[i:])              # 左边剩余
    result.extend(right[j:])             # 右边剩余
    return result


# 测试
if __name__ == "__main__":
    import random
    random.seed(42)
    arr = [random.randint(1, 1000) for _ in range(100000)]
    
    sorted_arr = p_merge_sort(arr)
    print(f"排序正确: {sorted_arr == sorted(arr)}")
    print(f"前10个: {sorted_arr[:10]}")
```

**C++实现**：

```cpp
#include <iostream>
#include <vector>
#include <future>
#include <algorithm>

using namespace std;

// 串行归并
vector<int> merge(const vector<int>& left, const vector<int>& right) {
    vector<int> result;
    result.reserve(left.size() + right.size());    // 预分配空间
    
    int i = 0, j = 0;
    // 双指针归并
    while (i < (int)left.size() && j < (int)right.size()) {
        if (left[i] <= right[j]) {
            result.push_back(left[i++]);           // 取左边
        } else {
            result.push_back(right[j++]);          // 取右边
        }
    }
    // 处理剩余
    while (i < (int)left.size()) result.push_back(left[i++]);
    while (j < (int)right.size()) result.push_back(right[j++]);
    
    return result;
}

// 多线程归并排序
vector<int> p_merge_sort(vector<int> arr, int threshold = 10000) {
    // 基本情况
    if (arr.size() <= 1) {
        return arr;
    }
    
    // 小数组用串行排序
    if ((int)arr.size() <= threshold) {
        sort(arr.begin(), arr.end());              // std::sort
        return arr;
    }
    
    int mid = arr.size() / 2;
    
    // 分割
    vector<int> left(arr.begin(), arr.begin() + mid);
    vector<int> right(arr.begin() + mid, arr.end());
    
    // ===== spawn: 并行排序 =====
    auto future_left = async(launch::async, p_merge_sort, 
                             move(left), threshold);    // spawn左半
    vector<int> sorted_right = p_merge_sort(move(right), threshold); // 主线程做右半
    
    // ===== sync =====
    vector<int> sorted_left = future_left.get();         // 等左半完成
    
    // 合并
    return merge(sorted_left, sorted_right);
}

int main() {
    srand(42);
    vector<int> arr(100000);
    for (auto& x : arr) x = rand() % 1000;
    
    vector<int> sorted_arr = p_merge_sort(arr);
    
    // 验证
    bool correct = is_sorted(sorted_arr.begin(), sorted_arr.end());
    cout << "排序正确: " << (correct ? "是" : "否") << endl;
    return 0;
}
```

---

## 4️⃣ 多线程归并（27.4 Multithreaded Merging）

### 4.1 归并的并行化挑战

**问题**：串行归并是从左到右逐个比较，每步依赖前一步的结果。这种线性依赖使得T∞ = Θ(n)。

```
串行归并过程：
A = [2, 5, 7, 9]
B = [1, 4, 6, 8]

Step 1: A[1]=2 vs B[1]=1 → 取1, C=[1]
Step 2: A[1]=2 vs B[2]=4 → 取2, C=[1,2]
Step 3: A[2]=5 vs B[2]=4 → 取4, C=[1,2,4]
Step 4: A[2]=5 vs B[3]=6 → 取5, C=[1,2,4,5]
...
每步都依赖前一步！无法并行！
```

**生活类比**：

```
两个人各拿一叠排好序的扑克牌，要合并成一叠：
- 串行：每次比较两叠最上面的牌，取较小的
  → 每次选择依赖上一次的结果位置
- 并行：能不能把两叠牌"切开"，各归并一段？
  → 可以！只要知道"切点"在哪里

关键问题：怎么找到正确的切点？
```

### 4.2 二元递归归并算法

**核心思想**：利用二分搜索找到分割点，将归并问题分成两个独立的子问题。

**算法**：

```
P-MERGE(A, p₁, r₁, B, p₂, r₂, C, p₃):
  输入: A[p₁..r₁]有序, B[p₂..r₂]有序
  输出: 归并结果存入C[p₃..p₃+n₁+n₂-1]
  
  // 确保A是较短的数组
  n₁ = r₁ - p₁ + 1
  n₂ = r₂ - p₂ + 1
  if n₁ < n₂
    交换A,B; 交换p₁,p₂; 交换r₁,r₂; 交换n₁,n₂
  
  if n₁ == 0    // 基本情况：A为空
    return       // B已经有序，不需要操作
    
  // 在A中找中点
  q₁ = (p₁ + r₁) / 2
  
  // 在B中用二分搜索找A[q₁]的位置
  q₂ = BINARY-SEARCH(B, p₂, r₂, A[q₁])
  
  // 计算A[q₁]在C中的位置
  q₃ = p₃ + (q₁ - p₁) + (q₂ - p₂)
  C[q₃] = A[q₁]
  
  // 递归归并两半（可以并行！）
  spawn P-MERGE(A, p₁, q₁-1, B, p₂, q₂-1, C, p₃)
  P-MERGE(A, q₁+1, r₁, B, q₂, r₂, C, q₃+1)
  
  sync
```

**算法直觉**：

```
A = [2, 5, 7, 9], B = [1, 4, 6, 8], 归并到C

Step 1: A的中点元素 A[q₁] = A[2] = 5
Step 2: 在B中二分搜索5的位置 → 5在B[2]=4和B[3]=6之间 → q₂ = 3
        意思是：B中小于5的元素有B[1..2] = [1, 4]
Step 3: C中5的位置 = p₃ + (2-1) + (3-1) = 1 + 1 + 2 = 4
        C = [_, _, _, _, 5, _, _, _]

Step 4: 分成两个独立子问题：
  左半: A[1..1]=[2] 和 B[1..2]=[1,4] → 归并到C[1..3]
  右半: A[3..4]=[7,9] 和 B[3..4]=[6,8] → 归并到C[5..8]

  两个子问题互不影响！可以并行！
```

**完整执行图解**：

```
P-MERGE([2,5,7,9], [1,4,6,8])

第1层: A中点=5, B中搜索5→位置3
  C = [_,_,_,_,5,_,_,_]
  ├── spawn 左半: P-MERGE([2], [1,4])
  └── 右半: P-MERGE([7,9], [6,8])

第2层左: A中点=2, B中搜索2→位置2(B[1]=1 < 2 < B[2]=4)
  C[1..3] = [_,2,_]
  ├── spawn: P-MERGE([], [1])   → 基本情况，放1
  └──: P-MERGE([], [4])         → 基本情况，放4
  C[1..3] = [1,2,4]

第2层右: A中点=7, B中搜索7→位置3(B[3]=6 < 7 < B[4]=8)
  C[5..8] = [_,7,_,_]
  ├── spawn: P-MERGE([], [6])   → 放6
  └──: P-MERGE([9], [8])       → 递归
    A中点=9, B中搜索9→位置5(越界)
    C = [_,9]
    ├── P-MERGE([], [8])  → 放8
    └── P-MERGE([], [])   → 空

最终: C = [1,2,4,5,6,7,8,9] ✓
```

### 4.3 二分搜索辅助函数

```
BINARY-SEARCH(B, left, right, key):
  // 在B[left..right]中找key的插入位置
  // 返回最小的i使得B[i] > key（即key应该插在i之前）
  
  if left > right
    return left                   // key比所有元素都大
    
  mid = (left + right) / 2
  if B[mid] == key
    return mid + 1               // key已存在，插在后面
  elif B[mid] > key
    return BINARY-SEARCH(B, left, mid-1, key)   // 搜索左半
  else
    return BINARY-SEARCH(B, mid+1, right, key)   // 搜索右半
```

**迭代版本**（避免递归栈开销）：

```python
def binary_search(arr, left, right, key):
    """
    在arr[left..right]中找key的插入位置
    返回: 第一个使得arr[i] > key的位置i
          如果所有元素都 ≤ key，返回right+1
    """
    while left <= right:
        mid = (left + right) // 2
        if arr[mid] == key:
            return mid + 1            # 相等，插在后面
        elif arr[mid] > key:
            right = mid - 1           # 搜索左半
        else:
            left = mid + 1            # 搜索右半
    return left                       # 插入位置
```

### 4.4 工作量与持续时间分析

**工作量 T₁**：

$$T_1(n) = 2T_1(n/2) + \Theta(\log n)$$

- 两个递归调用（规模约为n/2）
- 二分搜索Θ(log n)
- 其他Θ(1)

解这个递推：

$$T_1(n) = 2T_1(n/2) + \Theta(\log n)$$

用主定理：a=2, b=2, f(n)=Θ(log n)
- log_b(a) = 1
- f(n) = Θ(n^{1-ε}) for ε close to 1
- 情况1：T₁(n) = Θ(n)

> 总工作量Θ(n)，与串行归并相同！没有增加工作量。

**持续时间 T∞**：

$$T_\infty(n) = T_\infty(n/2) + \Theta(\log n)$$

- 两个递归可以并行执行，只取最大值T∞(n/2)
- 二分搜索Θ(log n)在关键路径上
- sync Θ(1)

解：T∞(n) = T∞(n/2) + Θ(log n) = Θ(log² n)

> 详细展开：T∞(n) = Θ(log n) + Θ(log(n/2)) + ... + Θ(1) = Θ(log n · log n) = Θ(log² n)

**并行归并的并行度**：

$$\text{并行度} = \frac{T_1}{T_\infty} = \frac{\Theta(n)}{\Theta(\log^2 n)} = \Theta\left(\frac{n}{\log^2 n}\right)$$

### 4.5 多线程归并代码实现

**Python实现**：

```python
import bisect
from concurrent.futures import ThreadPoolExecutor

def binary_search_insert_pos(arr, key):
    """
    在有序数组arr中找key的插入位置
    返回: 第一个使得arr[i] > key的位置
    """
    return bisect.bisect_right(arr, key)    # Python标准库：右插入位置

def p_merge(A, B, threshold=1000):
    """
    多线程归并两个有序列表
    参数:
        A: 有序列表
        B: 有序列表
        threshold: 小于此总长度时切换到串行归并
    返回:
        归并后的有序列表
    """
    n1 = len(A)
    n2 = len(B)
    
    # 确保A是较短的（减少递归深度）
    if n1 > n2:
        A, B = B, A
        n1, n2 = n2, n1
    
    # 基本情况：A为空
    if n1 == 0:
        return B[:]                  # 直接返回B的副本
    
    # 小数组用串行归并
    if n1 + n2 <= threshold:
        return merge(A, B)           # 调用前面的串行归并
    
    # 找A的中点
    q1 = n1 // 2
    mid_a = A[q1]                    # A的中点元素
    
    # 在B中二分搜索mid_a的位置
    q2 = binary_search_insert_pos(B, mid_a)
    
    # 构建结果
    result = [0] * (n1 + n2)         # 预分配结果数组
    
    # mid_a在结果中的位置
    q3 = q1 + q2
    result[q3] = mid_a               # 放入中点元素
    
    # ===== spawn: 并行归并两半 =====
    with ThreadPoolExecutor(max_workers=2) as executor:
        future_left = executor.submit(p_merge, A[:q1], B[:q2], threshold)
        future_right = executor.submit(p_merge, A[q1+1:], B[q2:], threshold)
        
        left = future_left.result()       # sync
        right = future_right.result()     # sync
    
    # 拼装结果
    result[:q3] = left                # 左半结果
    result[q3+1:] = right             # 右半结果
    
    return result


def merge(A, B):
    """串行归并（小规模用）"""
    result = []
    i = j = 0
    while i < len(A) and j < len(B):
        if A[i] <= B[j]:
            result.append(A[i])
            i += 1
        else:
            result.append(B[j])
            j += 1
    result.extend(A[i:])
    result.extend(B[j:])
    return result


# 测试
if __name__ == "__main__":
    A = [2, 5, 7, 9, 11, 13]
    B = [1, 4, 6, 8, 10, 12, 14]
    
    result = p_merge(A, B)
    expected = sorted(A + B)
    print(f"归并结果: {result}")
    print(f"正确: {result == expected}")
```

**C++实现**：

```cpp
#include <iostream>
#include <vector>
#include <algorithm>
#include <future>

using namespace std;

// 串行归并
vector<int> serial_merge(const vector<int>& A, const vector<int>& B) {
    vector<int> C;
    C.reserve(A.size() + B.size());              // 预分配
    int i = 0, j = 0;
    while (i < (int)A.size() && j < (int)B.size()) {
        if (A[i] <= B[j]) C.push_back(A[i++]);
        else C.push_back(B[j++]);
    }
    while (i < (int)A.size()) C.push_back(A[i++]);
    while (j < (int)B.size()) C.push_back(B[j++]);
    return C;
}

// 二分搜索找插入位置
int binary_search_insert_pos(const vector<int>& arr, int key) {
    // 返回第一个使得arr[i] > key的位置
    return upper_bound(arr.begin(), arr.end(), key) - arr.begin();
}

// 多线程归并
vector<int> p_merge(const vector<int>& A, const vector<int>& B, int threshold = 1000) {
    int n1 = A.size();
    int n2 = B.size();
    
    // 确保A较短
    if (n1 > n2) {
        return p_merge(B, A, threshold);         // 交换角色
    }
    
    // 基本情况
    if (n1 == 0) {
        return B;                                // A为空，直接返回B
    }
    
    // 小数组串行归并
    if (n1 + n2 <= threshold) {
        return serial_merge(A, B);
    }
    
    // A的中点
    int q1 = n1 / 2;
    int mid_a = A[q1];
    
    // 在B中找mid_a的位置
    int q2 = binary_search_insert_pos(B, mid_a);
    
    // 结果中mid_a的位置
    int q3 = q1 + q2;
    
    // 分割
    vector<int> A_left(A.begin(), A.begin() + q1);      // A[0..q1-1]
    vector<int> A_right(A.begin() + q1 + 1, A.end());  // A[q1+1..]
    vector<int> B_left(B.begin(), B.begin() + q2);      // B[0..q2-1]
    vector<int> B_right(B.begin() + q2, B.end());       // B[q2..]
    
    // ===== spawn: 并行归并 =====
    auto future_left = async(launch::async, p_merge<int>, 
                             cref(A_left), cref(B_left), threshold);
    vector<int> right = p_merge(A_right, B_right, threshold); // 主线程
    
    // ===== sync =====
    vector<int> left = future_left.get();
    
    // 拼装
    vector<int> result;
    result.reserve(n1 + n2);
    result.insert(result.end(), left.begin(), left.end());   // 左半
    result.push_back(mid_a);                                 // 中点
    result.insert(result.end(), right.begin(), right.end()); // 右半
    
    return result;
}
```

### 4.6 带并行归并的完整多线程归并排序

将P-MERGE整合到P-MERGE-SORT中：

```python
def p_merge_sort_with_p_merge(arr, threshold=1000):
    """
    完整的多线程归并排序（使用并行归并）
    """
    if len(arr) <= 1:
        return arr
    
    if len(arr) <= threshold:
        return sorted(arr)
    
    mid = len(arr) // 2
    
    # spawn排序两半
    with ThreadPoolExecutor(max_workers=2) as executor:
        future_left = executor.submit(p_merge_sort_with_p_merge, arr[:mid], threshold)
        future_right = executor.submit(p_merge_sort_with_p_merge, arr[mid:], threshold)
        left = future_left.result()
        right = future_right.result()
    
    # 并行归并
    return p_merge(left, right, threshold)
```

**复杂度总结**：

| 版本 | T₁ | T∞ | 并行度 |
|------|------|------|--------|
| 串行归并排序 | Θ(n log n) | Θ(n log n) | 1 |
| P-MERGE-SORT（串行归并） | Θ(n log n) | Θ(n) | Θ(log n) |
| P-MERGE-SORT（并行归并） | Θ(n log n) | Θ(log² n) | Θ(n / log n) |

> 使用并行归并后，T∞从Θ(n)大幅降低到Θ(log²n)！

---

## 5️⃣ 并行循环与竞争（27.5 Parallel Loops and Race Conditions）

### 5.1 parallel-for关键字

**动机**：很多算法的核心是循环，循环的每次迭代往往是独立的。

**生活类比**：

```
检查100份试卷：
- 串行：老师一份一份看（for循环）
- 并行：10个老师每人看10份（parallel-for）

但要注意：
- 如果每个人只在自己的试卷上打分 → 没问题（无竞争）
- 如果10个人往同一张成绩单上写 → 可能冲突！（竞争条件）
```

**语法**：

```
// 串行循环
for i = 1 to n
  A[i] = f(A[i])

// 并行循环
parallel for i = 1 to n
  A[i] = f(A[i])
```

**实现方式**：parallel-for可以递归展开：

```
PARALLEL-FOR(A, f, left, right):
  if left == right
    A[left] = f(A[left])           // 基本情况：单个元素
    return
  
  mid = (left + right) / 2
  spawn PARALLEL-FOR(A, f, left, mid)     // 并行执行左半
  PARALLEL-FOR(A, f, mid+1, right)        // 并行执行右半
  sync
```

**分析**：

- 工作量：T₁ = Θ(n)（每个元素处理一次）
- 持续时间：T∞ = Θ(log n)（递归深度log n）
- 并行度：Θ(n / log n)

### 5.2 parallel-for应用于矩阵乘法

用parallel-for重写朴素矩阵乘法：

```
P-MATRIX-MULTIPLY-LOOP(A, B, C, n):
  parallel for i = 1 to n           // 行并行
    parallel for j = 1 to n         // 列并行
      C[i][j] = 0
      for k = 1 to n               // 内层串行
        C[i][j] += A[i][k] * B[k][j]
```

**分析**：

```
外层parallel for: n次迭代，每次一个内层parallel for
内层parallel for: n次迭代，每次一个for循环（串行，Θ(n)）

T₁ = Θ(n³)                         // 与朴素乘法相同
T∞ = Θ(n)                          // 两层parallel for的对数 + 内层循环
    = O(log n) + O(log n) + Θ(n)
    = Θ(n)                          // 内层循环是瓶颈

并行度 = Θ(n³/n) = Θ(n²)
```

**对比递归版本**：

| 版本 | T₁ | T∞ | 并行度 |
|------|------|------|--------|
| 递归并行 | Θ(n³) | Θ(log n) | Θ(n³/log n) |
| parallel-for | Θ(n³) | Θ(n) | Θ(n²) |

> parallel-for版本实现更简单，但T∞更差（Θ(n) vs Θ(log n)），因为内层循环是串行的。

### 5.3 竞争条件（Race Conditions）

**定义**：当两个或多个线程同时访问同一个内存位置，且至少一个是写操作时，结果取决于执行顺序。

**生活类比**：

```
两个人同时往同一个银行账户存钱：
- 账户余额: 100元
- 人A要存50元：读余额→100, 计算100+50=150, 写入150
- 人B要存30元：读余额→100, 计算100+30=130, 写入130

时间线：
人A: 读(100) ──────── 计算(150) ──────── 写(150)
人B: ──── 读(100) ──────── 计算(130) ──────── 写(130)
                                                    ↑
                                            最终余额=130！
                                            应该是180，少了50！

这就是竞争条件！
```

**代码示例**：

```python
# 错误：有竞争条件
counter = 0

def increment():
    global counter
    for _ in range(1000000):
        counter += 1    # 读-改-写，不是原子操作！

# 两个线程同时执行
thread1 = threading.Thread(target=increment)
thread2 = threading.Thread(target=increment)
thread1.start()
thread2.start()
thread1.join()
thread2.join()

print(counter)  # 预期2000000，实际可能 < 2000000！
```

**在多线程算法中的具体例子**：

```
错误的多线程矩阵向量乘法：
parallel for i = 1 to n
  parallel for j = 1 to n
    y[i] += A[i][j] * x[j]      ← 竞争！多个j同时写y[i]

正确版本1：使用局部变量
parallel for i = 1 to n
  temp = 0                       ← 每个线程有自己的temp
  for j = 1 to n
    temp += A[i][j] * x[j]      ← 无竞争
  y[i] = temp                    ← 每个y[i]只被一个线程写

正确版本2：使用sync
parallel for i = 1 to n
  parallel for j = 1 to n
    y[i] += A[i][j] * x[j]      ← 需要保证不同j不同时写
  sync                           ← 但这不对，parallel for内部的j不同步
```

### 5.4 解决竞争的方法

**方法1：避免共享写（最好）**

```
最简单的方法：确保每个变量同一时刻只有一个线程在写

parallel for i = 1 to n
  y[i] = 0                      ← 每个i写不同的y[i]，无竞争
  for j = 1 to n
    y[i] += A[i][j] * x[j]     ← 只有线程i写y[i]，无竞争
```

**方法2：使用私有变量**

```
parallel for i = 1 to n
  local_sum = 0                  ← 线程私有
  for j = 1 to n
    local_sum += A[i][j] * x[j]
  y[i] = local_sum              ← 最后一次性写入
```

**方法3：使用互斥锁（Mutex）**

```
parallel for i = 1 to n
  for j = 1 to n
    lock(mutex)                  ← 获取锁
    y[i] += A[i][j] * x[j]     ← 安全写入
    unlock(mutex)               ← 释放锁
```

> 注意：方法3虽然安全，但锁的开销很大，而且会降低并行度。尽量用方法1和方法2。

**方法4：使用归约（Reduction）**

```
// 归约操作：多个线程各自计算部分和，最后合并
parallel for i = 1 to n
  partial_sum[i] = 0
  for j = 1 to n
    partial_sum[i] += A[i][j] * x[j]

// 然后用并行前缀和或树形归约合并
```

### 5.5 确定性与非确定性

**确定性竞争（Determinacy Race）**：无论调度如何，只要输入相同，输出就相同。

**非确定性竞争**：输出依赖于线程的调度顺序。

```
确定性竞争例子：
parallel for i = 1 to n
  x[i] = x[i] + x[i+1]    ← 读x[i+1]和写x[i]，如果有i和i+1同时执行...
  实际上，这里写的是x[i]，读的是x[i+1]
  如果线程i写x[i]的同时线程i-1读x[i] → 竞争！
  但不管谁先谁后，最终结果...不一定相同！
  → 非确定性竞争

如何避免：确保算法是确定性的
- 每个变量只被一个线程写入
- 读-写不冲突
```

**FAQ：如何判断一个多线程算法有没有竞争条件？**

> 方法：
> 1. 对每个共享变量，检查是否只有一个线程写入
> 2. 如果多个线程写入同一变量，检查是否使用了同步机制（锁、原子操作等）
> 3. 如果一个线程写入而另一个线程读取同一变量，检查读取是否在写入之前
> 
> 最安全的方法：**避免共享写**。在设计算法时就让每个线程只操作自己的数据。

### 5.6 parallel-for代码实现

**Python实现**：

```python
from concurrent.futures import ThreadPoolExecutor
import threading

def parallel_for(start, end, func, num_workers=4):
    """
    并行for循环的实现
    参数:
        start: 起始索引
        end: 结束索引（不含）
        func: 对每个索引执行的函数 func(i)
        num_workers: 线程数
    """
    n = end - start
    
    # 小任务直接串行执行
    if n <= num_workers * 2:
        for i in range(start, end):
            func(i)
        return
    
    # 分块：每个worker处理一段
    chunk_size = (n + num_workers - 1) // num_workers
    
    def worker(chunk_start, chunk_end):
        """每个worker处理[chunk_start, chunk_end)"""
        for i in range(chunk_start, chunk_end):
            func(i)
    
    with ThreadPoolExecutor(max_workers=num_workers) as executor:
        futures = []
        for w in range(num_workers):
            cs = start + w * chunk_size
            ce = min(start + (w + 1) * chunk_size, end)
            if cs < ce:
                futures.append(executor.submit(worker, cs, ce))
        
        for f in futures:
            f.result()                  # 等待所有worker完成


# 示例：并行矩阵-向量乘法
def matrix_vector_multiply(A, x, num_workers=4):
    """
    并行矩阵-向量乘法 y = Ax
    无竞争条件：每个线程只写自己的y[i]
    """
    n = len(A)
    y = [0] * n
    
    def compute_row(i):
        """计算y[i] = A[i] · x"""
        local_sum = 0                  # 线程私有变量
        for j in range(n):
            local_sum += A[i][j] * x[j]
        y[i] = local_sum              # 只写y[i]，不冲突
    
    parallel_for(0, n, compute_row, num_workers)
    return y


# 示例：并行求和（使用归约，避免竞争）
def parallel_sum(arr, num_workers=4):
    """
    并行求和：使用分块+合并策略
    无竞争条件：每个线程计算自己的部分和
    """
    n = len(arr)
    chunk_size = (n + num_workers - 1) // num_workers
    partial_sums = [0] * num_workers
    lock = threading.Lock()
    
    def worker(w):
        """计算第w个分块的和"""
        start = w * chunk_size
        end = min((w + 1) * chunk_size, n)
        local_sum = 0
        for i in range(start, end):
            local_sum += arr[i]
        partial_sums[w] = local_sum    # 每个worker写不同的位置，无竞争
    
    with ThreadPoolExecutor(max_workers=num_workers) as executor:
        futures = [executor.submit(worker, w) for w in range(num_workers)]
        for f in futures:
            f.result()
    
    # 合并部分和
    return sum(partial_sums)


# 测试
if __name__ == "__main__":
    import random
    random.seed(42)
    
    # 测试矩阵-向量乘法
    n = 100
    A = [[random.randint(1, 10) for _ in range(n)] for _ in range(n)]
    x = [random.randint(1, 10) for _ in range(n)]
    
    y_parallel = matrix_vector_multiply(A, x)
    
    # 验证
    y_serial = [sum(A[i][j] * x[j] for j in range(n)) for i in range(n)]
    print(f"矩阵-向量乘法正确: {y_parallel == y_serial}")
    
    # 测试并行求和
    arr = [random.randint(1, 100) for _ in range(10000)]
    print(f"并行求和正确: {parallel_sum(arr) == sum(arr)}")
```

**C++实现**：

```cpp
#include <iostream>
#include <vector>
#include <thread>
#include <numeric>
#include <mutex>

using namespace std;

// 并行for循环
template<typename Func>
void parallel_for(int start, int end, Func func, int num_workers = 4) {
    int n = end - start;
    if (n <= 0) return;
    
    // 小任务串行
    if (n <= num_workers * 2) {
        for (int i = start; i < end; i++) func(i);
        return;
    }
    
    int chunk_size = (n + num_workers - 1) / num_workers;
    vector<thread> threads;
    
    for (int w = 0; w < num_workers; w++) {
        int cs = start + w * chunk_size;
        int ce = min(start + (w + 1) * chunk_size, end);
        if (cs >= ce) break;
        
        threads.emplace_back([cs, ce, &func]() {
            for (int i = cs; i < ce; i++) func(i);   // 每个线程处理一段
        });
    }
    
    for (auto& t : threads) t.join();               // 等待所有线程
}

// 并行矩阵-向量乘法
vector<double> parallel_matrix_vector(const vector<vector<double>>& A, 
                                       const vector<double>& x,
                                       int num_workers = 4) {
    int n = A.size();
    vector<double> y(n, 0);
    
    parallel_for(0, n, [&](int i) {
        double local_sum = 0;                        // 私有变量
        for (int j = 0; j < n; j++) {
            local_sum += A[i][j] * x[j];
        }
        y[i] = local_sum;                           // 只写y[i]
    }, num_workers);
    
    return y;
}

// 并行求和（使用部分和数组，无竞争）
double parallel_sum(const vector<double>& arr, int num_workers = 4) {
    int n = arr.size();
    vector<double> partial_sums(num_workers, 0);
    
    int chunk_size = (n + num_workers - 1) / num_workers;
    vector<thread> threads;
    
    for (int w = 0; w < num_workers; w++) {
        int start = w * chunk_size;
        int end = min((w + 1) * chunk_size, n);
        if (start >= end) break;
        
        threads.emplace_back([&, w, start, end]() {
            double local = 0;
            for (int i = start; i < end; i++) {
                local += arr[i];                     // 私有变量累加
            }
            partial_sums[w] = local;                // 写不同位置
        });
    }
    
    for (auto& t : threads) t.join();
    
    // 合并部分和
    return accumulate(partial_sums.begin(), partial_sums.end(), 0.0);
}

int main() {
    int n = 1000;
    vector<vector<double>> A(n, vector<double>(n));
    vector<double> x(n);
    
    srand(42);
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) A[i][j] = rand() % 10;
        x[i] = rand() % 10;
    }
    
    // 并行矩阵-向量乘法
    auto y = parallel_matrix_vector(A, x);
    cout << "y[0] = " << y[0] << endl;
    
    // 并行求和
    vector<double> arr(10000);
    for (auto& v : arr) v = rand() % 100;
    cout << "并行求和: " << parallel_sum(arr) << endl;
    cout << "串行求和: " << accumulate(arr.begin(), arr.end(), 0.0) << endl;
    
    return 0;
}
```

### 5.7 竞争条件检测实例

**实例：并行前缀和中的竞争**

```python
# 错误示例：并行前缀和（有竞争条件）
def parallel_prefix_wrong(arr):
    """
    错误的并行前缀和实现
    竞争条件：多个线程同时读写同一元素
    """
    n = len(arr)
    result = arr[:]
    
    # 这是一个有竞争的"并行"前缀和
    for stride in range(1, n):         # stride = 1, 2, 4, 8, ...
        parallel_for(0, n, lambda i:   # 多个线程同时执行
            if i >= stride:
                result[i] += result[i - stride]   # 竞争！读和写重叠
        )
    
    return result
```

**正确的并行前缀和**（使用sync避免竞争）：

```
P-PREFIX-SUM(A, n):
  // 向上扫描阶段（类似并行规约）
  for stride = 1 to n/2
    parallel for i = stride to n-1 step 2*stride
      A[i] += A[i - stride]    ← 这里i之间不重叠！无竞争
    sync                        ← 等这一步全部完成
```

Wait，上面的分析不对。让我重新思考。

**Kogge-Stone并行前缀和**（无竞争版本）：

```python
def parallel_prefix_sum(arr, num_workers=4):
    """
    正确的并行前缀和（Kogge-Stone算法）
    关键：每一轮的读取发生在写入之前（同一轮内无依赖）
    """
    n = len(arr)
    result = arr[:]
    
    stride = 1
    while stride < n:
        # 每一轮：每个元素加上stride之前的元素
        # 同一轮内，不同i之间没有读写冲突
        new_result = result[:]            # 创建副本！避免读-写竞争
        
        parallel_for(stride, n, lambda i: 
            new_result[i] = result[i] + result[i - stride],
            num_workers
        )
        
        result = new_result
        stride *= 2
    
    return result
```

> **关键**：创建副本避免读-写竞争。在CLRS的spawn/sync模型中，sync保证了前一轮完全结束后才开始下一轮，所以不需要副本。但在实际的shared-memory模型中，必须使用副本或同步机制。

---

## 6️⃣ 综合实例与完整分析

### 6.1 完整多线程归并排序（带并行归并）

**完整Python实现**：

```python
import bisect
from concurrent.futures import ThreadPoolExecutor
import random

def p_merge(A, B, threshold=1000):
    """多线程归并"""
    n1, n2 = len(A), len(B)
    
    if n1 > n2:
        A, B = B, A
        n1, n2 = n2, n1
    
    if n1 == 0:
        return B[:]
    
    if n1 + n2 <= threshold:
        return _serial_merge(A, B)
    
    q1 = n1 // 2
    q2 = bisect.bisect_right(B, A[q1])
    q3 = q1 + q2
    
    with ThreadPoolExecutor(max_workers=2) as executor:
        f_left = executor.submit(p_merge, A[:q1], B[:q2], threshold)
        f_right = executor.submit(p_merge, A[q1+1:], B[q2:], threshold)
        left = f_left.result()
        right = f_right.result()
    
    result = [0] * (n1 + n2)
    result[:q3] = left
    result[q3] = A[q1]
    result[q3+1:] = right
    return result

def _serial_merge(A, B):
    """串行归并"""
    i = j = 0
    result = []
    while i < len(A) and j < len(B):
        if A[i] <= B[j]:
            result.append(A[i]); i += 1
        else:
            result.append(B[j]); j += 1
    result.extend(A[i:])
    result.extend(B[j:])
    return result

def p_merge_sort_full(arr, threshold=1000):
    """完整的多线程归并排序"""
    if len(arr) <= 1:
        return arr
    if len(arr) <= threshold:
        return sorted(arr)
    
    mid = len(arr) // 2
    with ThreadPoolExecutor(max_workers=2) as executor:
        f_left = executor.submit(p_merge_sort_full, arr[:mid], threshold)
        f_right = executor.submit(p_merge_sort_full, arr[mid:], threshold)
        left = f_left.result()
        right = f_right.result()
    
    return p_merge(left, right, threshold)


# 性能测试
if __name__ == "__main__":
    import time
    
    for n in [10000, 50000, 100000, 500000]:
        arr = [random.randint(1, 1000000) for _ in range(n)]
        
        # 串行排序
        t0 = time.time()
        sorted_serial = sorted(arr)
        t_serial = time.time() - t0
        
        # 并行排序
        t0 = time.time()
        sorted_parallel = p_merge_sort_full(arr)
        t_parallel = time.time() - t0
        
        correct = sorted_serial == sorted_parallel
        print(f"n={n:>7d}: 串行={t_serial:.3f}s, 并行={t_parallel:.3f}s, "
              f"正确={correct}")
```

**完整C++实现**：

```cpp
#include <iostream>
#include <vector>
#include <future>
#include <algorithm>
#include <chrono>
#include <random>

using namespace std;

// 串行归并
vector<int> serial_merge(const vector<int>& A, const vector<int>& B) {
    vector<int> C;
    C.reserve(A.size() + B.size());
    size_t i = 0, j = 0;
    while (i < A.size() && j < B.size()) {
        if (A[i] <= B[j]) C.push_back(A[i++]);
        else C.push_back(B[j++]);
    }
    while (i < A.size()) C.push_back(A[i++]);
    while (j < B.size()) C.push_back(B[j++]);
    return C;
}

// 二分搜索插入位置
int bin_search(const vector<int>& arr, int key, int lo, int hi) {
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;
        if (arr[mid] <= key) lo = mid + 1;
        else hi = mid - 1;
    }
    return lo;                       // 第一个大于key的位置
}

// 多线程归并
vector<int> p_merge(const vector<int>& A, const vector<int>& B, int threshold = 1000) {
    if (A.size() > B.size()) return p_merge(B, A, threshold);
    
    if (A.empty()) return B;
    if (A.size() + B.size() <= (size_t)threshold) return serial_merge(A, B);
    
    int q1 = A.size() / 2;
    int q2 = bin_search(B, A[q1], 0, B.size() - 1);
    
    vector<int> A_left(A.begin(), A.begin() + q1);
    vector<int> A_right(A.begin() + q1 + 1, A.end());
    vector<int> B_left(B.begin(), B.begin() + q2);
    vector<int> B_right(B.begin() + q2, B.end());
    
    auto f_left = async(launch::async, p_merge<int>, cref(A_left), cref(B_left), threshold);
    vector<int> right = p_merge(A_right, B_right, threshold);
    vector<int> left = f_left.get();
    
    vector<int> result;
    result.reserve(A.size() + B.size());
    result.insert(result.end(), left.begin(), left.end());
    result.push_back(A[q1]);
    result.insert(result.end(), right.begin(), right.end());
    return result;
}

// 多线程归并排序
vector<int> p_merge_sort(vector<int> arr, int threshold = 1000) {
    if (arr.size() <= 1) return arr;
    if (arr.size() <= (size_t)threshold) {
        sort(arr.begin(), arr.end());
        return arr;
    }
    
    int mid = arr.size() / 2;
    vector<int> left_arr(arr.begin(), arr.begin() + mid);
    vector<int> right_arr(arr.begin() + mid, arr.end());
    
    auto f_left = async(launch::async, p_merge_sort<int>, move(left_arr), threshold);
    vector<int> right = p_merge_sort(move(right_arr), threshold);
    vector<int> left = f_left.get();
    
    return p_merge(left, right, threshold);
}

int main() {
    mt19937 rng(42);
    int n = 100000;
    vector<int> arr(n);
    for (auto& x : arr) x = rng() % 1000000;
    
    auto t0 = chrono::high_resolution_clock::now();
    auto sorted_arr = p_merge_sort(arr);
    auto t1 = chrono::high_resolution_clock::now();
    
    double elapsed = chrono::duration<double>(t1 - t0).count();
    bool correct = is_sorted(sorted_arr.begin(), sorted_arr.end());
    
    cout << "排序耗时: " << elapsed << "s" << endl;
    cout << "排序正确: " << (correct ? "是" : "否") << endl;
    return 0;
}
```

### 6.2 算法复杂度全面对比

**本章所有算法的复杂度汇总**：

| 算法 | T₁ | T∞ | 并行度 T₁/T∞ |
|------|------|------|-------------|
| P-FIB(n) | Θ(φⁿ) | Θ(n) | Θ(φⁿ/n) |
| 朴素并行矩阵乘法 | Θ(n³) | Θ(log n) | Θ(n³/log n) |
| Strassen并行 | Θ(n^{2.81}) | Θ(log² n) | Θ(n^{2.81}/log² n) |
| parallel-for矩阵乘法 | Θ(n³) | Θ(n) | Θ(n²) |
| P-MERGE-SORT(串行归并) | Θ(n log n) | Θ(n) | Θ(log n) |
| P-MERGE-SORT(并行归并) | Θ(n log n) | Θ(log² n) | Θ(n/log n) |
| P-MERGE | Θ(n) | Θ(log² n) | Θ(n/log² n) |
| parallel-for | Θ(n) | Θ(log n) | Θ(n/log n) |

**关键观察**：

1. **工作量T₁与串行版本相同**：好的并行算法不应该增加总工作量
2. **T∞决定了加速上限**：即使有无限处理器，也不能比T∞更快
3. **并行度决定了实用处理器数**：并行度=1000意味着用超过1000个处理器就没意义了
4. **parallel-for比递归分治的T∞更差**：因为循环内的操作可能串行

### 6.3 处理器数与效率的权衡

**效率（Efficiency）**：

$$E_P = \frac{T_1}{P \cdot T_P}$$

- 理想情况 E_P = 1（完美并行）
- 实际 E_P < 1

**用贪心调度的结论**：

$$T_P \leq \frac{T_1}{P} + T_\infty$$

所以：

$$E_P \geq \frac{T_1}{P \cdot (T_1/P + T_\infty)} = \frac{T_1}{T_1 + P \cdot T_\infty}$$

**当P << T₁/T∞时**（处理器数远小于并行度）：

$$E_P \approx 1$$

> 处理器不多时，效率很高。

**当P >> T₁/T∞时**（处理器数远大于并行度）：

$$E_P \approx \frac{T_1}{P \cdot T_\infty} << 1$$

> 处理器太多反而浪费，因为大部分时间在等关键路径。

**数值示例**（P-MERGE-SORT，n = 10⁶）：

```
T₁ = 20,000,000
T∞ = 400
并行度 = 50,000

P = 100:   T_P ≈ 200,000 + 400 = 200,400   E ≈ 0.998  几乎完美！
P = 1000:  T_P ≈ 20,000 + 400 = 20,400     E ≈ 0.980  很好
P = 10000: T_P ≈ 2,000 + 400 = 2,400       E ≈ 0.833  还行
P = 50000: T_P ≈ 400 + 400 = 800           E ≈ 0.500  一半时间浪费
P = 100000:T_P ≈ 200 + 400 = 600           E ≈ 0.333  大量浪费

→ 最优处理器数 ≈ 并行度 = T₁/T∞
```

---

## 7️⃣ 更多并行算法实例

### 7.1 并行矩阵-向量乘法

**算法**：

```
P-MAT-VEC(A, x, y, n):
  parallel for i = 1 to n
    y[i] = 0                      ← 无竞争：每个i写不同y[i]
    for j = 1 to n
      y[i] += A[i][j] * x[j]     ← 无竞争：只有线程i写y[i]
```

**分析**：

- T₁ = Θ(n²)
- T∞ = Θ(n)（每个线程内部的串行循环）
- 并行度 = Θ(n)

**Python实现**：

```python
import numpy as np
from concurrent.futures import ThreadPoolExecutor

def p_mat_vec_multiply(A, x, num_workers=4):
    """
    并行矩阵-向量乘法
    """
    n = A.shape[0]
    y = np.zeros(n)
    
    def compute_row(i):
        """计算y[i]"""
        y[i] = np.dot(A[i], x)       # 使用numpy内积
    
    with ThreadPoolExecutor(max_workers=num_workers) as executor:
        executor.map(compute_row, range(n))
    
    return y

# 测试
if __name__ == "__main__":
    np.random.seed(42)
    n = 1000
    A = np.random.randn(n, n)
    x = np.random.randn(n)
    
    y_parallel = p_mat_vec_multiply(A, x)
    y_numpy = A @ x
    
    print(f"最大误差: {np.max(np.abs(y_parallel - y_numpy)):.2e}")
```

### 7.2 并行矩阵乘法（最优分块策略）

**问题**：朴素并行递归的线程创建开销太大。实际中用**分块策略**。

```python
import numpy as np
from concurrent.futures import ThreadPoolExecutor

def p_matrix_multiply_blocked(A, B, block_size=64, num_workers=4):
    """
    分块并行矩阵乘法
    参数:
        A, B: n×n numpy矩阵
        block_size: 分块大小
        num_workers: 线程数
    返回:
        C = A × B
    """
    n = A.shape[0]
    C = np.zeros((n, n))
    
    def compute_block(i_start, j_start):
        """计算C[i_start:i_start+bs, j_start:j_start+bs]"""
        bs = min(block_size, n - i_start, n - j_start)
        for k_start in range(0, n, block_size):
            ks = min(block_size, n - k_start)
            # C块 += A块 × B块
            C[i_start:i_start+bs, j_start:j_start+bs] += \
                A[i_start:i_start+bs, k_start:k_start+ks] @ \
                B[k_start:k_start+ks, j_start:j_start+bs]
    
    with ThreadPoolExecutor(max_workers=num_workers) as executor:
        # 每个块一个任务
        tasks = []
        for i in range(0, n, block_size):
            for j in range(0, n, block_size):
                tasks.append(executor.submit(compute_block, i, j))
        
        for task in tasks:
            task.result()                # 等待所有块完成
    
    return C
```

> **注意**：这里的compute_block对C的写入有重叠（多个k_start累加到同一C块），但由于是同一个线程内的串行累加，所以没有竞争条件。不同(i,j)块写入C的不同区域，也没有冲突。

### 7.3 并行快排

```
P-QUICKSORT(A, p, r):
  if p < r
    q = PARTITION(A, p, r)          // 串行划分
    spawn P-QUICKSORT(A, p, q-1)    // 并行排左半
    P-QUICKSORT(A, q+1, r)          // 并行排右半
    sync
```

**分析**：

- T₁(n) = Θ(n log n)（与串行快排相同，期望情况）
- T∞(n)：最坏情况 Θ(n)（每次划分只分出一个元素）
- T∞(n)：期望情况 Θ(log n)（每次大约对半分）

**并行度**（期望）：Θ(n)

> 快排的并行化非常简单——只需spawn两个递归调用。但PARTITION本身是串行的，这是瓶颈。

**Python实现**：

```python
import random
from concurrent.futures import ThreadPoolExecutor

def p_quicksort(arr, threshold=10000):
    """
    多线程快速排序
    """
    if len(arr) <= 1:
        return arr
    
    if len(arr) <= threshold:
        return sorted(arr)
    
    # 划分
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    
    # 并行排序两半
    with ThreadPoolExecutor(max_workers=2) as executor:
        f_left = executor.submit(p_quicksort, left, threshold)
        f_right = executor.submit(p_quicksort, right, threshold)
        sorted_left = f_left.result()
        sorted_right = f_right.result()
    
    return sorted_left + middle + sorted_right


# 测试
if __name__ == "__main__":
    random.seed(42)
    arr = [random.randint(1, 100000) for _ in range(100000)]
    sorted_arr = p_quicksort(arr)
    print(f"排序正确: {sorted_arr == sorted(arr)}")
```

---

## 8️⃣ FAQ汇总

### Q1：spawn和普通函数调用有什么区别？

> **普通调用**：调用者等待被调用者完成后再继续
> **spawn**：调用者不必等待，可以继续执行下一条语句
> 
> ```
> 普通调用：x = foo()  ← 等foo()返回后才执行下一条
> spawn:   spawn x = foo()  ← 不等！直接执行下一条
> ```
> 
> 生活类比：普通调用=你叫外卖等送到才吃饭；spawn=你叫外卖同时自己先做别的菜

### Q2：sync和join有什么区别？

> 在CLRS模型中，**sync**等待当前函数内所有spawn的子线程完成。它不是等待特定线程，而是等待"所有我spawn的"。
> 
> 在实际的线程API中，**join**通常等待特定的一个线程。sync更像是"等待所有子线程"。
> 
> ```
> spawn A()
> spawn B()
> spawn C()
> sync   ← 等A、B、C都完成
> ```
> 
> 生活类比：sync = 大厨喊"等所有帮厨都做完手头的活"

### Q3：为什么并行归并排序的T∞是Θ(log²n)而不是Θ(log n)？

> 因为每一层递归都需要做一次并行归并，而并行归并本身的T∞ = Θ(log n)。
> 
> 递归深度 = Θ(log n)，每层归并 = Θ(log n)
> 总T∞ = Θ(log n) × Θ(log n) = Θ(log² n)
> 
> 就像建房子：每一层需要Θ(log n)时间打地基，共Θ(log n)层，
> 总时间 = Θ(log² n)。如果每层地基时间O(1)就好了，但归并做不到。

### Q4：多线程算法一定能加速吗？

> **不一定！** 如果：
> 1. 并行度太低（T₁/T∞接近1），多处理器没用
> 2. 线程创建/同步开销大于计算本身
> 3. 存在竞争条件，需要大量加锁
> 4. 内存带宽成为瓶颈（所有处理器抢内存）
> 
> 例如，对n=10的数组排序，用4个处理器可能比1个处理器更慢——因为线程创建的开销远大于排序本身。

### Q5：什么是Amdahl定律？和本章有什么关系？

> **Amdahl定律**：如果程序的串行比例为α，则用P个处理器的最大加速比为：
> 
> $$\text{加速比} \leq \frac{1}{\alpha + (1-\alpha)/P}$$
> 
> 当P→∞时，加速比→1/α。
> 
> 这和本章的关系：**T∞就是不可并行部分的度量**。T∞/T₁就是α。
> 
> 例如P-MERGE-SORT：α = T∞/T₁ = log²n / (n log n) = log n / n
> → 非常小，所以加速比可以很高。
> 
> P-FIB：α = n / φⁿ → 极小，加速比极高。

### Q6：如何判断一个算法值得并行化？

> 三个条件：
> 1. **并行度足够高**：T₁/T∞ >> P（处理器数）
> 2. **工作量足够大**：T₁ >> 线程创建开销
> 3. **没有难以避免的竞争条件**
> 
> 经验法则：并行度至少是处理器数的10倍以上，才值得并行化。

### Q7：CLRS的spawn/sync模型和OpenMP/Cilk有什么区别？

> | 特性 | CLRS模型 | Cilk | OpenMP |
> |------|---------|------|--------|
> | 并行构造 | spawn/sync | spawn/sync | #pragma omp |
> | 调度 | 理论贪心 | 工作窃取 | 静态/动态调度 |
> | 循环并行 | parallel-for | cilk_for | #pragma omp for |
> | 实现方式 | 理论模型 | 实际语言扩展 | 编译制导 |
> | 目标 | 算法分析 | 高性能计算 | 通用并行 |
> 
> Cilk最接近CLRS的理论模型。OpenMP更适合循环级并行。

### Q8：多线程归并的T₁为什么是Θ(n)而不是Θ(n log n)？

> 因为多线程归并不是像归并排序那样分治递归到1个元素。
> 
> P-MERGE的递推：T₁(n) = 2T₁(n/2) + Θ(log n)
> - 两个递归：2T(n/2)
> - 二分搜索：Θ(log n)
> 
> 用主定理：a=2, b=2, f(n)=Θ(log n)
> log_b(a) = 1, f(n) = Θ(n^{1-ε})
> → 情况1：T₁(n) = Θ(n)
> 
> 和串行归并的Θ(n)一样！没有增加工作量。

### Q9：为什么P-MERGE中要确保A是较短的数组？

> 这是为了优化递归深度。如果总是对较短的数组取中点并二分搜索：
> - A的大小每层至少减半
> - 递归深度 = O(log(min(n₁,n₂)))
> 
> 如果A很长，取中点后在B中二分搜索可能只能切掉A的一半，但B可能几乎没被切。确保A短可以保证两层递归都能有效缩小问题。

### Q10：实际中如何选择并行算法？

> 决策流程：
> 
> ```
> 1. 数据规模够大吗？（n < 1000 → 串行）
>    ↓ 是
> 2. 有现成并行库吗？（BLAS, MKL → 直接用）
>    ↓ 没有
> 3. 有自然并行性吗？（循环独立 → parallel-for）
>    ↓ 没有
> 4. 能分治吗？（spawn递归 → 分治并行）
>    ↓ 不能
> 5. 重新设计算法（可能需要完全不同的思路）
> ```
> 
> 实际建议：优先使用并行库（如numpy的矩阵运算、TBB的parallel_sort），自己写并行算法是最后手段。

---

## 9️⃣ 习题提示

### 27.1节习题

**27.1-1**：画出P-FIB(5)的计算DAG
> 提示：从P-FIB(5)出发，spawn P-FIB(4)和P-FIB(3)，依次展开。注意标出spawn边和continue边。

**27.1-2**：证明P-FIB的T∞(n) = Θ(n)
> 提示：建立递推T∞(n) = max(T∞(n-1), T∞(n-2)) + O(1)，然后用归纳法证明T∞(n) ≥ cn。

**27.1-3**：证明贪心调度定理T_P ≤ T₁/P + T∞
> 提示：将时间步分为完全步和不完全步。完全步的总数 ≤ T₁/P，不完全步的总数 ≤ T∞。

**27.1-4**：构造一个计算DAG，使得T_P = T₁/P + T∞
> 提示：考虑一个"宽-窄"交替的DAG，前半部分有很多并行步骤，后半部分是一条长链。

### 27.2节习题

**27.2-1**：分析P-MATRIX-MULTIPLY-RECURSIVE的T₁和T∞
> 提示：T₁ = Θ(n³)（与串行相同），T∞需要考虑递归深度和每层的spawn/sync开销。

**27.2-2**：修改P-MATRIX-MULTIPLY使得矩阵加法也是并行的，分析T∞
> 提示：并行矩阵加法T∞ = O(1)，所以T∞不变。

### 27.3节习题

**27.3-1**：分析使用串行归并的P-MERGE-SORT的T∞
> 提示：T∞(n) = T∞(n/2) + Θ(n) = Θ(n)（归并是瓶颈）。

**27.3-2**：分析使用并行归并的P-MERGE-SORT的T∞
> 提示：T∞(n) = T∞(n/2) + Θ(log² n) ... 不对，应该是T∞(n) = T∞(n/2) + T∞^{merge}(n) = T∞(n/2) + Θ(log n) = Θ(log² n)

### 27.4节习题

**27.4-1**：写出P-MERGE对A=[1,3,5,7]和B=[2,4,6,8]的完整执行过程
> 提示：A中点=3，在B中搜索3的位置→2（B[2]=4>3），分成[1]与[2]和[5,7]与[4,6,8]。

**27.4-2**：证明P-MERGE的T₁ = Θ(n)
> 提示：T₁(n) = 2T₁(n/2) + Θ(log n)，用主定理情况1。

### 27.5节习题

**27.5-1**：写出一个没有竞争条件的并行矩阵转置
> 提示：parallel for i, j: B[j][i] = A[i][j]。每个B[j][i]只被一个线程写入。

**27.5-2**：找出以下代码中的竞争条件：
```
parallel for i = 1 to n-1
  A[i] = A[i-1] + A[i+1]
```
> 提示：线程i写入A[i]，线程i-1读取A[i-1]（即A[(i-1)+1]=A[i]），读写冲突！

---

## 🔟 本章小结

### 核心知识点

```
┌─────────────────────────────────────────────────────────┐
│               多线程算法核心框架                          │
├──────────────┬──────────────────────────────────────────┤
│  并行构造     │ spawn（创建并行线程）                     │
│              │ sync（等待所有spawn完成）                  │
│              │ parallel-for（并行循环）                   │
├──────────────┼──────────────────────────────────────────┤
│  度量指标     │ T₁（工作量/总操作数）                     │
│              │ T∞（持续时间/关键路径）                    │
│              │ 并行度 = T₁/T∞                            │
│              │ T_P（P个处理器的执行时间）                 │
├──────────────┼──────────────────────────────────────────┤
│  基本定律     │ Work Law: T_P ≥ T₁/P                    │
│              │ Span Law: T_P ≥ T∞                       │
│              │ Greedy: T_P ≤ T₁/P + T∞                  │
├──────────────┼──────────────────────────────────────────┤
│  串行等价性   │ 去掉spawn/sync后与串行算法相同            │
│              │ → 正确性自动保证                          │
├──────────────┼──────────────────────────────────────────┤
│  竞争条件     │ 多线程同时读写同一变量                    │
│              │ 解决：避免共享写/私有变量/锁/归约          │
└──────────────┴──────────────────────────────────────────┘
```

### 易错点

1. **混淆T₁和T_P**：T₁是总工作量（一个人干的量），不是1个处理器的时间（实际上T₁ = T₁个处理器的时间）
2. **以为T∞ = O(log n)就一定快**：如果T₁也小，并行度可能不够，不值得并行
3. **忘记sync**：忘记等子线程完成就使用结果，导致数据未就绪
4. **忽视竞争条件**：多个线程写同一变量是最常见的并发bug
5. **过度并行**：线程创建有开销，小任务并行反而更慢
6. **混淆并行和并发**：并行=同时执行；并发=交替执行（共享时间片）

### 与前后章节的联系

| 关联章节 | 联系 |
|---------|------|
| 第4章 分治策略 | 并行算法大量使用分治思想 |
| 第7章 快速排序 | 并行快排只需spawn两个递归 |
| 第2章 插入排序 | 串行算法无法有效并行 |
| 第15章 动态规划 | 动态规划的依赖关系使得并行困难 |
| 第28章 矩阵运算 | Strassen等算法的并行化 |

### 一句话总结

> **多线程算法用spawn/sync表达并行，用T₁和T∞衡量效率，核心目标是最大化并行度（T₁/T∞）同时不增加工作量（T₁不变），并避免竞争条件保证正确性。**

---

## 📎 附录A：工作窃取调度（拓展）

### A.1 为什么需要工作窃取？

贪心调度器是理论模型，需要全局视图来分配任务。实际中，每个处理器维护自己的任务队列，当队列为空时，**从其他处理器的队列中"窃取"任务**。

### A.2 工作窃取算法

```
每个处理器维护一个双端队列（deque）：
- 本处理器从队尾取任务（LIFO，缓存友好）
- 其他处理器从队头窃取（FIFO，窃取大任务）

处理器循环：
while True:
  if 本地队列非空:
    task = 本地队列.pop_back()      ← 取自己的任务
    execute(task)                    ← 执行
  else:
    victim = random_other_processor()
    task = victim.queue.pop_front()  ← 窃取别人的任务
    if task != None:
      execute(task)
```

**生活类比**：

```
4个厨师，每人有一叠订单：
- 厨师A忙不过来，订单堆积
- 厨师D手头做完了，空闲
- 厨师D从厨师A的订单堆顶部拿一张来做
→ "工作窃取"

为什么从顶部（队头）窃取？
→ 队头的任务通常是较大的子任务（因为LIFO，最近spawn的在队尾，旧的在队头）
→ 窃取大任务更有价值
```

### A.3 工作窃取的理论保证

**定理**：工作窃取调度的期望时间满足：

$$E[T_P] \leq \frac{T_1}{P} + O(T_\infty)$$

> 与贪心调度的T_P ≤ T₁/P + T∞类似，但多了期望值（因为是随机的）。

### A.4 Python模拟工作窃取

```python
import random
import threading
from collections import deque

class WorkStealingScheduler:
    """工作窃取调度器模拟"""
    
    def __init__(self, num_workers):
        self.num_workers = num_workers
        self.queues = [deque() for _ in range(num_workers)]
        self.locks = [threading.Lock() for _ in range(num_workers)]
        self.done = False
    
    def push(self, worker_id, task):
        """向指定worker的队列添加任务"""
        with self.locks[worker_id]:
            self.queues[worker_id].append(task)   # 添加到队尾
    
    def pop_local(self, worker_id):
        """本worker从自己队列取任务"""
        with self.locks[worker_id]:
            if self.queues[worker_id]:
                return self.queues[worker_id].pop()   # 从队尾取（LIFO）
        return None
    
    def steal(self, thief_id):
        """从其他worker窃取任务"""
        # 随机选一个victim
        victims = [i for i in range(self.num_workers) if i != thief_id]
        random.shuffle(victims)
        
        for victim_id in victims:
            with self.locks[victim_id]:
                if self.queues[victim_id]:
                    return self.queues[victim_id].popleft()  # 从队头窃取（FIFO）
        return None
    
    def worker_loop(self, worker_id):
        """Worker的主循环"""
        while not self.done:
            # 先从本地取
            task = self.pop_local(worker_id)
            if task is None:
                # 本地空了，尝试窃取
                task = self.steal(worker_id)
            
            if task is not None:
                task()              # 执行任务
            else:
                pass                # 没有任务，空转


# 使用示例
def demo():
    scheduler = WorkStealingScheduler(4)
    
    # 添加一些任务
    for i in range(20):
        worker_id = i % 4
        task_id = i
        scheduler.push(worker_id, lambda tid=task_id: print(f"Task {tid} done"))
    
    # 启动workers
    threads = []
    for w in range(4):
        t = threading.Thread(target=scheduler.worker_loop, args=(w,))
        t.start()
        threads.append(t)
    
    import time
    time.sleep(2)
    scheduler.done = True
    
    for t in threads:
        t.join()

if __name__ == "__main__":
    demo()
```

---

## 📎 附录B：Cilk Plus实战（拓展）

### B.1 Cilk Plus简介

Cilk Plus是Intel开发的并行编程语言扩展，最接近CLRS的spawn/sync模型。

### B.2 Cilk Plus语法

```cpp
#include <cilk/cilk.h>
#include <cilk/reducer_opadd.h>   // 规约器

// spawn → cilk_spawn
// sync → cilk_sync
// parallel-for → cilk_for

int cilk_fib(int n) {
    if (n <= 1) return n;
    
    int x = cilk_spawn cilk_fib(n - 1);   // spawn
    int y = cilk_fib(n - 2);
    cilk_sync;                              // sync
    
    return x + y;
}

// parallel-for
void parallel_array_add(double* A, double* B, double* C, int n) {
    cilk_for (int i = 0; i < n; i++) {
        C[i] = A[i] + B[i];               // 自动并行
    }
}

// 规约（避免竞争条件）
double parallel_sum(double* arr, int n) {
    cilk::reducer_opadd<double> total;      // 线程安全的累加器
    cilk_for (int i = 0; i < n; i++) {
        total += arr[i];                    // 安全累加
    }
    return total.get_value();
}
```

### B.3 Cilk Plus的分析工具

```bash
# 编译
gcc -fcilk-plus -o program program.c

# 设置worker数
CILK_NWORKERS=4 ./program

# 性能分析
cilkscale ./program    # 输出T₁, T∞, 并行度等信息
```

---

## 📎 附录C：OpenMP快速入门

### C.1 OpenMP基本语法

```cpp
#include <omp.h>

// 并行区域
#pragma omp parallel
{
    int tid = omp_get_thread_num();      // 线程ID
    int nthreads = omp_get_num_threads(); // 线程数
    printf("Thread %d of %d\n", tid, nthreads);
}

// 并行for
#pragma omp parallel for
for (int i = 0; i < n; i++) {
    C[i] = A[i] + B[i];                  // 自动分配迭代
}

// 归约
int sum = 0;
#pragma omp parallel for reduction(+:sum)
for (int i = 0; i < n; i++) {
    sum += arr[i];                        // 安全累加
}

// 任务并行（类似spawn/sync）
#pragma omp parallel
{
    #pragma omp single
    {
        #pragma omp task
        quicksort(A, 0, mid);             // spawn
        
        #pragma omp task
        quicksort(A, mid+1, end);         // spawn
        
        #pragma omp taskwait              // sync
    }
}
```

### C.2 OpenMP矩阵乘法

```cpp
void matrix_multiply(double** A, double** B, double** C, int n) {
    #pragma omp parallel for schedule(dynamic)  // 动态调度
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            C[i][j] = 0;
            for (int k = 0; k < n; k++) {
                C[i][j] += A[i][k] * B[k][j];
            }
        }
    }
}
```

---

## 📎 附录D：Gustafson定律（拓展）

### D.1 与Amdahl定律的对比

**Amdahl定律**（固定问题规模）：

$$\text{加速比} = \frac{1}{\alpha + (1-\alpha)/P}$$

> 悲观观点：串行部分限制了最大加速

**Gustafson定律**（固定时间，增大问题规模）：

$$\text{加速比} = \alpha + (1-\alpha) \cdot P$$

> 乐观观点：增加处理器时，可以处理更大的问题

**生活类比**：

```
Amdahl: 一根管子有细段（串行瓶颈），水流量受限
  → 不管管子多粗，细段限制了流量

Gustafson: 给更多时间，可以在细段后接更长的粗管
  → 整体效率可以随处理器数增长
```

### D.2 数值示例

```
假设串行比例α = 5%, P = 100个处理器

Amdahl: 加速比 ≤ 1/(0.05 + 0.95/100) = 1/0.0595 ≈ 16.8
  → 100个处理器最多加速16.8倍，很悲观！

Gustafson: 加速比 = 0.05 + 0.95×100 = 95.05
  → 如果问题规模增大100倍，加速95倍，很乐观！
```

**关键区别**：Amdahl假设问题规模不变，Gustafson允许问题规模随处理器数增长。实际中，我们通常会在有更多处理器时处理更大的问题，所以Gustafson更符合实际。

---

*本章完成时间: 2026年*
*核心收获: spawn/sync是优雅的并行表达模型；T₁和T∞是衡量并行算法的双维度指标；好的并行算法不应增加工作量，而应尽可能缩短关键路径*
