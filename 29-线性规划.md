# 第29章 线性规划（Linear Programming）

> 本章回答一个核心问题：在一组线性约束条件下，如何找到一个线性目标函数的最优解？——从标准型与松弛型的互相转化，到单纯形算法的迭代求解，再到对偶理论的深刻洞察，线性规划不仅是运筹学的基石，更是EDA中布局布线优化、资源分配、功耗最小化的数学引擎

## 📖 本章导读

### 🎯 问题引入：为什么需要线性规划？

**生活中的例子**：

想象你是一个**奶茶店老板**，每天要做两种奶茶：
- **珍珠奶茶**：每杯利润5元，需要2份奶、1份茶、1份珍珠
- **杨枝甘露**：每杯利润8元，需要1份奶、2份茶、2份芒果

你每天的原料库存有限：奶20份、茶15份、珍珠10份、芒果12份。

**问题**：每种奶茶做多少杯，才能让利润最大？

这就是一个**线性规划问题**——目标（利润最大）是线性的，约束（原料有限）也是线性的。

```
设：x₁ = 珍珠奶茶杯数，x₂ = 杨枝甘露杯数

最大化：z = 5x₁ + 8x₂          ← 目标函数（利润）

约束条件：
  2x₁ + 1x₂ ≤ 20    （奶的库存）
  1x₁ + 2x₂ ≤ 15    （茶的库存）
  1x₁       ≤ 10    （珍珠的库存）
       2x₂ ≤ 12    （芒果的库存）
  x₁, x₂ ≥ 0        （不能做负数杯）
```

**更多生活类比**：

| 场景 | 变量 | 目标 | 约束 |
|------|------|------|------|
| 奶茶店利润 | 各饮品产量 | 利润最大化 | 原料、工时有限 |
| 饲料配方 | 各原料用量 | 成本最小化 | 营养需求、配方比例 |
| 投资组合 | 各资产配比 | 收益/风险最优化 | 预算、风险容忍度 |
| 生产排程 | 各产品产量 | 产值最大化 | 机器工时、人手有限 |
| EDA布局 | 单元坐标 | 线长最小化 | 无重叠、面积约束 |
| 网络路由 | 各链路流量 | 延迟最小化 | 带宽约束 |
| 物流配送 | 各路线运量 | 运费最小化 | 车辆容量、时间窗 |

**与前面章节的关系**：

| 对比项 | 第24章 单源最短路径 | 第26章 最大流 | 第29章 线性规划 |
|--------|-------------------|-------------|----------------|
| 数学本质 | 特殊线性规划 | 特殊线性规划 | 一般线性规划 |
| 变量类型 | 连续 | 连续 | 连续 |
| 约束类型 | 图结构约束 | 容量约束 | 任意线性约束 |
| 求解方法 | Dijkstra/Bellman-Ford | Ford-Fulkerson | 单纯形算法 |
| 核心洞察 | 最优子结构 | 增广路径 | 对偶性 |

> **关键认知**：最短路径和最大流都是线性规划的**特例**！线性规划是更一般的框架，它能统一很多我们之前学过的优化问题。

**本章路线图**：

```
线性规划问题 ──→ 标准型/松弛型 ──→ 单纯形算法 ──→ 正确性证明 ──→ 对偶性 ──→ 初始基本可行解
  问题定义        29.1 形式转换      29.2 迭代求解    29.3 为什么对     29.4 对偶问题    29.5 如何启动
  几何直觉        松弛变量           旋转操作         29.4 强弱对偶     经济解释         两阶段法
                 基本可行解         最优性检验                         互补松弛
```

### 📚 前置知识清单

| 前置知识 | 来源章节 | 本章用到的地方 |
|---------|---------|--------------|
| 矩阵与向量运算 | 第28章 | 线性规划的矩阵表示 |
| 高斯消元 | 第28章 | 单纯形算法的旋转操作 |
| 图的基本概念 | 第22章 | 网络流作为线性规划特例 |
| 最大流问题 | 第26章 | 线性规划与最大流的关系 |
| 基础线性代数 | 数学基础 | 可行域、顶点、线性无关 |

---

## 1️⃣ 标准型和松弛型（29.1 Standard and Slack Forms）

### 1.1 线性规划的几何直觉

**生活中的类比**：想象你站在一片围栏围起来的草地上，围栏就是约束条件。你要找到草地上最高的点（目标函数最大值）。如果围栏和地面都是平的（线性），那最高点一定在围栏的**角落**——这就是线性规划的核心直觉。

**二维例子**：回到奶茶店的例子

```
可行域图示（x₁-珍珠奶茶，x₂-杨枝甘露）：

x₂ (杨枝甘露)
 ↑
12 ┤────────────────────╱  芒果约束: 2x₂ ≤ 12
  │                  ╱
10 ┤               ╱│╲
  │             ╱  │  ╲  珍珠约束: x₁ ≤ 10
  │          ╱    │    ╲
 8 ┤       ╱      │     ╲
  │    ╱         │      ╲
  │ ╱  茶约束    │       ╲
 6┤   1x₁+2x₂≤15│        ╲
  │ ╲           │         ╲
  │    ╲        │          ╲
 4 ┤      ╲     │     奶约束  ╲
  │        ╲    │   2x₁+x₂≤20  ╲
  │          ╲  │              ╲
 2 ┤           ╲ │                ╲
  │             ╲│                  ╲
 0 ┼──┬──┬──┬──┬╳──┬──┬──┬──┬──→ x₁ (珍珠奶茶)
 0  2  4  6  8 10

 角点（顶点）:
 A = (0, 0)     → z = 0
 B = (10, 0)    → z = 50
 C = (10, 2.5)  → z = 70  ← 茶约束与珍珠约束的交点
 D = (4.5, 5.25)→ z = 64.5 ← 茶约束与奶约束的交点（非角点？）
 E = (4, 6)     → z = 68   ← 奶约束与芒果约束的交点
 F = (0, 6)     → z = 48

 最优解在顶点！单纯形算法就是从一个顶点"走"到相邻顶点，
 沿着让目标函数增大的方向走，直到走不动为止。
```

**核心观察**：
1. 可行域是一个**凸多面体**（convex polyhedron）——像一块切好的豆腐
2. 最优解一定在**顶点**（vertex）上——因为线性函数在凸集上的极值在极点取到
3. 顶点的个数是有限的——所以可以枚举（但指数级，需要更聪明的办法）

> **FAQ：为什么最优解一定在顶点？**
>
> 直觉理解：想象一个倾斜的平面（目标函数）压在一个多面体上。平面从高处往下降，最先碰到多面体的地方一定是某个顶点（或某条边/面，但那也包含顶点）。线性目标函数就像这个倾斜的平面，它不可能在内部取到极值，因为内部任何点都可以沿着目标函数梯度的方向移动来改善目标值，直到碰到边界。

### 1.2 线性规划的标准型（Standard Form）

**定义**：线性规划的标准型要求所有约束都是**等式**，所有变量都是**非负**的。

```
标准型：
  最大化  c₁x₁ + c₂x₂ + ... + cₙxₙ
  约束条件：
    a₁₁x₁ + a₁₂x₂ + ... + a₁ₙxₙ = b₁
    a₂₁x₁ + a₂₂x₂ + ... + a₂ₙxₙ = b₂
    ...
    aₘ₁x₁ + aₘ₂x₂ + ... + aₘₙxₙ = bₘ
  非负约束：
    x₁, x₂, ..., xₙ ≥ 0

矩阵形式：
  最大化  cᵀx
  约束条件：Ax = b，x ≥ 0

其中：
  A 是 m×n 约束矩阵
  b 是 m×1 右端向量（要求 b ≥ 0）
  c 是 n×1 目标系数向量
  x 是 n×1 变量向量
```

**生活中的类比**：标准型就像"标准考试格式"——无论你原来什么卷子，都要转换成统一的答题卡格式才能被阅卷机读取。标准型是线性规划的"答题卡格式"。

### 1.3 任意线性规划转换为标准型

**转换规则表**：

| 原始形式 | 转换方法 | 例子 |
|---------|---------|------|
| 最小化 cᵀx | 最大化 -cᵀx | min 3x₁+2x₂ → max -3x₁-2x₂ |
| 不等式 ≤ | 加松弛变量 s≥0 | x₁+x₂ ≤ 5 → x₁+x₂+s = 5 |
| 不等式 ≥ | 减剩余变量 s≥0 | x₁+x₂ ≥ 5 → x₁+x₂-s = 5 |
| 无约束变量 x | 替换为 x⁺-x⁻ | x自由 → x = x⁺-x⁻, x⁺,x⁻≥0 |
| 右端 bᵢ < 0 | 等式两边乘-1 | -x₁+x₂ = -3 → x₁-x₂ = 3 |

**完整转换示例**：

```
原始问题：                       转换为标准型：
  最小化 3x₁ - 2x₂               最大化 -3x₁ + 2x₂ - 0s₁ - 0s₂
  约束：                          约束：
    x₁ + x₂ ≤ 6                    x₁ + x₂ + s₁ = 6
    x₁ - x₂ ≥ 2                    x₁ - x₂ - s₂ = 2
    x₁ ≥ 0                         x₁ ≥ 0, x₂⁺ ≥ 0, x₂⁻ ≥ 0
    x₂ 无约束                       x₂ = x₂⁺ - x₂⁻
                                    s₁ ≥ 0, s₂ ≥ 0
```

**图解转换过程**：

```
任意线性规划问题
       │
       ▼
┌──────────────────┐
│ 1. 最小化 → 最大化 │  乘以 -1
│ 2. ≤ → 加松弛变量  │  +s, s≥0
│ 3. ≥ → 减剩余变量  │  -s, s≥0
│ 4. 自由变量 → 拆分  │  x = x⁺-x⁻
│ 5. bᵢ<0 → 乘-1    │  翻转等式
└──────────────────┘
       │
       ▼
   标准型：max cᵀx, Ax=b, x≥0
```

### 1.4 松弛型（Slack Form）

**为什么需要松弛型？** 标准型把所有约束都变成了等式，但单纯形算法需要区分哪些变量是"自由的"（可以取非零值），哪些变量是"由等式决定的"。松弛型就是做这个区分。

**定义**：松弛型把变量分为**基本变量**（basic variables）和**非基本变量**（nonbasic variables），每个等式由一个基本变量等于非基本变量的线性组合来表示。

```
松弛型：
  z  = v + Σⱼ cⱼxⱼ         （目标函数，j遍历非基本变量）
  xᵢ = bᵢ - Σⱼ aᵢⱼxⱼ       （约束 i，j遍历非基本变量）

其中：
  xᵢ (i=1..m) 是基本变量（basic variables），每个等式左边的变量
  xⱼ (j=1..n) 是非基本变量（nonbasic variables），等式右边的变量
  v 是目标函数的常数项
```

**生活类比**：松弛型就像一个公司的组织结构——
- **非基本变量** = 自由员工（可以自由选择工作量，但≥0）
- **基本变量** = 派遣员工（工作量由自由员工的工作量决定）
- 每个派遣员工的工作量 = 固定配额 - 自由员工工作量的加权和

**从标准型到松弛型的转换**：

```
标准型：                        松弛型（令s₁,s₂,s₃为基本变量）：
  最大化 3x₁ + x₂ + 2x₃         z = 0 + 3x₁ + x₂ + 2x₃
  约束：                          s₁ = 30 - x₁ - x₂ - 3x₃
    x₁ + x₂ + 3x₃ + s₁ = 30     s₂ = 24 - 2x₁ + 2x₂ + 5x₃
    2x₁- 2x₂ + 5x₃ + s₂ = 24    s₃ = 36 - 4x₁ + x₂ + 2x₃
    4x₁ + x₂ + 2x₃ + s₃ = 36
  x₁,x₂,x₃,s₁,s₂,s₃ ≥ 0
```

> **注意**：在上面的松弛型中，s₁、s₂、s₃就是松弛变量（slack variables），它们在标准型中就已经存在了。如果标准型中的约束本来就是不等式 ≤，那么加进去的松弛变量自然就成了初始的基本变量。

### 1.5 基本解与基本可行解

**基本解（Basic Solution）**：在松弛型中，令所有非基本变量 = 0，则基本变量的值就由等式右边的常数项 bᵢ 决定。

```
上例中，令 x₁ = x₂ = x₃ = 0：
  s₁ = 30, s₂ = 24, s₃ = 36, z = 0
这就是一个基本解。
```

**基本可行解（Basic Feasible Solution, BFS）**：如果基本解中所有变量值都 ≥ 0，则称为基本可行解。

```
s₁ = 30 ≥ 0 ✓
s₂ = 24 ≥ 0 ✓
s₃ = 36 ≥ 0 ✓
所以上面的基本解也是基本可行解。
```

**生活类比**：
- **基本解** = 一种"人员配置方案"（自由员工都休息，派遣员工顶上）
- **基本可行解** = "合法的人员配置方案"（派遣员工的工作量不能为负，即不能反着干）

**核心定理**：线性规划的每个基本可行解对应可行域的一个**顶点**，反之亦然。

```
                    基本可行解 ←──1:1──→ 可行域顶点

这意味着：单纯形算法只要枚举基本可行解（顶点），就能找到最优解。
而基本可行解的个数是有限的（最多 C(n+m, m) 个），
所以算法一定终止。
```

### 1.6 代码示例：标准型与松弛型的转换

**Python 实现**：

```python
"""
线性规划：标准型与松弛型的转换
"""
import numpy as np

class LinearProgram:
    """线性规划问题表示"""
    
    def __init__(self, c, A_ub=None, b_ub=None, A_eq=None, b_eq=None, 
                 bounds=None, minimize=True):
        """
        参数:
            c: 目标函数系数 (n,)
            A_ub: 不等式约束系数 (m1, n), <= 约束
            b_ub: 不等式约束右端 (m1,)
            A_eq: 等式约束系数 (m2, n)
            b_eq: 等式约束右端 (m2,)
            bounds: 变量范围, 每个元素 (low, high), None表示无界
            minimize: True为最小化, False为最大化
        """
        self.c_orig = np.array(c, dtype=float)  # 保存原始目标系数
        self.minimize = minimize
        self.A_ub = np.array(A_ub, dtype=float) if A_ub is not None else None
        self.b_ub = np.array(b_ub, dtype=float) if b_ub is not None else None
        self.A_eq = np.array(A_eq, dtype=float) if A_eq is not None else None
        self.b_eq = np.array(b_eq, dtype=float) if b_eq is not None else None
        self.bounds = bounds  # 变量范围
        
    def to_standard_form(self):
        """
        将线性规划转换为标准型: max c^T x, Ax = b, x >= 0
        
        返回:
            c_std: 标准型目标系数
            A_std: 标准型约束矩阵
            b_std: 标准型右端向量
            var_info: 变量信息列表 (用于还原解)
        """
        n_orig = len(self.c_orig)  # 原始变量个数
        var_info = []  # 记录每个标准型变量对应原始变量的信息
        
        # 步骤1: 处理目标函数 —— 最小化转为最大化
        if self.minimize:
            c = -self.c_orig.copy()  # min c^T x 等价于 max -c^T x
        else:
            c = self.c_orig.copy()
        
        # 步骤2: 处理自由变量 —— 拆分为 x = x⁺ - x⁻
        c_expanded = []  # 扩展后的目标系数
        col_map = []     # 列映射: 标准型列索引 → (原始变量索引, +或-)
        
        for j in range(n_orig):
            if self.bounds is not None and self.bounds[j] is None:
                # 自由变量: xⱼ = xⱼ⁺ - xⱼ⁻
                c_expanded.append(c[j])    # xⱼ⁺ 的系数
                c_expanded.append(-c[j])   # xⱼ⁻ 的系数
                col_map.append((j, '+'))
                col_map.append((j, '-'))
                var_info.append({'orig_idx': j, 'type': 'positive'})
                var_info.append({'orig_idx': j, 'type': 'negative'})
            else:
                c_expanded.append(c[j])
                col_map.append((j, '+'))
                var_info.append({'orig_idx': j, 'type': 'normal'})
        
        n_expanded = len(c_expanded)  # 扩展后的变量个数
        c_expanded = np.array(c_expanded, dtype=float)
        
        # 步骤3: 构建约束矩阵
        # 先处理不等式约束
        m_ub = self.A_ub.shape[0] if self.A_ub is not None else 0
        m_eq = self.A_eq.shape[0] if self.A_eq is not None else 0
        m_total = m_ub + m_eq
        
        A_std = np.zeros((m_total, n_expanded + m_ub))  # 加上松弛变量
        b_std = np.zeros(m_total)
        c_std = np.zeros(n_expanded + m_ub)  # 松弛变量目标系数为0
        
        # 填入目标系数（扩展变量部分）
        c_std[:n_expanded] = c_expanded
        
        # 处理不等式约束: A_ub * x_expanded + s = b_ub
        if self.A_ub is not None:
            for i in range(m_ub):
                # 将原始系数映射到扩展列
                new_col = 0
                for j in range(n_orig):
                    if self.bounds is not None and self.bounds[j] is None:
                        A_std[i, new_col] = self.A_ub[i, j]      # xⱼ⁺
                        A_std[i, new_col + 1] = -self.A_ub[i, j]  # xⱼ⁻
                        new_col += 2
                    else:
                        A_std[i, new_col] = self.A_ub[i, j]
                        new_col += 1
                # 松弛变量
                A_std[i, n_expanded + i] = 1.0  # 第i个松弛变量
                b_std[i] = self.b_ub[i]
                var_info.append({'orig_idx': -1, 'type': 'slack', 'constr_idx': i})
        
        # 处理等式约束: A_eq * x_expanded = b_eq
        if self.A_eq is not None:
            for i in range(m_eq):
                new_col = 0
                for j in range(n_orig):
                    if self.bounds is not None and self.bounds[j] is None:
                        A_std[m_ub + i, new_col] = self.A_eq[i, j]
                        A_std[m_ub + i, new_col + 1] = -self.A_eq[i, j]
                        new_col += 2
                    else:
                        A_std[m_ub + i, new_col] = self.A_eq[i, j]
                        new_col += 1
                b_std[m_ub + i] = self.b_eq[i]
        
        return c_std, A_std, b_std, var_info

    def to_slack_form(self):
        """
        将标准型转换为松弛型
        松弛型: z = v + Σ cⱼxⱼ (非基本变量)
               xᵢ = bᵢ - Σ aᵢⱼxⱼ (基本变量)
        
        返回:
            slack: 字典, 包含松弛型的所有信息
        """
        c_std, A_std, b_std, var_info = self.to_standard_form()
        m, n = A_std.shape
        
        # 初始松弛型: 松弛变量为基本变量, 原始变量为非基本变量
        # 这要求 b_std >= 0 (标准型要求)
        
        # 基本变量索引 (初始为松弛变量, 即后m列)
        basic = list(range(n - m, n))  # 松弛变量在标准型中的列索引
        nonbasic = list(range(n - m))  # 原始变量 (含扩展变量)
        
        slack = {
            'c': c_std,           # 目标系数
            'A': A_std.copy(),    # 约束矩阵
            'b': b_std.copy(),    # 右端向量
            'basic': basic,       # 基本变量索引
            'nonbasic': nonbasic, # 非基本变量索引
            'v': 0.0,             # 目标函数常数项
            'var_info': var_info
        }
        return slack


# ===== 使用示例 =====
if __name__ == '__main__':
    # 奶茶店问题
    print("=== 奶茶店线性规划问题 ===")
    print("最大化 5x₁ + 8x₂")
    print("约束: 2x₁+x₂≤20, x₁+2x₂≤15, x₁≤10, 2x₂≤12, x₁,x₂≥0")
    
    lp = LinearProgram(
        c=[5, 8],
        A_ub=[[2, 1], [1, 2], [1, 0], [0, 2]],
        b_ub=[20, 15, 10, 12],
        minimize=False  # 最大化
    )
    
    c_std, A_std, b_std, var_info = lp.to_standard_form()
    print(f"\n标准型变量个数: {len(c_std)}")
    print(f"标准型约束个数: {A_std.shape[0]}")
    print(f"标准型目标系数 c = {c_std}")
    print(f"标准型右端 b = {b_std}")
    
    slack = lp.to_slack_form()
    print(f"\n松弛型:")
    print(f"  基本变量索引: {slack['basic']}")
    print(f"  非基本变量索引: {slack['nonbasic']}")
    print(f"  目标函数常数项 v = {slack['v']}")
```

**C++ 实现**：

```cpp
/**
 * 线性规划：标准型与松弛型的转换
 * 编译: g++ -std=c++17 -O2 lp_standard.cpp -o lp_standard
 */
#include <iostream>
#include <vector>
#include <iomanip>
#include <cassert>

using namespace std;
using Matrix = vector<vector<double>>;
using Vector = vector<double>;

// 打印矩阵
void printMatrix(const string& name, const Matrix& M) {
    cout << name << " = [" << endl;
    for (const auto& row : M) {
        cout << "  [";
        for (size_t j = 0; j < row.size(); j++) {
            cout << setw(8) << fixed << setprecision(2) << row[j];
            if (j < row.size() - 1) cout << ",";
        }
        cout << "]" << endl;
    }
    cout << "]" << endl;
}

// 打印向量
void printVector(const string& name, const Vector& v) {
    cout << name << " = [";
    for (size_t i = 0; i < v.size(); i++) {
        cout << fixed << setprecision(2) << v[i];
        if (i < v.size() - 1) cout << ", ";
    }
    cout << "]" << endl;
}

/**
 * 将一般线性规划转换为标准型
 * 输入: 目标系数c, 不等式约束(A_ub, b_ub), 是否最小化
 * 输出: 标准型的(c_std, A_std, b_std)
 */
void toStandardForm(
    const Vector& c_orig,        // 原始目标系数
    const Matrix& A_ub,         // 不等式约束矩阵 (≤)
    const Vector& b_ub,         // 不等式约束右端
    bool minimize,              // true=最小化, false=最大化
    Vector& c_std,              // 输出: 标准型目标系数
    Matrix& A_std,              // 输出: 标准型约束矩阵
    Vector& b_std               // 输出: 标准型右端向量
) {
    size_t n = c_orig.size();   // 原始变量个数
    size_t m = A_ub.size();    // 不等式约束个数
    
    // 步骤1: 最小化转最大化
    Vector c = c_orig;
    if (minimize) {
        for (auto& ci : c) ci = -ci;  // min → max by negating
    }
    
    // 步骤2: 添加松弛变量, 每个不等式约束加一个
    // 标准型变量个数 = n + m (原始变量 + 松弛变量)
    size_t n_std = n + m;
    
    c_std.assign(n_std, 0.0);
    A_std.assign(m, vector<double>(n_std, 0.0));
    b_std = b_ub;
    
    // 目标系数: 原始变量部分
    for (size_t j = 0; j < n; j++) {
        c_std[j] = c[j];  // 松弛变量目标系数保持为0
    }
    
    // 约束矩阵: 原始变量部分 + 松弛变量部分
    for (size_t i = 0; i < m; i++) {
        for (size_t j = 0; j < n; j++) {
            A_std[i][j] = A_ub[i][j];  // 原始变量系数
        }
        A_std[i][n + i] = 1.0;  // 第i个松弛变量系数为1
    }
    
    cout << "\n=== 标准型转换完成 ===" << endl;
    cout << "变量个数: " << n_std << " (原始 " << n << " + 松弛 " << m << ")" << endl;
    printVector("c_std", c_std);
    printMatrix("A_std", A_std);
    printVector("b_std", b_std);
}

int main() {
    // 奶茶店问题
    cout << "=== 奶茶店线性规划问题 ===" << endl;
    cout << "最大化 5x1 + 8x2" << endl;
    cout << "约束: 2x1+x2<=20, x1+2x2<=15, x1<=10, 2x2<=12" << endl;
    
    Vector c = {5, 8};
    Matrix A_ub = {
        {2, 1},   // 奶的约束
        {1, 2},   // 茶的约束
        {1, 0},   // 珍珠的约束
        {0, 2}    // 芒果的约束
    };
    Vector b_ub = {20, 15, 10, 12};
    
    Vector c_std, b_std;
    Matrix A_std;
    
    toStandardForm(c, A_ub, b_ub, false, c_std, A_std, b_std);
    
    return 0;
}
```

> **FAQ：标准型和松弛型有什么区别？看起来差不多？**
>
> 标准型是**数学表示**——所有约束都是等式，所有变量都非负，是一种"全局视角"。松弛型是**算法视角**——明确区分了基本变量和非基本变量，每个基本变量用非基本变量表示，是一种"局部视角"，方便单纯形算法操作。你可以把标准型理解为"原料清单"，松弛型理解为"烹饪步骤"——原料是一样的，但步骤更可操作。

---

## 2️⃣ 单纯形算法（29.2 The Simplex Algorithm）

### 2.1 算法核心思想

**生活中的类比**：单纯形算法就像"爬山"——你站在一个山顶（基本可行解），看看周围哪个相邻山顶更高，如果有的话就走到那个山顶，重复直到所有相邻山顶都不比自己高。

```
爬山过程：
                    ★ 最优解 (z=70)
                   ╱
                  ╱
    起点B(z=50) ╱
         │    ╱
         │  ╱  C(z=70) ← 到了！没有更高的相邻顶点了
         │╱
    A(z=0) ← 起点

但是！单纯形算法不是真的"爬山"，而是在多面体的顶点之间"走"。
每一步：
  1. 选一个"上升方向"（让目标函数增大的非基本变量）
  2. 沿着这个方向走，直到碰到某个约束的边界
  3. 到达新的顶点，更新基本变量和非基本变量
```

### 2.2 旋转操作（Pivot）

旋转是单纯形算法的**核心操作**——把一个非基本变量"换入"基本变量集合，同时把一个基本变量"换出"。

**生活类比**：旋转就像"换班"——
- 非基本变量 = "待命员工"（当前没在上班）
- 基本变量 = "在岗员工"（正在上班）
- 旋转 = 一个待命员工上岗，一个在岗员工下岗

**旋转的数学过程**：

```
旋转前（松弛型）：
  z  = 10 + 3x₁ + 2x₂       ← 非基本变量 x₁, x₂
  x₃ = 6  - 2x₁ - 1x₂       ← 基本变量 x₃
  x₄ = 8  - 1x₁ - 3x₂       ← 基本变量 x₄

假设选择 x₁ 入基（因为系数3 > 0，增大x₁可以增加z）

步骤1: 确定出基变量
  从 x₃: x₁ ≤ 6/2 = 3       ← x₁最多增到3
  从 x₄: x₁ ≤ 8/1 = 8       ← x₁最多增到8
  取最小值: x₁ = 3, 出基变量 = x₃（最紧约束）

步骤2: 从 x₃ 的等式中解出 x₁
  x₁ = 3 - 0.5x₃ - 0.5x₂    ← x₁ 变成基本变量

步骤3: 代入其他等式
  z  = 10 + 3(3 - 0.5x₃ - 0.5x₂) + 2x₂
     = 19 - 1.5x₃ + 0.5x₂

  x₄ = 8 - 1(3 - 0.5x₃ - 0.5x₂) - 3x₂
     = 5 + 0.5x₃ - 2.5x₂

旋转后（松弛型）：
  z  = 19 - 1.5x₃ + 0.5x₂    ← 非基本变量变成了 x₃, x₂
  x₁ = 3  - 0.5x₃ - 0.5x₂    ← x₁ 现在是基本变量
  x₄ = 5  + 0.5x₃ - 2.5x₂    ← x₄ 还是基本变量
```

**图解旋转过程**：

```
旋转操作流程：

  ┌──────────────────────────┐
  │   选择入基变量 (entering)  │ ← 在z行中选一个正系数的非基本变量
  │   条件: cⱼ > 0            │   （增大它能增加目标函数）
  └────────────┬─────────────┘
               │
               ▼
  ┌──────────────────────────┐
  │   确定出基变量 (leaving)   │ ← 对每个基本变量，计算：
  │   选择最紧约束             │   θᵢ = bᵢ / aᵢⱼ (aᵢⱼ > 0)
  │   θ* = min{bᵢ/aᵢⱼ}      │   取最小的θᵢ对应的变量出基
  └────────────┬─────────────┘
               │
               ▼
  ┌──────────────────────────┐
  │   执行旋转（高斯消元）     │ ← 从出基变量的等式解出入基变量
  │   - 入基变量变为基本变量    │   代入其他等式
  │   - 出基变量变为非基本变量  │
  └────────────┬─────────────┘
               │
               ▼
  ┌──────────────────────────┐
  │   检查z行的系数            │
  │   如果所有 cⱼ ≤ 0: 最优！  │ ← 没有方向可以增大z了
  │   否则: 继续旋转           │
  └──────────────────────────┘
```

### 2.3 单纯形算法完整流程

```
SIMPLEX(A, b, c):
  将线性规划转为松弛型
  while True:
    // 步骤1: 选择入基变量
    找到 j 使得 cⱼ > 0（在z行中选正系数的非基本变量）
    if 没有这样的 j:
      return "最优解已找到"  // 当前基本可行解就是最优的
    
    // 步骤2: 确定出基变量
    计算所有 i 的 θᵢ = bᵢ / aᵢⱼ（仅当 aᵢⱼ > 0）
    if 所有 aᵢⱼ ≤ 0:
      return "无界"  // xⱼ可以无限增大
    
    选择 i* 使 θᵢ* 最小 → xᵢ* 出基
    
    // 步骤3: 旋转
    PIVOT(当前松弛型, 入基变量xⱼ, 出基变量xᵢ*)
    // 从xᵢ*的等式解出xⱼ，代入其他等式
```

### 2.4 完整示例：奶茶店问题

```
原始问题：
  最大化  z = 5x₁ + 8x₂
  约束: 2x₁ + x₂ ≤ 20   (s₁)
        x₁ + 2x₂ ≤ 15   (s₂)
        x₁       ≤ 10   (s₃)
             2x₂ ≤ 12   (s₄)
        x₁, x₂ ≥ 0

转换为松弛型（s₁~s₄为基本变量）：
  z  = 0 + 5x₁ + 8x₂
  s₁ = 20 - 2x₁ - 1x₂
  s₂ = 15 - 1x₁ - 2x₂
  s₃ = 10 - 1x₁
  s₄ = 12        - 2x₂

===== 第1次旋转 =====
选择入基变量: x₂ (系数8最大，增大x₂对z贡献最大)

确定出基变量:
  s₁: θ₁ = 20/1 = 20
  s₂: θ₂ = 15/2 = 7.5   ← 最小！
  s₃: x₂的系数为0，跳过
  s₄: θ₄ = 12/2 = 6      ← 更小！

  出基变量 = s₄ (θ₄ = 6最小)

从 s₄ = 12 - 2x₂ 解出: x₂ = 6 - 0.5s₄

代入:
  z  = 0 + 5x₁ + 8(6 - 0.5s₄) = 48 + 5x₁ - 4s₄
  s₁ = 20 - 2x₁ - (6 - 0.5s₄) = 14 - 2x₁ + 0.5s₄
  s₂ = 15 - x₁ - 2(6 - 0.5s₄) = 3  - x₁  + s₄
  s₃ = 10 - x₁
  x₂ = 6 - 0.5s₄

当前解: x₁=0, x₂=6, z=48

===== 第2次旋转 =====
选择入基变量: x₁ (系数5 > 0)

确定出基变量:
  s₁: θ₁ = 14/2 = 7
  s₂: θ₂ = 3/1 = 3     ← 最小！
  s₃: θ₃ = 10/1 = 10

  出基变量 = s₂ (θ₂ = 3最小)

从 s₂ = 3 - x₁ + s₄ 解出: x₁ = 3 + s₄ - s₂

代入:
  z  = 48 + 5(3 + s₄ - s₂) - 4s₄ = 63 + s₄ - 5s₂
  s₁ = 14 - 2(3 + s₄ - s₂) + 0.5s₄ = 8 - 1.5s₄ + 2s₂
  x₁ = 3 + s₄ - s₂
  s₃ = 10 - (3 + s₄ - s₂) = 7 - s₄ + s₂
  x₂ = 6 - 0.5s₄

当前解: x₁=3, x₂=6, z=63

===== 第3次旋转 =====
选择入基变量: s₄ (系数1 > 0)

确定出基变量:
  s₁: θ₁ = 8/1.5 = 5.33
  x₁: s₄系数为1>0, 但 x₁=3+s₄-s₂，无法直接计算... 
  实际上需要看基本变量等式中s₄的系数:
    s₁ = 8 - 1.5s₄ + 2s₂   → s₄的系数为-1.5, 不行（需要正系数）
    等等，让我重新看：
    s₁中s₄系数 = -1.5, 不满足aᵢⱼ>0
    x₁中s₄系数 = 1 > 0, θ = (3+s₄-s₂) / 1... 
    
  实际上应该这样: 基本变量等式右侧s₄系数为负意味着增大s₄时该基本变量也增大，
  不会降到0。所以只看右侧s₄系数为正的行:
    s₃中s₄系数... 让我重写s₃的等式:
    s₃ = 7 - s₄ + s₂  → s₄系数是-1, 不行
    
  等等，我犯了一个符号错误。让我用标准松弛型重新检查：
  松弛型中 xᵢ = bᵢ - Σaᵢⱼxⱼ，所以"系数"是前面的负号后的值。

  重新检查z行: z = 63 + s₄ - 5s₂
  s₄的系数为+1，增大s₄可以增加z → 入基
  
  基本变量等式:
    s₁ = 8 - (-1.5)s₄ → 不对，让我按松弛型格式重写：

    标准松弛型格式: xᵢ = bᵢ - Σ aᵢⱼ xⱼ
    
    s₁ = 8 - (-1.5)s₄ - 2s₂ → a(s₁,s₄) = -1.5, 非正，跳过
    等等这越来越混乱了...
```

让我用一个更清晰的例子来演示：

```
CLRS书上的经典例子：

  最大化  z = 3x₁ + x₂ + 2x₃
  约束:   x₁ + x₂ + 3x₃ ≤ 30
         2x₁ + 2x₂ + 5x₃ ≤ 24
         4x₁ + x₂ + 2x₃ ≤ 36
         x₁, x₂, x₃ ≥ 0

松弛型（s₁, s₂, s₃为基本变量）：

  z  = 0 + 3x₁ + x₂ + 2x₃
  s₁ = 30 - x₁ - x₂ - 3x₃
  s₂ = 24 - 2x₁ - 2x₂ - 5x₃
  s₃ = 36 - 4x₁ - x₂ - 2x₃

初始基本可行解: x₁=x₂=x₃=0, s₁=30, s₂=24, s₃=36, z=0

===== 第1次旋转: x₃入基, s₂出基 =====

z行中x₃系数为2（正），选x₃入基

θ计算:
  s₁: θ₁ = 30/3 = 10
  s₂: θ₂ = 24/5 = 4.8   ← 最小
  s₃: θ₃ = 36/2 = 18

s₂出基。从 s₂ = 24 - 2x₁ - 2x₂ - 5x₃ 解出:
  x₃ = 4.8 - 0.4x₁ - 0.4x₂ - 0.2s₂

代入其他等式:
  z  = 0 + 3x₁ + x₂ + 2(4.8 - 0.4x₁ - 0.4x₂ - 0.2s₂)
     = 9.6 + 2.2x₁ + 0.2x₂ - 0.4s₂

  s₁ = 30 - x₁ - x₂ - 3(4.8 - 0.4x₁ - 0.4x₂ - 0.2s₂)
     = 15.6 + 0.2x₁ + 0.2x₂ + 0.6s₂

  s₃ = 36 - 4x₁ - x₂ - 2(4.8 - 0.4x₁ - 0.4x₂ - 0.2s₂)
     = 26.4 - 3.2x₁ - 0.2x₂ + 0.4s₂

  x₃ = 4.8 - 0.4x₁ - 0.4x₂ - 0.2s₂

当前解: x₁=0, x₂=0, x₃=4.8, z=9.6

===== 第2次旋转: x₁入基, s₃出基 =====

z行中x₁系数为2.2（最大正系数），选x₁入基

θ计算:
  s₁: x₁系数 = -0.2 (在松弛型xᵢ=bᵢ-Σaᵢⱼxⱼ中，实际系数为+0.2)
      这里s₁ = 15.6 + 0.2x₁，增大x₁时s₁增大，不会到0
  s₃: x₁系数为3.2 (在x₃=b-Σ...的格式中)
      s₃ = 26.4 - 3.2x₁，x₁最多增到 26.4/3.2 = 8.25
  x₃: x₁系数为0.4
      x₃ = 4.8 - 0.4x₁，x₁最多增到 4.8/0.4 = 12

取最小: θ = 8.25, s₃出基

从 s₃ = 26.4 - 3.2x₁ - 0.2x₂ + 0.4s₂ 解出:
  x₁ = 8.25 - 0.0625x₂ + 0.125s₂ - 0.3125s₃

代入:
  z = 9.6 + 2.2(8.25 - 0.0625x₂ + 0.125s₂ - 0.3125s₃) + 0.2x₂ - 0.4s₂
    = 27.75 + 0.0625x₂ - 0.125s₂ - 0.6875s₃

  （其他等式类似代入...）

现在z行中只剩x₂系数为正(0.0625)，继续旋转...

===== 最终 =====

经过若干次旋转后，z行中所有非基本变量系数 ≤ 0，
说明当前基本可行解就是最优解。
```

> **FAQ：为什么选最小θᵢ对应的变量出基？**
>
> 因为θᵢ表示"入基变量最多能增大多少而不让第i个基本变量变成负数"。我们必须保证所有变量都 ≥ 0（可行性），所以取最小的θᵢ——这就是"最紧约束"原则。就像水管一样，流量由最窄的那段决定。

### 2.5 Python 完整实现

```python
"""
单纯形算法完整实现
支持标准型线性规划: max c^T x, Ax = b, x >= 0
"""
import numpy as np
from typing import Optional, Tuple

class SimplexSolver:
    """单纯形算法求解器"""
    
    def __init__(self, c: np.ndarray, A: np.ndarray, b: np.ndarray):
        """
        初始化线性规划问题（标准型）
        
        参数:
            c: 目标函数系数 (n,)
            A: 等式约束矩阵 (m, n)
            b: 等式约束右端 (m,), 要求 b >= 0
        """
        self.c_orig = c.copy()       # 保存原始目标系数
        self.m, self.n = A.shape      # m=约束数, n=变量数
        self.A = A.copy()
        self.b = b.copy()
        
        # 松弛型数据结构
        self.c_bar = np.zeros(self.n)  # z行中非基本变量的系数
        self.v = 0.0                   # z行的常数项
        self.basic = list(range(self.n - self.m, self.n))    # 基本变量索引
        self.nonbasic = list(range(self.n - self.m))          # 非基本变量索引
        
        # 初始化z行的系数（非基本变量部分用原始c）
        for j in range(len(self.nonbasic)):
            self.c_bar[j] = c[self.nonbasic[j]]
    
    def pivot(self, entering_idx: int, leaving_row: int) -> None:
        """
        执行旋转操作
        
        参数:
            entering_idx: 入基变量在nonbasic列表中的索引
            leaving_row: 出基变量在basic列表中的索引（对应第几行约束）
        """
        j = self.nonbasic[entering_idx]  # 入基变量的原始列索引
        i = leaving_row                   # 出基变量所在行
        
        # 旋转主元
        pivot_val = self.A[i, j]
        assert pivot_val != 0, "旋转主元不能为0"
        
        # 步骤1: 归一化第i行（除以主元值）
        self.A[i] = self.A[i] / pivot_val  # 整行除以主元
        self.b[i] = self.b[i] / pivot_val
        
        # 步骤2: 消去其他行中的第j列元素（高斯消元）
        for k in range(self.m):
            if k == i:
                continue  # 跳过主元行
            factor = self.A[k, j]  # 第k行第j列的元素
            if factor != 0:
                self.A[k] = self.A[k] - factor * self.A[i]
                self.b[k] = self.b[k] - factor * self.b[i]
        
        # 步骤3: 更新z行
        factor = self.c_bar[entering_idx] if entering_idx < len(self.c_bar) else 0
        if factor != 0:
            # z行也需要消元
            self.v = self.v + factor * self.b[i]
            # 更新c_bar中其他非基本变量的系数
            # 这需要更细致的处理，参见下面的完整版
        
        # 步骤4: 交换基本变量和非基本变量
        self.basic[i], self.nonbasic[entering_idx] = \
            self.nonbasic[entering_idx], self.basic[i]
    
    def solve(self) -> Tuple[Optional[np.ndarray], float]:
        """
        执行单纯形算法
        
        返回:
            x_opt: 最优解向量 (n,)
            z_opt: 最优目标函数值
        """
        max_iter = 1000  # 最大迭代次数，防止无限循环
        
        for iteration in range(max_iter):
            # 步骤1: 选择入基变量
            # 在z行中找最大的正系数
            entering_idx = -1
            max_coeff = 0
            for idx, j in enumerate(self.nonbasic):
                # 计算当前z行中xⱼ的系数
                coeff = self._get_z_coeff(j)
                if coeff > max_coeff + 1e-10:
                    max_coeff = coeff
                    entering_idx = idx
            
            if entering_idx == -1:
                # 所有系数 ≤ 0，当前解最优
                break
            
            j = self.nonbasic[entering_idx]
            
            # 步骤2: 确定出基变量（最小比值测试）
            leaving_row = -1
            min_theta = float('inf')
            for i in range(self.m):
                if self.A[i, j] > 1e-10:  # 只看正系数
                    theta = self.b[i] / self.A[i, j]
                    if theta < min_theta - 1e-10:
                        min_theta = theta
                        leaving_row = i
            
            if leaving_row == -1:
                # 所有aᵢⱼ ≤ 0，问题无界
                print("问题无界！")
                return None, float('inf')
            
            # 步骤3: 执行旋转
            self._do_pivot(entering_idx, leaving_row)
        
        # 构造解向量
        x_opt = np.zeros(self.n)
        for i, bi in enumerate(self.basic):
            x_opt[bi] = self.b[i]
        
        z_opt = self._compute_objective(x_opt)
        return x_opt, z_opt
    
    def _get_z_coeff(self, j: int) -> float:
        """获取z行中变量xⱼ的系数"""
        # z = v + Σ c̄ⱼ xⱼ (j在nonbasic中)
        # 需要根据当前松弛型计算
        # 简化实现：使用c_orig和当前A来计算
        coeff = self.c_orig[j]
        for i in range(self.m):
            coeff -= self.c_orig[self.basic[i]] * self.A[i, j]
        return coeff
    
    def _compute_objective(self, x: np.ndarray) -> float:
        """计算目标函数值"""
        return np.dot(self.c_orig, x)
    
    def _do_pivot(self, entering_idx: int, leaving_row: int) -> None:
        """执行完整的旋转操作（含z行更新）"""
        j = self.nonbasic[entering_idx]
        i = leaving_row
        
        pivot_val = self.A[i, j]
        
        # 归一化主元行
        self.A[i] = self.A[i] / pivot_val
        self.b[i] = self.b[i] / pivot_val
        
        # 消去其他行
        for k in range(self.m):
            if k == i:
                continue
            factor = self.A[k, j]
            if abs(factor) > 1e-12:
                self.A[k] = self.A[k] - factor * self.A[i]
                self.b[k] = self.b[k] - factor * self.b[i]
        
        # 消去z行
        z_coeff = self._get_z_coeff(j)
        if abs(z_coeff) > 1e-12:
            self.v += z_coeff * self.b[i]
            # z行的系数通过_get_z_coeff动态计算
        
        # 交换基本/非基本变量
        self.basic[i], self.nonbasic[entering_idx] = \
            self.nonbasic[entering_idx], self.basic[i]
    
    def print_tableau(self, iteration: int) -> None:
        """打印单纯形表"""
        print(f"\n===== 迭代 {iteration} =====")
        print(f"z行常数项 v = {self.v:.4f}")
        print(f"基本变量: {self.basic}")
        print(f"非基本变量: {self.nonbasic}")
        print(f"b = {self.b}")
        
        # 打印z行系数
        z_coeffs = []
        for j in self.nonbasic:
            z_coeffs.append(f"{self._get_z_coeff(j):.4f}")
        print(f"z行非基本变量系数: {z_coeffs}")


def solve_lp_from_inequality(c, A_ub, b_ub, minimize=False):
    """
    从不等式形式的线性规划直接求解
    
    参数:
        c: 目标系数
        A_ub: ≤ 约束矩阵
        b_ub: ≤ 约束右端
        minimize: 是否最小化
    
    返回:
        x_opt, z_opt
    """
    n = len(c)
    m = len(b_ub)
    
    # 转标准型: 添加松弛变量
    A_std = np.zeros((m, n + m))
    A_std[:, :n] = A_ub
    for i in range(m):
        A_std[i, n + i] = 1.0  # 松弛变量
    
    c_std = np.zeros(n + m)
    if minimize:
        c_std[:n] = [-ci for ci in c]  # 取负
    else:
        c_std[:n] = c
    
    b_std = np.array(b_ub, dtype=float)
    
    # 确保b >= 0
    for i in range(m):
        if b_std[i] < 0:
            A_std[i] = -A_std[i]
            b_std[i] = -b_std[i]
    
    solver = SimplexSolver(c_std, A_std, b_std)
    x_opt, z_opt = solver.solve()
    
    if x_opt is not None:
        # 只取原始变量部分
        x_orig = x_opt[:n]
        if minimize:
            z_opt = -z_opt
        return x_orig, z_opt
    return None, None


# ===== 测试示例 =====
if __name__ == '__main__':
    # 奶茶店问题
    print("=" * 60)
    print("奶茶店利润最大化问题")
    print("=" * 60)
    
    c = [5, 8]  # 利润系数
    A_ub = [[2, 1],  # 奶
            [1, 2],  # 茶
            [1, 0],  # 珍珠
            [0, 2]]  # 芒果
    b_ub = [20, 15, 10, 12]
    
    x_opt, z_opt = solve_lp_from_inequality(c, A_ub, b_ub, minimize=False)
    if x_opt is not None:
        print(f"\n最优解: x₁ = {x_opt[0]:.4f}, x₂ = {x_opt[1]:.4f}")
        print(f"最大利润: z = {z_opt:.4f}")
    else:
        print("问题无界或无可行解")
    
    # 验证: 用scipy求解
    print("\n--- 用scipy验证 ---")
    from scipy.optimize import linprog
    result = linprog([-5, -8], A_ub=A_ub, b_ub=b_ub, bounds=[(0, None), (0, None)])
    print(f"scipy结果: x = {result.x}, z = {-result.fun}")
```

### 2.6 C++ 完整实现

```cpp
/**
 * 单纯形算法 C++ 完整实现
 * 编译: g++ -std=c++17 -O2 simplex.cpp -o simplex
 */
#include <iostream>
#include <vector>
#include <cmath>
#include <iomanip>
#include <limits>
#include <cassert>

using namespace std;
using Matrix = vector<vector<double>>;
using Vector = vector<double>;

const double EPS = 1e-10;  // 浮点容差

class SimplexSolver {
private:
    int m, n;           // m=约束数, n=变量数(含松弛变量)
    Matrix A;           // 约束矩阵
    Vector b;           // 右端向量
    Vector c_orig;      // 原始目标系数
    vector<int> basic;  // 基本变量索引
    vector<int> nonbasic; // 非基本变量索引

public:
    SimplexSolver(const Vector& c, const Matrix& A_in, const Vector& b_in)
        : A(A_in), b(b_in), c_orig(c) {
        m = A_in.size();
        n = A_in[0].size();
        // 初始基本变量 = 松弛变量(后m个)
        basic.resize(m);
        nonbasic.resize(n - m);
        for (int i = 0; i < m; i++) basic[i] = n - m + i;
        for (int j = 0; j < n - m; j++) nonbasic[j] = j;
    }

    // 获取z行中变量xⱼ的系数
    double getZCoeff(int j) const {
        double coeff = c_orig[j];
        for (int i = 0; i < m; i++) {
            coeff -= c_orig[basic[i]] * A[i][j];
        }
        return coeff;
    }

    // 执行旋转操作
    void pivot(int entering_idx, int leaving_row) {
        int j = nonbasic[entering_idx];  // 入基变量的列索引
        int i = leaving_row;              // 出基变量的行
        
        double pivot_val = A[i][j];
        assert(fabs(pivot_val) > EPS);
        
        // 步骤1: 归一化主元行
        for (int col = 0; col < n; col++) {
            A[i][col] /= pivot_val;
        }
        b[i] /= pivot_val;
        
        // 步骤2: 消去其他行中的第j列元素
        for (int k = 0; k < m; k++) {
            if (k == i) continue;
            double factor = A[k][j];
            if (fabs(factor) > EPS) {
                for (int col = 0; col < n; col++) {
                    A[k][col] -= factor * A[i][col];
                }
                b[k] -= factor * b[i];
            }
        }
        
        // 步骤3: 交换基本/非基本变量
        swap(basic[i], nonbasic[entering_idx]);
    }

    // 求解
    pair<Vector, double> solve(int max_iter = 1000) {
        for (int iter = 0; iter < max_iter; iter++) {
            // 步骤1: 选择入基变量 (Bland规则: 选最小下标的正系数变量)
            int entering_idx = -1;
            for (int idx = 0; idx < (int)nonbasic.size(); idx++) {
                double coeff = getZCoeff(nonbasic[idx]);
                if (coeff > EPS) {
                    entering_idx = idx;
                    break;  // Bland规则: 选第一个
                }
            }
            
            if (entering_idx == -1) break;  // 最优
            
            int j = nonbasic[entering_idx];
            
            // 步骤2: 确定出基变量 (最小比值测试)
            int leaving_row = -1;
            double min_theta = numeric_limits<double>::max();
            for (int i = 0; i < m; i++) {
                if (A[i][j] > EPS) {
                    double theta = b[i] / A[i][j];
                    if (theta < min_theta - EPS) {
                        min_theta = theta;
                        leaving_row = i;
                    }
                }
            }
            
            if (leaving_row == -1) {
                cout << "问题无界!" << endl;
                return {{}, numeric_limits<double>::infinity()};
            }
            
            // 步骤3: 旋转
            pivot(entering_idx, leaving_row);
        }
        
        // 构造解向量
        Vector x(n, 0.0);
        for (int i = 0; i < m; i++) {
            x[basic[i]] = b[i];
        }
        
        double z = 0.0;
        for (int j = 0; j < n; j++) {
            z += c_orig[j] * x[j];
        }
        
        return {x, z};
    }
    
    // 打印当前单纯形表
    void printTableau(int iter) const {
        cout << "\n===== 迭代 " << iter << " =====" << endl;
        cout << "基本变量: ";
        for (int bi : basic) cout << bi << " ";
        cout << endl;
        cout << "非基本变量: ";
        for (int nj : nonbasic) cout << nj << " ";
        cout << endl;
        cout << "b = [";
        for (int i = 0; i < m; i++) {
            cout << fixed << setprecision(4) << b[i];
            if (i < m - 1) cout << ", ";
        }
        cout << "]" << endl;
    }
};

// 从不等式形式直接求解
pair<Vector, double> solveLP(
    const Vector& c,       // 目标系数
    const Matrix& A_ub,    // ≤约束矩阵
    const Vector& b_ub,    // ≤约束右端
    bool minimize = false
) {
    int n = c.size();       // 原始变量个数
    int m = b_ub.size();    // 约束个数
    
    // 构建标准型: 添加松弛变量
    Matrix A_std(m, Vector(n + m, 0.0));
    Vector c_std(n + m, 0.0);
    Vector b_std = b_ub;
    
    for (int i = 0; i < m; i++) {
        for (int j = 0; j < n; j++) {
            A_std[i][j] = A_ub[i][j];
        }
        A_std[i][n + i] = 1.0;  // 松弛变量
    }
    
    for (int j = 0; j < n; j++) {
        c_std[j] = minimize ? -c[j] : c[j];
    }
    
    // 确保b >= 0
    for (int i = 0; i < m; i++) {
        if (b_std[i] < -EPS) {
            for (int j = 0; j < n + m; j++) A_std[i][j] = -A_std[i][j];
            b_std[i] = -b_std[i];
        }
    }
    
    SimplexSolver solver(c_std, A_std, b_std);
    auto [x_opt, z_opt] = solver.solve();
    
    if (!x_opt.empty()) {
        // 只取原始变量部分
        Vector x_orig(x_opt.begin(), x_opt.begin() + n);
        double z = minimize ? -z_opt : z_opt;
        return {x_orig, z};
    }
    return {{}, 0};
}

int main() {
    cout << "========================================" << endl;
    cout << "  奶茶店利润最大化问题" << endl;
    cout << "========================================" << endl;
    
    Vector c = {5, 8};  // 利润系数
    Matrix A_ub = {
        {2, 1},   // 奶约束
        {1, 2},   // 茶约束
        {1, 0},   // 珍珠约束
        {0, 2}    // 芒果约束
    };
    Vector b_ub = {20, 15, 10, 12};
    
    auto [x_opt, z_opt] = solveLP(c, A_ub, b_ub, false);
    
    if (!x_opt.empty()) {
        cout << "\n最优解:" << endl;
        for (int j = 0; j < (int)x_opt.size(); j++) {
            cout << "  x" << j+1 << " = " 
                 << fixed << setprecision(4) << x_opt[j] << endl;
        }
        cout << "最大利润: z = " << z_opt << endl;
    }
    
    return 0;
}
```

### 2.7 复杂度分析

| 指标 | 分析 |
|------|------|
| **最坏情况时间复杂度** | O(2^n) — 可能遍历所有顶点 |
| **平均情况** | O(m) 到 O(m²) 次旋转 — 实际中非常高效 |
| **空间复杂度** | O(mn) — 存储单纯形表 |

> **FAQ：既然最坏是指数级，为什么还用单纯形算法？**
>
> 1. **实际表现极佳**：在绝大多数实际问题中，单纯形算法只需要 O(m) 到 O(m²) 次旋转就能找到最优解，远好于理论最坏情况
> 2. **对偶单纯形**：对于微小变化的问题（如灵敏度分析），可以从上次最优解快速重启
> 3. **多项式时间替代方案**：内点法（Interior Point Method）可以在多项式时间内求解线性规划，但常数因子较大，对于中小规模问题反而更慢
> 4. **经典性**：单纯形算法是理解线性规划的基础，即使实际工程中用内点法，理解单纯形也是必要的

---

## 3️⃣ 单纯形算法的正确性（29.3 Correctness of the Simplex Algorithm）

### 3.1 需要证明什么？

单纯形算法的正确性需要证明三个关键性质：

```
1. 如果算法返回一个解，这个解是最优的
   → 最优性条件：z行所有非基本变量系数 ≤ 0

2. 如果算法说"无界"，问题确实无界
   → 无界性条件：某个入基变量在所有约束中的系数 ≤ 0

3. 算法一定能终止（不会无限循环）
   → 退化与循环问题
```

### 3.2 最优性证明

**定理**：如果在松弛型中，z行的所有非基本变量系数都 ≤ 0，则当前基本可行解是最优的。

**证明思路**：

```
设当前松弛型为：
  z  = v + c̄₁x₁ + c̄₂x₂ + ... + c̄ₖxₖ    (x₁..xₖ是非基本变量)
  xᵢ = bᵢ - ...                            (xᵢ是基本变量)

条件: c̄ⱼ ≤ 0 对所有 j

因为 xⱼ ≥ 0（非基本变量非负）且 c̄ⱼ ≤ 0，
所以 c̄ⱼxⱼ ≤ 0 对所有 j，
所以 z = v + Σ c̄ⱼxⱼ ≤ v

当 xⱼ = 0 对所有 j 时，z = v，这是z能达到的最大值。

而 xⱼ = 0 对所有 j 恰好对应当前基本可行解，
所以当前基本可行解是最优的！ □
```

**生活类比**：你站在山顶，看四周都是下坡路（所有方向的目标函数变化都是负的），那你就在山顶了！

### 3.3 无界性证明

**定理**：如果某个非基本变量 xⱼ 在z行中的系数 c̄ⱼ > 0，且在所有基本变量的等式中 xⱼ 的系数 ≤ 0，则问题无界。

**证明思路**：

```
设 xⱼ 是入基变量，c̄ⱼ > 0（增大xⱼ能增加z）

在所有基本变量等式中：
  xᵢ = bᵢ - aᵢⱼxⱼ - ... 

如果 aᵢⱼ ≤ 0 对所有 i：
  增大 xⱼ → xᵢ = bᵢ - aᵢⱼxⱼ - ... ≥ bᵢ ≥ 0
  （因为 -aᵢⱼ ≥ 0，所以 xᵢ 不会变负）

  所以 xⱼ 可以无限增大！
  而 z = v + c̄ⱼxⱼ + ... ，c̄ⱼ > 0
  所以 z → ∞，问题无界！ □
```

**生活类比**：你想往一个方向走，但那个方向没有任何围栏挡着你——你可以无限走下去，利润无限增长。

### 3.4 退化与循环问题

**退化（Degeneracy）**：某个基本变量 = 0。

```
退化情况：
  z  = v + c̄ⱼxⱼ
  x₁ = 0 - a₁ⱼxⱼ - ...     ← b₁ = 0，x₁退化了！

θ₁ = 0/|a₁ⱼ| = 0

这意味着旋转后，xⱼ变成基本变量，但xⱼ的值仍然是0。
目标函数值不变！
```

**循环（Cycling）**：退化可能导致算法在多个基本可行解之间循环，目标函数值不变。

```
循环示意：
  基可行解A → 旋转 → 基可行解B → 旋转 → 基可行解C → 旋转 → 基可行解A
  (z相同)      (z不变)     (z不变)      (z不变)     (z不变)

虽然z没变，但基本变量的组合在循环！
```

**Bland规则**——避免循环的简单方法：

```
Bland规则（最小下标规则）：
  1. 入基变量: 选z行中正系数最小的下标（不是最大系数！）
  2. 出基变量: 在最小比值测试中，如果有平局，选下标最小的

定理: 使用Bland规则，单纯形算法一定终止（不会循环）。

证明思路: 反证法。如果循环，则目标函数值不变，
每一次旋转都是退化旋转（θ=0）。
Bland规则保证了在退化旋转中，入基和出基变量的选择是唯一的，
从而不会回到之前的基本可行解。 □
```

> **FAQ：退化常见吗？需要担心循环吗？**
>
> 退化在实际问题中**非常常见**，尤其是在约束条件很多、可行域的顶点"挤"在一起的时候。但循环**非常罕见**——理论上可能，实际上几乎不会发生。很多实现甚至不处理循环，因为Bland规则虽然保证了终止，但可能让算法走更多步（因为不选最大系数方向）。实践中常用的策略是"默认不用Bland规则，如果检测到循环再切换"。

### 3.5 正确性证明的核心逻辑链

```
┌────────────────────────────────────────────────────────┐
│              单纯形算法正确性证明链                       │
├────────────────────────────────────────────────────────┤
│                                                        │
│  1. 每个基可行解 ↔ 可行域的一个顶点                      │
│     （线性代数 + 凸分析）                                │
│                                                        │
│  2. 顶点个数有限 ≤ C(n+m, m)                           │
│     （组合数学）                                        │
│                                                        │
│  3. 如果不退化: 每次旋转z严格增加                       │
│     → 不可能回到同一个顶点                               │
│     → 最多C(n+m,m)次旋转 → 一定终止                     │
│                                                        │
│  4. 如果退化: 用Bland规则                               │
│     → 也不会循环 → 一定终止                             │
│                                                        │
│  5. 终止时:                                            │
│     a) 所有c̄ⱼ ≤ 0 → 当前解最优（最优性证明）           │
│     b) 某c̄ⱼ>0且所有aᵢⱼ≤0 → 问题无界（无界性证明）     │
│                                                        │
│  结论: 单纯形算法是正确的，且一定终止！                   │
└────────────────────────────────────────────────────────┘
```

---

## 4️⃣ 对偶性（29.4 Duality）

### 4.1 对偶问题的直觉

**生活中的类比**：你是一个**工厂老板**，也是**原料供应商**。

- **原问题（老板视角）**：我有原料，要生产产品赚钱。每种产品需要不同原料，我要决定生产多少每种产品，使利润最大。
  - 变量：各产品的产量
  - 目标：利润最大化

- **对偶问题（供应商视角）**：我要给原料定价。每种原料的价格要足够高让老板愿意卖给我（而不是自己生产），同时我的总采购成本要尽可能低。
  - 变量：各原料的价格
  - 目标：采购成本最小化

**核心洞察**：老板的最大利润 = 供应商的最低采购成本。这就是**强对偶定理**！

```
老板的利润（最大化）     =     供应商的成本（最小化）
      ↑                            ↑
   原问题                      对偶问题
   
  max cᵀx                    min bᵀy
  s.t. Ax ≤ b                s.t. Aᵀy ≥ c
       x ≥ 0                      y ≥ 0
```

### 4.2 对偶问题的构造

**原问题（Primal）**：

```
最大化  cᵀx = c₁x₁ + c₂x₂ + ... + cₙxₙ
约束：  Ax ≤ b  (m个不等式)
       x ≥ 0   (n个非负约束)
```

**对偶问题（Dual）**：

```
最小化  bᵀy = b₁y₁ + b₂y₂ + ... + bₘyₘ
约束：  Aᵀy ≥ c  (n个不等式)
       y ≥ 0   (m个非负约束)
```

**构造规则表**：

| 原问题 | 对偶问题 |
|--------|---------|
| 最大化 | 最小化 |
| n个变量 | n个约束 |
| m个约束 | m个变量 |
| 第i个约束右端 bᵢ | 第i个对偶变量 yᵢ 的目标系数 |
| 第j个目标系数 cⱼ | 第j个对偶约束右端 cⱼ |
| 约束矩阵 A | 约束矩阵 Aᵀ（转置） |
| ≤ 约束 | yᵢ ≥ 0 |
| xⱼ ≥ 0 | ≥ 约束 |

**具体例子**：

```
原问题：
  最大化  5x₁ + 8x₂
  约束:   2x₁ + x₂ ≤ 20    ← y₁
          x₁ + 2x₂ ≤ 15    ← y₂
          x₁       ≤ 10    ← y₃
               2x₂ ≤ 12    ← y₄
          x₁, x₂ ≥ 0

对偶问题：
  最小化  20y₁ + 15y₂ + 10y₃ + 12y₄
  约束:   2y₁ + y₂ + y₃       ≥ 5    ← x₁对应的约束
          y₁ + 2y₂ +    + 2y₄ ≥ 8    ← x₂对应的约束
          y₁, y₂, y₃, y₄ ≥ 0

解释：
  yᵢ 是"第i种原料的影子价格"（shadow price）
  对偶约束2y₁+y₂+y₃ ≥ 5 说的是：
    生产1单位x₁消耗的各原料按影子价格计算的"价值" ≥ x₁的利润5
    → 如果 < 5，老板宁愿生产而不是卖原料
    → 均衡时 = 5（强对偶）
```

**图解原问题与对偶问题的关系**：

```
原问题 (Primal)                   对偶问题 (Dual)
┌──────────────────┐              ┌──────────────────┐
│  最大化 cᵀx       │              │  最小化 bᵀy       │
│  约束: Ax ≤ b     │  ←──转置──→  │  约束: Aᵀy ≥ c   │
│       x ≥ 0      │              │       y ≥ 0      │
│                  │              │                  │
│  n个变量, m个约束  │              │  m个变量, n个约束  │
│  可行域: x空间     │              │  可行域: y空间     │
└──────────────────┘              └──────────────────┘
        │                                │
        │     弱对偶: cᵀx ≤ bᵀy          │
        │     强对偶: cᵀx* = bᵀy*        │
        └────────────────────────────────┘
```

### 4.3 弱对偶定理

**定理**：如果 x 是原问题的可行解，y 是对偶问题的可行解，则 cᵀx ≤ bᵀy。

**证明**：

```
因为 x 可行: Ax ≤ b, x ≥ 0
因为 y 可行: Aᵀy ≥ c, y ≥ 0

从 Aᵀy ≥ c:
  两边左乘 xᵀ (x ≥ 0, 不改变不等号方向):
  xᵀAᵀy ≥ xᵀc
  (Ax)ᵀy ≥ cᵀx     ← 利用 (AB)ᵀ = BᵀAᵀ
  (Ax)ᵀy = yᵀ(Ax)   ← 转置的转置

从 Ax ≤ b 和 y ≥ 0:
  yᵀ(Ax) ≤ yᵀb      ← y的每个分量≥0, 乘以≤的不等式
  即 (Ax)ᵀy ≤ bᵀy

所以: cᵀx ≤ yᵀ(Ax) = (Ax)ᵀy ≤ bᵀy

即 cᵀx ≤ bᵀy  □
```

**生活类比**：老板的利润 ≤ 供应商的采购成本。这是因为供应商出的价必须足够高，才能"劝退"老板不自己生产——如果供应商出价低于老板的利润，老板就不会卖原料。

### 4.4 强对偶定理

**定理**：如果原问题有最优解 x*，则对偶问题也有最优解 y*，且 cᵀx* = bᵀy*。

**这是线性规划最重要的定理之一！**

```
强对偶的含义：

  原问题最优值 = 对偶问题最优值
  
  max cᵀx = min bᵀy
     (在可行域上)   (在可行域上)

这意味着：
  1. 如果你解了原问题，就自动得到了对偶问题的最优值
  2. 对偶问题的最优解 y* 就是原问题约束的"影子价格"
  3. 对偶性可以用来验证解的正确性
```

**强对偶的证明思路**（利用单纯形算法）：

```
假设单纯形算法对原问题终止，得到最优松弛型：

  z  = v + Σ c̄ⱼxⱼ      (c̄ⱼ ≤ 0 对所有j，最优性条件)
  xᵢ = bᵢ - Σ aᵢⱼxⱼ

令 N = 非基本变量集合, B = 基本变量集合

构造对偶最优解：
  yᵢ* = -c̄_{B(i)}      (基本变量在z行中的系数取负)

需要验证:
  1. y* 是对偶可行解: Aᵀy* ≥ c
  2. bᵀy* = v = cᵀx*    (对偶目标值 = 原问题目标值)

这利用了单纯形算法的最终表的特殊结构来构造证明。
```

### 4.5 互补松弛性

**定理**：如果 x* 和 y* 分别是原问题和对偶问题的最优解，则：

```
互补松弛条件：
  1. xⱼ* > 0  ⟹  (Aᵀy*)ⱼ = cⱼ   （如果原始变量严格为正，对应对偶约束紧）
  2. (Aᵀy*)ⱼ > cⱼ  ⟹  xⱼ* = 0    （如果对偶约束松弛，对应原始变量为0）
  3. yᵢ* > 0  ⟹  (Ax*)ᵢ = bᵢ     （如果对偶变量严格为正，对应原始约束紧）
  4. (Ax*)ᵢ < bᵢ  ⟹  yᵢ* = 0     （如果原始约束松弛，对应对偶变量为0）
```

**生活类比**：互补松弛就像"跷跷板"——
- 如果某种产品在最优方案中生产了（xⱼ > 0），那它对应的原料定价恰好等于利润（对偶约束紧）——老板和供应商在这点上"扯平"
- 如果某种原料在最优方案中有剩余（约束松弛），那它的影子价格就是0——既然用不完，就不值钱

### 4.6 对偶性的应用

**应用1：验证最优性**

```
如果你找到了原问题的可行解 x 和对偶问题的可行解 y，
且 cᵀx = bᵀy，那么 x 和 y 都是最优解！

这就像"双向夹击"——如果你从两个方向逼近，碰头了，那就到达了。
```

**应用2：灵敏度分析**

```
对偶变量 yᵢ* 就是"影子价格"——第i个约束右端 bᵢ 增加1个单位，
目标函数值增加多少。

例如：在奶茶店问题中，如果 y₁* = 2，
意味着"奶的库存增加1份，利润可以增加2元"。
这可以帮助你决定：是否值得花1.5元买额外的奶？
（如果1.5元 < 2元，值得！）
```

**应用3：EDA中的资源分配**

```
在芯片设计中：
  原问题: 给定面积/功耗约束，最大化性能
  对偶问题: 给定性能需求，最小化面积/功耗
  
  影子价格告诉你：
  - 哪个约束最"紧"（增加面积预算对性能提升最大）
  - 哪些约束可以放松（放松后对性能几乎没影响）
```

### 4.7 对偶性Python代码示例

```python
"""
对偶性验证：原问题与对偶问题的关系
"""
import numpy as np
from scipy.optimize import linprog

def verify_duality(c, A_ub, b_ub):
    """
    同时求解原问题和对偶问题，验证强对偶定理
    
    参数:
        c: 原问题目标系数 (max c^T x)
        A_ub: 原问题约束矩阵 (Ax <= b)
        b_ub: 原问题约束右端
    """
    n = len(c)  # 原问题变量个数
    m = len(b_ub)  # 原问题约束个数
    
    # ===== 求解原问题: max c^T x, Ax <= b, x >= 0 =====
    # scipy.optimize.linprog 做的是 min, 所以取负
    primal_result = linprog(-np.array(c), A_ub=A_ub, b_ub=b_ub,
                           bounds=[(0, None)] * n)
    
    if not primal_result.success:
        print(f"原问题求解失败: {primal_result.message}")
        return
    
    primal_opt = -primal_result.fun  # 最大化 → 取负还原
    x_opt = primal_result.x
    
    # ===== 构造对偶问题: min b^T y, A^T y >= c, y >= 0 =====
    # A^T y >= c 等价于 -A^T y <= -c
    dual_c = np.array(b_ub)          # 对偶目标系数 = b
    dual_A_ub = -np.array(A_ub).T    # 对偶约束矩阵 = -A^T
    dual_b_ub = -np.array(c)         # 对偶约束右端 = -c
    
    dual_result = linprog(dual_c, A_ub=dual_A_ub, b_ub=dual_b_ub,
                         bounds=[(0, None)] * m)
    
    if not dual_result.success:
        print(f"对偶问题求解失败: {dual_result.message}")
        return
    
    dual_opt = dual_result.fun       # 对偶是最小化，直接取
    y_opt = dual_result.x
    
    # ===== 验证强对偶定理 =====
    print("=" * 60)
    print("  对偶性验证")
    print("=" * 60)
    print(f"\n原问题最优值: {primal_opt:.6f}")
    print(f"对偶问题最优值: {dual_opt:.6f}")
    print(f"差值: {abs(primal_opt - dual_opt):.10f}")
    
    if abs(primal_opt - dual_opt) < 1e-6:
        print("✓ 强对偶定理验证通过！原问题最优值 = 对偶问题最优值")
    else:
        print("✗ 强对偶定理验证失败！")
    
    # ===== 验证互补松弛性 =====
    print("\n--- 互补松弛性验证 ---")
    Ax = np.array(A_ub) @ x_opt
    
    for j in range(n):
        Aty_j = np.array(A_ub)[:, j] @ y_opt  # A^T y 的第j个分量
        if x_opt[j] > 1e-8 and abs(Aty_j - c[j]) > 1e-6:
            print(f"✗ 变量 x{j}: x{j}={x_opt[j]:.4f}>0 但 A^Ty的第{j}分量={Aty_j:.4f}≠c{j}={c[j]}")
    
    for i in range(m):
        if y_opt[i] > 1e-8 and abs(Ax[i] - b_ub[i]) > 1e-6:
            print(f"✗ 约束 {i}: y{i}={y_opt[i]:.4f}>0 但 Ax的第{i}分量={Ax[i]:.4f}≠b{i}={b_ub[i]}")
    
    print("互补松弛性验证完成")
    
    # ===== 影子价格 =====
    print("\n--- 影子价格（对偶变量）---")
    for i in range(m):
        print(f"  约束{i+1} (b={b_ub[i]}): 影子价格 y{i} = {y_opt[i]:.4f}")
        if y_opt[i] > 1e-8:
            print(f"    → 增加b{i+1}一个单位，目标函数可增加 {y_opt[i]:.4f}")
    
    return x_opt, y_opt, primal_opt, dual_opt


# ===== 测试 =====
if __name__ == '__main__':
    # 奶茶店问题
    c = [5, 8]
    A_ub = [[2, 1], [1, 2], [1, 0], [0, 2]]
    b_ub = [20, 15, 10, 12]
    
    verify_duality(c, A_ub, b_ub)
```

### 4.8 各种形式线性规划的对偶

**一般对偶对照表**：

| 原问题 (max) | 对偶问题 (min) |
|-------------|---------------|
| 第i个约束是 ≤ | yᵢ ≥ 0 |
| 第i个约束是 ≥ | yᵢ ≤ 0 |
| 第i个约束是 = | yᵢ 无约束 |
| xⱼ ≥ 0 | 第j个约束是 ≥ |
| xⱼ ≤ 0 | 第j个约束是 ≤ |
| xⱼ 无约束 | 第j个约束是 = |

**记忆口诀**："max对应min，≤对应≥，等号对应无约束"

> **FAQ：对偶的对偶是什么？**
>
> 对偶的对偶就是原问题！如果你对对偶问题再求对偶，就回到了原问题。这就像"双重否定表肯定"——"不想不赚钱" = "想赚钱"。这叫做**自对偶性**（self-duality of the duality operation）。

---

## 5️⃣ 初始基本可行解（29.5 Initial Basic Feasible Solution）

### 5.1 问题描述

单纯形算法需要一个**初始基本可行解**作为起点。但问题是：

```
如果标准型中 b ≥ 0 且初始松弛变量可以作为基本变量，
那初始基本可行解自动得到（令所有原始变量 = 0）。

但如果：
  1. 某个 bᵢ < 0 → 令松弛变量 = bᵢ < 0，不可行！
  2. 等式约束中没有现成的松弛变量 → 需要额外处理
```

**生活类比**：单纯形算法就像GPS导航，你需要一个出发点。如果出发点本身不在路上（不可行），GPS就没法开始导航。初始基本可行解的问题就是"怎么找到一个合法的出发点"。

### 5.2 两阶段法（Two-Phase Method）

**核心思想**：先用一个辅助线性规划找到一个基本可行解，再用单纯形算法求解原问题。

```
两阶段法流程：

第一阶段：找初始基本可行解
  ┌──────────────────────────────────────┐
  │ 构造辅助问题:                         │
  │   最小化  x₀ (一个人工变量)            │
  │   约束: 原始约束 + x₀的调整           │
  │                                       │
  │ 如果辅助问题最优值 = 0:                │
  │   → 找到了原问题的基本可行解           │
  │ 如果辅助问题最优值 > 0:                │
  │   → 原问题无可行解！                   │
  └──────────────────────────────────────┘
                    │
                    ▼ (如果最优值 = 0)
第二阶段：解原问题
  ┌──────────────────────────────────────┐
  │ 从第一阶段的最终基本可行解出发           │
  │ 用单纯形算法求解原问题                  │
  └──────────────────────────────────────┘
```

### 5.3 辅助问题的构造

**步骤**：

```
原问题（标准型）：
  最大化  cᵀx
  约束:   Ax = b    （可能有 bᵢ < 0）
          x ≥ 0

步骤1: 确保b ≥ 0
  对 bᵢ < 0 的行，整行乘以-1：
    aᵢⱼ → -aᵢⱼ, bᵢ → -bᵢ

  （现在所有 bᵢ ≥ 0）

步骤2: 添加人工变量 x₀
  对每个约束 i，添加人工变量 x₀：
    Σⱼ aᵢⱼxⱼ + x₀ = bᵢ    （对每个i）

  现在令 x₀ = min(bᵢ) > 0, xⱼ = 0 对所有 j
  → 这就是一个基本可行解（基本变量 = {x₀}）

步骤3: 构造辅助问题
  最小化  x₀
  约束:   Σⱼ aᵢⱼxⱼ + x₀ = bᵢ  （对每个i）
          x₀, xⱼ ≥ 0

  这个问题一定有可行解（x₀ = max(bᵢ) 即可）
  且 x₀ ≥ 0，所以有下界，一定有最优解

步骤4: 用单纯形算法求解辅助问题
  如果最优值 x₀* = 0 → 原问题有可行解，当前基本可行解就是初始点
  如果最优值 x₀* > 0 → 原问题无可行解
```

**完整例子**：

```
原问题：
  最大化  2x₁ + 3x₂
  约束:   x₁ + 2x₂ ≤ 4
          x₁ + x₂ = 3       ← 等式约束，没有松弛变量！
          x₁, x₂ ≥ 0

转标准型（第1个约束加松弛变量s₁）：
  最大化  2x₁ + 3x₂
  约束:   x₁ + 2x₂ + s₁ = 4
          x₁ + x₂       = 3   ← 没有现成的基本变量

构造辅助问题（添加人工变量x₀到第2行）：
  最小化  x₀
  约束:   x₁ + 2x₂ + s₁     = 4    ← s₁是基本变量
          x₁ + x₂       + x₀ = 3    ← x₀是基本变量
          x₁, x₂, s₁, x₀ ≥ 0

初始基本可行解: s₁ = 4, x₀ = 3, x₁ = x₂ = 0
目标: 最小化 x₀ → 希望 x₀ → 0

用单纯形算法求解（最小化，选负系数入基...）

如果最终 x₀ = 0 → 成功找到初始基本可行解
然后进入第二阶段，解原问题
```

**图解两阶段法**：

```
                    原问题
                      │
                      ▼
            ┌─────────────────┐
            │  b 有负元素?     │──否──→ 直接用松弛变量作初始BFS
            └────────┬────────┘
                     │是
                     ▼
            ┌─────────────────┐
            │ 整行乘-1使b≥0    │
            └────────┬────────┘
                     │
                     ▼
            ┌─────────────────┐
            │ 有等式约束?      │──否──→ 松弛变量足够作初始BFS
            └────────┬────────┘
                     │是
                     ▼
         ┌──────────────────────────┐
         │  第一阶段: 构造辅助问题     │
         │  min x₀                   │
         │  s.t. Ax + x₀ = b, x≥0   │
         └────────────┬─────────────┘
                      │
                      ▼
         ┌──────────────────────────┐
         │  x₀* = 0 ?               │
         │  是 → 得到初始BFS         │──→ 第二阶段: 解原问题
         │  否 → 原问题无可行解       │──→ 报告"不可行"
         └──────────────────────────┘
```

### 5.4 大M法（替代方案）

**大M法**是两阶段法的替代方案：把人工变量直接加入原问题的目标函数，但给一个巨大的惩罚系数 M。

```
原问题: max cᵀx, Ax = b, x ≥ 0

大M法: max cᵀx - M·Σᵢ x₀ᵢ    （M是一个非常大的正数）
       s.t. Ax + x₀ = b, x, x₀ ≥ 0

如果原问题有可行解，最优解中所有 x₀ᵢ = 0
（因为 M 很大，任何 x₀ᵢ > 0 都会严重损害目标函数）

如果最优解中某个 x₀ᵢ > 0，说明原问题无可行解
```

**生活类比**：大M法就像"高额违约金"——你签了一个合同，如果违约（x₀ > 0）就要付巨额罚款（-M·x₀）。所以你会尽量不违约（x₀ = 0），实在不行（无可行解）就只能付罚款了。

> **FAQ：两阶段法和大M法哪个好？**
>
> | 对比 | 两阶段法 | 大M法 |
> |------|---------|-------|
> | 数值稳定性 | 更好 | M太大可能导致浮点问题 |
> | 实现复杂度 | 需要两次单纯形 | 只需一次 |
> | 理论清晰度 | 清晰 | 需要选M，不太严格 |
> | 工程实践 | 更常用 | 简单场景可用 |
> 
> 总体而言，**两阶段法更稳健**，是教科书和严肃实现的首选。

### 5.5 退化的初始基本可行解

```
问题: 第一阶段最优值 x₀* = 0，但 x₀ 仍然在基本变量集合中！

这意味着: x₀是退化的基本变量（值为0但仍在基中）

解决: 需要做额外的旋转，把 x₀ 从基本变量中"赶出去"

  1. 如果 x₀ 所在行有某个非基本变量 xⱼ 的系数 aⱼ ≠ 0:
     → 做一次旋转，让 xⱼ 入基，x₀ 出基
  2. 如果 x₀ 所在行所有非基本变量系数都是0:
     → 说明这一行是冗余的（可以被其他行线性表出）
     → 直接删除这一行

这就是"去退化"的过程。
```

### 5.6 Python实现：两阶段法

```python
"""
两阶段单纯形算法
"""
import numpy as np
from scipy.optimize import linprog

def two_phase_simplex(c, A_ub=None, b_ub=None, A_eq=None, b_eq=None):
    """
    两阶段单纯形法求解线性规划
    
    参数:
        c: 目标系数 (maximize)
        A_ub, b_ub: ≤约束 (可选)
        A_eq, b_eq: 等式约束 (可选)
    
    返回:
        x_opt: 最优解
        z_opt: 最优值
        status: 'optimal', 'infeasible', 'unbounded'
    """
    # 使用scipy求解作为参考实现
    # scipy做minimization, 所以取负
    result = linprog(
        -np.array(c, dtype=float),
        A_ub=np.array(A_ub, dtype=float) if A_ub is not None else None,
        b_ub=np.array(b_ub, dtype=float) if b_ub is not None else None,
        A_eq=np.array(A_eq, dtype=float) if A_eq is not None else None,
        b_eq=np.array(b_eq, dtype=float) if b_eq is not None else None,
        bounds=[(0, None)] * len(c),
        method='highs'
    )
    
    if result.success:
        return result.x, -result.fun, 'optimal'
    elif 'infeasible' in result.message.lower():
        return None, None, 'infeasible'
    elif 'unbounded' in result.message.lower():
        return None, float('inf'), 'unbounded'
    else:
        return None, None, 'unknown'

def two_phase_manual(c_orig, A_std, b_std):
    """
    手动两阶段法实现（教学版）
    
    参数:
        c_orig: 原始目标系数
        A_std: 标准型约束矩阵 Ax = b
        b_std: 标准型右端向量
    
    返回:
        x_opt, z_opt, status
    """
    m, n = A_std.shape
    
    # 确保b >= 0
    for i in range(m):
        if b_std[i] < 0:
            A_std[i] = -A_std[i]
            b_std[i] = -b_std[i]
    
    # ===== 第一阶段: 辅助问题 =====
    # min x₀
    # s.t. Ax + x₀·1 = b, x, x₀ ≥ 0
    
    # 判断是否需要人工变量
    # 如果已经有m个单位列（可以作初始基），就不需要
    
    # 检查哪些行需要人工变量
    need_artificial = []
    identity_cols = set()
    for i in range(m):
        # 找一个单位列，第i个元素为1，其余为0
        found = False
        for j in range(n):
            if abs(A_std[i, j] - 1.0) < 1e-10:
                all_zero = True
                for k in range(m):
                    if k != i and abs(A_std[k, j]) > 1e-10:
                        all_zero = False
                        break
                if all_zero and j not in identity_cols:
                    identity_cols.add(j)
                    found = True
                    break
        if not found:
            need_artificial.append(i)
    
    if len(need_artificial) == 0:
        # 不需要第一阶段，直接有初始BFS
        print("已有初始基本可行解，跳过第一阶段")
        # 直接用单纯形算法
        from scipy.optimize import linprog
        result = linprog(-c_orig, A_eq=A_std, b_eq=b_std,
                        bounds=[(0, None)] * n)
        if result.success:
            return result.x, -result.fun, 'optimal'
        return None, None, 'failed'
    
    print(f"需要人工变量的行: {need_artificial}")
    print("执行第一阶段...")
    
    # 构造辅助问题
    n_aux = len(need_artificial)  # 人工变量个数
    n_total = n + n_aux
    
    A_aux = np.zeros((m, n_total))
    A_aux[:, :n] = A_std
    for idx, i in enumerate(need_artificial):
        A_aux[i, n + idx] = 1.0  # 人工变量
    
    # 辅助目标: min Σ x₀ᵢ
    c_aux = np.zeros(n_total)
    for idx in range(n_aux):
        c_aux[n + idx] = 1.0
    
    # 初始基本可行解: 人工变量 = b[need_artificial]
    basic = list(range(n, n_total))  # 人工变量先作为基本变量
    nonbasic = list(range(n))         # 原始变量为非基本变量
    
    # 用scipy解辅助问题
    from scipy.optimize import linprog as lp
    aux_result = lp(c_aux, A_eq=A_aux, b_eq=b_std,
                    bounds=[(0, None)] * n_total)
    
    if not aux_result.success or aux_result.fun > 1e-8:
        print("第一阶段: 原问题无可行解！")
        return None, None, 'infeasible'
    
    print(f"第一阶段: 找到基本可行解，辅助目标值 = {aux_result.fun:.6f}")
    
    # ===== 第二阶段: 解原问题 =====
    print("执行第二阶段...")
    result = lp(-c_orig, A_eq=A_std, b_eq=b_std,
                bounds=[(0, None)] * n)
    
    if result.success:
        x_opt = result.x
        z_opt = -result.fun
        print(f"第二阶段: 最优解 z = {z_opt:.6f}")
        return x_opt, z_opt, 'optimal'
    
    return None, None, 'failed'


# ===== 测试 =====
if __name__ == '__main__':
    # 有等式约束的例子
    print("=" * 60)
    print("  两阶段法测试")
    print("=" * 60)
    
    # 例子: max 2x₁ + 3x₂
    # s.t. x₁ + 2x₂ ≤ 4
    #      x₁ + x₂ = 3
    #      x₁, x₂ ≥ 0
    
    c = [2, 3]
    A_ub = [[1, 2]]
    b_ub = [4]
    A_eq = [[1, 1]]
    b_eq = [3]
    
    x_opt, z_opt, status = two_phase_simplex(c, A_ub, b_ub, A_eq, b_eq)
    
    if status == 'optimal':
        print(f"\n最优解: x₁ = {x_opt[0]:.4f}, x₂ = {x_opt[1]:.4f}")
        print(f"最优值: z = {z_opt:.4f}")
    else:
        print(f"状态: {status}")
```

### 5.7 C++实现：两阶段法框架

```cpp
/**
 * 两阶段单纯形法 C++ 框架
 * 编译: g++ -std=c++17 -O2 two_phase.cpp -o two_phase
 */
#include <iostream>
#include <vector>
#include <cmath>
#include <iomanip>
#include <cassert>

using namespace std;
using Matrix = vector<vector<double>>;
using Vector = vector<double>;

const double EPS = 1e-8;

/**
 * 检查是否需要第一阶段
 * 如果标准型中已有m个单位列（可作为初始基），则不需要
 */
bool needPhaseOne(const Matrix& A, int m, int n) {
    vector<bool> row_covered(m, false);
    
    for (int j = 0; j < n; j++) {
        // 检查第j列是否是单位向量
        int one_row = -1;
        bool is_unit = true;
        for (int i = 0; i < m; i++) {
            if (fabs(A[i][j] - 1.0) < EPS) {
                if (one_row == -1) one_row = i;
                else { is_unit = false; break; }
            } else if (fabs(A[i][j]) > EPS) {
                is_unit = false; break;
            }
        }
        if (is_unit && one_row != -1 && !row_covered[one_row]) {
            row_covered[one_row] = true;
        }
    }
    
    for (int i = 0; i < m; i++) {
        if (!row_covered[i]) return true;  // 有行没被覆盖，需要第一阶段
    }
    return false;  // 不需要第一阶段
}

/**
 * 构造并求解辅助问题（第一阶段）
 * 返回: true=找到了初始BFS, false=无可行解
 */
bool solvePhaseOne(const Matrix& A, const Vector& b, 
                   int m, int n, Vector& initial_bfs) {
    // 找出需要人工变量的行
    vector<int> need_artificial;
    vector<bool> has_unit_col(m, false);
    
    // 简化检查: 标记哪些行已有单位列
    for (int j = 0; j < n; j++) {
        int one_row = -1;
        bool is_unit = true;
        for (int i = 0; i < m; i++) {
            if (fabs(A[i][j] - 1.0) < EPS) {
                if (one_row == -1) one_row = i;
                else { is_unit = false; break; }
            } else if (fabs(A[i][j]) > EPS) {
                is_unit = false; break;
            }
        }
        if (is_unit && one_row != -1) {
            has_unit_col[one_row] = true;
        }
    }
    
    for (int i = 0; i < m; i++) {
        if (!has_unit_col[i]) {
            need_artificial.push_back(i);
        }
    }
    
    int n_art = need_artificial.size();  // 人工变量个数
    int n_total = n + n_art;             // 总变量数
    
    cout << "第一阶段: 需要添加 " << n_art << " 个人工变量" << endl;
    
    // 构造辅助问题矩阵
    Matrix A_aux(m, Vector(n_total, 0.0));
    for (int i = 0; i < m; i++) {
        for (int j = 0; j < n; j++) {
            A_aux[i][j] = A[i][j];
        }
    }
    for (int idx = 0; idx < (int)need_artificial.size(); idx++) {
        A_aux[need_artificial[idx]][n + idx] = 1.0;
    }
    
    // 辅助目标: 最小化 Σ 人工变量
    // 初始BFS: 人工变量 = b[need_artificial]
    // 用单纯形法解辅助问题...
    
    // (简化实现: 此处省略完整单纯形求解，用标记说明流程)
    cout << "  构造辅助问题完成" << endl;
    cout << "  用单纯形法求解辅助问题..." << endl;
    cout << "  如果辅助最优值 = 0 → 找到初始BFS" << endl;
    cout << "  如果辅助最优值 > 0 → 原问题无可行解" << endl;
    
    initial_bfs = Vector(n, 0.0);  // 占位
    return true;
}

int main() {
    cout << "========================================" << endl;
    cout << "  两阶段单纯形法" << endl;
    cout << "========================================" << endl;
    
    // 例子: max 2x₁ + 3x₂
    // s.t. x₁ + 2x₂ ≤ 4
    //      x₁ + x₂ = 3
    //      x₁, x₂ ≥ 0
    
    int n = 4;  // x₁, x₂, s₁ (松弛), a₁ (人工)
    int m = 2;  // 2个约束
    
    Matrix A = {
        {1, 2, 1, 0},  // x₁ + 2x₂ + s₁ = 4
        {1, 1, 0, 1}   // x₁ + x₂ + a₁ = 3 (a₁是人工变量)
    };
    Vector b = {4, 3};
    
    if (needPhaseOne(A, m, n)) {
        cout << "需要第一阶段（找初始基本可行解）" << endl;
        Vector initial_bfs;
        bool feasible = solvePhaseOne(A, b, m, n, initial_bfs);
        if (feasible) {
            cout << "\n第一阶段成功！进入第二阶段..." << endl;
            cout << "用单纯形法求解原问题..." << endl;
        } else {
            cout << "\n原问题无可行解！" << endl;
        }
    } else {
        cout << "不需要第一阶段，直接求解" << endl;
    }
    
    return 0;
}
```

> **FAQ：如果第一阶段最优值恰好是0但精度问题导致认为是正的怎么办？**
>
> 这是数值计算中的经典问题。实践中需要设置一个容差 ε（如 10⁻⁸），如果 x₀* < ε 就认为等于0。如果容差设置不当，可能会误判。这也是为什么两阶段法比大M法更稳定——两阶段法的判断更清晰（x₀* = 0 vs > 0），而大M法中M的选择会影响数值精度。

---

## 🎯 本章小结

### 核心要点

| 序号 | 要点 | 一句话总结 |
|------|------|-----------|
| 1 | 线性规划标准型 | max cᵀx, Ax=b, x≥0 —— 一切LP的"标准答题卡" |
| 2 | 松弛型 | 区分基本/非基本变量，单纯形算法的操作格式 |
| 3 | 基本可行解 ↔ 顶点 | 可行域的顶点等价于基本可行解，最优解在顶点 |
| 4 | 单纯形算法 | 从一个顶点走到相邻顶点，沿目标函数增大方向 |
| 5 | 旋转操作 | 入基+出基，高斯消元更新单纯形表 |
| 6 | Bland规则 | 最小下标选择，保证算法不循环 |
| 7 | 对偶性 | 原问题max ↔ 对偶问题min，强对偶：最优值相等 |
| 8 | 影子价格 | 对偶变量 = 约束松紧度的"价格标签" |
| 9 | 互补松弛 | 原始约束松弛 ↔ 对偶变量为0，反之亦然 |
| 10 | 两阶段法 | 第一阶段找初始BFS，第二阶段解原问题 |

### 易错点

| 序号 | 易错点 | 正确理解 |
|------|--------|---------|
| 1 | 忘记转换最小化为最大化 | min cᵀx 等价于 max -cᵀx |
| 2 | 旋转时忘记更新所有行 | 必须消去所有行（包括z行）中入基变量的系数 |
| 3 | 最小比值测试时忽略系数符号 | 只看 aᵢⱼ > 0 的行 |
| 4 | 对偶约束方向搞反 | max → min, ≤ → ≥, 矩阵取转置 |
| 5 | 忽略退化导致的循环 | 退化时θ=0，可能循环，需要Bland规则 |
| 6 | 两阶段法中忘记赶出人工变量 | 第一阶段后x₀必须从基本变量中移除 |
| 7 | 大M法中M取值不当 | M太大会导致数值不稳定 |
| 8 | 对偶变量的经济含义混淆 | yᵢ是约束i的影子价格，不是xᵢ的价格 |

### 与前后章节的联系

```
                    第24章: 单源最短路径
                    (特殊的LP: 非负约束+三角不等式)
                           │
第28章: 矩阵运算 ─────→ 第29章: 线性规划 ←───── 第26章: 最大流
(LUP分解是单纯形         (一般性框架)          (特殊的LP:
 旋转的数学基础)                              容量约束+守恒)
                           │
                           ▼
                    第35章: 近似算法
                    (LP松弛 → 近似比)
```

**关键联系**：

1. **最大流是LP的特例**：把流量守恒写成等式约束，容量写成不等式约束，目标函数是源点流出量最大化
2. **最短路径是LP的特例**：min Σ dᵢⱼfᵢⱼ, 守恒约束 + 非负约束
3. **LP松弛用于近似算法**：整数规划太难，先解LP松弛，再取整
4. **矩阵运算与单纯形**：旋转操作本质是高斯消元（第28章），LUP分解是更稳定的变体
5. **对偶性与博弈论**：零和博弈的Nash均衡可以用LP对偶来求

---

## 📝 课后练习

### 基础题

**练习1**：将以下线性规划转换为标准型：
```
最小化  3x₁ - 2x₂ + x₃
约束:   x₁ + x₂ ≥ 5
        2x₁ - x₃ ≤ 3
        x₂ + x₃ = 4
        x₁ ≥ 0, x₃ ≥ 0, x₂ 无约束
```

**练习2**：对以下松弛型执行一次旋转，选择x₁入基：
```
z  = 10 + 3x₁ + 2x₂
s₁ = 6  - 2x₁ - x₂
s₂ = 8  - x₁ - 3x₂
```
写出旋转后的松弛型，以及新的基本可行解和目标函数值。

**练习3**：写出以下原问题的对偶问题：
```
最大化  4x₁ + 5x₂ + 2x₃
约束:   x₁ + 2x₂ + x₃ ≤ 10
        3x₁ + x₂ + 2x₃ ≤ 8
        x₁, x₂, x₃ ≥ 0
```

### 进阶题

**练习4**：证明弱对偶定理：如果x是原问题(max cᵀx, Ax≤b, x≥0)的可行解，y是对偶问题(min bᵀy, Aᵀy≥c, y≥0)的可行解，则cᵀx ≤ bᵀy。

**练习5**：用单纯形算法（手动执行）求解以下线性规划，写出每一步的单纯形表：
```
最大化  2x₁ + 3x₂
约束:   x₁ + 2x₂ ≤ 6
        2x₁ + x₂ ≤ 8
        x₁, x₂ ≥ 0
```

**练习6**：构造一个退化的线性规划例子，使得单纯形算法在不用Bland规则时可能循环。（提示：3个约束，2个变量，某些bᵢ = 0）

### 挑战题

**练习7**：实现完整的单纯形算法（包含两阶段法），并测试以下有等式约束的问题：
```
最大化  x₁ + 2x₂
约束:   x₁ + x₂ ≤ 4
        x₁ + x₂ = 3
        x₁ - x₂ ≤ 2
        x₁, x₂ ≥ 0
```

**练习8**：利用对偶性，证明以下结论：如果线性规划的原问题和对偶问题都有可行解，则它们都有最优解，且最优值相等。

**练习9**（EDA应用）：在VLSI布局中，有n个标准单元需要放置在芯片的m行中。每个单元有面积aⱼ和功耗pⱼ，每行有最大面积限制Aᵢ和最大功率限制Pᵢ。请建立线性规划模型来最小化总功耗（提示：使用0-1变量的LP松弛）。

---

## 📚 延伸阅读

| 主题 | 推荐资源 | 说明 |
|------|---------|------|
| 线性规划入门 |《运筹学》(希尔伯) | 经典教材，直觉清晰 |
| 单纯形算法深入 | Chvátal, *Linear Programming* | 最权威的LP教材 |
| 内点法 | Ye, *Interior Point Algorithms* | 多项式时间LP算法 |
| 对偶理论 | Bertsimas & Tsitsiklis, *Introduction to Linear Optimization* | 对偶性讲得最好 |
| 数值稳定性 | Gill, Murray & Wright, *Numerical Linear Algebra and Optimization* | 数值实现指南 |
| EDA中的优化 | Kahng et al., *VLSI Physical Design* | 布局布线中的LP应用 |

> **最后一句话**：线性规划是优化世界的"瑞士军刀"——看似简单的线性约束和线性目标，却能统一最短路径、最大流、匹配、分配等众多问题。理解了线性规划，你就拥有了将复杂优化问题"降维打击"的能力。而对偶性更是这个瑞士军刀上最锋利的刀片——它让你从两个角度看同一个问题，有时候对偶问题的视角反而更清晰！
