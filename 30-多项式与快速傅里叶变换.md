# 第30章 多项式与快速傅里叶变换（Polynomials and the FFT）

> 本章回答一个核心问题：如何把多项式乘法从O(n²)降到O(n log n)？——从系数表示到点值表示的"降维打击"，到单位复数根的精妙选取，再到蝶形运算的分治之美，FFT不仅是信号处理的基石，更是大整数乘法、卷积神经网络、EDA中频域分析的核心引擎

## 📖 本章导读

### 🎯 问题引入：为什么需要FFT？

**生活中的例子**：

想象你是一个**音乐制作人**，录了一段音频，想把它从时域（波形）转换到频域（频谱），看看低音、中音、高音各占多少能量——这叫**傅里叶变换**。

- 一段3分钟的CD音质音频 = 44100 × 180 ≈ 800万个采样点
- 朴素傅里叶变换：每个点都要和其他所有点做运算 → O(n²) = 64万亿次运算 → 计算机也得算好几分钟
- FFT：O(n log n) ≈ 800万 × 23 ≈ 1.8亿次运算 → **眨眼之间**

这就是FFT的威力：把一个O(n²)的问题变成O(n log n)，差距不是"快一点"，而是"从算不完到秒算"。

**更多生活类比**：

| 场景 | 输入 | FFT做什么 | 为什么需要快 |
|------|------|-----------|-------------|
| 音乐制作 | 音频波形 | 时域→频域（看频谱） | 实时混音、均衡器 |
| 手机通信 | 5G信号 | 调制/解调（OFDM） | 毫秒级延迟要求 |
| 图像压缩 | JPEG | 频域变换（丢弃高频细节） | 每秒处理30帧 |
| MRI医学成像 | 传感器信号 | 重建图像 | 病人在机器里等着 |
| 大整数乘法 | 超大数 | 多项式乘法加速 | RSA加密依赖 |
| 卷积神经网络 | 特征图 | 快速卷积 | 训练速度决定迭代效率 |
| EDA频域仿真 | 电路信号 | 频域分析/滤波 | 芯片信号完整性验证 |

**与前面章节的关系**：

| 对比项 | 第4章 分治策略 | 第28章 矩阵运算 | 第30章 多项式与FFT |
|--------|---------------|----------------|-------------------|
| 核心思想 | 分而治之 | 矩阵分解 | 系数→点值→乘→还原 |
| 关键技术 | Strassen算法 | LUP分解 | DFT/FFT/逆DFT |
| 复杂度提升 | O(n³)→O(n^2.81) | O(n³)→O(n³)更稳定 | O(n²)→O(n log n) |
| 共同点 | 都用分治 | 都用矩阵 | 都用分治+矩阵 |

**本章路线图**：

```
多项式表示 ──→ DFT定义 ──→ FFT算法 ──→ 逆DFT ──→ 多项式乘法 ──→ 应用
 30.1           30.2        30.2        30.2       完整流程       30.3
 系数/点值      单位根      分治O(nlgn)  还原系数    O(nlgn)乘法    电路实现
 互相转化       Vandermonde  蝶形运算     逆矩阵      卷积定理       位反转
```

---

## 1️⃣ 多项式的表示（30.1 Representations of Polynomials）

### 1.1 什么是多项式？

**定义**：一个次数界为n的多项式A(x)形如：

```
A(x) = a₀ + a₁x + a₂x² + ... + aₙ₋₁xⁿ⁻¹
```

其中a₀, a₁, ..., aₙ₋₁是系数，x是变量。

**生活中的类比**：多项式就像一个**配方表**——系数是"各原料的用量"，变量x是"份数"。代入不同的x值，就得到不同份数时的"总价"。

**关键概念区分**：
- **次数（degree）**：最高次幂的指数（如3x⁴+2x²的次数是4）
- **次数界（degree-bound）**：次数的上界（次数界n意味着次数≤n-1）

### 1.2 两种表示法

多项式有两种截然不同的"存储方式"，就像同一件商品可以有不同的编码系统：

#### 系数表示法（Coefficient Representation）

**存储内容**：直接存系数数组 [a₀, a₁, ..., aₙ₋₁]

```
类比：就像菜单上直接写着"珍珠2份、奶1份、茶3份"
      每个位置对应一个确定的原材料

例如：A(x) = 1 + 2x + 3x²
      系数表示 = [1, 2, 3]
```

**运算效率**：

| 运算 | 时间复杂度 | 说明 |
|------|-----------|------|
| 求值（代入x算A(x₀)） | O(n) | Horner法则 |
| 加法 | O(n) | 对应系数相加 |
| 乘法 | O(n²) | 卷积运算（每个aᵢ和每个bⱼ相乘） |

> **FAQ：为什么系数表示的乘法是O(n²)？**
>
> 因为A(x)·B(x)的第k个系数cₖ = Σᵢ₌₀ᵏ aᵢ·bₖ₋ᵢ，这是**卷积**运算。每个结果系数最多需要k+1次乘法，总共有2n-1个系数要算，总共约n²次乘法。这就是"瓶颈"所在！

#### 点值表示法（Point-Value Representation）

**存储内容**：存n个点值对 {(x₀, y₀), (x₁, y₁), ..., (xₙ₋₁, yₙ₋₁)}，其中yₖ = A(xₖ)

```
类比：就像只知道"1杯=6元、2杯=11元、3杯=16元"
      不直接告诉你配方，但能从这些"采样点"推算出来

例如：A(x) = 1 + 2x + 3x²
      选x = 0, 1, 2：
      A(0) = 1, A(1) = 6, A(2) = 17
      点值表示 = {(0,1), (1,6), (2,17)}
```

**为什么n个点就够了？** 因为n个不同的点唯一确定一个次数界为n的多项式——就像n个点确定一条n-1次曲线（2个点确定直线，3个点确定抛物线...）。

**运算效率**：

| 运算 | 时间复杂度 | 说明 |
|------|-----------|------|
| 求值 | O(n²) | 需要插值（拉格朗日插值） |
| 加法 | O(n) | 对应y值相加（同一个x点上） |
| **乘法** | **O(n)** | **对应y值相乘！** |

> **关键洞察**：点值表示法下，乘法只需要O(n)！因为：
> - C(xₖ) = A(xₖ) · B(xₖ)，即每个点上的值直接相乘
> - 但需要2n个点（因为乘积的次数界是2n），所以实际需要2n个点值对
> - 乘完之后再把点值表示转回系数表示

### 1.3 两种表示法的互相转化

整个FFT加速乘法的核心思路就是：

```
          DFT (求值)                 点值乘法               逆DFT (插值)
系数表示 ──────────→ 点值表示 ──────────→ 乘积点值 ──────────→ 乘积系数
 O(n²)朴素         O(n)快速           O(n)快速            O(n²)朴素
 O(n log n)FFT                                          O(n log n)逆FFT
```

**核心策略**：
1. **系数→点值（DFT）**：如果随便选n个点代入，需要O(n²)；如果"聪明地"选点，可以O(n log n)——这就是FFT
2. **点值→系数（逆DFT）**：插值看起来很难，但借助单位复数根的优美性质，也能O(n log n)

---

### 1.4 系数表示的运算详解

#### 多项式加法

```
A(x) = 1 + 2x + 3x²     → [1, 2, 3]
B(x) = 4 + 5x            → [4, 5, 0]  (高位补0)
C(x) = A(x) + B(x)       → [5, 7, 3]  (对应相加)
```

#### 多项式乘法（卷积）

```
A(x) = 1 + 2x            → [1, 2]
B(x) = 3 + 4x            → [3, 4]

C(x) = A(x)·B(x) = 1·3 + (1·4+2·3)x + 2·4x² = 3 + 10x + 8x² → [3, 10, 8]

卷积过程：
c₀ = a₀·b₀           = 1·3        = 3
c₁ = a₀·b₁ + a₁·b₀   = 1·4 + 2·3  = 10
c₂ = a₁·b₁           = 2·4        = 8

规律：cₖ = Σᵢ aᵢ·bₖ₋ᵢ（下标之和等于k的所有配对）
```

**图解卷积**（对角线法）：

```
        b₀=3   b₁=4
       ┌──────┬──────┐
a₀=1   │  3   │  4   │  → c₀=3, c₁=4
       ├──────┼──────┤
a₁=2   │  6   │  8   │  → c₁+=6, c₂=8
       └──────┴──────┘
         ↓      ↓
        c₀=3  c₁=10  c₂=8

对角线求和：主对角线→c₀，次对角线→c₁，再次→c₂
```

### 1.5 代码实现：系数表示的加法和乘法

**Python实现**：

```python
# 多项式加法（系数表示）O(n)
def poly_add(A, B):
    """多项式加法：对应系数相加"""
    # 取两个多项式的最大长度，高位补0
    n = max(len(A), len(B))
    C = [0] * n           # 初始化结果数组
    for i in range(len(A)):
        C[i] += A[i]      # 加A的系数
    for i in range(len(B)):
        C[i] += B[i]      # 加B的系数
    return C

# 多项式乘法（系数表示，朴素版）O(n²)
def poly_multiply_naive(A, B):
    """朴素多项式乘法：卷积运算"""
    n = len(A)
    m = len(B)
    # 乘积的次数界 = n + m - 1
    C = [0] * (n + m - 1)    # 初始化结果数组
    for i in range(n):
        for j in range(m):
            C[i + j] += A[i] * B[j]  # aᵢ·bⱼ 贡献到 cᵢ₊ⱼ
    return C

# 测试
A = [1, 2]       # 1 + 2x
B = [3, 4]       # 3 + 4x
print(poly_add(A, B))              # [4, 6] → 4 + 6x
print(poly_multiply_naive(A, B))   # [3, 10, 8] → 3 + 10x + 8x²
```

**C++实现**：

```cpp
#include <vector>
#include <iostream>
using namespace std;

// 多项式加法（系数表示）O(n)
vector<double> polyAdd(const vector<double>& A, const vector<double>& B) {
    // 取两个多项式的最大长度
    int n = max(A.size(), B.size());
    vector<double> C(n, 0.0);     // 初始化结果数组，默认0
    for (int i = 0; i < A.size(); i++)
        C[i] += A[i];             // 加A的系数
    for (int i = 0; i < B.size(); i++)
        C[i] += B[i];             // 加B的系数
    return C;
}

// 多项式乘法（朴素版）O(n²)
vector<double> polyMultiplyNaive(const vector<double>& A, const vector<double>& B) {
    int n = A.size(), m = B.size();
    vector<double> C(n + m - 1, 0.0);  // 乘积次数界 = n+m-1
    for (int i = 0; i < n; i++)
        for (int j = 0; j < m; j++)
            C[i + j] += A[i] * B[j];   // aᵢ·bⱼ 贡献到 cᵢ₊ⱼ
    return C;
}

int main() {
    vector<double> A = {1, 2};     // 1 + 2x
    vector<double> B = {3, 4};     // 3 + 4x
    auto sum = polyAdd(A, B);
    auto prod = polyMultiplyNaive(A, B);
    // sum: 4 + 6x, prod: 3 + 10x + 8x²
    return 0;
}
```

### 1.6 点值表示的运算

**点值加法**（同一组x点上）：

```
A = {(0,1), (1,6), (2,17)}    ← 1+2x+3x²
B = {(0,4), (1,9), (2,16)}    ← 4+5x+3x² (假设)
C = {(0,5), (1,15), (2,33)}   ← 对应y值相加
```

**点值乘法**（同一组x点上，注意需要足够的点）：

```
A(x) = 1 + 2x, 次数界2 → 需要4个点来表示乘积
x = 0, 1, 2, 3:
A = {(0,1), (1,3), (2,5), (3,7)}
B = {(0,3), (1,7), (2,11), (3,15)}
C = A·B: {(0,3), (1,21), (2,55), (3,105)}  ← y值直接相乘！
```

> **FAQ：为什么点值乘法需要2n个点？**
>
> 如果A的次数界是n，B的次数界是n，那么A·B的次数界是2n-1。根据"n个点确定n-1次多项式"的规则，确定2n-1次多项式需要2n个点。如果只用n个点，信息不够，插值出来的结果会出错。所以：**乘法前要把A和B都扩展到2n个点值对**。

---

## 2️⃣ 单位复数根（30.2 Roots of Unity）

### 2.1 复数回顾

**为什么需要复数？** 因为FFT的秘密武器——单位复数根——在实数域里找不到。

**复数基础**：

```
复数 z = a + bi，其中 i² = -1

极坐标表示：z = r·(cosθ + i·sinθ) = r·e^(iθ)
  r = |z| = √(a² + b²)  ——模（到原点的距离）
  θ = arctan(b/a)         ——幅角（与x轴的夹角）

类比：复数就像平面上的一个点
  直角坐标 (a, b) ↔ 极坐标 (r, θ)
  加法 = 向量加法
  乘法 = 模相乘，幅角相加
```

**复数乘法的几何意义**：

```
z₁ = r₁·e^(iθ₁)，z₂ = r₂·e^(iθ₂)
z₁·z₂ = r₁·r₂·e^(i(θ₁+θ₂))

含义：乘一个复数 = 旋转 + 缩放
  例如：乘以 i = 旋转90°（因为i = e^(iπ/2)）
  乘以 -1 = 旋转180°
  乘以 e^(iθ) = 只旋转，不缩放（r=1）
```

### 2.2 n次单位复数根

**定义**：满足 ωⁿ = 1 的复数ω称为**n次单位复数根**。

**关键公式**：

```
主n次单位根：ωₙ = e^(2πi/n) = cos(2π/n) + i·sin(2π/n)

所有n次单位根：ωₙ⁰, ωₙ¹, ωₙ², ..., ωₙⁿ⁻¹
  即 ωₙᵏ = e^(2πik/n)，k = 0, 1, ..., n-1
```

**图解**（n=8为例）：

```
              ω₈¹ = (√2/2, √2/2)
              ╱
    ω₈⁷     ╱     ω₈² = (0, 1)
   (-√2/2, √2/2) │
      ╲     │     ╱
       ╲    │    ╱
        ╲   │   ╱
─────────●─────────→ Re
        ╱   │   ╲
       ╱    │    ╲
      ╱     │     ╲
   ω₈⁶    │     ω₈³
  (-√2/2,-√2/2)│     (√2/2,-√2/2)
         ╲
    ω₈⁵     ω₈⁷ = (1, 0)→ ω₈⁰ = (1, 0)
         (-1, 0)

8个单位根均匀分布在单位圆上，相邻间隔45°
ω₈⁰ = 1
ω₈¹ = e^(iπ/4) = (√2/2 + i√2/2)
ω₈² = e^(iπ/2) = i
ω₈³ = e^(i3π/4) = (-√2/2 + i√2/2)
ω₈⁴ = e^(iπ) = -1
ω₈⁵ = e^(i5π/4) = (-√2/2 - i√2/2)
ω₈⁶ = e^(i3π/2) = -i
ω₈⁷ = e^(i7π/4) = (√2/2 - i√2/2)
```

### 2.3 单位根的核心性质

这些性质是FFT能够工作的基础——每一条都像一个"速写咒语"，让分治成为可能：

#### 性质1：消去引理（Cancellation Lemma）

```
对任意整数 n ≥ 0, k ≥ 0, d > 0：
ωdₙ^dk = ωₙᵏ

含义：下标和指数同乘d，值不变。
类比：1/4 = 2/8 —— 分子分母同乘2，分数值不变
```

**证明**：ωdₙ^dk = (e^(2πi/dn))^(dk) = e^(2πik/n) = ωₙᵏ ∎

**推论**：ω₂ₙ^n = ωₙ^(n/2) = ω₂ = -1（这个在FFT的合并步骤中至关重要！）

#### 性质2：折半引理（Halving Lemma）

```
如果 n > 0 是偶数，则 n 个 n 次单位根的平方 = n/2 个 n/2 次单位根（每个出现两次）

(ωₙᵏ)² = ωₙ/₂ᵏ，k = 0, 1, ..., n-1
```

**为什么"每个出现两次"？** 因为ωₙ^(k+n/2) = -ωₙᵏ，所以(ωₙ^(k+n/2))² = (ωₙᵏ)²。

**图解**（n=8）：

```
8次单位根的平方：
(ω₈⁰)² = ω₄⁰ = 1
(ω₈¹)² = ω₄¹ = i       ← 和(ω₈⁵)²相同！
(ω₈²)² = ω₄² = -1      ← 和(ω₈⁶)²相同！
(ω₈³)² = ω₄³ = -i      ← 和(ω₈⁷)²相同！
(ω₈⁴)² = ω₄⁰ = 1       ← 和(ω₈⁰)²相同！
(ω₈⁵)² = ω₄¹ = i
(ω₈⁶)² = ω₄² = -1
(ω₈⁷)² = ω₄³ = -i

结论：8个根的平方 → 只有4个不同的值（4次单位根），每个出现两次
这就是FFT能把n点问题分成两个n/2点问题的关键！
```

#### 性质3：求和引理（Summation Lemma）

```
对任意正整数 n 和不能被 n 整除的整数 k：

Σⱼ₌₀ⁿ⁻¹ (ωₙᵏ)ʲ = 0

即：1 + ωₙᵏ + ωₙ²ᵏ + ... + ωₙ⁽ⁿ⁻¹⁾ᵏ = 0
```

**直观理解**：n个单位根均匀分布在圆上，它们的"重心"在原点（0,0），所以和为0。

```
n=4为例，k=1：
1 + i + (-1) + (-i) = 0 ✓

图：
    i(ω₄¹)
     │
-1───●───1
     │
   -i(ω₄³)
四个点对称，合力为零！
```

> **FAQ：求和引理有什么用？**
>
> 它是逆DFT成立的数学基础。当我们从点值表示插值回系数表示时，需要证明Vandermonde矩阵的逆存在，而求和引理恰好给出了矩阵可逆的关键条件。

---

## 3️⃣ 离散傅里叶变换DFT（30.2 DFT and FFT）

### 3.1 DFT的定义

**定义**：给定系数向量a = (a₀, a₁, ..., aₙ₋₁)，其DFT为向量y = (y₀, y₁, ..., yₙ₋₁)，其中：

```
yₖ = A(ωₙᵏ) = Σⱼ₌₀ⁿ⁻¹ aⱼ · ωₙᵏʲ，k = 0, 1, ..., n-1
```

**通俗理解**：DFT就是把多项式A(x)在n个n次单位复数根处求值——把系数表示转成点值表示，而选的求值点恰好是单位根。

**矩阵形式**：

```
⎡y₀⎤   ⎡1    1      1     ...  1    ⎤ ⎡a₀⎤
⎢y₁⎥   ⎢1    ωₙ     ωₙ²   ...  ωₙⁿ⁻¹⎥ ⎢a₁⎥
⎢y₂⎥ = ⎢1    ωₙ²    ωₙ⁴   ...  ωₙ²⁽ⁿ⁻¹⁾⎥ ⎢a₂⎥
⎢ ⋮ ⎥   ⎢⋮    ⋮      ⋮          ⋮   ⎥ ⎢ ⋮ ⎥
⎣yₙ₋₁⎦ ⎣1    ωₙⁿ⁻¹  ωₙ²⁽ⁿ⁻¹⁾ ... ωₙ⁽ⁿ⁻¹⁾²⎦ ⎣aₙ₋₁⎦

即 y = Vₙ · a，其中Vₙ是Vandermonde矩阵
Vₙ[k,j] = ωₙᵏʲ
```

**朴素计算DFT**：O(n²)——每个yₖ需要n次乘法，共n个yₖ。

### 3.2 逆DFT

**目标**：从点值表示(y₀, y₁, ..., yₙ₋₁)还原系数(a₀, a₁, ..., aₙ₋₁)。

**关键定理**：

```
Vₙ⁻¹ 的 (j,k) 元素为 ωₙ⁻ʲᵏ / n

即：aⱼ = (1/n) Σₖ₌₀ⁿ⁻¹ yₖ · ωₙ⁻ʲᵏ，j = 0, 1, ..., n-1
```

**比较DFT和逆DFT**：

| DFT | 逆DFT |
|-----|-------|
| yₖ = Σⱼ aⱼ·ωₙᵏʲ | aⱼ = (1/n) Σₖ yₖ·ωₙ⁻ʲᵏ |
| 用ωₙ | 用ωₙ⁻¹（共轭根） |
| 矩阵Vₙ | 矩阵Vₙ⁻¹ = (1/n)·conj(Vₙ) |

> **惊人结论**：逆DFT和DFT的算法结构完全一样！只需要：
> 1. 把ωₙ换成ωₙ⁻¹（即取共轭）
> 2. 最后除以n
>
> 这意味着：**FFT算法稍加修改就能算逆DFT**，不需要单独设计算法！

---

## 4️⃣ 快速傅里叶变换FFT（30.2 The FFT）

### 4.1 核心思想：分治

**朴素DFT**：对n个点，每个点要算n次 → O(n²)

**FFT的关键洞察**：如果选择**单位复数根**作为求值点，利用折半引理，可以把n点DFT分解成两个n/2点DFT！

**分治策略**（按奇偶下标拆分）：

```
A(x) = a₀ + a₁x + a₂x² + a₃x³ + ... + aₙ₋₂xⁿ⁻² + aₙ₋₁xⁿ⁻¹

拆成：
A⁽⁰⁾(x) = a₀ + a₂x + a₄x² + ... + aₙ₋₂xⁿ/²⁻¹  ← 偶数下标
A⁽¹⁾(x) = a₁ + a₃x + a₅x² + ... + aₙ₋₁xⁿ/²⁻¹   ← 奇数下标

于是：
A(x) = A⁽⁰⁾(x²) + x·A⁽¹⁾(x²)
```

**类比**：把一副扑克牌按奇偶位置分成两半，奇数位置的归一组，偶数位置的归另一组。每一半的信息量只有原来的一半！

### 4.2 FFT递推公式

对k = 0, 1, ..., n/2-1：

```
yₖ = A⁽⁰⁾(ωₙ/₂ᵏ) + ωₙᵏ·A⁽¹⁾(ωₙ/₂ᵏ)     ← 前半部分
yₖ₊ₙ/₂ = A⁽⁰⁾(ωₙ/₂ᵏ) - ωₙᵏ·A⁽¹⁾(ωₙ/₂ᵏ)   ← 后半部分
```

**推导过程**：

```
A(ωₙᵏ) = A⁽⁰⁾((ωₙᵏ)²) + ωₙᵏ·A⁽¹⁾((ωₙᵏ)²)
        = A⁽⁰⁾(ωₙ²ᵏ) + ωₙᵏ·A⁽¹⁾(ωₙ²ᵏ)
        = A⁽⁰⁾(ωₙ/₂ᵏ) + ωₙᵏ·A⁽¹⁾(ωₙ/₂ᵏ)    ← 消去引理：ωₙ²ᵏ = ωₙ/₂ᵏ

A(ωₙᵏ⁺ⁿ/₂) = A⁽⁰⁾(ωₙ²⁽ᵏ⁺ⁿ/²⁾) + ωₙᵏ⁺ⁿ/²·A⁽¹⁾(ωₙ²⁽ᵏ⁺ⁿ/²⁾)
             = A⁽⁰⁾(ωₙ/₂ᵏ·ωₙⁿ) + ωₙᵏ·ωₙⁿ/²·A⁽¹⁾(ωₙ/₂ᵏ·ωₙⁿ)
             = A⁽⁰⁾(ωₙ/₂ᵏ) + (-1)·ωₙᵏ·A⁽¹⁾(ωₙ/₂ᵏ)    ← ωₙⁿ=1, ωₙⁿ/²=-1
             = A⁽⁰⁾(ωₙ/₂ᵏ) - ωₙᵏ·A⁽¹⁾(ωₙ/₂ᵏ)
```

> **FAQ：为什么ωₙⁿ/² = -1？**
>
> ωₙ = e^(2πi/n)，所以ωₙⁿ/² = e^(πi) = cos(π) + i·sin(π) = -1。这就是单位根"折半"后变号的秘密——在单位圆上转半圈就是-1！

### 4.3 FFT算法（递归版）

```
RECURSIVE-FFT(a)  // a = (a₀, a₁, ..., aₙ₋₁)，n是2的幂
1  n ← length[a]
2  if n = 1
3      return a                    // 基础情形：1点DFT就是自身
4  ωₙ ← e^(2πi/n)                 // 主n次单位根
5  ω ← 1                          // ωₙ⁰ = 1
6  a⁽⁰⁾ ← (a₀, a₂, ..., aₙ₋₂)   // 偶数下标
7  a⁽¹⁾ ← (a₁, a₃, ..., aₙ₋₁)   // 奇数下标
8  y⁽⁰⁾ ← RECURSIVE-FFT(a⁽⁰⁾)   // 递归计算偶数部分的DFT
9  y⁽¹⁾ ← RECURSIVE-FFT(a⁽¹⁾)   // 递归计算奇数部分的DFT
10 for k ← 0 to n/2 - 1
11     yₖ ← yₖ⁽⁰⁾ + ω · yₖ⁽¹⁾     // 蝶形运算：前半
12     yₖ₊ₙ/₂ ← yₖ⁽⁰⁾ - ω · yₖ⁽¹⁾ // 蝶形运算：后半
13     ω ← ω · ωₙ                  // ω = ωₙᵏ⁺¹
14 return y
```

### 4.4 图解FFT过程（n=8为例）

```
输入：a = (a₀, a₁, a₂, a₃, a₄, a₅, a₆, a₇)

第1层拆分（按奇偶）：
  a⁽⁰⁾ = (a₀, a₂, a₄, a₆)   ← 偶数
  a⁽¹⁾ = (a₁, a₃, a₅, a₇)   ← 奇数

第2层拆分：
  a⁽⁰⁰⁾ = (a₀, a₄)   a⁽⁰¹⁾ = (a₂, a₆)
  a⁽¹⁰⁾ = (a₁, a₅)   a⁽¹¹⁾ = (a₃, a₇)

第3层拆分（基础情形）：
  每个都只有一个元素，直接返回

合并（从底向上）：
  第3层→第2层：2点DFT合并
  第2层→第1层：4点DFT合并
  第1层→结果：8点DFT合并
```

**详细的8点FFT蝶形图**：

```
输入层(位反转序)     第1级(d=1)      第2级(d=2)       第3级(d=4)       输出(自然序)

a₀ ─────────────────────────────────────────────────────────── y₀
                                              ╱
a₄ ──────────────────────────────────────────╳────────────── y₄
                           ╱                  ╲
a₂ ──────────────────────╳───────────────────────────────── y₂
                          ╲                   ╱
a₆ ───────────────────────╳──────────────────╳────────────── y₆
          ╱                ╲                  ╲
a₁ ─────╳────────────────────────────────────────────────── y₁
         ╲                ╱                   ╱
a₅ ──────╳──────────────╳───────────────────╳────────────── y₅
          ╲              ╲                  ╱
a₃ ───────╳──────────────────────────────────────────────── y₃
           ╲            ╱ ╲                ╱
a₇ ────────╳──────────╳───╳──────────────╳──────────────── y₇

（简化表示，实际每个交叉点是一个蝶形运算）
```

### 4.5 蝶形运算详解

蝶形运算是FFT的基本计算单元——名字来源于数据流图的形状像蝴蝶：

```
输入：E = yₖ⁽⁰⁾（偶数部分第k个），O = yₖ⁽¹⁾（奇数部分第k个），ω = ωₙᵏ

蝶形运算：
        ┌──── yₖ = E + ω·O
E ──┬───┤
    │   └──── yₖ₊ₙ/₂ = E - ω·O
O ──┤
    │
ω ──┘

具体计算：
  t = ω · O              ← 旋转因子乘以奇数部分
  yₖ = E + t             ← 上输出
  yₖ₊ₙ/₂ = E - t        ← 下输出
```

**为什么叫"蝶形"？** 把上面的图旋转90度，就像一只蝴蝶展开翅膀🦋。

**蝶形运算的美妙之处**：
- 两个输入，两个输出
- 只需要1次复数乘法（t = ω·O）和2次复数加法
- 同一个t既用于加法又用于减法，没有任何浪费

### 4.6 复杂度分析

**递推式**：

```
T(n) = 2T(n/2) + Θ(n)    ← 两个n/2子问题 + Θ(n)的合并
     = Θ(n log n)         ← 主定理情况2
```

**详细计算**：

```
第0层：1个n点DFT → 0次运算
第1层：2个n/2点DFT → n次运算（n/2个蝶形，每个1次乘法）
第2层：4个n/4点DFT → n次运算
...
第log₂n层：n个1点DFT → n次运算

总运算量 = n × log₂n = Θ(n log n)
```

**对比**：

| 方法 | 乘法次数 | n=1024时 | n=10⁶时 |
|------|---------|----------|---------|
| 朴素DFT | n² | ≈10⁶ | ≈10¹² |
| FFT | n log₂ n | ≈10⁴ | ≈2×10⁷ |
| 加速比 | n/log n | 100× | 50000× |

---

## 5️⃣ 逆FFT与完整的多项式乘法（30.2 Inverse DFT）

### 5.1 逆FFT算法

如前所述，逆DFT和DFT几乎相同：

```
RECURSIVE-INVERSE-FFT(y)
1  n ← length[y]
2  if n = 1
3      return y
4  ωₙ ← e^(-2πi/n)          ← 注意：这里是ωₙ⁻¹，即取共轭！
5  ω ← 1
6  y⁽⁰⁾ ← (y₀, y₂, ..., yₙ₋₂)
7  y⁽¹⁾ ← (y₁, y₃, ..., yₙ₋₁)
8  a⁽⁰⁾ ← RECURSIVE-INVERSE-FFT(y⁽⁰⁾)
9  a⁽¹⁾ ← RECURSIVE-INVERSE-FFT(y⁽¹⁾)
10 for k ← 0 to n/2 - 1
11     aₖ ← (aₖ⁽⁰⁾ + ω · aₖ⁽¹⁾) / n    ← 注意除以n
12     aₖ₊ₙ/₂ ← (aₖ⁽⁰⁾ - ω · aₖ⁽¹⁾) / n
13     ω ← ω · ωₙ
14 return a
```

**更优雅的实现**：直接复用FFT函数！

```
INVERSE-FFT(y):
1  ȳ ← conj(y)         ← 对y取共轭（每个元素取共轭复数）
2  ǎ ← FFT(ȳ)          ← 用普通FFT计算
3  a ← conj(ǎ) / n     ← 再取共轭并除以n
4  return a
```

### 5.2 完整的多项式乘法流程

把所有步骤串起来：

```
FFT-MULTIPLY(A, B):    // A, B是系数表示的多项式
1  // Step 1: 补零到2n（n是2的幂，>= deg(A)+deg(B)+1）
2  Ā ← A 补零到长度 2n
3  B̄ ← B 补零到长度 2n
4  // Step 2: DFT（系数→点值）
5  ỹĀ ← FFT(Ā)
6  ỹB̄ ← FFT(B̄)
7  // Step 3: 点值乘法
8  ỹC ← ỹĀ · ỹB̄  （逐点相乘，O(n)）
9  // Step 4: 逆DFT（点值→系数）
10 C ← INVERSE-FFT(ỹC)
11 return C
```

**图解完整流程**：

```
系数表示          DFT              点值表示         点值乘        点值结果         逆DFT         系数结果
[a₀,a₁,...] ──FFT──→ [ŷ₀,ŷ₁,...] ──×──→ [ĉ₀,ĉ₁,...] ──IFFT──→ [c₀,c₁,...]
[b₀,b₁,...] ──FFT──→ [ŷ₀',ŷ₁',...] ┘      O(n)                  O(n log n)
  O(n log n)         O(n log n)                           O(n log n)

总复杂度：O(n log n)
```

**具体例子**：

```
A(x) = 1 + 2x, B(x) = 3 + 4x
次数界2，乘积次数界3，需要补到n=4（2的幂）

Step 1: 补零
  Ā = [1, 2, 0, 0]
  B̄ = [3, 4, 0, 0]

Step 2: FFT（4点）
  ω₄ = e^(2πi/4) = i
  ω₄⁰ = 1, ω₄¹ = i, ω₄² = -1, ω₄³ = -i

  FFT(Ā):
  Ā⁽⁰⁾ = [1, 0], Ā⁽¹⁾ = [2, 0]  (奇偶拆分)
  FFT([1,0]) = [1, 1]    (1点DFT=[1], 合并: 1±1·0)
  FFT([2,0]) = [2, 2]
  合并: y₀=1+1·2=3, y₂=1-1·2=-1, y₁=1+i·2=1+2i, y₃=1-i·2=1-2i
  ỹĀ = [3, 1+2i, -1, 1-2i]

  FFT(B̄): 同理
  ỹB̄ = [7, 3+4i, -1, 3-4i]

Step 3: 点值乘法
  ỹC = [21, (1+2i)(3+4i), 1, (1-2i)(3-4i)]
     = [21, 3+4i+6i+8i², 1, 3-4i-6i+8i²]
     = [21, -5+10i, 1, -5-10i]

Step 4: 逆FFT
  C = IFFT([-5+10i, -5-10i, 1, 21]) 的共轭/4...
  最终: C = [3, 10, 8, 0]

验证: (1+2x)(3+4x) = 3+10x+8x² ✓
```

---

## 6️⃣ FFT的高效实现（30.3 Efficient FFT Implementations）

### 6.1 迭代版FFT

递归版FFT虽然清晰，但递归调用有额外开销。实际实现用**迭代版**（也叫Cooley-Tukey算法）。

**核心思想**：从底向上，逐层执行蝶形运算。

**关键问题**：递归版不断按奇偶拆分，输入顺序被打乱了。观察拆分过程：

```
原始下标:  0  1  2  3  4  5  6  7
二进制:   000 001 010 011 100 101 110 111

第1次拆分(偶/奇):
  偶: 0(000) 2(010) 4(100) 6(110)
  奇: 1(001) 3(011) 5(101) 7(111)

第2次拆分:
  偶偶: 0(000) 4(100)  奇偶: 2(010) 6(110)
  偶奇: 1(001) 5(101)  奇奇: 3(011) 7(111)

第3次拆分:
  0(000) 4(100) 2(010) 6(110) 1(001) 5(101) 3(011) 7(111)

最终顺序: 0, 4, 2, 6, 1, 5, 3, 7
```

**位反转规律**：最终顺序恰好是原始下标的**二进制位反转**！

```
0 = 000 → 000 = 0
1 = 001 → 100 = 4
2 = 010 → 010 = 2
3 = 011 → 110 = 6
4 = 100 → 001 = 1
5 = 101 → 101 = 5
6 = 110 → 011 = 3
7 = 111 → 111 = 7

位反转序: 0, 4, 2, 6, 1, 5, 3, 7 ✓ 完全匹配！
```

### 6.2 位反转置换算法

```python
def bit_reverse_copy(a, n):
    """将数组a按位反转序重排"""
    result = [0] * n
    for k in range(n):
        rev = reverse_bits(k, n.bit_length() - 1)  # 反转k的二进制表示
        result[rev] = a[k]
    return result

def reverse_bits(x, num_bits):
    """反转x的低num_bits位"""
    result = 0
    for _ in range(num_bits):
        result = (result << 1) | (x & 1)  # 取最低位，加到结果最高位
        x >>= 1                            # 右移一位
    return result
```

### 6.3 迭代FFT算法

```
ITERATIVE-FFT(a)
1  n ← length[a]                           // n是2的幂
2  // Step 1: 位反转置换
3  A ← BIT-REVERSE-COPY(a, n)
4  // Step 2: 逐层蝶形运算
5  for s ← 1 to log₂n                      // s是当前级数
6      m ← 2ˢ                              // 当前子问题大小
7      ωₘ ← e^(2πi/m)                      // 主m次单位根
8      for k ← 0 to n-1 by m               // 处理每个子问题
9          ω ← 1                            // 旋转因子
10         for j ← 0 to m/2 - 1            // 蝶形运算
11             t ← ω · A[k + j + m/2]      // 旋转因子 × 奇数部分
12             u ← A[k + j]                // 偶数部分
13             A[k + j] ← u + t            // 上输出
14             A[k + j + m/2] ← u - t      // 下输出
15             ω ← ω · ωₘ                  // 更新旋转因子
16 return A
```

**图解迭代FFT（n=8）**：

```
位反转输入: [a₀, a₄, a₂, a₆, a₁, a₅, a₃, a₇]
  下标:      0    1    2    3    4    5    6    7

s=1 (m=2, d=1):          s=2 (m=4, d=2):          s=3 (m=8, d=4):
蝶形距离=1               蝶形距离=2               蝶形距离=4

[0]─┬─ + ──[0]          [0]──┬── + ──[0]         [0]───┬─── + ──[0]
[1]─┘ - ──[4]          [2]──┘  - ──[4]          [4]───┘    - ──[4]
                        [1]──┬── + ──[1]         [1]───┬─── + ──[1]
[2]─┬─ + ──[2]          [3]──┘  - ──[5]         [5]───┘    - ──[5]
[3]─┘ - ──[6]                                   [2]───┬─── + ──[2]
                        [4]──┬── + ──[2]         [6]───┘    - ──[6]
[4]─┬─ + ──[1]          [5]──┘  - ──[6]
[5]─┘ - ──[5]                                   [3]───┬─── + ──[3]
                        [6]──┬── + ──[3]         [7]───┘    - ──[7]
[6]─┬─ + ──[3]          [7]──┘  - ──[7]
[7]─┘ - ──[7]

(每个蝶形使用对应的旋转因子ωₘʲ)
```

### 6.4 完整代码实现

**Python实现（递归FFT）**：

```python
import cmath  # 复数运算库

def fft(a, invert=False):
    """
    FFT算法（Cooley-Tukey，递归版）
    a: 输入数组（复数列表），长度必须是2的幂
    invert: True表示逆FFT，False表示正FFT
    返回: FFT/逆FFT结果
    """
    n = len(a)
    
    # 基础情形：1点DFT就是自身
    if n == 1:
        return a
    
    # 按奇偶下标拆分
    a0 = [a[2*i] for i in range(n // 2)]    # 偶数下标: a₀, a₂, a₄, ...
    a1 = [a[2*i + 1] for i in range(n // 2)] # 奇数下标: a₁, a₃, a₅, ...
    
    # 递归计算两个子问题
    y0 = fft(a0, invert)   # 偶数部分的DFT
    y1 = fft(a1, invert)   # 奇数部分的DFT
    
    # 主单位根：正FFT用ωₙ = e^(2πi/n)，逆FFT用ωₙ⁻¹ = e^(-2πi/n)
    ang = 2 * cmath.pi / n * (-1 if invert else 1)
    wn = cmath.exp(complex(0, ang))   # ωₙ 或 ωₙ⁻¹
    w = complex(1, 0)                  # ω = ωₙ⁰ = 1
    
    # 蝶形运算：合并两个子问题的结果
    y = [complex(0, 0)] * n
    for k in range(n // 2):
        t = w * y1[k]                   # 旋转因子 × 奇数部分
        y[k] = y0[k] + t               # 前半: E + ω·O
        y[k + n // 2] = y0[k] - t      # 后半: E - ω·O
        w *= wn                          # 更新旋转因子: ω ← ω·ωₙ
    
    # 逆FFT需要除以n
    if invert:
        y = [yi / n for yi in y]
    
    return y

def multiply_polynomials(a, b):
    """
    使用FFT进行多项式乘法
    a, b: 系数数组（实数或复数列表）
    返回: 乘积的系数数组
    """
    # Step 1: 确定结果多项式的次数界
    result_degree = len(a) + len(b) - 1
    
    # 找到大于等于result_degree的最小2的幂
    n = 1
    while n < result_degree:
        n *= 2
    
    # 补零到长度n
    fa = [complex(x, 0) for x in a] + [complex(0, 0)] * (n - len(a))
    fb = [complex(x, 0) for x in b] + [complex(0, 0)] * (n - len(b))
    
    # Step 2: DFT（系数→点值）
    fa = fft(fa, invert=False)
    fb = fft(fb, invert=False)
    
    # Step 3: 点值乘法
    fc = [fa[i] * fb[i] for i in range(n)]
    
    # Step 4: 逆DFT（点值→系数）
    fc = fft(fc, invert=True)
    
    # 提取实部，四舍五入（理论上虚部应为0，但浮点误差可能产生微小虚部）
    result = [round(fc[i].real) for i in range(result_degree)]
    return result

# 测试
A = [1, 2]       # 1 + 2x
B = [3, 4]       # 3 + 4x
C = multiply_polynomials(A, B)
print(f"({1}+{2}x) × ({3}+{4}x) = {C}")
# 输出: [3, 10, 8] → 3 + 10x + 8x²

# 更大测试
A = [1, 1]       # 1 + x
B = [1, -1]      # 1 - x
C = multiply_polynomials(A, B)
print(f"(1+x) × (1-x) = {C}")
# 输出: [1, 0, -1] → 1 - x²
```

**C++实现（迭代FFT）**：

```cpp
#include <cmath>
#include <complex>
#include <vector>
#include <iostream>
using namespace std;

typedef complex<double> cd;  // 复数类型别名
const double PI = acos(-1.0); // π的精确值

// 位反转函数
int reverseBits(int x, int log2n) {
    int result = 0;
    for (int i = 0; i < log2n; i++) {
        result <<= 1;           // 结果左移，腾出最低位
        result |= (x & 1);     // 取x的最低位放入result
        x >>= 1;               // x右移，处理下一位
    }
    return result;
}

// 迭代FFT
void fft(vector<cd>& a, bool invert) {
    int n = a.size();
    int log2n = 0;
    while ((1 << log2n) < n) log2n++;  // 计算log₂n
    
    // Step 1: 位反转置换
    for (int i = 0; i < n; i++) {
        int j = reverseBits(i, log2n);
        if (i < j) swap(a[i], a[j]);  // 避免重复交换
    }
    
    // Step 2: 逐层蝶形运算
    for (int len = 2; len <= n; len <<= 1) {  // len = 2, 4, 8, ..., n
        double ang = 2 * PI / len * (invert ? -1 : 1);  // 旋转角度
        cd wn(cos(ang), sin(ang));  // 主len次单位根
        
        for (int i = 0; i < n; i += len) {   // 处理每个子问题
            cd w(1, 0);                        // ω = ωₙ⁰ = 1
            for (int j = 0; j < len / 2; j++) {  // 蝶形运算
                cd u = a[i + j];               // 偶数部分
                cd v = a[i + j + len / 2] * w; // 奇数部分 × 旋转因子
                a[i + j] = u + v;              // 上输出
                a[i + j + len / 2] = u - v;    // 下输出
                w *= wn;                        // 更新旋转因子
            }
        }
    }
    
    // 逆FFT除以n
    if (invert) {
        for (int i = 0; i < n; i++)
            a[i] /= n;
    }
}

// 使用FFT的多项式乘法
vector<int> multiplyPolynomials(const vector<int>& a, const vector<int>& b) {
    // 确定结果长度和FFT大小
    int result_size = a.size() + b.size() - 1;
    int n = 1;
    while (n < result_size) n <<= 1;  // 取大于等于result_size的最小2的幂
    
    // 补零并转为复数
    vector<cd> fa(n, 0), fb(n, 0);
    for (int i = 0; i < a.size(); i++) fa[i] = a[i];
    for (int i = 0; i < b.size(); i++) fb[i] = b[i];
    
    // FFT
    fft(fa, false);   // DFT(a)
    fft(fb, false);   // DFT(b)
    
    // 点值乘法
    for (int i = 0; i < n; i++)
        fa[i] *= fb[i];
    
    // 逆FFT
    fft(fa, true);
    
    // 提取结果
    vector<int> result(result_size);
    for (int i = 0; i < result_size; i++)
        result[i] = round(fa[i].real());  // 四舍五入到整数
    
    return result;
}

int main() {
    vector<int> A = {1, 2};     // 1 + 2x
    vector<int> B = {3, 4};     // 3 + 4x
    auto C = multiplyPolynomials(A, B);
    // C = {3, 10, 8} → 3 + 10x + 8x²
    for (int x : C) cout << x << " ";
    cout << endl;
    return 0;
}
```

---

## 7️⃣ 卷积定理（Convolution Theorem）

### 7.1 卷积的定义

**向量卷积**：给定两个长度为n的向量a和b，它们的卷积c = a ⊗ b定义为：

```
cₖ = Σⱼ₌₀ᵏ aⱼ · bₖ₋ⱼ，k = 0, 1, ..., 2n-2
```

**卷积和多项式乘法的关系**：多项式乘法的系数计算**就是**卷积运算！

```
A(x)·B(x)的系数 = a ⊗ b
```

### 7.2 卷积定理

**定理**（卷积定理）：对任意两个长度为n的向量a和b（n是2的幂），将它们补零到2n长度后：

```
a ⊗ b = DFT⁻¹(DFT(ā) · DFT(b̄))
```

其中ā和b̄是补零后的向量，·表示逐元素相乘。

**意义**：卷积（O(n²)）可以通过FFT转化为三次O(n log n)的运算——DFT、逐点乘、逆DFT。

### 7.3 卷积的应用

| 应用 | 输入a | 输入b | 卷积c的含义 |
|------|-------|-------|------------|
| 多项式乘法 | A的系数 | B的系数 | 乘积的系数 |
| 大整数乘法 | 数字的系数 | 数字的系数 | 乘积的数字（需要处理进位） |
| 信号滤波 | 输入信号 | 滤波器冲激响应 | 滤波后的信号 |
| 图像卷积 | 图像像素 | 卷积核 | 特征提取/模糊/锐化 |
| 概率论 | 两个独立随机变量的分布 | | 和的分布 |
| 字符串匹配 | 文本特征 | 模式特征 | 匹配位置 |

### 7.4 大整数乘法的FFT实现

**核心思路**：把大整数看作多项式，用FFT做多项式乘法，再处理进位。

```
例如：123 × 456

把123看作多项式：A(x) = 3 + 2x + 1x²（个位在前）
把456看作多项式：B(x) = 6 + 5x + 4x²

多项式乘法（FFT加速）：
C(x) = 18 + 27x + 28x² + 13x³ + 4x⁴

处理进位（从低位到高位）：
  18 → 8，进位1
  27+1=28 → 8，进位2
  28+2=30 → 0，进位3
  13+3=16 → 6，进位1
  4+1=5 → 5

结果：56088
验证：123 × 456 = 56088 ✓
```

**Python实现**：

```python
def multiply_large_integers(a_str, b_str):
    """使用FFT进行大整数乘法"""
    # 将整数字符串转为系数数组（个位在前）
    a = [int(c) for c in reversed(a_str)]
    b = [int(c) for c in reversed(b_str)]
    
    # 多项式乘法（FFT加速）
    c = multiply_polynomials(a, b)
    
    # 处理进位
    carry = 0
    for i in range(len(c)):
        temp = c[i] + carry
        c[i] = temp % 10       # 当前位
        carry = temp // 10     # 进位
    
    # 处理剩余进位
    while carry > 0:
        c.append(carry % 10)
        carry //= 10
    
    # 去掉前导零（如果有的话）
    while len(c) > 1 and c[-1] == 0:
        c.pop()
    
    # 反转回高位在前
    result = ''.join(str(d) for d in reversed(c))
    return result

# 测试
print(multiply_large_integers("123", "456"))  # 56088
print(multiply_large_integers("999", "999"))  # 998001
```

---

## 8️⃣ FFT电路实现（30.3 FFT Circuits）

### 8.1 为什么需要电路实现？

在信号处理中，FFT需要在硬件中以极低延迟运行——软件实现的微秒级延迟都不够快，需要**纳秒级**的专用电路。

### 8.2 蝶形运算电路

```
蝶形运算电路图：

         ┌───────── u+t ──────── yₖ
u ──┬────┤
    │    └───────── u-t ──────── yₖ₊ₙ/₂
    │
v ──┤    ┌────────────────┐
    └────┤  ω (旋转因子)  │
         └────────────────┘
              │
              t = ω·v
              
电路元件：
  1个复数乘法器（ω·v）
  1个复数加法器（u+t）
  1个复数减法器（u-t）
  延迟：O(1)
```

### 8.3 并行FFT电路

FFT的分治结构天然适合并行：

```
n=8的FFT电路结构：

深度1(蝶形距离1)    深度2(蝶形距离2)    深度3(蝶形距离4)

[0]──┬──────────────[0]──┬──────────────[0]──┬───────── y₀
[4]──┘ 蝶形(ω₂⁰)  [2]──┘ 蝶形(ω₄⁰)  [1]──┘ 蝶形(ω₈⁰)
                    [1]──┬──────────────[2]──┬───────── y₁
[2]──┬──────────────[3]──┘ 蝶形(ω₄¹)  [5]──┘ 蝶形(ω₈¹)
[6]──┘ 蝶形(ω₂⁰)                         [3]──┬───────── y₂
                                         [6]──┘ 蝶形(ω₈²)
[1]──┬──────────────[4]──┬──────────────[4]──┬───────── y₄
[5]──┘ 蝶形(ω₂⁰)  [6]──┘ 蝶形(ω₄²)  [2]──┘ 蝶形(ω₈⁴)
...                                     ...

电路深度：log₂n = 3 级
每级延迟：O(1)
总延迟：O(log n)
```

**关键参数**：

| 参数 | 值 | 说明 |
|------|-----|------|
| 电路深度 | Θ(log n) | 最大级数（路径长度） |
| 电路规模 | Θ(n log n) | 总蝶形运算数量 |
| 每级蝶形数 | n/2 | 同时可并行执行 |
| 总计算元件 | (n/2)·log₂n | 每级n/2个蝶形，共log₂n级 |

### 8.4 位反转置换电路

输入需要先做位反转排序，这也可以用电路实现：

```
位反转网络：
0(000) ←→ 0(000)    不变
1(001) ←→ 4(100)    交换
2(010) ←→ 2(010)    不变
3(011) ←→ 6(110)    交换
4(100) ←→ 1(001)    交换
5(101) ←→ 5(101)    不变
6(110) ←→ 3(011)    交换
7(111) ←→ 7(111)    不变

实现：用一组2×2开关，根据地址位的比较决定是否交换
```

---

## 9️⃣ 综合案例分析

### 案例1：使用FFT进行信号频谱分析

**场景**：有一个由3个频率叠加的信号，用FFT把它分解出来。

```python
import cmath
import matplotlib
# 注意：这里仅展示算法，实际绘图需要matplotlib环境

def generate_signal(n=256):
    """生成叠加信号：50Hz + 120Hz + 噪声"""
    signal = [0.0] * n
    for i in range(n):
        t = i / 256.0  # 采样1秒，256个点
        signal[i] = (1.0 * cmath.sin(2 * cmath.pi * 50 * t) +    # 50Hz分量
                     0.5 * cmath.sin(2 * cmath.pi * 120 * t) +    # 120Hz分量
                     0.3 * (cmath.sin(2 * cmath.pi * i) * 0 + 1)) # 近似噪声
    return signal

def spectrum_analysis():
    """频谱分析"""
    signal = generate_signal(256)
    
    # 补零到256（已是2的幂）
    complex_signal = [complex(s, 0) for s in signal]
    
    # FFT变换到频域
    freq = fft(complex_signal, invert=False)
    
    # 计算幅度谱（每个频率分量的强度）
    magnitude = [abs(f) for f in freq[:128]]  # 只看前半（对称性）
    
    # 找到峰值频率
    peak_idx = max(range(128), key=lambda i: magnitude[i])
    peak_freq = peak_idx  # 因为采样率256Hz，256个点
    
    return peak_freq, magnitude

# 结果：幅度谱会在50Hz和120Hz处出现明显峰值
# 这就是FFT的威力——从时域"混乱"的信号中提取出频率信息
```

### 案例2：EDA中的FFT应用——信号完整性分析

在芯片设计中，信号在传输线上传播时会受到反射、串扰等影响。工程师需要分析信号的**频域特性**：

```
时域信号（示波器看到的波形）  ──FFT──→  频域频谱（频谱仪看到的）

EDA工具中的典型流程：
1. 仿真得到时域波形（电压随时间变化）
2. FFT转换到频域
3. 分析谐振频率、带宽、频谱泄漏
4. 设计滤波器/匹配网络来改善信号质量
5. 逆FFT回到时域验证改善效果

为什么需要FFT而不是直接在时域处理？
  - 频域中滤波 = 简单的乘法（乘以滤波器的频率响应）
  - 时域中滤波 = 复杂的卷积（O(n²)→O(n log n) with FFT）
  - 很多物理现象在频域更直观（共振、带宽、谐波）
```

---

## 🔟 本章小结

### 核心要点

1. **两种表示法**：
   - 系数表示：加法O(n)，乘法O(n²)
   - 点值表示：加法O(n)，乘法O(n)（但转换有代价）

2. **FFT的精髓**：
   - 选单位复数根作为求值点
   - 利用折半引理，n点DFT → 两个n/2点DFT
   - 蝶形运算合并， Θ(n)合并代价
   - 总复杂度 Θ(n log n)

3. **逆FFT**：
   - 和FFT结构相同，只需ωₙ→ωₙ⁻¹，最后÷n
   - 可以直接复用FFT代码

4. **多项式乘法完整流程**：
   - 补零→FFT→逐点乘→逆FFT
   - 总复杂度O(n log n)

5. **卷积定理**：a ⊗ b = DFT⁻¹(DFT(a)·DFT(b))

### 算法复杂度总结

| 操作 | 朴素方法 | FFT方法 | 加速比 |
|------|---------|---------|-------|
| DFT | O(n²) | O(n log n) | n/log n |
| 逆DFT | O(n²) | O(n log n) | n/log n |
| 多项式乘法 | O(n²) | O(n log n) | n/log n |
| 大整数乘法 | O(n²) | O(n log n) | n/log n |
| 卷积 | O(n²) | O(n log n) | n/log n |

### 易错点

1. **补零不充分**：多项式乘法需要2n个点值对，忘记补零会导致信息丢失
2. **n必须是2的幂**：如果不是，需要补零到最近的2的幂
3. **逆FFT忘记除以n**：这是最容易忘记的步骤
4. **浮点精度**：FFT结果理论上虚部为0，但实际可能有微小虚部（1e-10量级），需要取实部并四舍五入
5. **位反转序**：迭代FFT必须先做位反转置换，否则结果完全错误
6. **旋转因子方向**：正FFT用e^(2πi/n)，逆FFT用e^(-2πi/n)，弄反了结果就错

### 与前后章节的联系

| 方向 | 章节 | 联系 |
|------|------|------|
| 前置 | 第4章 分治策略 | FFT是分治策略的经典应用 |
| 前置 | 第28章 矩阵运算 | DFT本质是Vandermonde矩阵乘法 |
| 前置 | 第26章 最大流 | 流网络中某些优化可建模为LP（上章） |
| 后续 | 第31章 数论算法 | 大整数乘法、模运算中的NTT |
| 后续 | 第32章 字符串匹配 | 通配符匹配可用FFT加速 |
| 应用 | 信号处理 | FFT是最常用的算法之一 |
| 应用 | 机器学习 | CNN中的快速卷积 |

---

## 📝 课后练习

### 基础题

**练习1**：计算多项式 A(x) = 2 + 3x + x² 和 B(x) = 1 + 4x + 2x² 的乘积：
- (a) 用朴素方法（卷积），逐步写出每个系数
- (b) 用点值表示法，选择x = 0, 1, 2, 3, 4五个点

**练习2**：计算4点DFT，输入a = (1, 0, 1, 0)：
- (a) 写出V₄矩阵
- (b) 计算 y = V₄·a
- (c) 验证 ω₄² = -1

**练习3**：手动执行4点FFT（递归版），输入a = (1, 2, 3, 4)：
- (a) 写出奇偶拆分的结果
- (b) 递归计算子问题
- (c) 蝶形合并，得到最终DFT

### 进阶题

**练习4**：证明折半引理：如果n > 0是偶数，则{(ωₙ⁰)², (ωₙ¹)², ..., (ωₙⁿ⁻¹)²} = {ωₙ/₂⁰, ωₙ/₂¹, ..., ωₙ/₂ⁿ/²⁻¹}（每个出现两次）。

**练习5**：实现一个完整的FFT多项式乘法程序，测试以下用例：
```
A(x) = 1 + x + x² + x³ = (x⁴-1)/(x-1)
B(x) = 1 - x + x² - x³
A(x)·B(x) = ?
```
验证结果是否为1 + x² + x⁴ + x⁶。

**练习6**：用FFT实现大整数乘法，计算 2^64 - 1 和 2^32 + 1 的乘积（用字符串表示输入）。

### 挑战题

**练习7**：推广FFT到n是3的幂的情况（基数-3 FFT）。写出递推公式和递推关系式，分析复杂度。

**练习8**（EDA应用）：在VLSI设计中，芯片的功耗分布可以用热传导方程建模。离散化后，每一步迭代需要做一次2D卷积（大小n×n）。用2D FFT加速这个卷积运算，分析复杂度从多少降到多少。

**练习9**（信号处理应用）：实现一个简单的数字滤波器：
- 生成一个含噪声的信号（有用信号频率<100Hz，噪声频率>200Hz）
- 用FFT变换到频域
- 在频域中把>200Hz的分量置零（低通滤波）
- 用逆FFT变回时域
- 比较滤波前后的信号质量

---

## 📚 延伸阅读

| 主题 | 推荐资源 | 说明 |
|------|---------|------|
| FFT直觉理解 | 3Blue1Brown「But what is the Fourier Transform?」 | 视频可视化，直觉清晰 |
| FFT数学推导 | Oppenheim & Willsky, *Signals and Systems* | 信号处理经典教材 |
| 数论变换(NTT) | 《信息学竞赛中的数论变换》 | 避免浮点误差的FFT变体 |
| FFT工程实现 | Frigo & Johnson, FFTW | 最快的FFT软件库 |
| 多项式算法 | Borodin & Munro, *The Computational Complexity of Algebraic and Numeric Problems* | 多项式计算的理论基础 |
| 卷积与FFT | Brigham, *The Fast Fourier Transform and Its Applications* | 卷积应用的百科全书 |
| EDA中的频域分析 | Najm, *Circuit Simulation* | 电路仿真中的FFT应用 |

> **最后一句话**：FFT是算法世界里最优雅的"降维打击"——它告诉我们，当你选对了坐标系（系数→点值），又选对了求值点（单位复数根），一个O(n²)的"硬算"问题就能被拆成O(n log n)的"巧算"。蝶形运算像蝴蝶一样轻盈，却在每一次振翅间完成了两个结果的计算——一加一减，没有浪费。从MP3压缩到5G通信，从MRI成像到深度学习，FFT无处不在。理解了它，你就掌握了从时域到频域的"任意门"🔑
