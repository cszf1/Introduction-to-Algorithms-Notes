# 第36章 算法视角下的机器学习（Machine-Learning Algorithms）

> 本章回答一个核心问题：传统算法是"人为设计规则"，机器学习是"让数据自己说话"——那从算法的视角来看，机器学习到底在做什么？它和我们在前面35章里学到的算法有什么本质联系和区别？本章从三个入口切入：聚类（无监督地发现数据中的分组结构）、加权多数算法（在线地从专家建议中学习）、梯度下降（通过迭代优化找到函数的最小值）。它们不是机器学习的全部，但代表了三种最核心的算法思想：发现结构、追踪最优、优化目标

## 📖 本章导读

### 🎯 问题引入：为什么算法研究者需要关心机器学习？

**生活中的例子**：

想象你是一个**医院的主治医生**：
- 每天来看病的病人，你根据自己的经验（规则）开药——这是**传统算法**：人写规则，程序执行
- 但你发现，有些病人的症状组合你从没见过，不知道怎么开药
- 这时你翻看病历库，发现过去1000个类似病人用了A药效果很好——这是**机器学习**：从数据中自动总结规律

再想想**推荐系统**：
- 传统的做法：写规则"如果用户买过手机，就推荐手机壳"——规则有限，覆盖不全
- 机器学习的做法：让算法从几百万用户的购买记录中自动发现"买了手机的人通常还会买X"——数据驱动，自动发现

**关键洞察**：传统算法解决的是"给定明确规则，如何高效执行"；机器学习解决的是"规则本身不确定，如何从数据中自动学习规则"。

```
传统算法 vs 机器学习算法：

  传统算法：人写规则，程序执行
  ┌──────────────────────────────┐
  │  输入 ──→ 规则(人写) ──→ 输出  │
  │  排序   → 比较交换     → 有序  │
  │  搜索   → 逐个/二分    → 找到  │
  └──────────────────────────────┘

  机器学习：数据驱动，自动学习规则
  ┌──────────────────────────────┐
  │  数据 ──→ 学习算法 ──→ 模型   │
  │  病历   → 聚类/分类   → 诊断  │
  │  购买   → 关联/推荐   → 预测  │
  └──────────────────────────────┘
```

**为什么这很重要？**

主人正在学习AI for EDA方向，核心就是用机器学习辅助芯片设计。理解ML的算法本质，而不是把ML当黑盒用，是区分"调参侠"和"算法工程师"的关键。本章从算法导论的视角解读ML，正是主人需要的"算法思维看ML"的入门。

| 维度 | 传统算法 | 机器学习算法 |
|------|---------|------------|
| 输入 | 数据+明确规则 | 数据+学习框架 |
| 输出 | 确定答案 | 近似答案（概率性的） |
| 核心 | 如何高效执行 | 如何从数据中学习 |
| 精确性 | 精确解 | 近似解 |
| 适用场景 | 规则明确的问题 | 规则不确定的问题 |

### 💡 机器学习三大范式速览

**生活类比**：三种学习方式
- **监督学习**：老师手把手教你——有标准答案，对着答案学
- **无监督学习**：自己看书摸索——没有标准答案，自己找规律
- **在线学习**：边做边学——每次做完才知道对错，不断调整

```
机器学习三大范式：

  监督学习（Supervised Learning）
  ┌────────────────────────┐
  │  输入：带标签的数据        │
  │  目标：学习输入→输出的映射 │
  │  例子：图片→"猫/狗"      │
  └────────────────────────┘

  无监督学习（Unsupervised Learning）
  ┌────────────────────────┐
  │  输入：无标签的数据        │
  │  目标：发现数据的内在结构  │
  │  例子：客户分群（聚类）    │
  └────────────────────────┘

  在线学习（Online Learning）
  ┌────────────────────────┐
  │  输入：逐个到来的数据+反馈 │
  │  目标：边做边学，减少错误  │
  │  例子：跟着专家建议做决策  │
  └────────────────────────┘
```

### 📊 本章核心问题一览

| 小节 | 问题 | 核心算法 | 关键性质 | 类比 |
|------|------|---------|---------|------|
| 36.1 | 聚类 | K-Means | 收敛到局部最优 | 把学生分成k组 |
| 36.2 | 在线学习（专家建议） | 加权多数算法 | 错误数 ≤ 2(1+ε)·最优+O(log n/ε) | 跟着投资顾问学 |
| 36.3 | 优化 | 梯度下降 | 凸函数→全局最优 | 下山找谷底 |

**与前面章节的关系**：

| 对比项 | 第15章 贪心算法 | 第36章 机器学习算法 |
|--------|---------------|-------------------|
| K-Means的分配步骤 | 贪心选择：每个点选最近的中心 | 相同思想 |
| K-Means的更新步骤 | 局部优化 | 交替优化（坐标下降） |
| 加权多数 | 类似"淘汰赛"但更温和 | 权重衰减而非直接淘汰 |

| 对比项 | 第35章 近似算法 | 第36章 机器学习算法 |
|--------|---------------|-------------------|
| 近似比 | 量化近似解的质量 | 量化聚类/在线学习的质量 |
| 局部最优 | 可能不是全局最优 | K-Means同样只保证局部最优 |
| 随机化 | 8/7期望近似比 | 随机化加权多数：(1+ε)OPT+O(log n/ε) |

| 对比项 | 第29章 线性规划 | 第36章 梯度下降 |
|--------|---------------|----------------|
| 优化对象 | 线性目标函数 | 一般可微函数 |
| 方法 | 单纯形法/内点法 | 梯度下降法 |
| 约束 | 线性约束 | 可通过投影处理 |

**本章路线图**：

```
聚类(36.1) ──→ 加权多数算法(36.2) ──→ 梯度下降(36.3)
 "发现结构"      "追踪最优"            "优化目标"
 K-Means迭代     Halving→WM→RWM        批量→随机→小批量
 无监督学习       在线学习               优化算法
```

---

## 1️⃣ 聚类（36.1 Clustering）

### 🎯 核心问题：把相似的东西分到一起

**生活类比**：

想象你是**大学新生报到处的老师**：
- 来了1000个新生，你需要把他们分到若干个宿舍
- 你希望：性格相似的住在一起（组内尽量"近"），不同性格的分开住（组间尽量"远"）
- 你不知道每个人属于哪个"类型"——这就是**无监督**的含义
- 你只需要指定"分几个宿舍"（k），然后让算法自动分组——这就是**K-Means**

再想想**超市货架布局**：
- 你有1000种商品，想分成几个区域
- 同类商品（牛奶、酸奶、奶酪）应该放在一起
- 不同类商品（水果和日用品）应该分开
- 聚类算法就是在做这件事

```
聚类问题的直觉：

  给定一组数据点（没有标签！），找出"自然分组"

  原始数据：          聚类后：
  ·  ·     · ·       A A     B B
  · · ·    · ·       A A A   B B
   · ·     · ·        A A    B B

  同一组的点彼此"近"
  不同组的点彼此"远"
```

### 📐 K-Means算法：最经典的聚类算法

**K-Means的核心思想**：交替优化——先固定中心找分配，再固定分配找中心

**算法步骤**（Lloyd算法）：

```
K-MEANS(data, k):
  1. 随机选择k个点作为初始中心 μ₁, μ₂, ..., μₖ
  2. repeat:
     3. 分配步骤：对每个数据点 xᵢ，将其分配到最近的中心
        Cⱼ = {xᵢ : ||xᵢ - μⱼ|| ≤ ||xᵢ - μₗ||, ∀l ≠ j}
     4. 更新步骤：重新计算每个簇的中心（取均值）
        μⱼ = (1/|Cⱼ|) Σ_{xᵢ∈Cⱼ} xᵢ
  5. until 中心不再变化（收敛）
```

**过程图解**：

```
Step 0: 初始化（随机选2个中心 μ₁, μ₂）

  数据点：          随机中心：
  · ·               μ₁=★₁
    · · ·
  · · ·               μ₂=★₂
    · ·

Step 1: 分配（每个点归入最近中心）

  属于μ₁的点(A)：    属于μ₂的点(B)：
  A A
    A · B          · = 不确定
  A · · B            B = 属于μ₂
    · B

Step 2: 更新中心（取每组的均值）

  新μ₁ = A组中心      新μ₂ = B组中心
  ★₁移到新位置        ★₂移到新位置

Step 3: 重新分配...

  重复直到收敛！
```

**Python实现**（逐行注释）：

```python
import numpy as np

def k_means(X, k, max_iters=100):
    """
    K-Means聚类算法
    
    参数:
        X: numpy数组, shape=(n_samples, n_features), 数据集
        k: int, 簇的数量
        max_iters: int, 最大迭代次数
    返回:
        labels: 每个数据点所属的簇编号
        centroids: 最终的簇中心
    """
    n_samples = X.shape[0]  # 数据点数量
    
    # Step 1: 随机选择k个数据点作为初始中心
    # 从n_samples个点中等概率地选k个，replace=False表示不重复选
    indices = np.random.choice(n_samples, k, replace=False)
    centroids = X[indices].copy()  # 复制出来作为初始中心
    
    labels = np.zeros(n_samples, dtype=int)  # 每个点的簇标签，初始化为0
    
    for iteration in range(max_iters):
        # Step 2: 分配步骤——每个点归入最近的中心
        for i in range(n_samples):
            # 计算第i个点到所有k个中心的距离
            # np.linalg.norm计算欧几里得距离
            distances = [np.linalg.norm(X[i] - centroids[j]) for j in range(k)]
            # argmin找到最小距离对应的簇编号
            labels[i] = np.argmin(distances)
        
        # Step 3: 更新步骤——重新计算每个簇的中心
        new_centroids = np.zeros_like(centroids)  # 预分配空间
        for j in range(k):
            # 找到属于第j簇的所有点
            cluster_points = X[labels == j]
            if len(cluster_points) > 0:
                # 新中心 = 该簇所有点的均值（这就是"Means"的含义）
                new_centroids[j] = cluster_points.mean(axis=0)
            else:
                # 如果某个簇为空，保持原中心不变
                new_centroids[j] = centroids[j]
        
        # Step 4: 检查是否收敛（中心不再变化）
        if np.allclose(centroids, new_centroids):
            print(f"在第{iteration+1}轮收敛")  # 打印收敛信息
            break
        
        centroids = new_centroids  # 更新中心，进入下一轮
    
    return labels, centroids


# 使用示例
if __name__ == "__main__":
    # 生成测试数据：3个高斯分布的簇
    np.random.seed(42)
    cluster1 = np.random.randn(30, 2) + np.array([2, 2])    # 中心在(2,2)的簇
    cluster2 = np.random.randn(30, 2) + np.array([-2, -2])  # 中心在(-2,-2)的簇
    cluster3 = np.random.randn(30, 2) + np.array([2, -2])   # 中心在(2,-2)的簇
    X = np.vstack([cluster1, cluster2, cluster3])            # 合并成90×2的数据集
    
    labels, centroids = k_means(X, k=3)  # 运行K-Means，分3簇
    print(f"簇中心: {centroids}")
```

**C++实现**（逐行注释）：

```cpp
#include <iostream>
#include <vector>
#include <cmath>
#include <cstdlib>
#include <ctime>
#include <limits>
#include <algorithm>

using namespace std;

// 二维点结构体
struct Point {
    double x, y;
    // 计算到另一个点的欧几里得距离
    double distanceTo(const Point& other) const {
        double dx = x - other.x;  // x坐标差
        double dy = y - other.y;  // y坐标差
        return sqrt(dx * dx + dy * dy);  // 欧几里得距离
    }
};

// K-Means聚类算法
void kMeans(vector<Point>& data, int k, int max_iters = 100) {
    int n = data.size();  // 数据点数量
    
    // Step 1: 随机选择k个数据点作为初始中心
    vector<Point> centroids(k);
    srand(time(0));  // 设置随机种子
    vector<int> indices(n);
    for (int i = 0; i < n; i++) indices[i] = i;  // 初始化索引数组
    random_shuffle(indices.begin(), indices.end());  // 随机打乱
    for (int j = 0; j < k; j++) {
        centroids[j] = data[indices[j]];  // 取前k个作为初始中心
    }
    
    vector<int> labels(n, 0);  // 每个点的簇标签，初始化为0
    
    for (int iter = 0; iter < max_iters; iter++) {
        bool changed = false;  // 标记本轮是否有点的归属发生变化
        
        // Step 2: 分配步骤——每个点归入最近的中心
        for (int i = 0; i < n; i++) {
            double min_dist = numeric_limits<double>::max();  // 初始化最小距离为无穷大
            int best_cluster = 0;  // 最佳簇编号
            
            for (int j = 0; j < k; j++) {
                double dist = data[i].distanceTo(centroids[j]);  // 到第j个中心的距离
                if (dist < min_dist) {
                    min_dist = dist;      // 更新最小距离
                    best_cluster = j;     // 更新最佳簇编号
                }
            }
            
            if (labels[i] != best_cluster) {
                labels[i] = best_cluster;  // 更新簇标签
                changed = true;             // 标记发生了变化
            }
        }
        
        // 如果没有点改变归属，说明已收敛
        if (!changed) {
            cout << "在第" << iter + 1 << "轮收敛" << endl;
            break;
        }
        
        // Step 3: 更新步骤——重新计算每个簇的中心
        for (int j = 0; j < k; j++) {
            double sum_x = 0, sum_y = 0;  // 簇内坐标之和
            int count = 0;                 // 簇内点数
            
            for (int i = 0; i < n; i++) {
                if (labels[i] == j) {      // 属于第j簇的点
                    sum_x += data[i].x;
                    sum_y += data[i].y;
                    count++;
                }
            }
            
            if (count > 0) {
                // 新中心 = 均值
                centroids[j].x = sum_x / count;
                centroids[j].y = sum_y / count;
            }
            // 如果簇为空，保持原中心不变
        }
    }
    
    // 输出结果
    cout << "簇中心:" << endl;
    for (int j = 0; j < k; j++) {
        cout << "  簇" << j << ": (" << centroids[j].x 
             << ", " << centroids[j].y << ")" << endl;
    }
}

int main() {
    // 生成测试数据（3个簇）
    vector<Point> data;
    srand(time(0));
    
    // 簇1：中心(2,2)
    for (int i = 0; i < 30; i++)
        data.push_back({2.0 + (rand()%100-50)/50.0, 2.0 + (rand()%100-50)/50.0});
    // 簇2：中心(-2,-2)
    for (int i = 0; i < 30; i++)
        data.push_back({-2.0 + (rand()%100-50)/50.0, -2.0 + (rand()%100-50)/50.0});
    // 簇3：中心(2,-2)
    for (int i = 0; i < 30; i++)
        data.push_back({2.0 + (rand()%100-50)/50.0, -2.0 + (rand()%100-50)/50.0});
    
    kMeans(data, 3);  // 运行K-Means，分3簇
    return 0;
}
```

### 📐 K-Means的代价函数与收敛性

**代价函数**（也叫惯性/Inertia）：

```
J(C, μ) = Σⱼ₌₁ᵏ Σ_{xᵢ∈Cⱼ} ||xᵢ - μⱼ||²

含义：所有数据点到其所属簇中心的距离平方之和
目标：最小化J

直觉：组内越"紧"，J越小
```

**为什么K-Means会收敛？**

```
关键观察：每一轮迭代，J严格递减（或不变）

  分配步骤：固定中心，找最优分配 → J不会增加
  更新步骤：固定分配，找最优中心 → J不会增加

  因为J有下界（≥0），且每步递减，所以算法必然收敛。

  ⚠️ 但注意：收敛到的是局部最优，不一定是全局最优！
```

**生活类比**：K-Means的局部最优
- 想象你在山中找最低的谷底
- 你每步都往下走（梯度下降的思想），但可能走到一个小坑就停了
- 全局最低点在山的另一边，但你走不到
- K-Means也一样：不同的初始中心可能导致不同的结果

```
全局最优 vs 局部最优：

  代价函数J的地形图：
  
  J ↑
    |  \  /\
    |   \/  \  ← 局部最优（小坑）
    |    \   \
    |     \   \/ ← 全局最优（最深谷底）
    |      \ /
    +──────────→ 参数空间
    
  K-Means可能停在小坑，而不是最深谷底
```

### 🔍 K-Means的时间复杂度

```
每轮迭代：O(n·k·d)
  n = 数据点数量
  k = 簇数量
  d = 数据维度

总轮数：通常O(1)到O(n)不等，实践中很快收敛

总体：O(n·k·d·T)，T为迭代次数
```

| 数据规模 | k | d | 每轮耗时估计 |
|---------|---|---|------------|
| 1000点 | 5 | 2 | 极快 |
| 10000点 | 10 | 100 | 毫秒级 |
| 1000000点 | 100 | 1000 | 秒级 |

### ❓ FAQ：聚类常见疑问

**Q1：k怎么选？**
> A：这是K-Means最头疼的问题。常用方法：
> - **肘部法则（Elbow Method）**：画k-J曲线，选"拐弯"处的k
> - **轮廓系数（Silhouette Score）**：衡量组内紧、组间开的程度
> - **领域知识**：如果你知道数据应该分几类，直接用
> - 没有完美的自动方法，这是无监督学习的本质困难

**Q2：初始中心选不好怎么办？**
> A：K-Means++初始化方法：
> - 第1个中心随机选
> - 之后每个新中心，优先选离已有中心远的点
> - 具体来说，每个点被选为新中心的概率正比于它到最近中心的距离平方
> - 这样能避免初始中心都挤在一起的情况

**Q3：簇的形状必须是球形的吗？**
> A：K-Means隐含假设簇是球形的（因为用欧几里得距离），所以对非球形分布（如环形、月牙形）效果不好。可以改用DBSCAN（基于密度的聚类）或谱聚类。

---

## 2️⃣ 加权多数算法（36.2 Weighted-Majority Algorithms）

### 🎯 核心问题：如何从专家建议中学习？

**生活类比**：

想象你是一个**股民**：
- 你订阅了n个投资顾问的每日推荐（"买"或"卖"）
- 每天你需要决定：听谁的？
- 你不知道哪个顾问最靠谱，但假设至少有一个还不错
- 你的目标：犯错次数不要比最好的顾问多太多

这就是**在线学习（Online Learning）**的核心设定：
- 数据逐个到来（不是一次性全给你）
- 每次必须做决策（不能等看完所有数据再决定）
- 做完决策后才能看到结果对不对

```
在线学习 vs 批量学习：

  批量学习（如K-Means）：
  ┌──────────────────────────────────┐
  │  所有数据 ──→ 一次性训练 ──→ 模型  │
  │  "先看完书再考试"                  │
  └──────────────────────────────────┘

  在线学习（如加权多数算法）：
  ┌──────────────────────────────────┐
  │  数据₁ ──→ 预测 ──→ 看答案       │
  │  数据₂ ──→ 预测 ──→ 看答案       │
  │  数据₃ ──→ 预测 ──→ 看答案       │
  │  ...                               │
  │  "边做题边对答案边学习"            │
  └──────────────────────────────────┘
```

### 📐 Halving算法：完美专家的简单情况

**假设**：n个专家中，至少有一个是完美的（从不出错）

**算法思路**：跟着多数走，淘汰错误者

```
HALVING(experts, T):
  1. S = {1, 2, ..., n}  // 初始时所有专家都在候选集中
  2. for t = 1 to T:
     3. 预测：跟随S中多数专家的预测
     4. 观察正确答案
     5. 从S中删除本轮预测错误的专家
  6. return 预测序列
```

**过程图解**：

```
4个专家 E₁, E₂, E₃, E₄，其中E₂是完美的

第1轮：
  E₁预测: ↑    E₂预测: ↓(✓)   E₃预测: ↑    E₄预测: ↑
  多数预测: ↑（3:1）  → 实际: ↓  → 算法犯错！
  淘汰E₁（预测↑错了）  → S = {E₂, E₃, E₄}

第2轮：
  E₂预测: ↑(✓)  E₃预测: ↑    E₄预测: ↓
  多数预测: ↑（2:1）  → 实际: ↑  → 算法正确！
  无淘汰

第3轮：
  E₂预测: ↓(✓)  E₃预测: ↑    E₄预测: ↑
  多数预测: ↑（2:1）  → 实际: ↓  → 算法犯错！
  淘汰E₃、E₄  → S = {E₂}

之后：S中只剩E₂，算法跟随E₂，永不出错！

总犯错：2次 ≤ ⌈log₂4⌉ = 2次 ✓
```

**Halving算法的保证**：

```
定理：如果存在一个完美专家，Halving算法最多犯 ⌈log₂n⌉ 次错误。

证明思路：
  每次犯错时，多数专家预测错误
  → 至少一半的候选专家被淘汰
  → 候选集大小至少减半
  → 最多log₂n次减半后，只剩完美专家
  → 之后永不出错
```

### 📐 加权多数算法：不完美专家的进阶版

**问题**：Halving算法假设存在完美专家，但现实中没有完美专家！

**生活类比**：
- Halving：一个顾问犯错就拉黑——太极端了
- 加权多数：一个顾问犯错就降低信任度——更合理

**算法思路**：给每个专家一个权重，犯错就降权，按权重投票

```
WEIGHTED-MAJORITY(experts, η, T):
  1. 对每个专家i，初始化权重 wᵢ = 1
  2. for t = 1 to T:
     3. 预测：按权重投票，选权重更大的那一方
        预测0如果 Σ_{i预测0} wᵢ ≥ Σ_{i预测1} wᵢ，否则预测1
     4. 观察正确答案
     5. 对每个预测错误的专家i，降低其权重：
        wᵢ ← (1 - η) · wᵢ     （η ∈ (0, 1/2] 是惩罚参数）
  6. return 预测序列
```

**过程图解**：

```
3个专家，η = 1/2（犯错就减半权重），二元预测（0或1）

初始：w₁=1, w₂=1, w₃=1

第1轮：E₁预测1, E₂预测0, E₃预测1
  权重和：预测1的w=1+1=2，预测0的w=1
  算法预测1 → 实际0 → 算法犯错！
  E₁和E₃预测错了，w₁=1/2, w₃=1/2, w₂=1不变

第2轮：E₁预测0, E₂预测1, E₃预测0
  权重和：预测0的w=1/2+1/2=1，预测1的w=1
  算法预测1 → 实际1 → 算法正确！
  无权重更新

第3轮：E₁预测1, E₂预测0, E₃预测1
  权重和：预测1的w=1/2+1/2=1，预测0的w=1
  算法预测1 → 实际0 → 算法犯错！
  E₁和E₃又错了，w₁=1/4, w₃=1/4, w₂=1

...可以看到E₂一直没错，权重保持1，影响力越来越大
```

**加权多数算法的保证**：

```
定理：令M为算法的犯错次数，m*为最佳专家的犯错次数。
      则 M ≤ 2(1+η)·m* + (2/η)·ln(n)

当η = 1/2时：M ≤ 3·m* + 4·ln(n)

直觉：
  2(1+η)·m*：因为我们是"跟多数走"，最坏情况下犯错数
              大约是最优专家的2倍（常数倍）
  (2/η)·ln(n)：与专家数n的对数成正比，n越大越多一点
              但增长很慢（对数级）
```

**证明核心思路**（势函数法）：

```
定义势函数 Φ = Σᵢ wᵢ（所有专家权重之和）

1. 初始：Φ₁ = n
2. 每次算法犯错时：
   - 多数方（我们选的）是错的
   - 所以至少一半的权重在错误方
   - 错误方的权重乘以(1-η)，至少减少η/2的比例
   - Φ_{t+1} ≤ Φ_t · (1 - η/2)

3. T轮后：Φ_T ≤ n · (1 - η/2)^M

4. 另一方面，最佳专家i*的权重：
   w_{i*} = (1-η)^{m*} ≥ 0

5. 因为 w_{i*} ≤ Φ_T：
   (1-η)^{m*} ≤ n · (1 - η/2)^M

6. 取对数并利用近似：
   m* · ln(1-η) ≥ ln(n) + M · ln(1-η/2)
   -m*·η ≥ ln(n) - M·η/2     （用 -x-x² ≤ ln(1-x) ≤ -x）
   M ≤ 2(1+η)·m* + 2·ln(n)/η
```

### 📐 随机化加权多数算法：突破2倍因子

**问题**：确定性算法最多做到2倍，能不能更好？

**关键洞察**：2倍因子是因为"确定性"——对手总能猜到你会选什么，然后让你错。

**生活类比**：
- 确定性：你总是听多数人的——对手可以故意让多数人错
- 随机化：你按权重比例随机选——对手无法确定你会选谁

```
确定性 vs 随机化：

  确定性加权多数：
  ┌────────────────────────────────┐
  │  算法总是选权重和更大的那一边    │
  │  → 对手可以构造序列让你总是错    │
  │  → 最坏情况：犯错 = 2 × 最优     │
  └────────────────────────────────┘

  随机化加权多数：
  ┌────────────────────────────────┐
  │  按权重比例随机选一边            │
  │  → 对手无法确定你会选什么        │
  │  → 期望犯错 = (1+ε) × 最优      │
  │  → 倍数从2降到1+ε！             │
  └────────────────────────────────┘
```

**随机化加权多数算法**：

```
RANDOMIZED-WEIGHTED-MAJORITY(experts, ε, T):
  1. 对每个专家i，初始化权重 wᵢ = 1
  2. for t = 1 to T:
     3. 计算概率分布：pᵢ = wᵢ / Σⱼ wⱼ
     4. 按概率{pᵢ}随机选一个专家，跟随其预测
     5. 观察正确答案
     6. 对每个预测错误的专家i：
        wᵢ ← wᵢ · (1 - ε)    （ε ∈ (0, 1)）
  7. return 预测序列
```

**随机化加权多数算法的保证**：

```
定理：令E[M]为算法期望犯错次数，m*为最佳专家犯错次数。
      E[M] ≤ (1+ε)·m* + ln(n)/ε

对比确定性版本：M ≤ 2(1+ε)·m* + 2·ln(n)/ε
→ 倍数从2(1+ε)降到(1+ε)！

当取ε = √(ln(n)/m*)时（平衡两项）：
  E[M] ≤ m* + 2√(m*·ln(n))

这意味着：算法犯错数只比最优专家多 O(√(m*·ln n)) 个！
```

**Python实现**（随机化加权多数算法，逐行注释）：

```python
import numpy as np

class RandomizedWeightedMajority:
    """随机化加权多数算法"""
    
    def __init__(self, n_experts, epsilon):
        """
        参数:
            n_experts: int, 专家数量
            epsilon: float, 惩罚参数，∈(0, 1)
        """
        self.n_experts = n_experts    # 专家数量
        self.epsilon = epsilon         # 惩罚参数
        self.weights = np.ones(n_experts)  # 初始权重全为1
        self.total_mistakes = 0        # 算法累计犯错次数
        self.expert_mistakes = np.zeros(n_experts)  # 每个专家累计犯错次数
    
    def predict(self, expert_predictions):
        """
        根据专家预测，按概率随机选择一个专家的预测
        
        参数:
            expert_predictions: list, 每个专家的预测（0或1）
        返回:
            prediction: int, 算法的预测
            chosen_expert: int, 选中的专家编号
        """
        # 计算归一化的概率分布
        total_weight = np.sum(self.weights)  # 总权重
        probabilities = self.weights / total_weight  # 每个专家被选中的概率
        
        # 按概率随机选一个专家
        chosen_expert = np.random.choice(self.n_experts, p=probabilities)
        prediction = expert_predictions[chosen_expert]  # 跟随选中专家的预测
        
        return prediction, chosen_expert
    
    def update(self, expert_predictions, true_label):
        """
        观察真实标签后更新权重
        
        参数:
            expert_predictions: list, 每个专家的预测
            true_label: int, 真实标签（0或1）
        """
        for i in range(self.n_experts):
            if expert_predictions[i] != true_label:
                # 专家i预测错了，降低其权重
                self.weights[i] *= (1 - self.epsilon)
                self.expert_mistakes[i] += 1  # 记录犯错


# 使用示例
if __name__ == "__main__":
    np.random.seed(42)
    n_experts = 5       # 5个专家
    n_rounds = 100      # 100轮
    epsilon = 0.1       # 惩罚参数
    
    rwm = RandomizedWeightedMajority(n_experts, epsilon)
    
    # 模拟：第2个专家只有10%的犯错率，其他专家50%犯错率
    for t in range(n_rounds):
        # 生成专家预测
        expert_preds = []
        for i in range(n_experts):
            if i == 1:  # 第2个专家比较靠谱
                expert_preds.append(np.random.choice([0, 1], p=[0.1, 0.9]))
            else:
                expert_preds.append(np.random.choice([0, 1]))
        
        # 真实标签（与好专家高度一致）
        true_label = np.random.choice([0, 1], p=[0.1, 0.9])
        
        # 算法预测
        pred, chosen = rwm.predict(expert_preds)
        if pred != true_label:
            rwm.total_mistakes += 1
        
        # 更新权重
        rwm.update(expert_preds, true_label)
    
    print(f"算法犯错次数: {rwm.total_mistakes}")
    print(f"最佳专家犯错次数: {min(rwm.expert_mistakes)}")
    print(f"理论界: (1+ε)·m* + ln(n)/ε = {(1+epsilon)*min(rwm.expert_mistakes) + np.log(n_experts)/epsilon:.1f}")
```

**C++实现**（加权多数算法，逐行注释）：

```cpp
#include <iostream>
#include <vector>
#include <cmath>
#include <cstdlib>
#include <ctime>
#include <algorithm>

using namespace std;

class WeightedMajority {
private:
    int n_experts;              // 专家数量
    double eta;                  // 惩罚参数η
    vector<double> weights;      // 每个专家的权重
    int algorithm_mistakes;      // 算法累计犯错次数
    vector<int> expert_mistakes; // 每个专家累计犯错次数

public:
    WeightedMajority(int n, double eta_val) 
        : n_experts(n), eta(eta_val), 
          weights(n, 1.0),          // 初始权重全为1
          algorithm_mistakes(0),
          expert_mistakes(n, 0) {}
    
    // 预测：按加权多数投票
    int predict(const vector<int>& expert_preds) {
        double weight_for_0 = 0, weight_for_1 = 0;  // 预测0和1的权重和
        
        for (int i = 0; i < n_experts; i++) {
            if (expert_preds[i] == 0) {
                weight_for_0 += weights[i];  // 预测0的专家权重累加
            } else {
                weight_for_1 += weights[i];  // 预测1的专家权重累加
            }
        }
        
        // 选权重和更大的那一边
        return (weight_for_1 >= weight_for_0) ? 1 : 0;
    }
    
    // 更新：惩罚犯错的专家
    void update(const vector<int>& expert_preds, int true_label) {
        // 先判断算法是否犯错
        int algo_pred = predict(expert_preds);
        if (algo_pred != true_label) {
            algorithm_mistakes++;  // 算法犯错
        }
        
        // 惩罚预测错误的专家
        for (int i = 0; i < n_experts; i++) {
            if (expert_preds[i] != true_label) {
                weights[i] *= (1 - eta);     // 降低权重：乘以(1-η)
                expert_mistakes[i]++;        // 记录犯错
            }
        }
    }
    
    // 输出统计信息
    void print_stats() {
        cout << "算法犯错次数: " << algorithm_mistakes << endl;
        int best_mistakes = *min_element(expert_mistakes.begin(), expert_mistakes.end());
        cout << "最佳专家犯错次数: " << best_mistakes << endl;
        cout << "理论上界: " << 2*(1+eta)*best_mistakes + 2*log(n_experts)/eta << endl;
    }
};

int main() {
    srand(time(0));
    
    int n_experts = 5;    // 5个专家
    double eta = 0.5;     // 惩罚参数
    int n_rounds = 100;   // 100轮
    
    WeightedMajority wm(n_experts, eta);
    
    for (int t = 0; t < n_rounds; t++) {
        vector<int> expert_preds(n_experts);
        
        // 生成专家预测
        for (int i = 0; i < n_experts; i++) {
            if (i == 1) {  // 第2个专家比较靠谱
                expert_preds[i] = (rand() % 10 < 8) ? 1 : 0;  // 80%预测1
            } else {
                expert_preds[i] = rand() % 2;  // 50%概率
            }
        }
        
        // 真实标签
        int true_label = (rand() % 10 < 8) ? 1 : 0;  // 80%为1
        
        // 更新
        wm.update(expert_preds, true_label);
    }
    
    wm.print_stats();
    return 0;
}
```

### 📐 加权多数算法与AdaBoost的联系

**重要洞察**：加权多数算法是AdaBoost的理论基础！

```
加权多数算法 → AdaBoost的思路：

  加权多数算法：
    维护专家权重，犯错就降权
  
  AdaBoost：
    维护样本权重，分错就提权
    训练弱分类器，按加权多数投票组合

  本质上是同一个"乘法权重更新"框架！
```

| 对比项 | 加权多数算法 | AdaBoost |
|--------|------------|---------|
| 权重对象 | 专家 | 训练样本 |
| 更新方向 | 降权（犯错→降权） | 升权（分错→升权） |
| 目标 | 追踪最佳专家 | 组合弱分类器为强分类器 |
| 核心数学 | 乘法权重更新 | 乘法权重更新 |
| 理论保证 | 错误数 ≤ (1+ε)·OPT + O(log n/ε) | 训练误差指数下降 |

### ❓ FAQ：加权多数算法常见疑问

**Q1：η（或ε）怎么选？**
> A：如果知道最优专家犯错数m*，取ε = √(ln(n)/m*)平衡两项。如果不知道m*，可以用"倍增法"：先猜一个小值，如果实际犯错超出预期就调大。

**Q2：为什么确定性算法最多做到2倍？**
> A：考虑2个专家，一个总说0，一个总说1。你确定性地选0，对手就让你错（答案是1）；你确定性地选1，对手也让你错（答案是0）。最优专家只错一半，你却全错——2倍差距。

**Q3：这个算法和"多臂老虎机"（Multi-Armed Bandit）有什么区别？**
> A：核心区别在于**信息量**：
> - 加权多数（完全信息）：每轮能看到所有专家的预测和结果
> - 多臂老虎机（部分信息）：每轮只能看到你选的那个臂的结果
> - 多臂老虎机更难，因为信息更少，需要"探索vs利用"的平衡

---

## 3️⃣ 梯度下降（36.3 Gradient Descent）

### 🎯 核心问题：如何找到一个函数的最小值？

**生活类比**：

想象你**蒙着眼睛下山**：
- 你站在山上某处，看不见全景
- 你只能用脚感知脚下的坡度（梯度）
- 哪边最陡往下走哪边——这就是**梯度下降**
- 走到坡度≈0的地方，你就到了谷底——这就是**最小值**

再想想**调整收音机频道**：
- 你想找到最清晰的位置
- 你往一个方向拧，如果声音变好（信号变强）就继续
- 如果变差（噪声变大）就反方向拧
- 不断微调直到最清晰——这就是梯度下降的逐次逼近思想

```
梯度下降的直觉：

  你在山上（高维空间中的一个点）
  每一步沿着最陡的下坡方向走
  
  J(θ) ↑
       |    ╲
       |     ╲  ← 起点高
       |      ╲
       |       ╲___  ← 谷底（最小值）
       +──────────→ θ

  步长 = 学习率α × 梯度
  太大 → 跳过谷底（发散）
  太小 → 走得太慢
  刚好 → 稳稳下山
```

### 📐 梯度下降的数学基础

**梯度的定义**：

```
对于函数 J: ℝᵈ → ℝ

梯度 ∇J(θ) = [∂J/∂θ₁, ∂J/∂θ₂, ..., ∂J/∂θᵈ]ᵀ

含义：
  - 梯度是一个向量，指向函数值上升最快的方向
  - 梯度的反方向 -∇J(θ) 是函数值下降最快的方向
  - 梯度的大小（模长）表示"坡度"有多陡
```

**梯度下降更新规则**：

```
θ^{t+1} = θ^t - α · ∇J(θ^t)

其中：
  θ^t    = 第t步的参数值
  α      = 学习率（步长），α > 0
  ∇J(θ^t) = 当前位置的梯度
  
减号是因为：梯度的反方向是下降方向
```

**生活类比**：学习率α的选择

| 学习率 | 类比 | 效果 |
|--------|------|------|
| 太大 | 下山时每步跨太远，跳过谷底 | 震荡、发散 |
| 太小 | 下山时每步挪一点点 | 收敛极慢 |
| 合适 | 每步走得恰到好处 | 稳定收敛 |

```
学习率对收敛的影响：

  α太大：               α合适：              α太小：
  J ↑                  J ↑                  J ↑
    | ↗↘               | ╲                  | ╲
    |↗  ↘              |  ╲___              |   ╲
    |    ↘↗            |      ___最小       |    ╲
    +────→ 步         +────→ 步           +────→ 步
    震荡/发散           稳定收敛             极慢收敛
```

### 📐 凸函数 vs 非凸函数

**关键区分**：梯度下降能否找到全局最优，取决于函数的形状

```
凸函数（Convex）：
  ┌──────────────────┐
  │  只有一个谷底      │
  │  ╲               │
  │   ╲___           │
  │       ╲          │
  │  梯度下降→全局最优 │
  └──────────────────┘
  
  例子：线性回归的MSE损失
  J(θ) = (1/n)Σ(θᵀxᵢ - yᵢ)²

非凸函数（Non-convex）：
  ┌──────────────────┐
  │  有多个谷底        │
  │  ╲  /╲            │
  │   ╲/  ╲___        │
  │   ↑局部  ↑全局     │
  │  梯度下降→可能局部  │
  └──────────────────┘
  
  例子：神经网络的损失函数
```

**凸函数的保证**：

```
定理：如果J是凸函数且可微，且学习率α足够小，
      则梯度下降收敛到全局最小值。

更精确地说，如果J满足L-光滑条件（梯度Lipschitz连续）：
  ||∇J(θ) - ∇J(θ')|| ≤ L||θ - θ'||

则取α = 1/L时：
  J(θ^T) - J(θ*) ≤ ||θ⁰ - θ*||² / (2T)

即：误差以O(1/T)的速度收敛到0
```

### 📐 三种梯度下降变体

**生活类比**：考试复习策略

| 变体 | 类比 | 每步计算量 | 收敛速度 | 适用场景 |
|------|------|-----------|---------|---------|
| 批量梯度下降（BGD） | 每次做完所有题才学 | O(n) | 稳定但慢 | 小数据集 |
| 随机梯度下降（SGD） | 每做一题就学 | O(1) | 快但波动 | 大数据集 |
| 小批量梯度下降（MBGD） | 每做32题学一次 | O(batch) | 平衡 | 最常用 |

```
三种变体的参数更新：

  BGD（批量）：θ = θ - α · (1/n)Σᵢ ∇Jᵢ(θ)
    → 用所有n个样本算梯度
    → 方向准确，但计算量大

  SGD（随机）：θ = θ - α · ∇Jᵢ(θ)  （随机选一个i）
    → 只用1个样本算梯度
    → 方向有噪声，但每步极快

  MBGD（小批量）：θ = θ - α · (1/b)Σᵢ∈batch ∇Jᵢ(θ)
    → 用b个样本算梯度（b通常为32~256）
    → 兼顾方向准确和计算效率
```

**收敛路径图解**：

```
  BGD（平滑路径）：       SGD（震荡路径）：       MBGD（较平滑）：
  J ↑                    J ↑                    J ↑
    | ╲                    | ╲╱                  | ╲
    |  ╲                   |  ╲╱                 |  ╲
    |   ╲___               |   ╲___╱             |   ╲___
    +────→ 步             +────→ 步             +────→ 步
    平滑收敛到最小          震荡但趋势向下         平滑+快速
```

### 📐 梯度下降的Python实现

**以线性回归为例**：

```python
import numpy as np

def gradient_descent(X, y, learning_rate=0.01, n_iterations=1000):
    """
    批量梯度下降求解线性回归
    
    目标：最小化 J(θ) = (1/2n) Σ(θᵀxᵢ - yᵢ)²
    
    参数:
        X: numpy数组, shape=(n, d), 特征矩阵
        y: numpy数组, shape=(n,), 目标值
        learning_rate: float, 学习率α
        n_iterations: int, 迭代次数
    返回:
        theta: 模型参数
        cost_history: 每轮的损失值
    """
    n, d = X.shape           # n=样本数, d=特征数
    theta = np.zeros(d)      # 初始化参数为全0向量
    cost_history = []         # 记录每轮损失
    
    for i in range(n_iterations):
        # 前向传播：计算预测值
        predictions = X @ theta              # X·θ, shape=(n,)
        
        # 计算误差
        errors = predictions - y             # shape=(n,)
        
        # 计算梯度
        # ∇J = (1/n) Xᵀ(Xθ - y)
        gradient = (1 / n) * (X.T @ errors)  # shape=(d,)
        
        # 参数更新：沿梯度反方向走一步
        theta = theta - learning_rate * gradient
        
        # 记录当前损失
        cost = (1 / (2 * n)) * np.sum(errors ** 2)  # MSE损失
        cost_history.append(cost)
    
    return theta, cost_history


def stochastic_gradient_descent(X, y, learning_rate=0.01, n_epochs=10):
    """
    随机梯度下降（SGD）求解线性回归
    
    参数:
        X: numpy数组, shape=(n, d)
        y: numpy数组, shape=(n,)
        learning_rate: float, 学习率
        n_epochs: int, 遍历数据的轮数
    返回:
        theta: 模型参数
        cost_history: 每轮的损失值
    """
    n, d = X.shape
    theta = np.zeros(d)
    cost_history = []
    
    for epoch in range(n_epochs):
        # 每轮打乱数据顺序（重要！避免周期性震荡）
        indices = np.random.permutation(n)
        
        for idx in indices:
            # 取单个样本
            xi = X[idx]           # shape=(d,)
            yi = y[idx]           # 标量
            
            # 计算该样本的梯度
            prediction = xi @ theta       # 标量
            error = prediction - yi       # 标量
            gradient = error * xi         # shape=(d,)
            
            # 立即更新参数
            theta = theta - learning_rate * gradient
        
        # 每轮结束后记录损失
        predictions = X @ theta
        cost = (1 / (2 * n)) * np.sum((predictions - y) ** 2)
        cost_history.append(cost)
    
    return theta, cost_history


def mini_batch_gradient_descent(X, y, learning_rate=0.01, n_epochs=50, batch_size=32):
    """
    小批量梯度下降求解线性回归
    
    参数:
        X: numpy数组, shape=(n, d)
        y: numpy数组, shape=(n,)
        learning_rate: float, 学习率
        n_epochs: int, 轮数
        batch_size: int, 每批样本数
    返回:
        theta: 模型参数
        cost_history: 每轮的损失值
    """
    n, d = X.shape
    theta = np.zeros(d)
    cost_history = []
    
    for epoch in range(n_epochs):
        indices = np.random.permutation(n)  # 打乱顺序
        
        for start in range(0, n, batch_size):
            # 取一个batch的样本
            batch_indices = indices[start:start + batch_size]
            X_batch = X[batch_indices]      # shape=(batch_size, d)
            y_batch = y[batch_indices]      # shape=(batch_size,)
            
            # 计算batch上的梯度
            predictions = X_batch @ theta
            errors = predictions - y_batch
            gradient = (1 / len(batch_indices)) * (X_batch.T @ errors)
            
            # 更新参数
            theta = theta - learning_rate * gradient
        
        # 每轮结束后记录损失
        predictions = X @ theta
        cost = (1 / (2 * n)) * np.sum((predictions - y) ** 2)
        cost_history.append(cost)
    
    return theta, cost_history


# 使用示例
if __name__ == "__main__":
    np.random.seed(42)
    
    # 生成线性数据：y = 3x₁ + 2x₂ + 1 + 噪声
    n, d = 200, 2
    X = np.random.randn(n, d)
    true_theta = np.array([3.0, 2.0])
    y = X @ true_theta + 1.0 + 0.5 * np.random.randn(n)  # 加噪声
    
    # 批量梯度下降
    theta_bgd, cost_bgd = gradient_descent(X, y, learning_rate=0.1, n_iterations=200)
    print(f"BGD: theta = {theta_bgd}, 最终损失 = {cost_bgd[-1]:.4f}")
    
    # 随机梯度下降
    theta_sgd, cost_sgd = stochastic_gradient_descent(X, y, learning_rate=0.01, n_epochs=50)
    print(f"SGD: theta = {theta_sgd}, 最终损失 = {cost_sgd[-1]:.4f}")
    
    # 小批量梯度下降
    theta_mbgd, cost_mbgd = mini_batch_gradient_descent(X, y, learning_rate=0.05, n_epochs=50, batch_size=32)
    print(f"MBGD: theta = {theta_mbgd}, 最终损失 = {cost_mbgd[-1]:.4f}")
```

### 📐 梯度下降的C++实现

```cpp
#include <iostream>
#include <vector>
#include <cmath>
#include <random>

using namespace std;

// 批量梯度下降求解线性回归
void gradientDescent(const vector<vector<double>>& X, 
                     const vector<double>& y,
                     vector<double>& theta,
                     double learning_rate = 0.01, 
                     int n_iterations = 1000) {
    int n = X.size();          // 样本数
    int d = X[0].size();       // 特征数
    
    theta.assign(d, 0.0);      // 初始化参数为0
    
    for (int iter = 0; iter < n_iterations; iter++) {
        // 计算梯度
        vector<double> gradient(d, 0.0);  // 梯度向量，初始化为0
        
        for (int i = 0; i < n; i++) {
            // 计算第i个样本的预测值
            double prediction = 0.0;
            for (int j = 0; j < d; j++) {
                prediction += X[i][j] * theta[j];  // θᵀxᵢ
            }
            
            double error = prediction - y[i];  // 预测误差
            
            // 累加梯度
            for (int j = 0; j < d; j++) {
                gradient[j] += error * X[i][j];  // (1/n)Σ(error·xᵢⱼ)
            }
        }
        
        // 更新参数
        for (int j = 0; j < d; j++) {
            gradient[j] /= n;                        // 除以n取平均
            theta[j] -= learning_rate * gradient[j]; // θ = θ - α·∇J
        }
        
        // 每100轮输出一次损失
        if (iter % 100 == 0) {
            double cost = 0.0;
            for (int i = 0; i < n; i++) {
                double pred = 0.0;
                for (int j = 0; j < d; j++) pred += X[i][j] * theta[j];
                cost += (pred - y[i]) * (pred - y[i]);
            }
            cost /= (2 * n);
            cout << "迭代" << iter << ", 损失=" << cost << endl;
        }
    }
}

int main() {
    int n = 200, d = 2;  // 200个样本，2个特征
    
    // 生成数据：y = 3x₁ + 2x₂ + 1 + 噪声
    mt19937 rng(42);
    normal_distribution<double> noise_dist(0.0, 0.5);
    normal_distribution<double> feature_dist(0.0, 1.0);
    
    vector<vector<double>> X(n, vector<double>(d));
    vector<double> y(n);
    
    for (int i = 0; i < n; i++) {
        X[i][0] = feature_dist(rng);  // 特征1
        X[i][1] = feature_dist(rng);  // 特征2
        y[i] = 3.0 * X[i][0] + 2.0 * X[i][1] + 1.0 + noise_dist(rng);  // 目标值+噪声
    }
    
    vector<double> theta;
    gradientDescent(X, y, theta, 0.1, 200);
    
    cout << "最终参数: theta = [" << theta[0] << ", " << theta[1] << "]" << endl;
    cout << "真实参数: [3.0, 2.0]" << endl;
    
    return 0;
}
```

### 📐 收敛速度分析

```
不同场景下的收敛速度：

  凸函数 + L-光滑：
    BGD: J(θᵀ) - J* ≤ O(1/T)           线性收敛（1/T速率）
    
  强凸函数 + L-光滑：
    BGD: J(θᵀ) - J* ≤ O((1-μ/L)^T)    指数收敛（线性速率）
    
    其中 μ = 强凸参数, L = 光滑参数
    条件数 κ = L/μ 越大，收敛越慢
    
  SGD：
    E[J(θᵀ)] - J* ≤ O(1/√T)            更慢，但每步更快
```

| 变体 | 每步时间 | 总步数 | 总时间 | 噪声 |
|------|---------|--------|--------|------|
| BGD | O(nd) | O(1/ε) | O(nd/ε) | 无 |
| SGD | O(d) | O(1/ε²) | O(d/ε²) | 大 |
| MBGD | O(bd) | O(1/ε) | O(bd/ε) | 中 |

### 📐 高级变体：动量法与Adam

**为什么需要高级变体？**

```
标准梯度下降的问题：

  1. 山谷中的震荡：
     ┌───────────────┐
     │  ╲  │  ╱       │  梯度在山谷壁上来回弹
     │   ╲ │ ╱        │  而不是沿着谷底前进
     │    ╲│╱         │
     └───────────────┘
     
  2. 鞍点停滞：
     ┌───────────────┐
     │  ╱    ╲       │  梯度≈0但不是最小值
     │ ╱  ·   ╲      │  算法"以为"到了终点
     │╱        ╲     │  实际上是鞍点
     └───────────────┘
```

**动量法（Momentum）**：

```
v^{t+1} = β·v^t + ∇J(θ^t)     // 累积历史梯度（"惯性"）
θ^{t+1} = θ^t - α·v^{t+1}      // 沿累积方向走

类比：球滚下山时有惯性
  - 在一致的方向上加速（山谷底部）
  - 在震荡的方向上抵消（山谷壁上）
  
β通常取0.9
```

**Adam优化器**（最常用的深度学习优化器）：

```
m = β₁·m + (1-β₁)·∇J(θ)     // 一阶矩估计（动量）
v = β₂·v + (1-β₂)·(∇J(θ))²   // 二阶矩估计（自适应学习率）
m̂ = m / (1-β₁^t)              // 偏差校正
v̂ = v / (1-β₂^t)              // 偏差校正
θ = θ - α·m̂ / (√v̂ + ε)       // 更新参数

默认：β₁=0.9, β₂=0.999, ε=10⁻⁸
直觉：梯度大的参数→小步走；梯度小的参数→大步走
     → 自动调整每个参数的学习率
```

```python
# Adam优化器的Python实现
def adam_gradient_descent(X, y, learning_rate=0.001, n_iterations=500,
                          beta1=0.9, beta2=0.999, epsilon=1e-8):
    """Adam优化器求解线性回归"""
    n, d = X.shape
    theta = np.zeros(d)     # 参数
    m = np.zeros(d)         # 一阶矩（动量）
    v = np.zeros(d)         # 二阶矩（梯度平方的指数移动平均）
    
    for t in range(1, n_iterations + 1):
        # 计算梯度
        predictions = X @ theta
        gradient = (1 / n) * (X.T @ (predictions - y))
        
        # 更新一阶矩（梯度的指数移动平均）
        m = beta1 * m + (1 - beta1) * gradient
        
        # 更新二阶矩（梯度平方的指数移动平均）
        v = beta2 * v + (1 - beta2) * gradient ** 2
        
        # 偏差校正（因为初始m和v接近0，前期的估计偏低）
        m_hat = m / (1 - beta1 ** t)
        v_hat = v / (1 - beta2 ** t)
        
        # 更新参数
        theta = theta - learning_rate * m_hat / (np.sqrt(v_hat) + epsilon)
    
    return theta
```

### ❓ FAQ：梯度下降常见疑问

**Q1：学习率怎么选？**
> A：常用策略：
> - 网格搜索：试 0.1, 0.01, 0.001 看哪个效果好
> - 学习率衰减：初始0.1，每100步减半
> - 自适应方法：用Adam，默认学习率0.001通常效果不错
> - 最保险的做法：先用Adam，不收敛再调

**Q2：怎么判断收敛了？**
> A：看损失曲线：
> - 损失连续N轮不降 → 可能收敛了
> - 损失开始上升 → 学习率太大，需要减小
> - 损失缓慢下降但波动 → 正常（尤其SGD）

**Q3：梯度下降和牛顿法有什么区别？**
> A：
> - 梯度下降：只用一阶导数（梯度），O(d)每步
> - 牛顿法：用二阶导数（Hessian矩阵），O(d²)每步
> - 牛顿法收敛更快（二次收敛），但每步更贵
> - 高维问题（d很大，如深度学习）中，Hessian太大存不下，只能用梯度下降

**Q4：为什么深度学习不用牛顿法？**
> A：牛顿法需要计算和存储Hessian矩阵（d×d），深度学习参数量动辄百万、上亿，Hessian根本存不下。梯度下降只需要梯度向量（d维），内存友好得多。拟牛顿法（如L-BFGS）是折中方案，但在深度学习中仍不如SGD/Adam主流。

---

## 🔗 三大算法的联系与对比

### 🎯 统一视角：三个算法都在做优化

```
聚类的K-Means：   交替优化（坐标下降）
  固定中心→优化分配，固定分配→优化中心
  
加权多数算法：     在线优化（乘法权重更新）
  每轮根据反馈调整权重，追踪最佳专家
  
梯度下降：        迭代优化（一阶方法）
  沿梯度反方向逐步逼近最小值
```

| 维度 | K-Means | 加权多数 | 梯度下降 |
|------|---------|---------|---------|
| 学习范式 | 无监督 | 在线学习 | 优化（可服务于监督学习） |
| 优化方式 | 坐标下降 | 乘法权重更新 | 一阶梯度法 |
| 收敛保证 | 局部最优 | 错误数有上界 | 凸→全局最优；非凸→局部 |
| 数据要求 | 所有数据可用 | 逐个到来 | 所有数据可用（批量）或逐个（SGD） |
| 输入 | 数据点+簇数k | 专家预测+真实标签 | 可微函数J(θ) |
| 输出 | 簇分配+中心 | 预测序列 | 最小化J的参数θ |

### 🎯 从算法导论到机器学习的桥梁

```
算法导论传统内容              机器学习视角
─────────────────           ─────────────────
第15章 贪心算法       ──→   K-Means的分配步骤是贪心
第14章 动态规划       ──→   隐马尔可夫模型的Viterbi算法
第29章 线性规划       ──→   SVM的优化问题就是QP
第5章 概率分析        ──→   机器学习的理论根基
第4章 分治            ──→   决策树的递归分裂
第24章 最短路径       ──→   HMM的Viterbi解码
第16章 摊销分析       ──→   在线学习的遗憾分析
第35章 近似算法       ──→   机器学习本就是"近似最优"
```

**核心洞察**：机器学习不是和算法导论割裂的"新领域"，而是算法思维在新问题上的应用——从"精确求解"到"近似学习"，从"确定性"到"概率性"，从"人写规则"到"数据驱动"。

---

## 📝 本章小结

### 核心概念速查

| 概念 | 一句话总结 | 生活类比 |
|------|-----------|---------|
| 聚类 | 把相似的东西分到一组 | 新生分宿舍 |
| K-Means | 交替优化：分配→更新→分配→更新 | 先分组再定组长，反复调整 |
| 在线学习 | 数据逐个到来，边做边学 | 考试边做边对答案 |
| Halving算法 | 淘汰错误者，跟多数走 | 一个顾问犯错就拉黑 |
| 加权多数 | 犯错降权，不直接淘汰 | 犯错的顾问降低信任度 |
| 随机化加权多数 | 按概率选，打破2倍下界 | 随机选顾问，防对手针对 |
| 梯度下降 | 沿梯度反方向走，找最小值 | 蒙眼下山 |
| 学习率 | 每步走多远 | 下山的步幅 |
| 凸函数 | 只有一个谷底 | 碗形 |
| BGD/SGD/MBGD | 用多少数据算梯度 | 复习一题/全部/部分 |

### 关键定理速查

| 定理 | 结论 | 条件 |
|------|------|------|
| K-Means收敛性 | J每轮递减，必收敛到局部最优 | 任意初始中心 |
| Halving错误界 | M ≤ ⌈log₂n⌉ | 存在完美专家 |
| 确定性WM错误界 | M ≤ 2(1+η)m* + 2ln(n)/η | η ≤ 1/2 |
| 随机化WM错误界 | E[M] ≤ (1+ε)m* + ln(n)/ε | ε > 0 |
| 凸函数GD收敛 | J(θᵀ)-J* ≤ O(1/T) | L-光滑，α=1/L |
| 强凸GD收敛 | J(θᵀ)-J* ≤ O((1-μ/L)^T) | μ-强凸+L-光滑 |

### 本章与主人的关联

> 本章内容对主人的AI for EDA方向有直接意义：
> - **梯度下降**是训练GNN/RL模型的核心优化器
> - **聚类**在EDA中用于电路划分（partitioning）
> - **加权多数算法**是在线学习和Boosting的理论基础，理解它有助于理解AdaBoost等集成方法
> - 理解ML的算法本质（而不是黑盒调参），是区分算法工程师和调参侠的关键
