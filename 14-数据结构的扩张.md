# 第14章 数据结构的扩张 (Augmenting Data Structures)

> 本章学习如何通过扩展已有的数据结构来构造功能更强大的新数据结构。

---

## 14.1 动态顺序统计 (Dynamic Order Statistics)

### 14.1.1 概念引入：什么是顺序统计？

**📚 学术定义**
在一个由 $n$ 个元素组成的集合中，第 $i$ 个**顺序统计量**（Order Statistic）是该集合中第 $i$ 小的元素。

**🏠 生活类比：班级成绩排名**
想象你是班主任，要回答两类问题：
1. **"第10名是谁？"** → 这是**选择问题**（OS-SELECT）
2. **"小明排在第几名？"** → 这是**求秩问题**（OS-RANK）

普通数组需要 $O(n)$ 时间，但我们可以做得更快！

### 14.1.2 顺序统计树（OS树）的定义

**💡 核心思想**
在红黑树的每个节点上增加一个 `size` 属性，记录以该节点为根的子树中包含多少个节点。

```
x.size = x.left.size + x.right.size + 1
```

**🎯 为什么选择红黑树？**
- 普通二叉搜索树可能退化成链表，查找需要 $O(n)$
- 红黑树保证高度为 $O(\log n)$，查找只需要 $O(\log n)$

---

### 14.1.3 选择操作：OS-SELECT

**📝 问题**：在以 $x$ 为根的子树中，查找第 $i$ 小的元素。

**🔄 算法思路（二分查找思想）**

```
OS-SELECT(x, i)
    r = x.left.size + 1        // r = x在其子树中的"排名"
    if i == r
        return x               // 找到了！
    elseif i < r
        return OS-SELECT(x.left, i)   // 在左子树找
    else
        return OS-SELECT(x.right, i-r) // 在右子树找（第i-r小）
```

**📊 过程图解**

```
          26(黑)
         /      \
      17(黑)    30(红)
      /  \       /   \
   10(红)  21(红)  41(黑)  47(黑)
   /  \     \    /
 3(黑) 14   23  38

查找第5小的元素：OS-SELECT(root, 5)

第1轮：x=26, 左子树有12个节点
       r = 12 + 1 = 13
       5 < 13 → 去左子树找第5小
       
第2轮：x=17, 左子树有5个节点
       r = 5 + 1 = 6
       5 < 6 → 去左子树找第5小
       
第3轮：x=10, 左子树有1个节点
       r = 1 + 1 = 2
       5 > 2 → 去右子树找第5-2=3小
       
第4轮：x=21, 左子树有0个节点
       r = 0 + 1 = 1
       3 > 1 → 去右子树找第3-1=2小
       
第5轮：x=23, 左子树有0个节点
       r = 1
       2 > 1 → 去右子树找第2-1=1小
       
第6轮：x=23, r=1, i=1, 找到！
结果：23
```

**⏱️ 时间复杂度**：$O(\log n)$（因为树高是 $O(\log n)$）

---

### 14.1.4 求秩操作：OS-RANK

**📝 问题**：在树 $T$ 中，给定节点 $x$，求它的秩（排名）。

**🔄 算法思路**
从节点 $x$ 一路向上到根，累加左子树的规模。

```
OS-RANK(T, x)
    r = x.left.size + 1        // x在以其为根的子树中的排名
    y = x                      // y用于向上遍历
    while y != T.root
        if y == y.p.right     // 如果y是右孩子
            r = r + y.p.left.size + 1  // 加上父节点的左子树大小+父节点自己
        y = y.p               // 继续向上
    return r
```

**📊 过程图解**

```
        26(黑)
       /      \
    17(黑)    30(红)
    /  \       /   \
 10(红)  21   41(黑) 47(黑)
 ...
 
求节点 35 的秩：

初始化：r = 21.size + 1 = 2（假设21的左子树大小为1）
        y = 35

第1轮：35是38的左孩子，不执行r的累加
        y = 38

第2轮：38是30的右孩子！
        r = 2 + 17.size + 1 = 2 + 5 + 1 = 8
        y = 30

第3轮：30是26的右孩子！
        r = 8 + 17.size + 1 = 8 + 5 + 1 = 14
        y = 26（到达根节点）

结果：35的秩是14
```

**⏱️ 时间复杂度**：$O(\log n)$

---

### 14.1.5 OS树的维护

#### 🅰️ 插入操作的维护

**插入分两个阶段**：

**第一阶段：从根到叶子的路径**
- 对路径上的每个节点执行 `x.size = x.size + 1`
- 新插入节点的 `size = 1`
- 额外代价：$O(\log n)$

**第二阶段：旋转调整**
- 变色操作不影响 `size`
- 旋转操作只需要修改旋转轴上的两个节点的 `size`
- 旋转最多发生2次，额外代价：$O(1)$

**🔄 左旋代码（含size维护）**

```python
# Python 实现
def left_rotate(T, x):
    """
    左旋：以节点x为轴向左旋转
    旋转前：     旋转后：
       x           y
      / \         / \
     α   y   →   x   γ
        / \     / \
       β  γ   α   β
    """
    y = x.right          # 1. y设为x的右孩子
    
    # 2. 将y的左孩子β变成x的右孩子
    x.right = y.left
    if y.left != T.NIL:
        y.left.parent = x
    
    # 3. 将y移到x的位置
    y.parent = x.parent
    if x.parent == T.NIL:
        T.root = y
    elif x == x.parent.left:
        x.parent.left = y
    else:
        x.parent.right = y
    
    # 4. 将x变成y的左孩子
    y.left = x
    x.parent = y
    
    # 5. 【重要】维护size属性
    # 旋转后，子树大小不变，但需要重新计算轴上两个节点
    y.size = x.size                    # y成为新父节点，继承原来的size
    x.size = x.left.size + x.right.size + 1  # x成为新子节点，重新计算
```

```cpp
// C++ 实现
struct OSTNode {
    int key;
    int color;          // 0=黑, 1=红
    OSTNode *parent;
    OSTNode *left;
    OSTNode *right;
    int size;           // 子树规模
};

void left_rotate(OSTree *T, OSTNode *x) {
    OSTNode *y = x->right;     // y设为x的右孩子
    
    // 将y的左孩子变成x的右孩子
    x->right = y->left;
    if (y->left != T->NIL) {
        y->left->parent = x;
    }
    
    // 将y移到x的位置
    y->parent = x->parent;
    if (x->parent == T->NIL) {
        T->root = y;
    } else if (x == x->parent->left) {
        x->parent->left = y;
    } else {
        x->parent->right = y;
    }
    
    // 将x变成y的左孩子
    y->left = x;
    x->parent = y;
    
    // 维护size属性
    y->size = x->size;  // y成为新父节点
    x->size = x->left->size + x->right->size + 1;  // 重新计算x的size
}
```

#### 🅱️ 删除操作的维护

**删除也分两个阶段**：

**第一阶段：物理删除 + size递减**
- 从被删除节点 $y$ 向上到根
- 路径上每个节点的 `size` 减1
- 额外代价：$O(\log n)$

**第二阶段：旋转调整**
- 同插入，最多3次旋转
- 每次旋转修改 $O(1)$ 个节点的 `size`
- 额外代价：$O(\log n)$

---

### 📌 FAQ：常见疑问

**Q1：为什么不直接存储节点的"秩"而是存储"子树大小"？**
A1：秩是动态变化的！当你插入或删除节点时，很多节点的秩都会改变，需要 $O(n)$ 更新。而子树大小只需要在插入/删除路径上更新，代价是 $O(\log n)$。

**Q2：为什么选择红黑树而不是普通二叉搜索树？**
A2：普通BST可能退化成链表（如依次插入1,2,3,4,5），此时 $O(\log n)$ 就退化成了 $O(n)$。红黑树通过平衡保证高度始终是 $O(\log n)$。

---

## 14.2 如何扩张数据结构 (How to Augment a Data Structure)

### 14.2.1 扩张数据结构的四步法

**📝 扩张不是随意的，需要系统的方法：**

```
┌─────────────────────────────────────────────────────────────┐
│                    扩张数据结构的四个步骤                     │
├─────────────────────────────────────────────────────────────┤
│  1️⃣ 选择基础数据结构                                        │
│      → 通常选择平衡二叉搜索树（如红黑树）                      │
│                                                             │
│  2️⃣ 确定要维护的附加信息                                     │
│      → 思考：我们需要在节点上存储什么额外信息？                │
│                                                             │
│  3️⃣ 验证基本修改操作能否维护附加信息                          │
│      → 插入、删除、旋转时，新属性是否容易更新？                │
│                                                             │
│  4️⃣ 设计新的操作                                            │
│      → 利用新信息实现我们需要的查询操作                        │
└─────────────────────────────────────────────────────────────┘
```

### 14.2.2 红黑树扩张定理

**📖 定理（简化版）**

> 设 $f$ 是对红黑树 $T$ 的每个节点 $x$ 扩张的属性。
> 如果 $f(x)$ 的值只依赖于：
> - 节点 $x$ 本身的信息
> - 节点 $x.left$ 和 $x.right$ 的 $f$ 值
> 
> 那么我们可以在 $O(\log n)$ 时间内维护所有节点的 $f$ 值。

**🔑 定理的核心洞察**

为什么这个定理成立？
1. 插入/删除只影响从叶子到根的路径（$O(\log n)$ 个节点）
2. 如果 $f(x)$ 只依赖于孩子节点的 $f$ 值，我们只需要沿路径向上更新
3. 变色不影响结构，只需要 $O(1)$
4. 旋转是局部操作，只需更新旋转轴上的 $O(1)$ 个节点

### 14.2.3 用四步法分析顺序统计树

| 步骤 | 决策 | 顺序统计树的例子 |
|------|------|-----------------|
| 1 | 基础数据结构 | 红黑树 |
| 2 | 附加信息 | `size[x] = 左子树大小 + 右子树大小 + 1` |
| 3 | 验证维护 | 插入时路径上+1，旋转时局部更新 |
| 4 | 新操作 | `OS-SELECT` 和 `OS-RANK` |

---

## 14.3 区间树 (Interval Trees)

### 14.3.1 问题引入：区间重叠检测

**🏠 生活类比：会议室预约系统**

假设学校有多个会议室，每个会议室有不同的使用时间段：
- 会议室A：[9:00, 11:00]
- 会议室B：[10:00, 12:00]
- 会议室C：[14:00, 16:00]

当你想要预约 [10:30, 11:30] 这个时间段时，系统需要快速找出：**哪个已预约的会议室与你的时间段重叠？**

**📝 区间定义**
闭区间 $[t_1, t_2]$ 表示 $t_1 \leq t \leq t_2$ 的所有实数。
- `low[i] = t_1`（低端点）
- `high[i] = t_2`（高端点）

**📖 区间重叠的判断**
两个区间 $i$ 和 $i'$ 重叠，当且仅当：
$$i \cap i' \neq \emptyset \Leftrightarrow low[i] \leq high[i'] \text{ 且 } low[i'] \leq high[i]$$

### 14.3.2 区间三分律 (Interval Trichotomy)

**🎯 重要性质**
任意两个区间 $i$ 和 $j$，必然满足且仅满足以下三条之一：

```
   1️⃣ i 和 j 重叠
   ┌─────┐     ┌─────┐
   │  i  │     │  j  │
   └─────┘     └─────┘
   
   2️⃣ i 在 j 左边：i.high < j.low
   ┌─────┐          ┌─────┐
   │  i  │          │  j  │
   └─────┘          └─────┘
      <────────────>
      i 在左边
   
   3️⃣ i 在 j 右边：j.high < i.low
   ┌─────┐          ┌─────┐
   │  j  │          │  i  │
   └─────┘          └─────┘
                   <────────────>
                      i 在右边
```

**💡 这个性质有什么用？**
当我们想找与 $i$ 重叠的区间时：
- 如果 $i$ 不与当前节点区间重叠，且左子树的 `max` < `low[i]`，则左子树不可能有重叠（满足性质2）
- 我们只需要检查可能包含重叠区间的子树

---

### 14.3.3 区间树的构建（四步法）

#### 第一步：选择基础数据结构
```
→ 红黑树（以区间低端点 low 为关键字进行中序遍历得到有序序列）
```

#### 第二步：确定附加信息
```
→ 每个节点存储 max[x] = 以 x 为根的子树中，所有区间高端点的最大值

        max = max(当前节点high, 左子树max, 右子树max)
```

#### 第三步：验证维护
```
→ 插入/删除时，从叶子到根更新路径上节点的 max
→ 旋转时，局部更新（O(1)）
→ 符合红黑树扩张定理！
```

#### 第四步：设计新操作

**INTERVAL-SEARCH(T, i)**：返回一个与区间 $i$ 重叠的区间。

```python
def interval_search(T, i):
    """
    在区间树T中查找与区间i重叠的区间
    """
    x = T.root
    while x != T.NIL and not overlaps(i, x.int):
        # 如果左子树可能包含重叠区间，去左子树
        if x.left != T.NIL and x.left.max >= i.low:
            x = x.left
        else:
            x = x.right
    return x

def overlaps(a, b):
    """判断区间a和b是否重叠"""
    return a.low <= b.high and b.low <= a.high
```

---

### 14.3.4 区间搜索算法图解

**📊 搜索过程示意**

```
区间树示例（按low排序的红黑树）：

           [16, 21]  max=23
          /          \
    [8, 9]            [25, 30]  max=30
      max=9          /          \
                [19, 21]         [26, 26]  max=26
                  max=21              max=26

查询区间 i = [22, 25]：

第1轮：x = 根节点 [16, 21]
       22 > 21，不重叠
       检查左子树 max=9 < 22（不满足条件）
       所以左子树不可能有重叠 → 去右子树

第2轮：x = [25, 30]
       22 ≤ 30 且 25 ≤ 25，重叠！
       找到答案：[25, 30]
```

**🔍 搜索逻辑解释**

为什么 `max[left[x]] >= i.low` 就能保证左子树有重叠？

```
设左子树中某个区间为 [l, h]，其 h = max[left[x]]

如果 h >= i.low：
    ┌─────────┐         ┌─────────┐
    │  左子树  │         │  查询   │
    │   [l,h] │         │ [i.low, ?]
    └─────────┘         └─────────┘
    
由于 h >= i.low 且 l <= h（区间定义）
→ 一定有 l <= h 且 i.low <= h
→ 即 low[左子树区间] <= high[查询] 且 low[查询] <= high[左子树区间]
→ 两个区间必然重叠！
```

---

### 14.3.5 区间树的完整实现

```python
# ============================================================
# 区间树 Python 实现
# ============================================================

class IntervalTreeNode:
    """区间树节点"""
    def __init__(self, low, high):
        self.key = low          # 关键字：低端点
        self.int = (low, high)  # 区间
        self.max = high         # 子树最大高端点
        self.color = 'RED'      # 红黑树颜色
        self.parent = None
        self.left = None
        self.right = None

class IntervalTree:
    """区间树"""
    def __init__(self):
        self.NIL = IntervalTreeNode(0, 0)  # 哨兵节点
        self.NIL.color = 'BLACK'
        self.NIL.max = 0
        self.root = self.NIL
    
    # --- 辅助函数 ---
    
    def _update_max(self, x):
        """更新节点x的max值"""
        if x != self.NIL:
            left_max = x.left.max if x.left else 0
            right_max = x.right.max if x.right else 0
            x.max = max(x.int[1], left_max, right_max)
    
    def _left_rotate(self, x):
        """左旋"""
        y = x.right
        x.right = y.left
        if y.left != self.NIL:
            y.left.parent = x
        y.parent = x.parent
        if x.parent == self.NIL:
            self.root = y
        elif x == x.parent.left:
            x.parent.left = y
        else:
            x.parent.right = y
        y.left = x
        x.parent = y
        self._update_max(x)  # 更新受影响节点的max
        self._update_max(y)
    
    def _right_rotate(self, y):
        """右旋"""
        x = y.left
        y.left = x.right
        if x.right != self.NIL:
            x.right.parent = y
        x.parent = y.parent
        if y.parent == self.NIL:
            self.root = x
        elif y == y.parent.right:
            y.parent.right = x
        else:
            y.parent.left = x
        x.right = y
        y.parent = x
        self._update_max(y)  # 更新受影响节点的max
        self._update_max(x)
    
    # --- 区间操作 ---
    
    def interval_insert(self, node):
        """插入区间"""
        z = IntervalTreeNode(node[0], node[1])
        z.left = self.NIL
        z.right = self.NIL
        
        y = self.NIL
        x = self.root
        # 标准BST插入
        while x != self.NIL:
            y = x
            if z.key < x.key:
                x = x.left
            else:
                x = x.right
        z.parent = y
        if y == self.NIL:
            self.root = z
        elif z.key < y.key:
            y.left = z
        else:
            y.right = z
        
        # 从插入点向上更新max
        self._fixup_insert(z)
    
    def _fixup_insert(self, z):
        """插入后的修复（简化版，保持红黑树性质）"""
        while z.parent.color == 'RED':
            if z.parent == z.parent.parent.left:
                y = z.parent.parent.right
                if y.color == 'RED':
                    z.parent.color = 'BLACK'
                    y.color = 'BLACK'
                    z.parent.parent.color = 'RED'
                    z = z.parent.parent
                else:
                    if z == z.parent.right:
                        z = z.parent
                        self._left_rotate(z)
                    z.parent.color = 'BLACK'
                    z.parent.parent.color = 'RED'
                    self._right_rotate(z.parent.parent)
            else:
                # 对称情况...
                pass
        self.root.color = 'BLACK'
    
    def interval_search(self, low, high):
        """查找与给定区间重叠的区间"""
        return self._interval_search_helper(self.root, (low, high))
    
    def _interval_search_helper(self, x, interval):
        """区间搜索的递归辅助函数"""
        if x == self.NIL:
            return None
        if self._overlaps(x.int, interval):
            return x.int
        if x.left != self.NIL and x.left.max >= interval[0]:
            result = self._interval_search_helper(x.left, interval)
            if result:
                return result
        return self._interval_search_helper(x.right, interval)
    
    def _overlaps(self, a, b):
        """判断两个区间是否重叠"""
        return a[0] <= b[1] and b[0] <= a[1]
```

```cpp
// ============================================================
// 区间树 C++ 实现
// ============================================================

#include <iostream>
#include <algorithm>
using namespace std;

// 区间树节点定义
struct IntervalNode {
    int key;            // 低端点作为关键字
    int low;            // 区间低端点
    int high;           // 区间高端点
    int max;            // 子树最大高端点
    int color;          // 0=黑, 1=红
    IntervalNode *parent;
    IntervalNode *left;
    IntervalNode *right;
};

// 哨兵节点
IntervalNode *NIL;

// 区间树类
class IntervalTree {
private:
    IntervalNode *root;
    
    // 更新节点的max值
    void updateMax(IntervalNode *x) {
        if (x != NIL) {
            int left_max = x->left->max;
            int right_max = x->right->max;
            x->max = max(x->high, max(left_max, right_max));
        }
    }
    
    // 左旋
    void leftRotate(IntervalNode *x) {
        IntervalNode *y = x->right;
        x->right = y->left;
        if (y->left != NIL)
            y->left->parent = x;
        y->parent = x->parent;
        if (x->parent == NIL)
            root = y;
        else if (x == x->parent->left)
            x->parent->left = y;
        else
            x->parent->right = y;
        y->left = x;
        x->parent = y;
        // 维护max值
        updateMax(x);
        updateMax(y);
    }
    
    // 右旋
    void rightRotate(IntervalNode *y) {
        IntervalNode *x = y->left;
        y->left = x->right;
        if (x->right != NIL)
            x->right->parent = y;
        x->parent = y->parent;
        if (y->parent == NIL)
            root = x;
        else if (y == y->parent->right)
            y->parent->right = x;
        else
            y->parent->left = x;
        x->right = y;
        y->parent = x;
        // 维护max值
        updateMax(y);
        updateMax(x);
    }
    
public:
    IntervalTree() {
        NIL = new IntervalNode();
        NIL->color = 0;  // 黑色
        NIL->max = 0;
        root = NIL;
    }
    
    // 插入区间
    void intervalInsert(int low, int high) {
        IntervalNode *z = new IntervalNode();
        z->key = low;
        z->low = low;
        z->high = high;
        z->max = high;
        z->color = 1;  // 红色
        z->left = NIL;
        z->right = NIL;
        
        IntervalNode *y = NIL;
        IntervalNode *x = root;
        
        // 标准BST插入
        while (x != NIL) {
            y = x;
            updateMax(x);  // 更新途经节点的max
            if (z->key < x->key)
                x = x->left;
            else
                x = x->right;
        }
        
        z->parent = y;
        if (y == NIL)
            root = z;
        else if (z->key < y->key)
            y->left = z;
        else
            y->right = z;
        
        // 插入修复...
        insertFixup(z);
    }
    
    // 区间搜索：返回与给定区间重叠的节点
    IntervalNode* intervalSearch(int low, int high) {
        IntervalNode *x = root;
        while (x != NIL && !overlaps(x, low, high)) {
            if (x->left != NIL && x->left->max >= low)
                x = x->left;
            else
                x = x->right;
        }
        return x;
    }
    
    // 判断区间是否重叠
    bool overlaps(IntervalNode *x, int low, int high) {
        return (x->low <= high) && (low <= x->high);
    }
    
    // 插入修复（简化版）
    void insertFixup(IntervalNode *z) {
        while (z->parent->color == 1) {
            if (z->parent == z->parent->parent->left) {
                IntervalNode *y = z->parent->parent->right;
                if (y->color == 1) {
                    z->parent->color = 0;
                    y->color = 0;
                    z->parent->parent->color = 1;
                    z = z->parent->parent;
                } else {
                    if (z == z->parent->right) {
                        z = z->parent;
                        leftRotate(z);
                    }
                    z->parent->color = 0;
                    z->parent->parent->color = 1;
                    rightRotate(z->parent->parent);
                }
            } else {
                // 对称情况...
            }
        }
        root->color = 0;
    }
};
```

---

## 14.4 本章小结与核心要点

### 📋 知识框架

```
┌─────────────────────────────────────────────────────────────┐
│                    第14章 知识框架                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   14.1 动态顺序统计                                          │
│   ├── 顺序统计树（OS树）= 红黑树 + size属性                   │
│   ├── OS-SELECT：O(log n)查找第i小元素                       │
│   ├── OS-RANK：O(log n)求元素秩                              │
│   └── 插入/删除维护：O(log n)                                │
│                                                             │
│   14.2 扩张方法论                                            │
│   ├── 四步法：选基础→定信息→验维护→设计操作                   │
│   └── 红黑树扩张定理：f值可局部计算则可O(log n)维护            │
│                                                             │
│   14.3 区间树                                                │
│   ├── 区间树 = 红黑树 + max属性                              │
│   ├── INTERVAL-SEARCH：O(log n)查找重叠区间                  │
│   └── 核心：区间三分律 + max值剪枝                           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 🎯 核心思想

1. **"增量式扩展"**：不从头设计新结构，而是在已有结构上增量添加
2. **"附加信息要可维护"**：新属性必须能在基本操作中高效更新
3. **"利用结构性质剪枝"**：像区间三分律这样的性质可以指导搜索方向

### 🔑 关键术语

| 术语 | 英文 | 含义 |
|------|------|------|
| 顺序统计量 | Order Statistic | 第i小的元素 |
| 顺序统计树 | OS-Tree | 支持顺序统计的红黑树 |
| 秩 | Rank | 元素在有序序列中的位置 |
| 区间树 | Interval Tree | 支持区间操作的红黑树 |
| 区间三分律 | Interval Trichotomy | 任意两区间必满足重叠/左/右之一 |

---

## 📌 习题精选与解答

### 14.1-1 OS-SELECT 追踪
> 在教材图14.1的树上执行 OS-SELECT(root, 10)

**解答**：
```
第1轮: x=26, 左子树大小=12, r=13, i=10<13 → 去左子树
第2轮: x=17, 左子树大小=5, r=6, i=10>6 → 去右子树找第4小
第3轮: x=21, 左子树大小=0, r=1, i=4>1 → 去右子树找第3小
第4轮: x=23, 左子树大小=0, r=1, i=3>1 → 去右子树找第2小
第5轮: x=30, 左子树大小=11, r=12, i=2<12 → 去左子树找第2小
第6轮: x=38, 左子树大小=0, r=1, i=2>1 → 去右子树找第1小
第7轮: x=41, 左子树大小=0, r=1, i=1=r → 找到！
答案：41
```

### 14.1-5 找第i个后继
> 设计算法，在O(log n)时间内找到元素x的第i个后继

**解答**：
```python
OS-FIND-I-SUCCESSOR(x, i):
    r = OS-RANK(T, x)        # 先求x的秩
    return OS-SELECT(T.root, r + i)  # 找第(r+i)小的元素
```

### 14.3-6 最小间隙问题 (MIN-GAP)
> 维护一个动态数集，支持INSERT、DELETE、SEARCH和MIN-GAP操作

**思路**：
```
1. 用红黑树存储所有数字（按值排序）
2. 每个节点增加：left_min, right_min, min_gap
3. min_gap = min(左子树min_gap, 右子树min_gap, 
                 当前值 - 前驱, 后继 - 当前值)
4. INSERT/DELETE时沿路径更新
5. MIN-GAP直接返回根节点的min_gap
```

---

## 🔗 延伸阅读

1. **《算法导论》原书第14章**：深入理解扩张定理的证明
2. **区间树的应用**：会议室预约、CPU调度、电路布线
3. **线段树**：区间树的变体，常用于区间统计问题
4. **树状数组(Fenwick Tree)**：另一种支持顺序统计的数据结构

---

*笔记完成日期：2026-05-08*
*版权声明：内容基于《算法导论》(Introduction to Algorithms, CLRS) 第14章整理*
