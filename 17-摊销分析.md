# 第17章 摊销分析 (Amortized Analysis)

> 本章介绍一种分析数据结构操作代价的独特方法——不纠缠于每次操作的具体代价，而是着眼于整体的平均表现

## 📖 本章导读

### 🎯 问题引入：为什么要"摊销"？

**生活中的例子**：

想象你去健身房办了一张年卡：
- **单次算账思维**：今天练了1小时，要花多少钱？——算得很细
- **摊销算账思维**：年卡2000块，一年内你可能去了100次，平均每次20块

这就是"摊销"的本质：**不是每次操作单独算账，而是把多次操作的代价"分摊"到整体来看**。

**传统分析 vs 摊销分析**：

| 分析方式 | 看什么 | 得到什么 |
|---------|-------|---------|
| 最坏情况分析 | 单次操作的最坏代价 | 上界保证，但可能过于悲观 |
| 平均情况分析 | 假设输入分布 | 需要对输入做假设 |
| **摊销分析** | **操作序列的整体代价** | **更紧致的上界** |

### 💡 摊销分析的核心思想

**摊销代价 (Amortized Cost)** = 对一连串操作总代价的"平均"

注意：这里的"平均"**不需要概率假设**，是一种**确定性的分析**！

**三种场景最需要摊销分析**：

1. **偶尔"贵"的操作**：如栈的倍增扩展
2. **积累效应**：如二进制计数器进位
3. **构建-维护问题**：如动态表的扩张与收缩

---

## 1️⃣ 栈的摊销分析

### 问题背景

还记得第10章的栈吗？栈有三个操作：
- `Push(S, x)`：压栈
- `Pop(S)`：弹栈
- `MultiPop(S, k)`：弹出栈顶k个元素（或全部）

**传统分析**：每个操作都是 O(1)，没问题。

**但是！** 假设我们这样用栈：

```
循环1000次：
    Push 一个元素  # 1000次Push
    Pop  一个元素  # 1000次Pop
```

看起来没问题。但如果结合"动态表扩张"呢？

### 栈操作的摊销分析（聚合分析）

假设我们执行 n 次栈操作，最坏情况下每次 Push 可能触发扩容：

**聚合分析 (Aggregate Analysis)**：所有操作的总代价是 O(n)

```
┌─────────────────────────────────────────┐
│  操作序列：Push, Push, Push, ..., Push   │
│           (n次)                          │
│                                         │
│  扩容时机：每2次扩容后容量翻倍             │
│  - 第1次扩容：容量4→8                    │
│  - 第2次扩容：容量8→16                   │
│  - ...                                   │
│                                         │
│  总代价 = n (Push) + 1+2+4+8+...+n/2    │
│         = n + (n-1)                     │
│         = O(n)                           │
│                                         │
│  摊销代价 = O(n)/n = O(1)                │
└─────────────────────────────────────────┘
```

**结论**：n次 Push 操作的总代价是 O(n)，每次 Push 的摊销代价是 **O(1)**

---

## 2️⃣ 三种摊销分析方法详解

### 方法一：聚合分析 (Aggregate Analysis)

**核心思想**：直接计算 n 次操作的总代价 T(n)，然后 T(n)/n 就是摊销代价。

**示例：二进制计数器递增**

**问题**：一个 k 位二进制计数器从 0 递增到 2^k - 1，需要多少次位翻转？

```
计数过程（k=3）：
┌─────────┬────────┬────────┬────────┬────────┐
│ 步骤    │ 操作前  │ 操作后  │ 翻转位数 │ 累计翻转 │
├─────────┼────────┼────────┼────────┼────────┤
│ 0       │ 000    │ 001    │ 1      │ 1      │
│ 1       │ 001    │ 010    │ 2      │ 3      │
│ 2       │ 010    │ 011    │ 1      │ 4      │
│ 3       │ 011    │ 100    │ 3      │ 7      │
│ 4       │ 100    │ 101    │ 1      │ 8      │
│ 5       │ 101    │ 110    │ 2      │ 10     │
│ 6       │ 110    │ 111    │ 1      │ 11     │
│ 7       │ 111    │ 000    │ 3      │ 14     │
└─────────┴────────┴────────┴────────┴────────┘
```

**规律**：
- 最低位：每次都翻转 → n 次
- 次低位：每2次翻转1次 → n/2 次
- 最高位：总共翻转1次 → 1 次

**聚合分析计算**：

```
T(n) = n + n/2 + n/4 + n/8 + ... + 1
     = 2n - 1
     = O(n)

摊销代价 = T(n)/n = O(1)
```

**Python 代码实现与注释**：

```python
def binary_increment(counter, k):
    """
    二进制计数器递增
    
    参数:
        counter: 列表，表示k位二进制数（低位在左）
        k: 位数
    返回:
        翻转次数
    """
    flips = 0  # 统计翻转次数
    
    # 从最低位开始（索引0）
    for i in range(k):
        if counter[i] == 0:
            # 【找到0，翻转它，这一轮就结束了】
            counter[i] = 1
            flips += 1
            return flips  # 提前返回，不再进位
        else:
            # 【遇到1，翻转成0，继续进位】
            counter[i] = 0
            flips += 1
            # 继续循环，检查更高位
    
    # 如果执行到这里，说明全部位都是1（溢出）
    # 但Python的list可以无限扩展，这里简化处理
    return flips

def analyze_counter_flips(n, k=8):
    """
    分析n次递增的总翻转次数
    
    参数:
        n: 递增次数
        k: 初始位数
    """
    counter = [0] * k  # 初始化计数器
    total_flips = 0
    
    for step in range(n):
        flips = binary_increment(counter, k)
        total_flips += flips
    
    print(f"n={n}次操作，总翻转={total_flips}次")
    print(f"摊销代价={total_flips/n:.2f}次/操作")
    
    return total_flips
```

**运行示例**：

```python
analyze_counter_flips(1000, 10)
# 输出：
# n=1000次操作，总翻转=1999次
# 摊销代价=2.00次/操作
```

### 方法二：核算法 (Accounting Method)

**核心思想**：给每次操作分配一个"账户余额"（摊销代价），余额可以预支未来的代价。

**关键规则**：
- 每次操作收取固定费用
- 多余的存入"银行"
- 当某个操作实际代价较高时，从银行取款支付

**图示理解**：

```
┌─────────────────────────────────────────────┐
│  核算法的工作原理                            │
│                                             │
│  银行账户                                    │
│  ┌─────────────────────────────────────┐   │
│  │  存入 +3  │ 取出 -5  │ 余额 = +8    │   │
│  └─────────────────────────────────────┘   │
│       ↑                    ↑               │
│    每次操作           昂贵操作（如扩容）      │
│    收取3元            花费5元               │
│                                             │
│  约束：余额永不为负！                        │
└─────────────────────────────────────────────┘
```

**栈扩容的核算法分析**：

```python
def stack_accounting_push():
    """
    栈操作的核算法分析
    
    策略：每次Push收取3元
    - 1元：支付当前Push本身
    - 2元：存银行，用于将来复制到新表时支付
    """
    print("""
    ┌────────────────────────────────────────────────┐
    │  Push操作的代价分配                             │
    │                                                │
    │  每次 Push 收取：3 元                           │
    │                                                │
    │  ┌──────────────────────────────────────────┐  │
    │  │  1元 → 支付本次 Push 的实际代价          │  │
    │  │  2元 → 存入银行，用于扩容时的复制操作    │  │
    │  └──────────────────────────────────────────┘  │
    │                                                │
    │  为什么是2元？                                 │
    │  当容量为 m 的表扩张到 2m 时：                  │
    │  - 有 m 个元素需要复制                        │
    │  - 之前存下的 m 个 Push 的"存款"正好 m × 2元  │
    │  - 正好够用！                                  │
    └────────────────────────────────────────────────┘
    """)
```

**C++ 实现**：

```cpp
#include <iostream>
#include <vector>
using namespace std;

class Stack {
private:
    vector<int> data;      // 存储数据的动态数组
    int topIndex;          // 栈顶索引
    
    // 计算当前容量
    int capacity() {
        return data.capacity();
    }
    
    // 扩容：容量翻倍并复制元素
    void resize() {
        int oldSize = capacity();
        int newSize = oldSize * 2;
        
        // 创建新容量的数组
        vector<int> newData(newSize);
        
        // 复制所有元素（这是O(n)的操作！）
        for (int i = 0; i < oldSize; i++) {
            newData[i] = data[i];
        }
        
        // 替换
        data = move(newData);
        
        cout << "扩容: " << oldSize << " -> " << newSize << endl;
    }
    
public:
    Stack() : topIndex(-1) {}
    
    // 核算法分析下的Push操作
    void push(int x) {
        // 如果满了，扩容
        if (topIndex + 1 >= capacity()) {
            resize();
        }
        
        // 压栈
        data[++topIndex] = x;
    }
    
    int pop() {
        if (topIndex < 0) {
            throw runtime_error("Stack underflow");
        }
        return data[topIndex--];
    }
    
    bool isEmpty() {
        return topIndex < 0;
    }
};
```

**核算法分析表格**：

| 操作 | 实际代价 | 收取费用 | 余额变化 | 累计余额 |
|-----|---------|---------|---------|---------|
| Push (普通) | 1 | 3 | +2 | 2 |
| Push (触发扩容) | m+1 | 3 | -m+2 | 0 |
| Pop | 1 | 3 | +2 | 2 |

**结论**：每次 Push/Pop 的摊销代价是 **O(1)**

### 方法三：势能法 (Potential Method)

**核心思想**：定义一个"势能函数"Φ，把数据结构的状态映射为一个数值。操作的实际代价 + 势能变化 = 摊销代价。

**数学定义**：

```
设：
- D₀ 是初始数据结构状态
- Dᵢ 是执行第i个操作后的状态
- Φ(D) 是势能函数（必须是非负的）
- cᵢ 是第i个操作的真实代价

摊销代价 ĉᵢ = cᵢ + Φ(Dᵢ) - Φ(Dᵢ₋₁)

总摊销代价 = Σ ĉᵢ = Σ(cᵢ) + Φ(Dₙ) - Φ(D₀)
```

**关键要求**：
1. Φ(D₀) = 0（初始势能为0）
2. Φ(Dᵢ) ≥ 0（势能永远非负）

**势能法的优势**：不依赖具体的操作序列，只依赖最终状态！

### 三种方法的对比

```
┌─────────────────────────────────────────────────────────────┐
│                    三种摊销分析方法对比                       │
├───────────────┬───────────────┬───────────────┬─────────────┤
│     方法       │   核心思想     │   优点        │    缺点     │
├───────────────┼───────────────┼───────────────┼─────────────┤
│  聚合分析      │  直接求和      │  直观简单     │  需要技巧   │
│               │  T(n)/n       │  适合简单场景  │  不够灵活   │
├───────────────┼───────────────┼───────────────┼─────────────┤
│  核算法        │  银行记账      │  易于理解     │  需要设计   │
│               │  存取余额      │  适合分析组合  │  收费策略   │
├───────────────┼───────────────┼───────────────┼─────────────┤
│  势能法        │  势能变化      │  最灵活强大   │  需要构造   │
│               │  ΔΦ = Φ(f)-Φ(i)│  不依赖序列   │  Φ函数     │
└───────────────┴───────────────┴───────────────┴─────────────┘
```

---

## 3️⃣ 动态表 (Dynamic Tables)

### 问题背景

**为什么需要动态表？**

静态表：固定大小，简单但可能空间浪费或不够用

动态表：根据需要自动扩张或收缩，灵活但有代价

**生活中的例子**：

就像你的衣柜：
- **太小**：衣服塞不下，总要整理
- **太大**：浪费空间
- **动态调整**：换季时整理衣柜，移走冬装放入夏装

### 动态表扩张 (Table Expansion)

**策略**：
- 插入时，如果表满了 → 分配一个更大的表
- 新表容量 = 旧表容量 × 2
- 复制所有元素到新表

**图示过程**：

```
初始：容量=1
┌─────┐
│  A  │ ← 满了
└─────┘

扩张：容量=2，复制A
┌─────┬─────┐
│  A  │  -  │
└─────┴─────┘

插入B
┌─────┬─────┐
│  A  │  B  │ ← 又满了
└─────┴─────┘

扩张：容量=4，复制A,B
┌─────┬─────┬─────┬─────┐
│  A  │  B  │  -  │  -  │
└─────┴─────┴─────┴─────┘

插入C,D
┌─────┬─────┬─────┬─────┐
│  A  │  B  │  C  │  D  │ ← 又满了
└─────┴─────┴─────┴─────┘

扩张：容量=8，复制A,B,C,D
┌─────┬─────┬─────┬─────┬─────┬─────┬─────┬─────┐
│  A  │  B  │  C  │  D  │  -  │  -  │  -  │  -  │
└─────┴─────┴─────┴─────┴─────┴─────┴─────┴─────┘
```

**聚合分析**：

```python
def dynamic_table_insert_analysis(n):
    """
    分析n次插入的总代价
    
    扩张序列：1→2→4→8→...→m (其中m是最大的容量)
    
    第i次扩张时：
    - 新表容量 = 2^(i-1)
    - 需要复制 2^(i-1) 个元素
    """
    total_cost = 0
    
    print("扩张分析：")
    capacity = 1
    insert_count = 0
    
    while insert_count < n:
        # 插入一个新元素
        insert_count += 1
        
        # 检查是否需要扩张
        if insert_count > capacity:
            # 扩张！
            old_capacity = capacity
            capacity *= 2
            copy_cost = old_capacity  # 复制所有已有元素
            total_cost += 1 + copy_cost  # 插入代价 + 复制代价
            print(f"  第{insert_count}次插入：扩张 {old_capacity}→{capacity}，代价=1+{copy_cost}")
        else:
            total_cost += 1  # 普通插入
    
    print(f"\n总代价: {total_cost}")
    print(f"摊销代价: {total_cost/n:.2f}")
    
    return total_cost

# 测试
dynamic_table_insert_analysis(10)
```

**输出**：

```
扩张分析：
  第2次插入：扩张 1→2，代价=1+1
  第4次插入：扩张 2→4，代价=1+2
  第8次插入：扩张 4→8，代价=1+4

总代价: 19
摊销代价: 1.90
```

**数学推导**：

```
插入n个元素，最多扩张⌊log₂n⌋次

总代价 = n（所有插入） + 1 + 2 + 4 + 8 + ... + n/2
       = n + (n-1)
       = 2n - 1
       = O(n)

摊销代价 = (2n-1)/n = O(1)
```

### 势能法分析动态表

**定义势能函数**：

```python
def potential_analysis():
    """
    势能函数定义
    
    Φ(T) = 2 * T.size - T.capacity  (当 size >= 1)
    Φ(T) = 0                         (当 size = 0)
    
    其中：
    - T.size: 当前元素个数
    - T.capacity: 当前表的容量
    """
    print("""
    ┌────────────────────────────────────────────────────────┐
    │  势能函数: Φ(T) = 2 × size - capacity                  │
    │                                                        │
    │  直观理解：                                             │
    │  - size 越多，势能越高（还有元素要复制）                  │
    │  - capacity 越大，势能越低（剩余空间多）                  │
    │                                                        │
    │  性质验证：                                             │
    │  - 初始状态 size=0, capacity=1: Φ = 0 ✓                 │
    │  - 当 size <= capacity/2 时: Φ <= 0                    │
    │  - 当 size > capacity/2 时: Φ > 0                      │
    └────────────────────────────────────────────────────────┘
    """)
```

**势能法计算**：

```python
def table_insert_amortized(size_before, capacity_before):
    """
    计算单次插入的摊销代价（势能法）
    
    参数:
        size_before: 插入前的元素个数
        capacity_before: 插入前的容量
    """
    # 判断是否需要扩张
    if size_before < capacity_before:
        # 普通插入
        # size' = size + 1
        # Φ' - Φ = 2(size+1) - c - (2size - c) = 2
        amortized = 1 + 2  # 实际代价 + 势能变化 = 1 + 2
        print(f"普通插入: 摊销代价 = {amortized}")
        return amortized
    else:
        # 扩张插入
        # 新容量 = 2c, 复制c个元素
        # 势能变化 = 2(c+1) - 2c - (2c - c) = c + 1 - c = 1
        amortized = (1 + capacity_before) + 1  # 实际 + 势能变化
        print(f"扩张插入: 摊销代价 = {amortized}")
        return amortized

# 验证
print("验证：")
table_insert_amortized(3, 4)  # 普通插入
table_insert_amortized(4, 4)  # 扩张插入
```

**结论**：每次插入的摊销代价是 **O(1)**

### 动态表收缩 (Table Shrinking)

**问题**：当删除元素后，表变得很空，是否应该缩小？

**策略**：当利用率低于某个阈值（如1/4）时，收缩到原来的一半

**Python 实现**：

```python
class DynamicTable:
    """
    支持扩张和收缩的动态表
    
    扩张策略：size == capacity 时，容量翻倍
    收缩策略：size == capacity/4 时，容量减半
    """
    
    def __init__(self):
        self.table = [None] * 1  # 初始容量1
        self.size = 0           # 当前元素个数
        self.capacity = 1       # 当前容量
    
    def _resize(self, new_capacity):
        """调整表的大小"""
        print(f"调整大小: {self.capacity} -> {new_capacity}")
        new_table = [None] * new_capacity
        
        # 复制所有元素
        for i in range(self.size):
            new_table[i] = self.table[i]
        
        self.table = new_table
        self.capacity = new_capacity
    
    def insert(self, x):
        """插入元素"""
        # 如果满了，扩张
        if self.size == self.capacity:
            self._resize(2 * self.capacity)
        
        self.table[self.size] = x
        self.size += 1
        print(f"插入 {x}, size={self.size}, capacity={self.capacity}")
    
    def delete(self):
        """删除元素"""
        if self.size == 0:
            print("表已空")
            return None
        
        x = self.table[self.size - 1]
        self.table[self.size - 1] = None
        self.size -= 1
        
        # 【关键】收缩检查
        # 当利用率降到1/4时，收缩到一半
        if self.size == self.capacity // 4 and self.capacity > 1:
            self._resize(self.capacity // 2)
        
        print(f"删除 {x}, size={self.size}, capacity={self.capacity}")
        return x
    
    def utilization(self):
        """返回当前利用率"""
        return self.size / self.capacity

# 测试序列：插入n个，删除n/2个
def test_shrink():
    table = DynamicTable()
    
    print("\n=== 插入测试 ===")
    for i in range(8):
        table.insert(i)
    
    print(f"\n当前利用率: {table.utilization():.2%}")
    
    print("\n=== 删除测试 ===")
    for _ in range(6):
        table.delete()
    
    print(f"\n最终利用率: {table.utilization():.2%}")

test_shrink()
```

**输出**：

```
=== 插入测试 ===
调整大小: 1 -> 2
插入 0, size=1, capacity=2
插入 1, size=2, capacity=2
调整大小: 2 -> 4
插入 2, size=3, capacity=4
插入 3, size=4, capacity=4
调整大小: 4 -> 8
插入 4, size=5, capacity=8
插入 5, size=6, capacity=8
插入 6, size=7, capacity=8
插入 7, size=8, capacity=8

当前利用率: 100.00%

=== 删除测试 ===
删除 7, size=7, capacity=8
删除 6, size=6, capacity=8
删除 5, size=5, capacity=8
删除 4, size=4, capacity=8
删除 3, size=3, capacity=8
删除 2, size=2, capacity=8  <- 此时 size=2, capacity=8, 利用率=25%，收缩！

最终利用率: 25.00%
```

**收缩的摊销分析**：

```python
def shrink_analysis():
    """
    动态表收缩的摊销分析
    
    【关键洞察】
    收缩和扩张是"镜像"的：
    - 扩张时：复制已有元素，存入银行
    - 收缩时：从银行取款，支付复制费用
    
    只要扩张和收缩使用相同的阈值（1/2容量），摊销代价仍然是O(1)
    """
    print("""
    ┌─────────────────────────────────────────────────────────┐
    │  扩张和收缩的对称性                                      │
    │                                                         │
    │  扩张阈值：size = capacity                              │
    │  收缩阈值：size = capacity / 4                          │
    │                                                         │
    │  代价模式：                                              │
    │  ┌─────────────────────────────────────────────────┐   │
    │  │  ┌─────────────────┐                           │   │
    │  │  │    可用空间      │ ← size到capacity的差距   │   │
    │  │  ├─────────────────┤                           │   │
    │  │  │    已用空间      │ ← 复制操作时，这部分要移动 │   │
    │  │  └─────────────────┘                           │   │
    │  └─────────────────────────────────────────────────┘   │
    │                                                         │
    │  定理：对于使用相同扩张因子(2)和收缩阈值的动态表，        │
    │        n次任意操作序列的摊销代价是O(1)                   │
    └─────────────────────────────────────────────────────────┘
    """)
```

---

## 4️⃣ 摊销分析与数据结构的实际应用

### 应用一：可扩展数组 (Vector/ArrayList)

C++ 的 `vector`、Java 的 `ArrayList`、Python 的 `list` 都是动态表的实现：

```python
# Python list 的摊销分析
# 当 list.append() 触发扩容时，容量翻倍
# 这就是为什么 append 平均是 O(1) 的原因

import sys

def analyze_python_list_growth():
    """分析 Python list 的扩容行为"""
    sizes = []
    capacities = []
    
    # 不断 append 直到容量达到一定大小
    lst = []
    for i in range(20):
        lst.append(i)
        sizes.append(len(lst))
        # 获取容量（通过尝试访问更大的索引会触发异常，这里简化处理）
        # Python 内部会记录 capacity，但不对外暴露
    
    print("Python list append 示例：")
    print("  - 每次 append 通常是 O(1)")
    print("  - 偶尔的扩容是 O(n)")
    print("  - 平均摊销代价是 O(1)")
    
    return sizes

analyze_python_list_growth()
```

### 应用二：函数调用栈的深度

**问题**：递归调用的最大深度受限于系统栈的大小

**摊销视角**：即使单次递归很深，但函数返回会"释放"栈空间，总体是平衡的

### 应用三：数据库索引的 B+ 树分裂

```python
def b_plus_tree_split():
    """
    B+树节点分裂的摊销分析
    
    【类似动态表扩张】
    - 当节点满时分裂
    - 分裂代价 O(m)，但只是偶尔发生
    - 多次插入的总代价是 O(n log_m n)
    - 单次插入的摊销代价是 O(log_m n)
    """
    print("""
    ┌─────────────────────────────────────────────────────────┐
    │  B+树节点分裂                                            │
    │                                                         │
    │  满节点分裂：                                             │
    │  ┌───────────────────────────────────┐                 │
    │  │ [key1|key2|key3|key4|key5|...]    │ ← 节点满了      │
    │  └───────────────────────────────────┘                 │
    │                      ↓ 分裂                             │
    │  ┌─────────────┐       ┌─────────────┐                 │
    │  │ [key1|key2] │  ←→   │ [key4|key5] │                 │
    │  └─────────────┘       └─────────────┘                 │
    │                                                         │
    │  摊销分析：                                              │
    │  - n 次插入，最多 n-1 次分裂（满节点才会分裂）           │
    │  - 每次分裂复制 O(m) 个元素                             │
    │  - 总代价 O((n/m) × m) = O(n)                           │
    └─────────────────────────────────────────────────────────┘
    """)
```

---

## 5️⃣ 摊销分析 vs 其他分析方法

### 全面对比

```
┌────────────────────────────────────────────────────────────────────┐
│                        四种分析方法全面对比                          │
├──────────────┬───────────────┬──────────────┬─────────────────────┤
│    方法      │   关注点       │   结果类型     │    适用场景          │
├──────────────┼───────────────┼──────────────┼─────────────────────┤
│ 最坏情况分析  │ 单次操作最坏   │ O(W(n))       │ 硬实时系统、协议证明  │
├──────────────┼───────────────┼──────────────┼─────────────────────┤
│ 平均情况分析  │ 假设输入分布   │ O(A(n))       │ 概率算法、随机化分析  │
├──────────────┼───────────────┼──────────────┼─────────────────────┤
│ 摊销分析      │ 操作序列整体   │ O(A(n))       │ 偶尔昂贵的数据结构    │
├──────────────┼───────────────┼──────────────┼─────────────────────┤
│ 竞争分析      │ 在线 vs 最优   │ O(competitive)│ 在线算法、贪婪策略    │
└──────────────┴───────────────┴──────────────┴─────────────────────┘

【关键区别】
- 摊销分析 ≠ 平均情况分析：摊销不需要概率假设
- 摊销分析关注：最坏情况下，长期运行的平均代价
- 平均情况分析关注：期望输入分布下的平均表现
```

### 摊销分析的例子：表扩张 vs 哈希冲突

```python
def hash_vs_dynamic_table():
    """
    对比：哈希表的摊销 vs 动态表的摊销
    
    哈希表 (开放寻址)：
    - 插入可能冲突，需要探测
    - 最坏情况 O(n)
    - 平均情况 O(1/α)，α是负载因子
    
    动态表：
    - 插入总是 O(1) 实际
    - 偶尔扩容 O(n)
    - 摊销 O(1)
    """
    print("""
    ┌──────────────────────────────────────────────────────────┐
    │                      两种摊销场景对比                      │
    │                                                          │
    │  【动态表】：操作代价稳定，偶尔"涨价"                       │
    │  ┌────────────────────────────────────────────────────┐ │
    │  │  普通插入  │  普通插入  │  ★扩容  │  普通插入  │ ... │ │
    │  │   O(1)    │   O(1)    │  O(n)   │   O(1)    │      │ │
    │  └────────────────────────────────────────────────────┘ │
    │  摊销 = O(1)                                              │
    │                                                          │
    │  【哈希冲突】：操作代价不稳定，但整体可控                    │
    │  ┌────────────────────────────────────────────────────┐ │
    │  │  插入A  │  插入B  │  插入C  │  插入D  │  ...       │ │
    │  │   1    │   2    │   1    │   3    │            │ │
    │  │ (命中) │ (冲突1次)│ (命中) │ (冲突2次)│            │ │
    │  └────────────────────────────────────────────────────┘ │
    │  平均 = O(1/α)                                            │
    └──────────────────────────────────────────────────────────┘
    """)
```

---

## 📝 FAQ：常见疑问解答

### Q1：摊销分析和平均情况分析有什么区别？

**答**：最大的区别在于**是否需要概率假设**。

| | 摊销分析 | 平均情况分析 |
|--|---------|-------------|
| 是否需要输入分布 | ❌ 不需要 | ✅ 需要 |
| 分析对象 | 操作序列的整体 | 单次操作的期望 |
| 结果含义 | n次操作的平均代价上界 | 单次操作的期望代价 |
| 适用场景 | 确定性的数据结构操作 | 依赖随机性的算法 |

**举例**：
- 摊销分析：无论你输入什么顺序，动态表插入的摊销代价都是 O(1)
- 平均情况分析：假设输入均匀分布，快速排序的期望比较次数是 O(n log n)

### Q2：摊销代价意味着每次操作都这么快吗？

**答**：**不是！** 摊销代价是"长期平均"，不代表单次操作。

**例子**：
```
动态表插入：
- 普通插入：O(1) ← 很快
- 扩容插入：O(n) ← 很慢！

但如果你说"摊销代价是 O(1)"，意思是：
连续执行 n 次插入，平均每次是 O(1)
你不能保证某一次插入不是 O(n) 的！
```

**这很重要**：摊销分析**不适用于实时系统**，因为单次最坏情况可能很糟糕。

### Q3：什么时候用聚合分析，什么时候用势能法？

**答**：

```
┌────────────────────────────────────────────────────────┐
│  选择策略                                                │
│                                                        │
│  如果你能"直接看出"总代价的规律 → 聚合分析（简单直接）    │
│                                                        │
│  如果问题涉及"状态变化"或"势能积累" → 势能法（更灵活）     │
│                                                        │
│  如果想用"银行账户"思维 → 核算法（直觉好，但有时难设计）   │
│                                                        │
│  【推荐学习顺序】                                        │
│  1. 先理解聚合分析（最直观）                             │
│  2. 再学核算法（培养直觉）                               │
│  3. 最后学势能法（最强大）                               │
└────────────────────────────────────────────────────────┘
```

### Q4：势能函数怎么设计？

**答**：设计势能函数是**艺术**，不是科学。有几个原则：

```
设计势能函数的指导原则：

1. 【非负性】Φ(D) ≥ 0，且 Φ(D₀) = 0
   - 初始状态势能为0
   - 任何状态势能不为负

2. 【直观性】Φ(D) 应该反映"数据结构的不均衡程度"
   - 容量很大但数据很少 → Φ 较低
   - 容量将满但还要插入 → Φ 较高

3. 【代价匹配】势能变化应该能"吸收"实际代价的波动
   - 实际代价高时，势能变化为正（银行取款）
   - 实际代价低时，势能变化为负（存款）

【常用势能函数模式】

模式A：剩余空间
Φ = capacity - size

模式B：与一半容量的差距
Φ = |size - capacity/2|

模式C：2倍关系（动态表）
Φ = 2*size - capacity  (当 size > 0)
```

### Q5：动态表收缩时，为什么用 1/4 而不是 1/2？

**答**：这是一个**避免振荡**的设计！

```
【问题】如果用 1/2 作为收缩阈值：

时间1：插入到 capacity/2，触发扩张
时间2：删除到 capacity/2，触发收缩
时间3：插入到 capacity/2，触发扩张
时间4：删除到 capacity/2，触发收缩
...不断振荡！每次操作都 O(n)！

【解决】用 1/4 作为收缩阈值：

扩张阈值：size = capacity
收缩阈值：size = capacity/4

中间地带：[capacity/4, capacity) 是"安全区"
- 不会扩张也不会收缩
- 只有当利用率真正"太低"时才收缩
```

---

## 🎯 章节总结

### 核心知识点

```
┌─────────────────────────────────────────────────────────────────┐
│                        第17章知识图谱                             │
│                                                                 │
│                         ┌──────────────┐                        │
│                         │  摊销分析     │                        │
│                         └──────┬───────┘                        │
│                                │                                 │
│          ┌─────────────────────┼─────────────────────┐          │
│          ↓                     ↓                     ↓          │
│   ┌──────────────┐     ┌──────────────┐     ┌──────────────┐    │
│   │  聚合分析     │     │  核算法       │     │  势能法       │    │
│   │  Aggregate   │     │  Accounting  │     │  Potential   │    │
│   └──────────────┘     └──────────────┘     └──────────────┘    │
│          │                     │                     │          │
│          ↓                     ↓                     ↓          │
│   直接求和 T(n)           银行账户思维           状态势能变化       │
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐  │
│   │                    动态表分析                             │  │
│   │                                                          │  │
│   │   扩张: size = capacity → 新容量 = 2c                   │  │
│   │   收缩: size = capacity/4 → 新容量 = c/2                 │  │
│   │                                                          │  │
│   │   摊销代价: O(1)                                         │  │
│   └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 关键定理

```
定理17.1（动态表插入的摊销代价）
对于一个初始容量为m的动态表，执行n次插入操作，
如果容量每次翻倍，则总代价为 O(m + n)，摊销代价为 O(1)。

定理17.2（动态表的任意操作序列）
对于一个动态表，执行任意n次插入和删除操作，
如果扩张因子为2，收缩阈值为1/4，
则摊销代价为 O(1)。

定理17.3（二进制计数器）
n次递增操作使k位二进制计数器从0到2^k-1，
总代价为 O(n + k)，摊销代价为 O(1)。
```

### 学习要点

```
✅ 理解摊销分析与平均情况分析的本质区别
✅ 掌握三种摊销分析方法的核心思想
✅ 能用聚合分析计算简单问题的摊销代价
✅ 能用势能法分析动态表
✅ 理解动态表扩张和收缩的阈值设计
✅ 区分什么时候用哪种分析方法
```

---

## 📚 延伸阅读

### 推荐练习

1. **基础题**：用三种方法分别分析栈的 MultiPop 操作
2. **进阶题**：分析斐波那契堆（Fibonacci Heap）的摊销代价
3. **设计题**：设计一个支持 O(1) 摊销的 "队列的 Reversal" 操作

### 实际应用

- Python `list` 的扩容策略
- C++ `vector` 的扩张机制
- Java `ArrayList` 的容量管理
- 文件系统的块分配算法

### 相关章节

- 第10章（基本数据结构）：栈的实现
- 第11章（散列表）：哈希表的扩容
- 第19章（斐波那契堆）：摊销分析的高级应用

---

> 📝 **笔记心得**：摊销分析是一种"宏观视角"的算法分析，它不拘泥于单次操作的得失，而是着眼于整体表现。这种思维方式在系统设计、性能优化中非常有用。
