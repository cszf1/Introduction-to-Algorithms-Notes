# 第19章 二项堆 (Binomial Heaps)

> 本章介绍一种比普通堆更强大的优先队列数据结构——二项堆，它能在更短的时间内完成合并操作

## 📖 本章导读

### 🎯 问题引入：为什么需要二项堆？

**生活中的例子**：

想象你在管理一个**医院的急诊室**：

- **普通二叉堆的场景**：有多个病人需要按病情优先级排队，医生可以快速找到最紧急的病人。但如果有**两个医院需要合并**（比如灾难救援时），把两个堆合并成一个，需要重新建堆，效率很低。

- **二项堆的场景**：它由多个**二项树**组成，每个二项树有固定的形状。当需要合并两个二项堆时，只需要**按位合并**各个二项树，像做二进制加法一样简单。

**核心区别**：

| 操作 | 普通二叉堆 | 二项堆 |
|------|-----------|--------|
| INSERT | O(log n) | O(log n) |
| EXTRACT-MIN | O(log n) | O(log n) |
| UNION (合并) | O(n) | **O(log n)** ⭐ |

二项堆的**最大优势就是合并操作**！这个优势让它在需要频繁合并优先队列的场景中非常有用。

---

### 💡 二项堆的核心设计思想

二项堆的名字来源于**二项树**（Binomial Tree），这是一个数学概念：

```
二项树 B_k 的特点：
- 有 2^k 个结点
- 高度为 k
- 第 i 层有 C(k,i) 个结点（二项系数）
```

**为什么叫"二项"堆？**

因为二项树的结构正好对应二项式 (a+b)^k 展开后的各项系数！

```
B_0:      ○                    (1个结点)
          
B_1:      ○                    (2^1=2个结点)
         /
        ○
        
B_2:      ○                    (2^2=4个结点)
        / \
       ○   ○
      / \
     ○   ○
     
B_3:      ○                    (2^3=8个结点)
        /|\
       ○ ○ ○
      /|   |\
     ○ ○   ○ ○
    /|     |\
   ○ ○     ○ ○
```

---

## 1️⃣ 二项树的数学性质

### 1.1 二项树的递归定义

二项树 B_k 可以递归定义为：

```
B_0: 单个结点

B_k: 把 B_{k-1} 作为根结点的最左孩子，
     再把另一个 B_{k-1} 接到它右边
```

**图解递归过程**：

```
B_0:   ○

B_1: 把B_0接到根节点右边
      ○          ○
     /    →     / \
    ○          ○   ○
    
B_2: 再把B_1接到右边
          ○              ○
         / \            /|\
        ○   ○    →    ○ ○ ○
                    /|  |\
                   ○ ○ ○ ○
                   
B_3: 继续...
```

### 1.2 二项树的关键性质

```
┌─────────────────────────────────────────────────────────┐
│                 二项树 B_k 的四大性质                      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  性质1：结点数    → 恰好有 2^k 个结点                    │
│                                                         │
│  性质2：树高度    → 高度为 k                             │
│                                                         │
│  性质3：深度d结点数 → 恰有 C(k,d) 个结点                 │
│                                                         │
│  性质4：根节点度数 → 度数为 k（k个孩子）                  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**性质3的具体例子**（以 B_3 为例）：

```
深度0（C(3,0)=1）：    ○
                       ↑
深度1（C(3,1)=3）：  ○ ○ ○
                     ↑↑↑
深度2（C(3,2)=3）：  ○ ○ ○
depth=3 (C(3,3)=1)   ○
```

### 1.3 性质证明（理解性）

**性质1的证明（数学归纳法）**：

```
基例：B_0 只有1个结点 = 2^0 ✓

归纳假设：B_{k-1} 有 2^{k-1} 个结点

归纳步骤：B_k = 两个 B_{k-1} + 一个根结点
         = 2 × 2^{k-1} + 1 = 2^k ✓
```

---

## 2️⃣ 二项堆的定义与结构

### 2.1 什么是二项堆？

**二项堆**是由一组**二项树**组成的森林，满足以下堆性质：

```
堆性质1：每棵二项树都满足最小堆（或最大堆）性质
堆性质2：每种度数的二项树至多只有一棵
```

**第二个性质的意义**：

因为每棵二项树 B_k 恰好有 2^k 个结点，如果允许同度数树存在，
就不知道某 n 个结点应该如何分配。但有了这个性质后，**任何一个 n 
都可以唯一表示为若干 2^k 的和**（二进制表示！）

### 2.2 二项堆的结构示例

**一个包含 13 个结点的二项堆**：

```
13 的二进制：1101 = 8 + 4 + 1 = 2^3 + 2^2 + 2^0

所以这个二项堆包含：
- 一棵 B_3（8个结点）
- 一棵 B_2（4个结点）
- 一棵 B_0（1个结点）
```

**图示**：

```
二项堆 H（13个结点）：

B_3 (根=5)              B_2 (根=12)           B_0 (根=18)
    5                        12                     18
   /|\                      / \                     |
  / | \                     8  15                   25
 2  7  9                    |
 |   \                    20
 6    10
```

### 2.3 二项堆的链表结构

**头结点表（root list）**：将各二项树的根结点用指针连接起来

```
二项堆的根链表：

→[B_0根] ↔ [B_1根] ↔ [B_2根] ↔ [B_3根] ↔ ...
```

**为什么要用链表？**

- 方便遍历所有根结点
- 方便合并时链接两棵树
- 按度数从小到大排列（度数为k的树在链表第k位）

---

## 3️⃣ 二项堆的Python实现

### 3.1 结点结构

```python
class BinomialNode:
    """
    二项堆的结点结构
    
    属性说明：
    - key: 结点的值（用于比较优先级）
    - degree: 结点的度数（孩子个数）
    - parent: 指向父结点的指针
    - child: 指向第一个孩子的指针
    - sibling: 指向下一个兄弟结点的指针
    """
    
    def __init__(self, key):
        self.key = key          # 结点存储的关键字
        self.degree = 0         # 初始度数为0（没有孩子）
        self.parent = None      # 父结点指针
        self.child = None       # 第一个孩子指针
        self.sibling = None     # 下一个兄弟指针
```

### 3.2 合并两棵同度数的二项树（核心操作）

**生活类比**：

想象你有两个**相同大小的书架**（都是B_1，各有2个格子），
每个格子里放着书。你想把它们合并成一个大书架（B_2，4个格子）。

规则是：哪个书架顶部的书更小，就把那个书架放在上面。

```python
def binomial_link(y, z):
    """
    合并两棵同度数的二项树
    
    参数：
    - y: 第一棵树的根结点
    - z: 第二棵树的根结点
    假设 y.key <= z.key（y的根更小）
    
    返回：
    - 合并后的树根（较小者 y）
    
    图解：
    
    合并前：              合并后：
       y                     y
      / \                   /|\
     y1  y2                y1 y2 z
                                  \
                                   z1 z2
    """
    # 将 z 成为 y 的孩子（因为 y 的根更小）
    z.parent = y           # z的父指针指向y
    z.sibling = y.child   # z的兄弟指针指向y原来的第一个孩子
    y.child = z            # y的第一个孩子变为z
    y.degree += 1         # y的度数+1
```

**过程图解**：

```
步骤1：初始状态
        y              z
       /|\            /|\
      y1 y2 y3      z1 z2 z3
      
      假设 y.key <= z.key

步骤2：将 z 接到 y 下
        y (度数+1)
       /|\
      y1 y2 y3
          |
          z
         /|\
        z1 z2 z3

步骤3：最终状态
        y
       /|\
      y1 y2 y3
          |
          z (成为y的最后一个孩子)
         /|\
        z1 z2 z3
```

### 3.3 合并两个二项堆（UNION操作）

**生活类比**：

想象两个**二进制计数器**，每个计数器上有多位（代表B_0, B_1, B_2...）。
合并时，把对应位的值相加，就像做二进制加法一样。

```python
def binomial_heap_merge(h1, h2):
    """
    合并两个二项堆的根链表（按度数排序）
    
    这类似于合并两个有序链表
    
    返回：合并后的根链表
    
    图解：
    
    h1根链表:  [B_0] ↔ [B_1] ↔ [B_3]
    h2根链表:  [B_0] ↔ [B_2]
    
    合并后:    [B_0] ↔ [B_0] ↔ [B_1] ↔ [B_2] ↔ [B_3]
    (按度数排序)
    """
    # 初始化一个虚拟头结点，简化边界处理
    prev = Node()          # 虚拟头结点，prev指针追踪当前结点
    curr = prev            # curr用于构建新链表
    
    # 两个指针分别遍历两个根链表
    pointer1 = h1.head    # h1的根链表头
    pointer2 = h2.head    # h2的根链表头
    
    # 合并过程：类似归并排序的合并
    while pointer1 and pointer2:
        if pointer1.degree <= pointer2.degree:
            # pointer1的度数更小或相等，先链接pointer1
            curr.sibling = pointer1
            pointer1 = pointer1.sibling
        else:
            # pointer2的度数更小，先链接pointer2
            curr.sibling = pointer2
            pointer2 = pointer2.sibling
        curr = curr.sibling
    
    # 处理剩余部分（类似归并排序）
    # 如果h1还有剩余，直接链接
    if pointer1:
        curr.sibling = pointer1
    
    # 如果h2还有剩余，直接链接
    if pointer2:
        curr.sibling = pointer2
    
    return prev.sibling  # 返回合并后的链表头


def binomial_heap_union(h1, h2):
    """
    合并两个二项堆的核心操作：UNION
    
    步骤：
    1. 合并根链表（按度数排序）
    2. 调整根链表，处理相同度数的树
    
    时间复杂度：O(log n)
    
    图解：
    
    两个二项堆：          合并根链表后：        调整后：
    H1: B_0, B_2         B_0-B_0-B_1-B_2-B_3  最终堆
    H2: B_0, B_1, B_3    
    """
    # 步骤1：合并根链表
    merged = binomial_heap_merge(h1, h2)
    
    # 步骤2：调整根链表
    # 需要处理相同度数的树：
    # - 1棵树：不变
    # - 2棵树：合并成高度+1的树
    # - 3棵树：先合并两个，剩下一个
    
    if merged is None:
        return None
    
    prev = None           # 前一个结点
    x = merged            # 当前结点
    next_x = x.sibling    # 下一个结点
    
    while next_x:
        # 四种情况：
        # 1. x.degree != next_x.degree：直接前进
        # 2. x.degree == next_x.degree == next_next.degree：还是前进
        # 3. x.degree == next_x.degree != next_next.degree：合并这两个
        # 4. x.degree == next_x.degree == next_next.degree：先合前两个
        
        if (x.degree != next_x.degree or
            (next_x.sibling and next_x.sibling.degree == x.degree)):
            # 情况1,2：前进
            prev = x
            x = next_x
        else:
            # 情况3,4：合并 x 和 next_x
            # 确保 x.key <= next_x.key
            if x.key > next_x.key:
                # 如果x比next_x大，交换它们
                # 使得x是根较小的那个
                saved_x = x
                x = next_x
                next_x = saved_x
            
            # 现在 x.key <= next_x.key，将next_x合并到x下
            next_x.sibling = x.child  # next_x的兄弟指向x原来的孩子
            x.child = next_x          # x的第一个孩子指向next_x
            x.degree += 1            # x的度数+1
            
            # prev应该指向新的x（如果prev不为空）
            if prev:
                prev.sibling = x
            else:
                merged = x
            
            # next_x更新为x的下一个兄弟
            next_x = x.sibling
    
    return merged
```

### 3.4 插入操作

```python
def binomial_heap_insert(H, node):
    """
    向二项堆H中插入一个结点
    
    步骤：
    1. 创建一个只包含这个结点的二项堆
    2. 与原堆合并
    
    时间复杂度：O(log n)
    
    生活类比：
    就像把一个新病人加入急诊室排队队列，
    只需要把他的病历卡放到对应位置就行
    """
    # 创建一个只包含node的新堆
    H_prime = BinomialHeap()
    H_prime.head = node
    
    # 合并原堆和新堆
    return binomial_heap_union(H, H_prime)


def binomial_minimum(H):
    """
    返回二项堆H中的最小关键字
    
    时间复杂度：O(log n)
    原因：需要遍历所有根结点
    
    生活类比：
    就像在急诊室找病情最紧急的病人，
    需要查看每个医生负责的队伍的最前面
    """
    min_node = None
    min_key = float('inf')
    
    # 遍历根链表，找到最小的根
    x = H.head
    while x:
        if x.key < min_key:
            min_key = x.key
            min_node = x
        x = x.sibling
    
    return min_node
```

### 3.5 提取最小关键字（EXTRACT-MIN）

```python
def binomial_heap_extract_min(H):
    """
    删除并返回二项堆H中的最小关键字结点
    
    步骤：
    1. 找到最小关键字所在的根结点
    2. 删除该根结点，形成一个新的二项堆
    3. 把这个新堆的根链表反转（孩子变成根）
    4. 与原堆合并
    
    时间复杂度：O(log n)
    
    生活类比：
    就像处理完最紧急的病人后，需要把他负责的
    所有实习医生重新分配到各个队伍中
    """
    # 步骤1：找到最小根结点
    min_node = None
    min_prev = None
    prev = None
    x = H.head
    
    while x.sibling:
        if x.sibling.key < (min_node.key if min_node else float('inf')):
            min_node = x.sibling
            min_prev = x
        prev = x
        x = x.sibling
    
    # 步骤2：删除最小根结点
    if min_prev:
        min_prev.sibling = min_node.sibling
    else:
        H.head = min_node.sibling
    
    # 步骤3：反转min_node的孩子链表，形成新堆
    # 注意：孩子链表需要反转，因为它们原本是按从左到右的顺序，
    # 但我们需要的根链表要求按度数从小到大排列
    
    # 收集所有孩子（它们构成一个森林）
    children = []
    child = min_node.child
    while child:
        children.append(child)
        child = child.sibling
    
    # 反转孩子链表
    children.reverse()
    
    # 构建新堆的根链表
    H_prime = BinomialHeap()
    if children:
        # 孩子现在是根，按度数排序（其实就是reverse后的顺序）
        H_prime.head = children[0]
        current = children[0]
        for i in range(1, len(children)):
            current.sibling = children[i]
            current = children[i]
    
    # 步骤4：合并原堆和新堆
    return min_node, binomial_heap_union(H, H_prime)
```

---

## 4️⃣ 二项堆的C++实现

### 4.1 结点结构

```cpp
#include <iostream>
#include <limits>
#include <vector>
using namespace std;

/**
 * 二项堆的结点结构
 * 
 * 使用多重链表实现：
 * - parent: 指向父结点
 * - child: 指向第一个孩子
 * - sibling: 指向下一个兄弟
 */
struct BinomialNode {
    int key;                    // 结点关键字（用于优先级比较）
    int degree;                 // 结点的度数（孩子个数）
    BinomialNode* parent;       // 父结点指针
    BinomialNode* child;        // 第一个孩子指针
    BinomialNode* sibling;      // 下一个兄弟指针
    
    // 构造函数
    BinomialNode(int k) : key(k), degree(0), parent(nullptr), 
                          child(nullptr), sibling(nullptr) {}
};
```

### 4.2 二项堆类

```cpp
/**
 * 二项堆类
 * 
 * 核心操作：
 * - insert: 插入
 * - minimum: 获取最小值
 * - extractMin: 提取最小值
 * - union: 合并两个堆
 */
class BinomialHeap {
private:
    BinomialNode* head;  // 指向根链表头结点的指针

public:
    BinomialHeap() : head(nullptr) {}
    
    // 获取根链表头
    BinomialNode* getHead() { return head; }
    
    // 设置根链表头
    void setHead(BinomialNode* h) { head = h; }
```

### 4.3 核心操作实现

```cpp
/**
 * 合并两棵同度数的二项树
 * 
 * 前提：y 和 z 度数相同，且 y.key <= z.key
 * 结果：z 成为 y 的孩子
 */
void link(BinomialNode* y, BinomialNode* z) {
    // 将 z 链接为 y 的孩子
    z->parent = y;           // z的父指针指向y
    z->sibling = y->child;   // z的兄弟指向y原来的第一个孩子
    y->child = z;             // y的第一个孩子变为z
    y->degree++;              // y的度数+1
}

/**
 * 合并两个二项堆的根链表（按度数排序）
 * 
 * 类似于归并排序的合并过程
 */
BinomialNode* mergeRootList(BinomialNode* h1, BinomialNode* h2) {
    // 如果任一链表为空，直接返回另一个
    if (!h1) return h2;
    if (!h2) return h1;
    
    BinomialNode* result = nullptr;  // 结果链表头
    BinomialNode** lastPtr = &result;  // 指向链表尾结点的指针
    
    // 遍历两个链表，选择度数较小的结点
    while (h1 && h2) {
        if (h1->degree <= h2->degree) {
            *lastPtr = h1;
            h1 = h1->sibling;
        } else {
            *lastPtr = h2;
            h2 = h2->sibling;
        }
        lastPtr = &((*lastPtr)->sibling);
    }
    
    // 处理剩余部分
    if (h1) {
        *lastPtr = h1;
    } else {
        *lastPtr = h2;
    }
    
    return result;
}

/**
 * 合并两个二项堆
 * 
 * 时间复杂度: O(log n)
 */
BinomialHeap* unionHeaps(BinomialHeap* h1, BinomialHeap* h2) {
    BinomialHeap* h = new BinomialHeap();
    
    // 步骤1：合并根链表
    h->head = mergeRootList(h1->head, h2->head);
    
    if (!h->head) {
        return h;
    }
    
    // 步骤2：调整根链表，处理相同度数的树
    BinomialNode* prev = nullptr;
    BinomialNode* x = h->head;
    BinomialNode* next_x = x->sibling;
    
    while (next_x) {
        // 三种情况：
        // 1. x和next_x度数不同 → 前进
        // 2. x、next_x和next_x->sibling度数都相同 → 前进
        // 3. x和next_x度数相同 → 合并
        if ((x->degree != next_x->degree) ||
            (next_x->sibling && next_x->sibling->degree == x->degree)) {
            // 情况1,2：前进
            prev = x;
            x = next_x;
        } else {
            // 情况3：合并 x 和 next_x
            if (x->key <= next_x->key) {
                // x较小，next_x成为x的孩子
                x->sibling = next_x->sibling;
                link(x, next_x);
            } else {
                // next_x较小，需要调整链表
                if (prev) {
                    prev->sibling = next_x;
                } else {
                    h->head = next_x;
                }
                link(next_x, x);
                x = next_x;
            }
        }
        next_x = x->sibling;
    }
    
    return h;
}

/**
 * 插入一个新结点
 * 
 * 时间复杂度: O(log n)
 */
void insert(int key) {
    BinomialNode* node = new BinomialNode(key);
    BinomialHeap* h = new BinomialHeap();
    h->head = node;
    
    BinomialHeap* result = unionHeaps(this, h);
    this->head = result->head;
}

/**
 * 获取最小关键字
 * 
 * 时间复杂度: O(log n)
 */
int minimum() {
    if (!head) {
        throw runtime_error("Heap is empty");
    }
    
    BinomialNode* minNode = head;
    BinomialNode* x = head->sibling;
    
    // 遍历根链表找最小值
    while (x) {
        if (x->key < minNode->key) {
            minNode = x;
        }
        x = x->sibling;
    }
    
    return minNode->key;
}
```

---

## 5️⃣ 二项堆的操作汇总

### 5.1 各操作时间复杂度

```
┌─────────────────────────────────────────────────────────┐
│              二项堆各操作的时间复杂度                      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  操作              │ 时间复杂度  │ 说明                  │
│  ─────────────────┼────────────┼─────────────────────  │
│  MAKE-HEAP        │   O(1)     │ 创建空堆               │
│  INSERT          │   O(log n) │ 插入新结点             │
│  MINIMUM         │   O(log n) │ 找最小值（遍历根链表）  │
│  EXTRACT-MIN     │   O(log n) │ 删除最小值并返回        │
│  UNION           │ O(log n)   │ 合并两个堆 ⭐           │
│  DECREASE-KEY    │   O(log n) │ 减小某个关键字的值      │
│  DELETE          │   O(log n) │ 删除某个关键字          │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 5.2 二项堆 vs 二叉堆 vs 斐波那契堆

| 操作 | 二叉堆 | 二项堆 | 斐波那契堆 |
|------|--------|--------|-----------|
| INSERT | O(log n) | O(log n) | **O(1)** |
| MINIMUM | O(1) | O(log n) | **O(1)** |
| EXTRACT-MIN | O(log n) | O(log n) | O(log n)* |
| UNION | O(n) | **O(log n)** | **O(1)** |
| DECREASE-KEY | O(log n) | O(log n) | **O(1)** |

> * 斐波那契堆的摊销代价

**斐波那契堆**是二项堆的改进版，它在理论上更优（INSERT和UNION是常数时间），
但实现复杂，实际中二项堆已经足够好用。

---

## 6️⃣ FAQ：常见疑问解答

### Q1: 二项堆为什么要用链表连接根结点而不是数组？

**A**: 主要有两个原因：

1. **灵活扩展**：二项树的数量是动态变化的（取决于结点数n的二进制表示），
   用链表可以方便地插入和删除根结点，而数组需要预先分配固定大小。

2. **合并方便**：合并两个二项堆时，需要把两个根链表合并成一个，
   链表的合并操作比数组更简单（O(1) vs O(n)）。

**生活中的类比**：想象你在管理多个球队的比赛日程，
- 数组方式：预先固定好有多少支球队参加
- 链表方式：随时可以加入新球队或移除球队

### Q2: 为什么二项堆中每种度数的树至多只有一棵？

**A**: 这是由二项堆的性质决定的。

**证明**：假设n个结点的二项堆包含度数为k的树B_k（度数为k意味着有2^k个结点）。
如果有两棵B_k，就有2×2^k = 2^{k+1}个结点。

这在二进制表示中意味着第k位会重复，但这在二进制表示中是**不允许的**
（因为每个二进制位只能是0或1）。

所以，**每种度数的树至多只有一棵**。

### Q3: 二项堆的合并操作为什么像二进制加法？

**A**: 让我们用具体的例子来看：

假设合并两个二项堆 H1 和 H2：
- H1 有 B_0, B_2（代表 1 + 4 = 5 个结点）
- H2 有 B_1, B_2（代表 2 + 4 = 6 个结点）

合并后总共有 5 + 6 = 11 个结点，11 的二进制是 1011 = 8 + 2 + 1 = B_3 + B_1 + B_0

**合并过程模拟二进制加法**：

```
        H1:      B_0   B_1   B_2
        H2:      B_0   B_1   B_2
        
        从低到高按位处理：
        
        B_0: H1有 ✓  H2有 ✓  → 2棵B_0 → 合并成1棵B_1，进位1棵B_1
        
        B_1: H1无    H2有 ✓  + 进位1  → 2棵B_1 → 合并成1棵B_2，进位1棵B_2
        
        B_2: H1有 ✓  H2有 ✓  + 进位1  → 3棵B_2 → 合并成1棵B_3，进位1棵B_3
        
        B_3: 进位1  → 1棵B_3
        
        最终结果: B_0, B_1, B_3 (即 1+2+8=11个结点) ✓
```

这完全就是二进制加法的过程！

### Q4: 二项堆的度数最多是多少？

**A**: 如果二项堆有 n 个结点，那么根结点度数最多为 ⌊log₂ n⌋。

**原因**：因为 B_k 树有 2^k 个结点。如果度数 k > log₂ n，
那么这棵树单独的结点数就超过 n 了，不可能存在。

例如 n = 13，log₂ 13 ≈ 3.7，所以度数最多是 3。
这意味着二项堆最多只有 4 种不同度数的树（B_0, B_1, B_2, B_3）。

---

## 7️⃣ 本章小结

### 核心要点

```
┌─────────────────────────────────────────────────────────┐
│                    第19章核心要点                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1. 二项树 B_k：2^k个结点，高度k，递归定义                │
│                                                         │
│  2. 二项堆：由多个二项树组成的森林                        │
│     - 每棵二项树满足堆性质                              │
│     - 每种度数的树至多一棵                              │
│                                                         │
│  3. 核心操作：                                          │
│     - UNION（合并）：O(log n) ⭐                        │
│     - INSERT：O(log n)                                 │
│     - EXTRACT-MIN：O(log n)                            │
│                                                         │
│  4. 应用场景：需要频繁合并优先队列的场合                │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 与其他数据结构的对比

| 特性 | 普通堆 | 二项堆 | 斐波那契堆 |
|------|--------|--------|-----------|
| 结构 | 完全二叉树 | 二项树森林 | 二项树森林+标记 |
| 合并 | O(n) | O(log n) | O(1) |
| 插入 | O(log n) | O(log n) | O(1) |
| 实现难度 | 简单 | 中等 | 复杂 |

### 下章预告

下一章我们将学习**斐波那契堆**，它是二项堆的改进版，
通过引入"懒惰删除"和"级联剪切"技术，实现了更优的摊销代价。
虽然实现更复杂，但在理论分析和某些实际应用中非常有价值。

---

**📚 参考资料**：
- 《算法导论》第19章，二项堆
- 本文所有图解均为原创，便于理解核心概念
