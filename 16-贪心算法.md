# 第16章 贪心算法 (Greedy Algorithms)

> 本章介绍一种更直接、更"短视"的算法思想——在每一步都做出当前最优的选择

## 📖 本章导读

### 🎯 问题引入：什么是贪心？

**生活中的例子**：

想象你是一个背包空间有限的拾荒者，走在一条有很多宝贝的路上：
- 面前有一堆金银珠宝，你只能捡有限的几件
- **动态规划的思路**：精打细算，计算每种组合的总价值，挑最值钱的那一组
- **贪心算法的思路**：看到值钱且轻的，直接拿！不纠结

再比如，你从北京开车去上海，想要路程最短：
- **动态规划**：研究所有可能的路线组合，算出最短路径
- **贪心算法**：每次遇到岔路，就选那个指向目的地的路（如果有好几条，优先选看起来更近的）

**贪心算法的核心哲学**：目光短浅，但每一步都追求最优。

### 💡 贪心 vs 动态规划

| 特征 | 动态规划 | 贪心算法 |
|------|---------|---------|
| 选择时机 | 先考虑全局，再做选择 | 每步只管当前 |
| 子问题 | 需要保存所有子问题的解 | 不需要，只需要当前状态 |
| 最优性 | 一定得到全局最优解 | 可能只是局部最优 |
| 适用性 | 适用范围广 | 只适用于有"贪心选择性质"的问题 |
| 时间复杂度 | 通常较高 | 通常较低 |

**关键问题**：什么时候贪心能成功？

---

## 1️⃣ 贪心算法的两大性质

### 性质一：贪心选择性质 (Greedy-Choice Property)

**定义**：**全局最优解可以通过一系列局部最优选择（贪心选择）来达到。**

**生活类比**：想象你在一座山上，想找到最高点：
- **动态规划**：先研究整座山的地形图，找出所有山峰的高度，再选最高的
- **贪心算法**：每次往坡度最大的方向走，最终你会到达一个"局部最高点"

**关键问题**：这个局部最高点是全局最高点吗？

不一定！但如果我们知道这座山地形的某些特性（比如只有一个山峰），那贪心就能成功。

**数学表达**：
```
如果存在一个最优解可以通过"先做贪心选择"得到，
那么原问题一定有贪心选择性质
```

### 性质二：最优子结构 (Optimal Substructure)

**定义**：一个问题的最优解包含其子问题的最优解。

**这和动态规划一样！** 贪心算法也需要最优子结构。

**贪心 vs 动态规划的关键区别**：
- 动态规划：从**底向上**，从子问题逐步构建
- 贪心算法：从**顶向下**，先做选择，再解子问题

```
贪心算法的工作方式：
┌─────────────────────────────────────┐
│  原问题                              │
│    ↓（做贪心选择）                    │
│  简化后的问题                         │
│    ↓（做贪心选择）                    │
│  更简化的问题                         │
│    ↓ ...                            │
│  基础情况                            │
└─────────────────────────────────────┘

动态规划的工作方式：
┌─────────────────────────────────────┐
│  基础情况 ← 小问题的解               │
│    ↓（组合）                         │
│  稍大的问题 ← 稍大问题的解            │
│    ↓ ...                            │
│  原问题 ← 大问题的解                 │
└─────────────────────────────────────┘
```

---

## 2️⃣ 贪心算法的通用框架

### 标准伪代码

```python
def greedy_algorithm(problem):
    """
    贪心算法通用框架
    
    参数:
        problem: 待解决的问题
    返回:
        问题的最优解（如果贪心适用的话）
    """
    # 1. 初始化：确定问题的初始状态
    solution = initialize(problem)
    
    # 2. 循环：直到问题解决或无法继续
    while not is_complete(solution) and has_next_choice(problem):
        # 【关键】选择当前最好的选项
        # 这是"贪心"的核心！
        best_choice = select_best_choice(problem)
        
        # 应用这个选择
        solution = apply(solution, best_choice)
        
        # 更新问题状态
        problem = update_state(problem, best_choice)
    
    # 3. 返回最终解
    return solution
```

### 贪心算法的设计步骤

**第一步**：分析问题，判断是否适用贪心
- 是否存在贪心选择性质？
- 是否具有最优子结构？

**第二步**：确定贪心策略
- "最好"的定义是什么？
- 如何衡量当前选择的好坏？

**第三步**：正确性证明（重要！）
- 证明贪心选择能导出最优解

---

## 3️⃣ 经典案例一：活动选择问题 (Activity Selection)

### 问题描述

假设你有一间会议室，需要安排尽可能多的活动：

| 活动编号 | 开始时间 | 结束时间 |
|:-------:|:-------:|:-------:|
| 1 | 1 | 4 |
| 2 | 3 | 5 |
| 3 | 0 | 6 |
| 4 | 5 | 7 |
| 5 | 3 | 9 |
| 6 | 5 | 10 |
| 7 | 6 | 8 |
| 8 | 8 | 11 |
| 9 | 8 | 12 |
| 10 | 2 | 14 |
| 11 | 12 | 16 |

**问题**：选择哪些活动能使被安排的活动数量最多？

**约束**：任何两个活动不能时间重叠。

### 生活类比

想象你是一个展会策划：
- 展会有一个场地，但有很多活动想申请
- 每个活动有固定的时间段
- 你的目标是：安排尽可能多的活动

### 贪心策略分析

**直觉策略**：
1. 先安排时间最短的活动？
   - ❌ 不可行：活动1(1-4)比活动2(3-5)短，但可能影响更多后续活动

2. 先安排开始最早的活动？
   - ❌ 不可行：活动3(0-6)最早开始，但占用了大量时间

3. **先安排结束最早的活动！** ✅
   - 🎯 理由：越早结束，就越能留出时间给更多后续活动

### 过程图解

**贪心选择过程**（按结束时间排序）：

```
排序后：活动1(1-4) → 活动4(5-7) → 活动7(6-8) → 活动8(8-11) → 活动11(12-16)
（按结束时间排序）

Step 1: 选择活动1(1-4)
        ═══════                     ← 第一个活动，结束最早
        ├────┤

Step 2: 跳过活动2、3（在活动1结束前开始）
              ║
        ═══════ ║
        ├────┤ ║ ← 跳过
        
Step 3: 选择活动4(5-7)
              ═══════
              ├────┤

Step 4: 跳过活动5、6（在活动4结束前开始）
                    ║
              ═══════ ║
              ├────┤ ║ ← 跳过

Step 5: 选择活动7(6-8)
                    ═══════
                    ├────┤

Step 6: 跳过活动8之前开始的... 
                          ║
                    ═══════ ║
                    ├────┤ ║ ← 跳过

Step 7: 选择活动8(8-11)
                          ═══════
                          ├────┤

...依此类推
```

**最终选择**：活动1, 4, 7, 8, 11 = 5个活动 ✅

### 正确性证明

**引理（交换论证）**：
- 设S是按结束时间排序后的活动序列
- 设A是贪心算法选择的第一个活动（结束时间最早的）
- 设O是任意一个最优解
- 如果O的第一个活动不是A，则把O中结束时间晚于A的活动替换为A，得到的仍然是可行解，且活动数量不变

**核心思想**：结束最早的活动永远不会影响我们选择后续活动的可能性。

### 代码实现

#### Python 实现

```python
def activity_selection_verbose(start, end):
    """
    活动选择问题 - 贪心算法实现（详细注释版）
    
    参数:
        start: 活动开始时间列表
        end:   活动结束时间列表
    返回:
        选择的最大兼容活动集合
    
    时间复杂度: O(n log n)，主要来自排序
    """
    n = len(start)
    
    # ========== Step 1: 将活动按结束时间排序 ==========
    # 创建一个包含(结束时间, 开始时间, 索引)的列表
    activities = []
    for i in range(n):
        activities.append((end[i], start[i], i))
    
    # 按结束时间排序（Python内置排序是稳定的）
    activities.sort(key=lambda x: x[0])
    
    print("排序后的活动（按结束时间）:")
    for end_time, start_time, idx in activities:
        print(f"  活动{idx}: {start_time} - {end_time}")
    
    # ========== Step 2: 贪心选择 ==========
    # selected 用于记录选择的活动
    selected = []
    
    # 记录当前选择的活动的结束时间
    # 初始化为-1表示没有活动
    last_end_time = -1
    
    print("\n贪心选择过程:")
    for end_time, start_time, idx in activities:
        # 如果这个活动的开始时间 >= 上一个活动的结束时间
        # 说明它们是兼容的
        if start_time >= last_end_time:
            selected.append(idx)
            print(f"  ✓ 选择活动{idx} ({start_time}-{end_time})")
            last_end_time = end_time  # 更新结束时间
        else:
            print(f"  ✗ 跳过活动{idx} ({start_time}-{end_time}) - 与已选活动冲突")
    
    print(f"\n最终选择: {selected}")
    print(f"选择数量: {len(selected)}")
    
    return selected


# ========== 完整示例 ==========
def activity_selection_demo():
    """完整演示"""
    print("=" * 50)
    print("活动选择问题 - 贪心算法")
    print("=" * 50)
    
    # 活动数据
    start_times = [1, 3, 0, 5, 3, 5, 6, 8, 8, 2, 12]
    end_times   = [4, 5, 6, 7, 9, 10, 8, 11, 12, 14, 16]
    
    print("\n原始活动数据:")
    for i in range(len(start_times)):
        print(f"  活动{i}: {start_times[i]:2d}:00 - {end_times[i]:2d}:00")
    
    print()
    result = activity_selection_verbose(start_times, end_times)
    
    return result


# 运行演示
if __name__ == "__main__":
    activity_selection_demo()
```

#### C++ 实现

```cpp
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

// 活动结构体
struct Activity {
    int start;   // 开始时间
    int end;     // 结束时间
    int index;   // 原始索引
};

// 按结束时间排序的比较函数
bool compareByEnd(const Activity& a, const Activity& b) {
    return a.end < b.end;
}

// 活动选择问题 - 贪心算法
vector<int> activitySelection(const vector<int>& start, 
                              const vector<int>& end) {
    int n = start.size();
    
    // ========== Step 1: 创建并排序活动 ==========
    vector<Activity> activities;
    for (int i = 0; i < n; i++) {
        activities.push_back({start[i], end[i], i});
    }
    
    // 按结束时间排序
    sort(activities.begin(), activities.end(), compareByEnd);
    
    // ========== Step 2: 贪心选择 ==========
    vector<int> selected;        // 记录选择的活动的原始索引
    int lastEndTime = -1;        // 上一个选中活动的结束时间
    
    for (const auto& act : activities) {
        // 如果当前活动的开始时间 >= 上一个活动的结束时间
        if (act.start >= lastEndTime) {
            selected.push_back(act.index);  // 选择这个活动
            lastEndTime = act.end;          // 更新结束时间
        }
    }
    
    return selected;
}

// 详细打印版本
void activitySelectionVerbose(const vector<int>& start, 
                              const vector<int>& end) {
    int n = start.size();
    
    cout << "========== 活动选择问题 ==========" << endl;
    
    // Step 1: 排序
    vector<Activity> activities;
    for (int i = 0; i < n; i++) {
        activities.push_back({start[i], end[i], i});
    }
    sort(activities.begin(), activities.end(), compareByEnd);
    
    cout << "\n排序后的活动（按结束时间）:" << endl;
    for (const auto& act : activities) {
        cout << "  活动" << act.index << ": " 
             << act.start << " - " << act.end << endl;
    }
    
    // Step 2: 贪心选择
    cout << "\n贪心选择过程:" << endl;
    vector<int> selected;
    int lastEndTime = -1;
    
    for (const auto& act : activities) {
        if (act.start >= lastEndTime) {
            selected.push_back(act.index);
            cout << "  ✓ 选择活动" << act.index 
                 << " (" << act.start << "-" << act.end << ")" << endl;
            lastEndTime = act.end;
        } else {
            cout << "  ✗ 跳过活动" << act.index 
                 << " (" << act.start << "-" << act.end << ")" << endl;
        }
    }
    
    cout << "\n最终选择: ";
    for (int idx : selected) cout << idx << " ";
    cout << endl;
    cout << "选择数量: " << selected.size() << endl;
}

int main() {
    // 测试数据
    vector<int> start = {1, 3, 0, 5, 3, 5, 6, 8, 8, 2, 12};
    vector<int> end   = {4, 5, 6, 7, 9, 10, 8, 11, 12, 14, 16};
    
    activitySelectionVerbose(start, end);
    
    return 0;
}
```

### 复杂度分析

| 指标 | 复杂度 | 说明 |
|------|--------|------|
| 时间复杂度 | O(n log n) | 主要来自按结束时间排序 |
| 空间复杂度 | O(n) | 存储排序后的活动和结果 |

---

## 4️⃣ 经典案例二：哈夫曼编码 (Huffman Coding)

### 问题背景

**数据压缩**：如何用更少的比特表示一段文字？

**核心问题**：不同的字符出现的频率不同，频率高的字符应该用更短的编码。

**约束**：
- 任何字符的编码都不能是另一个字符编码的前缀（前缀-free）
- 否则解码时会混淆

### 问题引入

假设有一段文字包含这5个字符及其频率：

| 字符 | 频率 |
|:----:|:----:|
| f | 5 |
| e | 9 |
| c | 12 |
| b | 13 |
| a | 45 |

**问题**：如何给每个字符分配二进制编码，使总编码长度最短？

### 生活类比

想象你是一个图书管理员：
- 有很多书需要分类放书架
- 常用字母的书（如a, e）应该放在最方便拿的位置（最短编码）
- 罕见字母的书（如z, x）可以放在角落（更长编码）

**哈夫曼编码的"贪心策略"**：
1. 把所有字符当作叶子节点建树
2. 频率最低的两个节点合并，生成一个新节点
3. 重复步骤2，直到只剩一棵树
4. 树的左分支 = 0，右分支 = 1
5. 每个字符的编码 = 从根到叶子的路径

### 过程图解

**Step 1: 初始状态**

```
        频率分布:
        ┌───┐
        │ f │ 5
        ├───┤
        │ e │ 9
        ├───┤
        │ c │12
        ├───┤
        │ b │13
        ├───┤
        │ a │45
        └───┘
```

**Step 2: 合并 f(5) 和 e(9)**

```
        ┌───┐
        │ e │ 9
        ├───┤
        │ c │12
        ├───┤
        │ b │13
        ├───┤
        │ a │45
        └───┘
        
        ┌───────┐
        │ f+e   │14
        └───┬───┘
           / \
          f   e
         (5) (9)
```

**Step 3: 合并 c(12) 和 b(13)**

```
        ┌───┐
        │ e │ 9
        ├───┤
        │ a │45
        └───┘
        
        ┌───────┐       ┌───────┐
        │ f+e   │14     │ c+b   │25
        └───────┘       └───────┘
           / \            / \
          f   e          c   b
         (5) (9)        (12)(13)
```

**Step 4: 合并 e(9) 和 f+e(14)**

```
        ┌───┐
        │ a │45
        └───┘
        
        ┌───────┐       ┌───────┐
        │ c+b   │25     │e+f+e  │23
        └───────┘       └───────┘
           / \            / \
          c   b          e  / \
                          9 / \
                           f   e
                          (5) (9)
```

**Step 5: 合并 e+f+e(23) 和 c+b(25)**

```
        ┌───┐
        │ a │45
        └───┘
        
        ┌─────────────────────────────────┐
        │    (e+f+e)+(c+b) = 48            │
        └───────────────┬─────────────────┘
                        / \
                       /   \
              ┌───────┐   ┌───────┐
              │e+f+e  │23 │ c+b   │25
              └───────┘   └───────┘
                 / \        / \
                e  / \      c   b
               9 /   \
                f   e
               (5) (9)
```

**Step 6: 最终合并**

```
        ┌─────────────────────────────────┐
        │        ROOT = 93                 │
        └───────────────┬─────────────────┘
                        / \
                       /   \
                      /     \
              ┌───────┐     ┌───────┐
              │   a   │ 45  │  rest │ 48
              └───────┘     └───────┘
                           / \
                          /   \
                         /     \
                  ┌───────┐   ┌───────┐
                  │e+f+e  │23 │ c+b   │25
                  └───────┘   └───────┘
                     / \        / \
                    e  / \      c   b
                   9 /   \
                     f   e
                    (5) (9)
```

### 编码结果

| 字符 | 编码 | 频率 | 加权长度 |
|:----:|:----:|:----:|:--------:|
| a | 0 | 45 | 45×1 = 45 |
| b | 101 | 13 | 13×3 = 39 |
| c | 100 | 12 | 12×3 = 36 |
| d | 无（不存在） | - | - |
| e | 110 | 9 | 9×3 = 27 |
| f | 111 | 5 | 5×3 = 15 |

**总比特数**: 45 + 39 + 36 + 27 + 15 = 162 bits

**对比固定长度编码（3位）**: (5+9+12+13+45) × 3 = 252 bits

**压缩率**: 1 - 162/252 ≈ 35.7%

### 正确性证明

**哈夫曼算法的贪心选择性**：
设C是字符集合，f(c)是字符c的频率。
设x和y是频率最低的两个字符。

**引理**：存在一个最优前缀码，其中x和y的编码长度相同，且只在最后一位不同（一个是0，一个是1）。

**证明**：
1. 在最优前缀码树中，频率最低的两个字符一定在树的同一层（最深层）
2. 如果它们不在同一层，把最深层的两个叶子交换位置不会增加编码长度
3. 如果它们不在同一父节点下，把它们移到同一父节点下，也不会增加编码长度

**哈夫曼算法的最优子结构**：
- 合并x和y后，得到新字符z，频率为f(z) = f(x) + f(y)
- 对C' = (C - {x, y}) ∪ {z}的最优解，加上x和y作为z的子叶，就得到C的最优解

### 代码实现

#### Python 实现

```python
import heapq
from collections import Counter

class HuffmanNode:
    """哈夫曼树节点"""
    def __init__(self, char=None, freq=0, left=None, right=None):
        self.char = char    # 字符（叶子节点有值）
        self.freq = freq    # 频率
        self.left = left    # 左子节点
        self.right = right  # 右子节点
    
    def __repr__(self):
        if self.char:
            return f"HuffmanNode('{self.char}', {self.freq})"
        return f"HuffmanNode(None, {self.freq})"


def build_huffman_tree(text):
    """
    构建哈夫曼树
    
    参数:
        text: 输入文本
    返回:
        哈夫曼树根节点
    """
    # ========== Step 1: 统计字符频率 ==========
    freq = Counter(text)
    print("字符频率统计:")
    for char, count in sorted(freq.items(), key=lambda x: x[1]):
        print(f"  '{char}': {count}")
    
    # ========== Step 2: 使用最小堆构建树 ==========
    # 堆的元素是 (频率, 节点)
    # Python的heapq是最小堆
    
    # 初始化：所有字符都是叶子节点
    heap = []
    for char, f in freq.items():
        node = HuffmanNode(char=char, freq=f)
        # 注意：需要唯一标识，防止比较节点对象
        heapq.heappush(heap, (f, id(node), node))
    
    print(f"\n初始堆: {[(x[0], x[1]) for x in heap]}")
    
    # ========== Step 3: 贪心合并 ==========
    print("\n合并过程:")
    iteration = 0
    while len(heap) > 1:
        iteration += 1
        
        # 取出频率最低的两个节点
        freq1, _, node1 = heapq.heappop(heap)
        freq2, _, node2 = heapq.heappop(heap)
        
        print(f"  迭代{iteration}: 合并 {node1} 和 {node2}")
        
        # 创建新节点
        new_freq = freq1 + freq2
        new_node = HuffmanNode(freq=new_freq, left=node1, right=node2)
        
        # 放回堆中
        heapq.heappush(heap, (new_freq, id(new_node), new_node))
        print(f"    → 新节点频率: {new_freq}")
    
    # 返回根节点
    _, _, root = heapq.heappop(heap)
    return root


def generate_codes(root):
    """
    从哈夫曼树生成编码表
    
    参数:
        root: 哈夫曼树根节点
    返回:
        编码表 dict{字符: 编码}
    """
    codes = {}
    
    def dfs(node, code=""):
        """深度优先遍历生成编码"""
        if node is None:
            return
        
        # 叶子节点：记录编码
        if node.char is not None:
            codes[node.char] = code if code else "0"  # 特殊处理只有一个字符的情况
            print(f"  字符 '{node.char}' 的编码: {code if code else '0'}")
            return
        
        # 非叶子节点：继续遍历
        dfs(node.left, code + "0")
        dfs(node.right, code + "1")
    
    dfs(root)
    return codes


def huffman_coding(text):
    """
    哈夫曼编码完整流程
    
    参数:
        text: 输入文本
    返回:
        编码表和编码后的比特串
    """
    print("=" * 50)
    print("哈夫曼编码")
    print("=" * 50)
    
    # Step 1: 构建树
    print("\n【Step 1】构建哈夫曼树")
    root = build_huffman_tree(text)
    print(f"树构建完成，根节点频率: {root.freq}")
    
    # Step 2: 生成编码
    print("\n【Step 2】生成编码表")
    codes = generate_codes(root)
    
    # Step 3: 编码
    print("\n【Step 3】编码结果")
    encoded = ''.join(codes[char] for char in text)
    print(f"  原文: {text}")
    print(f"  编码: {encoded}")
    print(f"  原始比特数: {len(text) * 8}")
    print(f"  编码比特数: {len(encoded)}")
    print(f"  压缩比: {len(encoded) / (len(text) * 8) * 100:.1f}%")
    
    return codes, encoded


# ========== 演示 ==========
if __name__ == "__main__":
    # 测试文本
    text = "abacabad"
    
    # 运行哈夫曼编码
    codes, encoded = huffman_coding(text)
    
    print("\n" + "=" * 50)
    print("编码表汇总:")
    print("=" * 50)
    for char in sorted(codes.keys()):
        print(f"  '{char}': {codes[char]}")
```

#### C++ 实现

```cpp
#include <iostream>
#include <queue>
#include <unordered_map>
#include <string>
using namespace std;

// 哈夫曼树节点
struct HuffmanNode {
    char ch;                    // 字符（叶子节点有值）
    int freq;                   // 频率
    HuffmanNode* left;          // 左子节点
    HuffmanNode* right;         // 右子节点
    
    HuffmanNode(char c, int f) : ch(c), freq(f), left(nullptr), right(nullptr) {}
    HuffmanNode(int f, HuffmanNode* l, HuffmanNode* r) 
        : ch('\0'), freq(f), left(l), right(r) {}
};

// 用于优先队列比较（频率越小越优先）
struct Compare {
    bool operator()(HuffmanNode* a, HuffmanNode* b) {
        return a->freq > b->freq;  // 小顶堆
    }
};

// 构建哈夫曼树
HuffmanNode* buildHuffmanTree(const unordered_map<char, int>& freq) {
    priority_queue<HuffmanNode*, vector<HuffmanNode*>, Compare> pq;
    
    // 创建叶子节点并加入优先队列
    for (auto pair : freq) {
        pq.push(new HuffmanNode(pair.first, pair.second));
    }
    
    // 贪心合并
    while (pq.size() > 1) {
        HuffmanNode* left = pq.top(); pq.pop();
        HuffmanNode* right = pq.top(); pq.pop();
        
        cout << "合并: '" << (left->ch ? left->ch : '#') 
             << "'(" << left->freq << ") + '" 
             << (right->ch ? right->ch : '#') 
             << "'(" << right->freq << ")" << endl;
        
        HuffmanNode* parent = new HuffmanNode(
            left->freq + right->freq, left, right
        );
        pq.push(parent);
    }
    
    return pq.top();
}

// 生成编码表
void generateCodes(HuffmanNode* root, const string& code, 
                   unordered_map<char, string>& codes) {
    if (root == nullptr) return;
    
    // 叶子节点
    if (root->ch != '\0') {
        codes[root->ch] = code.empty() ? "0" : code;
        cout << "字符 '" << root->ch << "' 的编码: " 
             << (code.empty() ? "0" : code) << endl;
        return;
    }
    
    generateCodes(root->left, code + "0", codes);
    generateCodes(root->right, code + "1", codes);
}

// 释放树内存
void deleteTree(HuffmanNode* root) {
    if (root == nullptr) return;
    deleteTree(root->left);
    deleteTree(root->right);
    delete root;
}

int main() {
    cout << "========== 哈夫曼编码 ==========" << endl;
    
    string text = "abacabad";
    
    // 统计频率
    unordered_map<char, int> freq;
    for (char c : text) {
        freq[c]++;
    }
    
    cout << "\n字符频率:" << endl;
    for (auto p : freq) {
        cout << "  '" << p.first << "': " << p.second << endl;
    }
    
    // 构建树
    cout << "\n合并过程:" << endl;
    HuffmanNode* root = buildHuffmanTree(freq);
    
    // 生成编码
    cout << "\n编码结果:" << endl;
    unordered_map<char, string> codes;
    generateCodes(root, "", codes);
    
    // 编码
    string encoded;
    for (char c : text) {
        encoded += codes[c];
    }
    
    cout << "\n编码结果:" << endl;
    cout << "  原文: " << text << endl;
    cout << "  编码: " << encoded << endl;
    cout << "  原始比特: " << text.length() * 8 << endl;
    cout << "  编码比特: " << encoded.length() << endl;
    cout << "  压缩比: " << (double)encoded.length() / (text.length() * 8) * 100 << "%" << endl;
    
    // 清理
    deleteTree(root);
    
    return 0;
}
```

### 复杂度分析

| 指标 | 复杂度 | 说明 |
|------|--------|------|
| 时间复杂度 | O(n log n) | n为字符种类数，堆操作 |
| 空间复杂度 | O(n) | 存储树和编码表 |

---

## 5️⃣ 经典案例三：分数背包问题 (Fractional Knapsack)

### 问题描述

你是一个小偷，背包容量是50：
- 金条：重量30，价值300元
- 银条：重量20，价值250元
- 铜条：重量40，价值200元

**问题**：怎么偷才能价值最大化？

**注意**：这里可以偷一部分！（分数背包）
- 可以只偷金条的1/2，价值150元

### 贪心策略

**策略**：按"价值/重量"（单位价值）排序，优先拿单位价值最高的！

### 过程图解

**Step 1: 计算单位价值**

| 物品 | 重量 | 价值 | 价值/重量 |
|:----:|:----:|:----:|:---------:|
| 金条 | 30 | 300 | 10 |
| 银条 | 20 | 250 | 12.5 |
| 铜条 | 40 | 200 | 5 |

排序后：银条 > 金条 > 铜条

**Step 2: 贪心选择**

```
背包容量: 50

Step 1: 拿银条（价值/重量最高 = 12.5）
        剩余容量: 50 - 20 = 30
        当前价值: 250

Step 2: 拿金条（价值/重量 = 10）
        剩余容量: 30 - 30 = 0
        当前价值: 250 + 300 = 550

Step 3: 背包已满，停止
```

**最终结果**：
- 银条：全部（20，250元）
- 金条：全部（30，300元）
- 铜条：0
- **总价值：550元**

### 对比：0-1背包问题不能用贪心！

| 问题类型 | 贪心适用 | 原因 |
|---------|---------|------|
| 分数背包 | ✅ 可以 | 可以取部分，总能填满背包 |
| 0-1背包 | ❌ 不行 | 必须整体拿，贪心可能选错 |

**0-1背包的反例**：
- 背包容量10
- 物品A：重量6，价值30（单位价值5）
- 物品B：重量10，价值40（单位价值4）

**贪心选择A**：拿A后还剩4容量，无法再拿东西，总价值30
**最优解**：拿B，总价值40

### 代码实现

#### Python 实现

```python
def fractional_knapsack_verbose(items, capacity):
    """
    分数背包问题 - 贪心算法
    
    参数:
        items: 物品列表，每项是 (重量, 价值) 元组
        capacity: 背包容量
    返回:
        最大价值和选择方案
    
    时间复杂度: O(n log n)
    """
    n = len(items)
    
    # ========== Step 1: 计算单位价值并排序 ==========
    # items_with_ratio: (重量, 价值, 单位价值, 原始索引)
    items_with_ratio = []
    for i, (weight, value) in enumerate(items):
        ratio = value / weight if weight > 0 else 0
        items_with_ratio.append((weight, value, ratio, i))
    
    # 按单位价值降序排序
    items_with_ratio.sort(key=lambda x: x[2], reverse=True)
    
    print("按单位价值排序后:")
    for weight, value, ratio, idx in items_with_ratio:
        print(f"  物品{idx}: 重量={weight}, 价值={value}, "
              f"单位价值={ratio:.2f}")
    
    # ========== Step 2: 贪心选择 ==========
    total_value = 0.0      # 总价值
    remaining = capacity    # 剩余容量
    selected = []           # 选择记录
    
    print(f"\n背包容量: {capacity}")
    print("\n选择过程:")
    
    for weight, value, ratio, idx in items_with_ratio:
        if remaining <= 0:
            print(f"  物品{idx}: 背包已满，无法选择")
            break
        
        if weight <= remaining:
            # 能完整装下
            selected.append((idx, 1.0, value))  # 取100%
            total_value += value
            remaining -= weight
            print(f"  物品{idx}: 全部放入 (取1.0份, 价值+{value})")
        else:
            # 只取一部分
            fraction = remaining / weight
            value_added = value * fraction
            selected.append((idx, fraction, value_added))
            total_value += value_added
            print(f"  物品{idx}: 只取 {fraction:.2%} ({value_added:.2f}元)")
            remaining = 0
    
    print(f"\n最终结果:")
    print(f"  总价值: {total_value:.2f}")
    print(f"  选择方案: {selected}")
    
    return total_value, selected


# ========== 演示 ==========
def demo():
    print("=" * 50)
    print("分数背包问题")
    print("=" * 50)
    
    # 物品: (重量, 价值)
    items = [
        (30, 300),   # 金条
        (20, 250),   # 银条
        (40, 200),   # 铜条
    ]
    
    capacity = 50
    
    total, selected = fractional_knapsack_verbose(items, capacity)


if __name__ == "__main__":
    demo()
```

#### C++ 实现

```cpp
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

// 物品结构
struct Item {
    int id;           // 原始索引
    double weight;    // 重量
    double value;     // 价值
    double ratio;     // 价值/重量
    
    Item(int i, double w, double v) : id(i), weight(w), value(v) {
        ratio = (w > 0) ? v / w : 0;
    }
};

// 分数背包问题
double fractionalKnapsack(vector<Item>& items, double capacity) {
    // 按单位价值排序
    sort(items.begin(), items.end(), [](const Item& a, const Item& b) {
        return a.ratio > b.ratio;
    });
    
    cout << "排序后物品:" << endl;
    for (const auto& item : items) {
        cout << "  物品" << item.id << ": 价值/重量=" << item.ratio << endl;
    }
    
    double totalValue = 0.0;
    double remaining = capacity;
    
    cout << "\n选择过程:" << endl;
    for (const auto& item : items) {
        if (remaining <= 0) break;
        
        if (item.weight <= remaining) {
            // 全部放入
            totalValue += item.value;
            remaining -= item.weight;
            cout << "  物品" << item.id << ": 全部放入 (+" << item.value << ")" << endl;
        } else {
            // 部分放入
            double fraction = remaining / item.weight;
            double valueAdded = item.value * fraction;
            totalValue += valueAdded;
            remaining = 0;
            cout << "  物品" << item.id << ": 部分放入 (+" << valueAdded << ")" << endl;
        }
    }
    
    return totalValue;
}

int main() {
    cout << "========== 分数背包问题 ==========" << endl;
    
    // 物品: (重量, 价值)
    vector<Item> items;
    items.emplace_back(0, 30, 300);  // 金条
    items.emplace_back(1, 20, 250);  // 银条
    items.emplace_back(2, 40, 200);  // 铜条
    
    double capacity = 50;
    double result = fractionalKnapsack(items, capacity);
    
    cout << "\n最大价值: " << result << endl;
    
    return 0;
}
```

---

## 6️⃣ 经典案例四：最小生成树 (Minimum Spanning Tree)

### 问题背景

**生成树**：一个无向图的生成树是包含所有顶点且边数最少的连通子图（恰好n-1条边）。

**最小生成树（MST）**：边的总权重最小的生成树。

### 生活类比

想象你要给城市铺设燃气管道：
- 有n个小区，需要铺设管道把它们连起来
- 两两之间铺设的成本不同
- 目标：把所有小区连起来，且总成本最低

### 两种贪心算法

#### 算法一：Prim算法

**核心思想**：从一个点开始，逐步向外扩展，每次选择距离"当前已选集合"最近的点。

```
过程图解：

初始: 只有顶点A在集合中

Step 1: A连接的边中，最短的是A-B(4)
        → 选择B

Step 2: {A, B}集合中，最短的是B-C(3)
        → 选择C

Step 3: {A, B, C}集合中，最短的是C-E(5) 或 A-D(6)
        → 选择E

...依此类推

最终得到最小生成树
```

#### 算法二：Kruskal算法

**核心思想**：把所有边按权重排序，从小到大依次考虑，只要不形成环就加入。

```
过程图解：

初始: 所有顶点独立

Step 1: 最短边A-B(3) → 加入（不形成环）

Step 2: 最短边B-C(4) → 加入（不形成环）

Step 3: 最短边A-D(5) → 加入（不形成环）

Step 4: 最短边C-E(6) → 加入（不形成环）

Step 5: 最短边D-E(7) → 跳过（会形成环）

...直到有n-1条边
```

### 代码实现

#### Python - Kruskal算法

```python
class UnionFind:
    """并查集 - 用于检测环"""
    
    def __init__(self, n):
        """初始化：每个节点都是独立的集合"""
        self.parent = list(range(n))  # parent[i] = i的父节点
        self.rank = [0] * n            # 树的高度
    
    def find(self, x):
        """查找x的根节点（带路径压缩）"""
        if self.parent[x] != x:
            self.parent[x] = self.find(self.parent[x])  # 路径压缩
        return self.parent[x]
    
    def union(self, x, y):
        """合并x和y所在的集合"""
        px, py = self.find(x), self.find(y)
        if px == py:
            return False  # 已经在同一集合，会形成环
        # 按秩合并
        if self.rank[px] < self.rank[py]:
            px, py = py, px
        self.parent[py] = px
        if self.rank[px] == self.rank[py]:
            self.rank[px] += 1
        return True


def kruskal_verbose(vertices, edges):
    """
    Kruskal算法 - 最小生成树
    
    参数:
        vertices: 顶点列表
        edges: 边列表 [(权重, u, v), ...]
    返回:
        MST的总权重和边列表
    
    时间复杂度: O(E log E)
    """
    n = len(vertices)
    
    # ========== Step 1: 按权重排序 ==========
    edges.sort()  # 按第一个元素（权重）排序
    print("边按权重排序:")
    for weight, u, v in edges:
        print(f"  ({vertices[u]}, {vertices[v]}): {weight}")
    
    # ========== Step 2: 贪心选择 ==========
    uf = UnionFind(n)
    mst_edges = []
    total_weight = 0
    
    print("\n选择过程:")
    for weight, u, v in edges:
        # 检查是否形成环
        if uf.union(u, v):
            mst_edges.append((vertices[u], vertices[v], weight))
            total_weight += weight
            print(f"  ✓ 选择 ({vertices[u]}, {vertices[v]}): {weight}")
        else:
            print(f"  ✗ 跳过 ({vertices[u]}, {vertices[v]}): {weight} （会形成环）")
        
        # 已有n-1条边，可以停止了
        if len(mst_edges) == n - 1:
            break
    
    print(f"\n最小生成树:")
    print(f"  边: {mst_edges}")
    print(f"  总权重: {total_weight}")
    
    return total_weight, mst_edges


# ========== 演示 ==========
def demo():
    print("=" * 50)
    print("Kruskal算法 - 最小生成树")
    print("=" * 50)
    
    vertices = ['A', 'B', 'C', 'D', 'E']
    # 边: (权重, u, v)
    edges = [
        (1, 0, 1),   # A-B
        (4, 0, 3),   # A-D
        (3, 0, 2),   # A-C
        (2, 1, 3),   # B-D
        (5, 1, 4),   # B-E
        (7, 2, 3),   # C-D
        (8, 2, 4),   # C-E
        (9, 3, 4),   # D-E
    ]
    
    total, edges = kruskal_verbose(vertices, edges)


if __name__ == "__main__":
    demo()
```

### 正确性证明（Kruskal算法）

**贪心选择证明**：
设e是按权重排序后第一条被考虑的边（最小权重边）。
设T是任意一个最小生成树。

- 如果e已经在T中，证毕。
- 如果e不在T中，T+e会形成一个环。
  - 环中至少有一条边f的权重≥e的权重（因为e是最小权重边）。
  - 用e替换f，得到T' = T - f + e。
  - T'仍然是生成树，且权重≤T的权重。
  - 因此T'也是最小生成树，且包含e。

**最优子结构证明**：
- 合并e和f后，剩下的边仍构成子图的最小生成树。

---

## 7️⃣ 经典案例五：单源最短路径 - Dijkstra算法

### 问题描述

给定一个有向图（有非负权重）和一个起点，找到从起点到所有其他顶点的最短路径。

### 生活类比

导航系统：
- 你是起点
- 要去很多目的地
- 每条路有不同的长度
- 每次你想去最近的目的地，导航会告诉你最短路

### 贪心策略

**核心思想**：每次选择当前距离最短的未处理顶点。

### 过程图解

```
初始: 起点A的距离=0，其他=∞

Step 1: 选择A（距离0），处理A的邻居
        → B距离=4, C距离=2, D距离=∞

Step 2: 选择C（距离2），处理C的邻居
        → B距离=3(经过C), D距离=6

Step 3: 选择B（距离3），处理B的邻居
        → D距离=4(经过B)

Step 4: 选择D（距离4）
        处理完成
```

### 代码实现

#### Python实现

```python
import heapq
from collections import defaultdict

def dijkstra_verbose(graph, start):
    """
    Dijkstra算法 - 单源最短路径
    
    参数:
        graph: 邻接表 {u: [(v, weight), ...]}
        start: 起点
    返回:
        到各点的最短距离
    
    时间复杂度: O((V+E) log V) 使用优先队列
    """
    print(f"起点: {start}")
    
    # ========== 初始化 ==========
    dist = {v: float('inf') for v in graph}
    dist[start] = 0
    visited = set()
    pq = [(0, start)]  # (距离, 顶点)
    
    print(f"\n初始距离: {dist}")
    
    # ========== 贪心选择 ==========
    step = 0
    while pq:
        step += 1
        
        # 选择距离最小的未访问顶点
        d, u = heapq.heappop(pq)
        
        if u in visited:
            continue
        
        visited.add(u)
        print(f"\nStep {step}: 选择顶点 {u}（距离={d}）")
        
        # 更新邻居的距离
        print(f"  处理邻居:")
        for v, weight in graph[u]:
            if v in visited:
                continue
            
            new_dist = d + weight
            if new_dist < dist[v]:
                dist[v] = new_dist
                heapq.heappush(pq, (new_dist, v))
                print(f"    → {v}: {dist[v]}→{new_dist} ✓")
            else:
                print(f"    → {v}: {dist[v]}（更短，不更新）")
    
    print(f"\n最终距离: {dist}")
    return dist


# ========== 演示 ==========
def demo():
    print("=" * 50)
    print("Dijkstra算法 - 单源最短路径")
    print("=" * 50)
    
    # 邻接表
    graph = {
        'A': [('B', 4), ('C', 2)],
        'B': [('C', 1), ('D', 5)],
        'C': [('D', 8), ('E', 10)],
        'D': [('E', 2)],
        'E': []
    }
    
    dijkstra_verbose(graph, 'A')


if __name__ == "__main__":
    demo()
```

---

## 8️⃣ 贪心算法的适用性判断

### 什么样的问题可以用贪心？

**必须满足两个条件**：

1. **贪心选择性质**：存在一个最优解是通过一系列局部最优选择得到的
2. **最优子结构**：问题的最优解包含子问题的最优解

### 如何判断？

**反例法**：尝试找出一个反例，说明贪心会失败

**分数背包 vs 0-1背包**：

```
反例：背包容量10
物品1：重量6，价值30
物品2：重量10，价值40

贪心选择物品1：价值30
最优选择：物品2：价值40
→ 贪心失败！
```

### 常见问题的贪心适用性

| 问题 | 贪心适用 | 贪心策略 |
|------|---------|---------|
| 活动选择 | ✅ | 按结束时间排序 |
| 哈夫曼编码 | ✅ | 合并频率最低的两个 |
| 分数背包 | ✅ | 按单位价值排序 |
| 0-1背包 | ❌ | 需要动态规划 |
| 最短路径(Dijkstra) | ✅ | 选择当前最短路径 |
| 最小生成树 | ✅ | Prim或Kruskal |
| 作业调度 | ✅/❌ | 取决于约束条件 |

---

## 9️⃣ 本章小结

### 核心要点

1. **贪心算法的思想**：每一步都做当前最优的选择，希望最终达到全局最优

2. **适用条件**：
   - 贪心选择性质
   - 最优子结构

3. **贪心 vs 动态规划**：
   - 贪心：从上到下，先选择后求解子问题
   - 动态规划：从下到上，先求解子问题再组合

4. **经典应用**：
   - 活动选择问题
   - 哈夫曼编码
   - 分数背包
   - 最小生成树
   - 最短路径

### 易错点

1. **混淆贪心和动态规划**：并不是所有最优解问题都能用贪心
2. **忽略边界情况**：空集合、只有一个元素等
3. **贪心选择不正确**：需要仔细分析什么才是"最优的局部选择"

### 与其他章节的联系

- **第15章（动态规划）**：贪心是动态规划的特例，要求更强
- **第21章（并查集）**：Kruskal算法中使用
- **第23章（图算法）**：Prim算法和Dijkstra算法

---

## ❓ FAQ - 常见疑问

### Q1: 贪心算法一定能得到最优解吗？

**A**: 不一定！只有当问题满足"贪心选择性质"时，贪心才能保证最优解。

**例子**：0-1背包问题，贪心可能给出次优解。

**如何验证**：通常需要通过"交换论证"或"数学归纳法"来证明。

---

### Q2: 如何判断一个问题是否适用贪心？

**A**: 
1. 尝试用贪心策略做一做
2. 找反例：如果能找到反例，说明贪心不适用
3. 形式化证明：如果找不到反例，尝试用数学方法证明

---

### Q3: 为什么哈夫曼编码要合并频率最低的两个？

**A**: 因为频率低的字符在最终编码中应该有更长的编码。通过把它们合并，我们可以让它们共享编码前缀，从而节省空间。

---

### Q4: 分数背包为什么能用贪心？

**A**: 因为物品可以分割！我们总是可以：
1. 先拿单位价值最高的物品
2. 如果装不下，就取一部分填满背包

这样一定能达到最优，因为任何最优解如果没用完容量，都可以用单位价值更高的物品替换。

---

### Q5: Dijkstra算法和Prim算法有什么关系？

**A**: 它们几乎是同一个算法！

| 算法 | 目标 | 维护的数据 |
|------|------|-----------|
| Dijkstra | 单源最短路径 | 源点到各点的距离 |
| Prim | 最小生成树 | 已选集合到未选集合的距离 |

核心思想都是："每次选择当前最近的顶点"。

---

## 📝 课后练习

### 基础题

1. **活动选择**：证明为什么按结束时间排序的贪心算法是正确的。

2. **哈夫曼编码**：给定字符频率{a:10, b:15, c:30, d:45}，手动构建哈夫曼树并写出编码。

3. **分数背包**：背包容量15，物品(重量, 价值)={(5,5), (10,8), (15,10)}，求最大价值。

### 提高题

4. **0-1背包反例**：构造一个反例，说明分数背包的贪心策略不适用于0-1背包。

5. **区间调度扩展**：如果要求安排活动使得总时间最长（而非活动数量最多），贪心策略应该如何修改？

6. **Huffman编码变体**：如果要求所有字符的编码长度不超过k，应该如何修改算法？

### 编程题

7. 实现Prim算法的最小生成树算法。

8. 实现一个简单的文件压缩工具（使用哈夫曼编码）。

---

## 📚 参考资料

- Thomas H. Cormen et al., *Introduction to Algorithms*, Chapter 16
- 《算法导论》第16章：贪心算法
