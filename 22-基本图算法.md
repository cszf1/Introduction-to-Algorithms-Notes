# 第22章 基本图算法（Elementary Graph Algorithms）

> 本章是图算法的入门——从图的存储方式，到两种核心搜索策略（BFS和DFS），再到两个经典应用（拓扑排序和强连通分量）。图算法是算法世界的"高速公路"，无数问题最终都能归结为图上的问题

## 📖 本章导读

### 🎯 问题引入：为什么需要图算法？

**生活中的例子**：

想象你是一个**快递站长**：

- 你需要知道哪些城市之间有直达路线（**图的表示**）
- 你需要找到从仓库到每个目的地的最短路径（**广度优先搜索**）
- 你需要遍历所有站点，了解路线的层次结构（**深度优先搜索**）
- 你需要安排发货顺序，确保A在B之前发出（**拓扑排序**）
- 你需要找出哪些城市之间可以互相到达（**强连通分量**）

图算法是计算机科学中**最重要的工具之一**——社交网络、地图导航、编译器、网络协议……几乎处处都是图。

**本章路线图**：

```
图的表示 ──→ 广度优先搜索(BFS) ──→ 深度优先搜索(DFS) ──→ 拓扑排序 ──→ 强连通分量
 22.1           22.2                 22.3               22.4          22.5
 基础设施      逐层探索              一条路走到黑        排先后顺序    找互相到达的区域
```

---

## 1️⃣ 图的表示（22.1 Representations of Graphs）

### 1.1 图的基本概念

**图 G = (V, E)** 由顶点集 V 和边集 E 组成。

**生活中的类比**：

- **顶点**：城市
- **边**：城市之间的公路
- **有向图**：单行道（u→v 和 v→u 是两条不同的边）
- **无向图**：双行道（u—v 只算一条边）

**关键符号约定**：

| 符号 | 含义 | 类比 |
|------|------|------|
| \|V\| | 顶点数 | 城市数量 |
| \|E\| | 边数 | 公路数量 |
| 稀疏图 | \|E\| 远小于 \|V\|² | 高速公路网（城市多，直连少） |
| 稠密图 | \|E\| 接近 \|V\|² | 城市间几乎都有直连 |

### 1.2 邻接链表（Adjacency List）

**核心思想**：每个顶点维护一个链表，记录它所有的邻居。

**图例**：

```
无向图 G：            邻接链表表示：
    1 ── 2            1 → [2, 5]
    | \   |           2 → [1, 5, 3, 4]
    |   \ |           3 → [2, 4]
    5 ── 3            4 → [2, 3]
    |   /             5 → [1, 2, 4]
    | /
    4

有向图 G：            邻接链表表示：
    1 → 2             1 → [2, 4]
    ↓   ↓             2 → [5]
    3 ← 4             3 → [2, 6]
    ↓   ↓             4 → [5]
    6 ← 5             5 → [3]
                      6 → []
```

**存储空间**：
- 有向图：Θ(V + E)（每条边只在出发顶点的链表出现一次）
- 无向图：Θ(V + E)（每条边在两个顶点的链表各出现一次，所以是 Θ(V + 2E)）

**邻接链表的优缺点**：

| 优点 | 缺点 |
|------|------|
| 空间紧凑，适合稀疏图 | 判断边(u,v)是否存在需要遍历链表 |
| 大多数图算法的标准表示 | |
| 容易扩展为带权图 | |

**FAQ：如何把邻接链表改为带权图？**

只需在链表节点中多存一个权重字段即可：

```
普通邻接链表：  1 → [2, 5]
带权邻接链表：  1 → [(2, w=3), (5, w=7)]    // 边(1,2)权3，边(1,5)权7
```

### 1.3 邻接矩阵（Adjacency Matrix）

**核心思想**：用 |V| × |V| 的矩阵 A 表示，A[i][j] = 1 表示边 (i,j) 存在。

**图例**：

```
无向图 G：            邻接矩阵：
    1 ── 2                1  2  3  4  5
    | \   |           1 [ 0  1  0  0  1 ]
    |   \ |           2 [ 1  0  1  1  1 ]
    5 ── 3            3 [ 0  1  0  1  0 ]
    |   /             4 [ 0  1  1  0  1 ]
    | /               5 [ 1  1  0  1  0 ]
    4

有向图 G：            邻接矩阵：
    1 → 2                1  2  3  4  5  6
    ↓   ↓           1 [ 0  1  0  1  0  0 ]
    3 ← 4           2 [ 0  0  0  0  1  0 ]
    ↓   ↓           3 [ 0  1  0  0  0  1 ]
    6 ← 5           4 [ 0  0  0  0  1  0 ]
                    5 [ 0  0  1  0  0  0 ]
                    6 [ 0  0  0  0  0  0 ]
```

**注意**：无向图的邻接矩阵是对称的（A[i][j] = A[j][i]），可以用上三角压缩存储。

**邻接矩阵的优缺点**：

| 优点 | 缺点 |
|------|------|
| O(1) 判断边(u,v)是否存在 | 空间 Θ(V²)，稀疏图浪费 |
| 适合稠密图 | 遍历某顶点所有邻居需 O(V) |

### 1.4 两种表示的对比

| 对比维度 | 邻接链表 | 邻接矩阵 |
|----------|----------|----------|
| 空间 | Θ(V + E) | Θ(V²) |
| 判断边是否存在 | O(degree) 最坏O(V) | O(1) |
| 列出所有邻居 | O(degree) | O(V) |
| 适合场景 | 稀疏图 | 稠密图 / 频繁查边 |
| 加权图扩展 | 链表节点加权重 | 矩阵存权重值 |

### 1.5 代码实现

```python
# Python 实现：邻接链表
class Graph:
    def __init__(self, directed=False):
        """初始化图
        directed: True表示有向图，False表示无向图
        """
        self.adj = {}           # 邻接链表：{顶点: [邻居列表]}
        self.directed = directed
    
    def add_vertex(self, v):
        """添加顶点"""
        if v not in self.adj:
            self.adj[v] = []    # 每个顶点对应一个空列表
    
    def add_edge(self, u, v, weight=None):
        """添加边"""
        # 确保顶点存在
        self.add_vertex(u)
        self.add_vertex(v)
        
        if weight is not None:
            self.adj[u].append((v, weight))    # 带权边
        else:
            self.adj[u].append(v)              # 无权边
        
        if not self.directed:
            # 无向图：两个方向都要添加
            if weight is not None:
                self.adj[v].append((u, weight))
            else:
                self.adj[v].append(u)
    
    def has_edge(self, u, v):
        """判断边(u,v)是否存在"""
        if u not in self.adj:
            return False
        # 遍历u的邻接链表查找v
        for neighbor in self.adj[u]:
            if isinstance(neighbor, tuple):
                if neighbor[0] == v:
                    return True
            elif neighbor == v:
                return True
        return False


# Python 实现：邻接矩阵
class GraphMatrix:
    def __init__(self, num_vertices, directed=False):
        """初始化邻接矩阵
        num_vertices: 顶点数量（顶点编号0到n-1）
        directed: True表示有向图
        """
        self.n = num_vertices
        self.directed = directed
        # 初始化n×n的零矩阵
        self.matrix = [[0] * num_vertices for _ in range(num_vertices)]
    
    def add_edge(self, u, v, weight=1):
        """添加边，默认权重1"""
        self.matrix[u][v] = weight
        if not self.directed:
            self.matrix[v][u] = weight    # 无向图对称
    
    def has_edge(self, u, v):
        """O(1)判断边是否存在"""
        return self.matrix[u][v] != 0
```

```cpp
// C++ 实现：邻接链表
#include <vector>
#include <unordered_map>
using namespace std;

class Graph {
private:
    bool directed;                              // 是否有向图
    unordered_map<int, vector<pair<int, int>>> adj;  // 邻接链表 {顶点: [(邻居, 权重)]}

public:
    Graph(bool dir = false) : directed(dir) {}  // 构造函数，默认无向图
    
    void addEdge(int u, int v, int weight = 1) {
        // 添加边 u → v（带权重）
        adj[u].push_back({v, weight});
        // 无向图还要添加 v → u
        if (!directed) {
            adj[v].push_back({u, weight});
        }
    }
    
    bool hasEdge(int u, int v) {
        // 遍历u的邻接链表查找v
        if (adj.find(u) == adj.end()) return false;
        for (auto& [neighbor, w] : adj[u]) {
            if (neighbor == v) return true;
        }
        return false;
    }
    
    vector<pair<int, int>>& neighbors(int u) {
        // 返回u的所有邻居（用于遍历）
        return adj[u];
    }
};

// C++ 实现：邻接矩阵
class GraphMatrix {
private:
    int n;                  // 顶点数量
    bool directed;          // 是否有向图
    vector<vector<int>> mat; // 邻接矩阵

public:
    GraphMatrix(int num_vertices, bool dir = false) 
        : n(num_vertices), directed(dir), 
          mat(num_vertices, vector<int>(num_vertices, 0)) {}
    
    void addEdge(int u, int v, int weight = 1) {
        mat[u][v] = weight;            // 设置矩阵值
        if (!directed) mat[v][u] = weight;  // 无向图对称
    }
    
    bool hasEdge(int u, int v) {
        return mat[u][v] != 0;         // O(1)查找
    }
};
```

---

## 2️⃣ 广度优先搜索（22.2 Breadth-First Search, BFS）

### 2.1 BFS的核心思想

**生活中的类比——水面涟漪**：

想象你在平静的湖面扔了一颗石子：
- 水波从落水点**一圈一圈向外扩散**
- **离落水点近的波纹先到达，远的后到达**
- 这就是BFS——从源点出发，**先发现距离近的，再发现距离远的**

**BFS的关键性质**：
- 从源点 s 出发，逐层探索
- 先发现距离为 k 的所有顶点，再发现距离为 k+1 的顶点
- 计算出的距离就是**最短路径**（边数最少）

### 2.2 三色标记法

BFS用三种颜色跟踪顶点状态：

| 颜色 | 含义 | 类比 |
|------|------|------|
| **白色(WHITE)** | 未被发现 | 还没收到水波的城市 |
| **灰色(GRAY)** | 已发现但邻居还没全处理 | 刚收到水波，正在通知邻居 |
| **黑色(BLACK)** | 已发现且邻居已全处理 | 水波已经从这传走了 |

**为什么要区分灰色和黑色？** 灰色顶点是"前沿"——它们的邻居可能还没被发现。黑色顶点已经处理完毕。队列里存的就是所有灰色顶点。

### 2.3 BFS算法伪代码

```
BFS(G, s)
1  for each vertex u ∈ G.V - {s}
2      u.color = WHITE        // 所有非源点初始化为白色
3      u.d = ∞                // 距离初始化为无穷大
4      u.π = NIL              // 前驱初始化为空
5  s.color = GRAY             // 源点标记为灰色（已发现）
6  s.d = 0                    // 源点到自己的距离为0
7  s.π = NIL                  // 源点没有前驱
8  Q = ∅                      // 初始化空队列
9  ENQUEUE(Q, s)              // 源点入队
10 while Q ≠ ∅                // 队列不空就继续
11     u = DEQUEUE(Q)         // 取出队头
12     for each v ∈ G.Adj[u]  // 遍历u的所有邻居
13         if v.color == WHITE         // 如果邻居还没被发现
14             v.color = GRAY          // 标记为已发现
15             v.d = u.d + 1           // 距离 = 父节点距离 + 1
16             v.π = u                  // 记录前驱
17             ENQUEUE(Q, v)            // 入队等待处理
18     u.color = BLACK          // u处理完毕，标黑
```

### 2.4 BFS执行过程图解

以CLRS图22-3为例，源点为 s：

```
原始图：                    BFS逐层扩散过程：

    r ── s                   第0层：s (d=0)
    |    | \                           
    v    w ─ t               第1层：w(d=1), t(d=1), x(d=1)
    |    | /                           
         x ─ u               第2层：r(d=2), y(d=2), u(d=2)
           \                           
             y               第3层：v(d=3)  ← 等等，实际从s出发
                                 
实际执行过程（源点u）：        
                              第0层：u (d=0)
                              第1层：x(d=1), t(d=1), y(d=1)
                              第2层：w(d=2), s(d=2)
                              第3层：r(d=3), v(d=4)
```

**详细追踪（源点 u）**：

```
初始：所有顶点白色，d=∞，π=NIL
u.color=GRAY, u.d=0, Q=[u]

出队u，处理邻居[x, t, y]：
  x: 白色 → 灰色, d=1, π=u, Q=[x,t,y]
  t: 白色 → 灰色, d=1, π=u
  y: 白色 → 灰色, d=1, π=u
u → 黑色

出队x，处理邻居[u, w, y]：
  u: 黑色，跳过
  w: 白色 → 灰色, d=2, π=x, Q=[t,y,w]
  y: 灰色，跳过
x → 黑色

出队t，处理邻居[u, w]：
  u: 黑色，跳过
  w: 灰色，跳过
t → 黑色

出队y，处理邻居[u, x]：
  u: 黑色，跳过
  x: 黑色，跳过
y → 黑色

出队w，处理邻居[x, t, s, v]：
  x: 黑色，跳过
  t: 黑色，跳过
  s: 白色 → 灰色, d=3, π=w, Q=[s,v]  ← 等等，实际看邻接链表顺序
  v: 白色 → 灰色, d=3, π=w
w → 黑色

出队s，处理邻居[r, w]：
  r: 白色 → 灰色, d=4, π=s
  w: 黑色，跳过
s → 黑色

...以此类推
```

### 2.5 BFS的正确性证明（最短路径）

**核心定理**：BFS结束后，对所有从s可达的顶点v，v.d = δ(s,v)，其中δ(s,v)是从s到v的最短路径长度。

**证明思路**（三步走）：

**引理1**：如果u在v之前入队，则 u.d ≤ v.d

> 直觉：队列是FIFO的，先入队的距离不会更大。可以用归纳法证明。

**引理2**：BFS结束后，对所有顶点v，v.d ≥ δ(s,v)

> 直觉：v.d是某条从s到v的路径的长度（发现v时经过的路径），而δ(s,v)是最短路径长度，当然 v.d ≥ δ(s,v)。

**主定理**：BFS结束后，v.d = δ(s,v)

> 反证法：假设存在顶点v使得 v.d ≠ δ(s,v)。由引理2，v.d > δ(s,v)。
> 设v是距s最近的"不满足等式"的顶点，考虑s到v的最短路径上v的前驱u。
> 因为u比v更近，所以 u.d = δ(s,u)。
> 于是 v.d > δ(s,v) = δ(s,u) + 1 = u.d + 1。
> 但当u出队时，v要么未发现（会被设为 u.d+1），要么已发现（d值 ≤ u.d+1），矛盾！ ∎

### 2.6 BFS时间复杂度

| 操作 | 次数 | 单次时间 |
|------|------|----------|
| 初始化 | V | O(1) |
| 入队/出队 | 每个顶点最多1次 | O(1) |
| 扫描邻接链表 | 每条边2次（无向图）或1次（有向图） | O(1) |

**总时间**：Θ(V + E)

### 2.7 广度优先树与路径打印

BFS构建的前驱子图（π关系）形成一棵**广度优先树**，从s到任何可达顶点v的树路径就是最短路径。

```
PRINT-PATH(G, s, v)
1  if v == s
2      print s
3  elseif v.π == NIL
4      print "no path from" s "to" v "exists"
5  else PRINT-PATH(G, s, v.π)  // 递归打印前驱
6       print v                 // 再打印自己
```

**图解**：

```
广度优先树：     路径 s → v：
    s              s → w → r → v
    |\
    w  t
    |\
    r  x
    |
    v

PRINT-PATH 递归过程：
PRINT-PATH(s, v) → v.π = r
  PRINT-PATH(s, r) → r.π = w
    PRINT-PATH(s, w) → w.π = s
      PRINT-PATH(s, s) → print s
    print w
  print r
print v

输出：s w r v
```

### 2.8 BFS代码实现

```python
# Python 实现：广度优先搜索
from collections import deque

def bfs(graph, source):
    """广度优先搜索
    graph: 邻接链表 {顶点: [邻居列表]}
    source: 源点
    返回: (distance字典, parent字典)
    """
    # 初始化
    color = {}        # 颜色：WHITE=0, GRAY=1, BLACK=2
    dist = {}         # 距离
    parent = {}       # 前驱
    
    for v in graph:
        color[v] = 0          # 所有顶点初始化为白色
        dist[v] = float('inf')  # 距离无穷大
        parent[v] = None       # 前驱为空
    
    # 源点初始化
    color[source] = 1          # 源点标记为灰色
    dist[source] = 0           # 源点距离为0
    parent[source] = None
    
    queue = deque([source])    # 队列初始化，源点入队
    
    while queue:               # 队列不空
        u = queue.popleft()    # 取出队头
        for v in graph[u]:     # 遍历u的所有邻居
            if color[v] == 0:  # 如果邻居是白色（未发现）
                color[v] = 1           # 标记为灰色
                dist[v] = dist[u] + 1  # 距离 = 父节点 + 1
                parent[v] = u          # 记录前驱
                queue.append(v)        # 入队
        color[u] = 2            # u处理完毕，标黑
    
    return dist, parent


def print_path(parent, source, target):
    """打印从source到target的最短路径"""
    if target == source:
        print(source, end=' ')
    elif parent[target] is None:
        print(f"no path from {source} to {target}")
    else:
        print_path(parent, source, parent[target])  # 递归打印前驱
        print(target, end=' ')                       # 再打印自己


# 使用示例
if __name__ == '__main__':
    # CLRS 图22-2 的有向图
    graph = {
        1: [2, 4],
        2: [5],
        3: [2, 6],
        4: [5],
        5: [3],
        6: []
    }
    dist, parent = bfs(graph, 3)
    print("距离:", dist)      # {1:∞, 2:1, 3:0, 4:∞, 5:2, 6:1}
    print("前驱:", parent)
```

```cpp
// C++ 实现：广度优先搜索
#include <iostream>
#include <vector>
#include <queue>
#include <unordered_map>
using namespace std;

const int WHITE = 0, GRAY = 1, BLACK = 2;

struct BFResult {
    unordered_map<int, int> dist;     // 距离
    unordered_map<int, int> parent;   // 前驱
};

BFResult bfs(const unordered_map<int, vector<int>>& graph, int source) {
    BFResult res;
    unordered_map<int, int> color;    // 颜色标记
    
    // 初始化所有顶点
    for (auto& [v, neighbors] : graph) {
        color[v] = WHITE;              // 白色（未发现）
        res.dist[v] = INT_MAX;         // 距离无穷大
        res.parent[v] = -1;            // 前驱为空（用-1表示NIL）
    }
    
    // 源点初始化
    color[source] = GRAY;              // 灰色（已发现）
    res.dist[source] = 0;              // 距离为0
    
    queue<int> q;
    q.push(source);                    // 源点入队
    
    while (!q.empty()) {
        int u = q.front();             // 取出队头
        q.pop();
        
        // 遍历u的所有邻居
        if (graph.count(u)) {
            for (int v : graph.at(u)) {
                if (color[v] == WHITE) {       // 白色（未发现）
                    color[v] = GRAY;            // 标灰
                    res.dist[v] = res.dist[u] + 1;  // 距离+1
                    res.parent[v] = u;          // 记录前驱
                    q.push(v);                  // 入队
                }
            }
        }
        color[u] = BLACK;               // 处理完毕，标黑
    }
    
    return res;
}

void printPath(const unordered_map<int, int>& parent, int source, int target) {
    // 递归打印从source到target的最短路径
    if (target == source) {
        cout << source << " ";
    } else if (parent.at(target) == -1) {
        cout << "no path from " << source << " to " << target;
    } else {
        printPath(parent, source, parent.at(target));  // 先打印前驱
        cout << target << " ";                          // 再打印自己
    }
}
```

### 2.9 FAQ

**Q1：BFS中的灰色真的必要吗？只用"已发现/未发现"两种状态够不够？**

够用。灰色和黑色的区分主要是教学目的——灰色表示"在队列中"，黑色表示"已出队"。实际实现中，我们可以用一个 `visited` 数组替代三色标记。但三色标记在理解算法行为时更直观，后续DFS中也有重要的理论意义。

**Q2：如果用邻接矩阵表示图，BFS的时间复杂度变多少？**

变 Θ(V²)。因为即使某顶点只有少数邻居，遍历它的邻接链表也需要扫描整行（O(V)）。所以邻接链表 + BFS = Θ(V+E)，而邻接矩阵 + BFS = Θ(V²)。

**Q3：BFS计算的距离和Dijkstra最短路径有什么区别？**

BFS计算的是**边数最少**的最短路径（每条边权重为1）。如果边有权重且不全相等，需要用Dijkstra算法。BFS是Dijkstra在"所有边权重=1"时的特例。

---

## 3️⃣ 深度优先搜索（22.3 Depth-First Search, DFS）

### 3.1 DFS的核心思想

**生活中的类比——迷宫探索**：

想象你在走迷宫：
- 你沿着一条路**一直走到底**（深度优先）
- 走到死胡同了就**回溯**到上一个岔路口
- 换一条路继续走到底
- 直到所有路都走过

**BFS vs DFS的核心区别**：

```
BFS（水面涟漪）：一层一层往外扩散
DFS（迷宫探索）：一条路走到黑，走不通再回头

BFS：用队列（先进先出）→ 保证先处理近的
DFS：用栈/递归（后进先出）→ 保证一条路走到底
```

### 3.2 DFS算法伪代码

```
DFS(G)
1  for each vertex u ∈ G.V
2      u.color = WHITE        // 初始化：所有顶点白色
3      u.π = NIL              // 前驱为空
4  time = 0                   // 全局时间戳
5  for each vertex u ∈ G.V
6      if u.color == WHITE    // 如果还没访问过
7          DFS-VISIT(G, u)    // 从u开始深度搜索

DFS-VISIT(G, u)
1  time = time + 1            // 时间+1
2  u.d = time                 // 记录发现时间
3  u.color = GRAY             // 标记为灰色（正在访问）
4  for each v ∈ G.Adj[u]     // 遍历u的所有邻居
5      if v.color == WHITE    // 如果邻居还没被访问
6          v.π = u            // 记录前驱
7          DFS-VISIT(G, v)    // 递归访问v
8  u.color = BLACK            // 标记为黑色（访问完毕）
9  time = time + 1            // 时间+1
10 u.f = time                 // 记录完成时间
```

**关键数据**：
- `u.d`：发现时间（变成灰色的时刻）
- `u.f`：完成时间（变成黑色的时刻）
- 时间范围：1 到 2|V|

### 3.3 DFS执行过程图解

```
有向图：              DFS执行过程（按字母顺序选择未访问顶点）：

  w → z               1. DFS-VISIT(w):
  ↑   ↑                 w: d=1 (灰) → 探索 w→z
  |   |                 z: d=2 (灰) → z无未访问邻居
  u → y                   z: f=3 (黑)
  ↑   ↑               w: f=4 (黑)
  |   |
  x ← v               2. DFS-VISIT(u):
                          u: d=5 (灰) → 探索 u→y
                          y: d=6 (灰) → y无未访问邻居
                             y: f=7 (黑)
                          u: f=8 (黑)

                       3. DFS-VISIT(v):
                          v: d=9 (灰) → 探索 v→x
                          x: d=10 (灰) → x→y 已黑, x→u 已黑
                             x: f=11 (黑)
                          v: f=12 (黑)

时间线（括号结构）：
  w[1  z[2  3]  4]     u[5  y[6  7]  8]     v[9  x[10  11]  12]
  |    |   |    |       |    |   |    |       |    |    |     |
  发现  发现 完成 完成  发现  发现 完成 完成  发现  发现  完成  完成
```

### 3.4 DFS的重要性质

#### 性质1：括号定理（Parenthesis Theorem）

对于任意两个顶点u和v，下面三种情况**恰有一种成立**：

| 情况 | 条件 | 含义 |
|------|------|------|
| 完全分离 | [u.d, u.f] ∩ [v.d, v.f] = ∅ | u和v互不为后代 |
| 嵌套(u在v内) | v.d < u.d < u.f < v.f | u是v的后代 |
| 嵌套(v在u内) | u.d < v.d < v.f < u.f | v是u的后代 |

**图解**：

```
情况1：完全分离          情况2：u是v的后代          情况3：v是u的后代
  w[1....4]  u[5....8]    v[1..........8]           u[1..........12]
                              u[3....6]                v[4.....9]

就像括号：                就像括号：                  就像括号：
( ) ( )                  ( ( ) )                    ( ( ) )
```

#### 性质2：白色路径定理（White Path Theorem）

**定理**：在DFS森林中，v是u的后代 **当且仅当** 在时刻u.d，存在一条从u到v的全部由白色顶点组成的路径。

> 直觉：如果v是u的后代，那DFS从u出发一定会走到v，沿途所有点都是白色。反过来，如果从u到v有一条全白色的路径，DFS一定会沿这条路径到达v。

#### 性质3：后代判定

v是u的后代 **当且仅当** u.d < v.d < v.f < u.f

### 3.5 边的分类

DFS不仅搜索图，还能对**边**进行分类，这在后续应用（拓扑排序、强连通分量）中至关重要。

| 边的类型 | 定义 | 识别方式 |
|----------|------|----------|
| **树边(Tree Edge)** | DFS森林中的边。v因(u,v)首次被发现 | v是白色 |
| **后向边(Back Edge)** | 从后代指向祖先的边（包括自环） | v是灰色 |
| **前向边(Forward Edge)** | 从祖先指向非直接后代的边 | v是黑色且 u.d < v.d |
| **横向边(Cross Edge)** | 其余所有边（既非祖先也非后代之间） | v是黑色且 v.d > u.f |

**图解**：

```
DFS森林中的边分类：

      u (d=1, f=8)
     / \
    v   x (d=2, f=5) (d=6, f=7)
    |
    y (d=3, f=4)

树边：u→v, u→x, v→y     （灰色时发现，构成DFS树）
后向边：y→u              （指向灰色祖先 = 有环！）
前向边：u→y              （u是y的祖先，但y不是u直接子节点）
横向边：x→y              （x和y不是祖先后代关系，但y已经完成）
```

**重要定理**：无向图的DFS中，每条边要么是树边，要么是后向边。

> 原因：假设无向图中有边(u,v)，当第一次探索到这条边时，v如果是白色就是树边；如果v是灰色，u是v的后代，这是后向边。v不可能是黑色——因为如果v已经完成，u必然也是v的后代（无向边双向可达），那u应该比v先完成，矛盾。

### 3.6 DFS时间复杂度

| 操作 | 总时间 |
|------|--------|
| 初始化 | Θ(V) |
| DFS-VISIT调用 | 每个顶点恰好被访问1次 → Θ(V) |
| 扫描邻接链表 | 每条边被扫描2次(无向图)/1次(有向图) → Θ(E) |

**总时间**：Θ(V + E)

### 3.7 DFS代码实现

```python
# Python 实现：深度优先搜索
class DFSResult:
    """DFS结果封装"""
    def __init__(self):
        self.parent = {}       # 前驱
        self.discover = {}     # 发现时间
        self.finish = {}       # 完成时间
        self.order = []        # 顶点完成顺序（拓扑排序用）
        self.edge_type = {}    # 边的类型

def dfs(graph):
    """深度优先搜索
    graph: 邻接链表 {顶点: [邻居列表]}
    返回: DFSResult对象
    """
    result = DFSResult()
    color = {}     # 颜色标记
    time = [0]     # 用列表包装以便在递归中修改（Python闭包特性）
    
    # 初始化所有顶点
    for v in graph:
        color[v] = 0          # 白色
        result.parent[v] = None
    
    # 对每个未访问的顶点执行DFS
    for v in graph:
        if color[v] == 0:     # 白色（未访问）
            dfs_visit(graph, v, color, time, result)
    
    return result


def dfs_visit(graph, u, color, time, result):
    """DFS访问单个顶点（递归）"""
    time[0] += 1
    result.discover[u] = time[0]    # 记录发现时间
    color[u] = 1                     # 标记为灰色
    
    # 遍历u的所有邻居
    for v in graph[u]:
        if color[v] == 0:            # 白色 → 树边
            result.edge_type[(u, v)] = 'tree'
            result.parent[v] = u
            dfs_visit(graph, v, color, time, result)  # 递归访问
        elif color[v] == 1:          # 灰色 → 后向边
            result.edge_type[(u, v)] = 'back'
        else:                        # 黑色
            if result.discover[u] < result.discover[v]:
                result.edge_type[(u, v)] = 'forward'   # 前向边
            else:
                result.edge_type[(u, v)] = 'cross'     # 横向边
    
    color[u] = 2                     # 标记为黑色
    time[0] += 1
    result.finish[u] = time[0]       # 记录完成时间
    result.order.append(u)           # 记录完成顺序


# 使用示例
if __name__ == '__main__':
    graph = {
        'u': ['v', 'x'],
        'v': ['y'],
        'w': ['y', 'z'],
        'x': ['v'],
        'y': ['x'],
        'z': ['z']   # 自环
    }
    result = dfs(graph)
    print("发现时间:", result.discover)
    print("完成时间:", result.finish)
    print("完成顺序:", result.order)
    print("边类型:", result.edge_type)
```

```cpp
// C++ 实现：深度优先搜索
#include <iostream>
#include <vector>
#include <unordered_map>
using namespace std;

const int WHITE = 0, GRAY = 1, BLACK = 2;

struct DFSResult {
    unordered_map<int, int> discover;   // 发现时间
    unordered_map<int, int> finish;     // 完成时间
    unordered_map<int, int> parent;     // 前驱
    vector<int> order;                  // 完成顺序（拓扑排序用）
};

class DFSGraph {
private:
    unordered_map<int, vector<int>> adj;  // 邻接链表
    int timer;                             // 全局时间戳
    
public:
    DFSGraph() : timer(0) {}
    
    void addEdge(int u, int v) {
        adj[u].push_back(v);
    }
    
    DFSResult dfs() {
        DFSResult res;
        unordered_map<int, int> color;
        
        // 初始化
        for (auto& [v, _] : adj) {
            color[v] = WHITE;          // 白色
            res.parent[v] = -1;        // 前驱为空
        }
        
        // 对每个未访问顶点执行DFS
        for (auto& [v, _] : adj) {
            if (color[v] == WHITE) {
                dfsVisit(v, color, res);
            }
        }
        
        return res;
    }
    
private:
    void dfsVisit(int u, unordered_map<int, int>& color, DFSResult& res) {
        timer++;
        res.discover[u] = timer;       // 记录发现时间
        color[u] = GRAY;               // 标灰
        
        // 遍历所有邻居
        for (int v : adj[u]) {
            if (color[v] == WHITE) {    // 白色 → 树边
                res.parent[v] = u;
                dfsVisit(v, color, res);  // 递归访问
            }
            // 灰色 → 后向边；黑色 → 前向边或横向边
        }
        
        color[u] = BLACK;              // 标黑
        timer++;
        res.finish[u] = timer;         // 记录完成时间
        res.order.push_back(u);        // 记录完成顺序
    }
};
```

### 3.8 FAQ

**Q1：DFS为什么需要外层循环？BFS只有一个源点，DFS为什么从每个顶点都试？**

因为BFS只关心从某个源点出发能到达的顶点。而DFS的目标是遍历整个图，可能有不连通的分量。外层循环确保每个连通分量都被访问到。

**Q2：DFS发现的"后向边"有什么意义？**

后向边 = 图中有环！如果DFS过程中遇到后向边，说明有向图中有环。这个性质是拓扑排序和强连通分量的基础。

**Q3：DFS的时间复杂度为什么是Θ(V+E)而不是更精确的？**

因为每个顶点恰好被访问一次（Θ(V)），每条边恰好被扫描一次（有向图）或两次（无向图），所以总时间是 Θ(V+E)。这是紧界——不存在更快的图遍历算法（至少要看完所有顶点和边）。

---

## 4️⃣ 拓扑排序（22.4 Topological Sort）

### 4.1 问题定义

**生活中的类比——课程先修关系**：

```
课程依赖图：
  高等数学 ──→ 线性代数 ──→ 机器学习
      |              |
      ↓              ↓
  概率统计 ──→ 深度学习

问题：按什么顺序选课，确保每门课的先修课都先上？
答案：高等数学 → 线性代数 → 概率统计 → 机器学习 → 深度学习
      （或者   高等数学 → 概率统计 → 线性代数 → 深度学习 → 机器学习）
      答案不唯一！
```

**形式化定义**：对有向无环图(DAG) G = (V, E)的拓扑排序是V中所有顶点的一种线性排列，使得若图G包含边(u, v)，则u在排序中位于v前面。

**前提**：图必须是DAG（有向无环图）！

> 如果图中有环，就不可能排拓扑序——就像"鸡生蛋蛋生鸡"，谁也排不到前面。

### 4.2 算法思想

**核心观察**：DFS的完成时间天然地给出了拓扑序！

一个顶点的完成时间越晚，说明它在依赖链中越靠前（被更多顶点依赖）。所以把顶点按完成时间**从大到小**排列就是拓扑序。

```
为什么？因为如果存在边(u, v)，u必须在v之前完成，
所以 u.f > v.f，按f降序排列自然满足u在v前面。
```

### 4.3 算法伪代码

```
TOPOLOGICAL-SORT(G)
1  call DFS(G) to compute finishing times v.f for each vertex v
2  as each vertex is finished, insert it onto the front of a linked list
3  return the linked list of vertices
```

**等价说法**：按完成时间从大到小排列，或按DFS完成顺序的逆序排列。

### 4.4 执行过程图解

```
DAG图：              DFS执行过程：

  袜子               从"袜子"开始DFS：
    ↓                袜子 d=1 → 鞋子 d=2 → （鞋子无未访问邻居）
  鞋子                   鞋子 f=3
                      袜子 f=4

  裤子               从"裤子"开始DFS：
  ↓ ↓                裤子 d=5 → 腰带 d=6 → （腰带无未访问邻居）
腰带 衬衫                腰带 f=7
  ↓                  裤子 → 衬衫 d=8 → 领带 d=9 → 外套 d=10
  外套 ← 领带             外套 f=11
                          领带 f=12
                          衬衫 f=13
                      裤子 f=14

完成时间：袜子4, 鞋子3, 裤子14, 腰带7, 衬衫13, 领带12, 外套11

按f降序排列（拓扑序）：
裤子(14) → 衬衫(13) → 领带(12) → 外套(11) → 腰带(7) → 袜子(4) → 鞋子(3)

验证：每条边(u,v)都满足u在v前面 ✓
  袜子→鞋子 ✓  裤子→腰带 ✓  裤子→衬衫 ✓  腰带→外套 ✓  衬衫→领带 ✓  领带→外套 ✓
```

### 4.5 正确性证明

**定理**：TOPOLOGICAL-SORT产生的顶点排列是G的拓扑排序。

**证明**：

只需证明：对任意边(u,v)∈E，在拓扑序列中u出现在v前面，即 v.f < u.f。

当DFS探索边(u,v)时，v不可能是灰色（否则v是u的祖先，存在从v到u的路径，加上(u,v)形成环，矛盾）。

- 若v是白色：v成为u的后代，由括号定理 u.d < v.d < v.f < u.f，所以 v.f < u.f ✓
- 若v是黑色：v已经完成，v.f已确定且 < 当前时间，而u尚未完成，所以 v.f < u.f ✓

### 4.6 代码实现

```python
# Python 实现：拓扑排序
def topological_sort(graph):
    """拓扑排序
    graph: 邻接链表（有向无环图）
    返回: 拓扑序列表（完成时间降序）
    """
    color = {}       # 颜色标记
    order = []       # 完成顺序
    
    # 初始化
    for v in graph:
        color[v] = 0   # 白色
    
    # DFS
    for v in graph:
        if color[v] == 0:
            if not dfs_topo(graph, v, color, order):
                return None   # 图中有环，无法拓扑排序
    
    return list(reversed(order))  # 逆序 = 按完成时间降序


def dfs_topo(graph, u, color, order):
    """DFS辅助函数，同时检测环
    返回: True表示无环，False表示有环
    """
    color[u] = 1    # 灰色
    
    for v in graph[u]:
        if color[v] == 0:       # 白色 → 继续搜索
            if not dfs_topo(graph, v, color, order):
                return False    # 子调用检测到环
        elif color[v] == 1:     # 灰色 → 后向边 → 有环！
            return False
    
    color[u] = 2    # 黑色
    order.append(u)  # 记录完成顺序
    return True


# 另一种实现：Kahn算法（BFS方式）
from collections import deque

def topological_sort_kahn(graph):
    """Kahn算法：基于入度的拓扑排序
    graph: 邻接链表
    返回: 拓扑序列表，若图有环返回None
    """
    # 计算入度
    in_degree = {v: 0 for v in graph}
    for u in graph:
        for v in graph[u]:
            in_degree[v] = in_degree.get(v, 0) + 1
    
    # 入度为0的顶点入队
    queue = deque([v for v in graph if in_degree[v] == 0])
    result = []
    
    while queue:
        u = queue.popleft()
        result.append(u)
        
        # 删除u的所有出边（邻居入度-1）
        for v in graph[u]:
            in_degree[v] -= 1
            if in_degree[v] == 0:   # 入度变为0，入队
                queue.append(v)
    
    # 如果还有顶点没处理，说明有环
    if len(result) != len(graph):
        return None
    
    return result
```

```cpp
// C++ 实现：拓扑排序（DFS方式）
#include <iostream>
#include <vector>
#include <unordered_map>
#include <algorithm>
using namespace std;

const int WHITE = 0, GRAY = 1, BLACK = 2;

bool dfsTopo(const unordered_map<int, vector<int>>& graph, 
             int u, unordered_map<int, int>& color, vector<int>& order) {
    color[u] = GRAY;                          // 标灰
    
    // 遍历邻居
    if (graph.count(u)) {
        for (int v : graph.at(u)) {
            if (color[v] == WHITE) {           // 白色 → 继续搜索
                if (!dfsTopo(graph, v, color, order))
                    return false;              // 检测到环
            } else if (color[v] == GRAY) {     // 灰色 → 后向边 → 有环
                return false;
            }
        }
    }
    
    color[u] = BLACK;                          // 标黑
    order.push_back(u);                        // 记录完成顺序
    return true;
}

vector<int> topologicalSort(const unordered_map<int, vector<int>>& graph) {
    unordered_map<int, int> color;
    vector<int> order;
    
    // 初始化
    for (auto& [v, _] : graph) {
        color[v] = WHITE;
    }
    
    // 对每个未访问顶点执行DFS
    for (auto& [v, _] : graph) {
        if (color[v] == WHITE) {
            if (!dfsTopo(graph, v, color, order)) {
                return {};   // 有环，返回空
            }
        }
    }
    
    reverse(order.begin(), order.end());  // 逆序 = 拓扑序
    return order;
}
```

### 4.7 FAQ

**Q1：拓扑排序的结果唯一吗？**

不唯一！当多个顶点之间没有依赖关系时，它们的相对顺序可以任意。不同DFS起始顶点、不同邻接链表顺序都会影响结果。但所有合法拓扑排序都满足：有边(u,v)→u在v前面。

**Q2：如何判断一个有向图是否有环？**

DFS过程中如果遇到后向边（灰色顶点），说明有环。也可以用Kahn算法——如果处理完入度为0的顶点后还有剩余顶点，说明有环。

**Q3：拓扑排序的时间复杂度？**

DFS方式：Θ(V + E)（就是DFS的时间）
Kahn方式：Θ(V + E)（每个顶点和边各处理一次）

---

## 5️⃣ 强连通分量（22.5 Strongly Connected Components）

### 5.1 问题定义

**生活中的类比——互相认识的朋友圈**：

```
社交网络：
  A认识B，B认识A       → A和B互相可达 → 在同一个强连通分量
  A认识C，C不认识A     → A到C有路径，C到A没有 → 不在同一个强连通分量

强连通分量 = 最大的"互相到达"的顶点集合
```

**形式化定义**：有向图G = (V, E)中，如果顶点u和v互相可达（u→v且v→u），则称u和v**强连通**。强连通分量是极大的强连通顶点子集。

**分量图**（Component Graph）：把每个强连通分量缩成一个"超级顶点"，得到的图一定是DAG。

```
原图：                   分量图（一定是DAG）：
  a → b                    C1 → C2 → C3
  b → a                    (a,b)  (c,d)  (e)
  a → c                    C1 → C2
  d → c                    C2 → C3
  c → d
  d → e

C1={a,b}, C2={c,d}, C3={e}
```

> **分量图为什么一定是DAG？** 如果分量图中有环，环上的所有分量应该合并成一个更大的强连通分量，矛盾。

### 5.2 Kosaraju算法

**算法步骤**：

```
STRONGLY-CONNECTED-COMPONENTS(G)
1  call DFS(G) to compute finishing times u.f for each vertex u    // 第1次DFS
2  compute G^T                                                     // 计算转置图
3  call DFS(G^T), but in the main loop, consider vertices 
   in order of decreasing u.f (as computed in line 1)              // 第2次DFS
4  output the vertices of each tree in the depth-first forest 
   formed in line 3 as a separate strongly connected component     // 每棵树=一个SCC
```

**转置图 G^T**：把G中所有边的方向反转。G和G^T有完全相同的强连通分量。

### 5.3 算法图解

```
原图 G：                     第1次DFS（从a开始）：

  a → b                       a: d=1, f=6
  ↑   ↓                       b: d=2, f=5
  |   c                       c: d=3, f=4
  d ← ↓                       d: d=7, f=8  （回溯后从d开始）
      e ← f                   e: d=9, f=12
  e → f                       f: d=10, f=11

完成时间排序：e(12), f(11), d(8), a(6), b(5), c(4)

转置图 G^T：                  第2次DFS（按f降序在G^T上）：
  a ← b
  ↓   ↑                       1. 从e开始DFS(G^T)：
  |   c                          e → f → c → b → a → d
  d → ↑                          访问到: {e, f, c, b, a, d}
      e → f                      等等，这不对。让我们用正确的图：

正确示例（CLRS图22-9）：

原图 G：                     第1次DFS：
  a → b                       a: d=1, f=4
  b → c,e                     b: d=2, f=7  （b→c→f→g，然后b→e→h）
  c → d,f                     c: d=3, f=6
  d → c,g                     d: d=?, ... 让我用更简单的例子
  e → f
  f → g                       实际上让我们用经典小例子：
  g → d,h
  h → g                       G: 1→2, 2→3, 3→1, 3→4, 4→5, 5→4

简化示例：                    SCC: {1,2,3} 和 {4,5}
  1 → 2 → 3 → 1 (环)
  3 → 4 → 5 → 4 (环)

第1次DFS（从1开始）：
  1: d=1 → 2: d=2 → 3: d=3 → 4: d=4 → 5: d=5, f=6
  4: f=7
  3: f=8
  2: f=9
  1: f=10

完成时间：1(10), 2(9), 3(8), 4(7), 5(6)

转置图 G^T：
  2→1, 3→2, 1→3, 4→3, 5→4, 4→5

第2次DFS（在G^T上，按f降序：1,2,3,4,5）：
  从1开始：1→3→2→... 2→1? 不对，G^T中1→3
  让我重新标注：
  G^T: 2→1, 3→2, 1→3, 4→3, 5→4, 4→5
  
  从1开始DFS(G^T): 1→3→2  (2→1已访问)
    树1: {1, 3, 2} = SCC₁ ✓
  
  从4开始DFS(G^T): 4→5  (5→4已访问)
    树2: {4, 5} = SCC₂ ✓

  从5开始: 已访问，跳过

结果: SCC₁ = {1,2,3}, SCC₂ = {4,5} ✓
```

### 5.4 正确性证明

**核心引理**：设C和C'是两个不同的强连通分量，如果G中存在从C到C'的边，则 f(C) > f(C')（其中 f(C) = max{f[u] : u ∈ C}）。

**直觉**：C能到C'但不能从C'到C（否则是同一个SCC），所以DFS访问完C中所有节点时C'还没被完全处理，C的完成时间更大。

**正确性证明概要**：

1. 第1次DFS后，f(C₁) > f(C₂) > ... > f(C_k)，其中C₁是"源分量"（没有其他分量能到它）
2. 在G^T中，C₁变成了"汇分量"（没有边从C₁到其他分量）——因为所有跨分量边方向反转了
3. 第2次DFS从f最大的顶点开始，必然落在C₁中。由于C₁在G^T中是汇分量，DFS只能在C₁内部转，不会逃出去 → 第1棵DFS树 = C₁
4. 删掉C₁后，C₂变成新的汇分量，重复上述过程 → 第2棵DFS树 = C₂
5. 以此类推，每棵DFS树恰好对应一个SCC

### 5.5 代码实现

```python
# Python 实现：Kosaraju强连通分量算法
def kosaraju_scc(graph):
    """Kosaraju算法求强连通分量
    graph: 邻接链表（有向图）
    返回: 强连通分量列表，每个分量是顶点集合
    """
    # ===== 第1步：在原图上DFS，记录完成顺序 =====
    visited = set()
    finish_order = []     # 完成时间递增的顺序
    
    def dfs1(u):
        """第1次DFS：记录完成顺序"""
        visited.add(u)
        for v in graph.get(u, []):
            if v not in visited:
                dfs1(v)
        finish_order.append(u)    # 顶点完成时加入列表
    
    # 对所有顶点执行DFS
    for v in graph:
        if v not in visited:
            dfs1(v)
    
    # ===== 第2步：构建转置图 =====
    graph_t = {}    # 转置图
    for u in graph:
        graph_t[u] = []
    for u in graph:
        for v in graph[u]:
            graph_t[v].append(u)    # 边方向反转
    
    # ===== 第3步：在转置图上按f降序DFS =====
    visited = set()
    scc_list = []    # 强连通分量列表
    
    def dfs2(u, component):
        """第2次DFS：收集强连通分量"""
        visited.add(u)
        component.append(u)
        for v in graph_t.get(u, []):
            if v not in visited:
                dfs2(v, component)
    
    # 按完成时间降序遍历
    for u in reversed(finish_order):    # 逆序 = f降序
        if u not in visited:
            component = []
            dfs2(u, component)
            scc_list.append(component)
    
    return scc_list


# 使用示例
if __name__ == '__main__':
    # 示例图：1→2→3→1(环), 3→4→5→4(环)
    graph = {
        1: [2],
        2: [3],
        3: [1, 4],
        4: [5],
        5: [4]
    }
    sccs = kosaraju_scc(graph)
    print("强连通分量:", sccs)
    # 输出: [[1, 3, 2], [4, 5]]
```

```cpp
// C++ 实现：Kosaraju强连通分量算法
#include <iostream>
#include <vector>
#include <unordered_map>
#include <algorithm>
using namespace std;

class KosarajuSCC {
private:
    unordered_map<int, vector<int>> graph;      // 原图
    unordered_map<int, vector<int>> graphT;     // 转置图
    unordered_map<int, bool> visited;
    vector<int> finishOrder;                     // 完成顺序
    vector<vector<int>> sccList;                 // 强连通分量列表

    void dfs1(int u) {
        // 第1次DFS：记录完成顺序
        visited[u] = true;
        for (int v : graph[u]) {
            if (!visited[v]) {
                dfs1(v);
            }
        }
        finishOrder.push_back(u);    // 完成时加入
    }

    void dfs2(int u, vector<int>& component) {
        // 第2次DFS：在转置图上收集分量
        visited[u] = true;
        component.push_back(u);
        for (int v : graphT[u]) {
            if (!visited[v]) {
                dfs2(v, component);
            }
        }
    }

public:
    void addEdge(int u, int v) {
        graph[u].push_back(v);
        graphT[v].push_back(u);    // 同时建转置图
    }

    vector<vector<int>> findSCC() {
        // 收集所有顶点
        for (auto& [v, _] : graph) {
            visited[v] = false;
        }
        for (auto& [v, _] : graphT) {
            visited[v] = false;
        }

        // 第1次DFS
        for (auto& [v, _] : graph) {
            if (!visited[v]) dfs1(v);
        }

        // 重置visited
        for (auto& [v, _] : visited) {
            v = false;   // 注意这里不能改key
        }
        // 正确的重置方式
        visited.clear();
        for (auto& [v, _] : graph) visited[v] = false;
        for (auto& [v, _] : graphT) visited[v] = false;

        // 第2次DFS（按f降序）
        for (int i = finishOrder.size() - 1; i >= 0; i--) {
            int u = finishOrder[i];
            if (!visited[u]) {
                vector<int> component;
                dfs2(u, component);
                sccList.push_back(component);
            }
        }

        return sccList;
    }
};
```

### 5.6 Tarjan算法（补充）

除了Kosaraju算法，**Tarjan算法**也是求强连通分量的经典方法，只需一次DFS，时间复杂度同样是 Θ(V + E)。

**核心思想**：
- 维护一个栈和每个顶点的 `dfn`（发现序号）和 `low`（能回溯到的最早祖先的dfn）
- 当 `low[u] == dfn[u]` 时，栈中从u到栈顶的所有顶点构成一个强连通分量

```python
# Python 实现：Tarjan强连通分量算法
def tarjan_scc(graph):
    """Tarjan算法求强连通分量
    只需一次DFS，效率更高
    """
    dfn = {}       # 发现序号
    low = {}       # 能回溯到的最早dfn值
    on_stack = {}  # 是否在栈中
    stack = []     # 栈
    timer = [0]    # 时间戳
    scc_list = []  # 结果
    
    def tarjan(u):
        dfn[u] = low[u] = timer[0]   # 初始化dfn和low
        timer[0] += 1
        stack.append(u)               # 入栈
        on_stack[u] = True
        
        for v in graph.get(u, []):
            if v not in dfn:           # v未访问 → 树边
                tarjan(v)
                low[u] = min(low[u], low[v])  # 用子节点的low更新
            elif on_stack.get(v, False):  # v在栈中 → 后向边
                low[u] = min(low[u], dfn[v])  # 用v的dfn更新
        
        # 如果low[u]==dfn[u]，u是强连通分量的根
        if low[u] == dfn[u]:
            component = []
            while True:
                v = stack.pop()
                on_stack[v] = False
                component.append(v)
                if v == u:
                    break
            scc_list.append(component)
    
    for v in graph:
        if v not in dfn:
            tarjan(v)
    
    return scc_list
```

### 5.7 FAQ

**Q1：为什么Kosaraju需要在转置图上做第2次DFS？不能直接在原图上做吗？**

不能！在原图上，从"源分量"出发DFS会跑到其他分量去，无法隔离单个SCC。转置图的作用是让"源分量"变成"汇分量"——汇分量没有出边到其他分量，DFS跑不出去，自然就只收集到这一个SCC里的顶点。

**Q2：Kosaraju和Tarjan哪个更好？**

| 维度 | Kosaraju | Tarjan |
|------|----------|--------|
| DFS次数 | 2次 | 1次 |
| 额外空间 | 需要存转置图 | 只需一个栈 |
| 实现难度 | 较简单 | 稍复杂 |
| 实际效率 | 略慢（两次遍历） | 略快 |
| 常数因子 | 较大 | 较小 |

竞赛中Tarjan更常用，但Kosaraju更容易理解和实现。

**Q3：强连通分量和连通分量有什么区别？**

- **连通分量**（无向图）：顶点之间互相可达（双向都通）
- **强连通分量**（有向图）：顶点之间互相可达（有向路径双向通）
- **弱连通分量**（有向图）：把有向边当无向边看时的连通分量

关键区别：有向图中的"互相可达"要求**有向路径**双向都存在。

---

## 📊 本章算法总览

| 算法 | 输入 | 输出 | 时间复杂度 | 核心数据结构 |
|------|------|------|------------|--------------|
| BFS | 图G, 源点s | 最短距离, 广度优先树 | Θ(V+E) | 队列 |
| DFS | 图G | 发现/完成时间, 深度优先森林 | Θ(V+E) | 栈/递归 |
| 拓扑排序 | DAG | 线性排列 | Θ(V+E) | DFS + 链表 |
| Kosaraju | 有向图G | 强连通分量 | Θ(V+E) | 2次DFS + 转置图 |
| Tarjan | 有向图G | 强连通分量 | Θ(V+E) | 1次DFS + 栈 |

---

## 📝 本章小结

### 核心要点

1. **图的表示**：邻接链表适合稀疏图Θ(V+E)，邻接矩阵适合稠密图Θ(V²)但查边O(1)
2. **BFS**：逐层扩展，用队列，计算最短路径（边数最少）
3. **DFS**：一条路走到底再回溯，用递归/栈，发现/完成时间有括号结构
4. **拓扑排序**：DFS完成时间降序，仅适用于DAG
5. **强连通分量**：Kosaraju两次DFS，核心是转置图让源变汇

### 易错点

| 易错点 | 正确理解 |
|--------|----------|
| BFS的距离就是最短路径 | 仅当所有边权重相同时成立 |
| DFS一定能检测环 | 有向图的后向边表示有环，但无向图的后向边不一定 |
| 拓扑排序结果唯一 | 不唯一，取决于DFS的起始顶点和邻接链表顺序 |
| 转置图改变了强连通分量 | 转置图和原图有完全相同的强连通分量 |
| 拓扑排序前不需要判环 | 必须！有环的图不存在拓扑排序 |

### 与前后章节的联系

```
前序知识：
  第10章（基本数据结构）→ 队列（BFS用）、栈（DFS用）
  第21章（并查集）→ 连通性问题

后续章节：
  第23章（最小生成树）→ 基于BFS/DFS思想扩展
  第24章（单源最短路径）→ BFS是Dijkstra的特例
  第25章（所有结点对最短路径）→ 邻接矩阵表示的扩展
  第26章（最大流）→ 强连通分量的应用
```

### 课后练习建议

1. **22.1-1**：给定邻接链表，计算每个顶点的出度和入度
2. **22.2-2**：手动追踪BFS在图22-3上的执行过程
3. **22.3-2**：手动追踪DFS，标出每条边的类型
4. **22.4-1**：手动执行拓扑排序
5. **22.5-1**：手动执行Kosaraju算法
6. **编程题**：实现BFS判断二分图、DFS检测环、拓扑排序课程表

---

*完成时间: 2026年5月27日*
*下一章: 第23章 最小生成树*
