# 第23章 最小生成树（Minimum Spanning Trees）

> 本章解决一个极其优雅的问题：给定一个连通无向带权图，如何用最小的总代价把所有顶点连起来？两个经典算法——Kruskal和Prim，分别从"边"和"顶点"两个视角给出答案，而它们的共同灵魂是一把叫做"安全边定理"的钥匙

## 📖 本章导读

### 🎯 问题引入：为什么需要最小生成树？

**生活中的例子**：

想象你是一个**村长**，要给村里7户人家通自来水：

- 每两户之间挖沟铺管的成本不同（有的近，有的远，有的要绕山）
- 你想让**所有人家都能通水**（连通）
- 你想让**总成本最低**（最小权重）
- 但你**不需要铺多余的管**——只要每家都能通到水源就行（树，无环）

这就是最小生成树问题！**"最小"**指总权重最小，**"生成"**指包含所有顶点，**"树"**指无环连通子图。

**更多生活类比**：

| 场景 | 顶点 | 边（权重） | 最小生成树含义 |
|------|------|-----------|--------------|
| 自来水管网 | 各住户 | 铺管成本 | 最省钱的全村通水方案 |
| 光纤网络 | 各机房 | 光纤长度 | 最短的总光纤长度 |
| 公路规划 | 各城镇 | 修路费用 | 花最少的钱让所有城镇互通 |
| 电路板布线 | 各芯片引脚 | 连线长度 | 最短总布线长度 |

**本章路线图**：

```
问题定义 ──→ 最小生成树的性质 ──→ 安全边定理(核心) ──→ Kruskal算法 ──→ Prim算法
 23.1概念       23.1唯一性等         23.1切割定理          23.2按边选        23.3按顶点扩展
 铺管通全村     为什么一定是树       选边的"安全"准则     从短到长贪心选    从一个点向外长
```

---

## 1️⃣ 最小生成树的概念与性质（23.1 Growing a Minimum Spanning Tree）

### 1.1 问题定义

**最小生成树问题**：

- **输入**：连通无向图 G = (V, E)，权重函数 w: E → ℝ
- **输出**：一棵生成树 T ⊆ E，使得 w(T) = Σ w(u,v) 最小

**三个关键词逐个拆解**：

| 关键词 | 含义 | 类比 |
|--------|------|------|
| **生成** | T 包含 G 的所有顶点 | 每户人家都通水了 |
| **树** | T 是无环连通的 | 不存在多余的管（环路=浪费） |
| **最小** | T 的总权重最小 | 铺管总成本最低 |

### 1.2 为什么生成子图一定是树？

**问题**：最优解会不会有环？

**答案**：不可能！

**直觉**：假设最优解有一个环，那么环上一定有一条边可以删掉——删掉后所有顶点仍然连通，而总权重变小了（或不变），所以有环的解不可能是最优的。

**严格证明**：

```
假设 T* 是总权重最小的生成子图，且 T* 中有环 C
在环 C 上任取一条边 e = (u, v)
删掉 e 后：
  - 顶点 u 和 v 仍然通过环 C 的其余路径连通
  - 图的连通性不变（所有顶点仍可达）
  - 总权重减少了 w(e)（或不变，若 w(e) = 0）
→ 与 T* 最小矛盾！
∴ 最优解一定是无环的
```

**推论**：n 个顶点的生成树恰好有 **n-1** 条边。

**生活理解**：7户人家通水，最少需要6段管——就像"牵线搭桥"，n个点连通最少n-1条线。

### 1.3 最小生成树的唯一性

**问题**：最小生成树唯一吗？

**答案**：不一定！

**例子**：

```
    1       1
A ──── B ──── C

两种MST：
  MST1: A─B, B─C  （总权重2）
  MST2: A─B, A─C  ← 不对！A─C 不存在
  
正确的例子：
    1       1
A ──── B ──── C
 └─────────────┘
       1

三种MST，每种的权重都是 2：
  MST1: A─B, B─C
  MST2: A─C, B─C  （如果A─C存在且权重1）
  MST3: A─B, A─C  （如果A─C存在且权重1）
```

**更清晰的例子**（三角形等权）：

```
        2
    A ───── B
    │  ╲    │
   2│   2╲  │2
    │      ╲ │
    C───────D
        2

边全为2，任意选3条不构成环的都是MST
```

**定理**：如果图中所有边的权重互不相同，则最小生成树**唯一**。

### 1.4 最核心定理——安全边定理（切割定理）

这是整个MST理论的**灵魂**，Kruskal和Prim算法都是它的具体实现。

**定义：切割（Cut）**

一个切割 (S, V-S) 是将顶点集 V 分成两个非空子集 S 和 V-S。

**定义：横跨（Cross）**

如果一条边 (u, v) 的一个端点在 S 中，另一个在 V-S 中，则称这条边**横跨**切割 (S, V-S)。

**定义：尊重（Respect）**

如果边集 A 中没有横跨切割 (S, V-S) 的边，则称切割 (S, V-S) **尊重**边集 A。

**图解**：

```
    S             V-S
 ┌────────┐   ┌────────┐
 │  a ── b│   │  e ── f│
 │  │     │   │  │     │
 │  c ── d│   │  g ── h│
 └────────┘   └────────┘
     ↑              ↑
     └────── i ──────┘
          横跨边

A 中的边都在 S 或 V-S 内部 → 切割尊重 A
横跨边 i 不在 A 中
```

**🔥 定理23.1（安全边定理 / 切割定理）**：

> 设 G = (V, E) 是连通无向图，A ⊆ E 是某个MST的边子集。设 (S, V-S) 是尊重 A 的任意切割，(u, v) 是横跨该切割的轻量边（权重最小的横跨边），则 (u, v) 对 A 是**安全的**——即 A ∪ {(u,v)} 也是某个MST的边子集。

**生活类比**：

> 你正在修自来水管网，已经修好了一些管（边集A），这些管都在村东或村西。现在你需要在村东和村西之间修一条管来连通两区——你当然选**最便宜的那条**横跨管！选了它，你依然在通向最优方案的路上。

**严格证明**：

```
已知：A ⊆ T（T 是某棵MST），(S, V-S) 尊重 A，(u,v) 是横跨轻量边

情况1：(u,v) ∈ T
  → A ∪ {(u,v)} ⊆ T，已经是MST的子集，安全！

情况2：(u,v) ∉ T
  → T 中必有某条横跨边 (x,y)（因为 T 连通 S 和 V-S）
  → 构造 T' = T - {(x,y)} ∪ {(u,v)}
  
  验证 T' 也是生成树：
    - T' 有 |V|-1 条边 ✓
    - T' 连通：因为 (u,v) 和 (x,y) 都横跨同一切割，
      去掉 (x,y) 后 S 和 V-S 通过 T 中的其他边各自连通，
      (u,v) 重新连接了 S 和 V-S ✓
    - T' 无环：T 无环，替换一条边不引入环 ✓
  
  比较权重：
    w(u,v) ≤ w(x,y)（因为 (u,v) 是轻量边）
    → w(T') = w(T) - w(x,y) + w(u,v) ≤ w(T)
    → T' 也是MST！
  
  ∴ A ∪ {(u,v)} ⊆ T'，安全！
```

**推论（关键直觉）**：

> 只要找到"尊重当前边集的切割"和"横跨该切割的最轻边"，就能安全地把这条边加入答案。

Kruskal 和 Prim 只是**不同的方式来构造切割和找轻量边**！

### 1.5 最小生成树的循环不变式

**算法框架**：

```
GENERIC-MST(G, w)
1  A = ∅
2  while A does not form a spanning tree
3      find an edge (u, v) that is safe for A
4      A = A ∪ {(u, v)}
5  return A
```

**循环不变式**：

- **初始化**：A = ∅，显然是某个MST的子集
- **保持**：每次加入安全边，由安全边定理保证 A 仍是某个MST的子集
- **终止**：A 形成一棵生成树（|A| = |V|-1 且无环连通），且 A 是某个MST的子集，所以 A 本身就是MST

---

## 2️⃣ Kruskal算法（23.2 The Algorithms of Kruskal）

### 2.1 核心思想

**一句话**：**把所有边按权重从小到大排序，依次取——只要不形成环就加入**。

**生活类比**：

> 你面前有一堆水管，按价格从低到高排好。你从最便宜的开始拿，每次拿一根，只要它不会造成"铺了两条路通向同一个人家"（形成环），就铺上。贪心地拿最便宜的，最后一定得到最省钱的方案。

### 2.2 算法步骤详解

```
KRUSKAL-MST(G, w)
1  A = ∅                                    // 结果边集
2  for each vertex v in G.V                 // 每个顶点自成一个集合
3      MAKE-SET(v)                          // 初始时每个顶点独立
4  sort the edges of G.E into nondecreasing // 边按权重升序排列
   order by weight w
5  for each edge (u, v) in G.E, taken in   // 按权重从小到大遍历
   nondecreasing order by weight
6      if FIND-SET(u) ≠ FIND-SET(v)        // u和v不在同一连通分量？
7          A = A ∪ {(u, v)}                 // 安全！加入结果
8          UNION(u, v)                      // 合并两个连通分量
9  return A
```

**核心问题**：如何判断"加入这条边是否会形成环"？

**答案**：用**并查集**（第21章）！
- `FIND-SET(u) ≠ FIND-SET(v)`：u 和 v 在不同连通分量 → 加入 (u,v) 不形成环
- `FIND-SET(u) = FIND-SET(v)`：u 和 v 已在同一连通分量 → 加入 (u,v) 会形成环，跳过

### 2.3 图解过程

**输入图**：

```
        4
    a ───── b
    │╲      │╲
   8│ ╲3   2│ ╲6
    │   ╲   │   ╲
    c──────d─────e
        5       1
         ╲     ╱
          ╲   ╱
           ╲ ╱ 2
            f
```

**边按权重排序**：

| 序号 | 边 | 权重 |
|:---:|:---:|:---:|
| 1 | (d, e) | 1 |
| 2 | (b, d) | 2 |
| 3 | (e, f) | 2 |
| 4 | (a, c) | 3 |
| 5 | (a, d) | 3 |
| 6 | (b, e) | 6 |
| 7 | (c, d) | 5 |
| 8 | (a, b) | 4 |
| 9 | (c, f) | 2 |
| 10 | (a, c) | 8 |

Wait，让我用CLRS教材经典示例图，更清晰：

**输入图（CLRS风格）**：

```
        4        8
    a ───── b ───── c
    │      │      │
   11│     8│     2│
    │      │      │
    f      g──────h
    │      │   1  │
    7│    6│      │7
    │      │      │
    i──────d──────e
       2      16

更标准的图：
      4      8      2
  a─────b──────c──────d
  │     │            │
  8     7            9
  │     │            │
  e─────f──────g──────h
      1     6     2
```

让我用一个更简单清晰的示例：

**输入图（9个顶点，14条边）**：

```
顶点: a, b, c, d, e, f, g, h, i

边及权重:
(a,b,4)  (a,h,8)  
(b,h,11) (b,c,8)  
(c,d,7)  (c,f,4)  (c,i,2)
(d,e,9)  (d,f,14)
(e,f,10)
(f,g,2)
(g,h,1)  (g,i,6)
(h,i,7)
```

**图的结构**：

```
    4       8       7       9
a─────b──────c──────d──────e
│            │              │
8            4              10
│            │              │
h──────g─────f──────────────┘
 │  1   │  2  │     14
 │      6     │
 11     i     │
  ╲    ╱2     │
   ╲  ╱      │
    ╲╱       │
 (b,h)      (d,f)
```

**Kruskal执行过程**：

```
初始状态：每个顶点自成集合 {a},{b},{c},{d},{e},{f},{g},{h},{i}

Step 1: (g,h,1) — g∈{g}, h∈{h} → 不同集合 ✓ 加入
  集合: {a},{b},{c},{d},{e},{f},{g,h},{i}
  A = {(g,h)}

Step 2: (c,i,2) — c∈{c}, i∈{i} → 不同集合 ✓ 加入
  集合: {a},{b},{c,i},{d},{e},{f},{g,h}
  A = {(g,h),(c,i)}

Step 3: (f,g,2) — f∈{f}, g∈{g,h} → 不同集合 ✓ 加入
  集合: {a},{b},{c,i},{d},{e},{f,g,h}
  A = {(g,h),(c,i),(f,g)}

Step 4: (a,b,4) — a∈{a}, b∈{b} → 不同集合 ✓ 加入
  集合: {a,b},{c,i},{d},{e},{f,g,h}
  A = {(g,h),(c,i),(f,g),(a,b)}

Step 5: (c,f,4) — c∈{c,i}, f∈{f,g,h} → 不同集合 ✓ 加入
  集合: {a,b},{c,f,g,h,i},{d},{e}
  A = {(g,h),(c,i),(f,g),(a,b),(c,f)}

Step 6: (b,c,8) → 但先看更小的边...

  下一条: (g,i,6) — g∈{c,f,g,h,i}, i∈{c,f,g,h,i} → 同一集合 ✗ 跳过！
  
Step 7: (a,h,8) — a∈{a,b}, h∈{c,f,g,h,i} → 不同集合 ✓ 加入
  集合: {a,b,c,f,g,h,i},{d},{e}
  A = {(g,h),(c,i),(f,g),(a,b),(c,f),(a,h)}

Step 8: (c,d,7) — c∈{a,b,c,f,g,h,i}, d∈{d} → 不同集合 ✓ 加入
  集合: {a,b,c,d,f,g,h,i},{e}
  A = {(g,h),(c,i),(f,g),(a,b),(c,f),(a,h),(c,d)}

Step 9: (d,e,9) — d∈{a,b,c,d,f,g,h,i}, e∈{e} → 不同集合 ✓ 加入
  集合: {a,b,c,d,e,f,g,h,i}
  A = {(g,h),(c,i),(f,g),(a,b),(c,f),(a,h),(c,d),(d,e)}

所有顶点已在同一集合 → MST完成！
总权重 = 1+2+2+4+4+8+7+9 = 37
```

**MST可视化**：

```
    4               7       9
a─────b          c──────d──────e
│                 │           
8                 4           
│                 │           
h──────g─────f    
   1    2   
        │
        i (2)
        
MST边: (a,b,4), (a,h,8), (c,d,7), (c,f,4), (c,i,2),
       (d,e,9), (f,g,2), (g,h,1)
总权重: 37
```

### 2.4 Kruskal与安全边定理的联系

每次选择 (u,v) 时：
- 当前边集 A 将顶点分成了若干连通分量
- 每个连通分量构成集合 S_i，相邻连通分量之间的切割 (S_i, V-S_i) 尊重 A
- (u,v) 是横跨某切割的**最轻边**（因为边按权重排序）
- 由安全边定理，(u,v) 是安全的！

### 2.5 代码实现

**Python实现**：

```python
class UnionFind:
    """并查集，支持路径压缩和按秩合并"""
    def __init__(self, n):
        self.parent = list(range(n))  # parent[i] = i，各自为根
        self.rank = [0] * n           # 秩（树高度的上界）
    
    def find(self, x):
        """查找x的根，带路径压缩"""
        if self.parent[x] != x:
            self.parent[x] = self.find(self.parent[x])  # 路径压缩：直接指向根
        return self.parent[x]
    
    def union(self, x, y):
        """合并x和y所在的集合，按秩合并"""
        rx, ry = self.find(x), self.find(y)  # 先找各自的根
        if rx == ry:
            return False  # 已在同一集合，无需合并
        # 按秩合并：矮树挂到高树下面
        if self.rank[rx] < self.rank[ry]:
            self.parent[rx] = ry       # rx的根指向ry
        elif self.rank[rx] > self.rank[ry]:
            self.parent[ry] = rx       # ry的根指向rx
        else:
            self.parent[ry] = rx       # 秩相同，随便挂，但被挂的秩+1
            self.rank[rx] += 1
        return True


def kruskal(n, edges):
    """
    Kruskal算法求最小生成树
    参数：
        n: 顶点数（顶点编号0到n-1）
        edges: 边列表，每条边是 (权重, 顶点u, 顶点v)
    返回：
        (mst_edges, total_weight) - MST的边列表和总权重
    """
    # Step 1: 边按权重升序排序
    edges.sort()  # 按元组第一个元素（权重）排序
    
    uf = UnionFind(n)  # 初始化并查集
    mst = []           # 存放MST的边
    total_weight = 0   # MST总权重
    
    # Step 2: 依次考虑每条边
    for w, u, v in edges:
        # 如果u和v不在同一连通分量，加入MST
        if uf.find(u) != uf.find(v):
            mst.append((u, v, w))  # 记录这条边
            total_weight += w       # 累加权重
            uf.union(u, v)          # 合并两个连通分量
    
    return mst, total_weight


# === 测试 ===
if __name__ == "__main__":
    # 9个顶点(a=0, b=1, ..., i=8)，14条边
    n = 9
    edges = [
        (4, 0, 1),   # (a,b,4)
        (8, 0, 7),   # (a,h,8)
        (11, 1, 7),  # (b,h,11)
        (8, 1, 2),   # (b,c,8)
        (7, 2, 3),   # (c,d,7)
        (4, 2, 5),   # (c,f,4)
        (2, 2, 8),   # (c,i,2)
        (9, 3, 4),   # (d,e,9)
        (14, 3, 5),  # (d,f,14)
        (10, 4, 5),  # (e,f,10)
        (2, 5, 6),   # (f,g,2)
        (1, 6, 7),   # (g,h,1)
        (6, 6, 8),   # (g,i,6)
        (7, 7, 8),   # (h,i,7)
    ]
    
    mst, total = kruskal(n, edges)
    print("MST边：")
    names = "abcdefghi"
    for u, v, w in mst:
        print(f"  ({names[u]}, {names[v]}) 权重={w}")
    print(f"总权重: {total}")
    # 输出: 总权重 37
```

**C++实现**：

```cpp
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

// 并查集
class UnionFind {
private:
    vector<int> parent;  // parent[i]: i的父节点
    vector<int> rank_;   // rank_[i]: 以i为根的树的秩

public:
    UnionFind(int n) {
        parent.resize(n);
        rank_.resize(n, 0);
        for (int i = 0; i < n; i++)
            parent[i] = i;  // 初始各自为根
    }
    
    // 查找根，带路径压缩
    int find(int x) {
        if (parent[x] != x)
            parent[x] = find(parent[x]);  // 路径压缩
        return parent[x];
    }
    
    // 合并两个集合，按秩合并
    bool unite(int x, int y) {
        int rx = find(x), ry = find(y);
        if (rx == ry) return false;  // 已在同一集合
        // 按秩合并
        if (rank_[rx] < rank_[ry])
            parent[rx] = ry;
        else if (rank_[rx] > rank_[ry])
            parent[ry] = rx;
        else {
            parent[ry] = rx;
            rank_[rx]++;
        }
        return true;
    }
};

// 边结构体
struct Edge {
    int u, v, weight;
    // 重载<运算符，用于排序
    bool operator<(const Edge& other) const {
        return weight < other.weight;
    }
};

// Kruskal算法
pair<vector<Edge>, int> kruskal(int n, vector<Edge>& edges) {
    // Step 1: 边按权重升序排序
    sort(edges.begin(), edges.end());
    
    UnionFind uf(n);
    vector<Edge> mst;  // MST的边
    int total = 0;     // 总权重
    
    // Step 2: 依次考虑每条边
    for (auto& e : edges) {
        if (uf.find(e.u) != uf.find(e.v)) {  // 不在同一集合
            mst.push_back(e);   // 加入MST
            total += e.weight;  // 累加权重
            uf.unite(e.u, e.v); // 合并集合
        }
    }
    
    return {mst, total};
}

int main() {
    int n = 9;
    vector<Edge> edges = {
        {0,1,4},  {0,7,8},  {1,7,11}, {1,2,8},
        {2,3,7},  {2,5,4},  {2,8,2},  {3,4,9},
        {3,5,14}, {4,5,10}, {5,6,2},  {6,7,1},
        {6,8,6},  {7,8,7}
    };
    
    auto [mst, total] = kruskal(n, edges);
    cout << "MST边：" << endl;
    const char* names = "abcdefghi";
    for (auto& e : mst)
        cout << "  (" << names[e.u] << ", " << names[e.v] 
             << ") 权重=" << e.weight << endl;
    cout << "总权重: " << total << endl;
    // 输出: 总权重 37
    return 0;
}
```

### 2.6 复杂度分析

| 步骤 | 时间 | 说明 |
|------|------|------|
| 排序边 | O(E log E) | 主导项 |
| 初始化并查集 | O(V) | 每个顶点MAKE-SET |
| 遍历边（2E次FIND-SET + V次UNION） | O(E α(V)) | α是反阿克曼函数，极慢增长 |
| **总计** | **O(E log E)** = **O(E log V)** | 因为 log E = O(log V²) = O(2 log V) |

**注**：α(V) 增长极慢，对于任何实际可能的 V 值（比如 V < 10^80，宇宙原子数），α(V) ≤ 4，所以可视为常数。

### 2.7 FAQ

**Q1: Kruskal为什么是贪心算法？它不会"贪心失效"吗？**

A: 关键在于安全边定理保证了每一步的贪心选择都是"安全"的。普通贪心可能因局部最优导致全局非最优，但MST问题有**最优子结构**——每一步选择最轻的安全边，必定可以扩展到全局最优。这就是切割定理的威力。

**Q2: 如果边权重相同怎么处理？**

A: 任选一条！安全边定理说的是"轻量边"（可能不唯一），选任何一条轻量边都是安全的。所以Kruskal对同权重边的处理顺序不影响正确性，但可能影响得到**哪一棵**MST。

**Q3: Kruskal对非连通图会怎样？**

A: 会得到**最小生成森林**（Minimum Spanning Forest）——每个连通分量各自一棵MST。结果边数为 V - C（C是连通分量数），而非 V-1。

---

## 3️⃣ Prim算法（23.3 The Algorithms of Prim）

### 3.1 核心思想

**一句话**：**从一个顶点出发，每次选"离当前树最近"的顶点加入**。

**生活类比**：

> 想象你在村口建了一个水塔，从水塔开始往外铺管。每次你看看哪个**还没通水的住户**离**已通水的住户**最近，就先给他铺管。这样逐步扩展，直到全村通水。

**Kruskal vs Prim 的视角差异**：

| | Kruskal | Prim |
|---|---------|------|
| **视角** | 从**边**出发 | 从**顶点**出发 |
| **类比** | 管子从便宜到贵，能铺就铺 | 从水塔开始，往最近的住户扩 |
| **增长方式** | 多棵树逐渐合并 | 一棵树逐渐长大 |
| **数据结构** | 并查集 | 优先队列（最小堆） |

### 3.2 算法步骤详解

```
PRIM-MST(G, w, r)  // r是起始顶点
1  for each u in G.V
2      u.key = ∞         // u到树的最小边权（"到树的距离"）
3      u.π = NIL         // u在MST中的父节点
4  r.key = 0              // 起始顶点距离为0（自己到自己为0）
5  Q = G.V                // 所有点放入优先队列（按key排序）
6  while Q ≠ ∅
7      u = EXTRACT-MIN(Q)  // 取出离树最近的顶点
8      for each v in G.Adj[u]  // 遍历u的所有邻居
9          if v ∈ Q and w(u,v) < v.key  // v还没加入树，且u到v更近
10             v.π = u           // 更新父节点
11             v.key = w(u,v)    // 更新到树的最小距离
12 return A = {(v, v.π) : v ∈ V - {r}}
```

**核心机制**：

- `key[v]`：顶点v到**当前MST**的最小边权重（v"到树的距离"）
- `π[v]`：通过哪条边到达v（v在MST中的父节点）
- `Q`：还没加入MST的顶点集合，按key排序的优先队列
- `EXTRACT-MIN(Q)`：取出key最小的顶点（离树最近的那个）

### 3.3 图解过程

**同样的9顶点图**：

```
初始状态（从顶点a开始）：
  key: [a:0, b:∞, c:∞, d:∞, e:∞, f:∞, g:∞, h:∞, i:∞]
  π:   [all NIL]
  Q:   {a(0), b(∞), c(∞), d(∞), e(∞), f(∞), g(∞), h(∞), i(∞)}

Step 1: EXTRACT-MIN → a (key=0)
  a的邻居: b(w=4), h(w=8)
  b: 4 < ∞ → key[b]=4, π[b]=a
  h: 8 < ∞ → key[h]=8, π[h]=a
  MST边: (a)
  key: [b:4, c:∞, d:∞, e:∞, f:∞, g:∞, h:8, i:∞]

Step 2: EXTRACT-MIN → b (key=4)
  b的邻居: a(已在树), c(w=8), h(w=11)
  c: 8 < ∞ → key[c]=8, π[c]=b
  h: 11 > 8 → 不更新（a到h更近，只要8）
  MST边: (a,b)
  key: [c:8, d:∞, e:∞, f:∞, g:∞, h:8, i:∞]

Step 3: EXTRACT-MIN → c 或 h (都是key=8，选c)
  c的邻居: b(已在树), d(w=7), f(w=4), i(w=2)
  d: 7 < ∞ → key[d]=7, π[d]=c
  f: 4 < ∞ → key[f]=4, π[f]=c
  i: 2 < ∞ → key[i]=2, π[i]=c
  MST边: (a,b), (b,c)
  key: [d:7, e:∞, f:4, g:∞, h:8, i:2]

Step 4: EXTRACT-MIN → i (key=2)
  i的邻居: c(已在树), g(w=6), h(w=7)
  g: 6 < ∞ → key[g]=6, π[g]=i
  h: 7 < 8 → key[h]=7, π[h]=i （更新！i到h比a到h更近）
  MST边: (a,b), (b,c), (c,i)
  key: [d:7, e:∞, f:4, g:6, h:7]

Step 5: EXTRACT-MIN → f (key=4)
  f的邻居: c(已在树), d(w=14), e(w=10), g(w=2)
  d: 14 > 7 → 不更新
  e: 10 < ∞ → key[e]=10, π[e]=f
  g: 2 < 6 → key[g]=2, π[g]=f （更新！f到g比i到g更近）
  MST边: (a,b), (b,c), (c,i), (c,f)
  key: [d:7, e:10, g:2, h:7]

Step 6: EXTRACT-MIN → g (key=2)
  g的邻居: f(已在树), h(w=1), i(已在树)
  h: 1 < 7 → key[h]=1, π[h]=g （更新！g到h比i到h更近）
  MST边: (a,b), (b,c), (c,i), (c,f), (f,g)
  key: [d:7, e:10, h:1]

Step 7: EXTRACT-MIN → h (key=1)
  h的邻居: a(已在树), b(已在树), g(已在树), i(已在树)
  所有邻居都已在树 → 无更新
  MST边: (a,b), (b,c), (c,i), (c,f), (f,g), (g,h)

Step 8: EXTRACT-MIN → d (key=7)
  d的邻居: c(已在树), e(w=9), f(已在树)
  e: 9 < 10 → key[e]=9, π[e]=d （更新！d到e比f到e更近）
  MST边: (a,b), (b,c), (c,i), (c,f), (f,g), (g,h), (c,d)

Step 9: EXTRACT-MIN → e (key=9)
  e的邻居: d(已在树), f(已在树)
  所有邻居都已在树 → 无更新
  MST边: (a,b), (b,c), (c,i), (c,f), (f,g), (g,h), (c,d), (d,e)

MST完成！总权重 = 4+8+2+4+2+1+7+9 = 37 ✓
```

**MST边列表**：

| MST边 | 权重 | 来源 |
|--------|------|------|
| (a,b) | 4 | Step 2 |
| (b,c) | 8 | Step 3 |
| (c,i) | 2 | Step 4 |
| (c,f) | 4 | Step 5 |
| (f,g) | 2 | Step 6 |
| (g,h) | 1 | Step 7 |
| (c,d) | 7 | Step 8 |
| (d,e) | 9 | Step 9 |
| **总计** | **37** | |

与Kruskal结果相同（虽然边可能不同，但权重相同，都是37）。

### 3.4 Prim与安全边定理的联系

每次 EXTRACT-MIN 取出顶点 u 时：
- 已在MST中的顶点构成集合 S，Q中的顶点构成 V-S
- 切割 (S, V-S) 尊重当前的边集 A（A 中的边都在 S 内部）
- u.key = min{w(u,v) : v ∈ S}，即 u 是横跨该切割的最轻边
- 由安全边定理，(u, π[u]) 是安全的！

### 3.5 代码实现

**Python实现（使用heapq）**：

```python
import heapq

def prim(n, graph, start=0):
    """
    Prim算法求最小生成树
    参数：
        n: 顶点数
        graph: 邻接表，graph[u] = [(v, weight), ...]
        start: 起始顶点
    返回：
        (mst_edges, total_weight) - MST的边列表和总权重
    """
    key = [float('inf')] * n    # 每个顶点到MST的最小距离
    parent = [-1] * n            # 每个顶点在MST中的父节点
    in_mst = [False] * n         # 是否已加入MST
    key[start] = 0               # 起始顶点距离为0
    
    # 优先队列：(key值, 顶点编号)
    pq = [(0, start)]
    
    while pq:
        # 取出离MST最近的顶点
        k, u = heapq.heappop(pq)
        
        if in_mst[u]:   # 已经在MST中，跳过（lazy deletion）
            continue
        in_mst[u] = True  # 加入MST
        
        # 遍历u的所有邻居
        for v, w in graph[u]:
            if not in_mst[v] and w < key[v]:
                # 发现更近的路径到v
                key[v] = w        # 更新到树的最小距离
                parent[v] = u     # 更新父节点
                heapq.heappush(pq, (key[v], v))  # 加入优先队列
    
    # 构建MST边列表
    mst = []
    total = 0
    for v in range(n):
        if parent[v] != -1:  # 非根节点
            mst.append((parent[v], v, key[v]))
            total += key[v]
    
    return mst, total


# === 测试 ===
if __name__ == "__main__":
    n = 9
    # 构建邻接表（无向图，每条边存两次）
    graph = [[] for _ in range(n)]
    edges = [
        (0,1,4),  (0,7,8),  (1,7,11), (1,2,8),
        (2,3,7),  (2,5,4),  (2,8,2),  (3,4,9),
        (3,5,14), (4,5,10), (5,6,2),  (6,7,1),
        (6,8,6),  (7,8,7)
    ]
    for u, v, w in edges:
        graph[u].append((v, w))
        graph[v].append((u, w))
    
    mst, total = prim(n, graph, start=0)
    names = "abcdefghi"
    print("MST边：")
    for u, v, w in mst:
        print(f"  ({names[u]}, {names[v]}) 权重={w}")
    print(f"总权重: {total}")
    # 输出: 总权重 37
```

**C++实现（使用priority_queue）**：

```cpp
#include <iostream>
#include <vector>
#include <queue>
#include <climits>
using namespace std;

typedef pair<int, int> pii;  // (距离, 顶点)

pair<vector<tuple<int,int,int>>, int> prim(int n, vector<vector<pii>>& graph, int start=0) {
    vector<int> key(n, INT_MAX);     // 到MST的最小距离
    vector<int> parent(n, -1);       // MST中的父节点
    vector<bool> inMST(n, false);    // 是否已加入MST
    key[start] = 0;
    
    // 最小堆：(key值, 顶点编号)
    priority_queue<pii, vector<pii>, greater<pii>> pq;
    pq.push({0, start});
    
    while (!pq.empty()) {
        auto [k, u] = pq.top();
        pq.pop();
        
        if (inMST[u]) continue;  // lazy deletion
        inMST[u] = true;
        
        // 遍历u的所有邻居
        for (auto [v, w] : graph[u]) {
            if (!inMST[v] && w < key[v]) {
                key[v] = w;          // 更新最小距离
                parent[v] = u;       // 更新父节点
                pq.push({key[v], v});
            }
        }
    }
    
    // 构建MST边列表
    vector<tuple<int,int,int>> mst;
    int total = 0;
    for (int v = 0; v < n; v++) {
        if (parent[v] != -1) {
            mst.push_back({parent[v], v, key[v]});
            total += key[v];
        }
    }
    return {mst, total};
}

int main() {
    int n = 9;
    vector<vector<pii>> graph(n);
    // 无向图，每条边存两次
    vector<tuple<int,int,int>> edges = {
        {0,1,4},  {0,7,8},  {1,7,11}, {1,2,8},
        {2,3,7},  {2,5,4},  {2,8,2},  {3,4,9},
        {3,5,14}, {4,5,10}, {5,6,2},  {6,7,1},
        {6,8,6},  {7,8,7}
    };
    for (auto [u, v, w] : edges) {
        graph[u].push_back({v, w});
        graph[v].push_back({u, w});
    }
    
    auto [mst, total] = prim(n, graph, 0);
    const char* names = "abcdefghi";
    cout << "MST边：" << endl;
    for (auto [u, v, w] : mst)
        cout << "  (" << names[u] << ", " << names[v] 
             << ") 权重=" << w << endl;
    cout << "总权重: " << total << endl;
    return 0;
}
```

### 3.6 复杂度分析

Prim的复杂度取决于优先队列的实现：

| 优先队列实现 | EXTRACT-MIN | DECREASE-KEY | 总复杂度 |
|-------------|-------------|-------------|---------|
| 二叉堆 | O(log V) | O(log V) | O(E log V) |
| 斐波那契堆（第20章） | O(log V) 摊销 | O(1) 摊销 | **O(E + V log V)** |

**分析**：
- 二叉堆：V 次 EXTRACT-MIN + 最多 E 次 DECREASE-KEY → O(V log V + E log V) = O(E log V)
- 斐波那契堆：V 次 EXTRACT-MIN(O(log V)) + E 次 DECREASE-KEY(O(1)) → O(E + V log V)

**何时用哪种？**

| 场景 | 推荐 | 原因 |
|------|------|------|
| 稀疏图（E ≈ V） | Prim + 斐波那契堆 | O(V log V) |
| 稠密图（E ≈ V²） | Prim + 斐波那契堆 | O(V²)，比Kruskal的O(V² log V)好 |
| 一般情况 | Prim + 二叉堆 | 实现简单，常数小 |
| 稀疏图且不想实现斐波那契堆 | **Kruskal** | O(E log V)，同样高效 |

### 3.7 FAQ

**Q1: Prim的起始顶点会影响结果吗？**

A: 不会影响**MST的总权重**（同一张图的最小生成树总权重唯一，除非有多条同权边），但可能影响**具体选了哪些边**（即得到哪一棵MST）。

**Q2: 为什么Prim不会形成环？**

A: 因为每次 EXTRACT-MIN 取出的顶点 u 是从Q（不在MST中的顶点）中选的，它通过 (u, π[u]) 与MST相连。u 之前不在MST中，所以 (u, π[u]) 一定连接了MST内和MST外的两个顶点，不可能形成环。

**Q3: Prim中的"lazy deletion"是什么？**

A: 上面Python代码中，当同一个顶点v被多次加入优先队列时（每次发现更短路径就push），我们不是去更新队列中已有的条目（堆不支持高效更新），而是直接push新的，等pop出来时检查 `in_mst[u]`，如果已在MST中就跳过。这就是"lazy deletion"——删得晚但不影响正确性。

---

## 4️⃣ Kruskal vs Prim 全面对比

### 4.1 核心对比

| 维度 | Kruskal | Prim |
|------|---------|------|
| **策略** | 按边选，从短到长 | 按顶点扩，从近到远 |
| **增长方式** | 多棵树合并成一棵 | 一棵树逐渐长大 |
| **核心数据结构** | 并查集（第21章） | 优先队列（最小堆） |
| **起始条件** | 无需指定起点 | 需要指定起始顶点 |
| **适合图类型** | 稀疏图 | 稠密图 |
| **时间复杂度** | O(E log E) = O(E log V) | O(E + V log V)* |
| **空间复杂度** | O(V + E) | O(V + E) |

*Prim的O(E + V log V)需要斐波那契堆；用二叉堆则为O(E log V)

### 4.2 选择策略

```
选哪个算法？
│
├── 图很稀疏（E ≈ V）？
│   └── Kruskal（排序快，并查集操作几乎O(1)）
│
├── 图很稠密（E ≈ V²）？
│   └── Prim + 斐波那契堆（O(V²) vs O(V² log V)）
│
├── 需要处理非连通图？
│   └── Kruskal（自动得到最小生成森林）
│
└── 需要在线/增量式构建？
    └── Prim（逐步扩展，天然支持）
```

### 4.3 两者与安全边定理的统一视角

```
安全边定理：找尊重A的切割 → 找横跨该切割的最轻边 → 安全！

Kruskal的实现：
  切割 = 各连通分量之间的分割
  最轻边 = 排序后当前最短的横跨边
  
Prim的实现：
  切割 = 已在MST的顶点 vs 不在MST的顶点
  最轻边 = Q中key最小的顶点对应的边

殊途同归，都是安全边定理的具体实例！
```

---

## 5️⃣ 扩展话题

### 5.1 边权重非唯一时的处理

当存在多条同权重边时，MST可能不唯一。两种算法选出的MST可能不同，但总权重相同。

**性质**：即使权重不唯一，同一张图的所有MST中，权重为某个值的边的条数相同（"MST的权重多面体"性质）。

### 5.2 最大生成树

把所有边权重取反（w' = -w），然后跑MST算法，得到的就是最大生成树。

或者更简单：Kruskal中改为**降序排列**，Prim中改为**取key最大的**。

### 5.3 第二小生成树

**问题**：找一棵总权重第二小的生成树。

**方法**：
1. 先求MST（O(E log V)）
2. 对MST中每条边 e，删掉 e 后MST分成两棵子树，找横跨两棵子树的最轻边替换（O(E log V) 或 O(V²)）
3. 在所有替换方案中选总权重最小的

### 5.4 Borůvka算法（补充）

这是历史上第一个MST算法（1926年），比Kruskal和Prim更早：

**思想**：每一轮，每个连通分量同时选一条最轻的出边加入，然后合并。重复直到只剩一个连通分量。

**特点**：
- 天然并行——每个分量独立选边
- 每轮连通分量至少减半 → 最多 O(log V) 轮
- 时间复杂度：O(E log V)
- 现代 parallel MST 算法的基础

---

## 📝 本章小结

### 核心要点

1. **MST定义**：连通无向带权图中，总权重最小的生成树（n个顶点n-1条边，无环连通）
2. **安全边定理**：尊重A的切割的横跨轻量边一定是安全的——这是MST理论的灵魂
3. **Kruskal**：按边权排序，用并查集判环，贪心选边；O(E log V)
4. **Prim**：从一个顶点扩展，用优先队列选最近顶点；O(E + V log V)（斐波那契堆）
5. **两者统一**：都是安全边定理的具体实例，只是构造切割的方式不同

### 易错点

| 易错点 | 正确理解 |
|--------|----------|
| MST的边数是V | MST有V-1条边（n个顶点n-1条边） |
| MST一定唯一 | 仅当所有边权互不相同时唯一；有权重相同的边时可能有多棵MST，但总权重相同 |
| Kruskal是选最短边就行 | 必须检查是否形成环（同一连通分量），形成环则跳过 |
| Prim的key值不变 | key值会被不断更新（decrease-key操作） |
| 两种算法结果一定相同 | 权重一定相同，但具体选的边可能不同（当存在同权边时） |
| MST问题需要图是连通的 | Kruskal可处理非连通图（得到最小生成森林）；Prim需要对每个连通分量各跑一次 |
| Prim用二叉堆的复杂度是O(V²) | 不对，是O(E log V)；只有不用堆直接遍历找最小值才是O(V²) |

### 与前后章节的联系

```
前序知识：
  第21章（不相交集合/并查集）→ Kruskal的核心数据结构
  第22章（基本图算法）→ 图的表示（邻接链表）、BFS（Prim的思想渊源）
  第6章（堆排序）→ Prim的优先队列
  第20章（斐波那契堆）→ Prim的最优实现
  第16章（贪心算法）→ MST是贪心策略的经典成功案例

后续章节：
  第24章（单源最短路径）→ Prim和Dijkstra结构极度相似
  第25章（所有结点对最短路径）→ MST可用于近似TSP
  第26章（最大流）→ 割的概念与MST的切割有关联
```

### 课后练习建议

1. **23.1-1**：证明删除环上最重的边不影响MST的存在性
2. **23.1-5**：证明边权重互不相同时MST唯一
3. **23.2-1**：手动执行Kruskal算法（用教材图23-1）
4. **23.2-2**：手动执行Prim算法（用同一张图）
5. **23.2-4**：分析不同图结构下Kruskal的效率
6. **编程题**：实现Kruskal和Prim，用同一组数据验证结果权重相同

---

*完成时间: 2026年5月30日*
*下一章: 第24章 单源最短路径*
