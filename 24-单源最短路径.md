# 第24章 单源最短路径（Single-Source Shortest Paths）

> 本章回答一个无比现实的问题：从一个起点出发，怎么走才能最省？——不管你是导航软件找最短路线，还是路由器选最快转发路径，核心都是"单源最短路径"。三个经典算法——Bellman-Ford、DAG松弛、Dijkstra，分别应对"有负权边""无环""无负权"三种情况，而它们的共同灵魂是一种叫"松弛"的局部优化操作

## 📖 本章导读

### 🎯 问题引入：为什么需要单源最短路径？

**生活中的例子**：

想象你站在**南京南站**，想去东南大学九龙湖校区：

- 有多条路线可选：地铁、公交、打车、骑行，甚至组合出行
- 每条路线的"代价"不同（时间、费用、体力）
- 你只想知道：**从南京南站出发，到每个可能的目的地，最优路线是什么？**

这就是单源最短路径问题！**"单源"**指只有一个起点，**"最短"**指路径上边的权重之和最小。

**更多生活类比**：

| 场景 | 顶点 | 边（权重） | 最短路径含义 |
|------|------|-----------|------------|
| 导航系统 | 路口 | 行驶时间 | 从出发地到各目的地的最快路线 |
| 网络路由 | 路由器 | 延迟/带宽 | 数据包的最快转发路径 |
| 航班规划 | 机场 | 票价 | 从出发城市到各城市的最便宜路线 |
| 游戏地图 | 格子 | 移动消耗 | 从起点到各位置的最省体力路线 |
| 货币兑换 | 货币 | 汇率的负对数 | 最有利的兑换链 |

**与第23章最小生成树的关系**：

| 对比项 | 最小生成树 | 单源最短路径 |
|--------|-----------|------------|
| 目标 | 连通所有顶点，总代价最小 | 从一个源点到各顶点，分别代价最小 |
| 结果 | 一棵树（覆盖所有顶点） | 一棵最短路径树（从源点出发） |
| 图类型 | 无向图 | 有向图（也可无向） |
| 边权 | 必须非负 | Dijkstra要求非负，Bellman-Ford允许负 |

**本章路线图**：

```
问题定义 ──→ 最短路径的性质 ──→ 松弛操作(核心) ──→ Bellman-Ford ──→ DAG最短路径 ──→ Dijkstra ──→ 差分约束
 24.0概念     24.5三角不等式等     24.1局部优化      24.1允许负权边    24.2无环图的特权   24.3非负权的贪心  24.4线性规划对偶
  从家到各处   为什么不能更短       "能松就松"        全局松弛|V|-1轮   拓扑排序后逐个松   优先队列贪心选     最短路径的应用
```

---

## 1️⃣ 最短路径问题定义与基本概念（24.0 Preliminaries）

### 1.1 问题定义

**单源最短路径问题**：

- **输入**：带权有向图 G = (V, E)，权重函数 w: E → ℝ，源点 s ∈ V
- **输出**：从 s 到每个顶点 v 的最短路径

**几个关键概念**：

| 概念 | 定义 | 类比 |
|------|------|------|
| **路径权重** | 路径p上所有边权重之和：w(p) = Σw(e) | 路线的总花费 |
| **最短路径权重** | δ(s, v) = min{w(p) : s→v的路径p}，若无路径则为∞ | 从s到v最少要花多少 |
| **最短路径** | 权重等于δ(s, v)的路径 | 真正最省的路线 |
| **前驱子图** | 以s为根的子图，记录每个顶点的"前驱" | 最短路径树——从s长出来的一棵树 |

### 1.2 负权边与负权环

**为什么要有负权边？**

现实中的"负代价"：
- 货币套利：A→B汇率1.5，B→C汇率0.8，C→A汇率0.9。绕一圈 1.5×0.8×0.9 = 1.08 > 1，赚了！这等价于负权边
- 奖励机制：某段路虽然远，但走这段路会获得补贴（如高速费减免）

**负权边的麻烦**：

```
    3        -6
s ────→ a ────→ b
 └──────────────┘
        2

s→a→b 的权重 = 3 + (-6) = -3
s→b 直接走 = 2

负权边让"绕远路"反而更短！
```

**负权环的灾难**：

```
    3        -6
s ────→ a ←──── b
         └────→┘
           2

a→b→a 的权重 = (-6) + 2 = -4 < 0  ← 负权环！

s→a→b→a→b→a→... 绕每圈权重 -4
路径可以无限绕圈，权重趋近 -∞
→ 最短路径不存在！
```

**关键结论**：
- **负权边**：允许存在，但要小心处理
- **负权环**：如果从 s 可达负权环，则 s 到某些顶点的最短路径**不存在**（权重可以是 -∞）

### 1.3 最短路径树

**定义**：最短路径树是一棵以 s 为根的有向树，使得从 s 到树中任意顶点 v 的唯一路径就是从 s 到 v 的最短路径。

**类比**：想象 s 是总指挥部，最短路径树就是从总指挥部发出的最优通信网络——每条链路都是到那个节点的最优路线。

**注意**：最短路径树**不唯一**——可能有多条等权的最短路径，选不同的路径就得到不同的树。

**最短路径树的性质**：

| 性质 | 含义 |
|------|------|
| 子路径最优 | 最短路径的子路径也是最短路径 |
| 无环 | 最短路径不可能有环（除非零权环） |
| 树结构 | n个顶点的最短路径树恰好有 n-1 条边 |

### 1.4 通用算法框架

所有单源最短路径算法都遵循同一个框架：

```
INITIALIZE-SINGLE-SOURCE(G, s)   // 初始化：s距离=0，其他=∞
while 某种松弛条件:
    对某些边执行 RELAX(u, v, w)  // 松弛：尝试缩短距离
```

**关键区别**在于"松弛哪些边"和"松弛多少轮"。

---

## 2️⃣ 松弛操作——所有算法的灵魂（24.1 核心）

### 2.1 松弛的直觉

**生活类比**：你听说从家到学校要走40分钟，但今天发现一条新路——先到超市（15分钟），再从超市到学校（20分钟）——总共只要35分钟！于是你**更新**了你的认知：到学校最快35分钟。

这就是松弛！**发现更短的路，就更新**。

### 2.2 松弛操作的形式化

**RELAX(u, v, w)**：对边 (u, v) 执行松弛

```
if v.d > u.d + w(u, v):    # 如果经过 u 到 v 更近
    v.d = u.d + w(u, v)     # 更新 v 的距离估计
    v.π = u                  # 记录 v 的前驱为 u
```

**图解过程**：

```
松弛前：
  u.d = 5 ──── w=3 ────→ v.d = 10
  5 + 3 = 8 < 10？是的！→ 更新

松弛后：
  u.d = 5 ──── w=3 ────→ v.d = 8, v.π = u
```

**另一种情况**（不需要松弛）：

```
  u.d = 5 ──── w=3 ────→ v.d = 6
  5 + 3 = 8 > 6？不！→ 不更新，保持 v.d = 6
```

### 2.3 初始化操作

**INITIALIZE-SINGLE-SOURCE(G, s)**：

```
for each vertex v in G.V:
    v.d = ∞       # 距离估计初始化为无穷大
    v.π = NIL     # 前驱初始化为空
s.d = 0           # 源点到自己的距离为0
```

**类比**：出发前，你只知道"自己到自己是0分钟"，到其他所有地方都是"不知道（∞）"。随着探索，逐渐"发现"更短的路线。

### 2.4 松弛的重要性质

**性质1：上界性质**

在算法执行过程中，对于所有顶点 v：**v.d ≥ δ(s, v)**（距离估计永远不会低于真实最短距离）

> 类比：你的估时永远不会比实际最短时间更短——只能从高估逐渐收敛到真实值

**性质2：三角不等式**

对于所有边 (u, v)：**δ(s, v) ≤ δ(s, u) + w(u, v)**

> 类比：从南京到上海，不可能比"南京→苏州→上海"更远（三角不等式——直走不绕路更短或相等）

**性质3：无路径性质**

如果从 s 到 v 不存在路径，则 **v.d = δ(s, v) = ∞** 始终成立。

**性质4：收敛性质**

如果 s→...→u→v 是一条最短路径，且在松弛 (u, v) 之前 u.d = δ(s, u)，则松弛后 **v.d = δ(s, v)**。

> 这是松弛能"传递正确性"的关键——一旦前驱正确，后继也会正确

**性质5：路径松弛性质**

如果 p = v₀→v₁→...→vₖ 是从 s=v₀ 到 vₖ 的最短路径，且边按 (v₀,v₁), (v₁,v₂), ..., (vₖ₋₁,vₖ) 的顺序被松弛，则 vₖ.d = δ(s, vₖ)。

> 这是Bellman-Ford正确性的基础——只要每条边被松弛足够多次，正确性就能沿路径"传播"

### FAQ：为什么叫"松弛"？

**答**："松弛"的英文是 Relaxation，源自数学中的"松弛法"——把一个紧的约束（当前估计）"放松"到更宽松的（更小的）值。想象一根绷紧的弦，当你发现可以更短时，就把弦"放松"一点，让它缩到更短的位置。

---

## 3️⃣ Bellman-Ford算法——允许负权边（24.1 The Bellman-Ford Algorithm）

### 3.1 算法思想

**一句话**：对所有边松弛 |V|-1 轮，每轮都让"好消息"传播一步。

**生活类比**：想象一个谣言传播模型——
- 第1轮：只有源点 s 知道真相（d=0），s 的邻居通过"松弛"知道了
- 第2轮：s 邻居的邻居也知道了
- ...最多 |V|-1 轮，消息传遍所有人（因为最短路径最多经过 |V|-1 条边）

**为什么需要 |V|-1 轮？** 因为最短路径最多经过 |V|-1 条边（n个顶点的路径最多n-1条边），每轮松弛至少让"正确距离"沿路径传播一条边。

### 3.2 算法伪代码

```
BELLMAN-FORD(G, w, s)
1  INITIALIZE-SINGLE-SOURCE(G, s)
2  for i = 1 to |G.V| - 1:          # 对每条边松弛 |V|-1 轮
3      for each edge (u, v) in G.E:  # 遍历所有边
4          RELAX(u, v, w)            # 尝试松弛
5  for each edge (u, v) in G.E:      # 检查负权环
6      if v.d > u.d + w(u, v):       # 还能松弛？
7          return FALSE               # 有负权环！
8  return TRUE                        # 无负权环，结果正确
```

### 3.3 图解执行过程

**示例图**：

```
       6         5
  s ──────→ a ──────→ c
  │         ↑         ↑
  7│       -2         -2
  ↓       /          /
  b ────→ d
       5
  还有边: b→e(8), d→e(-4), c→e(9)
  完整邻接：
  s→a(6), s→b(7)
  a→c(5)
  b→d(5), b→e(8)
  d→a(-2), d→e(-4)
  c→e(9), c→b(-2)
```

**初始化**：
```
s.d=0, a.d=∞, b.d=∞, c.d=∞, d.d=∞, e.d=∞
```

**第1轮松弛**（逐条边处理）：
```
s→a(6):  a.d = 0+6 = 6    ✓ 更新
s→b(7):  b.d = 0+7 = 7    ✓ 更新
a→c(5):  c.d = 6+5 = 11   ✓ 更新
b→d(5):  d.d = 7+5 = 12   ✓ 更新
b→e(8):  e.d = 7+8 = 15   ✓ 更新
d→a(-2): a.d = 12+(-2)=10? 但 a.d=6 < 10，不更新
d→e(-4): e.d = 12+(-4)=8 < 15  ✓ 更新！e.d=8
c→e(9):  e.d = 11+9=20 > 8，不更新
c→b(-2): b.d = 11+(-2)=9 > 7，不更新
```

**第1轮后**：`s=0, a=6, b=7, c=11, d=12, e=8`

**第2轮松弛**：
```
s→a(6):  a.d = 0+6=6 = a.d，不更新
s→b(7):  b.d = 0+7=7 = b.d，不更新
a→c(5):  c.d = 6+5=11 = c.d，不更新
b→d(5):  d.d = 7+5=12 = d.d，不更新
b→e(8):  e.d = 7+8=15 > 8，不更新
d→a(-2): a.d = 12+(-2)=10 > 6，不更新
d→e(-4): e.d = 12+(-4)=8 = e.d，不更新
c→e(9):  e.d = 11+9=20 > 8，不更新
c→b(-2): b.d = 11+(-2)=9 > 7，不更新
```

**第2轮后**：所有值不变 → 已收敛！后续轮次也不会有变化。

**负权环检测**：遍历所有边，没有还能松弛的 → 返回 TRUE。

**最终结果**：

| 顶点 | 最短距离 | 路径 |
|------|---------|------|
| s | 0 | - |
| a | 6 | s→a |
| b | 7 | s→b |
| c | 11 | s→a→c |
| d | 12 | s→b→d |
| e | 8 | s→b→d→e |

### 3.4 复杂度分析

| 复杂度 | 值 | 理由 |
|--------|-----|------|
| 时间 | **O(VE)** | |V|-1 轮 × 每轮遍历 |E| 条边 |
| 空间 | **O(V)** | 存储每个顶点的 d 和 π |

> V个顶点E条边的图，Bellman-Ford的时间复杂度是 O(VE)。对于稠密图 E≈V²，复杂度为 O(V³)，很慢！

### 3.5 正确性证明思路

**核心思路**：经过 i 轮松弛后，所有最多经过 i 条边的最短路径都被正确计算。

**数学归纳**：
- **基础**：0轮后，只有 s.d = δ(s,s) = 0 正确
- **归纳**：假设 i-1 轮后，最多经过 i-1 条边的最短路径正确。第 i 轮对边 (u,v) 松弛时，如果 s→...→u→v 是最多 i 条边的最短路径，则前 i-1 条边部分已正确（归纳假设），松弛让 v.d 也正确
- **结论**：|V|-1 轮后，所有最多 |V|-1 条边的最短路径正确——而这覆盖了所有无负权环的最短路径

### 3.6 代码实现

**Python实现**：

```python
class Edge:
    """边的数据结构"""
    def __init__(self, u, v, weight):
        self.u = u          # 起点
        self.v = v          # 终点
        self.weight = weight  # 权重

def bellman_ford(vertices, edges, source):
    """
    Bellman-Ford 算法
    :param vertices: 顶点列表，如 ['s', 'a', 'b', 'c']
    :param edges: 边列表，如 [Edge('s', 'a', 6), Edge('a', 'c', 5)]
    :param source: 源点
    :return: (dist, pred, has_negative_cycle)
             dist: 最短距离字典
             pred: 前驱字典
             has_negative_cycle: 是否存在负权环
    """
    # 第1步：初始化
    dist = {}          # 距离估计
    pred = {}          # 前驱节点
    for v in vertices:
        dist[v] = float('inf')   # 所有顶点距离初始化为∞
        pred[v] = None           # 前驱初始化为空
    dist[source] = 0             # 源点距离为0

    # 第2步：对所有边松弛 |V|-1 轮
    n = len(vertices)
    for i in range(n - 1):               # |V|-1 轮
        for e in edges:                   # 遍历所有边
            # 尝试松弛边 e.u → e.v
            if dist[e.u] + e.weight < dist[e.v]:
                dist[e.v] = dist[e.u] + e.weight   # 发现更短路径，更新
                pred[e.v] = e.u                      # 记录前驱

    # 第3步：检查负权环
    for e in edges:
        if dist[e.u] + e.weight < dist[e.v]:
            # 还能松弛 → 存在从源点可达的负权环
            return dist, pred, True    # 有负权环！

    return dist, pred, False           # 无负权环，结果正确

# 使用示例
vertices = ['s', 'a', 'b', 'c', 'd', 'e']
edges = [
    Edge('s', 'a', 6), Edge('s', 'b', 7),
    Edge('a', 'c', 5),
    Edge('b', 'd', 5), Edge('b', 'e', 8),
    Edge('d', 'a', -2), Edge('d', 'e', -4),
    Edge('c', 'e', 9), Edge('c', 'b', -2),
]
dist, pred, neg_cycle = bellman_ford(vertices, edges, 's')
print(f"负权环: {neg_cycle}")
for v in vertices:
    print(f"  s→{v}: 距离={dist[v]}, 前驱={pred[v]}")
```

**C++实现**：

```cpp
#include <iostream>
#include <vector>
#include <limits>
using namespace std;

// 边的数据结构
struct Edge {
    int u, v;           // 起点、终点
    double weight;       // 权重（用double支持负数和无穷大）
    Edge(int u, int v, double w) : u(u), v(v), weight(w) {}
};

// Bellman-Ford算法
// 返回值：true表示无负权环，false表示有负权环
bool bellmanFord(int V, const vector<Edge>& edges, int source,
                 vector<double>& dist, vector<int>& pred) {
    // 第1步：初始化
    dist.assign(V, numeric_limits<double>::infinity());  // 所有距离=∞
    pred.assign(V, -1);                                  // 所有前驱=-1
    dist[source] = 0;                                    // 源点距离=0

    // 第2步：对所有边松弛 |V|-1 轮
    for (int i = 0; i < V - 1; i++) {         // |V|-1 轮
        for (const Edge& e : edges) {          // 遍历所有边
            if (dist[e.u] + e.weight < dist[e.v]) {  // 发现更短路径
                dist[e.v] = dist[e.u] + e.weight;    // 更新距离
                pred[e.v] = e.u;                      // 更新前驱
            }
        }
    }

    // 第3步：检查负权环
    for (const Edge& e : edges) {
        if (dist[e.u] + e.weight < dist[e.v]) {
            // 还能松弛 → 存在从源点可达的负权环
            return false;
        }
    }
    return true;  // 无负权环
}

int main() {
    // 顶点编号: 0=s, 1=a, 2=b, 3=c, 4=d, 5=e
    int V = 6;
    vector<Edge> edges = {
        Edge(0, 1, 6), Edge(0, 2, 7),     // s→a, s→b
        Edge(1, 3, 5),                      // a→c
        Edge(2, 4, 5), Edge(2, 5, 8),     // b→d, b→e
        Edge(4, 1, -2), Edge(4, 5, -4),   // d→a, d→e
        Edge(3, 5, 9), Edge(3, 2, -2),    // c→e, c→b
    };

    vector<double> dist;
    vector<int> pred;

    bool noNegCycle = bellmanFord(V, edges, 0, dist, pred);

    if (noNegCycle) {
        cout << "无负权环，最短路径如下：" << endl;
        for (int i = 0; i < V; i++) {
            cout << "  s→" << i << ": 距离=" << dist[i]
                 << ", 前驱=" << pred[i] << endl;
        }
    } else {
        cout << "存在从源点可达的负权环！" << endl;
    }
    return 0;
}
```

### FAQ：Bellman-Ford 能处理什么图？

**答**：任何有向图——有负权边也行！只要不存在从源点可达的负权环，就能给出正确答案。如果存在负权环，算法会检测到并报告。

### FAQ：Bellman-Ford 为什么这么慢？能优化吗？

**答**：O(VE) 确实慢，但这是"允许负权边"的代价。常见优化：
- **提前终止**：如果某一轮没有发生任何松弛，说明已经收敛，直接跳出
- **SPFA（Shortest Path Faster Algorithm）**：用队列维护"距离发生了变化的顶点"，只对它们的出边松弛。平均 O(E)，最坏仍 O(VE)

---

## 4️⃣ DAG中的最短路径——无环图的特权（24.2 Single-Source Shortest Paths in Directed Acyclic Graphs）

### 4.1 算法思想

**一句话**：先拓扑排序，再按拓扑序逐个松弛——每条边只松弛一次就够！

**为什么DAG这么特殊？** 因为DAG没有环，所以：
1. 不可能有负权环（没有环，哪来的负权环！）
2. 拓扑排序保证了：处理顶点 u 时，所有能到达 u 的顶点都已处理完毕
3. 因此每条边只需松弛一次

**生活类比**：大学的课程有先修关系——必须先学高数才能学概率论。如果你按照"先修关系"的顺序（拓扑序）来排课，每门课只需要学一次，不需要反复。DAG最短路径也是这个道理——按依赖顺序处理，一次搞定。

### 4.2 算法伪代码

```
DAG-SHORTEST-PATHS(G, w, s)
1  对 G 进行拓扑排序，得到顶点序列
2  INITIALIZE-SINGLE-SOURCE(G, s)
3  for each vertex u in 拓扑序:
4      for each vertex v in G.Adj[u]:   # 对 u 的每个邻居
5          RELAX(u, v, w)                # 松弛
```

### 4.3 图解执行过程

**示例DAG**：

```
    2        3        6
s ───→ a ───→ b ───→ c
│      │              ↑
5│     4│             2│
↓      ↓              │
d ───→ e ────────────→┘
   7
还有边: d→f(1), e→f(-2), c→f(3)
```

**拓扑排序**：s, a, d, b, e, c, f

**初始化**：`s=0, 其他=∞`

**按拓扑序处理**：

| 处理顶点 | 松弛的边 | 更新结果 |
|---------|---------|---------|
| s | s→a(2), s→d(5) | a=2, d=5 |
| a | a→b(3), a→e(4) | b=5, e=6 |
| d | d→e(7), d→f(1) | e=min(6,5+7)=6不变, f=6 |
| b | b→c(6) | c=11 |
| e | e→f(-2), e→c(2) | f=min(6,6+(-2))=4✓, c=min(11,6+2)=8✓ |
| c | c→f(3) | f=min(4,8+3)=4不变 |
| f | 无出边 | - |

**最终结果**：

| 顶点 | 最短距离 | 路径 |
|------|---------|------|
| s | 0 | - |
| a | 2 | s→a |
| b | 5 | s→a→b |
| c | 8 | s→a→e→c |
| d | 5 | s→d |
| e | 6 | s→a→e |
| f | 4 | s→a→e→f |

**注意**：关键更新发生在处理 e 时——e→f(-2) 把 f 从6降到4，e→c(2) 把 c 从11降到8。这就是"负权边"的威力，但在DAG中完全可控！

### 4.4 复杂度分析

| 复杂度 | 值 | 理由 |
|--------|-----|------|
| 时间 | **O(V+E)** | 拓扑排序 O(V+E) + 每条边松弛一次 O(E) |
| 空间 | **O(V)** | 存储距离和前驱 |

> O(V+E)！这是所有单源最短路径算法中最快的。代价是图必须是DAG。

### 4.5 代码实现

**Python实现**：

```python
from collections import deque

def dag_shortest_paths(vertices, adj, source):
    """
    DAG 最短路径算法
    :param vertices: 顶点列表
    :param adj: 邻接表，adj[u] = [(v, weight), ...]
    :param source: 源点
    :return: (dist, pred)
    """
    # 第1步：拓扑排序（Kahn算法，基于入度）
    in_degree = {v: 0 for v in vertices}
    for u in vertices:
        for v, w in adj.get(u, []):
            in_degree[v] += 1

    queue = deque([v for v in vertices if in_degree[v] == 0])
    topo_order = []
    while queue:
        u = queue.popleft()
        topo_order.append(u)
        for v, w in adj.get(u, []):
            in_degree[v] -= 1
            if in_degree[v] == 0:
                queue.append(v)

    # 第2步：初始化
    dist = {v: float('inf') for v in vertices}
    pred = {v: None for v in vertices}
    dist[source] = 0

    # 第3步：按拓扑序松弛
    for u in topo_order:
        if dist[u] == float('inf'):
            continue  # 不可达的顶点，跳过
        for v, w in adj.get(u, []):
            if dist[u] + w < dist[v]:
                dist[v] = dist[u] + w
                pred[v] = u

    return dist, pred

# 使用示例
vertices = ['s', 'a', 'b', 'c', 'd', 'e', 'f']
adj = {
    's': [('a', 2), ('d', 5)],
    'a': [('b', 3), ('e', 4)],
    'd': [('e', 7), ('f', 1)],
    'b': [('c', 6)],
    'e': [('f', -2), ('c', 2)],
    'c': [('f', 3)],
}
dist, pred = dag_shortest_paths(vertices, adj, 's')
for v in vertices:
    print(f"  s→{v}: 距离={dist[v]}, 前驱={pred[v]}")
```

**C++实现**：

```cpp
#include <iostream>
#include <vector>
#include <queue>
#include <limits>
using namespace std;

// DAG最短路径算法
void dagShortestPaths(int V, const vector<vector<pair<int,double>>>& adj,
                      int source, vector<double>& dist, vector<int>& pred) {
    // 第1步：拓扑排序（Kahn算法）
    vector<int> inDegree(V, 0);
    for (int u = 0; u < V; u++) {
        for (auto& [v, w] : adj[u]) {
            inDegree[v]++;
        }
    }

    queue<int> q;
    for (int u = 0; u < V; u++) {
        if (inDegree[u] == 0) q.push(u);
    }

    vector<int> topoOrder;
    while (!q.empty()) {
        int u = q.front(); q.pop();
        topoOrder.push_back(u);
        for (auto& [v, w] : adj[u]) {
            if (--inDegree[v] == 0) q.push(v);
        }
    }

    // 第2步：初始化
    dist.assign(V, numeric_limits<double>::infinity());
    pred.assign(V, -1);
    dist[source] = 0;

    // 第3步：按拓扑序松弛
    for (int u : topoOrder) {
        if (dist[u] == numeric_limits<double>::infinity()) continue;  // 不可达
        for (auto& [v, w] : adj[u]) {
            if (dist[u] + w < dist[v]) {
                dist[v] = dist[u] + w;
                pred[v] = u;
            }
        }
    }
}

int main() {
    // 顶点: 0=s, 1=a, 2=b, 3=c, 4=d, 5=e, 6=f
    int V = 7;
    vector<vector<pair<int,double>>> adj(V);
    adj[0] = {{1,2}, {4,5}};    // s→a, s→d
    adj[1] = {{2,3}, {5,4}};    // a→b, a→e
    adj[4] = {{5,7}, {6,1}};    // d→e, d→f
    adj[2] = {{3,6}};           // b→c
    adj[5] = {{6,-2}, {3,2}};   // e→f, e→c
    adj[3] = {{6,3}};           // c→f

    vector<double> dist;
    vector<int> pred;
    dagShortestPaths(V, adj, 0, dist, pred);

    for (int i = 0; i < V; i++) {
        cout << "  s→" << i << ": 距离=" << dist[i]
             << ", 前驱=" << pred[i] << endl;
    }
    return 0;
}
```

### FAQ：DAG最短路径能处理负权边吗？

**答**：能！DAG没有环，不可能有负权环，所以负权边完全OK。这是DAG最短路径算法的一大优势——既快又允许负权。

### FAQ：DAG最长路径呢？

**答**：把所有边权重取反，跑DAG最短路径，再把结果取反即可。注意：一般图的最长路径是NP-hard问题，但DAG的多项式可解！

---

## 5️⃣ Dijkstra算法——非负权图的贪心利器（24.3 Dijkstra's Algorithm）

### 5.1 算法思想

**一句话**：每次选当前距离最小的顶点，把它"确定"下来，然后松弛它的所有出边。

**生活类比**：想象你在玩"探索地图"游戏——
- 你站在起点，周围有若干条路
- 每次选择**当前已知最近的未探索地点**
- 到达该地点后，刷新从该地点出发的路线信息
- 重复直到所有地点都探索完毕

**与Bellman-Ford的区别**：

| 对比 | Bellman-Ford | Dijkstra |
|------|-------------|----------|
| 松弛策略 | 暴力：所有边松弛\|V\|-1轮 | 贪心：只松弛已确定顶点的出边 |
| 负权边 | ✅ 允许 | ❌ 不允许 |
| 时间复杂度 | O(VE) | O((V+E)lgV) 二叉堆 / O(V²+ E) 数组 |
| 核心思想 | 全局反复松弛 | 局部贪心选择 |

### 5.2 算法伪代码

```
DIJKSTRA(G, w, s)
1  INITIALIZE-SINGLE-SOURCE(G, s)
2  S = ∅                          # 已确定最短路径的顶点集
3  Q = G.V                        # 优先队列，按 d 值排序
4  while Q ≠ ∅:
5      u = EXTRACT-MIN(Q)         # 取出距离最小的顶点
6      S = S ∪ {u}                # u 的最短路径已确定
7      for each vertex v in G.Adj[u]:
8          RELAX(u, v, w)          # 松弛 u 的所有出边
```

### 5.3 为什么Dijkstra要求非负权？

**关键**：Dijkstra的贪心选择依赖于一个性质——**一旦顶点u被取出（d值最小），u.d就是最短距离，不会再变**。

**如果允许负权边，这个性质被打破**：

```
    1        -5
s ────→ a ────→ b
 └──────────────┘
        4

Dijkstra执行：
1. 取出 s (d=0)，松弛 s→a, s→b → a.d=1, b.d=4
2. 取出 a (d=1)，松弛 a→b → b.d = 1+(-5) = -4 ← 更新了b！
   但 b 已经被"确定"了（d=4时应该被取出）

问题：a→b(-5) 让已经"确定"的b变得更短！
→ Dijkstra的贪心假设失效
```

### 5.4 图解执行过程

**示例图**（所有边权非负）：

```
       10        1
  s ──────→ a ──────→ d
  │         │         ↑
  5│       2│        3│
  ↓         ↓         │
  b ──────→ c ────────┘
      3         9
  还有边: b→d(2)
```

**完整邻接**：
- s→a(10), s→b(5)
- a→c(2), a→d(1)
- b→c(3), b→d(2)
- c→d(9)

**初始化**：`s=0, a=∞, b=∞, c=∞, d=∞, Q={s,a,b,c,d}`

**第1步**：取出 s (d=0)
```
松弛 s→a(10): a.d = 0+10 = 10  ✓
松弛 s→b(5):  b.d = 0+5 = 5   ✓
Q={b(5), a(10), c(∞), d(∞)}
```

**第2步**：取出 b (d=5)
```
松弛 b→c(3): c.d = 5+3 = 8    ✓
松弛 b→d(2): d.d = 5+2 = 7    ✓
Q={c(8), a(10), d(7)} → 排序后 Q={d(7), c(8), a(10)}
```

**第3步**：取出 d (d=7) ← 注意：d比a和c先被取出！
```
d 无出边
Q={c(8), a(10)}
```

**第4步**：取出 c (d=8)
```
松弛 c→d(9): d.d = 8+9=17 > 7，不更新
Q={a(10)}
```

**第5步**：取出 a (d=10)
```
松弛 a→c(2): c.d = 10+2=12 > 8，不更新
松弛 a→d(1): d.d = 10+1=11 > 7，不更新
Q=∅
```

**最终结果**：

| 顶点 | 最短距离 | 路径 |
|------|---------|------|
| s | 0 | - |
| a | 10 | s→a |
| b | 5 | s→b |
| c | 8 | s→b→c |
| d | 7 | s→b→d |

**关键观察**：d(d=7) 比通过 a→d 的路径(11) 更短——s→b→d 更优！这就是为什么Dijkstra每次选最小的：**已经被"确定"的顶点，其最短距离永远不会被推翻**（前提是非负权）。

### 5.5 复杂度分析

| 数据结构 | EXTRACT-MIN | DECREASE-KEY | 总时间复杂度 |
|---------|------------|-------------|-------------|
| 数组 | O(V) | O(1) | O(V²) |
| 二叉堆 | O(lgV) | O(lgV) | O((V+E)lgV) |
| 斐波那契堆 | O(lgV) 摊销 | O(1) 摊销 | O(VlgV + E) |

> 对于稀疏图 (E << V²)，用斐波那契堆的 O(VlgV + E) 最优；对于稠密图 (E ≈ V²)，用数组的 O(V²) 更简单。

### 5.6 代码实现

**Python实现（使用heapq优先队列）**：

```python
import heapq

def dijkstra(vertices, adj, source):
    """
    Dijkstra 算法（使用最小堆）
    :param vertices: 顶点列表
    :param adj: 邻接表，adj[u] = [(v, weight), ...]
    :param source: 源点
    :return: (dist, pred)
    """
    # 初始化
    dist = {v: float('inf') for v in vertices}
    pred = {v: None for v in vertices}
    dist[source] = 0

    # 优先队列：(距离, 顶点)
    pq = [(0, source)]
    visited = set()  # 已确定最短路径的顶点

    while pq:
        d_u, u = heapq.heappop(pq)     # 取出距离最小的顶点

        if u in visited:
            continue                    # 已处理过，跳过（惰性删除）
        visited.add(u)                  # 标记为已确定

        # 松弛 u 的所有出边
        for v, w in adj.get(u, []):
            if u in visited and v in visited:
                continue                # 两个都已确定，不需要松弛
            new_dist = dist[u] + w
            if new_dist < dist[v]:
                dist[v] = new_dist      # 更新距离
                pred[v] = u             # 更新前驱
                heapq.heappush(pq, (new_dist, v))  # 加入队列

    return dist, pred

# 使用示例
vertices = ['s', 'a', 'b', 'c', 'd']
adj = {
    's': [('a', 10), ('b', 5)],
    'a': [('c', 2), ('d', 1)],
    'b': [('c', 3), ('d', 2)],
    'c': [('d', 9)],
}
dist, pred = dijkstra(vertices, adj, 's')
for v in vertices:
    print(f"  s→{v}: 距离={dist[v]}, 前驱={pred[v]}")
```

**C++实现（使用priority_queue）**：

```cpp
#include <iostream>
#include <vector>
#include <queue>
#include <limits>
using namespace std;

typedef pair<double, int> pdi;  // (距离, 顶点编号)

void dijkstra(int V, const vector<vector<pair<int,double>>>& adj,
              int source, vector<double>& dist, vector<int>& pred) {
    // 初始化
    dist.assign(V, numeric_limits<double>::infinity());
    pred.assign(V, -1);
    dist[source] = 0;

    // 优先队列（小顶堆），存储 (距离, 顶点)
    priority_queue<pdi, vector<pdi>, greater<pdi>> pq;
    pq.push({0, source});

    vector<bool> visited(V, false);  // 是否已确定

    while (!pq.empty()) {
        auto [d_u, u] = pq.top();    // 取出距离最小的
        pq.pop();

        if (visited[u]) continue;    // 已处理，跳过
        visited[u] = true;           // 标记为已确定

        // 松弛 u 的所有出边
        for (auto& [v, w] : adj[u]) {
            double new_dist = dist[u] + w;
            if (new_dist < dist[v]) {
                dist[v] = new_dist;  // 更新距离
                pred[v] = u;         // 更新前驱
                pq.push({new_dist, v});  // 加入队列
            }
        }
    }
}

int main() {
    // 顶点: 0=s, 1=a, 2=b, 3=c, 4=d
    int V = 5;
    vector<vector<pair<int,double>>> adj(V);
    adj[0] = {{1,10}, {2,5}};    // s→a, s→b
    adj[1] = {{3,2}, {4,1}};     // a→c, a→d
    adj[2] = {{3,3}, {4,2}};     // b→c, b→d
    adj[3] = {{4,9}};            // c→d

    vector<double> dist;
    vector<int> pred;
    dijkstra(V, adj, 0, dist, pred);

    for (int i = 0; i < V; i++) {
        cout << "  s→" << i << ": 距离=" << dist[i]
             << ", 前驱=" << pred[i] << endl;
    }
    return 0;
}
```

### FAQ：Dijkstra中为什么用"惰性删除"？

**答**：Python的heapq不支持直接修改堆中元素的优先级。当顶点v的距离被更新时，我们往堆里插入一个新的(更小距离, v)，旧的条目还在堆里。取出时如果发现v已经visited，就跳过。这种方式叫"惰性删除"——不主动删除旧的，而是取出时再判断。

### FAQ：Dijkstra vs BFS？

**答**：BFS是Dijkstra在"所有边权相等"时的特例！如果所有边权都是1，Dijkstra退化为BFS——每层扩展一步，第一次到达就是最短的。Dijkstra可以理解为"带权BFS"。

---

## 6️⃣ 差分约束与最短路径（24.4 Difference Constraints and Shortest Paths）

### 6.1 问题引入

**生活类比**：项目管理中的时间约束——

- 任务A必须在任务B之前完成
- 任务B的完成时间不能早于任务A完成时间+3天
- 你想知道：每个任务最早什么时候能完成？

这就是**差分约束系统**：一组形如 xⱼ - xᵢ ≤ bₖ 的不等式。

### 6.2 差分约束系统的定义

**差分约束系统**：一组 m 个不等式，n 个变量：

```
x₁ - x₂ ≤ b₁
x₁ - x₃ ≤ b₂
x₂ - x₄ ≤ b₃
...
```

每个不等式都是"两个变量之差 ≤ 常数"的形式。

### 6.3 从差分约束到最短路径

**关键洞察**：差分约束 xⱼ - xᵢ ≤ bₖ 等价于最短路径中的松弛条件！

**映射关系**：

| 差分约束 | 最短路径 |
|---------|---------|
| 变量 xᵢ | 顶点 i 的距离 d[i] |
| 约束 xⱼ - xᵢ ≤ bₖ | 边 i → j，权重 bₖ |
| xⱼ ≤ xᵢ + bₖ | 松弛：d[j] ≤ d[i] + w(i,j) |

**图解对应**：

```
差分约束:              约束图:
x₁ - x₂ ≤ 3    →    2 ──3──→ 1
x₃ - x₁ ≤ 5    →    1 ──5──→ 3
x₃ - x₂ ≤ 7    →    2 ──7──→ 3
```

**定理**：如果约束图没有负权环，则差分约束系统有解，且最短路径距离就是一个可行解。

**为什么？**
- 最短路径保证 d[v] ≤ d[u] + w(u,v)（三角不等式）
- 这正是 xⱼ - xᵢ ≤ bₖ 的形式
- 所以最短路径的 d 值满足所有差分约束！

### 6.4 添加超级源点

为了用Bellman-Ford求解，我们需要一个源点。添加一个**超级源点** v₀，到所有其他顶点连一条权重为0的边：

```
v₀ ──0──→ x₁
v₀ ──0──→ x₂
v₀ ──0──→ x₃
...
```

这样从 v₀ 出发跑 Bellman-Ford 就能得到所有变量的值。

**为什么权重为0？** 因为我们只需要找到一组满足约束的解，不关心具体值——设源点到所有变量距离0，意味着"初始值可以相等"，然后约束会自然拉开差距。

### 6.5 完整求解流程

```
1. 将差分约束转化为约束图
2. 添加超级源点 v₀，连0权边到所有顶点
3. 从 v₀ 运行 Bellman-Ford
4. 如果有负权环 → 约束系统无解
5. 如果无负权环 → d[v] 就是变量 xᵢ 的一个可行解
```

### 6.6 实际应用

**应用1：项目调度**

```
约束：
  任务1完成时间 ≤ 任务0完成时间 + 3
  任务2完成时间 ≤ 任务0完成时间 + 5
  任务3完成时间 ≤ 任务1完成时间 + 2
  任务3完成时间 ≤ 任务2完成时间 + 1

→ 转化为约束图，跑Bellman-Ford
→ 得到每个任务最早完成时间
```

**应用2：布局约束**

```
约束（电路布局中元件间距）：
  元件B左边界 - 元件A右边界 ≥ 10  →  xA - xB ≤ -10
  元件C左边界 - 元件B右边界 ≥ 5   →  xB - xC ≤ -5
```

**应用3：时序分析**

```
约束（数字电路时序）：
  信号到达时间约束
  建立时间约束
  保持时间约束
→ 都可以表示为差分约束，用最短路径求解
```

### FAQ：如果差分约束系统无解怎么办？

**答**：无解等价于约束图有负权环。负权环意味着存在矛盾：比如 x₁ - x₂ ≤ 3, x₂ - x₃ ≤ 2, x₃ - x₁ ≤ -7，三式相加得 0 ≤ -2，矛盾！这种环的权重之和 = 3 + 2 + (-7) = -2 < 0，正是负权环。

---

## 7️⃣ 最短路径性质证明（24.5 Proofs of Shortest-Paths Properties）

> 本节是理论核心，为前面所有算法的正确性提供严格基础。如果你更关心"怎么用"，可以快速浏览；如果你需要面试/考试证明题，这里是重点。

### 7.1 三角不等式

**定理24.4（三角不等式）**：对任意边 (u, v) ∈ E，δ(s, v) ≤ δ(s, u) + w(u, v)。

**证明**：

```
情况1：s 到 u 不可达 → δ(s, u) = ∞ → 不等式显然成立

情况2：s 到 u 可达
  设 p 是 s→v 的最短路径
  设 p' 是 s→u 的最短路径，再接上边 (u,v)
  则 w(p') + w(u,v) 是 s→v 的某条路径的权重
  而 δ(s, v) 是所有 s→v 路径中的最小权重
  ∴ δ(s, v) ≤ δ(s, u) + w(u, v)  ∎
```

> 类比：从南京直接到上海，不可能比"南京→苏州→上海"更远——中间加一站只会更远或相等。

### 7.2 上界性质

**定理24.5（上界性质）**：在算法执行中，对所有 v ∈ V，v.d ≥ δ(s, v)，且一旦 v.d = δ(s, v)，之后不再改变。

**证明**：

```
归纳法：
  基础：初始化后，s.d = 0 = δ(s,s)，其他 v.d = ∞ ≥ δ(s,v) ✓

  归纳：假设松弛前 v.d ≥ δ(s,v)，考虑松弛 (u,v)
    新值 v.d' = min(v.d, u.d + w(u,v))
    由归纳假设 u.d ≥ δ(s,u)
    由三角不等式 δ(s,v) ≤ δ(s,u) + w(u,v)
    ∴ u.d + w(u,v) ≥ δ(s,u) + w(u,v) ≥ δ(s,v)
    ∴ v.d' = min(v.d, u.d + w(u,v)) ≥ δ(s,v) ✓

  一旦 v.d = δ(s,v)，此后松弛只能让 v.d 更小，
  但 v.d ≥ δ(s,v)（上界性质），所以不会再变。∎
```

### 7.3 收敛性质

**定理24.6（收敛性质）**：如果 s→...→u→v 是最短路径，且松弛 (u,v) 前 u.d = δ(s,u)，则松弛后 v.d = δ(s,v)。

**证明**：

```
松弛 (u,v) 时：
  v.d = min(v.d, u.d + w(u,v))
       = min(v.d, δ(s,u) + w(u,v))        （已知 u.d = δ(s,u)）
       ≤ δ(s,u) + w(u,v)
       = δ(s,v)                            （s→...→u→v 是最短路径）

又由上界性质 v.d ≥ δ(s,v)
∴ v.d = δ(s,v)  ∎
```

> 这是"正确性沿路径传播"的严格表述——前驱正确了，松弛就让后继也正确。

### 7.4 路径松弛性质

**定理24.7（路径松弛性质）**：如果 p = v₀→v₁→...→vₖ 是 s 到 vₖ 的最短路径，且边被按顺序松弛，则 vₖ.d = δ(s, vₖ)。

**证明思路**：

```
归纳法：
  v₀ = s, s.d = 0 = δ(s,s) ✓
  假设 vᵢ.d = δ(s,vᵢ)
  松弛 (vᵢ, vᵢ₊₁) 时，由收敛性质 vᵢ₊₁.d = δ(s,vᵢ₊₁) ✓
  一直传播到 vₖ ∎
```

### 7.5 无路径性质

**定理24.8**：如果从 s 到 v 不存在路径，则始终 v.d = δ(s,v) = ∞。

**证明**：初始化后 v.d = ∞。松弛 (u,v) 只有当 u.d < ∞ 时才可能改变 v.d。但如果 s 到 v 不可达，s 到 u 也必然不可达（否则 u→v 就构成了一条路径），所以 u.d = ∞，v.d 不会被改变。∎

---

## 8️⃣ 三大算法对比与选择指南

### 8.1 全面对比

| 特性 | Bellman-Ford | DAG最短路径 | Dijkstra |
|------|-------------|------------|----------|
| **负权边** | ✅ 允许 | ✅ 允许 | ❌ 不允许 |
| **负权环检测** | ✅ 能检测 | ✅ 自动（DAG无环） | ❌ 不能 |
| **时间复杂度** | O(VE) | O(V+E) | O((V+E)lgV) 堆 |
| **空间复杂度** | O(V) | O(V) | O(V) |
| **图类型要求** | 任意有向图 | DAG | 非负权有向图 |
| **核心策略** | 全局暴力松弛 | 拓扑序顺序松弛 | 贪心选最小 |
| **适用场景** | 有负权边/需检测负权环 | 有向无环图 | 导航/网络路由 |

### 8.2 选择决策树

```
你的图是什么类型？
├── 有负权环（从源可达）？→ 最短路径不存在！无法求解
├── DAG？→ 用 DAG最短路径 O(V+E) 最快
├── 有负权边（但无负权环）？→ 用 Bellman-Ford O(VE)
├── 所有边权非负？
│   ├── 稠密图 (E≈V²)？→ Dijkstra + 数组 O(V²)
│   └── 稀疏图 (E<<V²)？→ Dijkstra + 堆 O((V+E)lgV)
└── 不确定有没有负权环？→ 先跑 Bellman-Ford 检测
```

### 8.3 实际工程中的选择

| 应用场景 | 推荐算法 | 原因 |
|---------|---------|------|
| GPS导航 | Dijkstra/A* | 边权（距离/时间）非负 |
| 网络路由（RIP协议） | Bellman-Ford变体 | 需要处理可能的路由策略 |
| 网络路由（OSPF协议） | Dijkstra | 链路代价非负 |
| 项目调度 | DAG最短路径 | 任务依赖天然DAG |
| 货币套利检测 | Bellman-Ford | 汇率转换产生负权边 |
| 游戏AI寻路 | Dijkstra/A* | 网格代价非负 |

---

## 9️⃣ 本章小结

### 核心要点

1. **松弛是灵魂**：所有最短路径算法的核心都是"松弛"——发现更短的路就更新
2. **Bellman-Ford最通用**：允许负权边、能检测负权环，但 O(VE) 最慢
3. **DAG最特殊**：O(V+E) 最快，允许负权边，代价是图必须是DAG
4. **Dijkstra最常用**：非负权图的标准选择，贪心策略效率高
5. **差分约束是桥梁**：把约束优化问题转化为最短路径问题

### 易错点

| 易错点 | 正确理解 |
|--------|---------|
| ❌ Dijkstra能处理负权边 | ✅ Dijkstra**不能**处理负权边，否则贪心选择失效 |
| ❌ 负权边=负权环 | ✅ 负权边可以无害存在；负权环才导致最短路径不存在 |
| ❌ Bellman-Ford总是O(VE) | ✅ 加入提前终止优化，实践中往往远小于O(VE) |
| ❌ 最短路径唯一 | ✅ 可能有多条等权最短路径，对应的树也不唯一 |
| ❌ DAG最长路径是NP-hard | ✅ DAG的最长路径多项式可解（取反边权跑最短路） |

### 与前后章节的联系

```
第22章 BFS/DFS → 第24章 最短路径 → 第25章 全源最短路径
  图的遍历         单源最短路径       所有顶点对之间的最短路径

第6章 堆 → 第24章 Dijkstra的优先队列
  数据结构基础       算法应用

第16章 贪心 → 第24章 Dijkstra的贪心策略
  贪心思想           具体实例

第24章 差分约束 → 线性规划
  最短路径的对偶      更一般的优化框架
```

---

## 🔟 课后练习

### 思考题

1. **如果图中所有边权重相等，Dijkstra退化为哪种算法？为什么？**
   <details>
   <summary>点击查看提示</summary>
   退化为BFS。因为每次取出距离最小的顶点，等权时就是层序遍历。
   </details>

2. **Bellman-Ford为什么需要 |V|-1 轮？|V|-2 轮够不够？**
   <details>
   <summary>点击查看提示</summary>
   不够。最短路径最多经过 |V|-1 条边（最坏情况是一条链），每轮松弛让正确性传播一条边，所以需要 |V|-1 轮。|V|-2 轮只能保证最多 |V|-2 条边的路径正确。
   </details>

3. **如何用Bellman-Ford检测所有负权环（不只从源点可达的）？**
   <details>
   <summary>点击查看提示</summary>
   添加一个超级源点，到所有顶点连0权边，从超级源点跑Bellman-Ford。这样所有负权环都从超级源点可达。
   </details>

4. **DAG最短路径中，如果源点不是拓扑排序的第一个顶点，会怎样？**
   <details>
   <summary>点击查看提示</summary>
   在源点之前的顶点不会被更新（距离保持∞），因为它们不在源点的下游。算法仍然正确——不可达的顶点距离就是∞。
   </details>

5. **差分约束系统中，为什么添加超级源点后权重为0？换成其他值可以吗？**
   <details>
   <summary>点击查看提示</summary>
   权重为0意味着"初始时所有变量可以相等"。换成其他值相当于给所有变量加上一个常数偏移，不影响差分约束的可行性（因为不等式两边都有这个偏移，会抵消）。但0是最简洁的选择。
   </details>

### 编程题

1. **实现Bellman-Ford并测试负权环检测**：构造一个包含负权环的图，验证算法能正确检测
2. **实现Dijkstra并比较优先队列效率**：用二叉堆和数组两种方式实现，对比稀疏图和稠密图的性能
3. **差分约束求解器**：给定一组差分约束，转化为约束图并用Bellman-Ford求解
4. **SPFA实现**：在Bellman-Ford基础上实现SPFA（队列优化版），对比性能

---

*第24章完 | 下一章预告：第25章 全源最短路径（Floyd-Warshall & Johnson算法）*
